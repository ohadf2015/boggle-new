---
phase: 50-psychological-hooks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fe-next/components/blast/utils/blastChainCounter.ts
  - fe-next/components/blast/utils/__tests__/blastChainCounter.test.ts
  - fe-next/components/blast/BlastChainCounter.tsx
  - fe-next/components/blast/__tests__/BlastChainCounter.test.tsx
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/BlastGameLayout.tsx
autonomous: true
requirements: [PSYC-01]

must_haves:
  truths:
    - "Cascades display an escalating CHAIN x2, CHAIN x3... counter"
    - "Counter color progresses from white to yellow to gold to rainbow"
    - "Counter appears during cascade chain and disappears after chain ends"
    - "Reduced motion users see counter text without animation"
  artifacts:
    - path: "fe-next/components/blast/utils/blastChainCounter.ts"
      provides: "Pure logic: chain level to color mapping, label formatting"
      exports: ["getChainColor", "getChainLabel", "CHAIN_COLOR_PROGRESSION"]
    - path: "fe-next/components/blast/BlastChainCounter.tsx"
      provides: "Visual counter overlay component"
      exports: ["BlastChainCounter"]
  key_links:
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "BlastChainCounter"
      via: "cascadeChainLevel state exposed to BlastGameLayout"
      pattern: "cascadeChainLevel"
    - from: "fe-next/components/blast/BlastGameLayout.tsx"
      to: "BlastChainCounter"
      via: "renders BlastChainCounter with chainLevel prop"
      pattern: "BlastChainCounter.*chainLevel"
---

<objective>
Implement cascade chain counter with color-escalating visuals (PSYC-01).

Purpose: Give players visible feedback during cascade chains, creating excitement as chains build higher. The escalating color progression (white -> yellow -> gold -> rainbow) makes long chains feel increasingly rewarding.

Output: Pure utility for chain-to-color mapping + React overlay component + wiring into BlastGameLayout.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@fe-next/components/blast/types.ts
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/BlastGameLayout.tsx
</context>

<interfaces>
<!-- Existing cascade chain infrastructure in useBlastGame.ts -->
From fe-next/components/blast/types.ts:
```typescript
export const MAX_CASCADE_CHAIN = 2;
export const CASCADE_CHAIN_BONUS_MULTIPLIER = 0.5;
export const CASCADE_HIGHLIGHT_DURATION = 800;
export type CascadeHighlightPhase = 'idle' | 'highlighting';
```

From fe-next/components/blast/hooks/useBlastGame.ts:
```typescript
// Already tracks chain level internally:
cascadeChainLevel: number;  // in return value
cascadeChainLevelRef.current  // incremented on each cascade chain
// Reset to 0 after cascade sequence completes
```
</interfaces>

<feature>
  <name>Cascade Chain Counter</name>
  <files>
    fe-next/components/blast/utils/blastChainCounter.ts,
    fe-next/components/blast/utils/__tests__/blastChainCounter.test.ts,
    fe-next/components/blast/BlastChainCounter.tsx,
    fe-next/components/blast/__tests__/BlastChainCounter.test.tsx,
    fe-next/components/blast/hooks/useBlastGame.ts,
    fe-next/components/blast/BlastGameLayout.tsx
  </files>
  <behavior>
    Pure logic (blastChainCounter.ts):
    - getChainColor(1) returns "#FFFFFF" (white)
    - getChainColor(2) returns "#FFD700" (gold)
    - getChainColor(3) returns "#FF6B35" (orange)
    - getChainColor(4+) returns "rainbow" gradient string
    - getChainLabel(2) returns "CHAIN x2"
    - getChainLabel(0) returns null (no display)
    - CHAIN_COLOR_PROGRESSION is readonly array of hex colors

    Component (BlastChainCounter.tsx):
    - Renders nothing when chainLevel <= 0
    - Renders "CHAIN x{N}" text with correct color when chainLevel >= 1
    - Uses AdaptiveMotion for entrance/exit animations
    - data-testid="blast-chain-counter"
    - Applies scale animation on level change (pulse effect)
    - Reduced motion: no animation, still shows text + color

    Wiring:
    - useBlastGame already exposes cascadeChainLevel — ensure it's in the return object
    - BlastGameLayout renders BlastChainCounter positioned above the grid, passing chainLevel
    - Chain counter resets when cascadeHighlightPhase returns to 'idle' after all cascades
  </behavior>
  <implementation>
    1. Create blastChainCounter.ts with pure color/label functions
    2. Create BlastChainCounter.tsx using AdaptiveMotion with scale+opacity entrance
    3. Wire into BlastGameLayout — render BlastChainCounter above grid area, pass blast.cascadeChainLevel
    4. Raise MAX_CASCADE_CHAIN from 2 to 5 (or higher) to allow more visible chain building
    5. Add translation key blast.chainCounter to all 4 languages: "CHAIN x{{count}}"
  </implementation>
</feature>

<verification>
cd fe-next && npx jest --testPathPattern="blastChainCounter" --no-coverage && npx jest --testPathPattern="BlastChainCounter" --no-coverage
</verification>

<success_criteria>
- blastChainCounter.ts passes all color/label tests
- BlastChainCounter.tsx renders with correct testid, color, and text per chain level
- BlastGameLayout renders chain counter during active cascades
- Counter hidden when no cascade is active
</success_criteria>

<output>
After completion, create `.planning/phases/50-psychological-hooks/50-01-SUMMARY.md`
</output>
