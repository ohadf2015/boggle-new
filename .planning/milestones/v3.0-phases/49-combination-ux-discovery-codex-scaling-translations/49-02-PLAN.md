---
phase: 49-combination-ux-discovery-codex-scaling-translations
plan: 02
type: execute
wave: 2
depends_on: [49-01, 49-03]
files_modified:
  - fe-next/components/blast/BlastCodexModal.tsx
  - fe-next/components/blast/__tests__/BlastCodexModal.test.tsx
  - fe-next/components/blast/BlastReadyScreen.tsx
autonomous: true
requirements: [COMB-05]

must_haves:
  truths:
    - "The Combo Codex screen is accessible from the Blast mode ready screen"
    - "Codex shows discovered count vs total (e.g. 12/31 combos discovered)"
    - "Discovered combos show their translated name; undiscovered show ???"
    - "Codex displays all 31 combo pairs in a scrollable grid"
  artifacts:
    - path: "fe-next/components/blast/BlastCodexModal.tsx"
      provides: "Combo Codex overlay showing discovery progress"
      exports: ["BlastCodexModal"]
    - path: "fe-next/components/blast/BlastReadyScreen.tsx"
      provides: "Codex button added to ready screen"
      contains: "BlastCodexModal"
  key_links:
    - from: "BlastReadyScreen.tsx"
      to: "BlastCodexModal.tsx"
      via: "isCodexOpen state + button"
      pattern: "isCodexOpen|BlastCodexModal"
    - from: "BlastCodexModal.tsx"
      to: "blastComboScaling.ts"
      via: "CODEX_COMBOS import"
      pattern: "CODEX_COMBOS"
    - from: "BlastCodexModal.tsx"
      to: "useBlastComboDiscovery"
      via: "discoveredCombos prop"
      pattern: "discoveredCombos.*Set.*BlastComboType"
---

<objective>
Build the Combo Codex modal and wire a "Codex" button into BlastReadyScreen. The Codex shows all 31 combo pairs with discovered/undiscovered states and a progress counter.

Purpose: COMB-05 requires a collectible screen where players browse their combo discovery progress. This drives replayability by making the 31-pair matrix visible.

Output: BlastCodexModal component + Codex button on BlastReadyScreen, both with tests.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-combination-ux-discovery-codex-scaling-translations/49-RESEARCH.md
@.planning/phases/49-combination-ux-discovery-codex-scaling-translations/49-01-SUMMARY.md
@.planning/phases/49-combination-ux-discovery-codex-scaling-translations/49-03-SUMMARY.md

<interfaces>
From plan 49-01 (useBlastComboDiscovery):
```typescript
// Return type of useBlastComboDiscovery hook
{
  discoveredCombos: Set<BlastComboType>;
  pendingDiscovery: BlastComboType | null;
  onComboDetected: (combos: SpecialCombo[]) => void;
  acknowledgeDiscovery: () => void;
}
```

From plan 49-03 (blastComboScaling):
```typescript
export const CODEX_COMBOS: readonly BlastComboType[]; // 31 entries
export const CODEX_COMBO_COUNT: number; // 31
```

From fe-next/components/blast/BlastReadyScreen.tsx:
```typescript
interface BlastReadyScreenProps {
  onStart: () => void;
}
// Uses t() from useLanguage(), neo-brutalist styling
```

From fe-next/components/blast/utils/blastCombos.ts:
```typescript
export type BlastComboType = /* 33 variants */;
// SpecialCombo has label: string field = `blast.combo.${comboType}`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BlastCodexModal component</name>
  <files>
    fe-next/components/blast/BlastCodexModal.tsx,
    fe-next/components/blast/__tests__/BlastCodexModal.test.tsx
  </files>
  <behavior>
    - Test: renders nothing when isOpen is false
    - Test: when isOpen=true, renders modal with header showing t('blast.comboCodex') title
    - Test: header shows progress text using t('blast.codexProgress') with discovered count / total count
    - Test: with 0 discovered combos, all 31 combo cards show "???" placeholder text
    - Test: with 3 discovered combos, those 3 show their translated name via t(`blast.combo.${type}`), remaining 28 show "???"
    - Test: calls onClose when close button clicked
    - Test: renders with data-testid="combo-codex-modal"
  </behavior>
  <action>
    Create `BlastCodexModal` component:

    Props: `{ discoveredCombos: Set<BlastComboType>; isOpen: boolean; onClose: () => void; }`

    Structure:
    - Use `AdaptiveAnimatePresence` with a fixed/absolute inset-0 z-40 backdrop
    - Header: t('blast.comboCodex') title + t('blast.codexProgress', { discovered: discoveredCount, total: CODEX_COMBO_COUNT }) progress
    - Body: Scrollable 2-column grid of combo cards
    - Each card:
      - If discovered: bg-neo-yellow/20 border-neo-yellow, show t(`blast.combo.${comboType}`) name
      - If undiscovered: bg-gray-800/50 border-gray-600, show t('blast.codexLocked') = "???"
    - Close button (X) top-right
    - Neo-brutalist card style: border-3 border-black bg-neo-navy shadow-hard rounded-neo

    Import `CODEX_COMBOS`, `CODEX_COMBO_COUNT` from `../utils/blastComboScaling`.
    Use `useLanguage` for `t()`.
    Keep under 120 lines.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest BlastCodexModal --no-coverage</automated>
  </verify>
  <done>Codex modal renders grid of 31 combos with discovered/undiscovered states; progress counter accurate; all 7+ tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add Codex button to BlastReadyScreen and wire discoveredCombos</name>
  <files>
    fe-next/components/blast/BlastReadyScreen.tsx
  </files>
  <action>
    1. Add `discoveredCombos?: Set<BlastComboType>` to `BlastReadyScreenProps` (optional so existing callers don't break)
    2. Add `const [isCodexOpen, setIsCodexOpen] = useState(false);` state
    3. Add a "Combo Codex" button near the existing start button:
       - Icon: `BookOpen` from lucide-react (or `Library`)
       - Label: `t('blast.comboCodex')`
       - Style: secondary neo-brutalist button (border-neo, shadow-hard-sm, bg-neo-navy hover:bg-neo-navy/80)
       - onClick: `setIsCodexOpen(true)`
    4. Render `<BlastCodexModal isOpen={isCodexOpen} onClose={() => setIsCodexOpen(false)} discoveredCombos={discoveredCombos ?? new Set()} />`
    5. Import BlastCodexModal and BlastComboType

    Note: The parent (BlastView.tsx) must pass `discoveredCombos` from `useBlastComboDiscovery` to BlastReadyScreen. The executor should find where BlastReadyScreen is rendered in BlastView.tsx and pass the prop. If the hook is not yet called at that level, call `useBlastComboDiscovery()` in BlastView and pass `discoveredCombos` down.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="BlastReadyScreen|BlastCodexModal" --no-coverage</automated>
  </verify>
  <done>BlastReadyScreen has Codex button; clicking it opens BlastCodexModal; existing BlastReadyScreen tests pass</done>
</task>

</tasks>

<verification>
Run all blast tests:
```bash
cd fe-next && npx jest --testPathPattern="blast" --no-coverage
```
</verification>

<success_criteria>
- BlastCodexModal renders 31 combo cards with correct discovered/undiscovered states
- Progress counter shows accurate discovered/total
- Codex button appears on BlastReadyScreen
- All translations use t() — no hardcoded strings
- All tests green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/49-combination-ux-discovery-codex-scaling-translations/49-02-SUMMARY.md`
</output>
