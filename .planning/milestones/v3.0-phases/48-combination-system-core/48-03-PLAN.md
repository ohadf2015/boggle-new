---
phase: 48-combination-system-core
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - fe-next/components/blast/utils/blastComboEffects.ts
  - fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
autonomous: true
requirements:
  - COMB-02

must_haves:
  truths:
    - "prism+magnet fires vortex pull then full cross-clear from magnet center"
    - "prism+gem instantly completes gem + full cross-clear"
    - "prism+frozen frees all frost tiles on board (inner specials activate) + cross-clear"
    - "rainbow+mirror triples the best special's amplification (mirror doubles the rainbow's double)"
    - "rainbow+magnet doubles vortex radius"
    - "rainbow+gem applies rainbow multiplier to gem completion bonus"
    - "rainbow+frozen reveals (cracks) all frost tiles on board"
    - "mirror+magnet fires two vortex pull+explodes at both positions"
    - "mirror+gem doubles gem completion bonus + spawns 4 specials"
    - "mirror+frozen doubles frost reveal bonus, inner special fires twice"
    - "magnet+gem completes all gem-type tiles on board instantly"
    - "magnet+frozen advances all frost tiles 1 hit + vortex pull"
    - "gem+frozen completes gem + frees frost simultaneously"
  artifacts:
    - path: "fe-next/components/blast/utils/blastComboEffects.ts"
      provides: "10 new combo effect cases for vortex/frost/mirror/gem pairs"
      contains: "mirror_magnet"
    - path: "fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts"
      provides: "Tests for all 10 new combo effects"
  key_links:
    - from: "fe-next/components/blast/utils/blastComboEffects.ts"
      to: "fe-next/components/blast/types.ts"
      via: "VORTEX_PULL_RADIUS, TREASURE_GEM_COMPLETION_BONUS, FROST_HITS_REQUIRED constants"
      pattern: "VORTEX_PULL_RADIUS|TREASURE_GEM"
---

<objective>
Implement combo effects for the remaining 10 pairs involving Vortex (magnet), Frost (frozen), Mirror, and Treasure Gem (gem).

Purpose: Completes COMB-02 -- all 28 pairs now have unique effects. These pairs involve the reworked tiles from Phase 47, which have multi-hit mechanics (gem 3-hit, frost 2-hit) and unique behaviors (vortex pull, mirror doubling).
Output: 10 new cases in executeComboEffect. Combined with 48-02's 12 new + 6 existing = 28 total.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-combination-system-core/48-RESEARCH.md
@.planning/phases/48-combination-system-core/48-01-SUMMARY.md

@fe-next/components/blast/utils/blastComboEffects.ts
@fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
@fe-next/components/blast/types.ts

<interfaces>
<!-- Same ComboEffectContext/Result from 48-01 -->
<!-- Key multi-hit mechanics from Phase 47: -->

Vortex pull: axis-preference movement (larger delta axis first), swap only with cleared cells, radius 2
Frost: hitsRemaining starts at 2; first hit cracks + reveals innerType; second hit frees + activates innerType
Gem: hitsRemaining starts at 3; on completion: +25 bonus + spawn 2 random specials
Mirror: doubles partner effect; solo = 2x word score
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Prism and Rainbow cross-type combo effects (6 pairs)</name>
  <files>
    fe-next/components/blast/utils/blastComboEffects.ts,
    fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
  </files>
  <behavior>
    - Test: prism_magnet fires vortex pull (radius 2) then full cross-clear from magnet position
    - Test: prism_gem sets gem hitsRemaining to 0 + cross-clear from prism; bonusScore includes TREASURE_GEM_COMPLETION_BONUS
    - Test: prism_frozen sets ALL frost tiles on board to hitsRemaining 0 (freed) + cross-clear from prism
    - Test: rainbow_mirror increases bonusScore by 3x baseScore equivalent (mirror triples rainbow's amplification)
    - Test: rainbow_magnet returns bonusScore boost (rainbow enhances vortex -- vortex effect with doubled radius not exceeding grid)
    - Test: rainbow_gem adds RAINBOW_BOOST_MULTIPLIER * TREASURE_GEM_COMPLETION_BONUS to bonusScore
    - Test: rainbow_frozen decrements hitsRemaining by 1 on ALL frost tiles on board (mass reveal)
  </behavior>
  <action>
    Add 7 cases (prism_magnet, prism_gem, prism_frozen, rainbow_mirror, rainbow_magnet, rainbow_gem, rainbow_frozen):

    **prism_magnet (Vortex Lattice):** Execute vortex pull toward magnet position (radius 2, axis-preference movement). Then fire full cross-clear (row + column) from magnet position. Push 'combo' explosion intensity 4.

    **prism_gem (Crystal Lattice):** Set gem tile hitsRemaining=0, markCleared. Add TREASURE_GEM_COMPLETION_BONUS to bonusScore. Fire cross-clear from prism position. Push 'combo' explosion.

    **prism_frozen (Frost Lattice):** Scan entire grid: all tiles with type='frozen' and hitsRemaining>0 set to hitsRemaining=0 and markCleared (freed instantly -- inner specials activate is handled by the existing post-clear logic in useBlastGame). Fire cross-clear from prism position.

    **rainbow_mirror (Kaleidoscope):** Score-only combo. bonusScore += baseScore * 3 (mirror triples the rainbow's 2x amplification). Push 'combo' explosion at rainbow position.

    **rainbow_magnet (Whirlwind):** Execute vortex pull with effective radius 3 (VORTEX_PULL_RADIUS + 1, rainbow boost). Then explode at radius 1. Push 'combo' explosion.

    **rainbow_gem (Lucky Boost):** bonusScore += RAINBOW_BOOST_MULTIPLIER * TREASURE_GEM_COMPLETION_BONUS. Gem tile NOT auto-completed (rainbow boosts the eventual completion). Push 'combo' explosion.

    **rainbow_frozen (Frost Bloom):** Scan board: all frost tiles get hitMultiHitTile (advance 1 hit -- cracking them to reveal inner type). Push 'combo' explosion.

    TDD for each case.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest blastComboEffects --no-coverage</automated>
  </verify>
  <done>7 prism/rainbow cross-type combos produce correct effects. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Mirror, Magnet, Gem, Frozen cross-type combo effects (6 pairs)</name>
  <files>
    fe-next/components/blast/utils/blastComboEffects.ts,
    fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
  </files>
  <behavior>
    - Test: mirror_magnet fires vortex pull+explode at BOTH mirror and magnet positions
    - Test: mirror_gem doubles gem completion bonus (bonusScore += 2 * TREASURE_GEM_COMPLETION_BONUS) + spawns 4 specials indicator
    - Test: mirror_frozen advances frost tile by 2 hits (instant free if hitsRemaining was 2); inner special fires
    - Test: magnet_gem completes ALL gem-type tiles on board (hitsRemaining=0, markCleared) + bonusScore per gem
    - Test: magnet_frozen fires vortex pull + all frost tiles advance 1 hit
    - Test: gem_frozen completes gem (hitsRemaining=0) + frees frost (hitsRemaining=0); both bonuses
    - Test: no two of these 6 pairs produce identical board state changes
  </behavior>
  <action>
    Add 6 final cases:

    **mirror_magnet (Dual Vortex):** Execute vortex pull+explode centered on magnet position, then execute vortex pull+explode centered on mirror position. Two separate pull phases + two explosions. Push two 'combo' explosions.

    **mirror_gem (Twin Gems):** Set gem hitsRemaining=0, markCleared. bonusScore += 2 * TREASURE_GEM_COMPLETION_BONUS (mirror doubles). Set result field `spawnCount: 4` (doubled from normal 2). Push 'combo' explosion.

    **mirror_frozen (Mirror Frost):** Find frost tile from combo. Set hitsRemaining to max(0, hitsRemaining - 2) (mirror doubles the hit -- instant free if at 2). If freed (hitsRemaining=0), markCleared. bonusScore += 2 * FROST_REVEAL_BONUS. Push 'combo' explosion.

    **magnet_gem (Gem Suction):** Scan entire grid: all tiles with type='gem' get hitsRemaining=0 + markCleared. bonusScore += TREASURE_GEM_COMPLETION_BONUS per gem found. Execute vortex pull toward magnet. Push 'combo' explosion.

    **magnet_frozen (Frost Vortex):** Execute vortex pull toward magnet. Scan board: all frost tiles get hitMultiHitTile. Push 'combo' explosion.

    **gem_frozen (Crystal Prison):** Set gem hitsRemaining=0, markCleared, bonusScore += TREASURE_GEM_COMPLETION_BONUS. Set frost hitsRemaining=0, markCleared, bonusScore += FROST_REVEAL_BONUS. Push 'combo' explosion.

    After implementing all 6, add a meta-test: create a 6x6 grid with each pair and verify no two produce identical isCleared patterns (satisfies "distinct outcomes" requirement).

    TDD for each case.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest blastComboEffects --no-coverage</automated>
  </verify>
  <done>All 28 combo effects implemented. No two pairs produce identical outcomes. executeComboEffect covers all BlastComboType values. File under 500 lines.</done>
</task>

</tasks>

<verification>
cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="blastComboEffects|blastCombos" --no-coverage
All 28 combo effects tested and passing.
</verification>

<success_criteria>
1. All 10 remaining pairs produce distinct effects
2. Combined with 48-02, all 28 pairs have working executeComboEffect cases
3. Multi-hit tile mechanics (gem 3-hit, frost 2-hit) honored correctly
4. Vortex pull reuses axis-preference movement pattern from Phase 47
5. blastComboEffects.ts under 500 lines (split into helper file if approaching limit)
</success_criteria>

<output>
After completion, create `.planning/phases/48-combination-system-core/48-03-SUMMARY.md`
</output>
