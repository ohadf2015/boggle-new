---
phase: 48-combination-system-core
plan: 04
type: execute
wave: 3
depends_on: ["48-02", "48-03"]
files_modified:
  - fe-next/components/blast/BlastComboFlash.tsx
  - fe-next/components/blast/__tests__/BlastComboFlash.test.tsx
  - fe-next/components/blast/BlastGame.tsx
  - fe-next/components/blast/hooks/useBlastGame.ts
autonomous: true
requirements:
  - COMB-03

must_haves:
  truths:
    - "Combo triggers produce a full-screen color flash overlay visible during gameplay"
    - "Combo explosions always use intensity 4 (vs individual tile intensity 2-3)"
    - "An audio sting callback fires when any combination is detected"
    - "Flash color varies by combo tier (cyan for moderate, orange for powerful, rainbow gradient for ultimate)"
  artifacts:
    - path: "fe-next/components/blast/BlastComboFlash.tsx"
      provides: "Full-screen flash overlay component triggered on combo detection"
      exports: ["BlastComboFlash"]
    - path: "fe-next/components/blast/__tests__/BlastComboFlash.test.tsx"
      provides: "Render tests for flash component"
  key_links:
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "fe-next/components/blast/BlastGame.tsx"
      via: "activeComboFlash state + onSynergyDetected callback"
      pattern: "activeComboFlash|onSynergyDetected"
    - from: "fe-next/components/blast/BlastGame.tsx"
      to: "fe-next/components/blast/BlastComboFlash.tsx"
      via: "renders BlastComboFlash with activeFlash prop"
      pattern: "BlastComboFlash"
---

<objective>
Add the visual and audio distinction layer for combination effects: a full-screen flash overlay, tiered color system, and audio sting callback.

Purpose: COMB-03 requires combos to be visually and audibly distinct from individual tile clears. Individual tiles use intensity 2-3; combos use intensity 4 + a screen flash that doesn't exist yet. This plan adds the "wow factor" layer.
Output: BlastComboFlash component, combo tier color mapping, audio callback wiring, activeComboFlash state in useBlastGame.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-combination-system-core/48-RESEARCH.md
@.planning/phases/48-combination-system-core/48-01-SUMMARY.md

@fe-next/components/blast/BlastGame.tsx
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/BlastExplosionLayer.tsx
@fe-next/components/blast/types.ts

<interfaces>
From fe-next/components/blast/types.ts:
```typescript
export interface BlastComboEvent {
  comboType: BlastComboType;
  tiles: Array<{ row: number; col: number }>;
  label: string;
  clearedCount: number;
}
```

From fe-next/components/blast/utils/blastCombos.ts:
```typescript
export type BlastComboType = 'bomb_bomb' | 'bomb_lightning' | ... | 'gem_frozen'
  | 'gold_special' | 'rainbow_special' | 'triple_special';
```

Combo tier classification (from research):
- Tier 1 (moderate): bomb+lightning, bomb+mirror, bomb+gem, bomb+frozen, lightning+mirror, lightning+gem, lightning+frozen, rainbow+frozen -- scoreMultiplier 3-4
- Tier 2 (powerful): bomb+prism, bomb+magnet, bomb+rainbow, lightning+rainbow, lightning+magnet, prism+mirror, prism+magnet, prism+gem, prism+frozen, rainbow+mirror, rainbow+magnet, rainbow+gem, mirror+magnet, mirror+gem, mirror+frozen, magnet+gem, magnet+frozen, gem+frozen -- scoreMultiplier 4-6
- Tier 3 (ultimate): prism+prism, prism+rainbow, lightning+prism -- scoreMultiplier 6-10
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build BlastComboFlash component with tier-based coloring</name>
  <files>
    fe-next/components/blast/BlastComboFlash.tsx,
    fe-next/components/blast/__tests__/BlastComboFlash.test.tsx
  </files>
  <behavior>
    - Test: renders nothing when activeFlash is null
    - Test: renders a div with pointer-events-none when activeFlash is provided
    - Test: calls onComplete with flash id after animation
    - Test: tier 1 combo type maps to cyan (#00FFFF) background
    - Test: tier 2 combo type maps to orange (#FF6B35) background
    - Test: tier 3 combo type maps to rainbow gradient background
    - Test: component has z-40 and absolute inset-0 positioning
    - Test: respects reduceMotion (instant opacity 0 if reduced motion preferred)
  </behavior>
  <action>
    1. Create `BlastComboFlash.tsx`:
       ```typescript
       interface BlastComboFlashProps {
         activeFlash: { id: string; comboType: BlastComboType; } | null;
         onComplete: (id: string) => void;
       }
       ```
       - Export a `getComboTier(comboType: BlastComboType): 1 | 2 | 3` function (for testing):
         - Tier 3: prism_prism, prism_rainbow, lightning_prism (scoreMultiplier >= 6)
         - Tier 2: scoreMultiplier 4-5 pairs
         - Tier 1: scoreMultiplier 3 pairs + gold_special, rainbow_special, triple_special
       - Export a `getComboFlashColor(tier: 1|2|3): string` function:
         - Tier 1: '#00FFFF' (cyan)
         - Tier 2: '#FF6B35' (orange)
         - Tier 3: 'linear-gradient(135deg, #FF1493, #FFE135, #00FFFF, #FF6B35)' (rainbow)
       - Component renders `AnimatePresence` + `motion.div` from AdaptiveMotion:
         - `className="absolute inset-0 pointer-events-none z-40"`
         - `initial={{ opacity: 0.5 }}` `animate={{ opacity: 0 }}` `transition={{ duration: 0.4 }}`
         - `onAnimationComplete` calls `onComplete(flash.id)`
         - `style={{ background: getComboFlashColor(tier) }}`
       - If `useReducedMotion()` is true, render a div that immediately calls onComplete (no animation).

    2. Keep under 100 lines. Use AdaptiveMotion pattern (not raw framer-motion).

    3. Write tests FIRST using `@testing-library/react`. Mock framer-motion's AnimatePresence to render children immediately. Assert background color, z-index, pointer-events.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest BlastComboFlash --no-coverage</automated>
  </verify>
  <done>BlastComboFlash renders tier-colored flash overlay, auto-dismisses after animation, respects reduced motion. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire combo flash state + audio callback into useBlastGame and BlastGame</name>
  <files>
    fe-next/components/blast/hooks/useBlastGame.ts,
    fe-next/components/blast/BlastGame.tsx
  </files>
  <behavior>
    - Test: when detectedCombos.length > 0, useBlastGame sets activeComboFlash with combo type
    - Test: onSynergyDetected callback is called with combo type when combo fires
    - Test: activeComboFlash resets to null after clearComboFlash called
  </behavior>
  <action>
    1. In `useBlastGame.ts`:
       - Add `activeComboFlash` state: `useState<{ id: string; comboType: BlastComboType } | null>(null)`
       - Add `onSynergyDetected` to UseBlastGameOptions (optional callback):
         `onSynergyDetected?: (comboType: BlastComboType) => void`
       - In the combo detection block (after `executeComboEffect`), when `detectedCombos.length > 0`:
         - Set `activeComboFlash` with the first (highest priority) combo type
         - Call `options.onSynergyDetected?.(detectedCombos[0].type)`
       - Export `clearComboFlash` function that sets activeComboFlash to null
       - Return `activeComboFlash` and `clearComboFlash` from the hook

    2. In `BlastGame.tsx`:
       - Import `BlastComboFlash`
       - Pass `onSynergyDetected` to useBlastGame that calls `playComboSound(3)` (max combo level) from SoundEffectsContext
       - Render `<BlastComboFlash activeFlash={activeComboFlash} onComplete={() => clearComboFlash()} />` alongside BlastExplosionLayer
       - Position it in the existing overlay stack (after explosion layer, z-40)

    3. Test the state changes in useBlastGame using the existing renderHook pattern. For BlastGame wiring, verify BlastComboFlash is rendered by checking for its test-id.

    4. Ensure all combo explosions in executeComboEffect already use intensity: 4 (verify from 48-02/48-03 -- should be the case). If any use lower, bump to 4.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="BlastComboFlash|BlastGame|blastComboEffects" --no-coverage</automated>
  </verify>
  <done>Combo flash renders in BlastGame on combo trigger. Audio callback fires. All combo explosions use intensity 4. Flash auto-dismisses. All tests green.</done>
</task>

</tasks>

<verification>
cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="blast" --no-coverage
Full blast test suite passes. Combo visual+audio layer integrated.
</verification>

<success_criteria>
1. BlastComboFlash renders full-screen color overlay on combo trigger
2. Flash color matches combo tier (cyan/orange/rainbow)
3. Audio sting callback fires for every synergy detection
4. All combo explosions use intensity 4 (vs individual tile 2-3)
5. Flash auto-dismisses after 400ms animation
6. Reduced motion users get instant dismissal (no animation)
</success_criteria>

<output>
After completion, create `.planning/phases/48-combination-system-core/48-04-SUMMARY.md`
</output>
