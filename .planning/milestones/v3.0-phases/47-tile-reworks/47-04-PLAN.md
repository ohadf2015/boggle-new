---
phase: 47-tile-reworks
plan: 04
type: tdd
wave: 4
depends_on: ["47-03"]
files_modified:
  - fe-next/shared/types/blast.ts  # extends innerType added in 47-03; adds mirror/silver/diamond to union
  - fe-next/shared/types/__tests__/blast.test.ts
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/__tests__/useBlastGame.mirrorGoldTier.test.ts
  - fe-next/components/blast/types.ts
  - fe-next/components/blast/utils/blastTileUtils.ts
  - fe-next/components/blast/utils/blastLetterGenerator.ts
autonomous: true

must_haves:
  truths:
    - "Mirror tile doubles the partnered special's effect in a word"
    - "Mirror with no other special mirrors word score (2x)"
    - "Wildcard never appears on the board (removed from type union and distribution)"
    - "Silver tiles apply 1.5x multiplier, Gold 3x, Diamond 5x (wave 4+ only)"
  artifacts:
    - path: "fe-next/components/blast/__tests__/useBlastGame.mirrorGoldTier.test.ts"
      provides: "TDD tests for Mirror, Wildcard removal, and Gold tier system"
      min_lines: 100
    - path: "fe-next/shared/types/blast.ts"
      provides: "Updated BlastTileType with mirror, silver, diamond; without wildcard"
      contains: "mirror"
    - path: "fe-next/components/blast/types.ts"
      provides: "Mirror and Gold tier constants"
      contains: "MIRROR_MULTIPLIER"
  key_links:
    - from: "fe-next/shared/types/blast.ts"
      to: "fe-next/components/blast/hooks/useBlastGame.ts"
      via: "BlastTileType union import"
      pattern: "BlastTileType"
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "fe-next/components/blast/types.ts"
      via: "import MIRROR and GOLD_TIER constants"
      pattern: "MIRROR_MULTIPLIER|SILVER_MULTIPLIER|DIAMOND_MULTIPLIER"
---

<objective>
Add Mirror tile (doubles partner's effect), remove Wildcard from the game, and implement Silver/Gold/Diamond tier system replacing single gold type.

Purpose: Mirror creates "what if I combine X with Y?" moments. Wildcard had no mechanic. Gold tiers add progression depth (Diamond only in wave 4+).
Output: Working Mirror tile, Wildcard fully removed, Gold tier multipliers active.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tile-reworks/47-01-SUMMARY.md
@.planning/phases/47-tile-reworks/47-02-SUMMARY.md
@.planning/phases/47-tile-reworks/47-03-SUMMARY.md
@fe-next/shared/types/blast.ts
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/types.ts
@fe-next/components/blast/utils/blastLetterGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BlastTileType union, add constants, write failing tests</name>
  <files>fe-next/shared/types/blast.ts, fe-next/shared/types/__tests__/blast.test.ts, fe-next/components/blast/types.ts, fe-next/components/blast/__tests__/useBlastGame.mirrorGoldTier.test.ts</files>
  <action>
**Type union changes in `shared/types/blast.ts`:**

Update `BlastTileType` union:
- REMOVE: `'wildcard'`
- ADD: `'mirror'`, `'silver'`, `'diamond'`
- Keep all others unchanged

Update `BLAST_TILE_TYPE_LIST` array to match (remove wildcard, add mirror/silver/diamond).

Update `blast.test.ts`:
- Update expected count (11 → 13: -1 wildcard + 3 new)
- Assert 'wildcard' is NOT in list
- Assert 'mirror', 'silver', 'diamond' ARE in list

**Constants in `components/blast/types.ts`:**
- `MIRROR_MULTIPLIER = 2` (doubles partner effect)
- `SILVER_MULTIPLIER = 1.5`
- `GOLD_MULTIPLIER = 3` (already exists, keep)
- `DIAMOND_MULTIPLIER = 5`
- Remove wildcard from `SPECIAL_TILE_DISTRIBUTION` record. Add mirror: 0, silver: 0, diamond: 0 (zero-weighted initially; plan 47-05 sets real weights).

**Test file `useBlastGame.mirrorGoldTier.test.ts`:**

Mirror tests:
1. **Mirror + Bomb**: Word has mirror + bomb. Bomb detonates twice (mirror copies bomb effect). Verify double explosion count.
2. **Mirror + Lightning**: Lightning column-clear fires twice. Verify column cleared count.
3. **Mirror solo**: No other specials. Word score doubled (2x). Verify final score.
4. **Mirror + Rainbow Boost**: Both in same word with a bomb. Rainbow fires its copy of best special (bomb — picked by rank). Mirror fires its copy of first special (bomb — picked by position). They fire independently: bomb detonates 3 times total (original + rainbow copy + mirror copy). If best-ranked == first-in-path, that special fires 3x. Verify bomb explosion count = 3.

Wildcard removal tests:
5. **Wildcard fallback removed**: `rollSpecialFromDistribution` with distribution containing no wildcard never returns 'wildcard'. Old fallback line `return 'wildcard'` must be changed.
6. **generateTileStates never produces wildcard**: Generate 1000 tiles, assert none are 'wildcard'.

Gold tier tests:
7. **Silver multiplier**: Word with silver tile → score multiplied by 1.5x.
8. **Gold multiplier unchanged**: Word with gold tile → score multiplied by 3x (existing behavior).
9. **Diamond multiplier**: Word with diamond tile → score multiplied by 5x. (Diamond tiles only appear on the board in wave 4+ via spawn distribution gating in 47-05; the `case 'diamond'` handler itself fires unconditionally since if a diamond exists on the board, it was already wave-gated at spawn time.)
10. **Mixed tiers multiplicative**: Word with silver + gold → 1.5 * 3 = 4.5x multiplier.

All tests should FAIL.
  </action>
  <verify>Run `npx jest useBlastGame.mirrorGoldTier --no-coverage` — all FAIL. Run `npx jest blast.test --no-coverage` to verify type test updates pass.</verify>
  <done>10+ test cases for Mirror, Wildcard removal, Gold tiers. Type union updated. Tests fail pending implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mirror, remove Wildcard, implement Gold tiers</name>
  <files>fe-next/components/blast/hooks/useBlastGame.ts, fe-next/components/blast/utils/blastLetterGenerator.ts, fe-next/components/blast/utils/blastTileUtils.ts</files>
  <action>
**Mirror implementation in `clearTilesForWord`:**

Add `case 'mirror'` to the switch statement. Logic is similar to Rainbow Boost (from 47-01):
1. Scan word path for other specials (excluding mirror, standard, gold/silver/diamond).
2. If partner special found: re-execute partner's effect a second time (doubles it). Mirror picks the FIRST special in the path (not best — that's Rainbow's job).
3. If no partner: set `mirrorSoloMultiplier = MIRROR_MULTIPLIER` applied to word score at end.

**Key difference from Rainbow Boost:** Rainbow picks BEST special and doubles. Mirror picks FIRST special and doubles. When both Rainbow and Mirror are in the same word: Rainbow fires its copy of best, Mirror fires its copy of first special, resulting in potentially triple activation.

**Wildcard removal:**
1. Remove `case 'wildcard'` from switch statement in `clearTilesForWord`.
2. In `rollSpecialFromDistribution` fallback: change `return 'wildcard'` to `return 'standard'`.
3. In `blastLetterGenerator.ts` `rollSpecialType`: change fallback from `'wildcard'` to `'standard'`.
4. In `case 'magnet'` (now Vortex from 47-03): remove the `target.type === 'wildcard'` condition if it still exists.

**Gold tier system:**
1. Add `case 'silver'` and `case 'diamond'` alongside `case 'gold'` in switch:
   - `case 'silver'`: `goldMultiplier *= SILVER_MULTIPLIER;` + explosion
   - `case 'gold'`: unchanged (already `goldMultiplier *= GOLD_MULTIPLIER`)
   - `case 'diamond'`: `goldMultiplier *= DIAMOND_MULTIPLIER;` + explosion

2. In `blastTileUtils.ts`: Add `case 'mirror': return 0`, `case 'silver': return 0`, `case 'diamond': return 0` to `getInitialHitsRemaining`.

Import all new constants from `../types`.
  </action>
  <verify>Run `npx jest useBlastGame.mirrorGoldTier --no-coverage` — all pass (GREEN). Then `npx jest --testPathPattern="blast" --no-coverage` for no regressions.</verify>
  <done>Mirror doubles partner effect. Wildcard fully removed. Silver/Gold/Diamond tiers apply correct multipliers. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx jest useBlastGame.mirrorGoldTier --no-coverage  # All mirror/tier tests pass
npx jest blast.test --no-coverage                    # Type union test passes
npx jest --testPathPattern="blast" --no-coverage      # No regressions
```
</verification>

<success_criteria>
- Mirror + special = that special fires twice
- Mirror solo = 2x word score
- Wildcard completely removed from type union, distribution, fallbacks, and switch
- Silver 1.5x, Gold 3x, Diamond 5x multipliers work correctly
- Mixed tiers are multiplicative
- All existing blast tests pass (updated for no-wildcard world)
</success_criteria>

<output>
After completion, create `.planning/phases/47-tile-reworks/47-04-SUMMARY.md`
</output>
