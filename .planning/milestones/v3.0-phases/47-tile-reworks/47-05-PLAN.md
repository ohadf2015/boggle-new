---
phase: 47-tile-reworks
plan: 05
type: tdd
wave: 5
depends_on: ["47-04"]
files_modified:
  - fe-next/components/blast/utils/blastWaveConfig.ts
  - fe-next/components/blast/utils/__tests__/blastWaveConfig.test.ts
  - fe-next/components/blast/types.ts
  - fe-next/components/blast/BlastView.tsx
autonomous: true

must_haves:
  truths:
    - "Wave 1 only has basic specials (bomb, ice, gold/silver, rainbow); no advanced tiles"
    - "Mirror unlocks at wave 3+; Vortex unlocks at wave 6+; Frost and Diamond unlock at wave 4+"
    - "Wildcard has 0% spawn rate in all waves"
    - "Spawn distribution sums to 1.0 for every wave"
    - "Treasure Gem, Prism, Lightning unlock progression matches difficulty staircase"
  artifacts:
    - path: "fe-next/components/blast/utils/blastWaveConfig.ts"
      provides: "Updated WaveConfig interface and WAVE_TABLE with new tile flags"
      contains: "mirrorEnabled"
    - path: "fe-next/components/blast/utils/__tests__/blastWaveConfig.test.ts"
      provides: "Tests for spawn distribution tables"
      min_lines: 60
  key_links:
    - from: "fe-next/components/blast/utils/blastWaveConfig.ts"
      to: "fe-next/components/blast/BlastView.tsx"
      via: "getWaveConfig and getWaveDistribution consumed by BlastView"
      pattern: "getWaveConfig|getWaveDistribution"
---

<objective>
Update spawn distribution tables for all waves to include new/reworked tiles with balanced probabilities. Remove Wildcard from distributions. Add Mirror, Silver, Diamond spawn gates.

Purpose: Ensure the wave progression feels like a Candy Crush "complexity staircase" — new tile types unlock gradually, keeping early waves simple and late waves strategic.
Output: Updated WaveConfig, WAVE_TABLE, and getWaveDistribution with tests verifying all waves.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tile-reworks/47-04-SUMMARY.md
@fe-next/components/blast/utils/blastWaveConfig.ts
@fe-next/components/blast/utils/__tests__/blastWaveConfig.test.ts
@fe-next/components/blast/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for updated spawn distributions</name>
  <files>fe-next/components/blast/utils/__tests__/blastWaveConfig.test.ts</files>
  <action>
Add or update tests in the existing `blastWaveConfig.test.ts` file:

1. **Wave 1 distribution has no wildcard**: `getWaveDistribution(getWaveConfig(1))` should have wildcard=0 (or no wildcard key). Should have bomb, ice, gold, silver, rainbow > 0. Mirror, vortex(magnet), frost(frozen), prism, lightning, gem, diamond = 0.

2. **Wave 2 distribution unlocks gem**: gem share > 0. Still no mirror, lightning, prism, frozen, magnet, diamond.

3. **Wave 3 distribution unlocks prism + mirror**: prism > 0, mirror > 0 (both newly enabled). Still no lightning, magnet, frozen, diamond.

4. **Wave 4 distribution unlocks frost + lightning + diamond**: frozen > 0, lightning > 0, diamond > 0. Still no magnet.

5. **Wave 5 distribution unlocks everything except magnet**: All except magnet > 0.

6. **Wave 6+ distribution unlocks everything (including magnet/vortex)**: All tile types > 0.

7. **All distributions sum to 1.0**: For each wave 1-6, verify `Object.values(getWaveDistribution(...)).reduce((a,b) => a+b, 0)` is approximately 1.0 (within 0.01).

8. **Silver replaces portion of gold**: In waves where both exist, silver + gold share together is similar to old gold-only share. Verify silver takes roughly 1/3 of old gold budget.

9. **No wildcard in any wave**: For waves 1-10, verify wildcard is either missing or 0 in every distribution.

10. **WaveConfig has new flags**: `mirrorEnabled`, `silverEnabled`, `diamondEnabled` properties exist on WaveConfig interface.

Tests should FAIL because WaveConfig lacks new fields and distributions still include wildcard.
  </action>
  <verify>Run `npx jest blastWaveConfig --no-coverage` — new tests FAIL, existing tests may also fail from type changes.</verify>
  <done>10 distribution tests exist and fail pending implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Update WaveConfig, WAVE_TABLE, and getWaveDistribution</name>
  <files>fe-next/components/blast/utils/blastWaveConfig.ts, fe-next/components/blast/types.ts, fe-next/components/blast/BlastView.tsx</files>
  <action>
**WaveConfig interface updates:**
Add fields:
- `mirrorEnabled: boolean`
- `silverEnabled: boolean` (silver always available when gold is; but explicit flag for clarity)
- `diamondEnabled: boolean`
- `treasureGemEnabled: boolean` (rename gemEnabled to be explicit about new mechanic)

Remove or keep `magnetEnabled` as `vortexEnabled` (rename for clarity since magnet→vortex).

**WAVE_TABLE updates:**

Unlock progression (Candy Crush staircase):

| Wave | New Unlocks | Available Pool |
|------|-------------|----------------|
| 1 | (basics) | standard, bomb, ice, gold, silver, rainbow |
| 2 | gem→treasure gem | + treasure gem |
| 3 | prism, mirror | + prism, mirror |
| 4 | frost, lightning, diamond | + frost, lightning, diamond |
| 5 | (all except vortex) | + all minus vortex |
| 6+ | vortex | everything |

For each wave entry in WAVE_TABLE, set the enabled flags accordingly. Silver is available from wave 1 (it's just a lower-tier gold).

**getWaveDistribution updates:**

Remove wildcard entirely (no `wildcard: 0.17` base). Redistribute that 17% share among other tiles.

New base distribution (wave 1 — only basic tiles):
- bomb: 0.25
- ice: 0.20
- gold: 0.15
- silver: 0.15
- rainbow: 0.25

When new tiles unlock, they take share proportionally from the pool (same `takeShare` pattern). Add shares:
- `MIRROR_SHARE = 0.08`
- `SILVER_SHARE` — silver is always present, baked into base
- `DIAMOND_SHARE = 0.05`
- `TREASURE_GEM_SHARE = 0.08` (rename from GEM_SHARE)
- `VORTEX_SHARE = 0.08` (rename from MAGNET_SHARE)
- `FROST_SHARE = 0.06` (rename from FROZEN_SHARE)

Keep existing PRISM_SHARE, LIGHTNING_SHARE unchanged.

Ensure final distribution always sums to 1.0 (normalization at end already exists).

Update `SPECIAL_TILE_DISTRIBUTION` in `types.ts` to remove wildcard entry and add mirror: 0, silver: 0, diamond: 0.

**Also update WAVE_OBJECTIVES** if any reference wildcard or need adjustment for new tile names.

**BlastView.tsx:** This is the actual consumer of `getWaveConfig`/`getWaveDistribution` (imports at line 12, used at line 46). If the WaveConfig interface changes (new fields like `mirrorEnabled`, `diamondEnabled`, renamed fields like `vortexEnabled`), update BlastView.tsx accordingly to pass the new config shape. Check for any TypeScript errors after interface changes.
  </action>
  <verify>Run `npx jest blastWaveConfig --no-coverage` — all pass (GREEN). Then `npx jest --testPathPattern="blast" --no-coverage` for no regressions.</verify>
  <done>All waves have correct tile unlock progression. Wildcard removed from all distributions. New tiles (mirror, silver, diamond) have spawn rates. All sums normalize to 1.0. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx jest blastWaveConfig --no-coverage              # All wave config tests pass
npx jest --testPathPattern="blast" --no-coverage     # No regressions
```
</verification>

<success_criteria>
- Wave 1: only basic specials (bomb, ice, gold, silver, rainbow)
- Wave 2: + treasure gem
- Wave 3: + prism, mirror
- Wave 4: + frost, lightning, diamond
- Wave 5: + everything except vortex
- Wave 6+: all tiles available
- Wildcard = 0 in every wave distribution
- All distributions normalize to 1.0
- All existing blast tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-tile-reworks/47-05-SUMMARY.md`
</output>
