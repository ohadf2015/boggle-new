---
phase: 47-tile-reworks
plan: 03
type: tdd
wave: 3
depends_on: ["47-02"]
files_modified:
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/__tests__/useBlastGame.vortexFrost.test.ts
  - fe-next/components/blast/types.ts
  - fe-next/components/blast/utils/blastTileUtils.ts
  - fe-next/shared/types/blast.ts
autonomous: true

must_haves:
  truths:
    - "Vortex pulls tiles within radius 2 toward it, then explodes clearing the area"
    - "Frost tile cracks on first hit revealing inner special type, second hit frees and activates it"
    - "Frost inner tile type is determined at board generation and stored in tile state"
  artifacts:
    - path: "fe-next/components/blast/__tests__/useBlastGame.vortexFrost.test.ts"
      provides: "TDD tests for Vortex and Frost mechanics"
      min_lines: 100
    - path: "fe-next/components/blast/hooks/useBlastGame.ts"
      provides: "Vortex and Frost implementation in clearTilesForWord"
      contains: "case 'magnet'"
    - path: "fe-next/shared/types/blast.ts"
      provides: "innerType field on BlastTileState"
      contains: "innerType"
  key_links:
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "fe-next/shared/types/blast.ts"
      via: "BlastTileState.innerType for Frost"
      pattern: "innerType"
---

<objective>
Rework Magnet→Vortex (pull+explode creating new adjacencies) and Frozen→Frost (2-hit reveal with inner special).

Purpose: Magnet only cleared adjacent wildcards — minimal impact. Vortex physically rearranges the board. Frozen required 3 hits with no payoff. Frost reveals a hidden special, rewarding persistent play.
Output: Working Vortex and Frost with TDD tests.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tile-reworks/47-01-SUMMARY.md
@.planning/phases/47-tile-reworks/47-02-SUMMARY.md
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/types.ts
@fe-next/shared/types/blast.ts
@fe-next/components/blast/utils/blastTileUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for Vortex and Frost mechanics</name>
  <files>fe-next/components/blast/__tests__/useBlastGame.vortexFrost.test.ts, fe-next/components/blast/types.ts, fe-next/shared/types/blast.ts</files>
  <action>
**Type changes first:**

1. Add `innerType` optional field to `BlastTileState` in `shared/types/blast.ts`:
   ```typescript
   innerType?: BlastTileType;  // For Frost tiles: the hidden special revealed on first crack
   ```

2. Add constants to `components/blast/types.ts`:
   - `VORTEX_PULL_RADIUS = 2`
   - `VORTEX_EXPLODE_RADIUS = 1` (same as bomb)
   - `VORTEX_PULL_BONUS = 2` (per tile pulled)
   - `VORTEX_EXPLODE_BONUS = 2` (per tile exploded)
   - `FROST_HITS_REQUIRED = 2` (down from frozen's 3)
   - `FROST_REVEAL_BONUS = 3` (bonus when inner tile activates)

Create test file with cases:

**Vortex tests (rework of magnet):**

1. **Vortex pulls tiles within radius 2**: Place magnet(vortex) tile at (2,2) on 5x5 grid. After activation, tiles within Manhattan distance 2 should have their positions swapped toward the vortex center. Verify tiles at radius 2 moved closer to (2,2).

2. **Vortex explodes after pull**: After pull rearrangement, tiles within radius 1 of vortex position are cleared (same as bomb radius). Verify cleared count matches expected area.

3. **Vortex pull creates new letter adjacencies**: After pull, letters that were 2 apart are now adjacent, creating new potential word formations. Verify at least one tile changed position.

4. **Vortex bonus scoring**: Verify bonusScore includes VORTEX_PULL_BONUS per pulled tile + VORTEX_EXPLODE_BONUS per exploded tile.

**Frost tests (rework of frozen):**

5. **Frost first hit reveals inner type**: Frost tile with innerType='bomb' at hitsRemaining=2. After inclusion in word, hitsRemaining→1, activationEffect='frost-crack', inner type becomes visible (tile state retains innerType).

6. **Frost second hit frees and activates inner tile**: Frost at hitsRemaining=1 with innerType='bomb'. After inclusion, tile is cleared AND the inner tile's effect fires (bomb detonates). Verify bomb explosion occurs.

7. **Frost with innerType='lightning'**: Second hit triggers lightning column-clear from frost position. Verify column tiles cleared.

8. **Frost innerType assigned at board generation**: When generateTileStates creates a frost(frozen) tile, it should assign a random innerType from special types (bomb, lightning, prism, gem, rainbow). Verify innerType is set and is a valid special.

9. **Frost is 2-hit (not 3)**: Verify getInitialHitsRemaining returns 2 for frost/frozen tiles (changed from 3).

10. **Frost with innerType='gem' converts to Treasure Gem**: Frost with innerType='gem' at hitsRemaining=1. After second hit, tile converts to gem type with hitsRemaining=3 (from getInitialHitsRemaining('gem')). Tile is NOT cleared — it becomes a fresh Treasure Gem the player must collect shards from. Verify tile.type='gem', tile.hitsRemaining=3, tile.isCleared=false.

All tests should FAIL.
  </action>
  <verify>Run `npx jest useBlastGame.vortexFrost --no-coverage` — all tests FAIL (RED).</verify>
  <done>10 test cases covering Vortex pull+explode and Frost 2-hit reveal (including gem conversion edge case), all failing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Vortex pull+explode and Frost two-hit reveal</name>
  <files>fe-next/components/blast/hooks/useBlastGame.ts, fe-next/components/blast/utils/blastTileUtils.ts</files>
  <action>
**Frost changes:**

1. In `blastTileUtils.ts`: Change `case 'frozen': return 3` to `return 2` (FROST_HITS_REQUIRED).

2. In `generateTileStates` (useBlastGame.ts): When creating a frozen tile, assign `innerType` — roll using `rollSpecialFromDistribution` filtered by the current wave's distribution (so only wave-unlocked specials appear as inner types). This ensures diamond/mirror/vortex never appear as frost inner types before their unlock wave. Store on the tile state.

3. In the multi-hit handling section of `clearTilesForWord`:
   - Frozen/frost first hit (3→2 or 2→1): Set `activationEffect = 'frost-crack'`. Do NOT award any bonus.

4. In `case 'frozen'` (final hit):
   - Set `activationEffect = 'frost-free'`
   - Award `FROST_REVEAL_BONUS`
   - If `tile.innerType` exists, execute that tile type's effect:
     - 'bomb': enqueue in bombQueue
     - 'lightning': execute column-clear inline (same as lightning case)
     - 'prism': execute cross-clear inline (same as prism case)
     - 'gem': convert tile to gem type with hitsRemaining=3 (don't clear, let player collect shards)
     - 'rainbow': set rainbowSoloMultiplier or amplify (depends on 47-01 implementation)
   - **Wave-gating:** Frost innerType is assigned at board generation using `rollSpecialFromDistribution` filtered by current wave distribution, so it automatically respects wave-enabled flags (no diamond before wave 4, etc.).
   - Push frost-free explosion

**Vortex changes (rewrite `case 'magnet'`):**

**Important:** The tile type key remains `'magnet'` in the switch statement and type system — "Vortex" is the gameplay display name only. Rewrite the existing `case 'magnet':` block; do NOT add a new `case 'vortex':` branch.

1. **Pull phase:** For each tile within Manhattan distance VORTEX_PULL_RADIUS of magnet position:
   - Calculate direction toward magnet center
   - If the tile 1 step closer to center is cleared or is the magnet itself, swap tile to that position
   - Award VORTEX_PULL_BONUS per tile moved
   - Process pulls from outermost ring inward (radius 2 first, then radius 1)
   - Swap means: exchange letter, type, hitsRemaining, innerType, etc. between the two positions

2. **Explode phase:** After pulls, clear all non-cleared tiles within radius 1 of magnet position (same as bomb BFS but single-depth). Award VORTEX_EXPLODE_BONUS per tile. Enqueue any bombs found. Trigger any lightning found.

3. Push vortex explosion animation.

Remove old MAGNET_RADIUS and MAGNET_ATTRACT_BONUS imports if no longer used.
  </action>
  <verify>Run `npx jest useBlastGame.vortexFrost --no-coverage` — all pass (GREEN). Then `npx jest --testPathPattern="blast" --no-coverage` for no regressions.</verify>
  <done>Vortex pulls nearby tiles and explodes. Frost cracks in 2 hits revealing and activating inner special. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx jest useBlastGame.vortexFrost --no-coverage   # All vortex+frost tests pass
npx jest --testPathPattern="blast" --no-coverage    # No regressions
```
</verification>

<success_criteria>
- Vortex pulls tiles within radius 2 toward center, then explodes radius 1
- Vortex creates visible board rearrangement (tiles swap positions)
- Frost is 2-hit (down from 3)
- Frost first hit reveals inner special type
- Frost second hit frees and immediately activates the inner special
- All existing blast tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-tile-reworks/47-03-SUMMARY.md`
</output>
