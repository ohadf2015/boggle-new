---
phase: 55-tech-debt-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/components/blast/types.ts
  - fe-next/components/blast/__tests__/useBlastGame.test.ts
  - fe-next/components/blast/__tests__/useBlastGame.gem.test.ts
  - fe-next/shared/constants/blastMultiplayerConstants.ts
  - fe-next/components/blast/utils/blastComboEffectsTactical.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "blastComboEffects.ts is under 500 lines (already true, verified)"
    - "RAINBOW_BONUS, GEM_USE_BONUS, GEM_COLLECT_BONUS are removed from types.ts"
    - "MAGNET_RADIUS and MAGNET_ATTRACT_BONUS remain in types.ts (actively used)"
    - "No lint-suppression comments for no-duplicate-imports remain in modified files"
    - "All tests pass after constant removal and import merges"
  artifacts:
    - path: "fe-next/components/blast/types.ts"
      provides: "Blast type definitions without dead constants"
      contains: "MAGNET_RADIUS"
    - path: "fe-next/shared/constants/blastMultiplayerConstants.ts"
      provides: "Multiplayer constants with clean imports"
  key_links:
    - from: "fe-next/components/blast/__tests__/useBlastGame.test.ts"
      to: "fe-next/components/blast/types.ts"
      via: "import — RAINBOW_BONUS removed, literal inlined"
      pattern: "toBe\\(10\\)"
    - from: "fe-next/components/blast/__tests__/useBlastGame.gem.test.ts"
      to: "fe-next/components/blast/types.ts"
      via: "import — GEM_USE_BONUS/GEM_COLLECT_BONUS removed, test-local consts"
---

<objective>
Remove dead legacy constants from blast types.ts and fix no-duplicate-imports lint errors in blastMultiplayerConstants.ts and blastComboEffectsTactical.ts.

Purpose: Eliminate dead code and lint suppressions identified in the v3.0 milestone audit.
Output: Clean types.ts (3 dead constants removed), lint-clean import statements in 2 files, all tests green.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-tech-debt-docs-cleanup/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead constants from types.ts and update test consumers</name>
  <files>fe-next/components/blast/types.ts, fe-next/components/blast/__tests__/useBlastGame.test.ts, fe-next/components/blast/__tests__/useBlastGame.gem.test.ts</files>
  <action>
In `fe-next/components/blast/types.ts`:
- Remove `RAINBOW_BONUS` constant and its JSDoc comment (around L141). Value was `5`.
- Remove `GEM_USE_BONUS` constant and its JSDoc comment (around L163). Value was `3`.
- Remove `GEM_COLLECT_BONUS` constant and its JSDoc comment (around L165). Value was `8`.
- DO NOT remove `MAGNET_RADIUS` or `MAGNET_ATTRACT_BONUS` — they are actively used in `useBlastGame.ts` lines 23-24, 1098-1099, 1107.

In `fe-next/components/blast/__tests__/useBlastGame.test.ts`:
- Remove `RAINBOW_BONUS` from the import on ~L49.
- At ~L282, replace `expect(result.current.gameState.score).toBe(5 + RAINBOW_BONUS)` with `expect(result.current.gameState.score).toBe(10)` and add comment `// 5 (word score) + 5 (legacy rainbow flat bonus)`.

In `fe-next/components/blast/__tests__/useBlastGame.gem.test.ts`:
- Remove `GEM_USE_BONUS` and `GEM_COLLECT_BONUS` from imports.
- Add test-local constants at top of file: `const GEM_USE_BONUS = 3;` and `const GEM_COLLECT_BONUS = 8;` so existing assertions remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="useBlastGame" --passWithNoTests --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>RAINBOW_BONUS, GEM_USE_BONUS, GEM_COLLECT_BONUS removed from types.ts. MAGNET_RADIUS and MAGNET_ATTRACT_BONUS still present. All useBlastGame tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix no-duplicate-imports lint errors in two files</name>
  <files>fe-next/shared/constants/blastMultiplayerConstants.ts, fe-next/components/blast/utils/blastComboEffectsTactical.ts</files>
  <action>
In `fe-next/shared/constants/blastMultiplayerConstants.ts` (lines 6-7):
- Merge the two import statements into one:
  ```typescript
  import { BLAST_TILE_TYPE_LIST, type BlastTileType } from '@/shared/types/blast';
  ```
- Remove the `// eslint-disable-line no-duplicate-imports` comment.

In `fe-next/components/blast/utils/blastComboEffectsTactical.ts` (lines 11 and 13):
- Merge the `import type { ComboEffectContext, ComboEffectResult }` (line 11) and `import { applyToTile, fireVortex, pushExplosion }` (line 13) into one statement:
  ```typescript
  import { applyToTile, fireVortex, pushExplosion, type ComboEffectContext, type ComboEffectResult } from './blastComboEffects';
  ```
- Remove the `// eslint-disable-line no-duplicate-imports` comment from the old line 13.

Verify `blastComboEffects.ts` is under 500 lines: run `wc -l` (expect ~476).
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx eslint shared/constants/blastMultiplayerConstants.ts components/blast/utils/blastComboEffectsTactical.ts --quiet 2>&1 && wc -l components/blast/utils/blastComboEffects.ts</automated>
  </verify>
  <done>Both files have single merged import statements. No eslint-disable-line comments for no-duplicate-imports remain. blastComboEffects.ts confirmed under 500 lines. Lint passes clean.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/ohadfisher/git/boggle-new/fe-next && npm run lint -- --quiet && npm run test -- --passWithNoTests --no-coverage 2>&1 | tail -5
```
Full lint + test suite green.
</verification>

<success_criteria>
- SC-1: blastComboEffects.ts under 500 lines (verified, no changes needed)
- SC-2: RAINBOW_BONUS, GEM_USE_BONUS, GEM_COLLECT_BONUS removed from types.ts; MAGNET constants preserved
- SC-3: No lint errors in blastMultiplayerConstants.ts or blastComboEffectsTactical.ts
- All existing tests pass with updated imports/assertions
</success_criteria>

<output>
After completion, create `.planning/phases/55-tech-debt-docs-cleanup/55-01-SUMMARY.md`
</output>
