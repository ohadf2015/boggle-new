---
phase: 53-wildcard-type-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/shared/types/blast.ts
  - fe-next/shared/constants/blastMultiplayerConstants.ts
  - fe-next/components/blast/types.ts
  - fe-next/lib/phaser/logic/BlastTileRules.ts
  - fe-next/shared/types/__tests__/blast.test.ts
  - fe-next/backend/modules/__tests__/blastModeManager.test.ts
  - fe-next/lib/phaser/logic/__tests__/BlastTileRules.test.ts
  - fe-next/components/blast/utils/__tests__/blastLevelClear.test.ts
  - fe-next/components/blast/__tests__/useBlastGame.treasureGem.test.ts
  - fe-next/components/blast/__tests__/useBlastGame.rainbowBoost.test.ts
  - fe-next/phaser/objects/BlastTile.ts
autonomous: true
requirements:
  - TILE-06
  - TILE-08
  - SYNC-01

must_haves:
  truths:
    - "BlastTileType union has exactly 13 members (no 'wildcard')"
    - "BLAST_TILE_TYPE_LIST array has length 13 with no wildcard entry"
    - "BLAST_TILE_TYPES in multiplayer constants excludes wildcard"
    - "All blast-related tests pass with no contradictory wildcard assertions"
    - "TypeScript compiles with no errors (tsc --noEmit)"
  artifacts:
    - path: "fe-next/shared/types/blast.ts"
      provides: "Canonical BlastTileType union without wildcard"
      contains: "standard.*gold.*silver.*diamond.*bomb.*lightning.*rainbow.*prism.*vortex.*frost.*mirror.*gem.*ice"
    - path: "fe-next/shared/constants/blastMultiplayerConstants.ts"
      provides: "BLAST_TILE_BONUSES Record without wildcard key"
    - path: "fe-next/components/blast/types.ts"
      provides: "SPECIAL_TILE_DISTRIBUTION without wildcard key"
    - path: "fe-next/lib/phaser/logic/BlastTileRules.ts"
      provides: "TILE_TINTS, TILE_BORDERS, GLOW_BASES without wildcard keys"
  key_links:
    - from: "fe-next/shared/types/blast.ts"
      to: "fe-next/shared/constants/blastMultiplayerConstants.ts"
      via: "BlastTileType import for Record<BlastTileType, number>"
      pattern: "Record<BlastTileType"
    - from: "fe-next/shared/types/blast.ts"
      to: "fe-next/lib/phaser/logic/BlastTileRules.ts"
      via: "BlastTileType import for tint/border/glow Records"
      pattern: "Record<BlastTileType"
---

<objective>
Remove `'wildcard'` from the `BlastTileType` union, `BLAST_TILE_TYPE_LIST`, and all downstream Record objects and test assertions.

Purpose: Wildcard was removed from spawn distribution in Phase 47 but the type definition was left behind, creating contradictory test assertions and a stale type member that leaks into multiplayer constants. This completes TILE-06 (wildcard removal), confirms TILE-08 (unified type enum), and ensures SYNC-01 (MP uses same tile pool as SP).

Output: Clean 13-type `BlastTileType` union, all tests green, tsc clean.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-wildcard-type-cleanup/53-RESEARCH.md
@.planning/phases/53-wildcard-type-cleanup/53-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove wildcard from type union, list, and all Record objects</name>
  <files>
    fe-next/shared/types/blast.ts,
    fe-next/shared/constants/blastMultiplayerConstants.ts,
    fe-next/components/blast/types.ts,
    fe-next/lib/phaser/logic/BlastTileRules.ts,
    fe-next/phaser/objects/BlastTile.ts
  </files>
  <action>
    1. In `shared/types/blast.ts`:
       - Remove `| 'wildcard'` from the `BlastTileType` union (line ~24)
       - Remove `'wildcard'` entry from `BLAST_TILE_TYPE_LIST` array (line ~41)

    2. In `shared/constants/blastMultiplayerConstants.ts`:
       - Remove `wildcard: 1` from `BLAST_TILE_BONUSES` Record (line ~32)

    3. In `components/blast/types.ts`:
       - Remove `wildcard: 0` from `SPECIAL_TILE_DISTRIBUTION` Record (line ~228)
       - Update JSDoc comment that mentions "wildcard/rainbow attracted by magnet" to just "rainbow attracted by magnet" (line ~156)

    4. In `lib/phaser/logic/BlastTileRules.ts`:
       - Remove `wildcard: 0xc8c8ff` from `TILE_TINTS` Record (line ~40)
       - Remove `wildcard: 0xffffff` from `TILE_BORDERS` Record (line ~59)
       - Remove `wildcard: {...}` from `GLOW_BASES` Record (line ~84)
       - Remove/update the comment "Wildcard is in BlastTileType union but never spawned" (lines ~114-115)

    5. In `phaser/objects/BlastTile.ts`:
       - Remove the `blastType !== 'wildcard'` guard (lines ~727-728). The condition should simply proceed without that check.

    6. Run `cd fe-next && npx tsc --noEmit` to verify all TypeScript errors from the union change are resolved. Fix any remaining TS errors flagged by the compiler.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `BlastTileType` union has 13 members, no 'wildcard'
    - `BLAST_TILE_TYPE_LIST` has 13 entries, no 'wildcard'
    - All Record&lt;BlastTileType, ...&gt; objects have no wildcard key
    - `tsc --noEmit` passes with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all test files with contradictory or stale wildcard assertions</name>
  <files>
    fe-next/shared/types/__tests__/blast.test.ts,
    fe-next/backend/modules/__tests__/blastModeManager.test.ts,
    fe-next/lib/phaser/logic/__tests__/BlastTileRules.test.ts,
    fe-next/components/blast/utils/__tests__/blastLevelClear.test.ts,
    fe-next/components/blast/__tests__/useBlastGame.treasureGem.test.ts,
    fe-next/components/blast/__tests__/useBlastGame.rainbowBoost.test.ts
  </files>
  <action>
    1. In `shared/types/__tests__/blast.test.ts`:
       - Change `toHaveLength(14)` to `toHaveLength(13)` (lines ~8-9)
       - Change `toContain('wildcard')` to `not.toContain('wildcard')` (or replace with a positive assertion that wildcard is absent) (lines ~17-19)
       - Remove `'wildcard'` from the expected array in the ordering/completeness test (line ~42)

    2. In `backend/modules/__tests__/blastModeManager.test.ts`:
       - Change `toHaveLength(14)` to `toHaveLength(13)` (lines ~232-235)
       - Remove `'wildcard'` from any expected type list

    3. In `lib/phaser/logic/__tests__/BlastTileRules.test.ts`:
       - Update comment "All 14 tile types...including wildcard" to "All 13 tile types" (line ~21)
       - Remove `'wildcard'` from local `ALL_TYPES` array (line ~23)
       - Remove the `t !== 'wildcard'` filter from `SPECIAL_TYPES` (line ~32) — no longer needed since wildcard is not in the array

    4. In `components/blast/utils/__tests__/blastLevelClear.test.ts`:
       - Rename test from "includes rainbow and wildcard tiles" to "includes rainbow and mirror tiles" (line ~175)
       - Replace `makeTile(..., 'wildcard', ...)` with `makeTile(..., 'mirror', ...)` (line ~178-182)

    5. In `components/blast/__tests__/useBlastGame.treasureGem.test.ts`:
       - Update comment that lists `wildcard` as a possible spawn (line ~390) — remove wildcard from the comment

    6. In `components/blast/__tests__/useBlastGame.rainbowBoost.test.ts`:
       - Remove the `type === 'wildcard'` guard in the loop (line ~539) — the guard is dead code now

    Do NOT modify `blastWaveConfig.test.ts` — the `dist.wildcard ?? 0` pattern is forward-compatible and should remain.

    Run the quick test suite to verify all updated tests pass.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="shared/types/__tests__/blast|useBlastGame.mirrorGoldTier|blastModeManager|BlastTileRules|blastLevelClear|useBlastGame.treasureGem|useBlastGame.rainbowBoost" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 7 test files pass with no wildcard-contradictory assertions
    - `BLAST_TILE_TYPE_LIST` length asserted as 13
    - No test creates a tile with type 'wildcard'
    - No test asserts wildcard IS in any list
    - Existing regression test (`useBlastGame.mirrorGoldTier`) confirms wildcard never spawns
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run full verification:

1. TypeScript compilation: `cd fe-next && npx tsc --noEmit`
2. Quick blast test suite: `cd fe-next && npx jest --testPathPattern="shared/types/__tests__/blast|useBlastGame.mirrorGoldTier|blastModeManager|BlastTileRules|blastLevelClear" --no-coverage`
3. Grep for remaining wildcard references: `grep -rn "'wildcard'" fe-next/shared/types/blast.ts fe-next/shared/constants/blastMultiplayerConstants.ts fe-next/components/blast/types.ts fe-next/lib/phaser/logic/BlastTileRules.ts` — should return 0 matches
4. Full frontend test suite: `cd fe-next && npm run test -- --no-coverage` — should pass (ignoring pre-existing failures in NeoLoader, SinglePlayerGame, GlobalBottomNav, PlayerWaitingViewShareButton)
</verification>

<success_criteria>
- `BlastTileType` union has exactly 13 types (no 'wildcard')
- `BLAST_TILE_TYPE_LIST` has length 13 with no wildcard entry
- All blast-related tests pass with updated assertions
- `tsc --noEmit` compiles cleanly
- `grep "'wildcard'" fe-next/shared/types/blast.ts` returns nothing
- `BLAST_TILE_BONUSES`, `SPECIAL_TILE_DISTRIBUTION`, `TILE_TINTS`, `TILE_BORDERS`, `GLOW_BASES` all lack wildcard keys
</success_criteria>

<output>
After completion, create `.planning/phases/53-wildcard-type-cleanup/53-01-SUMMARY.md`
</output>
