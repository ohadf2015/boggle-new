---
phase: 46-foundation-unified-tile-types-bug-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fe-next/shared/types/blast.ts
  - fe-next/shared/types/index.ts
  - fe-next/shared/constants/blastMultiplayerConstants.ts
  - fe-next/shared/types/game.ts
  - fe-next/components/blast/types.ts
  - fe-next/backend/modules/blastModeManager.ts
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/utils/blastCombos.ts
  - fe-next/components/blast/utils/blastTileUtils.ts
  - fe-next/components/blast/utils/blastObjectiveGuarantee.ts
  - fe-next/components/blast/utils/blastLevelClear.ts
  - fe-next/components/blast/utils/blastLetterGenerator.ts
  - fe-next/components/blast/utils/blastGravity.ts
  - fe-next/components/blast/BlastExplosionLayer.tsx
  - fe-next/components/blast/BlastObjectiveDisplay.tsx
  - fe-next/components/blast/BlastCascadeOverlay.tsx
  - fe-next/components/blast/index.ts
  - fe-next/components/blast/hooks/useBlastObjectives.ts
  - fe-next/components/blast/BlastReadyScreen.tsx
  - fe-next/components/blast/BlastView.tsx
  - fe-next/components/game/BlastMultiplayerOverlay.tsx
  - fe-next/lib/phaser/logic/BlastTileRules.ts
  - fe-next/phaser/objects/BlastTile.ts
  - fe-next/phaser/objects/ScorePopupManager.ts
autonomous: true

must_haves:
  truths:
    - "A single BlastTileType union exists in shared/types/blast.ts and all 35+ consumer files import from it"
    - "No file in the codebase defines its own BlastTileType (grep returns only the canonical definition and re-exports)"
    - "MP blast uses 'standard' (not 'normal') consistent with SP"
    - "All existing blast tests still pass after migration"
  artifacts:
    - path: "fe-next/shared/types/blast.ts"
      provides: "Canonical BlastTileType union and BlastTileState interface"
      exports: ["BlastTileType", "BlastTileState"]
    - path: "fe-next/shared/types/blast.test.ts"
      provides: "Type-level verification tests"
  key_links:
    - from: "fe-next/components/blast/types.ts"
      to: "fe-next/shared/types/blast.ts"
      via: "re-export"
      pattern: "export.*from.*shared/types/blast"
    - from: "fe-next/shared/constants/blastMultiplayerConstants.ts"
      to: "fe-next/shared/types/blast.ts"
      via: "import type"
      pattern: "import.*BlastTileType.*from.*shared/types/blast"
    - from: "fe-next/shared/types/game.ts"
      to: "fe-next/shared/types/blast.ts"
      via: "import type"
      pattern: "import.*BlastTileType.*from.*[./]*blast"
---

<objective>
Unify the divergent BlastTileType definitions into a single canonical source in `shared/types/blast.ts`.

Purpose: SP and MP blast modes currently have different type definitions (SP: 11 types with 'standard', MP: 8 types with 'normal'). This divergence causes type errors and blocks all v3.0 tile rework. Unification is prerequisite for every subsequent phase.

Output: Single `BlastTileType` union in `shared/types/blast.ts`, all 35+ consumers migrated, MP 'normal' renamed to 'standard'.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@fe-next/components/blast/types.ts
@fe-next/shared/constants/blastMultiplayerConstants.ts
@fe-next/shared/types/game.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical BlastTileType in shared/types/blast.ts and write verification test</name>
  <files>
    fe-next/shared/types/blast.ts
    fe-next/shared/types/__tests__/blast.test.ts
    fe-next/shared/types/index.ts
  </files>
  <action>
1. Create `fe-next/shared/types/blast.ts` with:
   - `BlastTileType` union: `'standard' | 'gold' | 'bomb' | 'rainbow' | 'ice' | 'wildcard' | 'lightning' | 'magnet' | 'prism' | 'gem' | 'frozen'`
   - `BlastTileState` interface (move from `components/blast/types.ts`)
   - `BLAST_TILE_TYPE_LIST` const array for runtime iteration
   - Export all types

2. Write RED test `fe-next/shared/types/__tests__/blast.test.ts`:
   - Verify `BLAST_TILE_TYPE_LIST` contains exactly 11 entries
   - Verify 'standard' is in the list (not 'normal')
   - Verify all expected types are present
   - Run test → MUST FAIL (file doesn't exist yet)

3. Create the source file → run test → MUST PASS

4. Add re-export to `fe-next/shared/types/index.ts`
  </action>
  <verify>
    cd fe-next && npx jest --testPathPattern="shared/types/__tests__/blast" --no-coverage
  </verify>
  <done>
    `shared/types/blast.ts` exists with canonical BlastTileType union of 11 types. Test passes. Re-exported from index.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all consumers to import from shared/types/blast.ts</name>
  <files>
    fe-next/components/blast/types.ts
    fe-next/shared/constants/blastMultiplayerConstants.ts
    fe-next/shared/types/game.ts
    fe-next/backend/modules/blastModeManager.ts
    fe-next/components/blast/hooks/useBlastGame.ts
    fe-next/components/blast/utils/blastCombos.ts
    fe-next/components/blast/utils/blastTileUtils.ts
    fe-next/components/blast/utils/blastObjectiveGuarantee.ts
    fe-next/components/blast/utils/blastLevelClear.ts
    fe-next/components/blast/utils/blastLetterGenerator.ts
    fe-next/components/blast/utils/blastGravity.ts
    fe-next/components/blast/BlastExplosionLayer.tsx
    fe-next/components/blast/BlastObjectiveDisplay.tsx
    fe-next/components/blast/BlastCascadeOverlay.tsx
    fe-next/components/blast/index.ts
    fe-next/components/blast/hooks/useBlastObjectives.ts
    fe-next/components/blast/BlastReadyScreen.tsx
    fe-next/components/blast/BlastView.tsx
    fe-next/components/game/BlastMultiplayerOverlay.tsx
    fe-next/lib/phaser/logic/BlastTileRules.ts
    fe-next/phaser/objects/BlastTile.ts
    fe-next/phaser/objects/ScorePopupManager.ts
  </files>
  <action>
1. **components/blast/types.ts**: Remove the local `BlastTileType` and `BlastTileState` definitions. Replace with re-exports:
   `export type { BlastTileType, BlastTileState } from '@/shared/types/blast';`
   Keep all other types (BlastDifficulty, BlastGameConfig, BlastGameState, etc.) in this file — they are SP-specific config types, not the core tile type.

2. **shared/constants/blastMultiplayerConstants.ts**:
   - Remove the local `BLAST_TILE_TYPES` const array and `BlastTileType` type derivation
   - Import `BlastTileType` from `@/shared/types/blast`
   - Update `BLAST_TILE_TYPES` to use the shared list (or define MP-specific subset if needed)
   - Rename 'normal' → 'standard' in the array and in `BLAST_TILE_BONUSES` record
   - Add missing types to `BLAST_TILE_BONUSES`: wildcard: 1, prism: 2, frozen: 1.5

3. **shared/types/game.ts**: Change import of `BlastTileType` from `'../constants/blastMultiplayerConstants'` to `'./blast'`

4. **backend/modules/blastModeManager.ts**: Change import to use `@/shared/types/blast` for `BlastTileType`

5. **All other consumer files** (blast utils, hooks, phaser objects): Verify they import `BlastTileType` from `../types` (which now re-exports from shared). No changes needed if they already import from `components/blast/types.ts`.

6. Run `grep -rn "type BlastTileType" fe-next/` to confirm only ONE definition remains (in shared/types/blast.ts).
  </action>
  <verify>
    cd fe-next && npx jest --testPathPattern="blast" --no-coverage 2>&1 | tail -5 && grep -rn "type BlastTileType" --include="*.ts" --include="*.tsx" . | grep -v node_modules | grep -v "shared/types/blast.ts" | grep -v "re-export\|from.*blast"
  </verify>
  <done>
    All blast tests pass. `grep "type BlastTileType"` returns only the canonical definition in shared/types/blast.ts (plus re-exports). No file defines its own BlastTileType. MP uses 'standard' consistently.
  </done>
</task>

</tasks>

<verification>
1. `cd fe-next && npx jest --testPathPattern="blast" --no-coverage` — all blast tests pass
2. `grep -rn "type BlastTileType" fe-next/ --include="*.ts" --include="*.tsx" | grep -v node_modules` — only shared/types/blast.ts defines it
3. `grep -rn "'normal'" fe-next/shared/ --include="*.ts"` — no 'normal' references remain
4. `cd fe-next && npm run build 2>&1 | grep -i error` — no type errors from migration
</verification>

<success_criteria>
- Single BlastTileType in shared/types/blast.ts with 11 types
- All 35+ consumers import from shared path (directly or via re-export)
- MP 'normal' renamed to 'standard'
- All blast tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/46-foundation-unified-tile-types-bug-fixes/46-01-SUMMARY.md`
</output>
