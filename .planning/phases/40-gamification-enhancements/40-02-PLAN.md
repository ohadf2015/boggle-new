---
phase: 40-gamification-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - fe-next/lib/supabase/education/leaderboard.ts
  - fe-next/lib/supabase/education/leaderboard.test.ts
  - fe-next/hooks/useClassroomLeaderboard.ts
  - fe-next/components/education/ClassroomLeaderboard.tsx
  - fe-next/components/education/ClassroomLeaderboard.test.tsx
autonomous: true

must_haves:
  truths:
    - "Student can view weekly and monthly leaderboards via tab switching"
    - "Student sees rank change indicators (up arrow, down arrow, NEW, or dash for no change)"
    - "Student sees streak badges next to students with 3+ day streaks"
    - "Student sees full classroom ranking (not just top 3) with scrollable list"
    - "Leaderboard shows tier badges for top percentiles (Top 10%, Top 25%, Top 50%)"
  artifacts:
    - path: "fe-next/lib/supabase/education/leaderboard.ts"
      provides: "Extended leaderboard with weekly/monthly scopes, rank delta, full list"
      exports: ["getClassroomLeaderboard", "getLeaderboardWithRankDelta", "saveLeaderboardSnapshot"]
    - path: "fe-next/components/education/ClassroomLeaderboard.tsx"
      provides: "Enhanced leaderboard UI with tabs, rank deltas, streak badges, tier badges"
      contains: "LeaderboardTimeScope"
    - path: "fe-next/components/education/ClassroomLeaderboard.test.tsx"
      provides: "Tests for enhanced leaderboard component"
      contains: "weekly"
  key_links:
    - from: "fe-next/components/education/ClassroomLeaderboard.tsx"
      to: "fe-next/hooks/useClassroomLeaderboard.ts"
      via: "useClassroomLeaderboard hook"
      pattern: "useClassroomLeaderboard"
    - from: "fe-next/hooks/useClassroomLeaderboard.ts"
      to: "fe-next/lib/supabase/education/leaderboard.ts"
      via: "getLeaderboardWithRankDelta call"
      pattern: "getLeaderboardWithRankDelta"
    - from: "fe-next/lib/supabase/education/leaderboard.ts"
      to: "leaderboard_snapshots"
      via: "Supabase query"
      pattern: "from.*leaderboard_snapshots"
---

<objective>
Enhance the classroom leaderboard with weekly/monthly time scopes, rank change indicators, streak badges, full student list, and tier badges. Vertical slice: backend + hook + UI for GAMF-02.

Purpose: Students see competitive classroom leaderboards that reset weekly/monthly, showing who improved and who's on a streak.
Output: Enhanced leaderboard backend, hook, and UI component with tests.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-gamification-enhancements/40-RESEARCH.md
@.planning/phases/40-gamification-enhancements/40-01-SUMMARY.md

@fe-next/lib/supabase/education/leaderboard.ts
@fe-next/lib/supabase/education/types.ts
@fe-next/hooks/useClassroomLeaderboard.ts
@fe-next/components/education/ClassroomLeaderboard.tsx
@fe-next/components/education/ClassroomLeaderboard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend leaderboard backend with rank delta + time scopes</name>
  <files>fe-next/lib/supabase/education/leaderboard.ts, fe-next/lib/supabase/education/leaderboard.test.ts</files>
  <action>
**TDD: Write tests first, then implement.**

1. Write tests in leaderboard.test.ts (RED phase):
   - Test `getLeaderboardWithRankDelta` returns entries with previousRank, rankDelta, isNew fields
   - Test weekly scope filters by last 7 days
   - Test monthly scope filters by last 30 days
   - Test rank delta calculation (positive = moved up, negative = moved down)
   - Test `saveLeaderboardSnapshot` stores current rankings
   - Test `getFullClassroomLeaderboard` returns ALL students (not just top 3)
   - Mock supabase calls

2. Run tests - verify they FAIL (RED).

3. Extend leaderboard.ts (GREEN phase):

   Add `getFullClassroomLeaderboard` function:
   - Same as existing `getClassroomLeaderboard` but returns ALL entries (not just top 3)
   - Accepts `timeScope: LeaderboardTimeScope` parameter
   - For 'weekly': filter student_lesson_progress where last_practice_date >= 7 days ago
   - For 'monthly': filter where last_practice_date >= 30 days ago
   - For 'all-time': no date filter (existing behavior)
   - Returns full sorted array with rank assigned
   - Also fetch current_streak from student_lesson_progress for each student

   Add `getLeaderboardWithRankDelta` function:
   - Calls `getFullClassroomLeaderboard` for current rankings
   - Queries `leaderboard_snapshots` for previous snapshot (most recent for this classroom + time_scope)
   - Calculates rank delta: previousRank - currentRank (positive = improved)
   - Marks entries as isNew if no previous snapshot exists for that student
   - Returns `LeaderboardEntryWithDelta[]`

   Add `saveLeaderboardSnapshot` function:
   - Takes classroomId, timeScope, entries
   - Inserts into leaderboard_snapshots with current date
   - Uses upsert to handle re-runs on same day

   Add `getLeaderboardTier` helper:
   - Given rank and totalStudents, returns tier label
   - Top 10% = 'top10', Top 25% = 'top25', Top 50% = 'top50', rest = null
   - For small classes (<10): Top 1 = 'top10', Top 3 = 'top25', Top 5 = 'top50'

4. Run tests - verify they PASS (GREEN).

Keep the existing `getClassroomLeaderboard` function intact (backward compat). The new functions extend, not replace.
  </action>
  <verify>Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run test -- --testPathPattern="leaderboard" --no-coverage 2>&1 | tail -20`</verify>
  <done>Leaderboard backend has getLeaderboardWithRankDelta, getFullClassroomLeaderboard, saveLeaderboardSnapshot, getLeaderboardTier. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ClassroomLeaderboard UI with tabs, rank delta, full list</name>
  <files>fe-next/hooks/useClassroomLeaderboard.ts, fe-next/components/education/ClassroomLeaderboard.tsx, fe-next/components/education/ClassroomLeaderboard.test.tsx</files>
  <action>
**TDD: Write tests first, then implement.**

1. Write/extend tests in ClassroomLeaderboard.test.tsx (RED phase):
   - Test time scope tabs render (Weekly, Monthly, All-Time)
   - Test clicking tab changes displayed scope
   - Test rank delta indicators render (up arrow green, down arrow orange, NEW cyan, dash gray)
   - Test streak badge shows fire icon + count for streak >= 3
   - Test tier badge shows for top percentile students
   - Test full student list renders (not just top 3)
   - Test "Your Position" highlights current user in full list

2. Run tests - verify they FAIL (RED).

3. Update useClassroomLeaderboard hook:
   - Add `timeScope` state (default: 'weekly')
   - Add `setTimeScope` function
   - Call `getLeaderboardWithRankDelta` instead of `getClassroomLeaderboard`
   - Return full list of `LeaderboardEntryWithDelta[]` instead of just topThree
   - Expose `timeScope`, `setTimeScope` in return value

4. Rewrite ClassroomLeaderboard.tsx:
   - Add time scope tabs at top (Weekly | Monthly | All-Time) using neo-brutalist pill buttons
   - Active tab: bg-neo-yellow text-neo-black. Inactive: bg-neo-navy text-neo-white/60
   - Show FULL student list (scrollable, max-h with overflow-y-auto)
   - Each entry shows:
     * Rank number (left)
     * Avatar
     * Display name + level badge
     * XP amount
     * Rank delta indicator: green up-arrow + number, orange down-arrow + number, cyan "NEW", gray dash
     * Streak badge if currentStreak >= 3: fire emoji + streak count in neo-orange
     * Tier badge if applicable: small colored pill (Top 10% gold, Top 25% silver, Top 50% bronze)
   - Current user entry highlighted with bg-neo-cyan/20 border-neo-cyan
   - Use t() for ALL text (tabs labels, rank indicators, tier labels)
   - Keep RTL support (dir attribute, shadow flipping)
   - Animate entry appearance with Framer Motion stagger (0.05s between entries)
   - Empty state unchanged

   Keep component under 300 lines. If it exceeds, extract LeaderboardEntryRow into a sub-component file.

5. Run tests - verify they PASS (GREEN).

All UI text must use translation keys: education.leaderboard.weekly, education.leaderboard.monthly, education.leaderboard.allTime, education.leaderboard.rankUp, education.leaderboard.rankDown, education.leaderboard.newEntry, education.leaderboard.noChange, education.leaderboard.top10, education.leaderboard.top25, education.leaderboard.top50, education.leaderboard.streak
  </action>
  <verify>Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run test -- --testPathPattern="ClassroomLeaderboard" --no-coverage 2>&1 | tail -20`</verify>
  <done>ClassroomLeaderboard shows weekly/monthly/all-time tabs, rank delta indicators, streak badges, tier badges, full student list with current user highlight. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm run test -- --testPathPattern="leaderboard|ClassroomLeaderboard" --no-coverage` passes
- `npm run lint` passes
- `npm run build` passes
- ClassroomLeaderboard renders time scope tabs
- Rank delta indicators show correctly for each entry
</verification>

<success_criteria>
- Weekly/monthly leaderboard scopes work with date filtering
- Rank change indicators calculated from leaderboard_snapshots
- Streak badges visible for students with 3+ day streaks
- Tier badges (Top 10%, 25%, 50%) shown for qualifying students
- Full student list visible (not just top 3)
- All text uses t() translation keys
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/40-gamification-enhancements/40-02-SUMMARY.md`
</output>
