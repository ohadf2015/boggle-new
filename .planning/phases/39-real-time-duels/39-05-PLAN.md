---
phase: 39-real-time-duels
plan: 05
type: execute
wave: 4
depends_on: ["39-04"]
files_modified:
  - fe-next/components/education/duels/DuelLobby.tsx
  - fe-next/components/education/duels/DuelChallengeModal.tsx
  - fe-next/translations/en.ts
  - fe-next/translations/he.ts
  - fe-next/translations/sv.ts
  - fe-next/translations/ja.ts
autonomous: false

must_haves:
  truths:
    - "Student can select 'Real-Time' duel type when creating a challenge"
    - "Accepting a real-time challenge navigates to RealTimeDuelGame component"
    - "All new real-time duel UI text has translations in 4 languages (en, he, sv, ja)"
    - "Existing async duel flow still works unchanged"
  artifacts:
    - path: "fe-next/translations/en.ts"
      provides: "English translations for real-time duel keys"
      contains: "realtimeDuel"
    - path: "fe-next/translations/he.ts"
      provides: "Hebrew translations for real-time duel keys"
      contains: "realtimeDuel"
    - path: "fe-next/translations/sv.ts"
      provides: "Swedish translations for real-time duel keys"
      contains: "realtimeDuel"
    - path: "fe-next/translations/ja.ts"
      provides: "Japanese translations for real-time duel keys"
      contains: "realtimeDuel"
  key_links:
    - from: "fe-next/components/education/duels/DuelChallengeModal.tsx"
      to: "fe-next/hooks/useDuelSocket.ts"
      via: "createChallenge with duelType parameter"
      pattern: "createChallenge.*realtime"
    - from: "fe-next/components/education/duels/DuelLobby.tsx"
      to: "fe-next/components/education/duels/RealTimeDuelGame.tsx"
      via: "Conditional render based on duel type"
      pattern: "RealTimeDuelGame"
---

<objective>
Integrate real-time duels into existing lobby/challenge flow and add translations for all 4 languages. Students can select duel type (async/realtime) when creating challenges.

Purpose: Entry point for real-time duels via existing UI. Translations ensure 4-language support.
Output: Updated lobby/challenge with duel type toggle, complete translations, human verification checkpoint.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-real-time-duels/39-04-SUMMARY.md
@fe-next/components/education/duels/DuelLobby.tsx
@fe-next/components/education/duels/DuelChallengeModal.tsx
@fe-next/components/education/duels/DuelGameView.tsx
@fe-next/translations/en.ts
@fe-next/translations/he.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DuelChallengeModal with duel type selector and integrate RealTimeDuelGame routing</name>
  <files>fe-next/components/education/duels/DuelChallengeModal.tsx, fe-next/components/education/duels/DuelLobby.tsx</files>
  <action>
1. **DuelChallengeModal.tsx - Add duel type toggle:**
   - Add state: `const [duelType, setDuelType] = useState<'async' | 'realtime'>('async');`
   - Add toggle UI before the "Send Challenge" button:
     - Two option buttons/tabs: "Turn-Based" (async) and "Real-Time" (realtime)
     - Selected option has neo-yellow background, unselected has neo-navy
     - Use `t('duels.turnBased')` and `t('duels.realTime')` for labels
     - Brief description below each: `t('duels.turnBasedDesc')` and `t('duels.realTimeDesc')`
   - Pass `duelType` to `createChallenge()` call (the updated hook accepts optional duelType param)
   - Neo-brutalist styling for toggle (chunky border, hard shadow on selected)

2. **DuelLobby.tsx - Route to correct game component:**
   - When a challenge is accepted and `duel:accepted` event fires:
     - Check the duel type from the accepted data (or from local state based on how challenge was created)
     - If `realtime`: render `<RealTimeDuelGame duelId={...} studentId={...} opponentName={...} onBackToLobby={...} />`
     - If `async`: render existing `<DuelGameView duelId={...} studentId={...} onBackToLobby={...} />`
   - Import `RealTimeDuelGame` from barrel exports
   - When a real-time challenge is received (via notification), accepting it should also route to RealTimeDuelGame

IMPORTANT: The duel type needs to be preserved through the flow. Options:
- Store it in the challenge notification data (add duelType to ChallengeReceivedData)
- Store it in component state when creating/accepting
- Query it from the duel record after acceptance

Simplest approach: Add `duelType` field to `duel:challenge-received` and `duel:accepted` server events (update types.ts ChallengeReceivedData + DuelAcceptedData). This way the client knows which view to render.
  </action>
  <verify>
`cd fe-next && npx tsc --noEmit` - No TypeScript errors
`cd fe-next && npm run lint -- --quiet 2>&1 | head -20` - No lint errors
  </verify>
  <done>DuelChallengeModal has async/realtime toggle. DuelLobby routes to correct game view based on duel type. Existing async flow unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add translations for all real-time duel UI text in 4 languages</name>
  <files>fe-next/translations/en.ts, fe-next/translations/he.ts, fe-next/translations/sv.ts, fe-next/translations/ja.ts</files>
  <action>
Add the following translation keys to the `duels` section of each language file. Use flat key convention (matching Phase 38-08 decision).

**Translation keys needed:**

```
// Duel type selection
duels.turnBased: "Turn-Based" / "מבוסס תורות" / "Turbaserad" / "ターン制"
duels.realTime: "Real-Time" / "בזמן אמת" / "Realtid" / "リアルタイム"
duels.turnBasedDesc: "Play at your own pace" / "שחקו בקצב שלכם" / "Spela i din egen takt" / "自分のペースでプレイ"
duels.realTimeDesc: "Compete live head-to-head" / "התחרו בשידור חי" / "Tävla live mot varandra" / "ライブで対戦"

// Real-time gameplay
duels.waitingForStart: "Waiting for opponent to join..." / "ממתינים ליריב..." / "Väntar på motståndare..." / "対戦相手を待っています..."
duels.timeRemaining: "Time Remaining" / "זמן שנותר" / "Tid kvar" / "残り時間"
duels.yourScore: "Your Score" / "הניקוד שלך" / "Din poäng" / "あなたのスコア"
duels.opponentScore: "Opponent Score" / "ניקוד היריב" / "Motståndarens poäng" / "相手のスコア"
duels.wordsFound: "words found" / "מילים נמצאו" / "ord hittade" / "単語発見"
duels.wordAccepted: "Word accepted!" / "!המילה אושרה" / "Ord accepterat!" / "単語が承認されました！"
duels.wordRejected: "Word not valid" / "מילה לא תקפה" / "Ogiltigt ord" / "無効な単語"
duels.notOnBoard: "Not on board" / "לא על הלוח" / "Inte på brädet" / "ボードにありません"
duels.notInDictionary: "Not in dictionary" / "לא במילון" / "Inte i ordlistan" / "辞書にありません"
duels.alreadyFound: "Already found" / "כבר נמצאה" / "Redan hittat" / "既に発見済み"

// Disconnection
duels.opponentDisconnected: "Opponent Disconnected" / "היריב התנתק" / "Motståndaren frånkopplad" / "相手が切断されました"
duels.autoForfeitMessage: "You'll win automatically if they don't reconnect" / "תנצחו אוטומטית אם לא יתחברו מחדש" / "Du vinner automatiskt om de inte återansluter" / "相手が再接続しなければ自動的に勝利します"
duels.reconnecting: "Reconnecting..." / "...מתחבר מחדש" / "Återansluter..." / "再接続中..."
duels.opponentReconnected: "Opponent reconnected!" / "!היריב חזר" / "Motståndaren återanslöt!" / "相手が再接続しました！"
duels.waitingReconnect: "Waiting for reconnection" / "ממתינים לחיבור מחדש" / "Väntar på återanslutning" / "再接続を待っています"
duels.secondsLeft: "seconds left" / "שניות נותרו" / "sekunder kvar" / "秒残り"

// Forfeit
duels.forfeit: "Forfeit" / "פרישה" / "Ge upp" / "棄権"
duels.forfeitTitle: "Forfeit Duel?" / "?לפרוש מהדו-קרב" / "Ge upp duellen?" / "デュエルを棄権しますか？"
duels.forfeitDescription: "You'll lose this duel and your opponent will win. This can't be undone." / ".תפסידו בדו-קרב הזה והיריב ינצח. לא ניתן לבטל" / "Du förlorar denna duell och din motståndare vinner. Detta kan inte ångras." / "このデュエルに負けとなり、相手が勝利します。この操作は取り消せません。"
duels.forfeitConfirm: "Yes, forfeit" / "כן, לפרוש" / "Ja, ge upp" / "はい、棄権します"
duels.forfeitCancel: "Cancel" / "ביטול" / "Avbryt" / "キャンセル"
duels.wonByForfeit: "Won by forfeit" / "ניצחון בפרישת יריב" / "Vann genom uppgivning" / "棄権勝ち"
duels.lostByForfeit: "Lost by forfeit" / "הפסד בפרישה" / "Förlorad genom uppgivning" / "棄権負け"

// Results (real-time specific)
duels.realtimeDuel: "Real-Time Duel" / "דו-קרב בזמן אמת" / "Realtidsduell" / "リアルタイムデュエル"
duels.duelComplete: "Duel Complete!" / "!הדו-קרב הסתיים" / "Duellen avslutad!" / "デュエル完了！"
```

IMPORTANT:
- Follow flat key convention (duels.keyName, not nested duels.realtime.keyName) per 38-08 decision
- Hebrew text is RTL - verify string direction
- Place keys near existing duel translation keys in each file
- Do NOT modify or remove any existing keys
  </action>
  <verify>
`cd fe-next && npx tsc --noEmit` - No TypeScript errors
`cd fe-next && npm run build 2>&1 | tail -10` - Build succeeds
  </verify>
  <done>All 4 language files contain real-time duel translations (28+ new keys each)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Real-time duel integration: duel type toggle in challenge modal, real-time game view routing, translations in 4 languages.
  </what-built>
  <how-to-verify>
1. Start dev server: `cd fe-next && npm run dev`
2. Navigate to duel lobby
3. Open challenge modal - verify "Turn-Based" and "Real-Time" toggle appears
4. Check translations:
   - English: `http://localhost:3000/?locale=en` - verify labels are in English
   - Hebrew: `http://localhost:3000/?locale=he` - verify RTL rendering, Hebrew text
   - Swedish: `http://localhost:3000/?locale=sv` - verify Swedish labels
   - Japanese: `http://localhost:3000/?locale=ja` - verify Japanese text
5. Verify existing async duel flow still works (select Turn-Based, create challenge)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd fe-next && npx tsc --noEmit` - No TypeScript errors
2. `cd fe-next && npm run build` - Build succeeds
3. DuelChallengeModal shows async/realtime toggle
4. DuelLobby routes to correct game view
5. All 4 language files have real-time duel translations
6. Existing async flow unchanged
7. Human verification of visual rendering + RTL
</verification>

<success_criteria>
- Student can select real-time duel type when creating challenge
- Accepting real-time challenge shows RealTimeDuelGame
- All new text uses t() with translations in 4 languages
- Hebrew RTL renders correctly
- Existing async duel flow untouched
- Human visual verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/39-real-time-duels/39-05-SUMMARY.md`
</output>
