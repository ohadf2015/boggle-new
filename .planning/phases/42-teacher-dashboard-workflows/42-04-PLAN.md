---
phase: 42-teacher-dashboard-workflows
plan: 04
type: execute
wave: 3
depends_on: ["42-01", "42-02", "42-03"]
files_modified:
  - fe-next/components/teacher/TeacherDashboard.tsx
  - fe-next/components/teacher/LessonBuilder.tsx
  - fe-next/components/teacher/dashboard/DuelMonitoringPanel.tsx
  - fe-next/components/teacher/dashboard/DuelMonitoringPanel.test.tsx
  - fe-next/components/teacher/dashboard/index.ts
  - fe-next/components/teacher/index.ts
  - fe-next/app/[locale]/teacher/classroom/[id]/analytics/PageClient.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher dashboard shows assignment tracking section with create button"
    - "Teacher dashboard shows duel monitoring panel when classroom selected"
    - "LessonBuilder integrates template selector and enhanced bulk import"
    - "Analytics page includes assignment completion overview"
  artifacts:
    - path: "fe-next/components/teacher/TeacherDashboard.tsx"
      provides: "Enhanced dashboard with assignment tracking + duel monitoring sections"
      min_lines: 100
    - path: "fe-next/components/teacher/dashboard/DuelMonitoringPanel.tsx"
      provides: "Real-time duel activity panel"
      min_lines: 50
    - path: "fe-next/components/teacher/LessonBuilder.tsx"
      provides: "Enhanced lesson builder with template selector"
      min_lines: 200
  key_links:
    - from: "fe-next/components/teacher/TeacherDashboard.tsx"
      to: "fe-next/components/teacher/assignments/AssignmentTrackingPanel.tsx"
      via: "component import"
      pattern: "AssignmentTrackingPanel"
    - from: "fe-next/components/teacher/TeacherDashboard.tsx"
      to: "fe-next/components/teacher/dashboard/DuelMonitoringPanel.tsx"
      via: "component import"
      pattern: "DuelMonitoringPanel"
    - from: "fe-next/components/teacher/LessonBuilder.tsx"
      to: "fe-next/components/teacher/lesson-creation/TemplateLessonSelector.tsx"
      via: "component import"
      pattern: "TemplateLessonSelector"
---

<objective>
Wire all Phase 42 components into the existing teacher dashboard and lesson builder. Add DuelMonitoringPanel for real-time duel activity, integrate AssignmentTrackingPanel into dashboard, connect TemplateLessonSelector and BulkImportEnhanced into LessonBuilder, and add assignment overview to analytics page.

Purpose: Delivers the complete teacher experience (UIPOL-02) by connecting all new components into the existing UI flow.
Output: Updated TeacherDashboard, LessonBuilder, analytics page, and new DuelMonitoringPanel.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-teacher-dashboard-workflows/42-RESEARCH.md
@.planning/phases/42-teacher-dashboard-workflows/42-01-SUMMARY.md
@.planning/phases/42-teacher-dashboard-workflows/42-02-SUMMARY.md
@.planning/phases/42-teacher-dashboard-workflows/42-03-SUMMARY.md
@fe-next/components/teacher/TeacherDashboard.tsx
@fe-next/components/teacher/LessonBuilder.tsx
@fe-next/app/[locale]/teacher/classroom/[id]/analytics/PageClient.tsx
@fe-next/hooks/useClassroomActivity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DuelMonitoringPanel + Dashboard Integration</name>
  <files>
    fe-next/components/teacher/dashboard/DuelMonitoringPanel.tsx
    fe-next/components/teacher/dashboard/DuelMonitoringPanel.test.tsx
    fe-next/components/teacher/dashboard/index.ts
    fe-next/components/teacher/TeacherDashboard.tsx
    fe-next/components/teacher/index.ts
  </files>
  <action>
    **DuelMonitoringPanel** - Shows recent duel activity for a classroom:

    Props:
    ```typescript
    interface DuelMonitoringPanelProps {
      classroomId: string;
    }
    ```

    Implementation:
    1. Use existing useClassroomActivity(classroomId) hook which already fetches duel_completed events
    2. Display recent duel results in a compact list:
       - Winner name vs loser name, score, time ago
       - Duel type badge (async/realtime)
       - Win streak indicator (fire emoji for 3+ streak)
    3. Show "No recent duels" when empty
    4. Limit to 5 most recent duels
    5. Neo-brutalist card styling matching other dashboard sections
    6. Title: "Duel Activity" with Swords icon

    Create barrel `dashboard/index.ts`:
    ```typescript
    export { default as DuelMonitoringPanel } from './DuelMonitoringPanel';
    ```

    **TeacherDashboard Enhancement:**

    Read current TeacherDashboard.tsx carefully before modifying. Add 2 new collapsible sections BETWEEN the existing Quick Start and Classrooms sections:

    1. **Assignment Tracking Section** (new, between Quick Start and Classrooms):
       - Same collapsible pattern as Classrooms/Lessons sections
       - Badge: "Track" in neo-yellow
       - Icon: ClipboardCheck from lucide-react
       - Content: AssignmentTrackingPanel with first classroom's ID (or a classroom selector dropdown if multiple classrooms exist)
       - Includes "Create Assignment" button that opens AssignmentCreator dialog
       - AssignmentCreator dialog state managed in TeacherDashboard

    2. **Duel Monitoring Section** (new, after Assignment Tracking):
       - Same collapsible pattern
       - Badge: "Live" in neo-pink
       - Icon: Swords from lucide-react
       - Content: DuelMonitoringPanel with selected classroom ID
       - Only shows if teacher has at least one classroom

    Add classroom selector logic:
    - Use existing useClassrooms() hook (already imported via ClassroomManager)
    - If teacher has 1 classroom, auto-select it
    - If multiple, show dropdown selector at top of Assignment/Duel sections
    - Store selectedClassroomId in state

    Import new components:
    ```typescript
    import { AssignmentTrackingPanel, AssignmentCreator } from './assignments';
    import { DuelMonitoringPanel } from './dashboard';
    ```

    Update `components/teacher/index.ts` barrel to include new sub-modules:
    ```typescript
    export * from './assignments';
    export * from './dashboard';
    export * from './lesson-creation';
    ```

    Test for DuelMonitoringPanel:
    - Renders duel activity items
    - Shows empty state when no duels
    - Displays winner/loser names and scores
  </action>
  <verify>
    - `cd fe-next && npx jest components/teacher/dashboard/DuelMonitoringPanel.test --passWithNoTests 2>&1 | tail -10`
    - `cd fe-next && npx tsc --noEmit --pretty 2>&1 | head -30`
    - TeacherDashboard imports and renders AssignmentTrackingPanel, DuelMonitoringPanel, AssignmentCreator
  </verify>
  <done>
    - DuelMonitoringPanel shows recent duel activity from useClassroomActivity
    - TeacherDashboard has 2 new collapsible sections (assignments + duels)
    - Classroom selector works when teacher has multiple classrooms
    - AssignmentCreator dialog opens from dashboard
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: LessonBuilder + Analytics Integration</name>
  <files>
    fe-next/components/teacher/LessonBuilder.tsx
    fe-next/app/[locale]/teacher/classroom/[id]/analytics/PageClient.tsx
  </files>
  <action>
    **LessonBuilder Enhancement:**

    Read current LessonBuilder.tsx carefully before modifying. Add TemplateLessonSelector and replace BulkWordImporter with BulkImportEnhanced.

    Changes to Create Lesson Dialog:
    1. Add TemplateLessonSelector ABOVE the form fields in the create dialog
       - Show as a collapsible "Start from Template" section with BookTemplate icon
       - When teacher selects a template:
         - Pre-fill formData.name with template.name
         - Pre-fill formData.language with template.language
         - Set words to template.words
         - Show toast: "Template loaded: N words"
       - Collapse template section after selection (teacher can re-expand to change)

    2. Replace BulkWordImporter import with BulkImportEnhanced:
       - Change import: `import { BulkImportEnhanced } from './lesson-creation'`
       - Replace `<BulkWordImporter ... />` with `<BulkImportEnhanced ... />`
       - Same props interface, drop-in replacement

    3. Keep all existing functionality intact (drafts, edit dialog, template editor, assignment dialog)

    **Analytics PageClient Enhancement:**

    Read current PageClient.tsx carefully. Add assignment overview tab to the existing Tabs component.

    Changes:
    1. Add 4th tab "Assignments" to TabsList (change grid-cols-3 to grid-cols-4):
       ```tsx
       <TabsTrigger value="assignments" className="...">
         {t('education.analytics.viewAssignments')}
       </TabsTrigger>
       ```
       Style: data-[state=active]:bg-neo-yellow data-[state=active]:text-neo-black

    2. Add TabsContent for assignments:
       ```tsx
       <TabsContent value="assignments" className="space-y-4">
         <div className="bg-neo-navy/30 border-neo border-neo-black shadow-hard rounded-neo p-6">
           <AssignmentTrackingPanel classroomId={classroomId} />
         </div>
       </TabsContent>
       ```

    3. Import AssignmentTrackingPanel from '@/components/teacher/assignments'
  </action>
  <verify>
    - `cd fe-next && npx tsc --noEmit --pretty 2>&1 | head -30`
    - LessonBuilder create dialog includes TemplateLessonSelector
    - LessonBuilder uses BulkImportEnhanced instead of BulkWordImporter
    - Analytics page has 4 tabs including Assignments
  </verify>
  <done>
    - LessonBuilder create dialog has "Start from Template" section
    - Template selection pre-fills lesson name, language, and words
    - BulkImportEnhanced replaces BulkWordImporter with enhanced validation
    - Analytics page shows assignment tracking in new Assignments tab
    - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `cd fe-next && npx tsc --noEmit` passes
- `cd fe-next && npx jest components/teacher/ --passWithNoTests` passes (all existing + new tests)
- `cd fe-next && npm run lint 2>&1 | tail -10` - no new lint errors
- TeacherDashboard renders assignment tracking and duel monitoring
- LessonBuilder integrates templates and enhanced import
- Analytics page has 4 tabs
</verification>

<success_criteria>
- TeacherDashboard has 4 collapsible sections: Quick Actions, Assignments, Duels, Classrooms, Lessons
- DuelMonitoringPanel shows recent duel activity per classroom
- LessonBuilder create dialog includes template selector and enhanced bulk import
- Analytics page has Assignments tab with AssignmentTrackingPanel
- All existing functionality preserved (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/42-teacher-dashboard-workflows/42-04-SUMMARY.md`
</output>
