---
phase: 37-practice-modes
plan: 05
type: execute
wave: 3
depends_on: ["37-02", "37-03", "37-04"]
files_modified:
  - fe-next/components/practice/PracticeModeSelector.tsx
  - fe-next/components/practice/index.ts
  - fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx
  - fe-next/hooks/usePracticeSession.ts
  - fe-next/translations/en.json
  - fe-next/translations/he.json
  - fe-next/translations/sv.json
  - fe-next/translations/ja.json
autonomous: false

must_haves:
  truths:
    - "Student can select matching, spelling, or blitz from the practice mode selector"
    - "Selecting a new mode navigates to the corresponding practice component"
    - "Each mode shows session count and mode-specific description"
    - "All UI text in all 4 languages (English, Hebrew, Swedish, Japanese)"
    - "New modes integrate with existing XP system and show XP on completion"
  artifacts:
    - path: "fe-next/components/practice/PracticeModeSelector.tsx"
      provides: "Updated mode selector with 7 practice modes"
      contains: "matching"
    - path: "fe-next/components/practice/index.ts"
      provides: "Barrel exports for all practice components"
      contains: "WordMatchingPractice"
    - path: "fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx"
      provides: "Page routing for new practice modes"
      contains: "WordMatchingPractice"
  key_links:
    - from: "fe-next/components/practice/PracticeModeSelector.tsx"
      to: "fe-next/hooks/usePracticeSession.ts"
      via: "PracticeType import"
      pattern: "PracticeType"
    - from: "fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx"
      to: "fe-next/components/practice/WordMatchingPractice.tsx"
      via: "dynamic import for code splitting"
      pattern: "WordMatchingPractice"
    - from: "fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx"
      to: "fe-next/components/practice/SpellingChallengePractice.tsx"
      via: "dynamic import for code splitting"
      pattern: "SpellingChallengePractice"
    - from: "fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx"
      to: "fe-next/components/practice/TimedBlitzPractice.tsx"
      via: "dynamic import for code splitting"
      pattern: "TimedBlitzPractice"
---

<objective>
Integrate all three new practice modes into the mode selector, page routing, and add translation keys for all 4 languages.

Purpose: This wiring plan connects the independently-built practice modes into the existing UI so students can discover and use them.
Output: Updated PracticeModeSelector with 7 modes, PageClient routing, complete translations.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-practice-modes/37-RESEARCH.md

Key existing files:
@fe-next/components/practice/PracticeModeSelector.tsx
@fe-next/components/practice/index.ts
@fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx
@fe-next/hooks/usePracticeSession.ts

Prior plan outputs (from 37-02, 37-03, 37-04):
- WordMatchingPractice.tsx at components/practice/
- SpellingChallengePractice.tsx at components/practice/
- TimedBlitzPractice.tsx at components/practice/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PracticeModeSelector, PracticeType, and PageClient routing</name>
  <files>
    fe-next/hooks/usePracticeSession.ts
    fe-next/components/practice/PracticeModeSelector.tsx
    fe-next/components/practice/index.ts
    fe-next/app/[locale]/student/lessons/[id]/PageClient.tsx
  </files>
  <action>
    1. **Extend PracticeType** in `fe-next/hooks/usePracticeSession.ts`:
       Change: `export type PracticeType = 'flashcard' | 'solo_board' | 'warmup' | 'word_list';`
       To: `export type PracticeType = 'flashcard' | 'solo_board' | 'warmup' | 'word_list' | 'matching' | 'spelling' | 'blitz';`

       Also add session count support in PracticeProgress and the fetching logic:
       - Add `matching_sessions`, `spelling_sessions`, `blitz_sessions` to PracticeProgress interface
       - Update VALID_PRACTICE_TYPES in PageClient to include the 3 new types

    2. **Update PracticeModeSelector.tsx** to show 7 modes (4 existing + 3 new):
       Add to practiceOptions array:
       ```typescript
       {
         type: 'matching',
         icon: <Puzzle className="w-8 h-8" />,  // from lucide-react
         colorClass: 'text-neo-green',            // or appropriate color
         bgClass: 'bg-neo-green/10 hover:bg-neo-green/20',
       },
       {
         type: 'spelling',
         icon: <PenTool className="w-8 h-8" />,
         colorClass: 'text-neo-purple',
         bgClass: 'bg-neo-purple/10 hover:bg-neo-purple/20',
       },
       {
         type: 'blitz',
         icon: <Timer className="w-8 h-8" />,
         colorClass: 'text-neo-red',
         bgClass: 'bg-neo-red/10 hover:bg-neo-red/20',
       },
       ```

       Update `getTranslationKey` map to include new modes:
       ```typescript
       matching: { title: 'education.practice.matching', desc: 'education.practice.matchingDesc' },
       spelling: { title: 'education.practice.spelling', desc: 'education.practice.spellingDesc' },
       blitz: { title: 'education.practice.blitz', desc: 'education.practice.blitzDesc' },
       ```

       Update `getSessionCount` to handle new types.

       Check: Import icons from lucide-react. If 'Puzzle' doesn't exist in lucide, use 'Link2' or 'Shuffle'. For spelling use 'PenLine'. For blitz use 'Timer' or 'Zap' (Zap is taken by warmup, so use 'Timer').

       Also check: Do the neo-green, neo-purple, neo-red colors exist in the Tailwind config? If not, use existing colors (neo-cyan for matching, use a purple shade like purple-400, and red-400 for blitz). Verify by checking tailwind.config.

    3. **Update index.ts** barrel exports:
       Add exports for the 3 new components:
       ```typescript
       export { default as WordMatchingPractice } from './WordMatchingPractice';
       export { default as SpellingChallengePractice } from './SpellingChallengePractice';
       export { default as TimedBlitzPractice } from './TimedBlitzPractice';
       ```

    4. **Update PageClient.tsx** routing:
       - Import the 3 new components (use dynamic import for code splitting):
         ```typescript
         import dynamic from 'next/dynamic';
         const WordMatchingPractice = dynamic(() => import('@/components/practice/WordMatchingPractice'));
         const SpellingChallengePractice = dynamic(() => import('@/components/practice/SpellingChallengePractice'));
         const TimedBlitzPractice = dynamic(() => import('@/components/practice/TimedBlitzPractice'));
         ```
       - Add VALID_PRACTICE_TYPES entries: 'matching', 'spelling', 'blitz'
       - In the mode rendering switch/conditional, add cases for the new modes:
         ```typescript
         case 'matching':
           return <WordMatchingPractice words={lesson.words} onComplete={handleComplete} onBack={handleBack} />;
         case 'spelling':
           return <SpellingChallengePractice words={lesson.words} onComplete={handleComplete} onBack={handleBack} />;
         case 'blitz':
           return <TimedBlitzPractice words={lesson.words} onComplete={handleComplete} onBack={handleBack} />;
         ```
       - Wire onComplete to call completePracticeSession with the new mode type and XP data

    AVOID:
    - Don't break existing practice modes (flashcard, solo_board, warmup, word_list must still work)
    - Don't add modes if lesson has fewer than 4 words (matching needs at least 4 pairs; show "needs more words" state)
    - Don't forget to pass words with definitions (filter out words without definitions for matching/spelling)
  </action>
  <verify>
    - `cd fe-next && npx tsc --noEmit` passes
    - `cd fe-next && npm run lint` passes
    - `cd fe-next && npm run build` passes
    - PracticeModeSelector shows 7 mode cards
    - Selecting 'matching' renders WordMatchingPractice
    - Selecting 'spelling' renders SpellingChallengePractice
    - Selecting 'blitz' renders TimedBlitzPractice
  </verify>
  <done>
    All 7 practice modes appear in selector. New modes route correctly. Existing modes unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add translation keys for all 4 languages</name>
  <files>
    fe-next/translations/en.json
    fe-next/translations/he.json
    fe-next/translations/sv.json
    fe-next/translations/ja.json
  </files>
  <action>
    Add translation keys under `education.practice` namespace for all new practice mode text. Keys needed:

    **Mode selector labels:**
    - `education.practice.matching` — "Word Matching"
    - `education.practice.matchingDesc` — "Drag and match words with their definitions"
    - `education.practice.spelling` — "Spelling Challenge"
    - `education.practice.spellingDesc` — "Type the correct word from its definition"
    - `education.practice.blitz` — "Timed Blitz"
    - `education.practice.blitzDesc` — "60-second speed round — how many can you get?"

    **Word Matching mode:**
    - `education.practice.matchPairs` — "Match the Pairs"
    - `education.practice.pairsMatched` — "pairs matched"
    - `education.practice.dragToMatch` — "Drag a word to its definition"
    - `education.practice.tapToSelect` — "Tap a word, then tap its definition"
    - `education.practice.correctMatch` — "Correct!"
    - `education.practice.wrongMatch` — "Try again"

    **Spelling Challenge mode:**
    - `education.practice.spellTheWord` — "Spell the Word"
    - `education.practice.typeTheWord` — "Type the word that matches this definition"
    - `education.practice.hint` — "Hint"
    - `education.practice.streak` — "Streak"
    - `education.practice.difficulty.easy` — "Easy"
    - `education.practice.difficulty.medium` — "Medium"
    - `education.practice.difficulty.hard` — "Hard"
    - `education.practice.correctSpelling` — "Correct!"
    - `education.practice.incorrectSpelling` — "The correct word is:"

    **Timed Blitz mode:**
    - `education.practice.blitzTitle` — "Timed Blitz"
    - `education.practice.getReady` — "Get Ready!"
    - `education.practice.go` — "GO!"
    - `education.practice.timesUp` — "Time's Up!"
    - `education.practice.wordsFound` — "words found"
    - `education.practice.maxCombo` — "max combo"
    - `education.practice.combo` — "Combo"
    - `education.practice.blitzScore` — "Blitz Score"

    **Shared/results:**
    - `education.practice.needsMoreWords` — "This lesson needs at least 4 words with definitions for this mode"
    - `education.practice.sessionComplete` — "Session Complete!"

    Translate to all 4 languages:
    - **English (en.json):** As shown above
    - **Hebrew (he.json):** Hebrew translations (RTL)
    - **Swedish (sv.json):** Swedish translations
    - **Japanese (ja.json):** Japanese translations

    IMPORTANT: Read each translation file first to find the correct location (under education.practice section). Insert new keys alphabetically or grouped logically with existing keys.

    For Hebrew translations, use natural Hebrew that makes sense for students. Examples:
    - "Word Matching" -> "התאמת מילים"
    - "Spelling Challenge" -> "אתגר כתיב"
    - "Timed Blitz" -> "בליץ מתוזמן"
    - "Drag a word to its definition" -> "גרור מילה להגדרה שלה"

    For Swedish:
    - "Word Matching" -> "Ordmatchning"
    - "Spelling Challenge" -> "Stavningsutmaning"
    - "Timed Blitz" -> "Tidsblixt"

    For Japanese:
    - "Word Matching" -> "単語マッチング"
    - "Spelling Challenge" -> "スペリングチャレンジ"
    - "Timed Blitz" -> "タイムブリッツ"
  </action>
  <verify>
    - All 4 translation files have the new keys
    - `cd fe-next && npx tsc --noEmit` passes
    - `cd fe-next && npm run build` passes
    - No missing translation warnings in console when running dev server
  </verify>
  <done>
    All translation keys exist in en.json, he.json, sv.json, ja.json. New practice modes display correctly in all 4 languages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete practice modes integration:
    - 3 new modes (Word Matching, Spelling Challenge, Timed Blitz) accessible from mode selector
    - All modes track sessions and award XP
    - Full 4-language translation support
    - Drag-and-drop matching with keyboard accessibility
    - Progressive spelling difficulty with hints
    - 60-second timed blitz with combo multiplier
  </what-built>
  <how-to-verify>
    1. Run `cd fe-next && npm run dev`
    2. Navigate to a student lesson page (e.g., /en/student/lessons/[id])
    3. Verify mode selector shows 7 modes (4 existing + 3 new)
    4. **Test Word Matching:**
       - Select "Word Matching"
       - Drag a word to correct definition -> should show green feedback
       - Drag to wrong definition -> should show red shake
       - Complete all pairs -> results card with XP
    5. **Test Spelling Challenge:**
       - Select "Spelling Challenge"
       - See definition, type correct word -> green checkmark
       - Type wrong word -> red shake, correct word revealed
       - Test hint button -> reveals letters
       - Verify streak counter works
    6. **Test Timed Blitz:**
       - Select "Timed Blitz"
       - See 3-2-1 countdown
       - Type answers quickly, verify combo counter
       - Let timer run to 0 -> Time's Up results
    7. **Test Hebrew (RTL):** Add `?locale=he` to URL, verify layout flips correctly
    8. **Test with lesson that has fewer than 4 words:** should show "needs more words" message for matching/blitz
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd fe-next && npm run lint && npm run test && npm run build` all pass
- PracticeModeSelector shows all 7 modes
- Each new mode (matching, spelling, blitz) works end-to-end
- XP awards correctly on session completion
- All 4 languages display correctly
- RTL layout works for Hebrew
- Existing modes (flashcard, solo_board, warmup, word_list) are unaffected
</verification>

<success_criteria>
- Student sees 7 practice modes in selector with icons, descriptions, and session counts
- Word Matching: drag-and-drop pairing with instant feedback and keyboard accessibility
- Spelling Challenge: type word from definition with hints and progressive difficulty
- Timed Blitz: 60-second speed round with combo multiplier
- Sessions tracked in practice_sessions table with XP awarded
- All text translated in 4 languages
- Human verification confirms all modes work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/37-practice-modes/37-05-SUMMARY.md`
</output>
