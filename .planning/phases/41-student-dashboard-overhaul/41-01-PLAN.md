---
phase: 41-student-dashboard-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/components/student/QuickPlayPanel.tsx
  - fe-next/components/student/QuickPlayPanel.test.tsx
  - fe-next/components/student/StreakCalendar.tsx
  - fe-next/components/student/StreakCalendar.test.tsx
  - fe-next/app/[locale]/student/PageClient.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees quick-play buttons for Practice and Duel on the dashboard"
    - "Student sees a 7-day streak calendar showing which days they were active"
    - "Quick Practice button navigates to a random lesson"
    - "Quick Duel button navigates to the duel lobby"
  artifacts:
    - path: "fe-next/components/student/QuickPlayPanel.tsx"
      provides: "Quick-play action buttons for practice and duel"
      min_lines: 40
    - path: "fe-next/components/student/StreakCalendar.tsx"
      provides: "7-day visual streak calendar"
      min_lines: 40
  key_links:
    - from: "fe-next/app/[locale]/student/PageClient.tsx"
      to: "fe-next/components/student/QuickPlayPanel.tsx"
      via: "import and render in dashboard layout"
      pattern: "QuickPlayPanel"
    - from: "fe-next/app/[locale]/student/PageClient.tsx"
      to: "fe-next/components/student/StreakCalendar.tsx"
      via: "import and render in dashboard layout"
      pattern: "StreakCalendar"
---

<objective>
Add QuickPlayPanel and StreakCalendar widgets to the student dashboard, giving students fast access to practice/duel actions and a visual representation of their daily streak.

Purpose: Reduces friction to core actions (practice, duel) and gamifies consistency via the streak calendar. Addresses UIPOL-01 requirements for quick-play buttons and streak visualization.
Output: Two new components wired into the student dashboard between the hero card and challenges panel.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-student-dashboard-overhaul/41-RESEARCH.md

@fe-next/app/[locale]/student/PageClient.tsx
@fe-next/hooks/useWinStreak.ts
@fe-next/hooks/useStudentProgress.ts
@fe-next/components/student/StudentLessonView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickPlayPanel and StreakCalendar components with tests</name>
  <files>
    fe-next/components/student/QuickPlayPanel.tsx
    fe-next/components/student/QuickPlayPanel.test.tsx
    fe-next/components/student/StreakCalendar.tsx
    fe-next/components/student/StreakCalendar.test.tsx
  </files>
  <action>
    **QuickPlayPanel.tsx** - Create a component with two large action buttons in a 2-column grid:

    1. "Quick Practice" button (bg-neo-cyan, Zap icon from lucide-react):
       - On click, pick a random lesson from useStudentProgress().lessons and navigate to `/${language}/student/lessons/${lessonId}`
       - Disabled if no lessons available
       - Show loading spinner while navigating (useState for isNavigating)
       - Subtitle text: "Random lesson"

    2. "Quick Duel" button (bg-neo-pink, Swords icon from lucide-react):
       - On click, navigate to `/${language}/duels/lobby?classroomId=${classroomId}`
       - Subtitle text: "Challenge a classmate"

    Props: `{ classroomId: string; userId: string }`
    Use `useLanguage()` for `t()` and `language`. Use `useRouter()` for navigation.
    Use `useStudentProgress()` for lessons list.
    Neo-brutalist styling: `rounded-neo-lg border-neo-thick border-neo-black shadow-hard-lg`
    Framer Motion: `whileHover={{ scale: 1.02, rotate: -1 }}` on practice, `rotate: 1` on duel.
    `whileTap={{ scale: 0.98 }}`
    Use `t()` keys: `student.dashboard.quickPractice`, `student.dashboard.quickDuel`, `student.dashboard.randomLesson`, `student.dashboard.challengeClassmate` (these will be added in the translations plan).
    For now, the keys can be referenced - they will render as the key name until translations plan runs.

    **StreakCalendar.tsx** - Create a component showing last 7 days with streak status:

    Props: `{ currentStreak: number; lastWinDate: string | null }`
    Generate array of last 7 days (today back to 6 days ago).
    For each day, determine if active: if lastWinDate exists, the current streak covers the last N consecutive days ending at lastWinDate. Mark those days as active.
    Simple approach: if streak is 3 and lastWinDate is today, mark today and 2 prior days as active.
    If lastWinDate is yesterday, mark yesterday and 2 prior days as active.

    Layout: Row of 7 day circles with day abbreviation (Mon, Tue, etc.).
    Active days: `bg-neo-pink/30 border-neo-pink text-neo-pink` with a small flame icon.
    Inactive days: `bg-neo-black/20 border-neo-black/50 text-neo-white/30`.
    Today gets a ring highlight: `ring-2 ring-neo-cyan`.
    Show current streak count with flame emoji as header.
    Use `t()` key: `student.dashboard.streakCalendar`.
    Day abbreviations: Use date-fns `format(date, 'EEE')` for locale-aware day names, or simple `['Sun','Mon','Tue','Wed','Thu','Fri','Sat']` indexed by `date.getDay()`.
    Neo-brutalist card wrapper: `p-4 rounded-neo-lg border-neo-thick border-neo-black bg-neo-navy/40 shadow-hard-lg`.

    **Tests** - TDD: Write tests FIRST, verify they fail, then implement.

    QuickPlayPanel.test.tsx:
    - Renders two buttons (Quick Practice, Quick Duel)
    - Quick Practice button disabled when no lessons
    - Quick Duel button always enabled

    StreakCalendar.test.tsx:
    - Renders 7 day indicators
    - Marks correct days as active based on streak count and lastWinDate
    - Shows current streak count
    - Highlights today

    Mock useLanguage, useRouter, useStudentProgress, useAuth as needed.
  </action>
  <verify>
    cd fe-next && npx jest --testPathPattern="QuickPlayPanel|StreakCalendar" --passWithNoTests
  </verify>
  <done>
    QuickPlayPanel renders two action buttons with correct navigation targets. StreakCalendar renders 7-day visual with active/inactive state based on streak data. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire QuickPlayPanel and StreakCalendar into student dashboard</name>
  <files>
    fe-next/app/[locale]/student/PageClient.tsx
  </files>
  <action>
    Modify the StudentPageClient component in PageClient.tsx to add the two new widgets:

    1. Import QuickPlayPanel from `@/components/student/QuickPlayPanel`
    2. Import StreakCalendar from `@/components/student/StreakCalendar`
    3. Import useWinStreak (already imported)

    In the render, add after the Hero Card section and before the ChallengePanel:

    ```
    {/* Quick Play Panel */}
    {classroomId && (
      <div className="mb-6">
        <QuickPlayPanel classroomId={classroomId} userId={user.id} />
      </div>
    )}

    {/* Streak Calendar */}
    <div className="mb-6">
      <StreakCalendar currentStreak={currentStreak} lastWinDate={lastWinDate} />
    </div>
    ```

    To get streak data in StudentPageClient, use the useWinStreak hook (already imported at the top of the file for StudentProgress, but we need it in the parent too). Add `const { currentStreak: parentStreak, lastWinDate } = useWinStreak();` in StudentPageClient.

    Layout order should be:
    1. ClassroomGameBanner (if active)
    2. StudentProgress (hero card)
    3. QuickPlayPanel (NEW)
    4. StreakCalendar (NEW)
    5. ChallengePanel
    6. ClassroomLeaderboard
    7. Page Header + StudentLessonView

    Ensure the file stays under 500 lines. The current file is 375 lines; adding ~15 lines of imports and JSX is well within limit.
  </action>
  <verify>
    cd fe-next && npm run build 2>&1 | tail -5
  </verify>
  <done>
    Student dashboard renders QuickPlayPanel and StreakCalendar between hero card and challenges panel. Build succeeds without errors.
  </done>
</task>

</tasks>

<verification>
- `cd fe-next && npx jest --testPathPattern="QuickPlayPanel|StreakCalendar"` - all tests pass
- `cd fe-next && npm run build` - no build errors
- `cd fe-next && npm run lint` - no lint errors in modified files
- QuickPlayPanel renders two styled buttons with correct navigation
- StreakCalendar shows 7 days with active highlighting based on streak data
</verification>

<success_criteria>
- QuickPlayPanel component exists with Quick Practice (random lesson nav) and Quick Duel (lobby nav) buttons
- StreakCalendar component exists showing 7-day visual streak
- Both components wired into student dashboard between hero card and challenges
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/41-student-dashboard-overhaul/41-01-SUMMARY.md`
</output>
