---
phase: 48-combination-system-core
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - fe-next/components/blast/utils/blastComboEffects.ts
  - fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
autonomous: true
requirements:
  - COMB-02

must_haves:
  truths:
    - "bomb+rainbow fires cross-clear (4 compass directions) + 3x3 area"
    - "bomb+mirror fires two separate 3x3 blasts at both tile positions"
    - "bomb+magnet fires vortex pull then 5x5 bomb blast"
    - "bomb+gem instantly completes the gem + bomb 3x3 blast"
    - "bomb+frozen cracks all frost tiles on board + bomb 3x3 blast"
    - "lightning+rainbow clears columns of ALL rainbow-type tiles on board"
    - "lightning+mirror fires two column clears at both tile positions"
    - "lightning+magnet fires vortex pull then clears columns of pulled tiles"
    - "lightning+gem instantly completes gem + column clear"
    - "lightning+frozen advances all frost tiles 1 hit + column clear"
    - "prism+rainbow fires full cross-clear from every tile in word path"
    - "prism+mirror fires two full cross-clears from prism position"
  artifacts:
    - path: "fe-next/components/blast/utils/blastComboEffects.ts"
      provides: "12 new combo effect cases for bomb/lightning/prism/rainbow pairs"
      contains: "bomb_rainbow"
    - path: "fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts"
      provides: "Tests for all 12 new combo effects"
  key_links:
    - from: "fe-next/components/blast/utils/blastComboEffects.ts"
      to: "fe-next/components/blast/types.ts"
      via: "BlastExplosion type for returned explosions"
      pattern: "BlastExplosion"
---

<objective>
Implement combo effects for the first 12 pairs: all combinations involving Bomb, Lightning, Prism, and Rainbow Boost with each other.

Purpose: COMB-02 requires all 28 pairs to have distinct effects. This plan covers the 12 pairs from the 4 well-established tile types (Bomb, Lightning, Prism, Rainbow) which have proven explosion/clear patterns.
Output: 12 new cases in executeComboEffect, each with distinct game effects, fully tested.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-combination-system-core/48-RESEARCH.md
@.planning/phases/48-combination-system-core/48-01-SUMMARY.md

@fe-next/components/blast/utils/blastComboEffects.ts
@fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
@fe-next/components/blast/types.ts

<interfaces>
<!-- From 48-01 (will exist after plan 01 completes) -->

From fe-next/components/blast/utils/blastComboEffects.ts:
```typescript
export interface ComboEffectContext {
  combo: SpecialCombo;
  next: BlastTileState[][];
  gridSize: number;
  path: Array<{ row: number; col: number }>;
  now: number;
  markCleared: (tile: BlastTileState) => void;
  isMultiHitAlive: (tile: BlastTileState) => boolean;
  hitMultiHitTile: (tile: BlastTileState) => void;
}
export interface ComboEffectResult {
  explosions: BlastExplosion[];
  processedBombKeys: string[];
  processedLightningKeys: string[];
  bonusScore: number;
}
export function executeComboEffect(ctx: ComboEffectContext): ComboEffectResult;
```

Key constants from fe-next/components/blast/types.ts:
```typescript
export const BOMB_RADIUS = 1; // 3x3
export const VORTEX_PULL_RADIUS = 2;
export const VORTEX_EXPLODE_RADIUS = 1;
export const TREASURE_GEM_HITS_REQUIRED = 3;
export const TREASURE_GEM_COMPLETION_BONUS = 25;
export const FROST_HITS_REQUIRED = 2;
export const RAINBOW_BOOST_MULTIPLIER = 2;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Bomb-involving combo effects (bomb+rainbow, bomb+mirror, bomb+magnet, bomb+gem, bomb+frozen)</name>
  <files>
    fe-next/components/blast/utils/blastComboEffects.ts,
    fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
  </files>
  <behavior>
    - Test: bomb_rainbow clears 4 directional lines (up/down/left/right from bomb) + 3x3 around bomb; explosion type 'combo' intensity 4
    - Test: bomb_mirror clears 3x3 at bomb position AND 3x3 at mirror position (two blasts); two explosions returned
    - Test: bomb_magnet pulls tiles within radius 2 toward magnet (swap), then clears 5x5 around magnet; explosion type 'combo' intensity 4
    - Test: bomb_gem sets gem hitsRemaining to 0 (instant complete) + bomb 3x3 blast; bonusScore includes TREASURE_GEM_COMPLETION_BONUS
    - Test: bomb_frozen decrements hitsRemaining on ALL frost tiles on board by 1 + bomb 3x3 blast
    - Test: all 5 add bomb tile to processedBombKeys
    - Test: multi-hit tiles in blast radius get hitMultiHitTile (not markCleared)
  </behavior>
  <action>
    Add 5 cases to executeComboEffect switch:

    **bomb_rainbow (Prism Bomb):** Find bomb tile from combo.tiles. Clear all tiles in bomb's row and column (cross-clear) + standard 3x3 around bomb. Push 'combo' explosion at bomb position, intensity 4.

    **bomb_mirror (Twin Explosion):** Find bomb and mirror tiles. Clear 3x3 around each (using BOMB_RADIUS). Push two 'combo' explosions. Add bomb to processedBombKeys.

    **bomb_magnet (Gravity Bomb):** Find magnet tile. Execute vortex pull logic (swap tiles within VORTEX_PULL_RADIUS toward magnet center -- reuse the axis-preference movement from 47-03). Then clear 5x5 area (radius 2) centered on magnet. Push 'combo' explosion intensity 4.

    **bomb_gem (Gem Burst):** Find gem tile. Set gem's hitsRemaining to 0, markCleared on gem. Add TREASURE_GEM_COMPLETION_BONUS to bonusScore. Fire standard bomb 3x3 from bomb position. Each cleared tile adds +1 to bonusScore.

    **bomb_frozen (Cryo Blast):** Scan entire grid: every tile with type 'frozen' and hitsRemaining > 0 gets hitMultiHitTile (decrements by 1). Fire standard bomb 3x3 from bomb position.

    Use TDD: write each test, run (RED), implement case, run (GREEN).
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest blastComboEffects --no-coverage</automated>
  </verify>
  <done>5 bomb-involving combo effects produce correct board mutations, return proper explosions and processedBombKeys. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Lightning and Prism-involving combo effects (7 pairs)</name>
  <files>
    fe-next/components/blast/utils/blastComboEffects.ts,
    fe-next/components/blast/utils/__tests__/blastComboEffects.test.ts
  </files>
  <behavior>
    - Test: lightning_rainbow clears columns of ALL tiles with type 'rainbow' anywhere on board + lightning's own column
    - Test: lightning_mirror clears column at lightning position AND column at mirror position
    - Test: lightning_magnet pulls tiles toward magnet, then clears columns of all tiles that moved
    - Test: lightning_gem sets gem hitsRemaining to 0 + clears lightning column; bonusScore includes TREASURE_GEM_COMPLETION_BONUS
    - Test: lightning_frozen decrements all frost tiles on board by 1 hit + clears lightning column
    - Test: prism_rainbow fires cross-clear from every cell in the word path (not just prism position)
    - Test: prism_mirror fires two cross-clears from prism position (double the clear)
    - Test: all lightning combos add lightning tile to processedLightningKeys
  </behavior>
  <action>
    Add 7 cases to executeComboEffect:

    **lightning_rainbow (Rainbow Strike):** Find all tiles with type 'rainbow' on entire board. Clear full column for each rainbow tile found + lightning tile's column. Add all lightning coords to processedLightningKeys.

    **lightning_mirror (Double Strike):** Clear full column at lightning.col AND full column at mirror.col. Two 'combo' explosions. Add both to processedLightningKeys.

    **lightning_magnet (Magnetic Storm):** Vortex pull toward magnet. Track which tiles moved. Clear full column for each tile that was pulled. Add lightning to processedLightningKeys.

    **lightning_gem (Shatter Strike):** Set gem hitsRemaining to 0, markCleared. Add TREASURE_GEM_COMPLETION_BONUS. Clear lightning column. Add to processedLightningKeys.

    **lightning_frozen (Permafrost):** Clear lightning column. Scan board: all frost tiles (type 'frozen', hitsRemaining > 0) get hitMultiHitTile.

    **prism_rainbow (Aurora):** For EACH cell in the word path, fire a full cross-clear (row + column from that cell). This is the most destructive non-prism_prism combo. Push 'combo' explosion intensity 4.

    **prism_mirror (Twin Cross):** Fire standard prism cross-clear from prism position TWICE: first pass clears, second pass hits multi-hit tiles that survived first pass. Effectively double-damage cross. Push 'combo' explosion intensity 4.

    TDD: write each test first (RED), implement (GREEN), verify all pass.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest blastComboEffects --no-coverage</automated>
  </verify>
  <done>7 lightning/prism combo effects work correctly. processedLightningKeys populated for all lightning combos. Total of 18 cases in executeComboEffect (6 existing + 12 new). All tests green.</done>
</task>

</tasks>

<verification>
cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="blastComboEffects|blastCombos" --no-coverage
All combo detection and effect tests pass.
</verification>

<success_criteria>
1. All 12 new pairs (bomb/lightning/prism/rainbow combos) produce distinct board mutations
2. No two pairs produce identical outcomes
3. processedBombKeys/processedLightningKeys correctly prevent double-fire in main path loop
4. Multi-hit tiles hit (not cleared) by area damage
5. blastComboEffects.ts under 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/48-combination-system-core/48-02-SUMMARY.md`
</output>
