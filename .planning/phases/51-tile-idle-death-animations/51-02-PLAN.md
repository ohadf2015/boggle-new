---
phase: 51-tile-idle-death-animations
plan: 02
type: execute
wave: 1
depends_on: [51-01]
files_modified:
  - fe-next/phaser/objects/BlastTile.ts
  - fe-next/phaser/objects/__tests__/BlastTile.clearAnimation.test.ts
autonomous: true
requirements: [TILE-11]

must_haves:
  truths:
    - "Each tile type produces a visually distinct death animation when cleared — no two types use identical tween configs"
    - "Death animations complete within 400ms total (matching CLEAR_DURATION timing budget)"
    - "reduceMotion still produces a simple fade for ALL tile types (no per-type branching)"
    - "isLowEnd halves particle count for ALL tile types including new per-type deaths"
    - "playClearAnimation() still returns a Promise that resolves after animation completes"
  artifacts:
    - path: "fe-next/phaser/objects/BlastTile.ts"
      provides: "playClearByType() dispatch switch + per-type death methods"
      contains: "playClearByType"
    - path: "fe-next/phaser/objects/__tests__/BlastTile.clearAnimation.test.ts"
      provides: "Per-type death animation test cases"
      contains: "playClearByType"
  key_links:
    - from: "fe-next/phaser/objects/BlastTile.ts playClearAnimation()"
      to: "fe-next/phaser/objects/BlastTile.ts playClearByType()"
      via: "method delegation after reduceMotion check"
      pattern: "playClearByType"
    - from: "fe-next/phaser/objects/BlastTile.ts per-type death methods"
      to: "fe-next/lib/phaser/logic/BlastTileRules.ts"
      via: "getClearParticleColor for type-specific particle colors"
      pattern: "getClearParticleColor"
---

<objective>
Add per-type death/clear animations so each tile type has a visually distinct clear effect instead of the current generic squash-rotate-fade.

Purpose: TILE-11 requires each tile type to produce a distinct death animation (shatter, dissolve, refract, burst, etc.). Currently playClearAnimation() plays the same generic sequence for all types. This plan branches it by type.

Output: Updated BlastTile.ts with playClearByType() dispatch and 10+ per-type death methods, all clear animation tests green.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-tile-idle-death-animations/51-RESEARCH.md
@.planning/phases/51-tile-idle-death-animations/51-01-SUMMARY.md

@fe-next/phaser/objects/BlastTile.ts
@fe-next/phaser/objects/__tests__/BlastTile.clearAnimation.test.ts

<interfaces>
<!-- From BlastTile.ts — the death hook to refactor -->
playClearAnimation(options: ClearAnimationOptions = {}): Promise<void>
  // Currently: reduceMotion → simple fade; else → generic squash+rotate+fade+particles
  // Refactor: reduceMotion → simple fade; else → playClearByType(isLowEnd)

<!-- ClearAnimationOptions -->
export interface ClearAnimationOptions {
  reduceMotion?: boolean;
  isLowEnd?: boolean;
}

<!-- Test harness pattern — fires onComplete synchronously -->
function makeScene(): Phaser.Scene {
  const scene = new Phaser.Scene();
  (scene.tweens.add as jest.Mock).mockImplementation((config) => {
    if (typeof config.onComplete === 'function') config.onComplete();
    return { destroy: jest.fn() };
  });
  return scene;
}

<!-- Timing constants -->
const SQUASH_DURATION = 80;
const CLEAR_DURATION = 200;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write per-type death animation tests</name>
  <files>fe-next/phaser/objects/__tests__/BlastTile.clearAnimation.test.ts</files>
  <behavior>
    - Test: bomb death creates a tween with scaleX expanding to >= 1.5 (explosive burst)
    - Test: ice death creates a tween with angle rotation >= 90 (shatter spin)
    - Test: lightning death creates a tween with alpha flicker pattern (zap flash)
    - Test: prism death creates a tween with scaleX AND scaleY expanding (refraction burst)
    - Test: rainbow death creates a tween with alpha fading (dissolve)
    - Test: gem death emits particles (spark burst)
    - Test: frozen death creates a tween (icy melt)
    - Test: gold/silver/diamond death creates a tween with scale expansion (gold burst)
    - Test: magnet death creates a tween with angle rotation (magnetic pulse)
    - Test: mirror death creates a tween with scaleX (mirror shatter)
    - Test: standard tile still plays generic death (regression)
    - Test: each type produces at least 1 tween call distinct from the generic (by checking tween config properties)
    - Test: Promise resolves for every tile type
    - Test: reduceMotion still produces single fade tween for bomb, mirror, diamond (spot check 3 types)
    - Test: isLowEnd halves particle count for bomb, gem (spot check 2 types)
  </behavior>
  <action>
    Add a new describe block "BlastTile per-type death animations" to BlastTile.clearAnimation.test.ts.

    For each tile type, create a test that:
    1. Creates a tile of that type using makeTile()
    2. Clears mock calls
    3. Calls playClearAnimation()
    4. Inspects tween configs via getTweenCalls() for type-distinguishing properties

    The key differentiators per type (what makes each death visually distinct):
    - bomb: large scale expansion (scaleX to >= 1.5) — explosive
    - ice: high angle rotation (>= 90 deg) — shatter spin
    - lightning: rapid alpha oscillation — zap
    - prism: both scaleX and scaleY expand symmetrically — refraction
    - rainbow: pure alpha fade (no rotation) — dissolve
    - gem: extra particle emission — sparkle
    - frozen: lower angle rotation + alpha fade — icy melt
    - gold/silver/diamond: medium scale expansion — gold burst
    - magnet: spin (angle 360) — magnetic pulse
    - mirror: scaleX to -1 then fade (flip shatter) — mirror break

    Write tests RED first — they will fail until Task 2 implements playClearByType().
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest BlastTile.clearAnimation --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <done>Per-type death tests exist for all 13 special types + standard; tests are RED (failing) awaiting implementation</done>
</task>

<task type="auto">
  <name>Task 2: Implement playClearByType() dispatch and per-type death methods</name>
  <files>fe-next/phaser/objects/BlastTile.ts</files>
  <action>
    Refactor playClearAnimation() to delegate to playClearByType() after the reduceMotion guard.

    The reduceMotion path stays unchanged (simple alpha fade).
    The normal path calls this.playClearByType(isLowEnd) instead of the current inline squash+rotate.

    Add private playClearByType(isLowEnd: boolean): Promise<void> with switch:

    case 'bomb': playExplosiveDeath — scale to 1.5x, fast (80ms squash + 150ms expand+fade), more particles
    case 'ice': playShatterDeath — quick squash then high-angle spin (120deg) + fade, ice-colored particles
    case 'lightning': playZapDeath — rapid alpha flicker (3 pulses at 40ms each) then fade, yellow particles
    case 'prism': playRefractDeath — symmetric scale expansion (1.4x both axes) + fade, pink particles
    case 'rainbow': playDissolveDeath — pure alpha fade (no rotation, no squash), rainbow particles
    case 'gem': playSparkDeath — small squash + extra particle burst (2x normal count), emerald particles
    case 'frozen': playMeltDeath — gentle angle rotation (30deg) + slow alpha fade (250ms), blue particles
    case 'gold': case 'silver': case 'diamond': playGoldBurstDeath — scale expand 1.3x + fade, gold/silver/diamond-colored particles. Intensity scales: gold=1.0, silver=0.8, diamond=1.2 (particle count multiplier)
    case 'magnet': playMagneticPulseDeath — full 360 spin + fade, purple particles
    case 'mirror': playMirrorShatterDeath — scaleX to -1 (flip) + fade, white particles
    default: playGenericDeath — existing squash+rotate+fade behavior (moved from current inline code)

    All death methods:
    - Return Promise<void>
    - Call this.emitClearParticles(isLowEnd) for particles
    - Call this.startClearing() before resolve
    - Keep total duration <= 400ms
    - Use the existing makeScene/onComplete synchronous pattern for chained tweens

    Keep SQUASH_DURATION and CLEAR_DURATION constants. Per-type methods can use their own sub-durations as long as total <= 400ms.
  </action>
  <verify>
    <automated>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest BlastTile.clearAnimation BlastTile.idle --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <done>All per-type death tests GREEN; existing squash/rotation/particle/reduceMotion/isLowEnd/Promise tests still GREEN; no regressions in idle tests</done>
</task>

</tasks>

<verification>
cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest BlastTile.clearAnimation BlastTile.idle BlastTileRules --no-coverage
</verification>

<success_criteria>
- playClearAnimation() dispatches to per-type death methods via playClearByType()
- All 13 special types + standard have distinct death animation configs (different tween properties)
- reduceMotion still produces simple fade for all types (no per-type branching)
- isLowEnd halves particle count for all types
- All death animation promises resolve correctly
- No regressions in existing clear animation or idle tests
- Total death animation duration stays under 400ms for all types
</success_criteria>

<output>
After completion, create `.planning/phases/51-tile-idle-death-animations/51-02-SUMMARY.md`
</output>
