---
phase: 38-async-duels
plan: 07
type: execute
wave: 4
depends_on: ["38-05", "38-06"]
files_modified:
  - fe-next/components/education/duels/index.ts
  - fe-next/components/education/duels/ChallengeButton.tsx
  - fe-next/app/[locale]/education/duels/page.tsx
  - fe-next/app/[locale]/education/duels/[duelId]/page.tsx
  - fe-next/components/education/duels/__tests__/ChallengeButton.test.tsx
autonomous: true

must_haves:
  truths:
    - "Student can navigate to /education/duels to see lobby and history"
    - "Student can challenge classmate from a ChallengeButton placed on profile/roster"
    - "Student can navigate to /education/duels/[duelId] to play a specific duel"
  artifacts:
    - path: "fe-next/app/[locale]/education/duels/page.tsx"
      provides: "Duels page with lobby and history tabs"
    - path: "fe-next/app/[locale]/education/duels/[duelId]/page.tsx"
      provides: "Individual duel gameplay page"
    - path: "fe-next/components/education/duels/ChallengeButton.tsx"
      provides: "Reusable button to challenge specific student (SOC-02)"
    - path: "fe-next/components/education/duels/index.ts"
      provides: "Barrel export for all duel components"
  key_links:
    - from: "fe-next/app/[locale]/education/duels/page.tsx"
      to: "DuelLobby + DuelHistory"
      via: "Component composition with tabs"
      pattern: "DuelLobby|DuelHistory"
    - from: "fe-next/app/[locale]/education/duels/[duelId]/page.tsx"
      to: "DuelGameView"
      via: "Renders game view with duelId from params"
      pattern: "DuelGameView"
    - from: "fe-next/components/education/duels/ChallengeButton.tsx"
      to: "DuelChallengeModal"
      via: "Opens modal on click"
      pattern: "DuelChallengeModal"
---

<objective>
Create page routes for the duels section, a reusable ChallengeButton for SOC-02 (challenge from profile/roster), and barrel exports for all duel components.

Purpose: This wires all the UI components into navigable pages and enables the "challenge from anywhere" pattern (SOC-02 requirement).
Output: Routable pages + reusable challenge trigger + clean exports.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-async-duels/38-RESEARCH.md
@.planning/phases/38-async-duels/38-05-SUMMARY.md
@.planning/phases/38-async-duels/38-06-SUMMARY.md

@fe-next/components/education/duels/DuelLobby.tsx
@fe-next/components/education/duels/DuelHistory.tsx
@fe-next/components/education/duels/DuelGameView.tsx
@fe-next/components/education/duels/DuelChallengeModal.tsx
@fe-next/app/[locale]/education/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duels pages and routing</name>
  <files>
    fe-next/app/[locale]/education/duels/page.tsx
    fe-next/app/[locale]/education/duels/[duelId]/page.tsx
  </files>
  <action>
**duels/page.tsx** (server component wrapper + client component):
Main duels page with two tabs: "Lobby" and "History".

Structure:
- Server component fetches: current user session, classroom info, available lessons
- Passes data to a client component `DuelsPageContent` that renders tabs
- Tab 1 "Lobby": Renders DuelLobby with classroomId, studentId, lessons
- Tab 2 "History": Renders DuelHistory with studentId
- Also mounts DuelNotification at this level for persistent challenge alerts

Tab implementation: Use simple state-driven tabs (not a routing-based tab system). Active tab has neo-yellow underline, inactive is muted.

If student has no classroom: show a message "Join a classroom to start dueling" instead of lobby.

ALL text via `t()`. Keys: `duels`, `lobby`, `history`, `joinClassroomToDuel`.

**duels/[duelId]/page.tsx** (server component wrapper):
Individual duel gameplay page.

Structure:
- Server component: extract duelId from params, get current user session
- Render DuelGameView with duelId and studentId
- If duel not found or user not a participant: redirect to /education/duels or show error

ALL text via `t()`. Keys: `duelNotFound`, `notParticipant`.

Follow existing education page patterns (check layout.tsx for auth/session patterns).
  </action>
  <verify>Files exist with correct Next.js page structure. No hardcoded text strings.</verify>
  <done>Duels main page shows lobby/history tabs. Duel game page renders DuelGameView for specific duel.</done>
</task>

<task type="auto">
  <name>Task 2: ChallengeButton + barrel exports</name>
  <files>
    fe-next/components/education/duels/ChallengeButton.tsx
    fe-next/components/education/duels/__tests__/ChallengeButton.test.tsx
    fe-next/components/education/duels/index.ts
  </files>
  <action>
**ChallengeButton.tsx** ('use client'):
A reusable button that opens DuelChallengeModal for a specific student. This satisfies SOC-02 (challenge from profile or classroom roster).

Props: `{ opponentId: string; opponentName: string; opponentAvatar?: string | null; classroomId: string; lessons: Array<{ id: string; name: string }>; variant?: 'icon' | 'button' }`

- `variant='button'` (default): Full button with Swords icon + "Challenge" text
- `variant='icon'`: Just the Swords icon (for compact spaces like roster rows)

Behavior:
- Click opens DuelChallengeModal with pre-filled opponent info
- After challenge sent, show brief success state then reset

Styling:
- button variant: bg-neo-orange, text-white, border-3, shadow-hard-sm, hover scale
- icon variant: text-neo-orange, hover text-neo-yellow

ALL text via `t()`. Keys: `challenge`, `challengeSent`.

**Test:**
- Renders button with correct text
- Click opens modal (check modal renders)
- Icon variant renders without text

**index.ts** (barrel export):
```typescript
export { DuelLobby } from './DuelLobby';
export { DuelChallengeModal } from './DuelChallengeModal';
export { DuelGameView } from './DuelGameView';
export { DuelHistory } from './DuelHistory';
export { DuelNotification } from './DuelNotification';
export { ChallengeButton } from './ChallengeButton';
```
  </action>
  <verify>cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --testPathPattern="duels/__tests__/ChallengeButton" --no-coverage --verbose</verify>
  <done>ChallengeButton opens challenge modal for specific student. Barrel exports all duel components. SOC-02 requirement met.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="duels/__tests__/ChallengeButton" --no-coverage` passes
- /education/duels page exists with lobby/history tabs
- /education/duels/[duelId] page exists with DuelGameView
- ChallengeButton works in both icon and button variants
- Barrel export includes all 6 components
- All text uses t() function
- `npm run lint` passes
</verification>

<success_criteria>
- Student can navigate to /education/duels to see lobby + history
- Student can play specific duel at /education/duels/[duelId]
- ChallengeButton can be placed anywhere (profile, roster) to challenge specific student
- All components properly exported from barrel
- All text internationalized
</success_criteria>

<output>
After completion, create `.planning/phases/38-async-duels/38-07-SUMMARY.md`
</output>
