---
phase: 38-async-duels
plan: 08
type: execute
wave: 5
depends_on: ["38-07"]
files_modified:
  - fe-next/translations/en.js
  - fe-next/translations/he.js
  - fe-next/translations/sv.js
  - fe-next/translations/ja.js
autonomous: false

must_haves:
  truths:
    - "All duel UI text appears correctly in English"
    - "All duel UI text appears correctly in Hebrew (RTL)"
    - "All duel UI text appears correctly in Swedish"
    - "All duel UI text appears correctly in Japanese"
    - "Human verified the duel flow works end-to-end"
  artifacts:
    - path: "fe-next/translations/en.js"
      provides: "English translations for all duel keys"
      contains: "duelLobbyTitle"
    - path: "fe-next/translations/he.js"
      provides: "Hebrew translations for all duel keys"
      contains: "duelLobbyTitle"
    - path: "fe-next/translations/sv.js"
      provides: "Swedish translations for all duel keys"
      contains: "duelLobbyTitle"
    - path: "fe-next/translations/ja.js"
      provides: "Japanese translations for all duel keys"
      contains: "duelLobbyTitle"
  key_links:
    - from: "fe-next/components/education/duels/*.tsx"
      to: "translations/*.js"
      via: "t() function calls"
      pattern: "t\\("
---

<objective>
Add translations for all duel UI text in 4 languages (English, Hebrew, Swedish, Japanese) and perform human verification of the complete duel flow.

Purpose: LexiClash supports 4 languages. All UI text must be translated. Human verification catches visual/interaction issues automated tests miss.
Output: Complete translations + verified duel flow.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-async-duels/38-RESEARCH.md
@.planning/phases/38-async-duels/38-07-SUMMARY.md

@fe-next/translations/en.js
@fe-next/translations/he.js
@fe-next/translations/sv.js
@fe-next/translations/ja.js
@fe-next/components/education/duels/DuelLobby.tsx
@fe-next/components/education/duels/DuelChallengeModal.tsx
@fe-next/components/education/duels/DuelGameView.tsx
@fe-next/components/education/duels/DuelHistory.tsx
@fe-next/components/education/duels/DuelNotification.tsx
@fe-next/components/education/duels/ChallengeButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duel translations to all 4 languages</name>
  <files>
    fe-next/translations/en.js
    fe-next/translations/he.js
    fe-next/translations/sv.js
    fe-next/translations/ja.js
  </files>
  <action>
Scan all duel component files for t() calls and collect every translation key used. Then add translations to all 4 language files.

**Expected keys** (collect actual keys from components -- these are the expected ones based on plans):

Lobby:
- duelLobbyTitle, pendingChallenges, availableOpponents, quickMatch
- accept, decline, noPendingChallenges, noOpponentsOnline, challengeFrom

Challenge Modal:
- sendChallenge, selectLesson, challengeSent, cancel

Game View:
- playDuel, findWords, submitScore, waitingForOpponent
- youWin, youLose, draw, xpEarned, backToLobby
- wordsAccepted, wordsRejected, scoreToBeat

History:
- duelHistory, wins, losses, draws, winStreak, winRate
- recentDuels, noDuelsYet, challengeClassmate, you, vs, perOpponentStats

Notification:
- challengeReceived, challengedYou, viewChallenge

ChallengeButton:
- challenge, challengeSent

Pages:
- duels, lobby, history, joinClassroomToDuel, duelNotFound, notParticipant

**Translation approach:**
- English: Natural, concise text
- Hebrew: Proper RTL-compatible text (verify no LTR punctuation issues)
- Swedish: Natural Swedish translations
- Japanese: Natural Japanese translations (use katakana for borrowed terms where appropriate)

Follow flat key convention (37-06 decision): `duelLobbyTitle` not `duel.lobbyTitle`.

Add keys alphabetically within the duel section (group them together for maintainability). Add a comment `// Duels (Phase 38)` above the group.

Run `npm run lint` after to verify translation files parse correctly.
Run `npm run build` to verify no missing keys crash the build.
  </action>
  <verify>cd /Users/ohadfisher/git/boggle-new/fe-next && npm run lint && npm run build</verify>
  <done>All duel translation keys present in all 4 language files. Build passes. No missing keys.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete async duel system: lobby, challenge creation/acceptance, frozen board gameplay, score submission with server validation, duel history with stats, challenge button for profiles/roster, translations in 4 languages</what-built>
  <how-to-verify>
    1. Navigate to /education/duels - verify lobby and history tabs render
    2. Check lobby shows "No opponents online" empty state (or opponents if multiple sessions)
    3. Test ?locale=he (Hebrew) - verify RTL layout for lobby, history
    4. Test ?locale=sv (Swedish) and ?locale=ja (Japanese) - verify translations display
    5. If possible with 2 browser sessions: create a challenge from one student, verify opponent sees notification
    6. Verify DuelHistory empty state message appears correctly
    7. Check neo-brutalist styling: border-3, shadow-hard, rounded-neo on cards
    8. Verify ChallengeButton renders in both icon and button variants (if visible in roster)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint && npm run build` passes
- All t() keys in duel components have translations in all 4 languages
- Human verified UI appearance and flow
</verification>

<success_criteria>
- All duel UI text translated in English, Hebrew, Swedish, Japanese
- RTL layout correct in Hebrew
- Build passes with no missing translation keys
- Human approved the duel flow appearance and functionality
</success_criteria>

<output>
After completion, create `.planning/phases/38-async-duels/38-08-SUMMARY.md`
</output>
