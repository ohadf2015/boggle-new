---
phase: 43-practice-experience-design-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/components/practice/FlashcardOnboarding.tsx
  - fe-next/components/practice/FlashcardReview.tsx
  - fe-next/components/practice/FlashcardSwipeStack.tsx
  - fe-next/components/practice/PracticeHeader.tsx
  - fe-next/components/practice/QuickPracticeButton.tsx
  - fe-next/components/practice/SwipeFeedbackOverlay.tsx
  - fe-next/components/education/AchievementUnlockModal.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero raw framer-motion motion.div or AnimatePresence imports in any practice component — all use AdaptiveMotion/AdaptiveAnimatePresence"
    - "SwipeFeedbackOverlay still imports MotionValue and useTransform from framer-motion (hooks/types only), but uses AdaptiveMotion for JSX"
    - "Zero generic border-2 (without neo-) references in education/ components"
    - "All existing practice tests pass (139+)"
    - "Build succeeds with zero type errors"
  artifacts:
    - path: "fe-next/components/practice/FlashcardOnboarding.tsx"
      provides: "AdaptiveMotion-migrated flashcard onboarding"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/FlashcardReview.tsx"
      provides: "AdaptiveMotion-migrated flashcard review"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/FlashcardSwipeStack.tsx"
      provides: "AdaptiveMotion-migrated flashcard swipe stack"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/PracticeHeader.tsx"
      provides: "AdaptiveMotion-migrated practice header"
      contains: "AdaptiveMotion"
    - path: "fe-next/components/practice/QuickPracticeButton.tsx"
      provides: "AdaptiveMotion-migrated quick practice button"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/SwipeFeedbackOverlay.tsx"
      provides: "AdaptiveMotion-migrated swipe overlay (keeps MotionValue/useTransform from framer-motion)"
      contains: "AdaptiveMotion"
    - path: "fe-next/components/education/AchievementUnlockModal.tsx"
      provides: "Neo-brutalist compliant modal with border-neo"
      contains: "border-neo"
  key_links:
    - from: "fe-next/components/practice/FlashcardOnboarding.tsx"
      to: "fe-next/components/motion/AdaptiveMotion.tsx"
      via: "import AdaptiveMotion, AdaptiveAnimatePresence"
      pattern: "import.*AdaptiveMotion.*from.*AdaptiveMotion"
    - from: "fe-next/components/practice/SwipeFeedbackOverlay.tsx"
      to: "framer-motion"
      via: "import MotionValue, useTransform (hooks/types only)"
      pattern: "import.*MotionValue.*useTransform.*from.*framer-motion"
---

<objective>
Close 2 verification gaps from 43-VERIFICATION.md: migrate 6 remaining practice components from raw framer-motion to AdaptiveMotion, and fix border-2 design token violation in AchievementUnlockModal.

Purpose: Complete the AdaptiveMotion migration so ALL practice components skip animations on low-end devices, and ensure 100% neo-brutalist design token compliance across education pages.
Output: Zero raw framer-motion component imports in practice/, zero generic border-2 in education/.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-practice-experience-design-polish/43-VERIFICATION.md

Key files to read before implementation:
@fe-next/components/motion/AdaptiveMotion.tsx
@fe-next/components/practice/FlashcardOnboarding.tsx
@fe-next/components/practice/FlashcardReview.tsx
@fe-next/components/practice/FlashcardSwipeStack.tsx
@fe-next/components/practice/PracticeHeader.tsx
@fe-next/components/practice/QuickPracticeButton.tsx
@fe-next/components/practice/SwipeFeedbackOverlay.tsx
@fe-next/components/education/AchievementUnlockModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 practice components from raw framer-motion to AdaptiveMotion</name>
  <files>
    fe-next/components/practice/FlashcardOnboarding.tsx
    fe-next/components/practice/FlashcardReview.tsx
    fe-next/components/practice/FlashcardSwipeStack.tsx
    fe-next/components/practice/PracticeHeader.tsx
    fe-next/components/practice/QuickPracticeButton.tsx
    fe-next/components/practice/SwipeFeedbackOverlay.tsx
  </files>
  <action>
    For each of the 6 files, apply the same mechanical migration pattern:

    **FlashcardOnboarding.tsx** (~197 lines, uses motion + AnimatePresence):
    - Replace `import { motion, AnimatePresence } from 'framer-motion'` with `import { AdaptiveMotion, AdaptiveAnimatePresence } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` with `AdaptiveMotion.div` (and any other motion.X variants like motion.span, motion.p, etc.)
    - Replace all `<AnimatePresence` with `<AdaptiveAnimatePresence` and `</AnimatePresence>` with `</AdaptiveAnimatePresence>`

    **FlashcardReview.tsx** (~493 lines, uses motion + AnimatePresence):
    - Same pattern as above. This is the largest file — be thorough, there are ~8 motion/AnimatePresence usages.
    - Replace `import { motion, AnimatePresence } from 'framer-motion'` with `import { AdaptiveMotion, AdaptiveAnimatePresence } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` → `AdaptiveMotion.div`, `motion.span` → `AdaptiveMotion.span`, `motion.button` → `AdaptiveMotion.button`, `motion.p` → `AdaptiveMotion.p` (check all element types used)
    - Replace all `AnimatePresence` → `AdaptiveAnimatePresence`

    **FlashcardSwipeStack.tsx** (~237 lines, uses motion + AnimatePresence):
    - Same pattern. Replace import, all motion.X, all AnimatePresence.

    **PracticeHeader.tsx** (~182 lines, uses motion only — no AnimatePresence):
    - Replace `import { motion } from 'framer-motion'` with `import { AdaptiveMotion } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` → `AdaptiveMotion.div` (there's only ~1 usage)

    **QuickPracticeButton.tsx** (~244 lines, uses motion + AnimatePresence):
    - Same pattern. Replace import, all motion.X, all AnimatePresence.

    **SwipeFeedbackOverlay.tsx** (~75 lines, SPECIAL CASE — uses MotionValue + useTransform):
    - This file imports `motion, MotionValue, useTransform` from framer-motion.
    - `MotionValue` is a TYPE and `useTransform` is a HOOK — these are NOT JSX components and must STAY as framer-motion imports.
    - Split the import into two lines:
      ```typescript
      import { MotionValue, useTransform } from 'framer-motion';
      import { AdaptiveMotion } from '@/components/motion/AdaptiveMotion';
      ```
    - Replace all `motion.div` → `AdaptiveMotion.div` in JSX
    - Do NOT touch MotionValue type annotations or useTransform calls

    IMPORTANT:
    - Keep ALL existing data-testid attributes unchanged.
    - Keep ALL existing t() translation calls unchanged.
    - Keep ALL existing props/interfaces unchanged.
    - This is a mechanical migration — change ONLY import paths and JSX tag names.
    - Do NOT change any animation values, transitions, or logic.
  </action>
  <verify>
    Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/ 2>&1 | tail -30` — all practice tests pass.

    Verify zero raw motion component imports remain:
    `grep -n "from 'framer-motion'" fe-next/components/practice/FlashcardOnboarding.tsx fe-next/components/practice/FlashcardReview.tsx fe-next/components/practice/FlashcardSwipeStack.tsx fe-next/components/practice/PracticeHeader.tsx fe-next/components/practice/QuickPracticeButton.tsx` — should return empty.

    Verify SwipeFeedbackOverlay only imports hooks/types from framer-motion:
    `grep -n "from 'framer-motion'" fe-next/components/practice/SwipeFeedbackOverlay.tsx` — should show ONLY `MotionValue, useTransform` (no `motion`).

    Verify AdaptiveMotion imports present in all 6 files:
    `grep -n "AdaptiveMotion" fe-next/components/practice/FlashcardOnboarding.tsx fe-next/components/practice/FlashcardReview.tsx fe-next/components/practice/FlashcardSwipeStack.tsx fe-next/components/practice/PracticeHeader.tsx fe-next/components/practice/QuickPracticeButton.tsx fe-next/components/practice/SwipeFeedbackOverlay.tsx` — should show imports in all 6.
  </verify>
  <done>
    All 6 practice components migrated to AdaptiveMotion. Zero raw framer-motion component imports remain in practice/ (only MotionValue/useTransform hooks in SwipeFeedbackOverlay). All existing practice tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix AchievementUnlockModal border-2 design token + final build verification</name>
  <files>
    fe-next/components/education/AchievementUnlockModal.tsx
  </files>
  <action>
    In AchievementUnlockModal.tsx (line ~249):
    - Find the `'border-2'` CSS class (without a `border-neo` prefix)
    - Replace it with `'border-neo'` (which applies the 3px neo-brutalist border style)
    - Check if any other `border-2` occurrences exist in the file and migrate them too

    Then run full verification:
    1. Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/ 2>&1 | tail -30` — all practice tests pass
    2. Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run build 2>&1 | tail -10` — build succeeds
    3. Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run lint 2>&1 | tail -10` — lint passes
    4. Verify gap closure:
       - `grep -rn "from 'framer-motion'" fe-next/components/practice/*.tsx` — only SwipeFeedbackOverlay with MotionValue/useTransform
       - `grep -rn "border-2" fe-next/components/education/AchievementUnlockModal.tsx` — no results
  </action>
  <verify>
    Run all verification commands listed in action.
    Build must succeed with zero type errors.
    Lint must pass.
    All practice tests (139+) must pass.
  </verify>
  <done>
    AchievementUnlockModal uses border-neo instead of border-2. Build, lint, and all tests pass. Both verification gaps from 43-VERIFICATION.md are fully closed: (1) all practice components use AdaptiveMotion exclusively, (2) zero generic border-2 in education components.
  </done>
</task>

</tasks>

<verification>
Both gaps from 43-VERIFICATION.md are addressed:

**Gap 1 (AdaptiveMotion migration):**
- `grep -rn "from 'framer-motion'" fe-next/components/practice/*.tsx` returns ONLY SwipeFeedbackOverlay with `MotionValue, useTransform` (hooks/types, not components)
- `grep -rn "AdaptiveMotion" fe-next/components/practice/FlashcardOnboarding.tsx fe-next/components/practice/FlashcardReview.tsx fe-next/components/practice/FlashcardSwipeStack.tsx fe-next/components/practice/PracticeHeader.tsx fe-next/components/practice/QuickPracticeButton.tsx fe-next/components/practice/SwipeFeedbackOverlay.tsx` shows imports in all 6

**Gap 2 (design token violation):**
- `grep -rn "border-2" fe-next/components/education/AchievementUnlockModal.tsx` returns no results

**Full suite:**
- `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/ 2>&1 | tail -30` — all pass
- `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run build 2>&1 | tail -10` — succeeds
</verification>

<success_criteria>
1. Zero raw framer-motion motion.X or AnimatePresence component imports in any fe-next/components/practice/*.tsx file
2. SwipeFeedbackOverlay imports ONLY MotionValue and useTransform from framer-motion (hooks/types, not JSX components)
3. All 6 migrated files import from '@/components/motion/AdaptiveMotion'
4. AchievementUnlockModal uses border-neo instead of border-2
5. All existing practice tests pass (139+)
6. Build succeeds with zero type errors
7. Lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/43-practice-experience-design-polish/43-04-SUMMARY.md`
</output>
