---
phase: 43-practice-experience-design-polish
plan: 03
type: execute
wave: 2
depends_on: ["43-01", "43-02"]
files_modified:
  - fe-next/translations/en.js
  - fe-next/translations/he.js
  - fe-next/translations/sv.js
  - fe-next/translations/ja.js
autonomous: true

must_haves:
  truths:
    - "Practice modes display translated text in all 4 languages (English, Hebrew, Swedish, Japanese)"
    - "No fallback English strings appear when switching to Hebrew, Swedish, or Japanese locales"
    - "Extended stats labels (time, max streak, hints used) render in the user's selected language"
    - "Build passes with no missing translation warnings"
  artifacts:
    - path: "fe-next/translations/en.js"
      provides: "English translations for new practice polish keys"
      contains: "education.practice.time|education.practice.maxStreak|education.practice.hintsUsed"
    - path: "fe-next/translations/he.js"
      provides: "Hebrew translations for new practice polish keys"
      contains: "education.practice.time|education.practice.maxStreak"
    - path: "fe-next/translations/sv.js"
      provides: "Swedish translations for new practice polish keys"
      contains: "education.practice.time|education.practice.maxStreak"
    - path: "fe-next/translations/ja.js"
      provides: "Japanese translations for new practice polish keys"
      contains: "education.practice.time|education.practice.maxStreak"
  key_links:
    - from: "fe-next/components/practice/PracticeResultsCard.tsx"
      to: "fe-next/translations/en.js"
      via: "t() function calls"
      pattern: "t\\('education\\.practice\\."
---

<objective>
Add translation keys for all new UI text introduced in plans 43-01 and 43-02 across all 4 languages (English, Hebrew, Swedish, Japanese).

Purpose: Maintain translation-first principle. Every string visible to users must be translatable. Final step before phase completion.
Output: All 4 translation files updated with new keys. Build passes.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-practice-experience-design-polish/43-01-SUMMARY.md
@.planning/phases/43-practice-experience-design-polish/43-02-SUMMARY.md

Key files:
@fe-next/translations/en.js
@fe-next/translations/he.js
@fe-next/translations/sv.js
@fe-next/translations/ja.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add translation keys for practice polish to all 4 languages</name>
  <files>
    fe-next/translations/en.js
    fe-next/translations/he.js
    fe-next/translations/sv.js
    fe-next/translations/ja.js
  </files>
  <action>
    **Step 1: Identify all new t() keys introduced in plans 43-01 and 43-02.**

    Read the SUMMARY files from 43-01 and 43-02 to get exact list of new translation keys. Also grep the modified files for t() calls with fallback strings to catch any new keys.

    Expected new keys from plan 43-01 (PracticeResultsCard extended stats + PracticeModeSelector fixes):
    - `education.practice.time` — "Time" (stat label in results card)
    - `education.practice.maxStreak` — "Max Streak" (stat label in results card)
    - `education.practice.hintsUsed` — "Hints Used" (stat label in results card)
    - `education.practice.wordCount` — "words" (word count label in mode selector)
    - `education.practice.sessionsCompleted` — "sessions completed" (session count in mode selector)

    There may be additional keys — check the modified files.

    **Step 2: Add keys to each language file.**

    Add under the `education.practice` section in each translation file:

    English (en.js):
    ```javascript
    time: 'Time',
    maxStreak: 'Max Streak',
    hintsUsed: 'Hints Used',
    wordCount: 'words',
    sessionsCompleted: 'sessions completed',
    ```

    Hebrew (he.js):
    ```javascript
    time: 'זמן',
    maxStreak: 'רצף מקסימלי',
    hintsUsed: 'רמזים שנעשה בהם שימוש',
    wordCount: 'מילים',
    sessionsCompleted: 'אימונים הושלמו',
    ```

    Swedish (sv.js):
    ```javascript
    time: 'Tid',
    maxStreak: 'Basta svit',
    hintsUsed: 'Ledtradar anvanda',
    wordCount: 'ord',
    sessionsCompleted: 'sessioner avklarade',
    ```

    Japanese (ja.js):
    ```javascript
    time: '時間',
    maxStreak: '最大連続正解',
    hintsUsed: '使用したヒント',
    wordCount: '単語',
    sessionsCompleted: '完了したセッション',
    ```

    **Step 3: Verify no orphan keys.**

    Grep all modified practice components for t('...') calls and verify each key exists in en.js.

    IMPORTANT: Add keys in alphabetical order within their section, matching the existing convention in each translation file.
  </action>
  <verify>
    Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run build 2>&1 | tail -10` — build succeeds.
    Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run lint 2>&1 | tail -10` — lint passes.
    Verify keys exist: `grep -c "maxStreak\|hintsUsed\|wordCount\|sessionsCompleted" fe-next/translations/en.js` — should return count >= 4.
    Verify Hebrew: `grep -c "maxStreak\|hintsUsed\|wordCount\|sessionsCompleted" fe-next/translations/he.js` — should return count >= 4.
  </verify>
  <done>
    All new translation keys from plans 43-01 and 43-02 exist in all 4 language files (en, he, sv, ja). Build passes. No hardcoded English strings in modified components — all t() calls resolve to proper translations in every locale.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm run lint` passes
- All new t() keys from plans 43-01 and 43-02 exist in all 4 translation files
- No orphan keys (keys in code but not in translation files)
- Hebrew translations verified as RTL-appropriate
</verification>

<success_criteria>
1. All 4 translation files contain the new keys
2. Hebrew (RTL) translations are contextually appropriate
3. Build and lint pass with no warnings
4. No hardcoded English strings in any modified practice component — all use t() with proper keys
</success_criteria>

<output>
After completion, create `.planning/phases/43-practice-experience-design-polish/43-03-SUMMARY.md`
</output>
