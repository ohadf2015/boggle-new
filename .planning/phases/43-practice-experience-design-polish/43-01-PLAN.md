---
phase: 43-practice-experience-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/components/practice/PracticeResultsCard.tsx
  - fe-next/components/practice/SpellingChallengePractice.tsx
  - fe-next/components/practice/TimedBlitzPractice.tsx
  - fe-next/components/practice/WordMatchingPractice.tsx
  - fe-next/components/practice/PracticeModeSelector.tsx
  - fe-next/components/practice/PracticeHeader.tsx
  - fe-next/components/practice/__tests__/PracticeResultsCard.test.tsx
autonomous: true

must_haves:
  truths:
    - "Practice modes use AdaptiveMotion/AdaptiveAnimatePresence instead of raw motion/AnimatePresence"
    - "Feedback animations have multi-stage micro-delight effects (scale bounce, icon rotation, staggered text)"
    - "PracticeResultsCard displays enhanced session stats (time, streak, hints) when provided"
    - "PracticeModeSelector uses neo-brutalist design tokens (no text-slate-*, no CSS var references)"
    - "Practice progress indicators show clear question count and visual progress bar"
  artifacts:
    - path: "fe-next/components/practice/PracticeResultsCard.tsx"
      provides: "Enhanced session summary with optional extended stats"
      contains: "timeSpent|maxStreak|hintsUsed"
    - path: "fe-next/components/practice/SpellingChallengePractice.tsx"
      provides: "Adaptive-motion upgraded spelling mode"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/TimedBlitzPractice.tsx"
      provides: "Adaptive-motion upgraded blitz mode"
      contains: "AdaptiveMotion|AdaptiveAnimatePresence"
    - path: "fe-next/components/practice/PracticeModeSelector.tsx"
      provides: "Neo-brutalist compliant mode selector"
      contains: "text-neo-white"
  key_links:
    - from: "fe-next/components/practice/SpellingChallengePractice.tsx"
      to: "fe-next/components/motion/AdaptiveMotion.tsx"
      via: "import AdaptiveMotion, AdaptiveAnimatePresence"
      pattern: "AdaptiveMotion|AdaptiveAnimatePresence"
    - from: "fe-next/components/practice/PracticeResultsCard.tsx"
      to: "session stats display"
      via: "optional props rendering"
      pattern: "timeSpent|maxStreak"
---

<objective>
Polish all practice mode components with enhanced feedback animations, AdaptiveMotion migration, extended session summaries, and neo-brutalist compliance.

Purpose: Deliver the "feel" of practice sessions — snappy micro-delight feedback, comprehensive results, and consistent design tokens. This is the UIPOL-03 requirement.
Output: All 6 practice components upgraded with polished animations and consistent design.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-practice-experience-design-polish/43-RESEARCH.md

Key files to read before implementation:
@fe-next/components/motion/AdaptiveMotion.tsx
@fe-next/components/practice/PracticeResultsCard.tsx
@fe-next/components/practice/SpellingChallengePractice.tsx
@fe-next/components/practice/TimedBlitzPractice.tsx
@fe-next/components/practice/WordMatchingPractice.tsx
@fe-next/components/practice/PracticeModeSelector.tsx
@fe-next/components/practice/PracticeHeader.tsx
@fe-next/components/practice/__tests__/PracticeResultsCard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade practice modes to AdaptiveMotion + enhance feedback animations</name>
  <files>
    fe-next/components/practice/SpellingChallengePractice.tsx
    fe-next/components/practice/TimedBlitzPractice.tsx
    fe-next/components/practice/WordMatchingPractice.tsx
  </files>
  <action>
    In SpellingChallengePractice.tsx:
    - Replace `import { motion, AnimatePresence } from 'framer-motion'` with `import { AdaptiveMotion, AdaptiveAnimatePresence } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` with `AdaptiveMotion.div`
    - Replace all `AnimatePresence` with `AdaptiveAnimatePresence`
    - Enhance the feedback display (data-testid="feedback-display") with multi-stage animation:
      - Container: `animate={{ scale: [0, 1.2, 1], opacity: [0, 1, 1] }}` with `transition={{ duration: 0.25, times: [0, 0.6, 1] }}`
      - Icon: add `initial={{ rotate: -180, opacity: 0 }} animate={{ rotate: 0, opacity: 1 }} transition={{ delay: 0.1, duration: 0.2 }}`
      - Text: add `initial={{ x: -10, opacity: 0 }} animate={{ x: 0, opacity: 1 }} transition={{ delay: 0.15, duration: 0.15 }}`
    - Add `animate-neo-shake` class to incorrect feedback container (already has it in definition slot but not on main feedback div)
    - Enhance the progress text display with a visual neo-brutalist progress indicator below the counter text (small bar showing `wordIndex / totalWords`)

    In TimedBlitzPractice.tsx:
    - Replace `import { motion, AnimatePresence } from 'framer-motion'` with `import { AdaptiveMotion, AdaptiveAnimatePresence } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` with `AdaptiveMotion.div`
    - Replace `AnimatePresence` with `AdaptiveAnimatePresence`
    - Enhance combo badge animation: use keyframes `animate={{ scale: [0, 1.3, 1] }}` for punchier pop-in
    - Enhance score display with brief score-pop animation when score changes: wrap in AdaptiveMotion.div with `key={score}` and `animate={{ scale: [1, 1.15, 1] }}` transition `{{ duration: 0.2 }}`

    In WordMatchingPractice.tsx (already uses AdaptiveMotion partially):
    - Verify all motion usage goes through AdaptiveMotion (currently correct)
    - Enhance the progress counter "X / Y" with a small visual progress bar below it, using neo-brutalist styling:
      ```
      <div className="h-1.5 w-24 bg-neo-black/30 rounded-neo overflow-hidden mx-auto mt-1">
        <AdaptiveMotion.div className="h-full bg-neo-cyan" animate={{ width: `${(matchedPairs.size / words.length) * 100}%` }} transition={{ duration: 0.3 }} />
      </div>
      ```

    IMPORTANT: Keep all existing data-testid attributes. Keep all existing t() translation calls. Do NOT change component interfaces or hook contracts. Only enhance visual presentation.
  </action>
  <verify>
    Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/__tests__/SpellingChallengePractice.test components/practice/__tests__/TimedBlitzPractice.test components/practice/__tests__/WordMatchingPractice.test 2>&1 | tail -20`
    Verify no imports of `motion` or `AnimatePresence` from `framer-motion` remain in these 3 files (only AdaptiveMotion imports).
    Run `grep -n "from 'framer-motion'" fe-next/components/practice/SpellingChallengePractice.tsx fe-next/components/practice/TimedBlitzPractice.tsx` — should return empty.
  </verify>
  <done>
    All 3 practice mode components use AdaptiveMotion/AdaptiveAnimatePresence exclusively. Feedback animations have multi-stage micro-delight effects. Visual progress indicators added. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance PracticeResultsCard with extended stats + fix PracticeModeSelector design</name>
  <files>
    fe-next/components/practice/PracticeResultsCard.tsx
    fe-next/components/practice/PracticeModeSelector.tsx
    fe-next/components/practice/__tests__/PracticeResultsCard.test.tsx
  </files>
  <action>
    In PracticeResultsCard.tsx:
    - Replace `import { motion } from 'framer-motion'` with `import { AdaptiveMotion } from '@/components/motion/AdaptiveMotion'`
    - Replace all `motion.div` and `motion.p` with `AdaptiveMotion.div` and `AdaptiveMotion.p`
    - Extend `PracticeResultsCardProps` interface with optional enhanced fields:
      ```typescript
      timeSpent?: number;          // Total session time in seconds
      maxStreak?: number;          // Best streak achieved
      hintsUsed?: number;          // Hints consumed during session
      ```
    - After the encouragement message (delay 0.7) and before action buttons (delay 0.8), add a stats grid section at delay 0.75:
      ```
      {(timeSpent || (maxStreak && maxStreak > 1) || (hintsUsed !== undefined && hintsUsed > 0)) && (
        <AdaptiveMotion.div
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.75 }}
          className="mt-4 grid grid-cols-2 gap-3"
        >
          {timeSpent !== undefined && timeSpent > 0 && (
            <div className="p-3 bg-neo-navy/50 border-neo border-neo-black rounded-neo">
              <p className="text-xs text-neo-white/60 font-neo-body">{t('education.practice.time') || 'Time'}</p>
              <p className="text-lg text-neo-cyan font-neo-display">
                {Math.floor(timeSpent / 60)}:{(timeSpent % 60).toString().padStart(2, '0')}
              </p>
            </div>
          )}
          {maxStreak !== undefined && maxStreak > 1 && (
            <div className="p-3 bg-neo-navy/50 border-neo border-neo-black rounded-neo">
              <p className="text-xs text-neo-white/60 font-neo-body">{t('education.practice.maxStreak') || 'Max Streak'}</p>
              <p className="text-lg text-neo-yellow font-neo-display">{maxStreak}x</p>
            </div>
          )}
          {hintsUsed !== undefined && hintsUsed > 0 && (
            <div className="p-3 bg-neo-navy/50 border-neo border-neo-black rounded-neo">
              <p className="text-xs text-neo-white/60 font-neo-body">{t('education.practice.hintsUsed') || 'Hints Used'}</p>
              <p className="text-lg text-neo-purple font-neo-display">{hintsUsed}</p>
            </div>
          )}
        </AdaptiveMotion.div>
      )}
      ```
    - Adjust action buttons delay from 0.8 to 0.85 to account for stats section

    In PracticeModeSelector.tsx:
    - Replace ALL instances of `text-slate-400` with `text-neo-white/60` (there are ~3 occurrences: lesson name subtitle, word count "words" label, practice mode descriptions)
    - Replace `text-slate-500` with `text-neo-white/40` (session count display)
    - Replace `text-[var(--neo-cyan)]` with `text-neo-cyan` and `bg-[var(--neo-cyan)]` with `bg-neo-cyan` for matching mode
    - Replace `text-[var(--neo-purple)]` with `text-neo-purple` and `bg-[var(--neo-purple)]` with `bg-neo-purple` for spelling mode
    - Replace `text-[var(--neo-red)]` with `text-neo-red` and `bg-[var(--neo-red)]` with `bg-neo-red` for blitz mode (or `text-neo-pink` if neo-red doesn't exist)
    - Replace `"text-slate-400 hover:text-neo-white"` on back button with `"text-neo-white/70 hover:text-neo-white hover:bg-neo-white/10"`
    - Replace hardcoded `{wordCount === 1 ? 'word' : 'words'}` with `{t('education.practice.wordCount') || 'words'}`
    - Replace hardcoded `{getSessionCount(option.type)} sessions completed` with `{getSessionCount(option.type)} {t('education.practice.sessionsCompleted') || 'sessions completed'}`

    In PracticeResultsCard test:
    - Update tests if any existing assertions break due to AdaptiveMotion migration
    - Add test: "renders extended stats when provided" — pass timeSpent={125}, maxStreak={5}, hintsUsed={2} and verify they render
    - Add test: "does not render stats grid when no extended props provided" — verify stats grid not in DOM

    IMPORTANT: PracticeResultsCardProps new fields are ALL optional. Existing callers (WordMatchingPractice, SpellingChallengePractice, TimedBlitzPractice) should NOT be modified to pass these new props yet — that can be done incrementally later. The interface just needs to support them.
  </action>
  <verify>
    Run `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/__tests__/PracticeResultsCard.test 2>&1 | tail -20`
    Run `grep -n "slate" fe-next/components/practice/PracticeModeSelector.tsx` — should return empty.
    Run `grep -n "var(--neo" fe-next/components/practice/PracticeModeSelector.tsx` — should return empty.
    Run `grep -n "from 'framer-motion'" fe-next/components/practice/PracticeResultsCard.tsx` — should return empty.
  </verify>
  <done>
    PracticeResultsCard accepts and displays optional timeSpent, maxStreak, hintsUsed stats. PracticeModeSelector uses only neo-* design tokens with no slate or CSS variable references. All animations use AdaptiveMotion. Tests pass including new stats rendering tests.
  </done>
</task>

</tasks>

<verification>
- `cd /Users/ohadfisher/git/boggle-new/fe-next && npx jest --passWithNoTests components/practice/ 2>&1 | tail -30` — all practice tests pass
- `grep -rn "from 'framer-motion'" fe-next/components/practice/*.tsx` — only AdaptiveMotion imports remain (PracticeHeader may still use motion if not in scope, that's OK)
- `grep -rn "text-slate" fe-next/components/practice/PracticeModeSelector.tsx` — no results
- `cd /Users/ohadfisher/git/boggle-new/fe-next && npm run build 2>&1 | tail -10` — build succeeds
</verification>

<success_criteria>
1. All 3 practice modes (Spelling, Blitz, Matching) use AdaptiveMotion/AdaptiveAnimatePresence exclusively
2. Feedback animations have multi-stage effects (bounce, rotation, stagger)
3. PracticeResultsCard interface supports extended session stats
4. PracticeModeSelector uses only neo-brutalist design tokens
5. All existing tests pass, new stats tests added
</success_criteria>

<output>
After completion, create `.planning/phases/43-practice-experience-design-polish/43-01-SUMMARY.md`
</output>
