---
phase: 47-tile-reworks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/__tests__/useBlastGame.rainbowBoost.test.ts
  - fe-next/components/blast/types.ts
autonomous: true

must_haves:
  truths:
    - "Rainbow Boost in a word with another special copies and doubles that special's effect"
    - "Rainbow Boost alone in a word (no other specials) doubles the word score"
    - "Rainbow Boost picks the best special when multiple specials exist in the word"
  artifacts:
    - path: "fe-next/components/blast/__tests__/useBlastGame.rainbowBoost.test.ts"
      provides: "TDD tests for Rainbow Boost behavior"
      min_lines: 80
    - path: "fe-next/components/blast/hooks/useBlastGame.ts"
      provides: "Rainbow Boost implementation in clearTilesForWord"
      contains: "case 'rainbow'"
    - path: "fe-next/components/blast/types.ts"
      provides: "RAINBOW_BOOST_MULTIPLIER constant"
      contains: "RAINBOW_BOOST_MULTIPLIER"
  key_links:
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "fe-next/components/blast/types.ts"
      via: "import RAINBOW_BOOST_MULTIPLIER"
      pattern: "RAINBOW_BOOST_MULTIPLIER"
---

<objective>
Rework Rainbow tile into "Rainbow Boost" — an amplifier that copies and doubles the best special tile effect in the word. Solo Rainbow Boost doubles word score.

Purpose: Rainbow was a flat +5 bonus — boring. Rainbow Boost becomes the "universal enhancer" that makes every special tile combination exciting.
Output: Working Rainbow Boost with TDD tests proving all three behaviors (amplify best, solo double, multi-special selection).
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-foundation-unified-tile-types-bug-fixes/46-02-SUMMARY.md
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/types.ts
@fe-next/shared/types/blast.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for Rainbow Boost behavior</name>
  <files>fe-next/components/blast/__tests__/useBlastGame.rainbowBoost.test.ts, fe-next/components/blast/types.ts</files>
  <action>
Create test file `useBlastGame.rainbowBoost.test.ts` following the same mock pattern as `useBlastGame.chainPropagation.test.ts` and `useBlastGame.stateScoring.test.ts`.

Add constant `RAINBOW_BOOST_MULTIPLIER = 2` to `types.ts` (exported alongside existing constants).

Test cases (all should FAIL initially since `case 'rainbow'` still uses flat RAINBOW_BONUS):

1. **Rainbow Boost + Bomb**: Word path contains rainbow + bomb tiles. After clearTilesForWord, bomb detonation area should be doubled (two bomb detonation passes — the original plus a copy). Verify bomb explosions count or bonus score exceeds single-bomb baseline by 2x.

2. **Rainbow Boost + Lightning**: Word path contains rainbow + lightning. Lightning column-clear fires twice (two full column sweeps). Verify cleared tile count in the column is consistent with double activation.

3. **Rainbow Boost + Gold (gold is NOT amplified)**: Word path contains rainbow + gold + standard tiles only (no offensive specials). Gold is a score MULTIPLIER, not an explosion effect — Rainbow does NOT amplify it. Rainbow falls back to solo behavior (doubles word score), then gold multiplier applies separately on top. Verify final score = baseScore * RAINBOW_BOOST_MULTIPLIER * GOLD_MULTIPLIER (not GOLD_MULTIPLIER * RAINBOW_BOOST_MULTIPLIER^2).

4. **Rainbow Boost solo (no other specials)**: Word path contains rainbow + standard tiles only. Word score should be doubled (baseScore * 2). Verify final score = (wordLength + bonusScore) * 2.

5. **Rainbow Boost picks best offensive special (ignores gold)**: Word path contains rainbow + gold + bomb. Rainbow amplifies bomb (offensive effect) not gold (score multiplier). Verify bomb fires twice AND gold multiplier applies separately at end. Final score = (baseScore + doubleBombBonus) * GOLD_MULTIPLIER.

6. **Rainbow Boost with Prism**: Word path contains rainbow + prism. Prism cross-clear fires twice. Verify explosion count and bonus score reflect double prism activation.

Each test sets up a known grid, calls the word submission logic, and asserts on the resulting tile states, explosions, and scores.
  </action>
  <verify>Run `npx jest useBlastGame.rainbowBoost --no-coverage` — all tests should FAIL (RED phase).</verify>
  <done>6+ test cases exist and fail because Rainbow Boost logic is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Rainbow Boost in clearTilesForWord</name>
  <files>fe-next/components/blast/hooks/useBlastGame.ts</files>
  <action>
Rewrite `case 'rainbow'` in the `clearTilesForWord` switch statement inside `useBlastGame.ts`.

**Algorithm:**

1. **First pass (collect):** Before the main switch statement processes special effects, scan the entire word path and collect all special tile types present (excluding rainbow itself, standard, and all score-multiplier types: gold, silver, diamond).

2. **Determine best special:** Rank specials by impact (offensive specials only): prism > lightning > bomb > gem > magnet. Exclude ice/frozen AND gold/silver/diamond from the ranking — Rainbow only amplifies offensive specials that have an explosion/clear effect. Gold tiers are score MULTIPLIERS (not explosion effects), so they are never candidates for Rainbow amplification; they apply separately at the end of scoring. Ice has no explosion (only a flat ICE_CLEAR_BONUS), so doubling it is ambiguous and excluded. If multiple specials of same rank, pick first encountered.

3. **In `case 'rainbow'`:**
   - If `bestSpecial` exists: Re-execute that special's switch case logic a second time (the "copy"), effectively doubling its effect. The original special still fires normally from its own case. So rainbow causes a SECOND firing.
   - If no other specials in path (`bestSpecial === null`): Set a flag `rainbowSoloMultiplier = RAINBOW_BOOST_MULTIPLIER` that is applied to the final word score calculation at the end of the function.

4. **Score application:** At the end of `clearTilesForWord`, after all effects:
   - If `rainbowSoloMultiplier > 1`: `totalScore = baseScore * rainbowSoloMultiplier`
   - Gold multiplier applies separately (multiplicative with rainbow): `finalScore = totalScore * goldMultiplier`

Import `RAINBOW_BOOST_MULTIPLIER` from `../types`.

Remove the old `bonusScore += RAINBOW_BONUS` line. Remove `RAINBOW_BONUS` import if no longer used.

**Important:** The "copy" execution should use the SAME explosion/bomb/lightning infrastructure (bombQueue, processedBombs, processedLightning, etc.) to maintain chain propagation correctness from Phase 46 fixes.
  </action>
  <verify>Run `npx jest useBlastGame.rainbowBoost --no-coverage` — all tests should PASS (GREEN phase). Then run `npx jest --testPathPattern="blast" --no-coverage` to verify no regressions.</verify>
  <done>Rainbow Boost amplifies best special (2x effect) or doubles word score when solo. All rainbow boost tests pass. All existing blast tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx jest useBlastGame.rainbowBoost --no-coverage  # All rainbow boost tests pass
npx jest --testPathPattern="blast" --no-coverage    # No regressions in blast suite
```
</verification>

<success_criteria>
- Rainbow Boost + special = that special fires twice (copy + original)
- Rainbow Boost solo = 2x word score
- Rainbow Boost picks highest-impact special when multiple present
- All existing blast tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/47-tile-reworks/47-01-SUMMARY.md`
</output>
