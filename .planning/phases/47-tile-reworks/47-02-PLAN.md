---
phase: 47-tile-reworks
plan: 02
type: tdd
wave: 2
depends_on: ["47-01"]
files_modified:
  - fe-next/components/blast/hooks/useBlastGame.ts
  - fe-next/components/blast/__tests__/useBlastGame.treasureGem.test.ts
  - fe-next/components/blast/types.ts
  - fe-next/components/blast/utils/blastTileUtils.ts
  - fe-next/shared/types/blast.ts
autonomous: true

must_haves:
  truths:
    - "Treasure Gem tracks shard count (3 hits to complete) persisting across words"
    - "Completed Treasure Gem spawns 2 random specials on the board and awards +25 bonus"
    - "Each hit on Treasure Gem visually increments shard count (activationEffect reflects progress)"
  artifacts:
    - path: "fe-next/components/blast/__tests__/useBlastGame.treasureGem.test.ts"
      provides: "TDD tests for Treasure Gem shard collection"
      min_lines: 80
    - path: "fe-next/components/blast/hooks/useBlastGame.ts"
      provides: "Treasure Gem implementation in clearTilesForWord"
      contains: "TREASURE_GEM"
    - path: "fe-next/components/blast/types.ts"
      provides: "TREASURE_GEM constants"
      contains: "TREASURE_GEM_COMPLETION_BONUS"
  key_links:
    - from: "fe-next/components/blast/hooks/useBlastGame.ts"
      to: "fe-next/components/blast/types.ts"
      via: "import TREASURE_GEM constants"
      pattern: "TREASURE_GEM"
---

<objective>
Rework Gem tile into "Treasure Gem" — a 3-hit shard collector. Each hit drops a visible shard; completing all 3 gives a mega bonus (+25) and spawns 2 random special tiles on the board.

Purpose: Old gem was a flat bonus on collection. Treasure Gem creates anticipation — players actively plan to complete the 3-hit sequence for the big payoff.
Output: Working Treasure Gem with TDD tests proving shard tracking, completion reward, and special spawning.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tile-reworks/47-01-SUMMARY.md
@fe-next/components/blast/hooks/useBlastGame.ts
@fe-next/components/blast/types.ts
@fe-next/components/blast/utils/blastTileUtils.ts
@fe-next/shared/types/blast.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for Treasure Gem shard mechanics</name>
  <files>fe-next/components/blast/__tests__/useBlastGame.treasureGem.test.ts, fe-next/components/blast/types.ts</files>
  <action>
Add constants to `types.ts`:
- `TREASURE_GEM_COMPLETION_BONUS = 25`
- `TREASURE_GEM_SPAWN_COUNT = 2` (number of random specials spawned on completion)
- `TREASURE_GEM_HITS_REQUIRED = 3`

Gem already has `hitsRemaining: 3` in `blastTileUtils.ts` — keep that as-is.

Create test file following existing blast test patterns. Test cases:

1. **First hit (shard 1)**: Word path includes gem tile. After submission, gem.hitsRemaining decreases from 3→2. Gem is NOT cleared. activationEffect = 'gem-shard-1'.

2. **Second hit (shard 2)**: Gem at hitsRemaining=2 included in word. Decreases to 1. NOT cleared. activationEffect = 'gem-shard-2'.

3. **Third hit (completion)**: Gem at hitsRemaining=1 included in word. hitsRemaining→0, tile IS cleared. Bonus score includes TREASURE_GEM_COMPLETION_BONUS (25). activationEffect = 'gem-complete'.

4. **Completion spawns 2 random specials**: After gem completion, 2 previously-standard tiles on the board are converted to random special types (not standard, not gem). Verify exactly 2 tiles changed type. The spawned specials MUST respect wave-enabled flags (use `getWaveDistribution` to filter eligible types). In particular, diamond must never spawn before wave 4.

4b. **Treasure Gem never spawns diamond in wave 1**: Set wave=1, complete a Treasure Gem. Verify spawned specials do NOT include 'diamond' (diamond is wave 4+ only).

5. **Shard state persists across words**: Hit gem in word 1 (3→2), submit different word (gem untouched, stays at 2), hit gem in word 3 (2→1). Verify state persistence.

6. **Per-hit bonus removed**: Old GEM_USE_BONUS no longer awarded on non-final hits. Only TREASURE_GEM_COMPLETION_BONUS on final hit.

All tests should FAIL initially since current `case 'gem'` gives flat bonus without shard tracking or spawning.
  </action>
  <verify>Run `npx jest useBlastGame.treasureGem --no-coverage` — all tests should FAIL (RED phase).</verify>
  <done>6 test cases exist and fail because Treasure Gem logic is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Treasure Gem shard collector</name>
  <files>fe-next/components/blast/hooks/useBlastGame.ts, fe-next/components/blast/utils/blastTileUtils.ts</files>
  <action>
Modify `clearTilesForWord` in `useBlastGame.ts`:

**Multi-hit handling (already exists for gem):** The existing `isMultiHitAlive` check handles hits 3→2→1. Modify the multi-hit section to set shard-specific activationEffect:
- `hitsRemaining 3→2`: `activationEffect = 'gem-shard-1'`
- `hitsRemaining 2→1`: `activationEffect = 'gem-shard-2'`
- Remove the old `if (tile.type === 'gem') bonusScore += GEM_USE_BONUS;` from non-final hits.

**Final hit (`case 'gem'`):** Rewrite to:
1. Set `activationEffect = 'gem-complete'`
2. Award `TREASURE_GEM_COMPLETION_BONUS` (not old GEM_USE_BONUS + GEM_COLLECT_BONUS)
3. Push gem-complete explosion
4. **Spawn 2 random specials:** After the main path loop (but before bomb BFS), find 2 random non-cleared standard tiles on the board (`next[r][c].type === 'standard' && !next[r][c].isCleared`). Convert them to random special types using `rollSpecialFromDistribution` **filtered by the current wave's enabled flags** (pass `getWaveDistribution(getWaveConfig(currentWave))` so diamond/mirror/vortex/etc. only spawn if wave-unlocked). Set their `hitsRemaining` via `getInitialHitsRemaining`. Track spawned positions for animation events.

Import new constants from `../types`. Remove `GEM_USE_BONUS` and `GEM_COLLECT_BONUS` imports if no longer used anywhere.

Keep `getInitialHitsRemaining('gem')` returning 3 in `blastTileUtils.ts` — no change needed there.
  </action>
  <verify>Run `npx jest useBlastGame.treasureGem --no-coverage` — all tests pass (GREEN). Then `npx jest --testPathPattern="blast" --no-coverage` for no regressions.</verify>
  <done>Treasure Gem tracks 3 shards, awards +25 on completion, spawns 2 random specials. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx jest useBlastGame.treasureGem --no-coverage  # All treasure gem tests pass
npx jest --testPathPattern="blast" --no-coverage   # No regressions
```
</verification>

<success_criteria>
- Gem tracks 3-hit shard progression with distinct activationEffects per shard
- Completion awards +25 bonus and spawns exactly 2 random specials
- Shard state persists across word submissions
- No per-hit bonus on non-final hits (old GEM_USE_BONUS removed)
- All existing blast tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-tile-reworks/47-02-SUMMARY.md`
</output>
