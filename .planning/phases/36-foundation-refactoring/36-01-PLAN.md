---
phase: 36-foundation-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe-next/lib/supabase/education/types.ts
  - fe-next/lib/supabase/education/classrooms.ts
  - fe-next/lib/supabase/education/lessons.ts
  - fe-next/lib/supabase/education/progress.ts
  - fe-next/lib/supabase/education/assignments.ts
  - fe-next/lib/supabase/education/leaderboard.ts
  - fe-next/lib/supabase/education/curriculum.ts
  - fe-next/lib/supabase/education/duels.ts
  - fe-next/lib/supabase/education/practice.ts
  - fe-next/lib/supabase/education/index.ts
autonomous: true

must_haves:
  truths:
    - "All functions from teacher.ts exist in new modular files under lib/supabase/education/"
    - "Barrel export re-exports every public function and type from all sub-modules"
    - "Each module file is under 500 lines"
    - "TypeScript compilation succeeds with new module structure"
  artifacts:
    - path: "fe-next/lib/supabase/education/index.ts"
      provides: "Barrel export for all education modules"
      contains: "export * from"
    - path: "fe-next/lib/supabase/education/types.ts"
      provides: "Shared types (Classroom, VocabularyLesson, StudentLessonProgress, etc.)"
      exports: ["Language", "Classroom", "VocabularyWord", "VocabularyLesson", "WordAttempt", "StudentLessonProgress", "LessonAssignment", "ClassroomWithMembers", "ClassroomStudent"]
    - path: "fe-next/lib/supabase/education/classrooms.ts"
      provides: "Classroom CRUD + join + student queries"
      exports: ["getClassrooms", "getClassroom", "createClassroom", "updateClassroom", "deleteClassroom", "joinClassroom", "getStudentClassroom", "getClassroomStudents"]
    - path: "fe-next/lib/supabase/education/lessons.ts"
      provides: "Lesson CRUD"
      exports: ["getLessons", "getLesson", "createLesson", "updateLesson", "deleteLesson"]
    - path: "fe-next/lib/supabase/education/progress.ts"
      provides: "Student progress tracking"
      exports: ["getStudentProgress", "getClassProgress", "updateProgress"]
    - path: "fe-next/lib/supabase/education/assignments.ts"
      provides: "Lesson assignment operations"
      exports: ["assignLesson", "getStudentAssignedLessons"]
    - path: "fe-next/lib/supabase/education/leaderboard.ts"
      provides: "Classroom leaderboard"
      exports: ["getClassroomLeaderboard", "LeaderboardEntry", "ClassroomLeaderboardData"]
    - path: "fe-next/lib/supabase/education/curriculum.ts"
      provides: "Curriculum word list operations"
      exports: ["getCurriculumWordLists", "getCurriculumWordList", "importCurriculumToLesson", "GradeLevel", "CurriculumSubject", "CurriculumWordList", "CurriculumWordListFilters"]
    - path: "fe-next/lib/supabase/education/duels.ts"
      provides: "Duel function stubs for Phase 38/39"
    - path: "fe-next/lib/supabase/education/practice.ts"
      provides: "Practice session stubs for Phase 37"
  key_links:
    - from: "fe-next/lib/supabase/education/index.ts"
      to: "all sub-modules"
      via: "export * from './module'"
      pattern: "export \\* from '\\./classrooms'"
    - from: "fe-next/lib/supabase/education/classrooms.ts"
      to: "fe-next/lib/supabase/education/types.ts"
      via: "type imports"
      pattern: "import.*from './types'"
---

<objective>
Extract all functions and types from `lib/supabase/teacher.ts` (1260 lines) into modular files under `lib/supabase/education/` with barrel export pattern.

Purpose: Reduce file size to meet 500-line limit, establish modular structure for education feature development (duels, practice modes), and create foundation stubs for Phase 37-39.
Output: 10 new files in `lib/supabase/education/` directory. Original `teacher.ts` remains temporarily (removed in Plan 02).
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@fe-next/lib/supabase/teacher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create education module files with extracted functions</name>
  <files>
    fe-next/lib/supabase/education/types.ts
    fe-next/lib/supabase/education/classrooms.ts
    fe-next/lib/supabase/education/lessons.ts
    fe-next/lib/supabase/education/progress.ts
    fe-next/lib/supabase/education/assignments.ts
    fe-next/lib/supabase/education/leaderboard.ts
    fe-next/lib/supabase/education/curriculum.ts
  </files>
  <action>
Create `lib/supabase/education/` directory with 7 module files by extracting from `teacher.ts`:

**types.ts** — Extract ALL type/interface/type-alias exports from teacher.ts:
- `Language`, `Classroom`, `VocabularyWord`, `VocabularyLesson`, `WordAttempt`, `StudentLessonProgress`, `LessonAssignment`, `ClassroomWithMembers`, `ClassroomStudent`, `LeaderboardEntry`, `ClassroomLeaderboardData`, `GradeLevel`, `CurriculumSubject`, `CurriculumWordList`, `CurriculumWordListFilters`
- Also extract the private helper functions `containsHebrew` and `normalizeForStorage` into types.ts (they're utility functions used by progress.ts). Export them so progress.ts can import them.
- Import `normalizeWord`, `normalizeHebrewWord` from `@/shared/utils/wordNormalization`

**classrooms.ts** — Extract functions (lines 112-468 of teacher.ts):
- `getClassrooms`, `getClassroom`, `createClassroom`, `updateClassroom`, `deleteClassroom`, `joinClassroom`, `getStudentClassroom`, `getClassroomStudents`
- Import `supabase` from `@/lib/supabase`, `logger` from `@/utils/logger`
- Import types from `./types`

**lessons.ts** — Extract functions (lines 477-616):
- `getLessons`, `getLesson`, `createLesson`, `updateLesson`, `deleteLesson`
- Same imports pattern as classrooms.ts

**progress.ts** — Extract functions (lines 625-810):
- `getStudentProgress`, `getClassProgress`, `updateProgress`
- Import `normalizeForStorage` from `./types` (or create a local utility)
- Import types from `./types`

**assignments.ts** — Extract functions (lines 819-894):
- `assignLesson`, `getStudentAssignedLessons`

**leaderboard.ts** — Extract functions (lines 900-1094):
- `getClassroomLeaderboard`
- Import `LeaderboardEntry`, `ClassroomLeaderboardData` from `./types`

**curriculum.ts** — Extract functions (lines 1100-1260):
- `getCurriculumWordLists`, `getCurriculumWordList`, `importCurriculumToLesson`
- Import `GradeLevel`, `CurriculumSubject`, `CurriculumWordList`, `CurriculumWordListFilters` from `./types`

**IMPORTANT:** Preserve exact function signatures, JSDoc comments, error handling patterns. Each file imports `supabase` from `@/lib/supabase` and `logger` from `@/utils/logger`. Each file imports needed types from `./types`.
  </action>
  <verify>
    Run `npx tsc --noEmit` from fe-next/ to verify no type errors in new files.
    Verify each file is under 500 lines: `wc -l fe-next/lib/supabase/education/*.ts`
    Verify internal module wiring — each domain module imports from shared types: `grep "from './types'" fe-next/lib/supabase/education/classrooms.ts fe-next/lib/supabase/education/lessons.ts fe-next/lib/supabase/education/progress.ts fe-next/lib/supabase/education/assignments.ts fe-next/lib/supabase/education/leaderboard.ts fe-next/lib/supabase/education/curriculum.ts` — all 6 files should show type imports.
  </verify>
  <done>7 module files exist under lib/supabase/education/ with all functions extracted from teacher.ts. Each file under 500 lines. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel export, duel/practice stubs</name>
  <files>
    fe-next/lib/supabase/education/index.ts
    fe-next/lib/supabase/education/duels.ts
    fe-next/lib/supabase/education/practice.ts
  </files>
  <action>
**index.ts** — Barrel export re-exporting everything:
```typescript
export * from './types';
export * from './classrooms';
export * from './lessons';
export * from './progress';
export * from './assignments';
export * from './leaderboard';
export * from './curriculum';
export * from './duels';
export * from './practice';
```

**duels.ts** — Foundation stubs for Phase 38/39:
```typescript
/**
 * Duel Operations
 * Foundation stubs for Phase 38 (Async Duels) and Phase 39 (Real-Time Duels)
 * TODO: Implement in Phase 38/39
 */

// Placeholder types for duel feature development
export interface DuelChallenge {
  id: string;
  challengerId: string;
  opponentId: string;
  classroomId: string;
  lessonId: string;
  duelType: 'async' | 'realtime';
  status: 'pending' | 'active' | 'completed' | 'cancelled';
}

// Stub functions — will be implemented in Phase 38
// Exported so they appear in barrel export and can be imported early
```

**practice.ts** — Foundation stubs for Phase 37:
```typescript
/**
 * Practice Session Operations
 * Foundation stubs for Phase 37 (Practice Modes)
 * TODO: Implement in Phase 37
 */

export interface PracticeSession {
  id: string;
  studentId: string;
  lessonId: string;
  mode: 'matching' | 'spelling' | 'blitz';
  score: number;
  completedAt: string | null;
}

// Stub functions — will be implemented in Phase 37
```

After creating all files, verify the barrel export works by checking TypeScript compilation:
`cd fe-next && npx tsc --noEmit`
  </action>
  <verify>
    Run `cd fe-next && npx tsc --noEmit` — no errors.
    Verify `import { getClassrooms, DuelChallenge, PracticeSession } from '@/lib/supabase/education'` resolves correctly.
  </verify>
  <done>Barrel export re-exports all functions and types from all sub-modules. Duel and practice stubs exist with placeholder types. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. All 10 files exist in `fe-next/lib/supabase/education/`
2. `npx tsc --noEmit` passes (no type errors)
3. Each file under 500 lines
4. Every exported function from teacher.ts has a corresponding export in the new modules
5. Barrel export re-exports everything
</verification>

<success_criteria>
- 10 new files created under lib/supabase/education/
- All functions from teacher.ts extracted into appropriate modules
- Types centralized in types.ts
- Barrel export works — importing from `@/lib/supabase/education` provides all symbols
- Each file under 500 lines
- TypeScript compilation succeeds
- Original teacher.ts NOT modified yet (that's Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/36-foundation-refactoring/36-01-SUMMARY.md`
</output>
