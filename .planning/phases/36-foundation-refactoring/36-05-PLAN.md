---
phase: 36-foundation-refactoring
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md
  - fe-next/backend/modules/educationXpManager.ts
autonomous: true

must_haves:
  truths:
    - "XP economy spreadsheet models ALL existing + new activity sources with XP/session and XP/day estimates"
    - "New activity XP rates are balanced against existing rates (no mode earns 2x+ another)"
    - "Target XP/day documented to ensure students level up at intended pace"
    - "educationXpManager.ts has placeholder config entries for new practice modes and duels"
  artifacts:
    - path: ".planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md"
      provides: "Complete XP economy spreadsheet with balancing analysis"
      contains: "Activity.*XP/Session.*Sessions/Day"
    - path: "fe-next/backend/modules/educationXpManager.ts"
      provides: "Updated config with placeholder entries for duel and practice XP"
      contains: "DUEL_WIN"
  key_links:
    - from: ".planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md"
      to: "fe-next/backend/modules/educationXpManager.ts"
      via: "XP values derived from model and applied to config"
      pattern: "DUEL.*XP"
---

<objective>
Create XP economy model spreadsheet and add placeholder XP config entries to educationXpManager.ts for new duel and practice activities.

Purpose: Prevent XP inflation when Phase 37/38/39 add new activity types. Without a unified model, each phase would set XP independently, causing imbalanced progression rates. The model ensures students level up at consistent pace regardless of which activities they focus on.
Output: XP economy document + updated educationXpManager.ts with new activity config entries.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-foundation-refactoring/36-RESEARCH.md
@fe-next/backend/modules/educationXpManager.ts
@fe-next/backend/modules/xpManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XP economy model document</name>
  <files>.planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md</files>
  <action>
Create a comprehensive XP economy model document at `.planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md`.

**Document structure:**

```markdown
# XP Economy Model — Education 2.0

## Purpose
Unified model of all XP sources to prevent inflation when adding duels and practice modes.

## Design Principles
1. **Mastery over speed** — XP rewards learning quality, not grinding
2. **Mode parity** — Similar XP/hour across all practice modes (no mode favoritism)
3. **Anti-inflation** — New activities don't stack with existing XP (award once per activity type)
4. **Progression target** — Students level up every 3-4 days at early levels with daily practice

## Current XP Sources (from educationXpManager.ts)

| Activity | XP/Session | Typical Sessions/Day | XP/Day | Source |
|----------|-----------|---------------------|--------|--------|
| Flashcards (20 cards, 90% accuracy) | ~250 | 2 | ~500 | FLASHCARD_CORRECT + accuracy bonus |
| Solo Board (10 vocab words) | ~215 | 2 | ~430 | VOCABULARY_WORD_FOUND + board completion |
| Lesson Completion | 300 | 0.5 | ~150 | LESSON_COMPLETED + mastery bonus |
| Daily Practice Base | 20 | 1 | 20 | DAILY_PRACTICE_BASE |
| **Current Total** | | | **~1100** | |

### Current XP/Session Breakdown
- Flashcards: 10/correct * 18 cards + 50 accuracy bonus + 20 daily = ~250
- Solo Board: 15/vocab word * 10 words + 25 new word bonus * 3 + 50 completion + 20 daily = ~215
- Lesson Completion: 200 base + 100 mastery bonus = 300
- Streak bonuses: +50% at 7 days, +75% at 14 days, +100% at 30 days (multiplicative on session XP)

## New XP Sources (Phase 37-40)

| Activity | XP/Session | Typical Sessions/Day | XP/Day | Rationale |
|----------|-----------|---------------------|--------|-----------|
| **Async Duel (win)** | 200 | 1 | 200 | Similar to solo board (competitive board play). Winner gets full, loser gets 60% (120 XP). |
| **Async Duel (loss)** | 120 | 0 | 0 | Losing still rewards participation (60% of win). Counted in opponent's session. |
| **Real-time Duel (win)** | 250 | 0.5 | 125 | Premium: synchronous commitment. Winner 250, loser 150. |
| **Real-time Duel (loss)** | 150 | 0 | 0 | Higher than async loss (both players committed time). |
| **Word Matching** | 180 | 1 | 180 | Mid-tier: visual learning. 15 XP/pair * ~10 pairs + accuracy bonus. |
| **Spelling Challenge** | 200 | 1 | 200 | Higher: harder mode. 20 XP/correct * ~8 words + streak bonus. |
| **Timed Blitz** | 160 | 1 | 160 | Lower: speed-focused, less mastery. 10 XP/word + combo bonus. |
| **Daily Challenge** | 100 | 1 | 100 | Capped bonus. Encourages daily login without requiring it. |
| **New Total** | | | **~965** | |

## Combined Economy

| Source Category | Max XP/Day | % of Total |
|----------------|-----------|------------|
| Existing (flashcards, board, lessons) | ~1100 | 53% |
| Duels (async + realtime) | ~325 | 16% |
| Practice modes (matching, spelling, blitz) | ~540 | 26% |
| Daily challenges | ~100 | 5% |
| **Grand Total** | **~2065** | 100% |

## Progression Rate Analysis

### Level Curve
Using existing formula: `XP_needed = 100 * level^1.5`
- Level 1→2: 100 XP (< 1 day)
- Level 5→6: 1,118 XP (~0.5 days)
- Level 10→11: 3,162 XP (~1.5 days)
- Level 20→21: 8,944 XP (~4.3 days)
- Level 50→51: 35,355 XP (~17 days)

### With Streak Multipliers
At 30-day streak (2x multiplier): ~4,130 XP/day
- Level 20→21: ~2.2 days (from 4.3)
- Level 50→51: ~8.5 days (from 17)

### Assessment
- **Early levels (1-10):** Students level up daily — good for engagement hook
- **Mid levels (10-30):** Every 2-5 days — maintains momentum
- **Late levels (30-50):** Every 1-2 weeks — challenge without frustration
- **Endgame (50-100):** Weeks to months — prestige provides reset

**Verdict:** Balanced. No single mode dominates. A student playing ONLY duels (~325 XP/day) or ONLY practice (~540 XP/day) progresses at reasonable pace.

## Anti-Inflation Rules

1. **No double-counting:** Duel awards "duel XP", not "duel XP + word discovery XP"
2. **Daily cap consideration:** If any student consistently earns >3000 XP/day, add soft cap (diminishing returns after threshold). Not needed now — max theoretical is ~2065 without streaks.
3. **Activity-type once:** Each activity type awards XP once per session completion, not per sub-action
4. **Loss XP floor:** Losing a duel always awards minimum participation XP (prevents discouragement)
5. **Streak multiplier applies to session total:** Not per-word — prevents exploitation via many short sessions

## Configuration Values (for educationXpManager.ts)

### Duel XP Config
- DUEL_WIN_ASYNC: 200
- DUEL_LOSS_ASYNC: 120
- DUEL_WIN_REALTIME: 250
- DUEL_LOSS_REALTIME: 150
- DUEL_DRAW: 175 (midpoint)

### Practice Mode XP Config
- MATCHING_PAIR_CORRECT: 15
- MATCHING_ACCURACY_BONUS: { 90: 40, 80: 20, 70: 10 }
- MATCHING_PERFECT_SESSION: 60
- SPELLING_WORD_CORRECT: 20
- SPELLING_STREAK_BONUS: 5 (per consecutive correct)
- SPELLING_ACCURACY_BONUS: { 90: 50, 80: 30, 70: 10 }
- BLITZ_WORD_FOUND: 10
- BLITZ_COMBO_BONUS: 3 (per combo level, stacking)
- BLITZ_COMPLETION: 40

### Daily Challenge XP Config
- DAILY_CHALLENGE_COMPLETE: 100

## Review Schedule
- After Phase 37 ships: Verify practice mode XP rates match model
- After Phase 38 ships: Verify duel XP rates match model
- After Phase 40 ships: Full economy audit with real student data
```
  </action>
  <verify>
    File exists at `.planning/phases/36-foundation-refactoring/XP-ECONOMY-MODEL.md`
    Document contains all existing XP sources from educationXpManager.ts
    Document contains all new XP sources for duels and practice modes
    Combined total is documented
    Progression rate analysis shows reasonable leveling pace
  </verify>
  <done>XP economy model covers all 12+ activity types with XP/session, XP/day, rationale, and anti-inflation rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add new activity XP config to educationXpManager.ts</name>
  <files>fe-next/backend/modules/educationXpManager.ts</files>
  <action>
Add new config entries to `EDUCATION_XP_CONFIG` in `educationXpManager.ts` for duels and practice modes. Also update the `PracticeSessionXp` type to include new modes.

**Add to EDUCATION_XP_CONFIG object (after existing entries):**

```typescript
// Duel XP (from XP Economy Model)
DUEL_WIN_ASYNC: 200,
DUEL_LOSS_ASYNC: 120,
DUEL_WIN_REALTIME: 250,
DUEL_LOSS_REALTIME: 150,
DUEL_DRAW: 175,

// Word Matching Practice XP
MATCHING_PAIR_CORRECT: 15,
MATCHING_ACCURACY_BONUS: {
  90: 40,
  80: 20,
  70: 10,
} as Record<number, number>,
MATCHING_PERFECT_SESSION: 60,

// Spelling Challenge Practice XP
SPELLING_WORD_CORRECT: 20,
SPELLING_STREAK_BONUS: 5,  // Per consecutive correct
SPELLING_ACCURACY_BONUS: {
  90: 50,
  80: 30,
  70: 10,
} as Record<number, number>,

// Timed Blitz Practice XP
BLITZ_WORD_FOUND: 10,
BLITZ_COMBO_BONUS: 3,  // Per combo level
BLITZ_COMPLETION: 40,

// Daily Challenge XP
DAILY_CHALLENGE_COMPLETE: 100,
```

**Update `PracticeSessionXp` type — add new modes:**
```typescript
export interface PracticeSessionXp {
  type: 'flashcard' | 'solo_board' | 'lesson_completion' | 'matching' | 'spelling' | 'blitz' | 'duel_async' | 'duel_realtime' | 'daily_challenge';
  sessionData: {
    // Existing fields...
    cardsReviewed?: number;
    cardsCorrect?: number;
    vocabularyWordsFound?: string[];
    newWordsFound?: string[];
    masteryLevel?: 'not_started' | 'started' | 'practicing' | 'mastered';

    // New fields for Phase 37-39
    pairsMatched?: number;
    totalPairs?: number;
    wordsSpelled?: number;
    spellingStreak?: number;
    blitzWordsFound?: number;
    blitzMaxCombo?: number;
    duelResult?: 'win' | 'loss' | 'draw';
  };
  streakDays?: number;
}
```

**IMPORTANT:** Do NOT implement calculation functions for new modes yet — those will be implemented in Phase 37/38/39. Only add config values and type definitions so the types are ready when implementation begins.

Ensure the file stays under 500 lines after additions.
  </action>
  <verify>
    `cd fe-next && npx tsc --noEmit` passes
    `grep "DUEL_WIN_ASYNC" fe-next/backend/modules/educationXpManager.ts` returns a match
    `grep "MATCHING_PAIR_CORRECT" fe-next/backend/modules/educationXpManager.ts` returns a match
    `grep "'matching'" fe-next/backend/modules/educationXpManager.ts` returns a match (in type union)
    `wc -l fe-next/backend/modules/educationXpManager.ts` is under 500
  </verify>
  <done>educationXpManager.ts has config values for all new activity types and updated PracticeSessionXp type. File compiles cleanly and stays under 500 lines.</done>
</task>

</tasks>

<verification>
1. XP economy model document exists with comprehensive activity table
2. All existing XP sources listed with correct values from codebase
3. All new XP sources have values with rationale
4. Combined XP/day is analyzed for progression rate
5. educationXpManager.ts has new config entries
6. PracticeSessionXp type includes new modes
7. TypeScript compilation passes
8. File stays under 500 lines
</verification>

<success_criteria>
- Economy model covers 12+ activity types with XP/session, XP/day, and rationale
- Progression rate analysis confirms balanced leveling pace
- Anti-inflation rules documented
- Config values in educationXpManager.ts match economy model
- Type definitions ready for Phase 37-39 implementation
- No existing functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/36-foundation-refactoring/36-05-SUMMARY.md`
</output>
