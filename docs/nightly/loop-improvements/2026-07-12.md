# Loop Self-Review — 2026-07-12

## Run health (last 7 nights, 07-06..07-12)
| Lane | Success | Avg files | Notes |
|------|---------|-----------|-------|
| 01 triage | 7/7 | 2-5 | Always ships; restored 101 salvaged files 07-07 |
| 02 perf | 7/7 | 3-6 | Backbone; fetchLanding LCP −66%, MP CLS fix 07-12 |
| 03 engagement | 7/7 | 2-4 | Backbone; PostHog-delta reverts (exp-mp-room-join 07-12) |
| 04 competitor | 2/7 | docs | Skipped 5; Reddit RSS + 5 mode ideas when run |
| 05 landing | 7/7 | 3-8 | Backbone; mode polish, 0 reverts |
| 06 seo | 4/7 | 2-4 | Skipped 3; usage-limit prone; ES education |
| 07 self-learn | 2/7 | 2 | Skipped 5 (scheduler); this file |
| 08 adsense | 3/7 | 1-3 | Skipped 4; hero-image blocked on CLI |
| 09 monetization | 7/7 | 1-8 | Backbone; funnel + rewarded warming |
| 10 dictionary | 2/7 | docs | Skipped 5 (scheduler) |
| 11 mode-qa | 6/7 | 2-4 | 1 rc124 fail 07-11; word-tower GRADUATED |
| 12 telemetry | 4/7 | 1-2 | Skipped 3; results_viewed per-mode wiring |

Runs avg ~17.6 min; 7/7 hit usage-limit backoffs; 2/7 exit-124 (07-06, 07-11).

## What's working well
- **Mode-readiness treadmill delivered its first GRADUATION** — word-tower 84%→90%→Released over the window (11-mode-qa + 05-landing). The nightly component-audit + Sentry-guard loop is proven end-to-end, not just busywork.
- **Backbone 01/02/03/05/09 = 7/7** — perf/engagement/landing/monetization ship every night regardless of gate outcome (their code rides whichever tier passes, or salvages docs).
- **Reduced-tier gate is the release valve** — the 2 code-ship nights (07-07 tsc+affected, 07-11 typecheck) both merged real Sentry fixes past a wedged full gate. Without it, the window ships zero code.
- **Regression self-catch** — 03 reverted its own exp-mp-room-join regression via a rageclick delta (16 vs 1). Lanes are watching their prior ships.

## Friction points
- **The full gate wedges 4 of 6 scorable nights** (TS phase or full-test rc124). Salvage keeps master safe but throws away that night's lane code. This is the dominant loss — real fixes written, gated, dropped. Affects EVERY code lane.
- **Supabase MCP absent 17 straight nights** — lane 01/02's DB work (4 migrations) can't apply. A whole category of value is frozen. Human-only fix, but the loop keeps re-diagnosing it nightly (wasted budget).
- **Usage-limit backoffs on 7/7 runs** — heavy lanes (06, 11) burn the rate ceiling; later lanes get squeezed or skipped. 06/07/10/12 skip-rate is partly this, partly scheduler.
- **Feedback volume collapsed to 1 callback/7d** — the steering signal the loop is built to consume is near-empty. Either the cards aren't noticed or the founder is heads-down.

## Concrete improvements (ranked by impact × effort)
1. **Per-suite Vitest kill + changed-files scope in the gate** — the #1 loss. Replace the single idle-900s backstop with `vitest related <changed> --bail` per suite + a hard per-suite timeout. Turns rc124 "inconclusive" into "this one suite hung, others passed → ship tier." Effort: M. Impact: converts ~4 docs-only nights/week into code ships. File: `scripts/nightly/gate.sh` (test phase).
2. **MCP preflight probe that SKIPS + alerts instead of re-diagnosing** — add a 5s `supabase` MCP ping at run start; if down, set `LEXI_SUPABASE_MCP=down` so lanes 01/02 skip DB work silently (one Telegram line, not a per-lane re-investigation). Effort: S. Impact: stops ~15 min/night of repeated drought diagnosis. File: `scripts/nightly/run.sh` preflight + `scripts/nightly/tools/mcp-health.sh` (new).
3. **Stagger the two rate-heavy lanes (06, 11)** — they both hit usage-limit; don't schedule them back-to-back. Insert a light lane between them. Effort: S. Impact: fewer 120s backoffs, fewer downstream skips. File: nightly scheduler config.
4. **Retire resolved watches automatically** — `VirtualGamesList` lint sat in "avoid" for weeks after it was fixed 07-10. Lane 07 prompt should explicitly diff prior watches against the last report's "verified/resolved" markers. Effort: S (prompt edit). Impact: keeps carry-forward list actionable. File: lane-07 prompt.
5. **Add a `game_abandoned` instrumentation task to lane 12** — the completion crater (53%) can't be diagnosed without it. Effort: M. Impact: unblocks the biggest funnel question. File: lane-12 prompt + emit site in the game shell.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health.sh`** — pings supabase + sentry MCP, exports `LEXI_<SVC>_MCP=up|down`, emits one alert line if down. Removes per-lane re-diagnosis (fires every night, 17+ nights running). Effort: S. Called from run.sh preflight.
(Deferred: a gate-tier classifier script — fold into improvement #1 instead of a standalone tool.)

## New product/feature ideas surfaced by this week's data
- **Instrument + fix the game-completion crater** — 460→245 sessions (53%, 07-11/12 funnel). Even a 10pt completion lift is more retained players than any new feature. Source: completion funnel + confirmed `game_abandoned` gap.
- **Build Blast "Ghost Round"** — the only `idea:build` vote this window (07-09). Founder explicitly wants it. Source: feedback callback `idea:build:d24d4c78`.
- **Multiplayer CLS is a UX tax on the highest-traffic surface** — CLS 0.574–1.043 (n=80) pre-fix. 07-12 shipped a fix; if it lands, MP feels dramatically more stable — worth a dedicated MP-layout-stability pass. Source: lane-02 CWV 07-11/12.

## Loop changes shipped THIS WEEK
- None mechanically shipped from prior loop-improvements (07-06..11 lane-07 skipped 5/7 nights). The gate-tier/salvage doctrine in learnings.md is the de-facto change carried forward. This file re-surfaces the per-suite-kill fix (proposed before, not yet implemented) as the #1 item.
