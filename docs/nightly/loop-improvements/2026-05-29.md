# Loop Self-Review — 2026-05-29

## Run health (last 6 nights: 05-24 thru 05-29)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 2/6 (33%) | ~18m | 2 (when ships) | Sentry+Supabase MCP hang; high-value output when succeeds |
| 02 perf | 2/6 (33%) | ~19m | 3 (when ships) | Supabase execute_sql suspected; LCP/CLS fixes land |
| 03 engagement | 3/6 (50%) | ~28m | 6 (when ships) | Trending UP from 0% → 50%; experiments shipping |
| 04 competitor | 6/6 (100%) | ~7m | 2 | Most reliable; no MCP dependency in hot path |
| 05 landing | 2/6 (33%) | ~21m | 3.5 (when ships) | PostHog MCP = bottleneck (confirmed 05-29 skip→ship) |
| 06 seo | 5/6 (83%) | ~19m | 4 | Consistent; GSC data via REST, not MCP |
| 07 self-learn | 5/5 (100%) | ~6m | 2 | Fastest, lightest; pure file read+write |
| 08 adsense | 3/5 (60%) | ~9m | 0.4 | Low-value: mostly no-ops or minor edits |

**Fleet:** 31/47 shipped (66%), 13 timed out (28%), 3 kept-partial (6%).

## What's working well
- **Autonomy matrix = high-value triage output.** 6 real fixes shipped autonomously in 3 nights (search_path hardening, REVOKE anon, pathTrace null guard, sync_coins qualify, RLS initplan caching, WASM build script). Zero reverts. This is the loop's highest-impact pattern per-commit.
- **KEEP_TIMEOUT_PARTIALS first success.** 05-29: lanes 01/02/03 timed out but kept 17/13/10 gate-validated partial files. Previously these would be pure loss. Significant upgrade to fleet ROI.
- **Phase 0 intel brief deployed.** 11 signals injected per-lane without live MCP calls. First night — stale sources (search/sentry/supabase) but PostHog+Railway healthy. Should reduce timeout rate once all collectors work.
- **Lane 03 engagement recovery.** From 0/3 (05-24/05-25) → 3/4 shipped (05-26 thru 05-28). Difference: experiments.ts patterns + GrowthEvent types = smaller scope per night.
- **git-ship resilience.** 0 stranded commits since `a53d0c3c0` fix. Push failures no longer brick the next run.

## Friction points
- **MCP hang cascades (lanes 01/02/05).** Single 60s-clipped call is fine; 3-4 sequential hangs = 240s+ burned before real work starts. These lanes try Sentry search_issues → Supabase get_advisors → execute_sql sequentially. Each can hang.
- **Reddit API death (lane 04).** JSON endpoint returns HTML since 05-29. Lane 04 fell back to WebSearch but lost thread-level context (can't draft precise replies without reading full threads). OAuth or alternative needed.
- **Lane 08 low ROI.** 3/5 ships but output is mostly "nothing to do" or minor copy edits. 1 real fix (leaderboard seoContent accuracy 05-28) in 6 nights. AdSense approval likely blocked by traffic volume, not content quality at this point.
- **Phase 0 collector auth gaps.** 3/6 collectors returned stale/empty on first run (search=no GSC API key exposed to collector, sentry=MCP-dependent, supabase=MCP-dependent). Phase 0 needs REST alternatives for these 3.
- **Telegram feedback sparse.** 3 signals in 7 days. Buttons exist but founder rarely taps them. The `night:good/meh` signal that was supposed to drive self-critique has zero recent data.

## Concrete improvements (ranked by impact x effort)
1. **Route lanes 01/02/05 through Phase 0 brief ONLY (skip live MCP).** Currently these lanes still make live Sentry/Supabase MCP calls after receiving the brief. Make the brief sufficient: expand Phase 0 collectors to include Sentry top-5 unresolved + Supabase advisor warnings via REST. Effort: M. Impact: timeout rate drops from 34% → ~15%. File(s): `scripts/nightly/lib/intel/collectors/`, lane prompt files.
2. **Add Reddit OAuth to lane 04 or switch to cached Reddit data.** Reddit blocked JSON API. Options: (a) OAuth client_credentials flow in `scripts/nightly/tools/reddit-fetch.sh`, (b) cache r/wordgames via daily cron curl with OAuth token, (c) switch to Pullpush API for historical threads. Effort: S-M. Impact: restores thread-level Reddit research. File(s): `scripts/nightly/tools/reddit-fetch.sh`.
3. **Lower MCP_TOOL_TIMEOUT from 60s → 45s.** 5 nights at 60s with zero false clips. Lower timeout = faster fail-through = more time for real work. Effort: S. Impact: saves 15s per hung call x ~3 hangs/lane = 45s recovered per affected lane. File(s): `scripts/nightly/run.sh` or env config.
4. **Merge lane 08 (adsense) into lane 06 (SEO) as a sub-task.** Lane 08 runs independently for 9min, ships no-ops 60% of the time. The 1 real fix it produced (leaderboard accuracy) could have been a 2-minute check within lane 06. Effort: S. Impact: frees 1 lane slot; reduces total run time by ~9min; SEO lane already touches the same pages. File(s): lane 06 prompt, lane 08 prompt (delete or merge).
5. **Add `tsc --noEmit --files <authored>` to pre-gate.** Push failed 05-24 (type error on LandingView.tsx isAdmin). Pre-gate type-check on authored files would catch this before the full build gate. Effort: S. Impact: prevents 1-in-6-nights push failure. File(s): `scripts/nightly/lib/gate.sh`.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/sentry-rest.sh`** — Query Sentry issues via REST API (GET /api/0/projects/:org/:project/issues/?query=is:unresolved) instead of MCP. Returns top-5 unresolved with last_seen + event count. Removes Sentry MCP dependency from lane 01 + Phase 0. Effort: S. Called from: Phase 0 sentry collector.
- **`scripts/nightly/tools/reddit-oauth.sh`** — Reddit OAuth2 client_credentials flow → bearer token → JSON API access. Stores token in `/tmp/reddit-token-$DATE` (1h TTL). Falls back to current curl if token fetch fails. Effort: M. Called from: `reddit-fetch.sh`.

## New product/feature ideas surfaced by this week's data
- **Sealed Bid multiplayer** — SP version shipped 05-29 (admin-only, 26 tests). Natural next step: real-time MP sealed bid where N humans simultaneously pick words, reveal together. Socket infra already exists. Source: founder directive + lane 04 "poker-reveal mechanic" research (05-28).
- **Shareable emoji MP result card** — 5 consecutive nights of lane 04 mentions. Wordle 90k→300k DAU case study. r/wordgames thread actively recruiting indie games for score-sharing. Source: lane 04 ideas 05-24 through 05-28.
- **Ghost Replay Mode** — race your own previous run on the same board. Zero social infra needed. Fits Blast V2 (move history already emitted). Source: lane 04 idea 05-27.
- **Fading Grid Sprint** — tiles disappear one-by-one; score before board goes dark. New Blast V2 modifier using existing `modifier` pipeline. Source: lane 04 idea 05-29, sourced from Wordrace/bloob.io competitor analysis.

## Loop changes shipped THIS week (from prior loop-improvements)
- `a53d0c3c0` fix(nightly): never strand a push, never give up on a lane — git-ship preserve+reset + KEEP_TIMEOUT_PARTIALS + mandatory-min-artifact (proposed in loop-improvements, shipped 05-28)
- `c19b3f10e` fix(nightly): kill the 3 failure modes that wasted 2026-05-27 — preflight unpushed-commit abort + gate-parser fix + dropper intersect authored (proposed after 05-27 failure, shipped 05-28)
- `364c9f4f0` + `66a7631f8` feat(nightly): Phase 0 intelligence layer — collectors + scorer + driver + lane injection (proposed as "MCP-prefetch" in 4 prior loop-improvements, finally shipped 05-29)
- `435a39aa8` fix(nightly): unblock gate, harden summary composer, downgrade lane 05 — build:schemas pre-gate + stdin composer pipe + lane 05 sonnet/600 (shipped 05-26)
