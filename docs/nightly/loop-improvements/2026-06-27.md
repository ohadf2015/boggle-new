# Loop Self-Review — 2026-06-27

## Run health (last 6 nights, 06-22..06-27)
| Lane | Success | Avg files | Notes |
|------|---------|-----------|-------|
| 01 triage | 5/6 | ~3 | exit-75 stale-token 06-22/23 (infra, not code); 68 SECURITY DEFINER backlog |
| 02 perf | 6/6 | ~4 | most reliable; /es-mp LCP +53% structural still open; MCP-absent nights deferred DB work |
| 03 engagement | 6/6 | ~3 | 7 exps live, 0 rollback; leaderboard_viewed wired-not-firing |
| 04 competitor | 6/6 | docs | idea:build ×12 callbacks — appetite spiked; reddit-fetch jq parse fail ×4 |
| 05 landing/polish | 6/6 | ~5 | sealed-bid needed 3 nights to settle; 0 reverts |
| 06 seo | 6/6 | ~6 | native HE/SV/JA/ES review; blog hero image placeholder |
| 07 self-learn | 5/6 | 2 | prompt-only, never times out |
| 08 adsense | 4/6 | ~3 | E-E-A-T cleared; 2 MCP-degraded nights; education OG images open |
| 09 monetization | 4/6 | ~2 | education pricing; coin-sync RPC deferred (safety rail) |
| 10 dict | 6/6 | ~6 | top throughput; 6MB swedish-words bundle leak FIXED |
| 11 mode-qa | 6/6 | ~3 | word-tower readiness 0→58%; modeqa:ontrack ×4 |
| 12 telemetry | 4/6 | ~2 | fixed user_identified + brain-drill game_started; 55+ never-wired backlog |

**Aggregate:** ~60/72 lane-runs shipped/partial (83%). Only 06-24 was a fully-clean gate run; 06-22/23 exit-75, 06-25 TESTS-INCONCLUSIVE, 06-26 partial-drop. **4/6 nights had ≥1 MCP-absent boot** (Supabase + Sentry), retry recovered most.

## What's working well
- **0 code reverts in 6 nights** despite broad ship — salvage floor + eslint-changed-only + single end-of-run commit holds. (lanes 02/03/05/10/11)
- **Baseline-red gate fix landed** (`d6ae0896d`+`e759e8624`+`415c39188`) — the standing #1 gate-drop cause is gone; TESTS-INCONCLUSIVE should fall.
- **Mandatory-Minimum-Artifact floor never zeroed a night** — every degraded run still shipped its lane-NN artifact.
- **Founder-directive fast path** consistently same-run ships (tower physics, sealed-bid, education pricing).

## Friction points
- **MCP token staleness** (Supabase + Sentry) hit 4/6 nights — lane 02/08/09 silently degrade to docs-only when DB-advisor is unreachable. Refresh is still manual/launchd-drift.
- **Gate full-Vitest wedge** persists structurally (900s idle → 06-25 inconclusive, 06-26 partial-drop) even after baseline-red fix — the gate doesn't scope tests to changed files.
- **Lane 04 reddit-fetch jq parse fails ×4** — RSS fallback parse not error-tolerant enough; produces empty idea pools some nights.
- **Lane 05 over-scope** — sealed-bid took 3 nights (06-22/23/25) to settle; 5-locale-first rule not always followed.
- **word-tower file-cap violations** (3 files >500L) block flag-wiring and risk unguarded-Record crashes — carried 5+ nights with no split.

## Concrete improvements (ranked impact × effort)
1. **MCP token preflight refresh** — add a `scripts/nightly/tools/mcp-health-refresh.sh` that re-mints/validates Supabase+Sentry tokens before lane fan-out and ALERTS on failure (never silent). Effort: M. Impact: kills 4/6-night degrade for lanes 02/08/09. Files: `scripts/nightly/run.sh` preflight + new tool.
2. **Scope gate tests to changed files** — gate runs `vitest related --run $(changed)` with a hard per-suite timeout+kill instead of full-suite 900s idle. Effort: M. Impact: removes TESTS-INCONCLUSIVE (hit 2/6). File: gate-runner script.
3. **Lane 04 prompt: emit 1-3 BUILDABLE ideas with named mode+file** — idea:build ×12 vs pass ×4 proves appetite; current ideas too abstract to action. Effort: S. Impact: converts the loudest callback signal into shipped features. File: lane-04 prompt.
4. **Lane 05 prompt: hard "write all 5 locales FIRST, ONE mode, smallest slice"** gate before any TDD. Effort: S. Impact: stops 3-night settle cycles. File: lane-05 prompt.
5. **Lane 11 prompt: schedule word-tower modular split as a dedicated night** (extract scene/manager sub-modules under 500L) before more feature-wiring. Effort: M. Impact: unblocks crash-guard + double-scaling fixes. Files: `WordTowerPlay/Scene/Manager`.
6. **Lane 08 blog-cadence recipe** — codify founder's every-2-day blog as `scripts/nightly/tools/blog-topic-rotate.sh` (topic queue + Higgsfield hero-image prompt template + source-citation checklist) so it's repeatable, not hand-built. Effort: M. Impact: satisfies 06-27 directive durably. File: lane-08 prompt + new tool.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health-refresh.sh`** — preflight Supabase/Sentry token validate+refresh, Telegram-alert on failure (no silent degrade). Called from `run.sh` before fan-out. Pattern fired 4/6 nights. Effort: M. (NOT shipped this lane — proposal; flagged for lane 02/infra night.)
- **`scripts/nightly/tools/feedback-aggregate.sh`** — the `cat feedback/*.ndjson | grep callback_data | uniq -c` aggregation lane 7 runs every night; codify so lane 7 + the manager summary share one source. Effort: S. (Recurring 7+ nights.)

## New product/feature ideas surfaced by this week's data
- **Daily-Challenge Word Tower as the default surface** — founder 06-24/25 + lane 11 readiness 58%; field data shows MP LCP is the worst route (+53% /es), so routing tower into the already-fast daily flow both honors the directive and dodges the MP perf floor. Cite: lane 02 /es-mp LCP, lane 11 readiness card.
- **"Learn a language with AI" education blog → ESL word-games CTA** — founder blog directive + education growth priority; the newest-angle topic ("use AI to learn a language") is exactly the ESL/vocabulary keyword cluster lane 06 targets. Cite: founder 06-27, education.landing keys.
- **Surface the idea:build pipeline as a weekly shippable** — idea:build ×12 this week vs ×1 prior; the founder is now actively greenlighting lane-04 ideas, so a "build queue" that carries the top-voted idea into lane 05 next night closes the loop. Cite: feedback aggregate idea:build ×12.

## Loop changes shipped THIS WEEK
- Baseline-red gate poison cleared (`d6ae0896d`,`e759e8624`,`415c39188`) — prior weeks' #1 loop-improvement ("fix baseline-red dropping clean code") actually landed.
- Build engine flipped turbopack→webpack (`dcdda2375`) after route-export refactor — prior "webpack homepage first-load lever" watch resolved.
