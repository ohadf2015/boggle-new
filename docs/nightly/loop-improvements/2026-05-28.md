# Loop Self-Review — 2026-05-28

## Run health (last 6 nights: 05-23 thru 05-28)

| Lane | Success rate | Avg duration (success) | Avg files touched | Notes |
|------|-------------|----------------------|-------------------|-------|
| 01 triage | 3/6 (50%) | ~11m | 2-4 | Ships when MCP cooperates; autonomy matrix = high value per ship |
| 02 perf | 2/6 (33%) | ~11m | 3 | High-value output (CLS, LCP fixes) but Supabase MCP hangs kill it |
| 03 engagement | 4/6 (67%) | ~25m | 3-11 | Trending UP from 50%. Longest lane when it runs. results_viewed + replay CTA = real product |
| 04 competitor | 6/6 (100%) | ~7m | 2 | Fastest, most reliable. Curl-based, no MCP dependency |
| 05 landing | 1/6 (17%) | ~20m (the 1 ship) | 0 (validation) | Worst lane. opus AND sonnet both timeout. 5/6 exit-124 |
| 06 seo | 5/6 (83%) | ~12m | 3-4 | One timeout (05-27), recovered 05-28. Lightweight and effective |
| 07 self-learn | 5/6 (83%) | ~8m | 2 | Consistent. Fast. No MCP needed |
| 08 adsense | 3/5 (60%) | ~9m | 0-1 | Produces copy/audit when it runs; 2 timeouts in window |

**Fleet totals:** 30/46 shipped (65%), 16 timed out (35%), 0 cap-reverted.
**Push success:** 4/6 nights pushed (05-24 x2 failed type-check/conflict, 05-27 merge conflict).

## What's working well
- **Lightweight lanes are the backbone.** L04 (100%), L06/L07 (83%) carry the fleet. All share a pattern: minimal MCP, curl/local-only data, focused scope.
- **Autonomy matrix unlocked real security fixes.** L01 shipped pathTrace null guard, search_path hardening, REVOKE on 6 anon RPCs — all autonomously, all reversible, zero incidents.
- **Lane 03 engagement improving.** 4/6 shipped (was 3/6 prior week). `results_viewed` blackhole fix + replay CTA = tangible product improvements with TDD tests.
- **Docs-only salvage** recovers value from code-gate failures. 4/6 nights salvaged docs when code failed.
- **Idea feedback loop working.** First `idea:build` tap (Survival Rounds, 05-26) proves the Telegram card loop generates actionable signal.

## Friction points
- **MCP hangs = 35% timeout rate.** 16/46 lane-runs killed by exit-124. Per-call 60s timeout helps but doesn't solve the root: 23-25 MCP servers spawning per lane, most unused. Startup overhead + connection pool exhaustion suspected.
- **Lane 05 is a dead lane.** 1/6 ships. Model downgrade (opus→sonnet) failed. 5 consecutive timeouts. The lane prompt asks for a FULL page variant (hero, copy, CTA, tests) which is too much for one lane run.
- **Push failures cost a full night's code work.** 05-24 (isAdmin type error) and 05-27 (MascotCelebrationVideo merge conflict from concurrent founder edits) both meant code lanes shipped locally but never reached prod. No pre-integration type-check catches this early enough.
- **Lane 03 runs long (~25m).** It ships 67% of the time but is the longest lane, risking timeout as scope grows. PostHog REST pre-fetch would cut 5-10m of MCP waiting.
- **Stream-timeline diagnosis stalled.** Proposed 4 nights ago to grep stream-*.ndjson for the exact MCP tool causing hangs. Still unactioned. Without it, "MCP hang" remains a black box.

## Concrete improvements (ranked by impact x effort)

1. **Trim MCP server set per lane** — each lane currently spawns 23-25 MCP servers. Lane 04 needs 0 MCP, lane 07 needs 0, lane 06 needs GSC only. Add `--mcp-servers` allow-list to lane dispatch so each lane only connects servers it uses. Effort: M. Impact: fewer connections = faster startup, fewer hang vectors, lower timeout rate. File(s): `scripts/nightly/run-nightly.sh`, lane dispatch config.

2. **Pre-gate `tsc --noEmit` on authored files** — run TypeScript check on authored files BEFORE the full build gate. Catches type errors (like the 05-24 isAdmin miss) before 10+ minutes of build. Effort: S. Impact: prevents "shipped locally, push failed" on type errors. File(s): `scripts/nightly/run-nightly.sh` (gate section).

3. **Lane 05 scope cut to CTA-only** — reduce lane 05 prompt from "build full page variant" to "update title/meta/CTA text on existing pages with <200 sessions." No new components, no tests, no hero. Just copy edits. Effort: S. Impact: lane might actually ship (currently 17%). File(s): lane 05 prompt in `scripts/nightly/prompts/`.

4. **Lane 03 PostHog REST pre-fetch** — before dispatching lane 03, run a PostHog REST query for 7d funnel data and inject the JSON as prompt context. Eliminates the main MCP hang vector for engagement. Proposed 5 nights, still unshipped. Effort: S. Impact: L03 from 67%→80%+ success rate. File(s): `scripts/nightly/lib/posthog-rest.sh` (exists), lane 03 dispatch.

5. **Stream-timeline auto-diagnosis on exit-124** — after any lane exits 124, immediately grep its `stream-*.ndjson` for the last tool call before kill. Append culprit tool name to the failure digest. Effort: S. Impact: turns "MCP hang" from black box into actionable data. File(s): `scripts/nightly/run-nightly.sh` (post-lane section).

## New helper scripts to ship

- **`scripts/nightly/tools/stream-culprit.sh`** — reads a stream-*.ndjson file, finds the last `>` (tool invocation) line before EOF, extracts tool name + elapsed time. Called from run-nightly.sh after any exit-124. Effort: S. Removes manual diagnosis of which MCP tool hung.

## New product/feature ideas surfaced by this week's data

- **Shareable emoji MP result card** — mentioned 4 consecutive nights by lane 04 (05-23 thru 05-26). Active r/wordgames demand for shareable word-game results (Wordle-style). S effort. Source: competitor research + Reddit thread analysis.
- **Survival Rounds (last-word-standing elimination)** — received first `idea:build` tap from user on 05-26. Elimination MP mode where lowest scorer drops each round. Source: lane 04 concept 05-26, user-validated via Telegram.
- **Quick-replay button on results page** — lane 03 wired `exp-results-replay-cta-v1` on 05-28. Current funnel: 87% game completion rate but unmeasured replay rate. If replay CTA lifts game_started/7d, it's the cheapest retention lever. Source: lane 03 funnel analysis 05-28.
- **Ghost Replay Mode (async self-competition)** — replay your own best game against a ghost of your prior performance. 2 nights of lane 04 mentions (05-27, 05-28). Adds mastery loop without requiring live opponents. Source: competitor research.

## Loop changes shipped THIS WEEK
- `c19b3f10e` (05-28): nightly gate-parser excludes node_modules, dropper intersects authored allowlist, drop-and-re-gate reverts dropped files, docs-salvage uses original authored set. Killed 3 failure modes from 05-27.
- `d2700a45f` (05-28): preflight aborts on unpushed non-docs commits (closed "local ahead" gap that caused 05-27 push conflict).
- `8482cb890` (05-28): lane 05 timeout 600→1200s, lanes 06/08 720→900s safety margin.
- `eba60ce35` (05-28): removed distracting AI mascot celebration video fanfare from daily results (user feedback).
