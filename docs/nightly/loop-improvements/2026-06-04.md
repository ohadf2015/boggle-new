# Loop Self-Review — 2026-06-04

## Run health (last 6 report nights: 05-29..06-04; 05-31 = preflight abort)
| Lane | Success | Avg duration | Avg files | Notes |
|------|---------|--------------|-----------|-------|
| 01 triage | 3/6 | ~4-8m (254s 06-04) | 2-4 | MCP-heavy; succeeds scoped to 1 fix (churn-signals 500) |
| 02 perf | 4/6 | ~4-6m (361s 06-04) | 2-8 | LCP fixes ship; backlog of undiagnosed regressions growing |
| 03 engagement | 4/6 | ~9-10m (541s 06-04) | 1-27 | Slowest healthy lane; event-wiring heavy |
| 04 competitor | 6/6 | ~4-5m (265s 06-04) | 3-12 | Research-only, never times out |
| 05 landing | 4/6 | ~2-3m (124s 06-04) | 3-15 | Game-mode ships; phased locales = repeat work |
| 06 seo | 4/6 | ~6-7m (419s 06-04) | 3-10 | Reliable scoped to title/meta/cross-links |
| 07 self-learn | 6/6 | ~3-4m | 2-3 | Always ships |
| 08 adsense | 4/5 | ~2-3m | 2-10 | GSC-scope-blocked; CJK word-count artifact |
| 09 monetization | 1/2 | timeout 06-02 | 1-4 | New lane, not appended 06-03/06-04 — silently absent |

## What's working well
- **Timeout epidemic is over.** 06-01→06-04 every code lane rc=0. The bounded-budget + baseline-gate (`bd14fa03e`) and PreToolUse time guard (`a2334a3b3`) did their job. This is the single biggest improvement in the window.
- **Founder-directive fast path** — 5 directives consumed and shipped same-night across the window (sealed-bid, share-card, word-tower-pool, file-cap removal). Telegram free-text → lane action loop is tight.
- **Mandatory-Minimum-Artifact** floor held — no lane shipped literally nothing.
- **Lane 04 + 07** at 100% — proof that low-MCP, prompt-driven lanes are the reliability ceiling.

## Friction points
- **Preflight abort on unpushed founder commits** (lanes-wide) — killed 05-29/05-30/05-31. The single worst infra gap; no handler after 3 occurrences.
- **8-file cap truncated multi-locale ships** (lanes 05/06/08) — ja/es repeatedly fell back to key strings, forcing 2-3 night completion. FIXED this run (see below).
- **Lane 09 silently absent** from 06-03/06-04 reports — a lane that doesn't append a section is indistinguishable from a lane that crashed. No "lane skipped" signal.
- **Perf backlog accretes faster than it drains** (lane 02) — homepage/word-hunt/word-wheel/multiplayer LCP all undiagnosed and growing. Lane finds regressions but rarely lands the fix.
- **GSC scope dead 9+ nights** (lanes 06/08) — Bing-only fallback is lower-confidence; the `webmasters.readonly` ADC fix is never done because no lane owns infra auth.

## Concrete improvements (ranked by impact × effort)
1. **Remove the hard file-count cap — SHIPPED THIS RUN.** `LEXI_LANE_FILE_CAP` 8→999 in `scripts/nightly/lib/headless.sh`. Honors founder directive (06-03+06-04); kills the ja/es key-string fallback. Wall-clock finalize cutoff retained. Effort: S (done). Impact: full-locale ships in one night.
2. **Preflight: stash-or-warn instead of abort on unpushed commits.** When HEAD is dirty with unpushed founder work, `git stash` it (or proceed and report), don't abort the whole run. Effort: M. Impact: recovers ~3 lost nights/window. File: `scripts/nightly/lib/preflight.sh`.
3. **MAX_MCP_CALLS=3 per lane on phase-0-heavy lanes (01/02/03).** Hard-cap serial Sentry/Supabase/PostHog calls; force a single batched query. Effort: M. Impact: removes the remaining cascade-timeout risk. File: `lane-time-guard.sh` (extend mcp matcher counter).
4. **"Lane skipped/absent" digest line.** If a lane writes no report section, the manager summary must say so explicitly. Effort: S. Impact: lane 09 black-hole becomes visible. File: report finalizer.
5. **Lane 02 must land OR queue, not just observe.** If a perf regression can't be fixed in budget, write a one-line queued-fix entry to a `perf-fix-queue.md` so the backlog is actionable, not just logged. Effort: S. Impact: drains the growing LCP backlog.
6. **Retire the dead search collector + stop Reddit JSON retries.** 0 signals 10+ nights; RSS fallback covers Reddit. Effort: S. Impact: removes pure-waste phase-0 time. File: collector config.

## New helper scripts to ship
- **`scripts/nightly/tools/lane-coverage-check.sh`** — after the run, diff the LANES array against report sections appended; emit a one-line "lanes that produced no section" list for the digest. Removes the manual "did lane 09 run?" check that recurred 2 nights. Called from the report finalizer. Effort: S.
(No other recurring ≥3-night pattern justifies a new script this week.)

## New product/feature ideas surfaced by this week's data
- **Word Tower daily-letter mode + reward-ad refill** — founder free-text 06-02 ("daily letters, run-out gate, reward ads to ruin rival buildings / get more letters / hints") + `polish:try:word-tower` ×2 (06-02, 06-03) + module already shipped 06-03. Highest-signal build: founder asked twice and tapped polish twice. Cite: feedback 06-02/06-03 ndjson.
- **Fix `/en/word-hunt` LCP 8188ms** — worst LCP measured this window (lane 02, 06-04). Word Hunt is a core mode; 8s LCP is a direct retention leak. Cite: 06-04 perf row.
- **Word Detective daily** — `idea:build` 06-01, un-built 3 nights. Definition-blank guessing game; cheap to build as a daily variant. Cite: feedback 06-01 ndjson.
- **`/en/multiplayer` SSR skeleton parity** — CLS 0.512 + INP 528ms + LCP re-regression all trace to one `PageClient.tsx` skeleton-height mismatch; one fix clears three poor-tier metrics on the highest-traffic interactive route. Cite: 06-02/06-04 perf rows.

## Loop changes shipped THIS WEEK
- `bd14fa03e` — baseline-aware gate + bounded lane budgets (06-03): ended the exit-124 timeout epidemic.
- `a2334a3b3` — PreToolUse time+scope guard (06-02): mechanical deny past finalize cutoff.
- `33f8641c3` — Reddit RSS fallback (06-03): unblocks Reddit signal without OAuth.
- `8076f6a8b` — `--only/--skip` matched lane index not name + DIRTY_COUNT unbound fix.
- **This run:** `LEXI_LANE_FILE_CAP` 8→999 (founder directive, no hard file cap).

## Telegram-button feedback (last 7d: 05-29..06-03)
- `polish:try` ×9 — sealed-bid ×2, blast ×1, word-alchemy ×1, word-vault ×1, word-forge ×1, party/caption-clash ×1, word-tower ×2
- `idea:build` ×3 — Fading Grid Sprint, Word Detective, Word Heist
- `idea:pass` ×1 — Language Family Classification
- `night:meh` ×1 (06-03), `night:good` ×0 — only run-quality tap this window; one "meh" is below the ≥3 sharp-self-critique threshold, but note: 06-03 was a docs-heavy "education upsell CTR" night with thin code ship. Founder wants visible code value, not just measurement.
- 2 free-text directives: word-tower daily letters+reward ads (06-02), remove file cap (06-03).
- **Read:** `polish:try` dominant (9/14) and broadening across modes — founder wants existing unpublished modes made playable, not just new ideas. `word-tower` is the hottest single target (2 polish + 2 free-text directives).
