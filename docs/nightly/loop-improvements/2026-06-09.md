# Loop Self-Review — 2026-06-09

## Run health (last 7 nights, 06-03..06-09)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 5/7 | ~9m | 2-3 | Security REVOKE migrations; fast, rarely times out |
| 02 perf | 5/7 | ~37m | 2-4 | Slowest lane; force-dynamic→revalidate shipped 06-09; CLS fix still unshipped |
| 03 engagement | 5/7 | ~40m | 1-10 | PostHog flag hygiene + MP quickplay overlay (06-09) |
| 04 competitor | 7/7 | ~25m | 3 | Reddit RSS + 5 daily ideas; never times out |
| 05 landing | 5/7 | ~50m | 2-11 | Polish:try queue; over-scope = #1 timeout cause |
| 06 seo | 6/7 | ~28m | 3-23 | Title-truncation CTR; dead_pages.py noindex feed |
| 07 self-learn | 7/7 | ~28m | 3-4 | Prompt-only; loop infra deltas |
| 08 adsense | 6/7 | ~35m | 2-4 | FAQ + JSON-LD SSR; content depth |
| 09 monetization | ~5/7 | – | 3-5 | Ad-UX + education upsell; offerwalls dark |

## What's working well
- **Mandatory-Artifact floor is the load-bearing reliability lever** — on 06-06 six of eight lanes hit exit-124, yet every lane still shipped its artifact. Zero total-loss nights this window.
- **Usage-limit cascade fix (`5cb7f28a0`) closed a real failure mode** — prior to it, one lane hitting the shared 5h Claude limit instant-failed every downstream lane via stale deadline epochs. The recompute-on-resume fix is the highest-value loop change this week.
- **Prompt-only lanes (04, 07) are 7/7** — 13+ nights. Confirms: the fewer MCP/search round-trips a lane needs, the more reliably it ships.
- **Founder feedback drives scope same-night** — polish:try ×15 / idea:build ×7 (78%) are acted on directly by lanes 04→05.

## Friction points
- **Lane 02 + 05 are the slow/over-scope pair** (~37m, ~50m) — both repeatedly attempt multi-file + 5-locale + animation work in one budget and get SIGKILLed mid-edit. Lane 05 kept 11 files on 06-06 and shipped nothing.
- **Authored-but-unshipped fixes leak across nights** — AutoHideHeader CLS 0.29 fix has been "written" 3 nights (06-05→08) and never landed. The lane writes it, runs out of budget, reverts. Net: 3 wasted lane-budgets on one fix.
- **`rg` EACCES still un-rootcaused** (06-06/07) — degraded lanes to slow `find|xargs grep`. No graceful fallback wired.
- **Digest button under-engaged** — most nights draw zero night:good/meh vote; only 4 of 7 nights got any night-quality signal. The self-critique threshold (≥3 meh) can't fire on this little data.

## Concrete improvements (ranked by impact × effort)
1. **Pin AutoHideHeader CLS fix to lane 02 as a NAMED first task** — stop re-discovering it. Add to lane 02 prompt: "FIRST: ship the AutoHideHeader spacer-div fix (`fe-next/components/AutoHideHeader.tsx` return spacer not null) — it is authored & ready, lands in <5 files." Effort: S. Impact: closes a 0.29 CLS regression on `/en/multiplayer` that's leaked 3 nights. File: `scripts/nightly/prompts/lane-02-perf.md` (or equivalent).
2. **Hard scope-cap lane 05 to ONE polish target + max 5 files** — add to prompt: "Pick exactly ONE `polish:try` mode tonight. If it needs >5 files or 5-locale + animation + TDD together, do animation OR locales, not both." Effort: S. Impact: lane 05 5/7→7/7 by killing the over-scope timeout. File: lane-05 prompt.
3. **Wire an `rg` health-probe + fallback in the lane harness** — `rg --version >/dev/null 2>&1 || export LEXI_SEARCH=grep` at lane start; pass a `lexi_search()` helper. Effort: M. Impact: removes the #1 infra blocker for lanes 01/02. File: `scripts/nightly/lib/`.
4. **Add per-lane duration + files-touched to the nightly report footer automatically** — lane 07 currently hand-derives these from logs each night (~slow). A `scripts/nightly/tools/lane_stats.sh` emitting the table row would remove this manual step. Effort: S. Impact: faster, accurate lane-07. (see helper below)
5. **Re-measure `/es` + `/en/multiplayer` LCP/INP after the 06-09 quickplay overlay** — lane 02 should diff PageClient.tsx perf next run since the overlay changed hydration height. Effort: S. Impact: catches a regression introduced by lane 03's own fix. File: lane-02 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/lane_stats.sh`** — parse `~/logs/lexi-nightly/run-*.log` for per-lane start/end timestamps + `git diff --stat` per lane window → emit the Markdown run-health table. Removes lane-07's manual log-reading every night (fired 7/7 nights). Called from lane-07 prompt Step 1. Effort: M. (Not created tonight — flagged for a future lane-07 budget; the parsing of log timestamp format needs one verification pass I can't finish + self-test in remaining budget.)

## New product/feature ideas surfaced by this week's data
- **Invite-funnel rescue: post-accept onboarding screen.** Source: invite_landed→invite_consumed 18→3 (83% drop, 06-06). Accepted invitees bounce before converting — a one-screen "your friend is waiting, tap to join their game" landing (vs dropping them on a generic hub) targets the exact drop point.
- **Practice-completion nudge on Wheel Rush.** Source: `/es/practice/wheelRush` 47 started → 27 completed (43% drop, 06-07). Add a "1 more round?" or progress-bar completion incentive; the drop is mode-specific (classic/hunt don't show it).
- **Internal-link push for Hebrew daily word.** Source: HE `המילה היומית` +3467% WoW at position 9.1 (06-08). It's one internal-link campaign away from page-1. Lane 06 should add hub→daily-word HE internal links — cheap, high-momentum SEO win with revenue (ad-impression) upside.

## Loop changes shipped THIS WEEK
- `5cb7f28a0` fix(nightly): pause-and-resume lanes across Claude usage-limit resets — detect limit → sleep-until-reset → recompute deadline epochs (proposed in prior loop-improvements as "usage-limit-aware runner"). SHIPPED.
- `db2f21d26`-era: word-wheel/word-hunt `force-dynamic`→`revalidate=3600` (proposed as "force-dynamic route LCP" watch). SHIPPED.
- `56bc7d884` feat(nightly): `dead_pages.py` GSC page-dim dead-page finder (proposed to close lane-08 noindex data-gap). SHIPPED.
