# Loop Self-Review — 2026-07-18

## Run health (last 7 nights: 07-12..07-18)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 6/7 run-nights ship | ~9m (517s 07-18) | 3 | Pixi guards, index migrations (MCP-blocked apply), Sentry triage |
| 02 perf | 6/7 | ~23m (1370s 07-18) | 3 | LCP mascot `initial={false}` fix; MP CLS + webp-poster open; runs LONG |
| 03 engagement | 6/7 | ~22m (1310s 07-18) | 3 | Same-run flag+event wire; PostHog-delta revert; runs LONG |
| 04 competitor | 0/7 code | research-only | 0 | Skipped/research every night; feeds 05 polish ideas |
| 05 landing | 5/7 | ~11m (687s) | 3 | Mode polish; 0 reverts |
| 06 seo | 3/7 | ~10m | 3 | Off-rotation most nights; ES education CTR |
| 07 self-learn | 6/7 | ~10m | 2 | learnings + loop-improvements (this file) |
| 08 adsense | 2/7 | ~8m | 2 | Off-rotation; blog cadence blocked |
| 09 monetization | 7/7 | ~10m | 4 | Companion to 03/12 |
| 10 dict | 2/7 | ~12m | 6 | Off-rotation |
| 11 mode-qa | 7/7 | ~11m (644s 07-18) | 8 | crossword 55→90% Released-ready; shiritori 42% |
| 12 telemetry | 5/7 | ~17m (1047s 07-18) | 3 | Coverage closures; game_completed brain-drill |

**Whole-run health:** 07-16 preflight-abort (git divergence) → recovered by 180-min retry. **07-17 preflight-abort (feature branch + dirty tree) → NO report, night fully lost.** 07-18 ran 8/12 (rotated out 04/06/08/10). No rc124 this window; Sentry + Supabase MCP both failed to connect 07-18.

## What's working well
- **Fork-cap fix converted salvage→ship** — `c3973c715` (07-14) held: 1 salvage (07-15), 0 code-drops since. The "attack the classifier" move beat the prior "restore next morning" reflex. Evidence: impact-ledger `gate-fix-2026-07-10-vitest-pool-namespace` → improved.
- **Mode-readiness ladder delivers** — lane 11 drove crossword 55→90% over 5 audit nights, then cleanly rotated to a new mode (shiritori). Incremental per-night audit + explicit readiness % is a repeatable graduation pipeline.
- **Same-run flag+event wiring** — 07-18 lane 03 shipped experiment + its telemetry event together; no zombie flags this window (contrast: 07-09/11 shipped dead flags).
- **Mandatory-Minimum-Artifact floor holds** — degraded lanes still leave a gate-clean `docs/nightly/artifacts/lane-NN-*.md`; no total-loss lanes on nights that ran.

## Friction points
- **Preflight abort has no live alert** (whole-run, HIGH). 07-16 degraded + 07-17 lost. The repo was left on `feat/pricing-psychology-i18n` with a dirty tree; the safety check correctly refused to auto-run, but nobody knew until 9am. One lost night ≈ 8 lanes of output.
- **Supabase MCP dead 22 nights** (infra, HIGH). Triage/perf WRITE migrations nightly, 6 now backlogged, apply-step blocked. 07-18 failed after 3 connection attempts. Human-blocked on a never-expire PAT.
- **02-perf + 03-engagement run ~22-23min** (per-lane, MED). Both exceed the nominal 15m budget on 07-18 yet passed (rc=0) — suggests the hard-kill is softer than the prompt claims, but they're the timeout-risk lanes if load rises. Heavy PostHog/web-vitals queries dominate.
- **04-competitor 0/7 code, off-rotation 06/08/10** (coverage, LOW). Acceptable by design (rotation), but Ghost-Round `idea:build` vote has sat unbuilt since 07-11 because no lane owns turning a vote into a slice on a fixed cadence.

## Concrete improvements (ranked by impact × effort)
1. **Preflight-abort Telegram alert.** On any preflight abort, send a Telegram message with the reason (off-master / dirty-tree / divergence) BEFORE scheduling the 180-min retry. Effort: S. Impact: turns a silently-lost night (07-17) into a 1-line human fix at 1am. File: `scripts/nightly/run.sh` (preflight block) + reuse existing Telegram sender.
2. **Pre-midnight repo-hygiene warn (cron).** A 23:30 cron that checks `git branch` + `git status --porcelain` and Telegram-warns if off-master or dirty, so the human can clean up before the 01:00 run. Effort: S. Impact: prevents the abort entirely (would have saved 07-16 + 07-17). File: new `scripts/nightly/tools/preflight-warn.sh` + crontab.
3. **Ghost-Round vote → lane-05 buildable-slice handoff.** Add to lane 04/05 prompt: "if an `idea:build` callback is >5 days unbuilt, lane 05 must ship its smallest coherent slice this run." Effort: S. Impact: closes the one open steering vote; stops votes rotting. File: lane 04 + 05 prompt.
4. **Split 02-perf PostHog queries behind a shared cached helper.** Both 02 and 03 re-query overlapping web-vitals/PostHog windows (~22min each). A single cached fetch reused across lanes cuts wall-clock. Effort: M. Impact: −5-8min/night on the two slowest lanes; lowers rc124 risk. File: `scripts/nightly/tools/posthog-webvitals.sh` (new).

## New helper scripts to ship
- **`scripts/nightly/tools/preflight-warn.sh`** — pre-midnight `git branch`/`status` check → Telegram warn if off-master or dirty. Removes the recurring "night lost to dirty tree" pattern (2 nights this window). Called from crontab @23:30. Effort: S. (bash -n before commit; idempotent — read-only, no writes.)
> Deferred: the PostHog/web-vitals cache helper (#4) is real repetition (02+03 both query it) but needs a cache-invalidation design — spec it before scripting. Not shipping tonight (out of budget).

## New product/feature ideas surfaced by this week's data
- **Shiritori turn-timer ring (reuse crossword/word-tower timer UI).** Source: lane 11 07-18 — "no turn timer UI; server eliminates after 15s with zero client feedback." This is both the shiritori release blocker AND a retention risk (silent elimination = rage-quit). Cite: `useShiritoriGame.ts` / `ShiritoriView.tsx` audit.
- **MP lobby "Connecting…" feedback as a default, not just an experiment.** Source: lane 03 — ES multiplayer rage-clicks #1 signal (6/24h), root cause = 5s silent socket wait. `exp-mp-lobby-connect-feedback-v1` is live 50/50; if it wins, promote to default fast — the silent-wait is a measured funnel leak, not a preference.
- **Homepage hero poster-frame.** Source: lane 02 — 393KB animated winner.webp, no poster, LCP still budget-dominated after the `initial={false}` fix. A static first-frame poster is a known-good SPEED lever the founder prioritizes.

## Loop changes shipped THIS WEEK
- `c3973c715` (07-14) — vitest fork-concurrency cap; this week's data confirms it converted the salvage treadmill to 0 code-drops (validated, not just proposed). This was last week's #1 loop-improvement proposal actually landing.
