# Loop Self-Review — 2026-07-19

## Run health (last 7 nights: 07-13/14/15/16/18/19 ran; 07-17 aborted)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 5/5 run | ~20m | 5–13 | Pixi null-guards; REVOKE security fix + migration applied 07-16 |
| 02 perf | 5/5 | ~12m | 2–3 | Framer `initial={false}` LCP win; MP CLS 0.979 + webp-poster OPEN |
| 03 engagement | 5/5 | ~18m | 2–4 | Flag+event wired same-night; no zombie flags |
| 04 competitor | research-only | ~10m | 0 | Feeds lane 05 polish votes; pitches CULLED modes (bug) |
| 05 landing | 5/5 | ~13m | 1–3 | Crossword ClueScramble, shiritori, sealed-bid; 0 reverts |
| 06 seo | 3/3 (rotation) | ~15m | 1–5 | ES MP CTR rewrites; native reviews |
| 07 self-learn | 3/3 | ~n/a | 3 | This file + learnings.md |
| 08 adsense | 2/2 (rotation) | ~12m | 1–2 | ≥300w audits; hero-image blocked |
| 09 monetization | docs-only | ~n/a | 1–3 | Companion to 03/12 |
| 10 dict | 2/2 (rotation) | ~n/a | 5–7 | Candidate review |
| 11 mode-qa | 5/5 | ~15m | 3–9 | Crossword 55→90% Released; shiritori 42→55% |
| 12 telemetry | 3/3 | ~18m | 1–3 | brain-drill game_completed; orphan-event found |

## What's working well
- **Two multi-week blockers cleared this window.** Supabase MCP reconnected (07-16, 07-19) and a stale migration finally drained — the drought was connectivity, not the apply path. Crossword graduated 90% and lane 11 rotated cleanly to shiritori (42→55%). The gate classifier fix (`c3973c715`) held: 1 salvage in 6 nights vs 4/7 pre-fix.
- **Zero pushed-code reverts** across the window. Salvage-before-push doctrine means a gate-fail never regresses master.
- **Mandatory-Minimum-Artifact floor + eslint-changed-files-only + single end-of-run commit** — no timeout-with-nothing-shipped nights among the 6 that ran.

## Friction points
- **Preflight abort is silent until 9am** (07-17). Correct to refuse off-master/dirty auto-run, but a lost night with no live signal is the single most expensive failure mode now that the gate classifier is stable. Affects the whole loop.
- **Idea/polish generator pitches culled modes** (word-vault/forge/alchemy — deleted 07-06). Wastes the founder's finite Telegram vote budget every night. Affects lanes 04/05.
- **MP CLS is worsening, not just open** (0.574→0.979 on /en over the window). Lane 02 keeps re-diagnosing the same async-socket header-collapse each night without landing the fix (needs a small refactor + visual QA it can't self-verify in 15m).
- **Supabase MCP still ~2/6 uptime** — connected nights work, but the never-expire PAT is unminted, so 4/6 nights can't touch the 5-migration backlog.

## Concrete improvements (ranked by impact × effort)
1. **Preflight-abort Telegram alert** — in `scripts/nightly/run.sh` preflight, on any hard-abort (off-master/dirty/divergence) send the existing Telegram card with the abort reason instead of exiting silently. Effort: S. Impact: converts a silently-lost night into a 1-line 1am fix. File: `scripts/nightly/run.sh` (preflight block).
2. **Cull-list filter in the idea/polish generator** — gate the mode pool against the 4 culled slugs (party, word-alchemy, word-forge, word-vault) before emitting `polish:try`/`idea:build` cards. Effort: S. Impact: every founder vote lands on a live mode. File: lane 04/05 generator (`scripts/nightly/` idea builder + lane 04 prompt).
3. **Pre-midnight dirty-tree warn cron** — a cron ~23:30 that checks `git -C <repo> status --porcelain` + current branch and Telegram-warns if dirty/off-master, so the human can clean before the 01:00 run. Effort: S. Impact: prevents the 07-17 class entirely. File: new `scripts/nightly/tools/preflight-warn.sh` + cron entry.
4. **Split MP-CLS into a human-queue card, stop re-diagnosing** — lane 02 has diagnosed this 3+ nights; the fix needs visual QA outside the 15m budget. Emit ONE review-by-eod card with the exact fix (always-render header, toggle visibility) and stop re-spending lane time on re-diagnosis. Effort: S (prompt line). Impact: frees ~1 lane-night/wk; unblocks a CRITICAL CLS. File: lane 02 prompt.
5. **Add `lobby_daily_ember_shown` (+ any raw-postHog events) to the `GrowthEvent` union** — the telemetry classifier can't audit events fired outside the typed union. Effort: S. Impact: closes a class of orphan-event blind spots. File: `fe-next/lib/experiments.ts` or the analytics event union.

## New helper scripts to ship
- **`scripts/nightly/tools/preflight-warn.sh`** — idempotent pre-midnight check: dirty tree OR off-master → Telegram warn with reason; exit 0 always. Removes the silent-abort class (#3 above, fires whenever the repo is left dirty — ≥1 night this window). Called from a ~23:30 cron. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Homepage LCP poster frame** — the 393KB animated `winner.webp` has no poster; lane 02 confirms weight is the residual LCP driver after the `initial={false}` fix. A static first-frame poster is a free SPEED win (founder priority #1). Source: lane 02 07-18/19.
- **Ship Blast "Ghost Round"** — the only unbuilt `idea:build` steering vote (07-11) on a LIVE mode; flash top-scorer's ghost board post-round highlighting 3 missed words. Return-hook: mastery. Source: feedback `idea:build:d24d4c78`.
- **MP header-always-render refactor as an engagement play** — CLS 0.979 on the highest-intent page (/multiplayer) likely suppresses join conversion; fixing it is both SPEED and funnel. Source: lane 02 CLS trend + lane 03 MP-lobby experiments.

## Loop changes shipped THIS WEEK
- `2e6040222` fix(nightly): treat baseline vitest worker-crash as inconclusive, not a new failure — direct follow-through on the `c3973c715` fork-cap fix; salvage rate stayed at 0 after.
- `9926f4674` fix(nightly): recover 11 lane files false-dropped by gate + quarantine stale adventure tests — reactive salvage of the 07-18 false-drop, root-caused to the same worker-crash classifier gap.
