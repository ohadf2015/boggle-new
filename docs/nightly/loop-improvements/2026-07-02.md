# Loop Self-Review — 2026-07-02

## Run health (last 7 nights, 06-27..07-02 + 07-01 rerun)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 4/6 shipped, 6/7 ran | ~13m | 3 | 2 RPC hardenings auto-shipped. FAILED 07-01 (exit 75, usage-limit/preflight). Highest-variance lane (11–20m). |
| 02 perf | 7/7 ran, 1 code / 5 partial | ~9m | 4 | Most reliable + fastest. Blocked on MCP for DB wins; shipped CLS fix 07-02. |
| 03 engagement | 6/6 ran | ~16m | 2 | Ships flags but 3 have NO render path (dark/zombie). |
| 04 competitor | 6/6 research-only, 2 code-fail | ~14m | 3 | 0 code by design; idea:build 86%. Reddit source stale (RSS only). |
| 05 landing | 3 shipped / 2 partial / 1 fail | ~16m | 8 | Mode-polish backbone; over-scoped 07-02 (page.tsx deferred at file cap). |
| 06 seo | 6/6 content shipped | ~17m | ~8 | Consistent; native-reviewed 5 locales. No code. |
| 07 self-learn | 5/6 (07-01 whole-run abort) | ~5m | 2 | Prompt-only, reliable. |
| 08 adsense | 6/6 content | ~10m | 3 | Blog/JSON-LD ships; hero-image blocked on Higgsfield CLI. |
| 09 monetization | 0 code 6/6 | ~12m | 3 | Ship-drought — research-only or reverted every night. |
| 10 dict | 5/6 (07-01 fail) | ~14m | 6 | Wiktionary + Russian throughput; heavy file counts. |
| 11 mode-qa | 7/7 | ~15m | 5 | Word Tower 58→79%. modeqa:ontrack ×5-6. |
| 12 telemetry | 6/6 | ~8m | 2 | Extractor fix recovered 7 events; wiring DEAD backlog. |

## What's working well
- **02-perf and 11-mode-qa never fail** (7/7, no timeout, no salvage-drop). Low-MCP + no full-test self-check + concrete per-night target = reliability. These two are the model every lane should copy.
- **Autonomy fast-path pays off** — 3 reversible Supabase RPC hardenings shipped this window with zero human intervention, all marked 9am-skim. The founder's "stop deferring" directive is being honored without a single bad autonomous change.
- **Salvage floor held under stress** — 3 consecutive gate-fails (06-28/29/30) each preserved lane code + shipped docs. No night produced zero output.
- **Mode-polish TDD pattern converts reliably** — 4 polished mode features shipped (Hot Streak Perks, Shiritori bonus, Word Forge tiers, Sealed Bid engine) with 0 reverts.

## Friction points
- **Gate wedge dominated the window** — 3 straight docs-only salvages (06-28/29/30) dropped ~all lane code pre-push. Root: next-build "Running TypeScript" phase + full Vitest tripping the 5400s idle watchdog. Fix `c826e1924` landed 07-02; unproven until 07-03/04 run clean. This is the single biggest yield loss — 3 nights of code work salvaged-but-not-shipped.
- **Supabase MCP token staleness, 6/6 nights** — every DB-dependent lane (01, 02) degrades: FK indices, RLS initplan, auth-pool, and 3 written migrations all stalled. This is a 60-second manual fix (mint never-expire PAT) that has cost ~6 nights of DB work.
- **07-01 preflight silent-abort** — shared tree left on a feature branch + dirty by a concurrent session → the entire 01:00 run was rejected, 5 lanes exit-75/1. The preflight fails safe but fails SILENT-to-the-loop (no auto-recovery).
- **Lane 03 flag-without-render-path recurs** — 3 dark/zombie flags created this window. The "check render-path exists" rule is in learnings but keeps being violated because nothing enforces it.
- **Lane 09 ship-drought** — 0 code 6/6 nights. The lane researches instead of shipping a reversible slice.

## Concrete improvements (ranked by impact × effort)
1. **Mint never-expire Supabase PAT into nightly env.** Add `SUPABASE_ACCESS_TOKEN` (Expires=Never) to `~/.config/lexi-nightly/env` + a preflight probe that alerts if the MCP handshake fails. Effort: S. Impact: unblocks 6-night DB backlog (FK indices, RLS initplan, 3 pending migrations) — directly improves p75 query time. File(s): nightly env + `scripts/nightly/preflight.sh`.
2. **Verify the gate-wedge fix, then add a per-suite hard kill.** `c826e1924` skips the next-build TS phase; if 07-03/04 still salvage, replace the 5400s idle backstop with scope-Vitest-to-changed-files + a per-suite timeout that kills+reports (never idles). Effort: M. Impact: stops the 3-night code-salvage bleed. File(s): `scripts/nightly/gate.sh` (or equivalent test-runner wrapper).
3. **Preflight auto-recovery for off-master/dirty tree.** When HEAD≠master or tree dirty, `git stash` + `git checkout master` (preserving concurrent WIP in the stash) and Telegram-alert, instead of aborting the whole run. Effort: M. Impact: recovers the ~1-in-7 whole-run loss (07-01). File(s): `scripts/nightly/preflight.sh`.
4. **Lane 03 prompt: forbid creating a flag without a live render path.** Add a hard STEP-0 gate: "Before creating any PostHog flag, grep for the variant-B render conditional in code. If it does not exist, WIRE it this night or do not create the flag. Delete one zombie flag before creating a new one." Effort: S. Impact: kills the dark-flag accumulation (3 this window), improves experiment eval rate. File(s): lane-03 prompt.
5. **Lane 09 prompt: mandate one reversible shipped slice/night.** Change the lane from research-first to ship-first: "STEP 0 — ship ONE reversible monetization slice (ad-UX flag, education upsell copy, CTA). Research is secondary." Effort: S. Impact: ends the 6-night ship-drought on a revenue-priority lane. File(s): lane-09 prompt.
6. **Lane 05 prompt: enforce write-all-5-locales-first + fully-wired slice.** Restate: "Write all 5 locale strings FIRST. Pick a slice small enough to FULLY WIRE (engine + UI + page) in one budget — a shipped engine with unwired UI is a half-mode." Effort: S. Impact: avoids the sealed-bid-style half-ships. File(s): lane-05 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/mcp-health-probe.sh`** — one idempotent probe that pings Supabase + PostHog + Sentry MCP handshakes at preflight and emits `ok|stale|down` per service to a Telegram line. Removes the per-lane "is MCP up?" guesswork that silently degrades 01/02/03/12 nightly. Called from preflight. Effort: S. (Deferred to a lane with env access — lane 07 has no MCP creds to test it tonight.)
- **`scripts/nightly/tools/flag-render-path-check.sh`** — grep-based check: given a flag key, confirm a matching variant-B conditional exists in `fe-next/`. Called from lane-03 as a STEP-0 self-gate. Effort: S. (Deferred — needs the exact flag-usage grep pattern lane 03 uses.)

## New product/feature ideas surfaced by this week's data
- **Word Vault escape-room redesign** (founder directive 06-30 + polish:try ×2). Rework puzzles + UI to feel like a real escape room; ties the mastery hook the feedback (idea:build ×6, 86%) shows appetite for. Source: 04-competitor ideas + Telegram directive.
- **Word Tower daily leaderboard** — the ONE blocker from 79%→90% readiness (lane 11, modeqa:ontrack ×5-6). Needs a founder design call (1-attempt/day vs unlimited) then a small Layer-B backend. Highest-confidence mode to reach public-release. Source: 06-28..07-02 lane-11 reports.
- **Wire `results_viewed` + `results_cta_clicked`** — 81 SP completions produced 0 `results_viewed` events (genuine 0, lane 12 07-02). The results screen is a prime revenue/retention CTA surface flying blind. Source: lane-12 07-02 report.

## Loop changes shipped THIS WEEK
- **`c826e1924` (07-02)** — "skip next-build TS phase in ALL gate tiers (kills the wedge)": the prior week's loop-improvement (scope/kill the gate wedge) landed as a concrete gate fix. First run after it (07-02) ran clean — the first non-salvaged night in 4.
- **`posthog-coverage.sh` extractor fix (06-30)** — prior loop-improvement "helper-script-the-repeatable" paid off: fixed awk early-exit, recovered 7 live events from false-DEAD classification.
