# Loop Self-Review — 2026-05-21

> **Data caveat:** first complete multi-lane sweep was 2026-05-21. 05-19 finished only lane 4; all 05-20 runs aborted at preflight (dirty tree). The table below is dominated by the single 05-21 run — treat rates as N≈1, not N=7.

## Run health (last 7 nights)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 1/1 | ~8m | 5 | shipped `AvatarRendererSsr` + 4 tests; queued 7, incl. URGENT anon-callable RPCs |
| 02 perf | 1/1 | ~11m | 3 + 1 migration | shipped `sync_coins` single-statement rewrite (#2 DB CPU consumer) |
| 03 engagement | **0/1** | 12m (= ceiling) | 0 | **exit 124 timeout, reverted** — sonnet/720s |
| 04 competitor | 2/2 | ~7m | 2–3 | Reddit blocked; pullpush.io works; 4 ideas surfaced |
| 05 landing | **0/1** | 18m (= ceiling) | 0 | **exit 124 timeout, reverted** — opus/1080s |
| 06 seo | 1/1 | ~9m | 4–5 | shipped ES/SV/EN meta + internal links |
| 07 self-learn | 1/1 | ~8m | 2 | this file + learnings.md |
| 08 adsense | n/a | — | — | lane exists in `LANES` array but absent from 05-21 report — verify it ran |

## What's working well
- **Dirty-tree fix (`3e0217129`) unblocked the whole loop.** Three prior nights aborted at preflight; 05-21 was the first end-to-end sweep. This was the single highest-leverage change.
- **Per-lane revert isolation held under fire** — two lanes timed out and the other four still shipped clean. The architecture's core safety property is proven.
- **Lanes do honest ground-truth checks** — no fabricated-feature edits this week (lane 1 read the route, lane 6 verified the missing keyword). The anti-hallucination discipline is sticking.

## Friction points
- **Timeout-revert burns ~30 min/night for zero output** (lanes 3 + 5). Both ran to their exact ceiling then got killed and reverted — the most expensive failure mode, made worse by lane 5 being opus. Partial work is discarded, not checkpointed.
- **getUpdates poller collision** — long-poll daemon + run's own feedback-poll both call Telegram getUpdates; 05-20 log shows `terminated by other getUpdates request`. Button acks drop silently.
- **Lane labels are mislabeled in logs** — `lanes/03-engagement.sh:12` passes `"02-engagement"`, `05-landing.sh:12` passes `"04-landing"`, `07-self-learn.sh:12` passes `"06-self-learn"`. Off-by-one against the real lane number; makes log triage confusing.
- **Every lane summary prints twice** in the run log (duplicate `headless:` line + duplicate summary block). Doubles log size, no information gain.
- **Feedback signal is empty** — only smoke-test taps exist; the `meh ≥3 → self-critique` mechanism has never fired on a real run.

## Concrete improvements (ranked by impact × effort)
1. **Diagnose the lanes 3 + 5 timeouts BEFORE raising budgets — they were SILENT, not slow.** The 094357 log shows zero output between dispatch and exit 124 for both (lane 3: `[10:03:38]`→`[10:15:58]`; lane 5: `[10:22:47]`→`[10:41:13]`). A sonnet/opus lane working for 12–18m should print *something*; total silence points at a **hang** (stuck MCP call to Sentry/PostHog/Supabase, or a tool retry storm), NOT scope-too-broad. So:
   - **First (S):** add `--verbose`/heartbeat logging to `headless.sh` and re-run lanes 3+5 with `only=03-engagement` / `only=05-landing` to see *where* they stall. Don't raise budgets blind — if they're hung, more budget just burns more opus for nothing.
   - **Then, only if they produce partial output before the kill:** raise lane 3 720→900s, lane 5 1080→1500s, AND add to both prompts *"At ~80% of budget, stop new work; commit/queue what's done and exit clean."*
   - **If they're hung:** the fix is a per-tool-call timeout / MCP health-probe at lane start, not a bigger ceiling.
   Effort: S (diagnose) → M (fix). Impact: recovers ~30 min/night of zero-output compute (incl. opus). Files: `scripts/nightly/lib/headless.sh`, `scripts/nightly/lanes/03-engagement.sh`, `scripts/nightly/lanes/05-landing.sh`, `scripts/nightly/prompts/0{3,5}-*.md`.
2. **Resolve the getUpdates collision** — make the run's feedback-poll read from the daemon's buffer (or skip getUpdates entirely when the daemon PID is alive) instead of opening a second long-poll. Effort: M. Impact: reliable button acks → the feedback loop (run-quality `meh` signal) actually starts producing data. File: `scripts/nightly/run.sh` (feedback-poll block) + the daemon script.
3. **Fix lane-label first-args** — set them to match the real lane number (`03-engagement`, `05-landing`, `07-self-learn`). Effort: S. Impact: log triage stops being off-by-one. Files: `scripts/nightly/lanes/{03,05,07}-*.sh:12`.
4. **De-duplicate lane log output** — `tee` is likely echoing both the headless wrapper and the captured stdout. Pipe once. Effort: S. Impact: halves log size, easier next-night parsing. File: `scripts/nightly/lib/headless.sh`.
5. **Verify lane 8 (adsense) actually runs** — it's in `LANES` but produced no report section on 05-21. Either it silently no-op'd or never executed. Effort: S (investigate first). Impact: a configured lane is dark. File: `scripts/nightly/lanes/08-adsense.sh`.
6. **Fix self-learn lane-number drift in the prompt** — `learnings.md` header now says "Rewritten by **lane 7**" (correct: adsense became lane 8, self-learn shifted from 6→7). If `scripts/nightly/prompts/07-self-learn.md` still says "lane 6", next night's run rewrites the header back. Effort: S. File: `scripts/nightly/prompts/07-self-learn.md` (grep for "lane 6"). Out of this run's 2-file cap — flagged for a future lane.

## New helper scripts to ship
> Per-lane 2-file cap this run (learnings.md + this file) — proposing, not creating. Both patterns have fired ≥2 nights and justify a helper next run.
- **`scripts/nightly/tools/reddit-pull.sh`** — wraps the *only* working Reddit path: pullpush.io `sort=score&sort_type=desc`, client-side `created_utc` window filter, skip the reddit.com/old.reddit.com/JSON-API dead ends entirely. Removes lane 4's nightly trial-and-error against blocked endpoints (the `after=7d → 400` mistake was re-derived 2 nights). Called from `prompts/04-competitor.md`. Effort: S.
- **`scripts/nightly/tools/lane-budget-guard.sh`** (or inline prompt rule) — emits remaining-seconds so a lane can self-check "am I near my ceiling?" and ship-partial. Underpins improvement #1. Effort: S–M.

## New product/feature ideas surfaced by this week's data
- **Lazy/deferred game-client hydration on `/multiplayer`** — lane 2: `/en/multiplayer` LCP p75 = **5806ms (POOR)**, n=11, game-client hydration is the suspected LCP element. Splitting the socket/game bundle out of the critical path could move LCP under the 4000ms threshold. (cites perf-watch.md row.)
- **In-room emoji reactions on found words** — lane 4: Nanagrams' voice chat is its #1 differentiator on "play word games with friends" review sites; LexiClash rooms have **zero in-game comms**. Reaction events already fire on word submission (WebSocket) — displaying them is frontend-only. Effort: S. (cites ideas/2026-05-21.md #4.)
- **Parseword-style 3-step onboarding funnel** — lane 4 flagged Parseword's tight onboarding; memory says LexiClash onboarding skip = 45%. A guided 3-step first-run could cut that. (cites ideas/2026-05-21.md #1 + posthog-insights memory.)
- **Close the anon-callable RPC hole as a product-trust item** — lane 1: `award_ad_coins` + `increment_blast_progress` are callable by the `anon` role (unauthenticated `/rest/v1/rpc/...` could grant coins). Not a feature, but a coin-economy integrity risk worth a human REVOKE pass. (cites triage-queue.md.)

## Loop changes shipped THIS WEEK
- `3e0217129` fix(nightly): run on dirty tree instead of aborting; restore WIP on failure; push residue — directly resolved the 3-night preflight-abort streak. **This was the prior week's #1 friction point, now closed.**
- `948c6b42c` fix(nightly): isolate gate build into `.next-nightly` so it can't race a dev server.
- `765ebe958` test(nightly): extract `ship_nightly_commit()` + prove push-recovery across 5 scenarios.
- `69bb90dd5` / `95e794637`: stop committing volatile `translation-report.json` / `tsconfig.json` (root cause of stranded pushes).
- `ffd8ffe21` feat(nightly): added AdSense-approval lane 8 (run-verification pending — see improvement #5).
