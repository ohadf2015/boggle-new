# Loop Self-Review — 2026-06-19

## Run health (last 6 nights: 06-14..06-19)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 6/6 ship-attempt, 0 autonomous-fix 06-19 | ~19m | 3 | Blocked by MCP-unauthorized + Sentry 403; mostly authors migrations + investigates dead Sentry issues |
| 02 perf | 6/6 | ~40m | 3 | Slowest lane; REST-fallback applied LIVE 06-19 despite MCP down (advisor 20→0). Frontend root-causes often deferred |
| 03 engagement | 6/6 | ~32m | 5–6 | Closed last dark flag (practice-wheel-cta); deactivated a fake-running experiment (leaderboard-cta) |
| 04 competitor | 6/6 docs-only | ~24m | 3 | Reliable; jq parse errors on 2 reddit feeds recur but non-fatal |
| 05 landing | 5/6 (1 partial) | ~38m | 8 | Polish ships, but locale-drop recurs (es.js key missing 06-19) — working-set cap bites |
| 06 seo | 6/6 | ~28m | 3 | Reliable; autonomous native review now standard |
| 07 self-learn | 6/6 | ~34m | 2 | Prompt-only, never times out |
| 08 adsense | 6/6 | ~36m | 2–5 | Structural quality bar cleared 06-19; re-submit is now a manual op |

## What's working well
- **Gate self-repair landed and held.** Two distinct wedge bugs fixed this window (`06be04691` cone-scope, `2f520b937` baseline-red ship-verify). 06-19 = first 6/6 clean multi-lane night. The loop is fixing its own plumbing.
- **REST-API fallback proved out** — lane 02 applied policy consolidation + index drops LIVE when Supabase MCP was Unauthorized. MCP is no longer a hard dependency for DB work.
- **Mandatory-Minimum-Artifact floor** — never zero output, even on salvage nights.
- **Define→wire→flag in one lane** finally closed the 4-night-dark practice-wheel-cta. Discipline works when enforced.

## Friction points
- **Supabase MCP token expiry has no auto-detect / refresh.** 06-19 ran the whole night Unauthorized; lane 01 silently degraded to local-authoring with no alert. Sentry write-token is also 403 (can't resolve issues). Two stale credentials, zero proactive warning.
- **Lane 05 locale-drop recurs** — the ~8-file working-set cap drops the 5th locale (es.js again 06-19). Same failure mode flagged last window. Prompt says "locales first" but the cap still bites when component+test+page already eat 4 slots.
- **Telegram feedback is stale** — identical 17 records as last window; zero new button presses in 7d. Either the card isn't delivering or buttons aren't being pressed. The whole feedback-driven steering loop is running on frozen data.
- **Lane 02 is the slow lane (~40m)** — frontend perf root-causing inside budget rarely converges (`/he` LCP deferred 2 windows running).
- **Sentry issues investigated repeatedly but never resolved** — 1KQ/1CW re-investigated 06-19, confirmed already-fixed/dead, but 403 blocks programmatic resolve → they resurface in the brief every night.

## Concrete improvements (ranked by impact × effort)
1. **Preflight credential probe + alert** — extend the existing MCP preflight to actually call a write-scoped Supabase + Sentry op and, on `Unauthorized`/`403`, emit a single Telegram line "creds expired: SUPABASE_ACCESS_TOKEN / SENTRY_TOKEN — refresh". Effort: S. Impact: stops silent night-long degradation (lane 01 shipped 0 autonomous 06-19). Files: `scripts/nightly/lib/*preflight*`, run.sh.
2. **Lane 05: enforce locale-first as a hard gate, not advice** — add a STEP that writes the new key into all 5 translation files BEFORE touching component/test/page, and fail the lane's self-check if any of the 5 lacks the key. Effort: S. Impact: kills the recurring es.js raw-key bug (2 windows). Files: lane-05 prompt.
3. **Auto-resolve dead Sentry issues via a queued manual list** — since the token is 403, stop re-investigating the same dead issues nightly: write confirmed-dead issue IDs to a `sentry-resolve-queue.md` and have the brief SKIP any ID already on it. Effort: S. Impact: removes ~3 wasted lane-01 investigations/night. Files: triage-queue handling + brief generator.
4. **Diagnose Telegram feedback staleness** — verify the callback collector is still writing ndjson (last new record predates this window). If delivery is broken the loop is steering blind. Effort: M. Impact: restores the only direct user-signal channel. Files: feedback collector script.
5. **Split lane 02 perf into backend-DB (fast, REST-fallback) and frontend-vitals (deferred-friendly)** — backend work ships reliably; frontend root-causing eats the budget and rarely converges. Let the DB half finish fast and not be held hostage by the vitals half. Effort: M. Impact: faster lane 02, frees budget. Files: lane-02 prompt.

## New helper scripts to ship
- **`scripts/nightly/tools/creds-probe.sh`** — idempotent probe: one write-scoped Supabase call + one Sentry API call; prints `OK|EXPIRED` per credential. Called from preflight; feeds improvement #1. Effort: S. (Deferred to a code lane — not authored tonight to stay inside budget + gate-clean.)

## New product/feature ideas surfaced by this week's data
- **Surface a +N score pop on every accepted word in tile-drop modes** — lane 04 06-19 Reddit research: the #1 failure mode is "I lost but don't know why"; a per-word score pop removes the black-box feel. Cites r/wordgames Droplet feedback thread. Targets crane/wordfall feel.
- **Crane post-session drop highlight reel (social-share)** — lane 04 06-19: compose top-3 precision drops into a 5s shareable strip before the score screen; crane already records `effectiveDropError` per tile. Return-hook = share + mastery; S effort.
- **Investigate `/es/multiplayer` rage-click cluster (score 0.584)** — lane 03 06-19 flagged but unaddressed; highest rage signal. A real UX defect here is likely costing MP retention in the surging ES market (scrabble queries +1283% WoW, lane 06).

## Loop changes shipped THIS WEEK
- `2f520b937` fix(nightly): baseline-red ship path type-verifies (tsc), not next-build — closed the 2nd gate-wedge that dropped 16 files 06-18.
- `ee5ec6c9b` feat(nightly): deliver lane code falsely dropped by gate (2026-06-18) — recovered the dropped lane code.
- `74f8a405e` perf(nightly): snapshot only untracked WIP, restore tracked from HEAD — cut per-lane snapshot tax 1GB/~16min → 44K/0.2s.
