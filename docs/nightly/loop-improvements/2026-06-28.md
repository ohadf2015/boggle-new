# Loop Self-Review — 2026-06-28

## Run health (last 6 report nights, 06-23..06-28)
| Lane | Success | Avg dur | Avg files | Notes |
|------|---------|---------|-----------|-------|
| 01 triage | 6/6 | ~21m | 2–3 | search-path hardening; MCP/Sentry silent 3–4× |
| 02 perf | 6/6 | ~11m | 2–8 | most reliable; asset re-encode + SSR wins; MCP advisor absent 3 nights |
| 03 engagement | 6/6 | ~14m | 1–9 | flag-wiring sweep; dot-key mis-route latent |
| 04 competitor | 6/6 | ~8m | 2–7 | reddit jq fail 4×; search source stale all 6 |
| 05 landing | 6/6 | ~21m | 1–8 | mode polish every night, 0 reverts |
| 06 seo | 6/6 | ~18m | 3–6 | native review ×24; scrabble-vanity gap unworked (correct) |
| 07 self-learn | 6/6 | ~10m | 3 | learnings + loop-improvements every night |
| 08 adsense | 6/6 | ~6m | 1–4 | blog ISR; hero images blocked on Higgsfield CLI |
| 09 monetization | 6/6 | n/a | 1–11 | education pricing + tower upgrades |
| 10 dict | 6/6 | n/a | 4–8 | top throughput, no regressions |
| 11 mode-qa | 6/6 | ~16m | 5–8 | word-tower 38→65%; 2 crash-guard PRs |
| 12 telemetry | 6/6 | ~9m | 1–4 | user_identified + brain-drill fixed |

## What's working well
- **0 code reverts across 6 nights, all 12 lanes 6/6.** The baseline-red gate-poison fix (last week) removed the dominant clean-code-drop cause; this window has no gate-drop of clean lane work.
- **Salvage floor held every degraded night** — 06-23 docs-only gate-fail and 06-26 partial-drop both preserved code + shipped artifacts. No night zeroed.
- **Prompt-only lanes (04/07/12) never time out** — confirms the "no MCP round-trip + no full-test self-check = reliable finish" thesis at 30+ nights.
- **Founder-directive fast path converts same-run** — blog cadence (06-27), tower physics (06-26), sealed-bid all landed the night they were issued.

## Friction points
- **Supabase MCP token staleness — 4/6 nights** (06-25/26/27/28): advisor absent → lane 02 FK-index/unused-index/auth-pool and lane 01 Sentry-sync silently deferred. Single biggest recurring cost this window; root is a missing never-expire PAT in the nightly env + no preflight refresh. Affects 01, 02.
- **Gate Vitest can still 900s-wedge** (06-25/06-27 TESTS-INCONCLUSIVE) — eased by baseline-red fix but structural; a single hung suite idles the whole gate to inconclusive.
- **Lane 04 reddit source dead 6/6 nights** — jq parse fails 4×; OAuth un-configured. Lane keeps spending budget on a blocked path that returns nothing.
- **Higgsfield CLI sudo-install blocks blog hero images 6 nights** — founder's blog-every-2-days directive can't go fully autonomous until this is solved.

## Concrete improvements (ranked by impact × effort)
1. **Mint a never-expire Supabase PAT into the nightly env** — add `SUPABASE_ACCESS_TOKEN` (Expires=Never) to `~/.config/lexi-nightly/env` + a 1-line preflight that fails loud if absent. Effort: S. Impact: recovers lane 02/01 advisor work on 4/6 nights. File(s): `scripts/nightly/lib/mcp-probe.sh`, nightly env. **Top pick.**
2. **Hard per-suite timeout in the gate Vitest runner** — replace the 900s idle watchdog with a per-suite `--testTimeout` + kill, so one hung suite fails that suite, not the whole gate to INCONCLUSIVE. Effort: M. Impact: kills the 2/6 TESTS-INCONCLUSIVE rate. File(s): gate runner (`scripts/nightly/lib/gate-isolated.sh`).
3. **Lane 04: short-circuit the dead reddit path** — detect blocked source once, log it, skip remaining reddit fetches that night (don't retry 4×). Redirect freed budget to the 100%-build-rate "buildable idea w/ named mode+file" output the founder actually clicks. Effort: S. Impact: stops wasting lane-04 budget; raises idea throughput. File(s): lane-04 prompt + reddit fetch helper.
4. **Lane 03 prompt: forbid dot-keys in new PostHog flag names** — add a one-line rule "flag keys use dashes not dots (`blast-v2` not `blast.v2`)" so the silent mis-route stops recurring. Effort: S. Impact: prevents dead experiments. File(s): lane-03 prompt.
5. **Auto-gen-check or gitignore `deploymentChangelog.generated.json`** — it shows an untracked diff every run, noising up `git status` for every lane. Effort: S. Impact: cleaner gate + lane git reads. File(s): `.gitignore` or the generator.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/feedback-tally.sh`** — aggregates `docs/nightly/feedback/*.ndjson` callback_data into the `type:action` + per-slug tallies this lane re-derives by hand every night (the awk/sed/uniq pipeline run above). Idempotent, read-only, called from lane-07 prompt. Effort: S. (Deferred tonight — out of budget; flagged for next lane-07 run.)

## New product/feature ideas surfaced by this week's data
- **Word-tower DAILY-first refocus** — `modeqa:ontrack:word-tower ×5` + founder 06-24/25 directive both point the same way: the daily challenge is the trusted surface, MP is liquidity-starved. Lean the tower's primary CTA into daily, de-emphasize MP entry. Source: feedback tally + lane-11 readiness 65%.
- **Promote a near-ready mode to limited public** — word-tower at 65% readiness with 5 nights of `ontrack` votes is the closest beta to release; a flag-gated soft launch would test real retention. Source: lane-11 readiness trend 38→65%.
- **Buildable-idea pipeline w/ named mode+file** — `idea:build` is 100% (n=3) when ideas name a concrete target. Lane 04 should always end with 1–3 such ideas, never abstract market commentary. Source: feedback tally.

## Loop changes shipped THIS WEEK
- Baseline-red gate-poison fix (prior week, `d6ae0896d`+`e759e8624`+`415c39188`) confirmed effective this window: 0 clean-code gate-drops across 6 nights.
- `b1710a072` — added agent bg-task/pipe exit-masking to Class 4 recurring-pitfalls rules (loop-hygiene doc improvement).
