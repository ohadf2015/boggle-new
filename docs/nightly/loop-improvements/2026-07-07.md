# Loop Self-Review — 2026-07-07

## Run health (last 6 nights, 07-02..07-07)
| Lane | Shipped code | Avg files | Notes |
|------|--------------|-----------|-------|
| 01 triage | 2/6 | 2-3 | MCP-blocked on DB migrations; ships FE-only |
| 02 perf | 2/6* | 2-3 | backbone; salvaged w/ rest 3 nights |
| 03 engagement | 2/6* | 1-4 | backbone; funnel + rage-click |
| 04 competitor | 1/6 | 3 | skipped 3 nights (scheduler/budget) |
| 05 landing | 2/6* | 3-10 | backbone; polished 2 now-deleted modes |
| 06 seo | 3/6 | 5-7 | usage-limit prone; native review |
| 07 self-learn | 5/6 | 2-3 | reliable (docs, gate-clean) |
| 08 adsense | 2/6 | 1-3 | hero-image blocked on CLI |
| 09 monetization | 3/6 | 1-2 | daily-first funnel |
| 10 dict | 1/6 | 7 | intermittent scheduler skips |
| 11 mode-qa | 2/6* | 3 | backbone; word-tower 86→87% |
| 12 telemetry | 2/6 | 1 | results_viewed unresolved |

*`code shipped` counts the 07-03/07-04 tier-ships. 07-02/05/06 were docs-only salvage across ALL lanes — lane code was written+backed-up but dropped at gate, then restored the next night. So "2/6" undercounts effort; it counts nights the code actually reached master unblocked.

## What's working well
- **Salvage never zeroes a night + never regresses master** — 3 docs-only salvages this window, 0 pushed-code reverts. The floor holds.
- **Reduced-strength gate tiers** (typecheck-tier 07-03, baseline-red 07-04) are the only reason ANY code shipped — full re-gate wedged 4 of 6 nights.
- **Mandatory-Minimum-Artifact** — every lane produced its `lane-NN-*.md` even when degraded.
- **Backbone lanes** (02/03/05/11) produce quality work consistently when the gate lets it through.

## Friction points
- **Gate wedge is the dominant failure mode** — 3 docs-only salvages + 2 idle-timeouts (rc124, 07-04/05) + recurring `VirtualGamesList.tsx:39:23` lint. The gate, not the lanes, is what drops code. Salvage→restore treadmill is masking this, not fixing it.
- **Supabase MCP drought ~12 nights** — lanes 01/02 write DB migrations that never apply; 4 pending. Pure wasted effort until a token lands.
- **Founder-roadmap vs loop lag** — lanes 05 polished word-alchemy + word-forge (per Telegram votes) days before the 07-06 cull deleted both. No signal fed the roadmap change back into lane prompts.
- **Intermittent lanes 04/10** skipped 3+ nights (scheduler/budget) — silent, no "why skipped" line.

## Concrete improvements (ranked by impact × effort)
1. **Fix the `VirtualGamesList.tsx:39:23` lint at source** — S effort, removes a recurring full-gate blocker that wedged 07-03 & 07-06. File: `fe-next/components/admin/today-games/components/VirtualGamesList.tsx`. Impact: fewer gate fails → more nights ship at full tier, not salvage.
2. **Scope Vitest to changed-files + per-suite hard-kill in the gate** — M effort, infra. Kills the idle-900s→rc124 wedge (07-04/05). Impact: gate finishes deterministically; salvage becomes rare not routine. File: gate runner in `scripts/nightly/`.
3. **Add a "mode liveness" check to lane 04/05 prompts** — S effort, prompt-only. Before pitching/polishing a mode, grep it's still routed (not in a recent `remove.*mode` commit). Impact: no more polish on cull-bound modes (wasted 2 lane-05 slots this window). File: lane 04 + 05 prompts.
4. **Supabase PAT preflight probe + never-expire mint** — M effort, HUMAN + infra. The #1 blocker; 4 migrations rotting. Impact: unblocks 01/02 DB work that's been dead ~12 nights.
5. **Emit a one-line "skipped: <reason>" for scheduler-skipped lanes** — S effort. Lanes 04/10 vanish silently 3+ nights; a reason line distinguishes "no work" from "never ran". File: `scripts/nightly/run.sh`.
6. **Feed roadmap deltas back into prompts** — M effort. When a mode is removed, a post-cull hook should strike it from lane rotation prompts. Impact: closes the vote↔roadmap lag gap.

## New helper scripts to ship
- **`scripts/nightly/tools/mode-liveness.sh <slug>`** — S. Greps `git log --since=14d` for `remove.*<slug>` + checks the slug is still referenced in a live route/registry; exits nonzero if cull-flagged. Called from lane 04/05 STEP 0 before committing a polish target. Removes the recurring "polished a dead mode" waste. (fires ≥3× this window → justified)

## New product/feature ideas surfaced by this week's data
- **Word Tower daily challenge (Layer B)** — cited: word-tower stuck at 87%, the ONE blocker is the daily-leaderboard backend + a 1-attempt/day design call. Highest-readiness mode; shipping daily could be the first fully-public beta graduate.
- **Fix `results_viewed` then gate a "results→replay" funnel experiment** — cited: lane 12 confirms the event fires 0× for 14d despite a live call site (`SinglePlayerResults.tsx:189`). Once instrumented, the results screen is an un-measured retention surface.

## Loop changes shipped THIS WEEK
- `c826e1924` (07-02) skip-next-build-TS-phase in gate — partial wedge relief; did NOT fully close (07-05/06 still salvaged).
- Salvage→restore pipeline exercised 05→06→07 (`e347fe29c`, `8d3e2adb2`) — validated but treadmill, not cure.
