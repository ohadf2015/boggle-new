# Loop Self-Review — 2026-06-25

## Run health (last 6 report nights, 06-20..06-25)
| Lane | Success | Avg files | Notes |
|------|---------|-----------|-------|
| 01 triage | 5/6 | 2 | 1 revert = 06-22 exit-75 usage cascade (infra, not code) |
| 02 perf | 6/6 | 4 | workhorse; JWT local-verify, batch windowing, policy consolidation |
| 03 engagement | 5/6 | 5 | 4 dark exps wired live; render-path-first works |
| 04 competitor | 4/5 | 3 | prompt-only, docs-only — 0 timeouts ever |
| 05 polish | 5/5 | 5 | crossword/sealed-bid/alchemy TDD; 8f on 06-25 Showdown |
| 06 seo | 5/6 | 5 | 1 revert = 06-22 cascade; ES/SV landing pages top rework burden |
| 07 self-learn | 5/5 | 3 | prompt-only, 100% ship |
| 08 adsense | 4/4 | 4 | ISR + JSON-LD; 0 gate failures |
| 09 monetization | 5/5 | 2 | Monetag zone + CSP + ad-UX |
| 10 dict | 5/5 | 6 | es/sv/he banks + clue craft |
| 11 mode-qa | 2/2 | 5 | NEW 06-24; word-tower 28% audit; modeqa:ontrack ×2 |
| 12 telemetry | 1/1 | 1 | NEW 06-24; registry audit (59 dead events) |

## What's working well
- **Prompt-only / low-MCP lanes (04, 07, 12) never time out.** The reliability lever is eliminating MCP round-trips + full-test self-checks — confirmed 20+ nights.
- **Core 6 lanes (02, 05, 07, 08, 09, 10) at 100%, 0 reverts in window.** The reliable spine; both reverts (01, 06) were the same 06-22 usage-cascade, not code.
- **Salvage floor holds.** Every gate-failed night (06-22/23/24) still shipped docs + backed up code to `salvaged-code-*`. No night produced zero.
- **Founder-directive fast path is the highest-signal input.** 6 free-text directives 06-24 → sealed-bid Showdown + word-tower refocus shipped 06-25. Feedback has shifted from idea-pitching to implementation direction; the loop absorbs it same-run.

## Friction points
- **Gate-runner is now the #1 failure source, not lane code.** 3 of last 6 nights gate-failed for runner reasons: 06-22 + 06-24 full-Vitest **900s idle wedge** (exit 124), 06-23 **baseline-red tests** (DrillWordLimits, PracticeWheelSandbox, WordTowerCrane pre-fail on clean master). Clean code shipped UNVERIFIED twice; once dropped entirely. Affects ALL code lanes.
- **Shared-usage cascade has no stagger.** 06-22 lane 01 hit a 120s usage backoff > its deadline → exit 75, took lanes 01+06 down together. No lane-start stagger or concurrency cap vs the shared 1M-token Claude window.
- **ES/SV SEO landing pages re-touched 5×/6 nights** (`juego-de-palabras-multijugador`, `swedish-multiplayer-word-game`) for the same scrabble-cluster CTR gap with no measured movement yet — risk of churn without 28d-lag confirmation. Lane 06 already holds these on a ride; keep it.
- **word-tower readiness stuck at 28%** — 3 files >500 lines block the polish + flag wiring that lanes 05/11 need. Same structural blocker as PageClient/leaderboard. Refactor debt is gating multiple lanes.

## Concrete improvements (ranked by impact × effort)
1. **Scope gate tests to changed files.** Make the isolated-gate run `vitest related <changed files>` (or quarantine the 3 known-red suites) instead of the full suite. Effort: M. Impact: kills the 900s-idle-wedge (exit 124) AND the baseline-red poison — the two top failure modes; restores VERIFIED ships. File(s): the nightly gate-runner script under `scripts/nightly/` (gate/isolated-gate step).
2. **Hard per-suite test timeout + kill, never 900s idle.** If gate-test step exceeds e.g. 180s, SIGKILL the suite and mark `TESTS-INCONCLUSIVE` immediately rather than idling to the 900s wall. Effort: S. Impact: turns a full run-killing timeout into a fast inconclusive flag; saves ~12 min/wedge-night. File(s): gate-runner script.
3. **Triage the 3 baseline-red suites (lane 01 task).** Add a one-line lane-01 prompt directive: "FIRST, if DrillWordLimits / PracticeWheelSandbox / WordTowerCrane are red on clean master, fix or `.skip` them with a tracking comment." Effort: S. Impact: removes the gate-poison root cause. File(s): lane-01 prompt + the 3 test files.
4. **Lane-start stagger + concurrency cap.** Sleep `lane_index * 20s` before each lane's first Claude call; cap concurrent code lanes at N. Effort: M. Impact: prevents the exit-75 usage-cascade (cost lanes 01+06 on 06-22). File(s): `scripts/nightly/run.sh` (lane dispatch loop).
5. **Add a `>500-line refactor` micro-lane budget hint.** Several lanes (05/11 word-tower, 03 leaderboard) are blocked by the same big files. A lane-05/11 prompt note: "if a target file >500 lines blocks the change, extracting ONE sub-component IS the night's shipped slice." Effort: S. Impact: unblocks word-tower (28%→) + flag-wiring backlog incrementally. File(s): lane-05/11 prompts.

## New helper scripts to ship
- **`scripts/nightly/tools/changed-test-scope.sh`** — given the lane's `git diff --name-only` set, emit the matching `*.test.ts(x)` files (or `vitest related` args) for a scoped gate run. Removes the full-suite wedge that fired 06-22 + 06-24. Called from the gate-runner. Effort: M. (Defer build to a code lane with budget; this lane is docs-only — logged here as the actionable spec.)

## New product/feature ideas surfaced by this week's data
- **Sealed-bid daily-challenge variant** (founder directive 06-24, Showdown slice shipped 06-25): full daily mode = "find the best word nobody else thought of; your bet × rarity = points." Cite: founder free-text #5 + `polish:try` on sealed-bid reveal. Highest founder-signal idea.
- **`/es/multiplayer` LCP code-split fix as a growth play, not just perf.** ES "scrabble online" sits at 10k impr / pos 5.2 / 0.34% CTR (~568 click/day deficit) AND the ES MP page is the +53% LCP regression (5.9s). Same audience, two problems — a perf fix on that route compounds with the SEO ride. Cite: lane 02 LCP watch + lane 06 scrabble cluster.
- **brain-drill `game_started` event = unblock a real funnel.** Lane 12 found brain-drill has `game_completed` but no entry event → completion-rate is uncomputable. Cheapest telemetry fix with the biggest analytics payoff. Cite: lane 12 registry audit (59 dead events, this is the #1).

## Loop changes shipped THIS WEEK
- Infra self-heal from prior weeks held: `d91b5fa76` off-master preflight auto-recover + `8c705ba2c` MCP boot retry contained the 06-22 cascade to 2 lanes instead of all. New never-expire Supabase PAT recovered the 06-21/22 token drift by 06-23. No NEW loop-runner change landed this week — the gate-runner test-scoping fix (improvement #1 above) is the top un-shipped item carried into next week.
