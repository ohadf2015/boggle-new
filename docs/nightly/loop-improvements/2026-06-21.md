# Loop Self-Review — 2026-06-21

## Run health (last 6 nights, 06-16..06-21)
| Lane | Code-ship rate | Avg duration | Avg files | Notes |
|------|------|------|------|------|
| 01 triage | 0/6 | ~20m | 2.8 | MCP/Sentry blocked 4-5/6; queues fixes it can't apply |
| 02 perf | 0/6 | ~30m (+gate re-runs) | 3.5 | Supabase MCP down 5/6; REST fallback works when used |
| 03 engagement | 1/6 (wired flags 4/6) | ~19m | 3.2 | 06-20 timed out (exit 124, 3h idle) |
| 04 competitor | 0/6 (docs by design) | ~12m | 3.0 | Never times out; Reddit jq errors persist |
| 05 landing | 3/6 | ~28m | 6.2 | **Best shipper** — mode polish, 0 reverts |
| 06 seo | 2/6 | ~10m | 7.0 | High-impact CTR wins; native review clean |
| 07 self-learn | docs only | ~8m | 3.5 | Cut at time limit 06-21 |
| 08 adsense | 0/6 (docs by design) | ~25m | 3.7 | Structural bar cleared |
| 09 monetization | 0/6 | ~10m | 2.5 | Monitoring; revenue stale |
| 10 dictionary | 0/6 | ~30m | 5.5 | Maintenance |

(06-20 was a partial run: lanes 01-05 idle-killed by usage cascade. 06-19 `run-011237` aborted at preflight on github DNS fail; 11:41 manual re-run recovered.)

## What's working well
- **Lane 05 mode-polish is the engine** — 3/6 code ships, 0 reverts, 100% `polish:try` callback rate (13/13). The "improve one admin-beta mode/night, TDD pure module" recipe is the single most productive loop pattern.
- **Lane 06 SEO punches above weight** — 2 ships, +800 and +568 projected clicks, zero MCP dependence, ~10m duration. Low-MCP lanes are both fastest AND most reliable.
- **Gate clean-ship held** — the two wedge fixes from last window prevented any `next build` drop this window. Salvage-floor (mandatory artifact) meant even the 06-20 cascade and 06-19 abort shipped *something*.
- **REST-API fallback** rescued lane 02 when Supabase MCP was Unauthorized (advisor 20→0 live).

## Friction points
- **Stale MCP token (lanes 01/02)** — `SUPABASE_ACCESS_TOKEN` in `~/.claude.json` rots independently of shell env (launchd inherits none). Unauthorized 5/6 nights. This is the #1 value-leak: triage + perf lanes can't apply their own findings, so security migrations and index work queue indefinitely.
- **No lane-start stagger** — 06-20: lane 03 idled >300s, exit 124, then 4 more lanes died as the shared Claude usage window drained. One slow lane cascades into a half-lost run.
- **Brittle preflight** — 06-19 a single github.com DNS blip aborted the entire run; no retry.
- **Gate re-runs inflate lane 02 wall-clock** — baseline-red re-check adds ~30m; acceptable but it's the longest tail.
- **Lanes 09/10 produce monitoring-only docs most nights** — low signal-per-token.

## Concrete improvements (ranked by impact × effort)
1. **Preflight credential probe + auto-swap** — before lane 01 runs, curl Supabase Mgmt API `/v1/projects` with the `~/.claude.json` token; on 401, attempt jq-swap from a known-good env var and Telegram-alert if none. Effort: M. Impact: unblocks lanes 01/02 autonomy (currently 0/6 ship). File(s): `scripts/nightly/lib/preflight.sh` (or wherever preflight lives) + new `scripts/nightly/tools/mcp-token-probe.sh`.
2. **Stagger lane starts under shared usage** — insert a small offset between lane spawns and abort-don't-idle when a lane stalls >Ns (06-20 cascade). Effort: S-M. Impact: prevents 5-lane simultaneous exit-124 loss. File(s): `headless.sh` orchestration loop.
3. **Retry transient preflight network fail** — wrap the github.com / DNS preflight check in a 3× retry w/ backoff before aborting the run. Effort: S. Impact: avoids whole-run aborts on a blip (06-19). File(s): preflight script.
4. **Demote lanes 09/10 to every-other-night or merge monitoring into lane 01** — they ship monitoring-only docs 6/6. Freeing their usage-window slice reduces cascade risk. Effort: S. Impact: more usage headroom for shipping lanes. File(s): nightly lane schedule config.
5. **Lane 05 locale-first guard** — prompt edit: STEP 0 must write all 5 locale keys BEFORE touching the component (es.js key dropped 06-19 from working-set cap). Effort: S. Impact: kills the recurring raw-key-in-Spanish bug. File(s): lane 05 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/mcp-token-probe.sh`** — curl Supabase Mgmt API with the `~/.claude.json` token, print OK/EXPIRED, exit non-zero on 401 so preflight can branch. Removes the manual "is the token alive?" check that recurred 5 nights. Called from preflight. Effort: S. (NOT created this lane — out of time budget; queued for a future lane 07 or human.)

## New product/feature ideas surfaced by this week's data
- **Word-Hunt "tap leaderboard" hint is dead** — `/he/daily/word-hunt` rage-click score 0.693 (lane 03, 06-21). 06-21 wired `exp-wordhunt-hint-v1` to suppress it; if the variant wins, remove the dead affordance entirely.
- **`/he` LCP 6.3s + `/es/mp` CLS 0.288** (lane 02) — both point at an unoptimized above-fold asset (mascot/splash). A single critical-image `priority`/preload fix could recover the worst Core Web Vitals on the two highest-traffic locales.
- **Crossword hidden meta-answer** (lane 04, 06-21) — surfaced as a competitor-research idea; pairs with the already-shipped crossword MP capture-lock for a daily-puzzle retention loop. Cite: lane 04 06-21 top idea.

## Loop changes shipped THIS WEEK
- `e9c75581b` / `85789f68f` — nightly cutoff-guard + silent-usage-throttle handling + cascade circuit breaker (from 06-20 loop-improvements). Reduces but does not eliminate the 06-20 cascade; stagger (improvement #2) still needed.
