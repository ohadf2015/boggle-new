# Loop Self-Review — 2026-07-06

## Run health (last 6 nights, 07-01..07-06)
| Lane | Ship rate | Avg files | Notes |
|------|-----------|-----------|-------|
| 01 triage | 5/6 | ~5 | 07-01 exit-75 cascaded whole run; else idempotent REVOKE/index work |
| 02 perf | 6/6 | ~9 | backbone: boot-loader CLS, socket-stall, Pixi null-guards |
| 03 eng | 6/6 | ~5 | rage-click + daily-first funnel, render-path wired |
| 04 comp | 2/3 run | ~3 | idea:build 3/3; skipped 3 nights (scheduler) |
| 05 landing | 6/6 | ~13 | mode polish 3/4 shipped, 0 reverts |
| 06 seo | 5/6 | ~9 | native 5-locale meta + RU blog |
| 07 self | 5/6 | 2 | 07-01 cascade-killed; else 2 docs/night |
| 08 adsense | 4/6 run | ~3 | JSON-LD+ISR; hero-image gen still blocked |
| 09 mon | 4/6 run | ~4 | 07-01 exit-1; funnel + rewarded warming |
| 10 dict | 2/6 run | ~7 | 07-01 exit-75 + scheduler skips; high throughput when run |
| 11 qa | 6/6 | ~12 | word-tower 82→86% (FTUE+a11y+perk-display) |
| 12 telem | 5/6 | ~2 | event-registry fixes; results_viewed still 0 |

**Gate:** 07-01 early cascade abort · 07-02 docs-salvage · 07-03 typecheck-tier · 07-04 baseline-red override · 07-05 docs-salvage · 07-06 FIRST clean full ship + restored 07-05 drop. No exit-124 timeouts this window.

## What's working well
- **Reduced-strength gate tiers are a real improvement** — 07-03 typecheck-tier + 07-04 baseline-red override shipped code that a blanket docs-only drop would have lost. This is the right direction: degrade gracefully, don't zero the night.
- **Salvage → next-night restore closes the loop** — 07-05 dropped lane code shipped 07-06 (`e347fe29c`). No lane code permanently lost.
- **Telegram polish:try votes have ~1-night turnaround** — vote 07-02 → ship 07-03. Fast, tight feedback loop; keep the mode-polish cards flowing.
- **Perf/eng/landing/qa 6/6** — the backbone lanes never miss. Their eslint-changed-only + single-commit discipline is the model.

## Friction points
- **Supabase MCP token absent ~11 nights** — silently zeroes ALL DB work (lanes 01/02). 3 migrations written-but-unapplied. This is now the single biggest unrealized-value leak in the loop: DB lanes do research, write migration files, and cannot apply them. Human-blocked (60s PAT mint) but the loop keeps re-attempting nightly with no alert escalation.
- **Single early-lane crash aborts downstream lanes (07-01)** — lane 01 exit-75 killed 03/04 before launch. Lanes are NOT isolated; one crash cascades. 4 lanes lost that night.
- **Scheduler skips concentrate on lanes 04/08/10** — dict + competitor + adsense ran only 2-4/6. If the brief-gated scheduler keeps de-prioritizing them, throughput on dict/SEO-content silently drops.
- **`results_viewed` investigation keeps timing out (lane 12)** — 3rd night hitting budget before root cause. Needs a scoped one-shot probe, not a from-scratch investigation each night.

## Concrete improvements (ranked by impact × effort)
1. **Isolate lane failures (continue-on-error)** — one lane's non-zero exit must NOT skip downstream lanes; only the GATE gates the push. Effort: S. Impact: recovers ~4 lanes/cascade-night (07-01 lost 01/03/04/07/09/10). File: `scripts/nightly/run.sh` (lane dispatch loop — trap per-lane rc, log+continue).
2. **MCP-token preflight probe + LOUD escalation** — preflight pings Supabase MCP; on failure, Telegram-alert "DB lanes degraded, mint PAT" ONCE (not silent) and set an env flag so lanes 01/02 skip DB-write attempts and log "deferred: MCP down" instead of failing. Effort: S. Impact: converts an 11-night silent drought into a visible 1-tap human fix. File: `scripts/nightly/tools/mcp-health-probe.sh` (new) + preflight call.
3. **Scope Vitest to changed-files + per-suite hard kill in the gate** — close the last of the gate wedge (07-02/05 still salvaged). Never a 5400s idle; per-suite timeout with named-suite kill. Effort: M. Impact: full-ship rate 1/6 → target 5/6. File: gate script (next-build TS phase already skipped `c826e1924`; add `vitest related --run` + `--test-timeout`).
4. **Persist a `results_viewed` probe script** — codify the exact HogQL/distinct_id-overlap query lane 12 keeps re-deriving. Effort: S. Impact: turns a 3-night-timeout investigation into a 1-call lookup. File: `scripts/nightly/tools/posthog-event-trace.sh` (new).

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health-probe.sh`** — pre-run probe of Supabase MCP reachability; emits `MCP_DB_OK=0/1` env + one Telegram alert on cold. Removes the nightly silent-drought re-attempt. Called from preflight. Effort: S.
- **`scripts/nightly/tools/posthog-event-trace.sh`** — parametrized HogQL: given an event name, return 14d fire-count + distinct_id overlap with a funnel step. Kills lane 12's repeated hand-authored query for `results_viewed`/`game_started`. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Word Vault escape-room redesign** — founder-directive + `idea:build` YES 07-02, still unshipped 3 report nights later. Cite: learnings FOUNDER DIRECTIVE 06-30 + Telegram idea:build `abcec326`. Lane 05 should claim it as next mode-polish target.
- **Word Tower daily leaderboard (Layer B)** — the named ONE blocker keeping readiness at 86% (lane 11, 6/6 nights). Cite: modeqa readiness 82→86%, spec `docs/specs/word-tower-daily-seed.md`. Needs founder's 1-attempt-vs-unlimited call — surface as a Telegram decision card.
- **`results_viewed` funnel gap** — event wired but 0 fires 14d means the post-game results screen may not be reaching users on the classic path, or consent is dropping it. Cite: lane 12 07-06. If real, it's a retention-visibility hole worth a lane-02 render-path trace, not just a telemetry fix.

## Loop changes shipped THIS WEEK
- `e347fe29c` feat(nightly-salvage): ship 2026-07-05 lane code dropped on gate wedge — the salvage→restore path from last week's loop-improvements now demonstrably recovers dropped code the next night.
- `c826e1924` (07-02, prior week's proposal) skip next-build TS phase in ALL gate tiers — reduced but did not eliminate gate wedges; improvement #3 above is the follow-through.
