# Loop Self-Review — 2026-05-22

> **Data caveat:** clean full sweeps so far = 05-21 (×1). The two 05-22 runs (18:14, 19:36) were manual re-runs after the 02:39 dedup-gate abort; both shipped lanes 4+6 but their logs cut off mid-sweep and no integration-commit landed (output is the current WIP tree: `word-alchemy/`, SEO pages, `sv.js`). Rates below are N≈1.x, not N=7.

## Run health (last 7 nights)
| Lane | Success | Avg duration | Avg files | Notes |
|------|---------|--------------|-----------|-------|
| 01 triage | 1/3 | ~12m | 4 | shipped AvatarRendererSsr 05-21; **NEW: timeout-reverted both 05-22 runs (~15m44s)** after feedback-digest injected |
| 02 perf | 1/2 | ~11m | 3 + 1 migration | shipped `sync_coins` 05-21; 05-22 reverted on 9>8 file cap |
| 03 engagement | **0/3** | ~12m (= ceiling) | 0 shipped | exit-124 silent hang ×2; 05-22 also blew 8-file cap (10, then 31) |
| 04 competitor | 3/3 | ~7m | 2 | reliable; Reddit fully blocked, pivoted to pullpush/WebSearch |
| 05 landing | **0/2** | ~22m (= ceiling) | 0 shipped | exit-124 silent hang ×2 even after budget raised to 1500s |
| 06 seo | 2/2 | ~9m | 4 | ES/SV/EN meta; cheapest reliable shipper |
| 07 self-learn | 1/1 | ~8m | 2 | learnings.md + this file |
| 08 adsense | 1/1 | ~9m | 2 | GuidesCalloutLink; only ran 05-21 |

## What's working well
- **WIP-safe scoped revert is now battle-tested.** `81b3c8680`+`558169c97` mean a reverting lane no longer eats concurrent founder edits — proven across two 05-22 re-runs that left founder WIP intact. Last week's worst wound, closed.
- **Per-lane isolation held under fire twice.** 05-22: lanes 1+3+5 failed, lanes 4+6 still shipped. The failure of half the lanes never blocks the other half.
- **Lanes 4 + 6 are the dependable core.** 3/3 and 2/2 respectively, cheap (~7-9m, sonnet), no reverts. The research+SEO axis is the loop's proven value generator while the build lanes stabilize.
- **Dedup-per-calendar-day gate works** (`e00becb48`) — the 02:39 run correctly no-op'd instead of double-shipping.

## Friction points
- **Budget raise did NOT fix lanes 3+5 — they hang, they aren't slow.** This is the headline. Last week proposed "raise budgets"; budgets were raised (3→1080s, 5→1500s) and **the timeouts reproduced at the new higher ceiling**, lane 5 burning the full 1500s of opus with zero output. ~37 min of compute/night (incl. opus) → nothing. The lanes are blocked on a call, not short on time.
- **Lane 1 regressed into the same hang class on 05-22** — coincides exactly with the `18734c040` feedback-digest injection into its prompt. Adding MCP/scope to a previously-healthy lane pushed it over.
- **8-file cap is a blunt instrument** — perf (baseline+watch+report = 3 docs) and engagement (experiment files) legitimately exceed it, and the cap reverts the WHOLE lane, discarding good edits. 05-22 lost a real perf baseline update and engagement experiment to off-by-one/two overflows.
- **getUpdates poller collision unresolved** — daemon + run both long-poll Telegram; acks drop; `update_id=59463629` stuck since 05-19 → the feedback feature ships zero usable signal.
- **Two 05-22 runs never integration-committed** — manual re-runs after the dedup abort produced lane 4+6 output that's still uncommitted WIP. The manual-rerun path doesn't reach the commit stage cleanly.

## Concrete improvements (ranked by impact × effort)
1. **Add heartbeat logging to `headless.sh`, then diagnose lanes 1/3/5 — STOP raising budgets.** A lane silent for 12-25m then exit-124 is hung on a tool call, not slow. Emit a timestamped line per tool invocation (or wrap each MCP call in a 90s per-call timeout). Re-run `only=05-landing` with heartbeat on to see the exact stall point. Effort: S (logging) → M (per-call timeout). Impact: recovers ~37 min/night of dead opus compute; unblocks the only two lanes that never ship. Files: `scripts/nightly/lib/headless.sh`, `prompts/0{1,3,5}-*.md`.
2. **Revert the lane-1 feedback-digest injection OR move it behind a per-call timeout.** Lane 1 was 1/1 before `18734c040`, 0/2 after. Either gate the digest read so it can't hang, or feed feedback to lane 7 (which already parses it) instead of lane 1. Effort: S. Impact: restores the highest-value lane (Sentry triage). File: `prompts/01-triage.md`, `scripts/nightly/lanes/01-triage.sh`.
3. **Count docs separately from code in the file cap (or +2 for perf/engagement).** Don't revert a whole lane for writing one report file over the line. Effort: S. Impact: stops discarding good perf/engagement edits. File: the cap check in `scripts/nightly/lib/*` or `run.sh`.
4. **Resolve the getUpdates collision** — skip the run's own poll when the daemon PID is alive; read the daemon's buffer instead. Effort: M. Impact: unblocks the entire Telegram feedback signal (currently zero real acks). Files: `run.sh` feedback-poll block + daemon script.
5. **Make manual re-runs reach the integration-commit stage.** The two 05-22 re-runs left shippable output uncommitted. The post-dedup manual path should still run the gate+commit. Effort: S–M. Impact: lane 4+6 work stops stranding as WIP. File: `run.sh`.

## New helper scripts to ship
> Per user principle: anything repeatable → script it. Both below have fired ≥3 nights; proposing, not creating (lane 7 2-file cap this run = learnings.md + this file).
- **`scripts/nightly/tools/reddit-pull.sh`** — STILL not created (proposed last week, dir `tools/` doesn't exist). But the recommendation has CHANGED: Reddit is now fully unreachable for recent threads (reddit IP-block + pullpush 12mo lag + site: tool-limit). The helper should wrap the *actual* working path = WebSearch SERP + named competitor domains, NOT pullpush. Defer until lane 4's working recipe stabilizes. Effort: S.
- **`scripts/nightly/tools/lane-heartbeat.sh`** — emits `[lane][HH:MM:SS] tool=<name> elapsed=<s>` so a hung lane is visible in the log instead of a 12-min silence before exit-124. Underpins improvement #1. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Shareable Wordle-style emoji result card for MP results** — lane 4 (05-22, top idea): LexiClash MP results have NO share mechanic; Wordle went 90k→300k DAU in one month on emoji-grid sharing; Parseword reviews note sharing is expected. Frontend-only (emoji score, no spoilers, winner+delta+best-combo+time; RTL fix exists). Cites `ideas/2026-05-22.md`.
- **Lazy/deferred game-client hydration on `/multiplayer`** — lane 2: `/en/multiplayer` LCP p75 4228ms POOR at n=29 (now high-confidence). Game-client hydration is the suspected LCP element; split the socket/game bundle out of the critical path. Cites `perf-watch.md`.
- **Funnel fix: homepage→game_start is the biggest leak (−40.5%, n=37)** — lane 3 (05-22): the reverted `exp_landing_hero_cta_v1` (CTA above the mode grid) targets exactly this drop, hypothesis 59.5%→70%+. Worth re-attempting once lane 3's timeout is fixed. Cites lane 3 funnel.
- **Confirm /ja & /he CLS regressions are admin-skeleton-only before acting** — lane 2: `/ja` CLS 0.572, `/he/word-tower` CLS 1.050 at n<10, all admin sessions. If real-user data confirms, it's a quick loading-height/SSR-skeleton match fix; if admin-only, ignore. Cites `perf-baseline.json`.

## Loop changes shipped THIS WEEK
- `3e0217129` run on dirty tree instead of aborting (closed the 3-night preflight-abort streak — last week's #1).
- `81b3c8680` + `558169c97` + `8fe8ddb6b` — scoped WIP-safe revert; exclude founder WIP from autonomous commit (closed last week's #1 friction).
- `e00becb48` — dedup per calendar-day, not 18h rolling.
- `ab9525195` — schedule at 02:00 local.
- `39cd78976` — stop wasting timed-out lanes + salvage reports on gate fail.
- `05f12f465` + `18734c040` — founder Telegram texts steer next run; player feedback fed into loop (but see improvement #2 — it regressed lane 1).
- **NOT shipped:** lane-budget raise was applied (3→1080s, 5→1500s) but is now disproven as the fix — see improvement #1. `tools/reddit-pull.sh` proposed last week never created.
