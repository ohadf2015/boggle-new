# Loop Self-Review — 2026-07-13

## Run health (last 6 nights, 07-08..07-13)
| Lane | Success rate | Avg files | Notes |
|------|--------------|-----------|-------|
| 01 triage | 6/6 | ~18 (incl. restores) | Sentry null-guards + file restore; MCP migrations blocked |
| 02 perf | 6/6 | ~2-3 | /en LCP −66%, MP CLS fix, homepage LCP still 6319ms |
| 03 engagement | 6/6 | ~1-3 | funnel flags + PostHog-delta revert; 33 dark flags found |
| 04 competitor | 2/6 | n/a | scheduler-skipped 4 nights; ideas fed to 05 when run |
| 05 landing | 6/6 | ~2-4 | mode polish; 07-12/13 research-only (over-scope) |
| 06 seo | 2/6 | ~3-5 | scheduler-skipped; ES education queries when run |
| 07 self-learn | 1/6 | 2-3 | scheduler-skipped 5 nights |
| 08 adsense | 3/6 | ~1-8 | JSON-LD/BreadcrumbList; hero-image blocked |
| 09 monetization | 4/6 | ~3-8 | daily-first funnel + rewarded warming |
| 10 dict | 1/6 | ~7 | scheduler-skipped 5 nights |
| 11 mode-qa | 5/6 | ~2-4 | word-tower GRADUATED; crossword 0→75%; rc124 07-11 |
| 12 telemetry | 3/6 | ~1-3 | results_viewed per-mode wiring; game_abandoned gap |

## What's working well
- **Full-gate pass returned (07-13)** — first clean full merge in the window after 4 docs-only nights. Small lanes + eslint-changed-files self-check make the gate passable. The salvage system is the safety net, not the norm.
- **Mode-QA treadmill graduates modes** — word-tower Released 07-10; crossword 0→75% in 3 nights. The systematic per-night audit cadence (logic→hooks→edge-cases→visual) is repeatable and produces a shippable mode every ~4 nights.
- **Self-correcting engagement lane** — 03 caught + reverted its own regression (exp-mp-room-join 1→16 rageclicks) via PostHog delta, no human in the loop.
- **Supabase MCP cold-boot retry recovered the token 2/6 nights** — the retry mechanism added earlier works; drought is no longer total.
- **Feedback→build loop closed** — both `polish:try` votes became shipped lane-05 slices within 1-2 nights.

## Friction points
- **Supabase MCP recovered but migrations NOT applied** — 07-11 + 07-13 had a working token, yet the 4 pending migrations still sat unapplied. The lanes detect recovery but have no "drain the migration queue" step. This is now the binding gap, not the token itself.
- **Usage-limit backoffs on 6/6 nights; 07-11 mode-qa rode 10 backoffs → rc124 → revert.** Heavy lanes (11, 06) burn the rate ceiling. No stagger + no per-suite kill = a wedged suite rides the idle backstop to a timeout that's indistinguishable from a real failure.
- **Scheduler skips half the lanes** — 04/06/10 ran 2/6 or less; 07 self-learn ran 1/6. Intermittent lanes mean carry-forward watches (Blast Ghost-Round, blog cadence, dict throughput) stall for days.
- **Lane 05 over-scoped into 2 research-only nights (07-12, 07-13)** — spec'd but shipped no code. The "write all locales first then a slice" rule isn't preventing the over-scope.
- **`game_abandoned` still uninstrumented** — the 53% completion crater can't be diagnosed without knowing WHERE users drop; every night it stays a mystery is a night of blind funnel work.

## Concrete improvements (ranked by impact × effort)
1. **Migration-drain step on MCP recovery** — when lane 01/02 detects a working Supabase token, immediately apply the queued migrations in `docs/nightly/pending-migrations/` (or wherever the 4 live) before other work. Effort: M. Impact: unblocks 18-night backlog (RLS-initplan, REVOKE, SECURITY DEFINER audit) the moment the token is up. File(s): lane 01/02 prompts + a `scripts/nightly/tools/apply-pending-migrations.sh` wrapper.
2. **Per-suite Vitest hard-kill + changed-files scope in the gate** — never let a wedged suite ride the idle backstop to rc124. Effort: M. Impact: kills the #2 infra failure (07-11 mode-qa revert; recurring rc124). File(s): the gate runner script (scripts/nightly gate), lane 11 verify path.
3. **Wire `game_abandoned` before any more funnel analysis** — one instrumentation task, then the 53% crater becomes diagnosable. Effort: S. Impact: unblocks lane 03/12 funnel work that's currently blind. File(s): the game session lifecycle hook (single emit on unmount/route-away without completion).
4. **Stagger heavy lanes (11 mode-qa, 06 seo, 08 adsense)** — don't schedule the two most backoff-prone lanes adjacent. Effort: S. Impact: fewer 6/6 backoff nights, fewer rc124 timeouts. File(s): the lane scheduler/cron config.
5. **Lane 05: enforce a ship-or-shrink rule** — if a mode-polish slice can't fully wire in-budget, ship a SMALLER slice (e.g. copy/animation-only) rather than going research-only. Two research-only nights in a row (07-12/13) = the target was too big; pick a 1-file slice next. Effort: S (prompt). Impact: restores 05's 0-research-night ship rate. File(s): lane 05 prompt.
6. **Blast `results_viewed` rename** — `BlastResultsSummary` fires `blast_results_viewed`; rename to canonical `results_viewed` to close the last silent-results mode. Effort: S. Impact: completes the per-mode results_viewed sweep. File(s): `BlastResultsSummary`.

## New helper scripts to ship
- **`scripts/nightly/tools/apply-pending-migrations.sh`** — on a night where the Supabase MCP token is live, apply every queued migration idempotently (check `applied_migrations` first, skip already-applied), log each result, alert on any failure (never silent). Removes the recurring "recovered but didn't apply" miss. Called from lane 01/02 after MCP health probe. Effort: M. (Justified: MCP recovered 2/6 nights and neither drained the queue — a clear repeatable gap.)

## New product/feature ideas surfaced by this week's data
- **Crossword "Clue Scramble" mini-anagram gate** — cited: `polish:try:crossword` vote (07-11) + lane-04 concept. Earn each clue by solving a 5-letter anagram (10s). Already spec'd 07-12; ship the slice. Adds variable-reward loop to the near-release crossword mode.
- **Blast "Ghost Round"** — cited: `idea:build:d24d4c78` (07-11, the one standing build vote). Post-round overlay flashing the top-scorer's board with 3 missed high-value words. Drives a "I could've had that" replay loop.
- **Homepage LCP quick win** — cited: lane-02 metric homepage LCP 6319ms, 393KB animated `winner.webp`. Swap the animated hero webp for a static poster + lazy-load the animation, or drop to a smaller encode. Direct LCP win on the highest-traffic page (founder priority #1).
- **Multiplayer reserved-height banner slot** — cited: lane-02 CLS 0.574–1.043 across /en,/es,/he from post-hydration banner injection. Reserve a fixed-height slot for ClassroomMode/Education/Spectator banners so they never shift layout. Turns the recurring MP CLS fire into a structural fix.

## Loop changes shipped THIS WEEK
- Supabase MCP **cold-boot retry** (recovered token 07-11 + 07-13) — the retry mechanism from a prior loop-improvement is now empirically working; the follow-on gap (apply-step) is item #1 above.
- Reduced-tier **typecheck gate** as a ship valve (07-11) — the prior week's "reduced-strength tiers" proposal shipped real code when the full gate wedged.
