# Loop Self-Review — 2026-06-20

## Run health (last 7 nights, 06-14..06-20)
| Lane | Ship rate | Avg files | Notes |
|------|-----------|-----------|-------|
| 01 triage | 7/7 | ~9 (47 on 06-20) | Security REVOKE/RLS doctrine; blocked on Sentry resolve (403 write) |
| 02 perf | 7/7 | ~4 | REST-API fallback when Supabase MCP down; `/he` LCP + `/es/mp` CLS unprofiled |
| 03 engagement | 6/7 | ~4 | Timed out 06-20 (usage cascade, 2h10m idle); flags wired+live rest of week |
| 04 competitor | 7/7 | 3 | Docs-only, never times out; ~21 ideas, 0 reverts |
| 05 landing | 7/7 | ~6 | 4 mode improvements TDD-green; es `streakNewPB` locale dropped 06-19 |
| 06 seo | 7/7 | ~7 | Timed out 06-20 then recovered rc=0; autonomous native review 7/7 |
| 07 self-learn | 6/7 | 3 | Timed out 06-20 (usage cascade); prompt-only otherwise reliable |
| 08 adsense | 7/7 | ~2 | E-E-A-T bar cleared; re-submit is a human op |
| 09 monetization | 6/7 | ~2 | Timed out 06-20 (usage cascade) |
| 10 dictionary | 6/7 | ~6 | Timed out 06-20 (usage cascade) |

## What's working well
- **REST-API fallback when Supabase MCP is Unauthorized** — lane 02 still landed policy consolidation + index drops LIVE (advisor 20→0) through 6 nights of MCP outage. The loop no longer hard-stops on one broken integration.
- **Mandatory-Minimum-Artifact floor held 7/7** — even the 5 lanes that exit-124'd tonight left a gate-clean artifact. Floor never zero.
- **Polish-mode pipeline (lane 04 surfaces → lane 05 TDD-builds)** shipped 4 admin-gated mode improvements, 0 reverts. Feedback confirms appetite (13 polish:try / 0 pass).
- **Define→wire→flag-in-one-lane** closed two PostHog experiments (practice-wheel-cta, abandon-confirm) after the rule was codified — the playbook correction worked.
- **Baseline-red `typeonly_notest` gate** let clean-built code ship over pre-existing master-red suites twice (06-15, 06-19) instead of being dropped.

## Friction points
- **Shared-usage-limit cascade (NEW, biggest loss)** — daytime Opus + nightly draw one usage window. 06-20 it drained mid-run and idle-killed 5 lanes (03, 06, 07, 09, 10); lane 03 alone burned 2h10m sleeping on a 9550s wait-gate before the 900s idle-watchdog fired. Pure dead wall-clock. Fix `e9c75581b` shipped tonight (cutoff-abort + Telegram alert) but the deeper problem — concurrency competing for one quota — is unaddressed.
- **Supabase MCP token expired 6 nights running; Sentry MCP down/403 4 nights** — recurring credential rot with no auto-probe or alert. Lane 01 silently degrades (no Sentry queries) every night.
- **Over-scoped mode-polish keeps dropping a locale** — recurred again 06-19 (es `streakNewPB`). The "5 locales FIRST" rule is in learnings but not enforced anywhere mechanical.
- **Gate verification is unreliable** — across the window it variously wedged, ran baseline-red, or went inconclusive (rc=3) tonight. Code generally still lands via isolated-ship, but operators can't trust a single "gate green" signal.

## Concrete improvements (ranked by impact × effort)
1. **Stagger lane starts + cap concurrency against the usage window** — the cascade hit because lanes run hot-concurrent into a quota daytime Opus already taxed. Add a preflight that (a) checks remaining usage and (b) serializes or delays high-token lanes (03/05/10) when the window is low. Effort: M. Impact: removes the single biggest wall-clock loss (5 lanes / ~3h tonight). Files: `scripts/nightly/headless.sh`, `scripts/nightly/run.sh`.
2. **Preflight credential probe + Telegram alert on token expiry** — proposed 06-19, still not built. One `curl` per integration (Supabase REST, Sentry, PostHog) at preflight; alert + mark-degraded on 401/403 instead of discovering it per-lane. Effort: S. Impact: kills 6 nights of silent Supabase degradation + Sentry-blind triage. Files: new `scripts/nightly/tools/credential-probe.sh`, called from `run.sh` preflight.
3. **Empty-sidecar (rc=124 + 0-byte output) → one fast retry** — tonight's silent-throttle lanes produced a 0-byte sidecar; the detector was blind and reverted. A single short retry on `rc=124 && sidecar==0 bytes` would reclaim lanes that never emitted a token. Effort: S. Impact: recover 2-3 lanes per throttled night. File: `scripts/nightly/headless.sh`.
4. **Mechanical "all 5 locales present" gate for lane 05/09 string keys** — grep the new `t('...')` keys added in a lane's diff against en/he/sv/ja/es and fail the lane if any locale is missing the key. Effort: S. Impact: ends the recurring raw-key-in-Spanish bug (es `streakNewPB`, prior es.js drops). File: new `scripts/nightly/tools/locale-parity-check.sh`.
5. **Single trustworthy gate verdict** — collapse the wedge/baseline-red/inconclusive states into one explicit verdict line (`SHIPPED_CLEAN | SHIPPED_BASELINE_RED | ISOLATED_SHIP | DROPPED`) in the report header so operators stop reverse-engineering ship status from salvage dirs. Effort: M. Impact: operator trust; faster morning triage. Files: `scripts/nightly/run.sh` report writer.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/credential-probe.sh`** — preflight curl probe of Supabase REST / Sentry / PostHog; emits a degraded-integrations list + Telegram alert on 401/403. Removes per-lane silent-degradation discovery (fired 6+ nights). Called from `run.sh` preflight. Effort: S. (NOT created tonight — out of lane-7 time budget; flagged for next run.)
- **`scripts/nightly/tools/locale-parity-check.sh`** — diff-scoped check that every new `t('key')` exists in all 5 locale files; non-zero exit names the missing locale. Removes the recurring locale-drop bug. Called from lane 05/09 self-verify. Effort: S. (NOT created tonight.)

## New product/feature ideas surfaced by this week's data
- **ES "scrabble online con amigos" landing optimization is the single highest-ROI SEO bet** — lane 06: `scrabble online` ES is pos 5.3 with 10,092 impr but 0.4% CTR vs 6% expected. That gap alone is ~568 est. clicks/period. The "con amigos" intent-meta shipped 06-20; next step is a dedicated ES multiplayer scrabble landing, not just meta. (source: 06-20 lane 06 report)
- **Game-completion drop (42%, 185 abandons/7d) is the largest funnel leak** — abandon-confirm flag is live (06-19, id 209541) but only an intercept; pair it with a one-tap "resume last game" entry on the home/landing to recover abandoners on return. (source: lane 03 funnel, 06-18/06-19)
- **Sealed-bid is the runaway feedback favorite (3 of 13 polish:try this week, 2 idea:build)** — concentrate lane 05 polish + a public-promote candidacy review here; it's the mode the founder keeps green-lighting. (source: 06-14/15/17 feedback + 06-17 idea:build "Round Table")
- **`/es/multiplayer` INP 442ms POOR + rage 0.584** — a perf fix here is also a monetization fix: it's the ES multiplayer page the scrabble SEO traffic lands on. Profile WheelRushView `elementFromPoint` before more ES SEO push. (source: lane 02/03 watch, 06-15..06-19)

## Loop changes shipped THIS WEEK
- **`e9c75581b` / PR#576 (`70de58efc`)** — `headless.sh` cutoff guard `_stall_should_abort` (abort rc=75 vs sleep past 06:30) + `_notify_stall` Telegram alert for silent multi-hour stalls; 19/19 tests. Directly addresses the prior week's "silent multi-hr gap" friction point.
- **REST-API fallback codified as doctrine** (lane 02) — applied LIVE through 6-night Supabase MCP outage, 0 reverts.
