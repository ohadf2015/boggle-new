# Loop Self-Review — 2026-06-06

## Run health (last 6 nights)
| Lane | Success rate | Timeout nights | Notes |
|------|-------------|----------------|-------|
| 01 triage | 5/6 | 06-06 (rg EACCES) | Security hardening reliable when MCP connects |
| 02 perf | 4/6 | 06-02, 06-06 | MCP-dependent; both failures = MCP collapse |
| 03 engagement | 4/6 | 06-02, 06-06 | Same MCP pattern; funnel instrumentation ships on good nights |
| 04 competitor | 6/6 | none | Zero MCP dependency = immune. Model lane. |
| 05 landing | 4/6 | 06-02, 06-06 | Over-scope on 06-06 (11 files attempted); MCP on 06-02 |
| 06 seo | 4/6 | 06-02, 06-06 | Spanish SEO momentum despite failures |
| 07 self-learn | 6/6 | none | Zero MCP, prompt-only. Model lane. |
| 08 adsense | 4/6 | 06-02 | Stable since 06-03; GSC-scope-blocked limits signal |
| 09 monetization | 2/6 | rare runs | ayeT dark-committed; low priority |

## What's working well
- **Lane 04+07 = 100% reliability** — zero MCP calls = zero timeout risk. Every MCP-free lane ships.
- **Mandatory-Minimum-Artifact** — even timed-out lanes produce status files. Zero total-loss lanes this week.
- **Founder directive fast path** — `polish:try` callbacks translate to same-night lane 05 work. Feedback loop is tight.
- **TDD-first on pure modules** — heatMeter, curator ratify, dailyLetterPool all shipped without reverts.
- **Single end-of-run commit** — clean rollback story; no partial-commit gate failures.
- **Curator feature velocity** — P0 foundation → P1 backend → admin UI shipped across 3 consecutive runs.

## Friction points
- **MCP connectivity is the #1 ship-killer.** 2 of 6 nights had MCP collapse (06-02, 06-06). Each collapse takes out 3-5 lanes. `MAX_MCP_CALLS` cap proposed 12+ nights ago, never shipped. This single fix would recover ~15 lane-nights of lost output.
- **Preflight aborts on dirty tree / wrong branch** — 06-04 01:00 + 06-05 01:00 both aborted before any lane ran. No recovery handler; entire run lost.
- **Lane 05 scope creep** — 06-06 attempted TDD + 5 locales + animation = 11 files, timed out. Pattern: ambitious scope → timeout → partial files → gate-dropped. Needs hard scope guidance in prompt.
- **GSC token scope missing 12+ nights** — lanes 06/08 have no Google Search Console data. Bing-only SEO is flying half-blind. This is a 30-second `gcloud auth` fix the user needs to do manually.
- **ripgrep EACCES (new 06-06)** — lane 01 couldn't verify callsites. Likely `.claude/` or `node_modules/` permission drift. One-time fix but blocks triage lanes.
- **Reddit draft delivery gap** — zero reddit callbacks in 7d feedback. Either drafts aren't sent, or user ignores them. Lane 04 effort may be wasted on reddit drafts.

## Concrete improvements (ranked by impact x effort)
1. **Ship MAX_MCP_CALLS=3 cap in headless.sh** — add `MCP_CALL_COUNT` env + early-exit when exceeded. Effort: S. Impact: prevents 3-5 lane timeouts per MCP-bad night. File: `scripts/nightly/headless.sh`.
2. **Preflight stash-or-warn on unpushed commits** — `git stash --include-untracked` before aborting, or switch to master + stash. Effort: S. Impact: recovers 2 lost runs this week. File: `scripts/nightly/run.sh` preflight section.
3. **Add lane 05 scope cap to prompt** — "Pick ONE slug from polish:try queue. ONE feature. Ship it complete with 5 locales. Do not attempt a second." Effort: S (prompt edit). Impact: stops 06-06-style 11-file timeouts.
4. **Fix ripgrep permissions** — `chmod -R a+r .claude/ node_modules/` or add `--no-ignore` flag. Effort: S. Impact: unblocks lane 01. File: investigate + one-liner.
5. **MCP graceful skip in lane prompts** — add "If MCP X fails to connect, skip its signals and proceed with local-only data" to lanes 01/02/03. Effort: S (prompt edit). Impact: lanes degrade instead of spinning.
6. **Reddit draft delivery audit** — verify Telegram bot sends reddit drafts. If 0 delivered, remove reddit from lane 04 to save budget. Effort: S. Impact: either fixes delivery or frees 04 budget.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health-probe.sh`** — pre-lane MCP connectivity check (timeout 10s per MCP). If fails, set `SKIP_MCP_<name>=1` env for that lane. Called from `run.sh` before each lane. Effort: S. Removes: serial MCP retry inside lanes (the timeout cause).

## New product/feature ideas surfaced by this week's data
- **Spanish-first SEO content** — Spanish queries growing +200-271% WoW (lane 06 data). Dedicated Spanish landing pages for "juego de palabras" variants could capture this wave. Source: 06-06 lane 06 impression data.
- **Invite flow fix** — 83% drop in invite funnel (18→3, lane 03). If the invite link UX is broken, fixing it is higher-ROI than any new feature. Source: 06-06 lane 03 funnel analysis.
- **Word Detective daily mode** — founder clicked `idea:build` on this (hash `6fd3c339`). Daily puzzle with detective/deduction mechanic. Source: Telegram feedback callback.
- **Sealed Bid polish** — 3× `polish:try` callbacks = strongest founder signal. Prioritize sealed-bid visual polish over other modes. Source: Telegram feedback aggregation.

## Loop changes shipped THIS WEEK
- 06-04: `LEXI_LANE_FILE_CAP` 8→999 in `headless.sh` (founder directive; enables 5-locale ships)
- 06-05: `animate-ai` skill added to lane 05 recommendation (Word Alchemy heat bar)
- 06-05: `idx_web_vitals_player_id` index shipped (perf lane; seq scan 3207ms → index)
