# Loop Self-Review — 2026-07-20

## Run health (last 6 nights run: 07-14/15/16/18/19/20; 07-17 aborted)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 5/6 | ~18m/night total | 3.5 | autonomy matrix solid; Pixi guards + REVOKE fix |
| 02 perf | 2/6 ship | (research-heavy) | 2.5 | CLS fix shipped 07-20; most nights research-only |
| 03 engagement | 6/6 | — | 2.5 | most reliable; 4 flags wired+evented |
| 04 competitor | 0/6 code | — | — | research-only by design; feeds 05 |
| 05 landing | 5/6 | — | 2.5 | mode polish; 0 reverts |
| 06 seo | 3/6 | — | 3 | rotation-gated; ES multiplayer meta |
| 07 self-learn | 0/6 code | — | 2 (docs) | docs-only by design; code-gate N/A |
| 08 adsense | 1/6 | — | 2 | rotation + Higgsfield-blocked |
| 09 monetization | 4/6 | — | 2.5 | copy/translation companion |
| 10 dict | 1/6 | — | 7 | rotation-gated |
| 11 mode-qa | 5/6 | — | 4 | crossword→90% Released; shiritori 0→55% |
| 12 telemetry | 4/6 | — | 2 | event wiring, delta-verified |

Overall: ~36/66 lane-nights shipped code (55%). Runs 18–24 min wall. Gate salvage: 07-14/07-15 (vitest rc124), then 0 for 3 nights.

## What's working well
- **Lane 03 same-run flag+event discipline** — 6/6, 0 zombie flags. The "create flag → wire render → wire event, same night, verify delta next night" loop is the strongest repeatable pattern in the loop. Lane 12 mirrors it (delta-verified +125% word-goal).
- **Gate classifier fix (`c3973c715` fork cap) held** — vitest rc124 salvage went 07-14/15 → 0 for last 3 nights. Attacking the classifier (not the tests) is paying compounding dividends.
- **Mode pipeline graduation cadence** — crossword 85→90→Released in 3 nights, then clean rotation to shiritori (0→55% in 2). Lane 11's per-mode %-tracking gives the founder a legible release signal.
- **0 pushed reverts, 45+ nights** — salvage-before-push doctrine means a gate-fail never regresses master. Zero-risk floor holds.

## Friction points
- **Supabase MCP mostly-dry (~1-2/6 nights)** — affects lanes 01/02/12. 6 migrations pending, FK/RLS audits blocked. Root = PAT/transport intermittency; needs never-expire token (human). Last week's learnings over-claimed "drought broke" — it did not.
- **Cull-list filter never shipped** — 2nd consecutive week: idea/polish generator (lane 04/05) pitches deleted modes (Word Alchemy, Word Forge), founder spends votes on dead cards. Proposed last week, not built. This is a 15-line filter, not a research problem.
- **Preflight silent-abort (07-17)** — whole night lost, no alert until 9am. The abort is correct; the silence is the bug. Recurs whenever repo is left off-master/dirty at midnight.
- **Homepage LCP regressed (+28.8%) mid-window** — lane 02 fix (`initial={false}`) landed for mascot but AnimatePresence root still serializes `opacity:0`; visual QA gate blocks the ship. Perf lane is bottlenecked on human visual-QA for above-fold changes.

## Concrete improvements (ranked by impact × effort)
1. **Ship the cull-list filter in the idea/polish generator** — hard-code the deleted-mode slugs (`party, word-alchemy, word-forge, word-vault`) as a reject-set the card generator filters against before emitting. Effort: S. Impact: stops wasting the founder's finite vote budget (4/6 votes this window hit dead modes). File(s): lane 04/05 idea-gen prompt + `scripts/nightly/` card generator if one exists.
2. **Telegram-alert on preflight abort** — when the pre-run guard aborts (dirty tree / off-master), fire a Telegram message with the reason before exiting 0. Effort: S. Impact: recovers whole lost nights (1 lost 07-17 = 1/6 of the week). File(s): `scripts/nightly/run.sh` preflight block.
3. **Pre-midnight dirty-tree cron warning** — a 23:00 cron that checks `git status --porcelain` + branch != master and warns the founder to clean up before the nightly fires. Effort: S. Impact: prevents the abort in #2 from happening at all. File(s): new cron + tiny script.
4. **Mark lane 07 code-gate N/A in run summary** — lane 07 is docs-only; its "gate fail" every night is a false-alarm inflating the 4/6 gate-failure stat and triggering needless salvage. Classify lane 07 as docs-lane so the manager summary stops counting it as a failure. Effort: S. Impact: cleaner run-health signal; less salvage noise. File(s): `scripts/nightly/` gate/summary classifier.
5. **Survival `results_viewed` wiring (lane 12 next pick)** — 56 `game_completed`, 0 `results_viewed` = a clear instrumentation hole with a completed baseline. Effort: S. Impact: unblocks survival-mode funnel analysis. File(s): survival results component.

## New helper scripts to ship
- **`scripts/nightly/tools/preflight-alert.sh`** — wraps the preflight guard; on abort, POSTs the reason to the Telegram ops chat (reuse existing token/chat env from run.sh) then exits. Removes the silent-loss failure mode that recurred 07-17. Called from `run.sh` preflight. Effort: S. (Deferred — out of this lane's 15-min budget + touches run.sh which needs careful testing; logged for a run.sh-owning session.)

## New product/feature ideas surfaced by this week's data
- **MP results "rival's best word" as a permanent feature** (source: `exp-mp-results-rival-best-word-v1` created 07-20, en-only). If the flag shows lift, the rival-comparison hook directly targets the game_started→game_completed 56%→32% drop (largest funnel leak this window) — social-comparison drives rematch. Cite: lane 03/12 funnel row.
- **Homepage cube click-feedback → onboarding tutorial trigger** (source: `exp-homepage-click-feedback-v1`, targets ES rage-clicks #1 severity, 6 reach). Rage-clicks on the homepage cube = intent + confusion; the scale/brightness feedback is a band-aid — the signal says surface a 1-tap "how to play" on repeated cube-taps. Cite: ES multiplayer rage-click row.
- **Shiritori as an ESL/education hook** (source: lane 11 shiritori 55%, lane 06 `juegos de inglés gratis` pos 9.9). Shiritori (word-chain) is a classic language-learning drill — once it graduates, route it into `/[locale]/education/esl-word-games` as a featured mode. Cite: SEO ES education query + mode-readiness row.

## Loop changes shipped THIS WEEK
- None of last week's proposed loop changes shipped (cull-filter, preflight-alert both carried unbuilt). The gate-classifier fork-cap fix (`c3973c715`) from 07-10 continued paying off (0 vitest salvages last 3 nights) — the only loop-infra win still compounding.
