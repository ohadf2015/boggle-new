# Loop Self-Review — 2026-07-31

Window 2026-07-25..07-31. All timings measured from `~/logs/lexi-nightly/run-*.log`, not from reports.

## Run health (last 7 nights)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 5/7 | ~11m | 5.4 | rc75 on 07-25 + 07-29 (both usage-window nights, not lane fault); 07-31 spent entirely on restore-queue, 0 autonomous ships |
| 02 perf | 6/7 | ~16m | 2.2 | most consistent shipper; Mgmt-API migration win 07-30; still cannot capture Layout Shifts headlessly |
| 03 engagement | 6/6 | ~15m | 2.5 | never scheduled out, never failed — the loop's most reliable lane |
| 04 competitor | 2/2 | ~9m | 3.0 | rotation-only; `reddit-fetch search` still returns garbage |
| 05 landing | 6/6 | ~15m | 2.5 | 0 reverts across the window |
| 06 seo | 2/2 | ~12m | 3.5 | rotation-only |
| 07 self-learn | 2/7 | ~24m | 3.0 | scheduled out 07-25..07-29; ran 07-30 + 07-31. Slot still not pinned |
| 08 adsense | 2/2 | ~16m | 1.5 | rotation-only; hero-image blocked on Higgsfield CLI |
| 09 monetization | 5/5 | ~14m | 2.6 | bundled findings only, no standalone feature surfaced in 5 runs |
| 10 dictionary | 1/1 | ~7m | 4.0 | lowest-frequency lane in the loop |
| 11 mode-qa | 5/7 | ~12m | 4.0 | sealed-bid 45→79%; **6th consecutive "needs 1–2 more nights"** — plateaued, not progressing |
| 12 telemetry | 2/2 | ~24m | 3.5 | **ran twice on 07-30**, wrote two contradicting blocks |

Run-level: 5 of 7 nights shipped. 07-25 shipped **zero** (pre-lane stall → 3× rc75 → circuit-breaker). 07-30 ran all 8 lanes green then **lost 8 code files to a 2h22m gate re-gate loop**.

## What's working well
- **Lane throughput is genuinely healthy when the night starts on time.** 07-26/27/28/30 ran 8 lanes each with zero lane failures. The lanes are not the bottleneck — the phases around them are.
- **Fallback gate tiers stop drop-all in the attributable case.** `baseline-red` and `typecheck-tier` carried four nights and shipped every one.
- **Lanes 03 and 05 are at 6/6 with 0 reverts.** Small-slice + `frontend-design` is a reproducible recipe; both lanes ship 2–3 files a night and never regress.
- **The Mandatory-Minimum-Artifact floor held.** Even 07-25, which shipped no code at all, left readable artifacts. No night in the window produced nothing.
- **MCP is no longer load-bearing.** The Management API raw-SQL fallback (07-30) retired a 30-night carry; a failed probe is now informational.

## Friction points
- **The gate's peel loop cannot conclude "innocent."** 07-30, verbatim: `parser returned 11 non-authored path(s) — ignoring` → re-gate without lint → `still fails rc=1` → `not a clean baseline-poison` → docs-only salvage. Every offender it found was somebody else's; it never turned that into a ship decision. Cost: 2h22m + 8 code files. Affects every lane on any night the baseline is red.
- **4 baseline-red test files are the residual `rc=1` above.** They stopped being a tax and became a drop cause.
- **Pre-lane stall, 2 of 7 nights.** 07-25 (5h03m) and 07-29 (4h23m) between run start and `baseline sha`, entirely **unlogged**. 07-25's stall consumed the usage window and cost the whole night. The other 5 nights: 14 s to 52 min. *Last night's review called this "#1 friction" — the 7-night data does not support that; it is intermittent and undiagnosable rather than chronic.*
- **The MCP probe is not the 3-hour villain it was written up as.** Measured per-attempt: 101 s (07-30), 96 s (07-31), 101 s (07-26). Only 07-29 degraded to ~1h51m/attempt under extreme load. The `mcp-probe.sh:63` unbounded-pipeline bug is real but small.
- **Lane 11 has spent 6 nights on the same verdict.** Every night re-audits code already verified clean because the one real blocker (visual QA) is a tooling problem no lane owns.
- **No lane idempotence guard.** Lane 12 ran twice on 07-30 and re-diagnosed its own uncommitted fix from scratch.

## Concrete improvements (ranked by impact × effort)
1. **Gate: add an "all offenders non-authored → SHIP" terminal state.** After 2 consecutive peels where every parser-named offender is outside the authored allowlist, ship the authored set instead of falling through to docs-only salvage. Effort: S. Impact: prevents the exact 07-30 failure — 8 code files + 2h22m per occurrence. File(s): `scripts/nightly/lib/gate.sh` (drop-and-re-gate block).
2. **Fix the 4 baseline-red test files outright.** `daily/__tests__/DailyChallengeGame.trackGameStart.test.tsx`, `tutorial/ModeCoach.test.tsx`, `api/stats/record-game/__tests__/route.test.ts`, `daily/__tests__/WordWheelResults.crossPromoTranslation.test.tsx`. Effort: S/M. Impact: removes the residual `rc=1` that makes every red night undecidable; lets the gate simply pass. Assign to lane 01 with an explicit STEP 0 override.
3. **Lane 11 prompt: escalate-on-repeat rule.** "If the previous 2 nights' verdicts are identical, do NOT re-audit — spend the night on the named blocker, or write a human-queue escalation and exit." Effort: S (prompt-only). Impact: converts a 6-night plateau into either a promotion or a clean hand-off. File(s): lane 11 prompt.
4. **agent-browser: pre-seed the cookie-consent key before first navigation.** Write the consent cookie/localStorage into the profile at browser launch so the overlay never renders. Effort: S/M. Impact: unblocks sealed-bid promotion (79% → ≥90%) *and* lane 02's Layout-Shifts capture — two lanes, 6 nights stalled. File(s): lane 02 + lane 11 prompts, or a shared launch helper.
5. **Lane idempotence guard.** Before running lane N, skip it if `### Lane <N>` already exists in tonight's report. Effort: S. Impact: kills duplicate runs (07-30 lane 12) and the contradictory reports they produce. File(s): `scripts/nightly/run.sh` lane dispatch loop.
6. **Heartbeat the 01:00→baseline gap.** Log a line every 5 min during pre-lane so the next 4-hour stall is diagnosable instead of invisible. Effort: S. Impact: the stall cost an entire night on 07-25 and is currently unattributable. Do this *before* rebuilding the phase. File(s): `scripts/nightly/run.sh` pre-lane block.
7. **Impact-check: assert the denominator first.** Report `no-exposure` when the upstream event has 0 volume, instead of `neutral`. Effort: S. Impact: stops zero-denominator checks from reading as validated-neutral (07-30: `issue-probe`, `brain-drill-word-replay`). File(s): impact-ledger writer + lane 03/12 prompts.

## New helper scripts to ship
Deferred deliberately this run — proposed, not created, because the finalize cutoff was near and a half-written script poisons the gate. Both patterns have fired ≥5 nights and are ready to codify:

- **`scripts/nightly/tools/browser-consent-seed.sh`** — launch/prepare an agent-browser profile with the cookie-consent key pre-set. Removes the manual dismissal dance that has failed 6 nights running across lanes 02 and 11. Called from lane 02 + lane 11 prompts. Effort: S/M.
- **`scripts/nightly/tools/lane-report-guard.sh`** — idempotence check: exit non-zero if tonight's report already has this lane's header. Called from the run.sh dispatch loop. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Surface Brain Drill from a mode people actually reach.** `drill_completed` = 0 for 9 days and died 07-22, *before* the 07-26 feature ship — the mode has no traffic to validate against. Building more Brain Drill features is measuring into a void; one entry point from the daily results screen would generate the denominator first. (source: lane 12 telemetry, `drill_completed` 0/9d)
- **`RoomListView` skeleton at lobby dimensions.** MP CLS is 0.92–0.96 CRITICAL with the root cause confirmed (socket `connecting→lobby` full-viewport DOM swap; `NativeLanguageBanner` and `CookieConsent` both ruled out). This is the single largest CWV defect in the app and directly serves founder priority #1 (SPEED). 4–6h — genuinely human-queue, not a lane. (source: lane 02 perf, 07-28 + 07-30)
- **Measure the teacher district upsell that shipped 07-30.** `ef3df8473` landed the education upsell but no lane has checked its funnel. Education growth is founder priority #3 and this is the first real conversion surface in it — a single impact-ledger entry would tell us whether to expand or cut. (source: git `ef3df8473`, lane 09 scope)
- **Promote sealed-bid on the screenshot, not on more auditing.** 79%, 0 code blockers, 5 nights of clean verification behind it. The only artifact missing is a visual-QA capture. (source: lane 11, 6 consecutive identical verdicts)

## Loop changes shipped THIS WEEK
- `1c2e05659` — age-escalate stranded restore-queue work so it can't strand silently (from the 07-30 salvage strand; Class-4 silent-failure guard).
- `fb449f84f` — restored nightly TDD tests dropped by the 07-30 gate salvage.
- `ef3df8473` — landed the 07-30 lane code that the gate had dropped (teacher district upsell, LCP fetchpriority, wordcraft count-up, games-played push counter, posthog flag deactivate).
- `posthog-experiment.sh deactivate` verb (lane 03, 07-29) — used same-night to kill a 4-night zombie flag; the write-side verb removed a recurring log-only finding.
- Not shipped from last week's list: MCP probe pipeline bounding, lane 07 slot pinning, lane 12 idempotence guard, baseline-red test fixes. All four carry forward.
