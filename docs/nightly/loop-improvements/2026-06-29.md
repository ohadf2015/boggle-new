# Loop Self-Review — 2026-06-29

## Run health (last 7 nights, 06-23..06-29)
| Lane | Shipped | Avg files | Notes |
|------|---------|-----------|-------|
| 01 triage | 6/7 | ~2 | search_path pins, REVOKE-PUBLIC sweep; MCP-absent slowed DB work |
| 02 perf | 7/7 | ~3 | most reliable; asset re-encode + SSR wins; 06-29 correctly skipped n<50 routes |
| 03 engagement | 7/7 | ~4 | flag hygiene 0 rollback; word-tower founder directives |
| 04 competitor | 6/7 | ~3 | Reddit source stale all window; idea:build 75% |
| 05 landing | 7/7 | ~4 | mode polish 0 reverts; sealed-bid, tower perks |
| 06 seo | 7/7 | ~4 | scrabble CTR ride; native review autonomous |
| 07 self-learn | 7/7 | 2 | prompt-only, never times out |
| 08 adsense/blog | 6/7 | ~3 | blog ISR+JSON-LD; hero-image gen blocked |
| 09 monetization | 6/7 | ~3 | sparse some nights; education pricing |
| 10 dict | 6/7 | ~5 | top throughput (es/sv crosswords) |
| 11 mode-qa | 6/7 | ~6 | word-tower 0→67%; modeqa:ontrack ×6 |
| 12 telemetry | 6/7 | ~1 | prompt-only; brain-drill game_started fixed |

> Per-lane wall-clock not separable from run logs (lanes run concurrently, no per-lane start/end markers). See improvement #3.

## What's working well
- **0 code reverts of pushed work** for the 2nd consecutive window. Baseline-red gate poison stays fixed.
- **Prompt-only lanes (04/07/12) are 100% reliable** — no MCP, no full-test self-check = never wedged. This is the reliability template.
- **Autonomy directive is converting to merged PRs** — 10+ nightly fixes reached master via auto-PR this window. Fewer hand-offs, founder-aligned.
- **Mandatory-Minimum-Artifact + salvage floor** held: every degraded night (06-23, 06-28) still shipped docs + flagged code. No zeroed nights.
- **Lane 11 readiness cards trusted 6 nights running** (modeqa:ontrack:word-tower ×6) — the per-mode readiness format is landing with the founder.

## Friction points
- **Gate Vitest wedge: 4/7 nights TESTS-INCONCLUSIVE.** ~5400s idle backstop fires, build-only re-gate passes every time. This is the single biggest time-sink and the only thing masking real test failures. Affects ALL code lanes.
- **Supabase MCP token absent 3 nights straight (06-27/28/29).** Lanes 01/02 silently degrade — FK index, RLS initplan, auth-pool work deferred with no remediation path in-loop.
- **Reddit source dead all window** (lane 04) — OAuth un-configured since 05-27; lane still spends budget probing before RSS fallback.
- **Higgsfield CLI install blocked 7 nights** — blog cadence (founder #1) can't generate hero images; lane 08 ships text-only or stalls.
- **No per-lane timing in run logs** — can't measure which lanes are slow vs which timeout; run-health "avg duration" is unfillable.

## Concrete improvements (ranked by impact × effort)
1. **Scope gate Vitest to changed files** — replace the full-suite + 5400s idle backstop with `vitest related $(git diff --name-only origin/master)` + a hard 600s per-suite kill. Effort: M. Impact: kills the 4/7 TESTS-INCONCLUSIVE wedge AND surfaces real failures the wedge hides. File: `scripts/nightly/run.sh` (gate block).
2. **Preflight Supabase MCP token probe + alert** — at run start, ping the MCP with a trivial query; if it fails, Telegram-alert "DB lanes degraded, mint never-expire PAT" instead of 3 nights of silent deferral. Effort: S. Impact: ends silent DB-work loss. File: `scripts/nightly/run.sh` preflight + new `scripts/nightly/tools/mcp-health-probe.sh`.
3. **Emit per-lane start/end timestamps to run log** — wrap each lane dispatch with `echo "LANE_START NN $(date +%s)"` / `LANE_END`. Effort: S. Impact: makes lane-7's run-health table measurable; lets us spot the actual slow lane. File: `scripts/nightly/run.sh`.
4. **Short-circuit Reddit probe in lane 04** — skip the JSON/OAuth attempt entirely, go straight to RSS. Effort: S. Impact: reclaims lane-04 budget for buildable ideas (idea:build 75%). File: lane-04 prompt + RSS fallback helper.
5. **gitignore `deploymentChangelog.generated.json`** — it dirties `git status` every run, risking accidental staging in surgical commits. Effort: S. Impact: cleaner commits, fewer entangle accidents. File: `fe-next/.gitignore`.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health-probe.sh`** — single trivial Supabase MCP query at run start; non-zero exit → Telegram alert + sets `DB_LANES_DEGRADED=1` env so lanes 01/02 skip-with-notice instead of silent-defer. Removes 3-nights-running silent loss. Called from run.sh preflight. Effort: S.
- **`scripts/nightly/tools/gate-scoped-tests.sh`** — `vitest related` over changed files + per-suite hard kill, replacing the full-suite idle backstop. Removes the 4/7 wedge. Called from run.sh gate. Effort: M.

## New product/feature ideas surfaced by this week's data
- **Wire `daily_challenge_completed` + a daily-streak nudge** — lane 12 flags it never wired (0 fires); daily challenge is the founder's chosen retention surface (06-24/25 DAILY-over-MP). Source: lane 12 telemetry dead-event backlog. Without the event we're blind to the one funnel we're told to grow.
- **Surface word-tower as the daily-challenge hero** — readiness 0→67% (lane 11) + founder "optimize DAILY, de-emphasize MP" (06-24/25). The mode is now release-adjacent; promoting it into the daily slot is the highest-leverage use of 7 nights of QA. Source: lane 11 readiness + founder directive.
- **/es/multiplayer LCP fix as an education-funnel unblock** — /es LCP +23% structural (lane 02), and Spanish is a top education-growth market. A real Chrome-profiler trace + lazy-mount of the 4 `ssr:false` chunks could lift both perf and the ES education funnel. Source: lane 02 perf watch.

## Loop changes shipped THIS WEEK
- Baseline-red gate-poison fix continues to hold (carried from prior window; 0 reverts again).
- `b1710a072` codified agent bg-task/pipe exit-masking into Class 4 recurring pitfalls — a prior loop-improvement (verify-by-STATE) now enforced in rules.
- Lane 02 n≥50 sample-gate enforced live 06-29 (correctly skipped /sv/daily, /en/daily/word-wheel) — a prior "never flag on single reading" learning now operationalized.
