# Loop Self-Review — 2026-06-13

## Run health (last 6 nights, 06-08..06-13)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 6/6 | ~25m | 2–3 | DEFINER/RLS REVOKE migrations; never reverts; reliable |
| 02 perf | 5/6 | ~10–25m | 2–4 | revalidate>force-dynamic doctrine; 1 timeout window |
| 03 engagement | 5/6 | ~50m | 3–8 | wires experiments but flags stay dark; 2 gate-fails |
| 04 competitor | 6/6 | ~15m | 3–8 | prompt-only; 5 ideas + 3 reply drafts; 0 timeouts |
| 05 landing | 6/6 | ~25–45m | 2–11 | 06-13 monster night (adventure+wordcraft+vault); file-cap drops locales |
| 06 seo | 6/6 | ~15m | 4–23 | seo-daily; ES momentum; IndexNow batches |
| 07 self-learn | 6/6 | ~10m | 3 | prompt-only; never times out |
| 08 adsense | 6/6 | ~10m | 1–28 | JSON-LD + blog ISR migration; reliable |
| 09 monetization | 5/6 | ~25m | 3–5 | ad-UX/upsell; offerwalls dark; 1 timeout |
| 10 dictionary | 5/6 | ~20m | 4–7 | dual-judge candidates; vitest OOM-wedge risk |

## What's working well
- **Founder-directive fast path proved its highest value yet** — 06-13 converted 3 free-text directives (word-vault, adventure, wordcraft Conquest) into shipped code in a single run. Direct founder text is the most reliable work-source the loop has.
- **Prompt-only lanes (04/06/07) = 18/18 ship.** MCP round-trips are the dominant timeout cause; lanes that avoid them never miss.
- **Mandatory-Minimum-Artifact floor** keeps every lane non-zero even on salvage nights — 6/6 nights had artifacts.
- **n≥50 sample-floor gate held** — 06-13 `/es/multiplayer` INP +28% was correctly classified MONITOR (composition noise), not a false regression. The gate added in lane-02 prompt is doing its job.

## Friction points
- **PostHog flag-creation is a 5+ night human bottleneck.** Lane 03 keeps wiring experiments (`exp-mp-quickplay-wait-v1`, `exp-invite-arrival-clarity-v1`, `exp-practice-wheel-cta-v1`) that never get flags → zero measurement. This is the single biggest ROI leak: real funnel drops (83% invite, 43% practice, 94 rage-clicks) sit unmeasured because the handoff has no teeth.
- **Lane 05/09 working-set cap (~8 files) silently drops locales.** When a polish touches component+page+tests, ES/JA translations get cut. Affects i18n completeness on ~2/6 nights.
- **Gate clean-rate ~1/6.** Most nights are salvage/partial. vitest abort-traps still wedge the gate on dictionary nights despite 8GB heap.
- **Lane 03 takes ~50m** (longest non-timeout lane) — heavy PostHog MCP usage; most of that round-trip cost produces dark experiments that can't measure anyway.

## Concrete improvements (ranked by impact × effort)
1. **Auto-emit a FLAG-NEEDED block to the Telegram digest + a tracked file.** Lane 03 should append every dark experiment to `docs/nightly/flag-queue.md` (one line: `key | variants | hypothesis | wired-date`) AND surface it in the manager-summary so the human creates flags in one batch. Effort: S. Impact: unblocks 3 experiments measuring real funnel drops (invite 83%, practice 43%). Files: `prompts/03-engagement.md`, runner digest assembler.
2. **Write-translations-FIRST enforcement in lane 05/09 prompts.** Reorder the prompt so the lane writes all 5 locale JSON blocks before touching any component/page/test, so the file-cap (if hit) drops code-not-yet-shipped rather than a locale. Effort: S. Impact: kills the recurring ES/JA drop on polish nights. Files: `prompts/05-landing.md`, `prompts/09-monetization.md`.
3. **Cap lane 03 PostHog round-trips to a pre-fetched brief.** Have the Phase-0 intelligence brief pull the funnel deltas once and inject them, so lane 03 spends its budget wiring + flag-handoff instead of re-querying. Effort: M. Impact: cuts lane-03 duration ~50m→~25m, frees budget for the UI-wire it keeps deferring. Files: Phase-0 collector, `prompts/03-engagement.md`.
4. **Isolate dictionary vitest into its own sharded gate step.** The dictionary lane's heavy fixtures OOM-wedge the shared gate. Run its tests in a dedicated `--shard` pass with a hard `--no-threads` fallback. Effort: M. Impact: lifts gate clean-rate from ~1/6. Files: gate runner, `.husky`-equivalent nightly gate.
5. **Add a "register+wire+flag-handoff in one pass" checklist to lane 03.** `exp-practice-wheel-cta-v1` was registered 06-12 but never wired to UI — half-done experiments measure nothing. Effort: S. Impact: no more orphaned registrations. File: `prompts/03-engagement.md`.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/feedback-aggregate.sh`** — collapses last-7 `docs/nightly/feedback/*.ndjson` into `type:action` counts (polish:try/pass, idea:build/pass, night:good/meh, reddit:*). Lane 7 ran this by hand-piped `grep|sed|sort|uniq` tonight; it fires every night. Effort: S. Called from `prompts/07-self-learn.md` step 1.
- **`scripts/nightly/tools/flag-queue-append.sh`** — idempotent append of a dark-experiment line to `docs/nightly/flag-queue.md` (dedupe by key). Removes the manual "remember to surface the flag" step that has leaked 5 nights. Effort: S. Called from `prompts/03-engagement.md`.

## New product/feature ideas surfaced by this week's data
- **Word Vault escape-room rework with real cipher/logic puzzles** (founder directive 2026-06-13 + lane-05 next-steps: "cipher jars 1.2, logic-sequence 1.4"). The hub UI shipped tonight; the mechanic depth is the open half. Cite: founder text + lane-05 artifact.
- **Surface the practice-wheel CTA experiment to recover the 43% drop** (`/es/practice/wheelRush` 47→27). The experiment is registered but UI-dark; wiring it is the cheapest funnel-recovery available. Cite: practice funnel watch.
- **Invite-arrival clarity overlay → measure the 83% invite drop** (`invite_landed`→`invite_consumed` 18→3, 29 rage-clicks). Wired, needs flag. Cite: invite funnel watch.
- **Lean into ES "scrabble online" momentum (+156% impr, pos 6.5→5.8)** with a dedicated ES Scrabble-alternative landing block — riding a rising query is cheaper than ranking a cold one. Cite: lane-06 SEO.

## Loop changes shipped THIS WEEK
- Phase-0 switched to PostHog Error Tracking issues for error triage (`d73bc23b3`, 06-12) — replaces ad-hoc Sentry-only triage.
- Pre-push vitest OOM serialization + hub-fan sharding (`7bf109f84`, 06-12) — reduces the gate-wedge class (still fragile on dictionary nights).
