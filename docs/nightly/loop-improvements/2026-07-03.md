# Loop Self-Review — 2026-07-03

## Run health (last 7 nights, 06-27..07-03; 07-03 gate runs after lanes)
| Lane | Success | Avg dur | Avg files | Notes |
|------|---------|---------|-----------|-------|
| 01 triage | 6/7 | ~10m | 2–3 | exit-75 on 07-01 preflight abort; SECURITY DEFINER REVOKE sweep |
| 02 perf | 7/7 | ~9m | 3–8 | backbone; JWT-verify + CLS wins; 3 migrations pending on MCP |
| 03 engagement | 7/7 | ~11m | 1–4 | flags now wired w/ render path (improving) |
| 04 competitor | 6/7 | ~12m | 2–3 | exit-1 on 07-01; Reddit source stale (RSS fallback) |
| 05 landing | 7/7 | ~14m | 3–21 | mode-polish every night; Sealed-bid partial (file-cap) |
| 06 seo | 7/7 | ~13m | 3–27 | native-review sweep; query targeting |
| 07 self-learn | 6/7 | ~4m | 3 | exit-75 on 07-01; prompt-only, cheap |
| 08 adsense | 6/7 | ~10m | 2–4 | JSON-LD + word-count; blog hero-gen blocked |
| 09 monetization | 5/7 | — | thin | ship-drought — research-heavy |
| 10 dict | 6/7 | — | — | Wiktionary +988 clues + Russian (top throughput) |
| 11 mode-qa | 7/7 | ~15m | 2–8 | word-tower 58→82%; 1 design blocker |
| 12 telemetry | 7/7 | ~5m | 1–2 | event-registry + dual-emit fix |

Loop-wide: **80/84 lane-runs shipped (95%)**, but **gate ship-reliability 06-28..07-02 was low** (3 salvages + 1 abort) — recovered after `c826e1924` (07-02). All 4 lane failures cluster on the single 07-01 preflight abort.

## What's working well
- **Salvage floor never zeroes a night** — 06-28/29/30 gate-fails still shipped docs + backed up lane code; 0 master reverts all window. The floor design is sound.
- **Backbone lanes (02/05/06/11/12) are 7/7** — asset/SSR perf, mode-polish, native SEO, mode-QA, telemetry hygiene ship reliably inside budget. These are the loop's value core.
- **Founder-directive + Telegram feedback fast-path** — free-text directives captured same night; `polish:try` steering (11 votes/9 modes) feeds lane-05 target rotation.
- **Mandatory-Minimum-Artifact** — every lane leaves a gate-clean `docs/` artifact even when killed. No total-loss lanes.

## Friction points
- **Supabase MCP token staleness (4-night drought 06-27..06-30, still flaky 07-03)** — affects lanes 01/02; 3 migrations written-but-unapplied. Highest-cost recurring friction: silent DB-work degrade.
- **Gate wedge (06-28/29/30)** — next-build TS-phase + full Vitest tripped the 5400s idle watchdog → 3 salvages. Fix landed 07-02; unverified across a full clean week.
- **07-01 preflight abort is silent + unrecoverable** — a shared tree left on a feature branch rejected the WHOLE run; 5 lanes exit-75/1; 8h manual recovery. No auto-stash/checkout.
- **Lane 09 ship-drought** — monetization produces research docs, not reversible code slices. Lowest-yield lane 2 weeks running.
- **Lane 05 file-cap over-scope** — engine+tests+i18n ship but UI page.tsx deferred = half-modes (Sealed Bid). Slice sizing still mis-estimated.

## Concrete improvements (ranked by impact × effort)
1. **Mint never-expire `SUPABASE_ACCESS_TOKEN` into nightly env + preflight probe** — kills the #1 friction (4 lost DB-work nights). Effort: S (60s mint + env line). Impact: unblocks FK-index/RLS/migration backlog for lanes 01/02. Files: `scripts/nightly/preflight.sh` (add MCP probe + alert), `~/.config/lexi-nightly/env`.
2. **Preflight auto-recover instead of silent-abort** — if tree not on master/dirty: `git stash` + `git checkout master`, log LOUD Telegram alert, continue; abort only if stash fails. Effort: M. Impact: eliminates 07-01-style whole-run loss (cost 5 lanes + 8h). Files: `scripts/nightly/preflight.sh`.
3. **Scope gate Vitest to changed files + per-suite hard-kill** — if a salvage recurs 07-03/04, replace the 5400s global idle backstop with `vitest related <changed>` + per-suite timeout. Effort: M. Impact: removes the salvage root cause structurally, not just the TS-phase symptom. Files: `scripts/nightly/gate.sh`.
4. **Lane 09 prompt: mandate ONE reversible code slice/night** — add STEP 0 "ship a flagged ad-UX tweak or education-upsell copy edit; research doc alone = degraded." Mirrors lane 05's mode-polish mandate. Effort: S (prompt edit). Impact: ends 2-week ship-drought. Files: lane-09 prompt.
5. **Lane 05 prompt: 'write all 5 locales + fully-wire ONE slice' guardrail** — forbid shipping an engine whose page.tsx is deferred; pick a smaller slice that fully wires. Effort: S. Impact: no more half-modes. Files: lane-05 prompt.
6. **Run-log lane-timing emitter** — lanes don't stamp start/end consistently, so this review estimates durations. Add `echo "LANE $N START $(date +%s)"` / END sentinels in run.sh per-lane wrapper. Effort: S. Impact: lane-07 gets real avg-duration data instead of estimates. Files: `scripts/nightly/run.sh`.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-health-probe.sh`** — one idempotent probe: hit Supabase MCP list-migrations, echo OK/STALE + Telegram alert on fail; called from preflight. Removes the per-night manual "is MCP up?" check that fired 6/7 nights. Effort: S.
- **`scripts/nightly/tools/feedback-aggregate.sh`** — codifies the python callback-counter this lane ran inline (idea/polish/modeqa/night tallies from `feedback/*.ndjson`). Lane 07 reruns it every night. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Brain-drill polish sprint** — `polish:try:brain-drill ×2` (only repeat vote this window) + broad appetite. Cite: feedback n=17, brain-drill top. Lane 05 next-target candidate.
- **Word Vault escape-room redesign** — founder directive 06-30 + fresh `polish:try:word-vault` this window. Cite: feedback + directive. Lane 05/09, M effort.
- **Word Tower daily leaderboard (Layer B)** — the single blocker holding 82%→90% readiness across 7 nights. Cite: lane-11 trajectory 58→82%, 1 blocker every night. Needs founder 1-attempt-vs-unlimited call; spec exists.
- **Monetization: education-upsell CTA on `/[locale]/education` sub-routes** — lane 09 has shipped 0 code; a flagged trial-CTA is a reversible revenue slice matching founder priority #3. Cite: lane-09 ship-drought + founder education-growth directive.

## Loop changes shipped THIS WEEK
- `c826e1924` (07-02) — skip next-build TS phase in ALL gate tiers (kills the 3-salvage wedge).
- `27f61a310` (07-02) — re-verify before docs-only drop-all + resilient Telegram alerts (salvage-safety).
- `0477568fc` (06-30, #673) — ship reliably under concurrent branch-switch; never drop build-clean code.
