# Loop Self-Review — 2026-06-08

## Run health (last 7 nights, 06-03..06-08 reports)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 4/7 | ~13m | 2-3 | rg EACCES + DEFINER queue stall the misses |
| 02 perf | 3/7 | ~14m | 2-4 | force-dynamic LCP undiagnosed; RLS+CLS fixes land |
| 03 engagement | 2/7 | ~13m | 3 | MCP/flag-power gaps; ships only when wiring is pure-code |
| 04 competitor | 7/7 | ~9m | 1-2 | RSS-only, zero MCP — most reliable |
| 05 landing | 5/7 | ~14m | 4-11 | over-scope = the 2 misses; `polish:try` queue works |
| 06 seo | 7/7 | ~10m | 1-2 | seo-daily skill, GSC-scope-capped but ships titles |
| 07 self-learn | 7/7 | ~8m | 2 | prompt-only, never times out |
| 08 adsense | 7/7 | ~11m | 1-3 | JSON-LD + word-count; GSC-scope-blocked on noindex |
| 09 monetization | 2/7 | ~14m | 4-15 | ayeT dark-committed not live; biggest reliability gap |

## What's working well
- **MAX_MCP_CALLS cap (shipped 06-04) ended the collapse cascade** — 3 prior collapse events (06-02/06/…), zero on 06-07/08. The single highest-ROI infra fix of the month landed and held.
- **Mandatory-Artifact floor absorbs sub-step timeouts** — 06-08 logged 6×rc=124 yet all 9 lanes produced output. Failures degraded instead of zeroing.
- **`polish:try` Telegram callbacks as a live priority queue** — lane 05 no longer guesses which party game to polish; the click tally ranks it (word-tower/forge/alchemy/sealed-bid each ≥2×).
- **Reliability tracks MCP/infra coupling, not difficulty** — the 4 lanes at 7/7 (04/06/07/08) are exactly the low-external-dependency ones. Pattern is now 12+ nights consistent.

## Friction points
- **ripgrep EACCES (lanes 01/02)** — `rg` permission error 06-06/07; `find|xargs grep` fallback is slow enough to eat the budget. Now the dominant blocker since MCP was fixed.
- **force-dynamic route LCP (lane 02)** — 4 game routes (word-wheel, brain) at 5.5–6.2s LCP, undiagnosed across 3 nights. No repeatable diagnosis recipe → lane re-discovers each night.
- **Lane 09 2/7** — monetization keeps over-scoping (15-file nights) then timing out with dark-committed, non-live work (ayeT offerwall). Worst reliability + the work isn't reaching users.
- **GSC `webmasters.readonly` scope (lanes 06/08)** — 9+ nights blocking noindex + IndexNow decisions. A one-time OAuth re-consent would unblock both lanes permanently.
- **Over-scope is the #1 ship-killer** — lane 05 06-06 (11 files) + lane 09 repeatedly. The wall-clock cutoff exists but lanes still bite off multi-file+multi-locale+TDD in one budget.

## Concrete improvements (ranked by impact × effort)
1. **Add a hard scope-pledge line to lanes 05 + 09 prompts** — require the lane to name ONE complete deliverable in its first artifact write and forbid opening a 2nd feature until the 1st is eslint-clean. Effort: S (prompt edit). Impact: directly attacks the 2/7 (09) and 5/7 (05) miss rate; over-scope is named cause in both. Files: lane 05 + lane 09 prompt templates.
2. **Ship `scripts/nightly/tools/rg-or-grep.sh` graceful search wrapper** — try `rg`, on EACCES fall back to a tuned `find ... -prune | xargs grep` scoped to `fe-next/{app,components,lib,backend}`. Effort: S. Impact: unblocks lanes 01/02 on EACCES nights (2 lost so far). Files: new script + reference in lane 01/02 prompts.
3. **Give lane 02 a force-dynamic LCP recipe block** — embed the diagnosis steps (grep `export const dynamic = 'force-dynamic'` across `app/[locale]/**`, check if route truly needs per-request data, propose `revalidate` or static) so the lane stops re-discovering. Effort: S. Impact: turns a 3-night-undiagnosed 5–6s LCP into a scripted fix candidate. Files: lane 02 prompt.
4. **One-time GSC OAuth re-consent task for the user** — surface in the manager digest as a single actionable: "re-auth ADC with `webmasters.readonly` to unblock lanes 06+08 noindex/IndexNow." Effort: S (digest line) / user does the consent. Impact: removes a 9-night recurring block. Files: digest template.
5. **Lane 09: require live-by-default or explicit dark-flag note** — every monetization change must either be live or state in the artifact why it's dark + what flips it on. Effort: S. Impact: stops silent dark-commits that read as "shipped" but reach no users. Files: lane 09 prompt.

## New helper scripts to ship
- **`scripts/nightly/tools/rg-or-grep.sh`** — graceful ripgrep→find|grep fallback scoped to fe-next source dirs; removes the EACCES stall repeated 2 nights. Called from lanes 01/02. Effort: S. (Proposed, not yet written — next lane-07 run should land it after this prompt-edit batch.)
- **`scripts/nightly/tools/feedback-tally.sh`** — aggregate `docs/nightly/feedback/*.ndjson` callback_data by type/slug (the exact grep|sed|uniq pipeline this lane ran by hand tonight, and will every night). Output a 5-line digest. Effort: S. Fires every lane-07 night → meets the ≥3-night repeatable bar.

## New product/feature ideas surfaced by this week's data
- **Cache the daily-mode routes (word-wheel/brain) → reclaim 5–6s LCP** — source: lane 02 metrics `/en/daily/word-wheel` 6168ms, `/he/brain` 5444ms, all `force-dynamic`. Faster daily load = direct retention lever on the most-promoted surface.
- **Invite-funnel repair experiment** — source: lane 03 `invite_landed→consumed` 83% drop (18→3). A landed-invite that auto-joins the inviter's room (vs dumping to generic lobby) is a concrete A/B; biggest untapped engagement signal of the week.
- **Spanish-first daily-mode landing** — source: lane 06 ES +200% WoW + "scrabble en linea" cluster. Pair with lane 04's Reverse Dictionary Daily (multilingual, S-effort) for a Spanish-led launch riding existing momentum.
- **Reverse Dictionary Daily (admin-gated build)** — source: lane 04 06-08 + `idea:build` 80% founder rate. Definition→headword, 5 guesses, reuses existing lexicon; founder's stated appetite + S effort.

## Loop changes shipped THIS WEEK
- **MAX_MCP_CALLS=3 cap** — proposed prior lane-07 nights, shipped 06-04; verified no MCP collapse 06-07/08.
- **`LEXI_LANE_FILE_CAP` 8→999** — shipped 06-04 (`scripts/nightly/lib/headless.sh`, bash -n clean), enabling 5-locale single-night ships.
