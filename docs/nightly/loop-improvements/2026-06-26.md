# Loop Self-Review — 2026-06-26

## Run health (last 7 nights, 2026-06-20..06-26)
| Lane | Success | Avg files | Notes |
|------|---------|-----------|-------|
| 01 triage | 5/7 | ~2 | 2 reverts = infra (exit-75 stale MCP token 06-22, usage cascade 06-20), not code |
| 02 perf | 7/7 | ~3 | most reliable; FK index + RLS initplan + idempotent migrations |
| 03 engagement | 7/7 | ~5 | 3 A/B exps wired live, 0 rollback |
| 04 competitor | 7/7 | ~4 | docs-only; never times out (prompt-only) |
| 05 landing | 7/7 | ~5 | mode polish TDD-clean; tower physics, sealed-bid, alchemy |
| 06 seo | 5/7 | ~4 | 1 revert = exit-75 06-22; CTR rewrites + native review |
| 07 self-learn | 6/7 | ~3 | this lane; ships every night it runs |
| 08 adsense | 7/7 | ~4 | blog ISR + JSON-LD; 0 gate failures |
| 09 monetization | 7/7 | ~5 | education pricing swap + tower upgrades |
| 10 dict | 7/7 | ~6 | highest throughput; infinite es/sv crosswords |
| 11 mode-qa | 3/7 | ~4 | new lane; word-tower readiness 0→45%; modeqa:ontrack ×3 |
| 12 telemetry | 2/7 | ~3 | new lane; user_identified + brain-drill game_started fixed |

## What's working well
- **Baseline-red gate poison finally fixed this week** (`d6ae0896d`+`e759e8624`+`415c39188`, 21 red files). That was the #1 standing "what to avoid" — clean lane code was being dropped for unrelated red. Should lift the 06-24/25 `TESTS-INCONCLUSIVE` rate.
- **Prompt-only lanes (04/07/12) never time out** — confirms the doctrine: every MCP round-trip and full-test self-check a lane removes buys reliability.
- **Founder-directive fast path is the strongest signal-to-ship loop** — 06-26 word-tower directive landed same run (`b636fab59`). Free-text directives consistently top-of-queue, same-night.
- **Mandatory-Minimum-Artifact floor held 6/6** — no night produced literally nothing.
- **Feedback is uniformly positive** — polish:try 7/8, modeqa:ontrack ×3, zero night:meh. No run-quality complaint in 7 nights.

## Friction points
- **Gate-runner Vitest 900s idle wedge** (06-22/24/25) — even with baseline-red fixed, the full-suite gate has no per-suite timeout, so any single hung worker burns the whole 900s and ships code UNVERIFIED. Affects ALL code lanes' verification confidence.
- **No lane-start stagger vs shared Claude usage window** (06-20) — concurrent lane starts hit the shared 1M-token quota, cascade-killed 5 lanes (exit 124). Pure infra; recurs whenever many lanes fire at once.
- **Stale launchd Supabase MCP token** (06-22) — exit-75 reverted lanes 01+06. Never-expire PAT minted but no pre-run health-probe, so a dead token isn't caught until a lane blocks on it.
- **Lane 11/12 ship rate low while maturing** (3/7, 2/7) — partly that they don't run every night, partly word-tower's >500-line files block in-place edits. Not a prompt problem; a target-file-size problem.
- **Sub-agent report drift** — the evidence-gathering Explore agent invented specifics (exact LCP ms, experiment IDs) not in the source. Lane 7 has to cross-check claims against git log / feedback it reads directly. Mild but recurring.

## Concrete improvements (ranked by impact × effort)
1. **Hard per-suite Vitest timeout in the gate-runner** — wrap each test shard in `timeout 120 …` and `kill` on expiry instead of one 900s idle budget. Effort: S. Impact: kills the #1 failure mode (3 `TESTS-INCONCLUSIVE` nights → verified ships). File(s): nightly gate-runner script (`scripts/nightly/*gate*` / run.sh test tier).
2. **Lane-start stagger + concurrency cap** — sleep `lane_index * 30s` before each lane start, cap to ~6 concurrent. Effort: S. Impact: eliminates the 06-20 usage-limit cascade (reverted 5 lanes). File(s): `scripts/nightly/run.sh` lane-dispatch loop.
3. **Pre-run Supabase MCP health probe** — one `select 1` REST call at run start; if it 401s, refresh token (or flag DEFER) before any lane queries. Effort: S. Impact: stops exit-75 cascade (lanes 01+06 06-22). File(s): `scripts/nightly/tools/mcp-health.sh` (new) + run.sh preflight.
4. **Lane-7 prompt: require cross-checking sub-agent claims against primary sources** — add one line telling lane 7 to anchor concrete numbers to git log / feedback it read directly, not the evidence-agent's prose. Effort: S. Impact: prevents fabricated metrics leaking into learnings.md preamble (which all 12 lanes ingest). File(s): lane-07 prompt.
5. **Lane 11 prompt: guard-then-shrink order for >500-line mode files** — instruct it to add crash guards (cheap, shippable) BEFORE attempting refactors (expensive, partial-risk). Effort: S. Impact: raises lane 11 ship rate; the 4 unguarded WordTowerPlay lookups are 1-line fixes blocked behind a refactor framing. File(s): lane-11 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/mcp-health.sh`** — preflight `select 1` against Supabase REST; exit non-zero + emit WARN if 401. Removes the recurring "token drifted dead, found out mid-lane" pattern (06-21, 06-22). Called from run.sh preflight. Effort: S. (Not created this run — 16-min budget; flagged for next self-learn or human.)
> Did not ship a script this run: the two recurring patterns (gate timeout, MCP token) live in the gate-runner / run.sh which lane 7 doesn't own edit-safely under a 15-min budget. Proposed above for the human queue.

## New product/feature ideas surfaced by this week's data
- **Word-tower green-zone "perfect drop" celebration + combo** — founder 06-26 explicitly asked for green-part drops to be possible and celebrated. Source: founder directive #5. A satisfying perfect-drop chain is the cheapest engagement lever on an already-polished mode (modeqa:ontrack ×3).
- **Surface ONE buildable idea per night in lane 04** — `idea:build ×1` this week is the first idea-build callback in 35+ days; the founder is signalling appetite for concrete builds, not just polish. Source: feedback aggregate (idea:build ×1, prior 35d zero).
- **Education images mascot-correct + route CTAs into real `/education` sub-pages** — founder 06-24 flagged off-brand images; education growth is standing priority #3. Source: founder directive 06-24 #2 + standing priorities.
- **`/es/multiplayer` SSR/skeleton for LCP** — the regression is structural (4× `ssr:false`). A server-rendered shell skeleton is a real product win (Spanish is the top SEO-traffic locale per "scrabble online" cluster). Source: lane 02 carry-watch + lane 06 ES CTR gap.

## Loop changes shipped THIS WEEK
- **Baseline-red triage** (`d6ae0896d`, `e759e8624`, `415c39188`) — directly resolves the prior loop-improvements ask to "quarantine/fix known-red suites poisoning the gate". The single highest-impact loop fix of the window.
