# Loop Self-Review — 2026-06-15

## Run health (last 6 nights: 06-10..06-15)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 6/6 | ~31m | 2–4 | Security REVOKE + index drops, 0 reverts |
| 02 perf | 6/6 | ~27m | 1–3 | ISR + auth-JWT, 0 reverts |
| 03 engagement | 4/6 (2 partial) | ~42m | 4–6 | 4 experiments wired, ALL dark (no flag) |
| 04 competitor | 6/6 | ~27m | docs | Prompt-only, 0 timeouts |
| 05 landing | 6/6 (1 partial) | ~44m | 6–9 | Polish shipped; 06-12 ja/es dropped on cap |
| 06 seo | 6/6 | ~31m | 1–2 | seo-daily, 0 timeouts |
| 07 self-learn | 6/6 | ~15m | 2 | Prompt-only |
| 08 adsense | 6/6 | ~45m | 1–3 | E-E-A-T, ISR |
| 09 monetization | 6/6 | ~43m | 3–5 | ad-UX/upsell |
| 10 dictionary | 6/6 | ~59m | varies | SLOWEST lane; 29–73m, progressive slowdown |

**Gate: rc=124 timeout 5/6 nights** (06-09/12/13/14 + cap-drops 06-11/12/13). Push failed 06-13 (×4 "lost the window") + 06-15. Only 06-10 fully clean.

## What's working well
- **Prompt-only / low-MCP lanes (04, 06, 07) = 18/18 ship, 0 timeouts.** Eliminating MCP round-trips is the single strongest reliability lever.
- **Mandatory-Minimum-Artifact floor** caught every wedged night — 6/6 non-zero even when the gate dropped code.
- **Salvage pipeline** (`~/logs/lexi-nightly/salvaged-code-*`) preserved authored code on all 5 wedge nights; docs still shipped.
- **TDD pure-module ship rate** (lane 05) — 6 polish features, 0 reverts.

## Friction points
- **The gate runs the FULL vitest suite** → 281–358 pool-timeout events/night, hits the 5400s backstop → rc=124 → docs-only salvage. This is the #1 cause of the 1/6 clean rate. Affects ALL code lanes (01/02/03/05/09/10).
- **Chronic-red suites poison the gate** every night: `PracticeWheelSandbox` (drag/pixiIndices mock-drift), `wordHandler.blast*` (scoring/emit), `blastTileGeneration` (locked/key). These are not new lane breakage — they're standing red that the full-suite gate keeps re-hitting.
- **Push race ("lost the window")** — 06-13 burned 4 retries, 06-15 failed. Local commit survives but unsynced; next run rebases over stale remote.
- **Blog-ISR batch + lane code in one night exceeds the gate file budget** → code dropped 06-11/12/13.
- **Lane 10 (dictionary) at 59m avg** drags total wall-clock and compounds gate pressure.
- **4 dark experiments ≥6 nights** — the loop produces the code but cannot create the PostHog flag, so 100% of engagement A/B output is unmeasured.

## Concrete improvements (ranked by impact × effort)
1. **Scope the gate test run to the changed-cone, not the full repo.** The pre-push hook already shards by cone (`ef650a29e`); the nightly GATE does not — it runs `npm run test` whole-repo and wedges. Reuse the same cone-sharding in the gate. Effort: M. Impact: converts the 5/6 timeout nights to clean ships; removes chronic-red-suite poisoning. File(s): `scripts/nightly/*gate*.sh`, reuse `fe-next` cone-shard logic.
2. **Quarantine the 3 chronic-red suites** (`PracticeWheelSandbox`, `wordHandler.blast*`, `blastTileGeneration`) behind a `describe.skip` + tracked TODO until root-fixed, OR fix the mock-drift (same class as `5d3e28453`/`a8a6c0736`). Effort: S (skip) / M (fix). Impact: removes the recurring red that wedges the gate even when cone-scoped. File(s): the three `__tests__` files.
3. **A one-shot PostHog flag-creation handoff.** Lane 03 already emits "FLAG NEEDED" to the digest but nothing consumes it. Either (a) a `scripts/nightly/tools/flag-digest.sh` that writes a single copy-pasteable `posthog feature-flag create` block per dark experiment to the Telegram manager summary, or (b) document the 4 flags as a single human checklist item pinned at the top of every digest until count→0. Effort: S. Impact: unblocks 94 rage-clicks + 3 funnel drops (83/43/42%) of measurement. File(s): lane 03 prompt + new tool script.
4. **Split blog-ISR batches from feature code across nights.** Add a lane-08/06 rule: never edit >10 blog pages in the same night as feature lanes. Effort: S (prompt edit). Impact: stops the file-cap docs-only drop (3 nights). File(s): lane 06/08 prompts.
5. **Harden the push step: rebase-then-push with verify-by-origin-grep.** Replace SHA-equality check with a content grep against `origin/master` after push. Effort: S. Impact: kills the 2/6 push-fail stranding. File(s): `scripts/nightly/*push*.sh`.
6. **Profile lane 10 (dictionary) 59m avg.** Cap per-night candidate count or shard by language to keep it under ~30m. Effort: M. Impact: reduces total wall-clock + gate pressure. File(s): dictionary-improvement skill invocation.

## New helper scripts to ship
- **`scripts/nightly/tools/flag-digest.sh`** — reads the wired dark-experiment registry (or greps lane-03 artifacts for "FLAG NEEDED") and emits a single copy-pasteable PostHog flag-creation block to the Telegram manager summary every night until the count hits 0. Removes the manual "which flags are still missing" reconstruction each digest. Called from the manager-summary step. Effort: S. (Fires the ≥3-night pattern: flag-creation surfaced manually 6 nights running.)

## New product/feature ideas surfaced by this week's data
- **In-game quit-confirm modal (ship the `exp-game-abandon-confirm-v1` variant to 100% once flag-validated).** Source: 185 silent game abandons/7d = 42% completion drop. Even pre-A/B, a confirm step is a near-zero-risk retention lever.
- **Hebrew-first LCP/CLS fix as a feature, not just a watch.** Source: `/he` CLS 0.460 + INP 264ms (+67%) now n≥50. RTL users get the worst experience of any locale; a font-display/direction-recalc fix is a measurable UX win for the only RTL market.
- **Double down on Spanish "scrabble" intent with a dedicated `/es/scrabble` landing.** Source: "scrabble online español" +1283% WoW, "jugar al scrabble" +231%, pos 5.9→5.6. Demand is surging faster than rank; a purpose-built page could capture it.

## Loop changes shipped THIS WEEK
- `ef650a29e` / `03d6abaed` — pre-push test sharding by measured cone size (killed exit-144 OOM on translation/LanguageContext mega-hubs). Note: gate does NOT yet reuse this — improvement #1 above.
- `a9ab9fe2c` — targeted baseline gate (stop false docs-only drops on a wedged baseline).
- `e6da04178` — git-enumerated tar snapshot (40k→11.5k files, 27min→5s) — large nightly-runtime win.
- `5d3e28453` / `a8a6c0736` / `fc1bb598a` — repaired several stale vi.mock master-red suites (the class of fix improvement #2 wants extended to the remaining 3).
