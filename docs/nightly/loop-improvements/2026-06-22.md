# Loop Self-Review — 2026-06-22

## Run health (last 6 nights, 06-17..06-22)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 1/6 | ~7m | 1-2 | MCP-blocked (exit 75) 4 nights inc. tonight; only ships when Supabase/Sentry reachable |
| 02 perf | 6/6 | ~8m | 4-6 | most reliable shipper; REST fallback when MCP down; 0 reverts |
| 03 engagement | 3/6 | ~24m | 0-6 | slowest lane; flag-wire heavy; blocked by >500-line PageClient refactors |
| 04 competitor | 6/6 (docs) | ~9m | 3 docs | never ships code (by design); reddit RSS jq parse errors recur |
| 05 landing | 4/6 | ~6m | 6-8 | mode polish TDD; over-scope drops a locale ~1/6 |
| 06 seo | 4/6 | ~9m | 3-20 | autonomous native review; MCP-blocked tonight (exit 75) |
| 07 self-learn | 6/6 | ~5m | 2-3 docs | prompt-only, never times out |
| 08 adsense | 5/6 | ~7m | 1-3 | blog schema/noindex; structural bar cleared |
| 09 monetization | 4/6 | ~6m | 1-3 | low volume |
| 10 dictionary | 5/6 | ~7m | 5-7 | steady |

## What's working well
- **Infra self-heal proposals from prior loop-improvements actually shipped** — off-master preflight `d91b5fa76` + MCP transient retry `8c705ba2c`. The loop is now self-improving its own harness, not just the product.
- **Lane 02 perf is the workhorse** — 6/6 ship, 0 reverts. REST-API fallback when MCP is down keeps it productive even on bad-MCP nights. Model lane for others.
- **Mandatory-Minimum-Artifact floor held 6/6** — no zero-output night even on preflight failure.
- **No exit-124 timeouts in 5 of 6 nights** (06-20 cascade the lone exception) — finalize-cutoff hook + eslint-changed-only self-check keep lanes inside budget.

## Friction points
- **MCP reachability still gates lanes 01 + 06** — exit 75 tonight (06-22) despite never-expire token minted 06-21. Root cause shifted from 401-expiry to preflight/transport flakiness; the boot-probe retry helps but didn't fully cover tonight. Affects: 01, 06.
- **Lane 03 is the slow lane (~24m)** — flag wire-up + PostHog round-trips + blocked on >500-line component refactors (`leaderboard/PageClient.tsx` 519). Burns budget, ships partial. Affects: 03.
- **Shared-usage cascade has no stagger** — 06-20 idle-killed 5 lanes. Cutoff-guard alerts but doesn't prevent. Affects: all concurrent lanes.
- **Reddit RSS jq parse errors recur** on some feeds (lane 04) — fetch not error-tolerant.

## Concrete improvements (ranked by impact × effort)
1. **Stagger lane starts + concurrency cap** — start lanes N seconds apart and cap concurrent Claude sessions to avoid draining the shared usage window. Effort: M. Impact: prevents the 06-20-class 5-lane idle-kill (exit 124). File(s): `scripts/nightly/run.sh` / headless launcher. (top infra ask, recurring 2 windows)
2. **Preflight MCP credential PROBE with auto-retry-then-degrade** — before lanes run, hit Supabase Mgmt API `/v1/projects` (200/401) and Sentry; on transient fail retry 3×, on hard 401 swap to backup PAT, else mark MCP-degraded so lanes 01/06 take REST-fallback path instead of exit-75-aborting. Effort: M. Impact: recovers lanes 01+06 on bad-MCP nights (4/6 lost this window). File(s): `scripts/nightly/preflight.sh`, `scripts/nightly/lib/mcp_probe.sh`.
3. **Lane 03 prompt: skip flags blocked on >500-line refactor; pick a renderable variant first** — add a STEP-0 check "does variant-B render path exist AND is the host file <500 lines? if not, pick a different experiment." Effort: S (prompt edit). Impact: stops lane 03 burning ~24m on `exp-leaderboard-play-cta-v1`-style dead ends. File(s): lane-03 prompt.
4. **Error-tolerant reddit RSS fetch** — wrap jq parse in `|| continue` and validate JSON before parse. Effort: S. Impact: removes recurring lane-04 noise. File(s): `scripts/nightly/tools/reddit-rss.sh` (if exists) or lane-04 fetch.
5. **Lane 05 prompt: hard-require all 5 locale keys written FIRST, before component** — already in learnings but recurred (es.js `streakNewPB` dropped 06-19). Promote to a STEP-0 gate in the lane-05 prompt with an explicit checklist. Effort: S. Impact: kills the ~1/6 dropped-locale partial. File(s): lane-05 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/mcp_probe.sh`** — probe Supabase Mgmt API + Sentry, return reachable/degraded/dead; swap to backup PAT on 401; called from preflight + per-lane mcp config build. Removes the recurring "trust stale token → exit 75" failure. Effort: M. (deferred to a code lane — not built tonight, out of budget)

## New product/feature ideas surfaced by this week's data
- **Pre-empt `/he` LCP with a static above-fold hero** — `/he` LCP oscillates 4.3–6.3s (lane 02, 06-16/19/22). Suspected above-fold mascot/splash asset. Ship a static/`priority` LCP image for HE locale, lazy the animated splash. Source: lane-02 perf rows.
- **Fix `/es/multiplayer` CLS 0.288 + rage-clicks 0.584** — highest rage-click + doubled CLS on the same route (lane 03, 06-17/21). A layout-shift culprit is also frustrating clicks. One fix likely lands both KPIs. Source: lane-02 CLS + lane-03 rage rows.
- **Word Alchemy element-order nudge** (06-22 idea) — directional ↑/↓ hint after a failed reaction; rides the 100% `polish:try` appetite on alchemy. Source: lane-04 06-22 idea doc + polish:try ×10.

## Loop changes shipped THIS WEEK
- `d91b5fa76` fix(nightly): self-heal off-master preflight instead of silent abort — closed the 06-19-class "wrong-branch silent no-op" watch.
- `8c705ba2c` fix(nightly): retry supabase MCP boot probe on transient transport fail — partial cover of the MCP-reachability watch.
- `e9c75581b` (prior) cutoff-guard + stall Telegram alert — addresses shared-usage cascade detection (not yet prevention).
