# Loop Self-Review — 2026-05-31

## Run health (last 7 nights)
| Lane | Ship rate | Timeout rate | Avg files touched (shipped) | Notes |
|------|-----------|--------------|----------------------------|-------|
| 01 triage | 3/7 (43%) | 4/7 | 3-7567 | Success = MCP health. 05-31 shipped (sentry+supabase recovered) |
| 02 perf | 1/7 (14%) | 6/7 | 3 (once) | Worst lane. Only shipped 05-27 (LCP fix). Needs structural change |
| 03 engagement | 3/7 (43%) | 4/7 | 4-11 | Shipped 3 consecutive (05-26/27/28), then 3 consecutive timeouts |
| 04 competitor | 7/7 (100%) | 0/7 | 2-3 | Perfect. No MCP dependency. Reddit blocked but WebSearch compensates |
| 05 landing | 1/7 (14%) | 6/7 | 7 (once) | Only shipped with founder directive (sealed bid). Scope too ambitious |
| 06 seo | 5/7 (71%) | 2/7 | 4-5 | Reliable. 05-30 timeout was anomaly. Recovered 05-31 |
| 07 self-learn | 7/7 (100%) | 0/7 | 2-3 | Perfect. Prompt-only, no MCP |
| 08 adsense | 5/6 (83%) | 1/6 | 1-2 | Reliable. Light scope helps |

**Fleet totals:** 32/54 shipped (59%), 10 timeout-reverted (19%), 12 timeout-partial (22%).
**Trend:** 05-26=6/8, 05-27=5/8, 05-28=5/8, 05-29=4/8, 05-30=2/6(!), 05-31=4/6. V-shaped recovery after 05-30 trough.

## What's working well
- **Lightweight lanes are bulletproof** — 04/07/08 = 19/20 shipped (95%). Zero MCP dependency = zero timeout. This is the model for reliability.
- **KEEP_TIMEOUT_PARTIALS** — 3 nights, zero gate-poisoning. Rescued ~40 partial files from total loss. Single biggest resilience improvement this week.
- **Phase 0 intel brief** — sentry+supabase RECOVERED on 05-31 after being dead 05-30. When healthy, triage ships. Proves the intel layer's value.
- **Autonomy matrix** — 7 autonomous fixes shipped, 0 reverted. Security (REVOKE, search_path, security-invoker) shipped safely without human review delay.
- **Mandatory-minimum-artifact** — every lane produces something. No total-loss runs since implementation.

## Friction points
- **MCP cumulative budget** — individual 60s cap works but lanes making 5+ sequential MCP calls still exceed 900s budget. Lanes 01/02/03 most affected.
- **Lane 02 structural problem** — needs both Supabase (execute_sql for metrics) AND PostHog (funnel data). Each MCP call risks a hang. 14% ship rate = not viable in current form.
- **Lane 05 scope mismatch** — prompt asks for a new landing page from scratch. Only shipped when founder pre-built the code. 14% ship rate = wrong scope.
- **Lane 03 regression** — shipped 3/3 early in the week, then 0/4. No prompt change. Suggests MCP infrastructure degraded mid-week, not a lane-specific issue.
- **Reddit fully blocked** — 5 consecutive nights. OAuth spec exists (`docs/nightly/reddit-oauth-setup.md`) but never implemented. Lane 04 compensates but Reddit drafts = 0.
- **Search collector permanently dead** — 0 signals all 7 nights. No error investigated. Wasting brief tokens.
- **GSC token scope gap** — `webmasters.readonly` missing. Blocks lane 06 live query pulls. Needs one-time interactive gcloud auth (human action).

## Concrete improvements (ranked by impact x effort)
1. **Kill search collector from Phase 0** — Remove dead search collector that returns 0 every night. Saves brief tokens + removes confusion. Effort: S. Impact: cleaner briefs for all lanes. File: `scripts/nightly/lib/collect-search.sh` or Phase 0 config.
2. **Lane 02 → REST-only mode** — Replace Supabase execute_sql + PostHog MCP with pre-baked curl REST queries (PostHog REST helper already proven stable). Eliminates MCP dependency. Effort: M. Impact: 02 ship rate 14%→60%+. Files: lane-02 prompt + `scripts/nightly/tools/supabase-rest.sh`.
3. **Lane 05 scope to "improve existing page"** — Change prompt from "build new landing" to "improve an existing landing page's copy/SEO/CTA." Matches what actually ships. Effort: S. Impact: 05 ship rate 14%→50%+. File: lane-05 prompt.
4. **MAX_MCP_CALLS=3 budget cap** — Hard limit on MCP tool invocations per lane. Force lanes to use brief intel instead of live queries. Effort: S. Impact: timeout rate 41%→<25%. File: headless.sh or per-lane wrapper.
5. **Reddit OAuth or retirement** — Either implement OAuth (spec exists) or remove Reddit from lane 04 prompt to eliminate dead-path code. Effort: M (OAuth) or S (retire). Impact: lane 04 prompt clarity. Files: `scripts/nightly/tools/reddit-fetch.sh`, lane-04 prompt.

## New helper scripts to ship
- **`scripts/nightly/tools/supabase-rest.sh`** — curl-based Supabase REST queries (schema info, row counts, recent errors) without execute_sql MCP. Enables lane 02 REST-only mode. Called from lane-02 prompt. Effort: M.
- **`scripts/nightly/tools/gsc-rest.sh`** — GSC Search Analytics API via curl + ADC token. Replaces blocked MCP pull. Called from lane-06 prompt. Effort: M (blocked on gcloud auth scope fix).

## New product/feature ideas surfaced by this week's data
- **Word Transform Daily** — Parseword got 50K users in 48h with seed-word→target chain-solving. LexiClash could build this as a 6th daily challenge. Naturally multilingual. Source: lane 04 competitor research (05-31).
- **Fading Grid Sprint** — tile-decay countdown modifier for Blast. Founder tapped `idea:build` + `polish:try`. Source: lane 04 (05-29), Telegram feedback.
- **Forge Share Certificate** — branded shareable card for Word Forge best word. Social-share = #1 viral driver in word games (Wordle model). Source: lane 04 (05-31).
- **Shareable emoji MP result card** — 5-night consensus across multiple lane 04 reports. No implementation yet. Source: lane 04 (05-26 thru 05-30).

## Loop changes shipped THIS WEEK
- KEEP_TIMEOUT_PARTIALS default=1 (05-29) — rescued ~40 files across 3 nights
- Phase 0 intelligence layer (05-29) — sentry+supabase recovered 05-31
- Mandatory-minimum-artifact contract (05-29) — zero total-loss runs since
- git-ship preserve+reset (05-28) — 8 consecutive clean nights
- Pre-push skip tsc for docs-only (05-29) — faster docs-only salvage path
