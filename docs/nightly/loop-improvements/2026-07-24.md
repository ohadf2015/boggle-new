# Loop Self-Review — 2026-07-24

## Run health (last 6 nights, 07-19..07-24)
| Lane | Ships | Avg dur | Avg files | Notes |
|------|-------|---------|-----------|-------|
| 01 triage | 3/6 | ~13m | 3–4 | MCP-gated; strong when Supabase up (down all 6) |
| 02 perf | 2 minimal /6 | ~21m | 2 | 6-night CLS research loop, no headless visual QA |
| 03 engagement | 4/5 | ~21m | 1–4 | most reliable; flag+event same night |
| 04 competitor | 2/2 | ~9m | 3 | off-rotation 4 nights; Brain Drill won BUILD vote |
| 05 landing | 5/6 | ~18m | 1–7 | 0 reverts; 5-locale-first variable-reward polish |
| 06 seo | 4/4 | ~18m | 3–5 | off 2; `scrabble online` focus |
| 07 self-learn | 2/6 | ~8m | 3 | rotation; fastest lane |
| 08 adsense | 2/2 | ~12m | 2 | off 4; blocked on Higgsfield CLI |
| 09 monetization | 5/5 | ~18m | 2–3 | always bundled, no standalone feature |
| 10 dictionary | 1/6 | ~7m | 7 | off-rotation by design |
| 11 mode-qa | 5/6 | ~20m | 2–11 | shiritori 42→90 Released; sealed-bid 45% |
| 12 telemetry | 6/6 research | ~22m | 1 | classifier false-positive breakthrough |

Lanes finish ~03:30. **Gate + drop-and-re-gate then runs to 06:47 (07-22) / 07:08 (07-23)** — a 2–6h tail that dwarfs any single lane.

## What's working well
- **Lane structure is healthy** — landing 5/6, mode-qa 5/6, engagement 4/5, 0 pushed-code reverts in 45+ nights. The failure mode is NOT lane output quality.
- **Stacked-incremental mode audit graduated shiritori** (42→90% over 5 nights, founder `modeqa:ontrack` vote). Repeatable template now pointed at sealed-bid.
- **docs-only salvage kept master green** through 2 gate false-drops — code recovered next day, master never regressed.
- **Rotation scheduler** (8-9/12 lanes/night, idle lanes every ~3 nights) keeps run length bounded without starving low-freq lanes.

## Friction points
- **Gate false-drops clean code (07-21: 14 files, 07-23: 11 files)** — both build-clean, both restored next day. Unattributable red → drop-ALL instead of bisect-to-offender. Affects the whole run's output.
- **2–6h gate tail** — the conclusive typecheck tier re-runs on the full authored set after every ambiguous red. Same root as the false-drops.
- **Supabase MCP down all 6 nights** — blocks lane 01/02 impact verdicts; 6 migrations stalled 3–27d. Pure infra/human blocker.
- **Lane 02 has no headless visual QA** — re-derives the same 3 CLS suspects nightly for 6 nights with no landed root-cause. Wasted ~2h/week.
- **Zombie flag flagged 4× unactioned** — the loop surfaces human-only items but has no escalation path when they sit.

## Concrete improvements (ranked by impact × effort)
1. **Finish the subset-peel gate backstop** — extend `ad65aa350` so an unattributable red BISECTS the authored set (drop only the failing file) instead of drop-all. Effort: M. Impact: kills both the false-drops (2/6 nights) AND shrinks the 2–6h re-gate tail. Files: `scripts/nightly/` gate logic. **#1 — this is the deferred class-killer.**
2. **Give lane 02 a headless Layout-Shifts capture** — add `playwriter`/agent-browser CLS-attribution to the perf prompt so it lands a root-cause fix instead of research-cycling. Effort: M. Impact: CLS 0.9→<0.1 across routes. Files: lane 02 prompt + a `scripts/nightly/tools/cls-capture.sh`.
3. **Force one-CLS-hypothesis-tested-per-night in lane 02 prompt** — cheap interim if #2 slips: require the lane to LAND one hypothesis test (even negative) per night, not re-list all three. Effort: S. Impact: converges the 6-night loop. Files: lane 02 prompt.
4. **Escalation counter on human-only items** — when a `HUMAN:` watch (zombie flag, PAT mint, GSC creds) recurs ≥3 nights unactioned, self-learn emits a single consolidated Telegram "stuck human queue" card. Effort: S. Impact: unblocks trivial items (zombie flag sat 4 nights). Files: lane 07 prompt + run.sh Telegram hook.
5. **Preflight abort Telegram alert** (carried from 07-19, still unshipped) — alert WITH reason on preflight abort so a lost night isn't silent until 9am. Effort: S. Impact: eliminates silent-loss class. Files: `scripts/nightly/run.sh` preflight.

## New helper scripts to ship
- **None tonight** — the two docs are the deliverable and building+syntax-checking a new script risks the finalize cutoff for zero floor gain. Scripts #2 (`cls-capture.sh`) and #4 (escalation counter) are proposed above for a future non-time-boxed session.

## New product/feature ideas surfaced by this week's data
- **Brain Drill Post-Session Word Replay** — founder `idea:build` vote (07-22, `913934c2`); flash top-3 missed words letter-by-letter w/ score delta. Source: feedback callback + NYT Crossplay "Cross Bot" precedent. S-effort, mastery hook.
- **Sealed-bid countdown timer** — `roundDeadline` already tracked but never rendered (lane 11 07-24 audit); rendering it is the MAJOR blocker to sealed-bid release AND a UX/urgency win. Source: lane 11 readiness audit.
- **MP bot special-tile fix as the mp_round lever** — sentiment stuck avg 1.4/3, root cause: special tiles unresponsive with bots (he locale). Source: lane 03 07-24 + `exp-mp-round-issue-probe-v1` triage. Highest-impact engagement fix on the board.
- **Shiritori Solo shrinking-timer sudden-death** — lane 04 07-21, validated by shiritoriwithfriends.com; adds the pressure arc a now-Released mode still lacks. S-effort (one config + pulse).

## Loop changes shipped THIS WEEK
- Gate remediation series landed: `617463687` / `96a56f417` (SIGABRT→inconclusive) / `ad65aa350` (peel offending source, partial) / restores `40f7838bf` + `8a9b047d4`. The subset-peel backstop (improvement #1) is the completion of this thread.
