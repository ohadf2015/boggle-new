# Loop Self-Review — 2026-05-27

## Run health (last 7 nights)
| Lane | Success rate | Avg duration | Notes |
|------|-------------|--------------|-------|
| 01 triage | 4/7 (57%) | ~15m shipped, 21m timeout | MCP_TOOL_TIMEOUT fixed 05-26; shipped 2 consecutive (05-26+05-27). Trending UP. |
| 02 perf | 2/6 (33%) | ~13m timeout, ~8m shipped | Supabase execute_sql suspected hang. High-value when ships (CLS 05-24, LCP 05-27). |
| 03 engagement | 3/6 (50%) | ~12m shipped | Improving: shipped 3 of last 4. PostHog REST pre-fetch would stabilize further. |
| 04 competitor | 7/7 (100%) | ~5m | No MCP dependency. Pure curl+WebSearch. Most reliable lane. |
| 05 landing | 1/6 (17%) | 20-41m timeout | Worst lane. Opus/1500s AND sonnet/600s both time out. Scope cut only option. |
| 06 seo | 6/7 (86%) | ~7m shipped | First timeout 05-27 broke 100% streak. Likely transient — monitor. |
| 07 self-learn | 5/6 (83%) | ~10m | Prompt-only, no MCP. Reliable. |
| 08 adsense | 4/5 (80%) | ~8m shipped | Skips gracefully when no work. Timeout 05-25 = MCP hang. |

**Fleet totals:** 33/54 shipped (61%), 16 timed out (30%), 0 cap-reverted, 1 skipped.

## What's working well
- **Autonomy matrix proved out.** Lane 01 shipped 3 real security fixes on 05-27 without human gating. Pattern: reversible + small blast = auto-ship; irreversible = queue. Zero incidents from autonomous fixes across 4 nights.
- **MCP_TOOL_TIMEOUT is working.** Lane 01 went from 0/2 (05-24, 05-25) to 2/2 (05-26, 05-27) after the 60s clip was added. Silent MCP hangs no longer stall entire lane budgets.
- **Instrumentation blackhole detection.** Lane 03 found `results_viewed` existed in type union but was never fired — invisible gap. Fixed on 05-27. This pattern (type exists, emitter missing) may recur; add a lint or test.
- **Lane 04 value density increasing.** First `idea:build` Telegram tap ever (Survival Rounds). Ghost Replay, Shiritori streak, Word Alchemy theme reveal — ideas grounded in code-level feasibility, not fantasy.
- **Best code night = 05-27.** 5/8 lanes shipped (4/6 code lanes), 3 autonomous security fixes, 1 LCP fix, 1 experiment + instrumentation. No gate failure.

## Friction points
- **Lane 05 = dead weight.** 1/6 ships across 7 nights. Burned ~8500s of opus/sonnet compute with near-zero output. Sonnet downgrade (05-27) didn't help — still timed out at 20m. The lane's scope (full landing-page variant) is too broad for a time-boxed headless agent.
- **Lane 06 first timeout (05-27).** Previously 100% reliable (lightest lane). If GSC MCP hung, adding REST fallback for position data would be cheap insurance.
- **Lane 03 REST pre-fetch proposed 4 nights, still unshipped.** PostHog funnel queries via REST before dispatch would eliminate the main hang vector. Each night it's proposed; no one acts.
- **20 MCP servers spawn per lane.** Most unused (figma, atlassian, rive, ahrefs, fal-ai, animate-ai). Each adds startup time + potential hang surface. Lanes need 2-4 servers max.
- **Gate failures still produce "docs salvage" commits.** 3 of 7 nights had gate failures (05-22, 05-24, 05-25). Even when code fails lint/tsc, docs/ideas/reports get committed — polluting git history with half-baked outputs.
- **Stream-timeline data collected but never read back.** Tool added 05-26 but no automated grep-and-attribute step runs post-failure. The data exists; the analysis doesn't.

## Concrete improvements (ranked by impact x effort)
1. **Scope-cut lane 05 to title+meta+CTA only.** Remove full-page variant generation from prompt. Lane writes 3 fields (title, description, CTA text) per page, not a whole component. Effort: S (prompt edit). Impact: may finally ship consistently — even a 30s run producing 3 optimized strings beats 40m of timeout.
2. **Ship lane 03 PostHog REST pre-fetch.** Add `scripts/nightly/tools/prefetch-funnel.sh` that curls PostHog REST API for 7d funnel data + writes JSON to `/tmp/nightly-funnel.json`. Lane 03 prompt reads file instead of calling MCP. Effort: S. Impact: eliminates main lane 03 hang vector, lifts 50%→80%+ ship rate.
3. **Trim MCP server set per lane.** In the lane dispatch script, pass `--mcp-servers` whitelist: lane 01 = sentry+supabase; lane 02 = posthog+supabase; lane 03 = posthog; lane 04 = none; lane 06 = posthog (GSC via REST). Effort: M (dispatch script edit + test). Impact: fewer hang surfaces, faster cold start.
4. **Add post-failure stream-timeline grep.** After any exit-124 lane, auto-grep `stream-*.ndjson` for last `>` with no `✓`, log the hung tool name to the report. Effort: S (bash one-liner in revert handler). Impact: finally attributes MCP hangs to specific tools — enables targeted fixes.
5. **Instrument type→emitter lint.** `results_viewed` existed in `GrowthEvent` type for weeks with no emitter. A lint rule or test: "every GrowthEvent member must appear in at least one `trackGrowth(...)` call" catches these. Effort: M (grep-based test). Impact: no more instrumentation blackholes.
6. **Gate salvage = separate commit prefix.** When gate fails, commit docs with `docs(nightly):` prefix, not mixed with code. Or skip the commit entirely — ideas/reports are ephemeral and don't need git persistence every night. Effort: S. Impact: cleaner git history.

## New helper scripts to ship
- **prefetch-funnel.sh** — Curl PostHog REST `/api/projects/{id}/insights/funnel` with the standard 7d funnel query (homepage→CTA→game_started→game_completed→streak_continued). Write JSON to `/tmp/nightly-funnel-{date}.json`. Lane 03 prompt reads this instead of calling PostHog MCP. Called from: lane 03 dispatch (pre-flight). Effort: S.
- **attribute-hang.sh** — After any exit-124 lane, grep that lane's `stream-*.ndjson` for the last `>` entry without a subsequent `✓`. Print `HUNG_TOOL=<tool_name>` to stderr and append to the nightly report. Called from: revert handler in `scripts/nightly/run.sh`. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Ghost Replay Mode** (lane 04, 05-27): After any solo game, save move sequence as a "ghost." Next session, race your own previous run. Async self-competition with zero social infra. Fits Blast V2 (move history already emitted). Source: r/wordgames "hidden gem" thread + Drift playdrift.today comparison.
- **Survival Rounds** (lane 04, 05-26; idea:build tap 05-26): Last-word-standing elimination mode. First `idea:build` Telegram tap ever — founder greenlit this concept.
- **Daily CTA on results** (lane 03, 05-27): Experiment `exp-daily-cta-on-results-v1` shipped. Targets 22.8%→30%+ streak retention by surfacing daily challenge CTA above the auto-play countdown. Source: funnel data showing game_completed→streak_continued = biggest gap.
- **Instrumentation blackhole audit** (lane 03, 05-27): `results_viewed` was a dead type member. If one event was dead, others may be too. Source: lane 03 PostHog query returned 0 `results_viewed` events despite being in the type union.

## Loop changes shipped THIS WEEK
- `2a08d07844` — MCP_TOOL_TIMEOUT=60000 per-call watchdog. 2 confirmation nights (lane 01: 05-26, 05-27).
- `6cc98a3d7` — Stream-json tool timeline for MCP attribution. Data collecting; read-back not yet automated.
- `d6fd55f1e` — Idea Telegram card loop. First `idea:build` tap received 05-26.
- `435a39aa8` — Lane 05 downgraded opus→sonnet/600s. Result: still times out (scope cut needed next).
- `057269ab9` — Reddit pitch variety + triage autonomy raise + lane 05 bounds.
