# Loop Self-Review — 2026-07-14

## Run health (last 6 nights: 07-09..07-14)
Durations not per-lane-instrumented in logs; success/files from report bodies.

| Lane | Success rate | Files touched (avg) | Notes |
|------|--------------|---------------------|-------|
| 01 triage | 5/6 | ~10 (restore-heavy) | Restore + Sentry guards; 1PV/1RP verified by delta |
| 02 perf | 4/6 | ~1-2 | /es MP CLS −20%; homepage winner.webp LCP open; MCP blocked backend fixes |
| 03 engagement | 5/6 | ~2 | Flag wiring + revert (exp-mp-room-join); game_abandoned instrumented 07-14 |
| 04 competitor | 2/6 | 0 (drafts) | Rotation; both polish votes shipped downstream |
| 05 landing | 4/6 | ~3-4 | Gem Hunt + Crossword Clue Scramble; 1 research-only (07-12) |
| 06 seo | 2/6 | ~1 | Rotation; ES education preposition fixes |
| 07 self-learn | 2/6 | 2 | Rotation (07-12, 07-13, + tonight) |
| 08 adsense | 2/6 | ~1 | BreadcrumbList JSON-LD; hero-image blocked |
| 09 monetization | 5/6 | ~1 | Flag wiring, daily-first funnel |
| 10 dict | 2/6 | ~5-7 | Rotation; candidate lists |
| 11 mode-qa | 5/6 | ~3-5 | Crossword 55→85%; rc124 07-12 (reverted, re-gated) |
| 12 telemetry | 2/6 | ~1-2 | results_viewed blast fix 07-14; game_abandoned gap closed |

## What's working well
- **Salvage-then-restore round-trips cleanly** — `6fb8daf47` restored 07-12+07-13 gate-dropped code on 07-14; 0 pushed-code reverts across the window. The safety net is proven.
- **Mode-readiness treadmill is a repeatable machine** — crossword 55→75→85% in 3 nights on a systematic per-layer audit; word-tower graduated the same way. This is the loop's highest-signal recurring output.
- **Regression self-catch is closed-loop** — engagement lane both created and reverted `exp-mp-room-join-loading-v1` within the window via PostHog delta, no human intervention.
- **Polish-vote → shipped-slice loop closes** — both `polish:try` Telegram votes became shipped lane-05 features (Gem Hunt Dice, Crossword Clue Scramble). The steering channel works.
- **Two long-standing watches resolved 07-14** — blast `results_viewed` canonical-name fix (22 tests) and `game_abandoned` SPA instrumentation. Watches don't just accumulate; they close.

## Friction points
- **Supabase MCP flaky/absent most of the span → 4 migrations stale-backlogged.** Blocks lane 01/02 backend work every night. The apply-step never fires even on recovered nights. (#1 infra)
- **Gate wedges to rc124 idle-timeout 4/6 nights** — TS/test phase under CPU contention, not code errors. Vitest runs the FULL suite; a single slow/wedged suite rides the idle backstop to rc124 and drops the whole night's code to salvage.
- **rc124 kills whole heavy lanes** — 07-12 mode-qa timed out and reverted. No per-suite hard kill, so one wedged suite = lost lane.
- **Rotation lanes (04/06/08/10) at 2/6** — expected by schedule, but competitor ideas (Blast Ghost Round) and blog cadence stall between rotations with no carry-forward action owner.

## Concrete improvements (ranked by impact × effort)
1. **Per-suite Vitest hard-kill + changed-files scope in the gate** — wrap each test suite in a `timeout` and scope Vitest to files changed since last push. Effort: M. Impact: converts the 4/6 salvage nights toward full-pass (each avoids the idle→rc124 wedge that dropped code). File(s): `scripts/nightly/gate.sh` (or equivalent test-phase invocation), add `--changed` + per-suite `timeout 180`.
2. **Migration-drain step on MCP recovery** — in lane 01/02 preamble, add: "IF Supabase MCP responds this run, FIRST apply every file in the pending-migration queue before other work." Effort: S (prompt-only). Impact: unblocks the 4-migration backlog (upsert_push_token, add_league_xp audit, teacher_access RLS, RLS-initplan) the moment MCP is up. File(s): lane 01 + lane 02 prompt files.
3. **Homepage LCP poster-frame fix** — swap the 393KB animated `winner.webp` for a static first-frame poster + lazy-load the animation. Effort: S. Impact: homepage LCP (currently TTFB+asset-dominant) drops meaningfully. File(s): homepage hero component + `public/` asset.
4. **Carry-forward owner for the standing `idea:build` vote** — add to lane 04/05 preamble: "If an `idea:build` Telegram vote is >7d unbuilt, surface a buildable slice THIS run." Effort: S (prompt). Impact: Blast Ghost Round (voted 07-11, still unbuilt) gets an owner instead of stalling between rotations.
5. **Zombie-flag reaper** — lane 03 already flags PostHog flags reverted-in-code-but-active; add: auto-emit a one-line `deactivate flag <id>` to the human-ops queue. Effort: S. Impact: `exp-mp-room-join-loading-v1` (id 219697) and future zombies get deactivated instead of lingering as noise.

## New helper scripts to ship
- **`scripts/nightly/tools/feedback-aggregate.sh`** — reads all `docs/nightly/feedback/*.ndjson` in the last-7d window and prints counts by `type` (night:good/meh, polish:try, idea:build, reddit:*, mode:*). Lane 7 hand-aggregates this every run (repeated ≥3 nights). Effort: S. Called from: lane 07 prompt STEP-feedback. (Deferred tonight — budget; flagged for next self-learn run to author + `bash -n`.)

## New product/feature ideas surfaced by this week's data
- **Diagnose the completion crater with a per-mode abandon funnel** — now that `game_abandoned` fires (07-14, 56% completion, 262 abandons surfaced), build a per-mode drop-point breakdown. Source: lane 03/12 funnel, 07-14. This is analysis, not a feature, but it will name the highest-leverage mode fix.
- **Blast "Ghost Round"** — post-round overlay flashing the top-scorer's ghost board with 3 highlighted missed words. Source: `idea:build:d24d4c78` Telegram vote (07-11) + lane 04 competitor note (Alcove). The one unbuilt steering vote — highest founder-intent signal available.
- **Crossword Clue Scramble as the crossword hook** — already shipped as a mode-05 slice (07-14); with crossword at 85% readiness, promoting Clue Scramble to the crossword landing surface could be the release differentiator. Source: lane 05 (07-14) + lane 11 readiness (07-14).

## Loop changes shipped THIS WEEK
- **Contention-aware timeouts extended to lanes + MCP boot** (`c21664fe9`, `20d410bc9`, 07-12) — the fixed-seconds-timeout bug from 07-13 memory got applied to lane idle + MCP boot, reducing false-positive gate wedges from CPU contention. This is a prior loop-improvement proposal actually landing.
- **Vitest worker-pool flake reclassification** (`04bd9a59d`, 07-10) — gate no longer misclassifies vitest worker-pool flakes as code breaks.
