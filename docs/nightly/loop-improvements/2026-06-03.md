# Loop Self-Review — 2026-06-03

## Run health (last 6 nights)

| Lane | Success rate | Timeout rate | Avg duration | Notes |
|------|-------------|--------------|--------------|-------|
| 01 triage | 2/6 (33%) | 4/5 runs | 15-55m | MCP-heavy; only ships when scoped to 1 fix |
| 02 perf | 3/6 (50%) | 2/5 runs | 6-27m | Improved; perf-baseline expansion reliable |
| 03 engagement | 4/6 (67%) | 2/5 runs | 7-37m | Best code lane; word-tower pool shipped |
| 04 competitor | 6/6 (100%) | 0/5 runs | 2-23m | Zero timeouts; lightest MCP footprint |
| 05 landing | 1/6 (17%) | 4/5 runs | 10-42m | Worst lane; "build from scratch" always fails |
| 06 seo | 3/6 (50%) | 3/5 runs | 5-23m | Succeeds on title/meta only; fails on page builds |
| 07 self-learn | 6/6 (100%) | 0/5 runs | 5-11m | Pure docs; always ships |
| 08 adsense | 3/6 (50%) | 3/5 runs | 6-33m | Regressed from 86%; timeout on heavier tasks |
| 09 monetization | 0/2 (0%) | 2/2 runs | 59m | New; 600s budget insufficient for scope |

**Gate pass rate: 1/6 (17%).** Down from 50% last window. Only 05-28 passed cleanly.

## What's working well
- **Evening run (06-01 19:05) proved system CAN work**: all 9 lanes, <7min each, 53min total. When scope is tight, infra is fine.
- **Lightweight lanes (04, 07) = 100%**: zero MCP dependency, always finish, always gate-clean. Model for other lanes.
- **PreToolUse time guard** (shipped 06-02): mechanical deny past 80% budget prevents runaway edits. 2 nights validated.
- **TDD on pure modules** (word-wheel rarity, daily letter pool): highest ship rate pattern. Module + tests = gate-clean every time.
- **Mandatory artifact**: floor never zero. Even timed-out lanes produce docs.
- **Founder-directive fast path**: 4/4 shipped (sealed-bid, share-card, word-tower-pool, families). Fastest lane pattern.

## Friction points
- **Lane 01/05 timeout 80%**: these lanes attempt MCP-heavy investigation + multi-file edits in 15min. Structurally impossible. Lanes 01 and 05 need hardcoded scope caps in their prompts.
- **Gate pass rate collapse** (17% vs 50%): more lanes timing out = more half-written files = more gate failures. Self-verify (`tsc --noEmit`) not yet enforced mechanically.
- **Lane 09 scope mismatch**: 600s budget, 59m actual. Either raise budget or drastically reduce scope.
- **Reddit blocked 9 nights**: lane 04 compensates but native Reddit engagement = 0. OAuth spec exists but nobody implements it.
- **Search collector**: 9 nights, 0 signals. Dead code running every night.
- **Homepage LCP 7107ms**: NEW catastrophic regression (06-03). ScrollTrigger/showcase3D hero likely cause. Not yet triaged.
- **Preflight abort on dirty HEAD**: 05-31 lost a full run. No stash-or-branch handler exists.

## Concrete improvements (ranked by impact x effort)

1. **Hardcode scope caps in lane 01/05 prompts** — "Lane 01: MAX 1 Sentry fix, MAX 2 files. Lane 05: ONLY title+meta+CTA or pre-built component wiring." Effort: S. Impact: timeout rate 80%→~40%. File(s): `scripts/nightly/headless.sh` or lane prompt templates.

2. **Mechanical tsc self-verify in time guard hook** — extend `lane-time-guard.sh` to inject `npx tsc --noEmit --pretty <changed-files>` before lane exit. Blocks shipping broken code. Effort: M. Impact: gate pass rate 17%→~50%+. File(s): `scripts/nightly/hooks/lane-time-guard.sh`.

3. **Remove search collector** — dead 9 nights, 0 signals. Remove from phase 0. Effort: S. Impact: cleaner phase 0, no false-hope signals. File(s): phase 0 script/prompt.

4. **Raise lane 09 budget or split scope** — 600s for monetization lane = structurally insufficient. Either 1200s or split into "ad-UX check" (fast) + "monetization experiment" (deferred). Effort: S. Impact: lane 09 from 0% to viable. File(s): `scripts/nightly/headless.sh`.

5. **Add dirty-HEAD handler to preflight** — `git stash` or create temp branch before aborting. Effort: S. Impact: prevents full-run loss (happened 05-31). File(s): preflight script.

6. **Homepage LCP triage** — 7107ms is catastrophic. Lane 02 should prioritize this over multiplayer CWV. Add to lane 02 prompt as top signal. Effort: S (prompt edit). Impact: homepage = highest-traffic page. File(s): lane 02 prompt.

## New helper scripts to ship

- **`lint-changed-only.sh`** — runs `npx tsc --noEmit` + `npx eslint` on only git-diffed files (vs HEAD~1). Lanes call this before finishing. Eliminates full-codebase lint cost. Effort: S. Called from: lane-time-guard.sh or each lane's finalize step.

## New product/feature ideas surfaced by this week's data

- **Word Tower daily-letter economy + rewarded ads** — founder directive 06-03. Module shipped (`dailyLetterPool.ts`). Ad-refill (watch ad = +15 letters), hint purchase, rival-sabotage (ruin opponent tower section) all scoped in constants. Revenue-positive via rewarded ad integration. Source: founder directive.
- **Homepage hero perf** — 7107ms LCP means most visitors bounce before seeing content. A static-first hero (SSR text + deferred canvas) would fix LCP AND improve SEO. Source: lane 02 perf snapshot 06-03.
- **Word Heist MP mode** — `idea:build` tapped by founder. Steal-mechanics complement existing cooperative modes. Source: Telegram feedback.
- **SSR skeleton for multiplayer** — CLS 0.512 = layout shift on load. Server-rendered skeleton would fix. Improves both UX and Core Web Vitals. Source: lane 02 perf tracking (3 nights).

## Loop changes shipped THIS WEEK
- PreToolUse time guard hook (`a2334a3b3` 06-02) — mechanical deny past 80% budget. Validated 2 nights, 0 runaway edits since.
- Baseline-poison salvage (`7b2471aef` 06-01) — re-gates authored set lint-skipped when non-authored file fails on HEAD.
- Lane 09 wired (`a4eb5361a` 06-01) — new monetization lane added to headless.sh.
- Failure digest on kill/abort — Telegram notifies even on no-ship runs. Stable 12+ nights.
