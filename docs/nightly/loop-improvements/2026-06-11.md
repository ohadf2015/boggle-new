# Loop Self-Review — 2026-06-11

## Run health (last 6 nights: 06-06..06-11)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 4/6 | ~15-25m | ~4 | DEFINER/RLS REVOKEs reliable; 06-06 timeout, 06-09 gate-fail |
| 02 perf | 4/6 | ~30-40m | ~3 | revalidate/CLS wins; 06-06+07 timed out (MCP-heavy) |
| 03 engagement | 4/6 | ~30-40m | ~6 | overlays wired but flags never created → dark |
| 04 competitor | 6/6 | ~10-15m | 3 | bulletproof, prompt-only, RSS fallback |
| 05 landing | 4/6 | ~25-40m | ~7 | polish ships; over-scope timeouts 06-06+07 |
| 06 seo | 5/6 | ~20-30m | ~6 | ES +156% impr; 06-06 timed out |
| 07 self-learn | 6/6 | ~10m | 3 | prompt-only, never fails |
| 08 adsense | 5/6 | ~15m | 2 | JSON-LD + author page; 06-06 timed out |
| 09 monetization | 4/6 | ~40m | ~4 | ad-UX/upsell; over-scope + 06-09 gate-fail |
| 10 dictionary | 4/6 | ~60-90m | ~5 | dual-judge ships; vitest abort-traps wedge gate |

Aggregate: ~23/60 lane-nights shipped code (38%), ~14/60 timeouts (23%), gate clean 2/6 nights.

## What's working well
- **Prompt-only lanes (04/07, mostly 06/08) never time out** — 0 MCP round-trips = 0 timeouts. The reliability dividing line is MCP-call count, not task difficulty.
- **Mandatory-Artifact floor** held on both gate-fail nights (06-06, 06-09) — zero fully-empty nights in 6.
- **Security-migration lane (01)** shipped 4 DEFINER REVOKEs across the window with 0 reverts — the single most consistent *code* output.
- **Founder polish pipeline** (lane 04 idea → lane 05 build, 1-night latency) converted 5 mode-polish components clean.
- **revalidate=3600 pattern** is now reusable doctrine — proven LCP 4903→1706ms.

## Friction points
- **Experiments ship dark.** Lane 03's two overlays (`exp-mp-quickplay-wait-v1`, `exp-invite-arrival-clarity-v1`) are fully built but neither PostHog flag was ever created — so the code measures nothing and the funnel drops it targets stay open. The loop has no handoff for the human flag-creation step. This is the single highest-waste leak: real engineering effort with zero measurable payoff.
- **MCP-heavy lanes timeout-cluster.** 06-06 lost 6 lanes to >15min timeouts simultaneously; all 6 were the MCP-using lanes. No per-lane MCP-call cap exists.
- **Over-scope timeouts on 05/09.** Both repeatedly attempt TDD + 5-locale + component + page-wire in one 15-min budget; ES locale is the part that gets dropped at the file cap.
- **Gate flakiness.** Dictionary nights (lane 10) trigger vitest abort-traps that wedge the full-suite gate even after the 8GB heap bump; this forces docs-only salvage.
- **Push-fail stranding.** 06-07's commit landed in `refs/nightly-pending/` because master advanced (daemon) mid-run → non-fast-forward. Runner doesn't rebase-before-push.

## Concrete improvements (ranked by impact × effort)
1. **Digest "FLAG NEEDED" block** — when lane 03 (or any lane) wires an experiment, it MUST append a one-line `FLAG NEEDED: <key> | <variants> | <hypothesis>` to the manager-summary digest. Effort: S (prompt edit to `prompts/03-engagement.md` + digest template). Impact: unblocks 2 dark experiments now + every future one; turns wasted wiring into measurable funnel tests. File(s): `prompts/03-engagement.md`, digest formatter.
2. **Per-lane MCP-call cap** — add `MAX_MCP_CALLS≈4` guard surfaced into MCP-using lane prompts (01/02/03/05/06/08/09/10) with "prefer prompt-only path; if you need a 5th MCP round-trip, defer and document". Effort: S (prompt preamble line). Impact: directly attacks the 23% timeout rate (06-06 lost 6 lanes this way). File(s): shared lane preamble.
3. **Locale-first ordering for 05/09** — mandate translations land BEFORE component/page work so the file cap never drops ES. Effort: S (prompt edit). Impact: kills the recurring "ES incomplete" ship. File(s): `prompts/05-landing.md`, `prompts/09-monetization.md`.
4. **Rebase-before-push in runner** — `git pull --rebase` (or fetch+rebase onto origin/master) before the single end-of-run push to avoid non-fast-forward stranding. Effort: M (runner shell). Impact: recovers the ~1/6 push-fail nights. File(s): `headless.sh`/gate push step.
5. **Quarantine dictionary tests from the full-suite gate** — run lane 10's vitest in an isolated `--project` so its abort-traps can't wedge the shared gate. Effort: M. Impact: lifts gate clean-rate above 2/6 on dictionary nights. File(s): `vitest.config.ts`, gate script.

## New helper scripts to ship
- **`scripts/nightly/tools/feedback-tally.sh`** — one-shot aggregator: reads `docs/nightly/feedback/*.ndjson` for last 7 dated files, emits `polish:try/pass`, `idea:build/pass`, `night:good/meh`, `reddit:*` counts. Lane 7 hand-rolls this `grep|sed|sort|uniq` chain every night; codify it. Called from lane 07 prompt. Effort: S. (Defer to a code-budget night — not started tonight to avoid a half-written script poisoning the gate.)

## New product/feature ideas surfaced by this week's data
- **Invite arrival = make the landed→consumed bridge visible.** Source: invite funnel 83% drop (`invite_landed` 18 → `invite_consumed` 3, 29 rage-clicks). The `exp-invite-arrival-clarity-v1` spinner card is built but dark; once flagged, the data says a clear "joining {host}'s game…" state should recover most of that drop.
- **Practice wheel-drag onboarding hint.** Source: `/es/practice/wheelRush` 47 started → 27 completed (43% drop, 5+ nights). The ring UI gives no drag affordance; a one-time drag-hint (mirroring the classic FTUE glow-tile that already works) is the cheapest fix.
- **Hebrew daily-word internal link.** Source: `המילה היומית` 0→180 impr, pos 8.7 in 2 nights with zero internal links. A single `/he/daily` cross-link is the lowest-effort SEO win on the board.

## Loop changes shipped THIS WEEK
- `00d96ad3f` — n≥50 sample gate added to `prompts/02-perf.md` (kills sub-floor false regression verdicts; debunked the phantom `/es/multiplayer` INP regression).
- `5cb7f28a0` — usage-limit-aware runner (sleep-until-reset + recompute deadline epochs) ended the instant-fail cascade.
- `b266ca10e` — vitest heap ceiling raised to 8GB to reduce gate OOM-wedge.
