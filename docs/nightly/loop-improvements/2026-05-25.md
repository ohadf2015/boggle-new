# Loop Self-Review — 2026-05-25

## Run health (last 7 nights)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 2/5 (40%) | ~15m | 4 | 3 MCP timeouts; feedback-digest adds scope |
| 02 perf | 2/5 (40%) | ~11m | 3 | 1 cap-revert (fixed), 2 MCP timeouts |
| 03 engagement | 1/5 (20%) | ~20m | 6 | Worst lane; PostHog funnel MCP = hang vector |
| 04 competitor | 6/6 (100%) | ~7m | 2.5 | Best lane; no heavy MCP, lightweight |
| 05 landing | 1/4 (25%) | ~22m | 2 | Opus/1500s burns fully; 1 correct skip |
| 06 seo | 5/5 (100%) | ~12m | 3.5 | Tied best; IndexNow + GSC reads reliable |
| 07 self-learn | 3/4 (75%) | ~8m | 2 | Prompt-only, fast; 1 MCP timeout |
| 08 adsense | 3/3+ (100%) | ~5m | 1 | Fastest; correct no-ops when bar not met |
| **Total** | **23/37 (62%)** | **~12m** | **3** | **12 timeouts (32%), 2 cap-reverts (5%)** |

## What's working well
- Lanes 04/06/08 at 100% ship rate — lightweight, no heavy MCP dependency, fast (<12m avg). These three reliably produce value every night.
- Per-lane scoped revert: 05-25 reverted 4 lanes while keeping 2 shipped — zero collateral damage, proven 5 nights.
- File caps removed (`4b59a9a35`): zero cap-reverts since 05-24 (was 5% of lane-runs before). Second-most-impactful infra fix this week.
- PostHog REST helper + MCP graceful degrade: PostHog-specific hangs eliminated (1 of 3 MCP providers fixed).
- Reddit curl helper: lane 04 gets real subreddit data after 5 nights of total IP blockade.
- Lane 06 catching Spanish indexing burst (+840-6667%) in real-time and targeting meta edits — time-sensitive SEO window.
- Failure digest Telegram: even 05-25's worst-night-yet produced a useful alert.

## Friction points
- **MCP silent hangs dominate everything.** 12/37 lane-runs (32%) exit 124 with zero output. PostHog REST fixed ONE provider; Sentry + Supabase MCP calls still hang. This is the single bottleneck — all other improvements are marginal until this is fixed.
- **Tonight (05-25) = worst run: 33% ship rate (2/6).** 4 consecutive timeouts. No new infra shipped to address root cause. Trend is WORSENING (24% on 05-24, 32% cumulative, 67% tonight).
- **Lane 03 almost useless at 20% ship rate.** High-value when it ships (experiment setup) but too expensive at current failure rate. Each timeout burns ~25m of sonnet/1500s.
- **Lane 05 opus budget wasteful.** Burns 1500s of opus on timeout with zero output. Lane 04 ships 100% on sonnet/600s.
- **Lane 01 feedback-digest injection = net negative.** Ship rate dropped 100% to 40% after `18734c040`. Extra MCP reads push past timeout.
- **Telegram feedback loop dead.** 6 taps in 6 nights (5 smoke-test). In-game feedback: zero. No signal to calibrate quality.
- **Admin-session false regressions in lane 02.** CLS 0.882, LCP 8775ms = admin-only artifacts polluting baselines 3 nights running.

## Concrete improvements (ranked by impact x effort)
1. **Per-call MCP timeout wrapper** — wrap each MCP tool call in `gtimeout 30`; kill and skip on hang. Effort: S. Impact: timeout rate 32% to <10%, saves ~90 opus-min/night. File(s): `scripts/nightly/headless.sh`. **PROPOSED 4 NIGHTS — SHIP OR ESCALATE.**
2. **Lane 05 model downgrade** — switch opus/1500s to sonnet/720s. Effort: S (1 config line). Impact: timeout costs 60x less per token; fail-fast. File(s): lane runner config.
3. **Lane 01 digest trim** — inject only top-3 feedback items, not full digest. Effort: S. Impact: ship rate 40% to 60%+. File(s): `scripts/nightly/lanes/01-triage.md`.
4. **Admin-session filter in perf queries** — add `AND properties.$user_id NOT IN ('<admin_ids>')` to web_vitals HogQL. Effort: S. Impact: eliminates 3 nights of false CLS/LCP regressions. File(s): `scripts/nightly/lanes/02-perf.md`.
5. **Lane 03 REST pre-fetch** — fetch 7d funnel via PostHog REST before lane dispatch; inject as local JSON. Effort: M. Impact: lane 03 ship rate 20% to 60%+. File(s): `scripts/nightly/tools/posthog-funnel.sh` + lane prompt.
6. **MCP health probe in preflight** — ping Sentry/PostHog/Supabase with 10s timeout; skip dependent lanes if provider down. Effort: S. Impact: don't waste budget on doomed lanes. File(s): `scripts/nightly/headless.sh`.

## New helper scripts to ship
- **mcp-timeout-wrap.sh** — wraps a single MCP tool invocation in `gtimeout 30`; on timeout, logs tool name + lane to `~/logs/lexi-nightly/mcp-hangs.log` and returns empty. Called from lane runner. Effort: S. Removes the single biggest failure mode.
- **posthog-funnel-prefetch.sh** — fetches 7d conversion funnel via PostHog REST API; writes JSON to `/tmp/nightly-funnel-{date}.json`. Called before 03-engagement. Effort: M. Eliminates #1 hang vector for lane 03.

## New product/feature ideas surfaced by this week's data
- **Shareable emoji result card for MP** — lane 04 TOP idea 4 consecutive nights. r/wordgames indie score-sharing thread (8 upvotes). Wordle case study: 90k to 300k DAU from share mechanic. Daily already has emoji sharing; MP doesn't. Effort: S. Source: lane 04 reports 05-22/23/24/25.
- **Lazy game-client hydration on /multiplayer** — LCP p75 4228-5806ms (POOR). Split socket/game bundle out of critical path. Effort: M-L. Source: lane 02 reports 05-21/22/23.
- **Post-game "words you missed" coach** — Nanagrams differentiator; LexiClash has zero post-game learning. Retention hook + educational value prop. Effort: M. Source: lane 04 report 05-23.
- **"Games actually finish" positioning** — Crossplay #1 complaint = "graveyard of half-finished matches"; 3-min format solves structurally. Pure copy/SEO, zero risk. User passed this concept (idea:pass:41b1c6b6). Effort: S. Source: lane 04 05-23.

## Loop changes shipped THIS WEEK
- `eefb0d52e` PostHog REST helper — eliminated PostHog MCP hangs (1 of 3 providers fixed)
- `3da3f4407` MCP outage graceful degrade — preflight no longer aborts on single provider down
- `4b59a9a35` Remove file-count caps — eliminated #2 revert cause (zero cap-reverts since)
- `311af3d19` Reddit curl + UA helper — lane 04 gets real subreddit data
- `d7b6e4058` getUpdates poller collision fix — Telegram feedback unblocked
- `c79e116f5` Isolated worktree gate — build safe from dev server races
- `477fdac41` Drop-and-re-gate salvage — ship docs despite one bad code file
- `5c812fa9c` Push survives dirty tree (autostash) + reduce lane 1/3 MCP hangs
- `047f424d0` Autonomous improvement loop 2026-05-24
