# Loop Self-Review — 2026-06-05

## Run health (last 6 report nights, 05-30..06-05)
| Lane | Success rate | Avg files | Notes |
|------|--------------|-----------|-------|
| 01 triage | 4/6 | ~3.2 | sec-hardening migrations; timed out 05-30/06-02 |
| 02 perf | 3/6 | ~2.3 | LCP/CLS diag + db index; timed out 05-30/06-02/06-03 |
| 03 engagement | 3/6 | ~4 | experiment-event wiring; timed out 05-30/06-01/06-02 |
| 04 competitor | 6/6 | ~3.2 | 100%; ideation + reddit drafts, 0 timeouts |
| 05 landing | 4/6 | ~4.7 | sealed-bid/word-tower/caption-clash polish |
| 06 seo | 4/6 | ~4.3 | title/meta rewrites; timed out 05-30/06-02 |
| 07 self-learn | 6/6 | ~3.5 | 100%; prompt-only |
| 08 adsense | 3/6 | ~2.7 | JSON-LD + word-count; GSC-scope-blocked |
| 09 monetization | 2/6 | ~4.7 | runs rarely; ayeT adapters dark, not live |

## What's working well
- **Low-MCP lanes (04, 07) = 100%** across 6 nights incl. the 06-02 collapse. The clearest causal signal in the data: timeouts track MCP/external-call count, not task difficulty.
- **Baseline-aware gate + bounded budgets** keep 06-04/06-05 fully clean — the guard works when MCP load is light.
- **Founder-directive fast path** turned same-night directives (file-cap 06-04, party-polish 06-05) into shipped action.
- **Mandatory-Minimum-Artifact** held the floor on every timed-out lane.

## Friction points
- **MCP-spiral collapse recurred 06-02** (6 lanes rc=124). The single biggest failure mode is still un-fixed: lanes 01/02/03/06/08 each open Sentry+Supabase+PostHog serially with no per-lane call budget. 11+ nights unaddressed.
- **Lanes 02/03/08 hover at 3/6** — all heavy MCP consumers. Same root cause as the collapse, not three separate problems.
- **Lane 09 runs only 2/6** — scheduling/gating unclear; monetization work stalls.
- **Dead experiment flags pile up** (4+ wired, 0 exposures) — instrumentation ships but PostHog flags never get created, so A/B data never flows. Wasted lane-02/03 effort.
- **`night:meh` 06-03** — the one negative Telegram vote lands on a partial/timeout night, confirming users feel the collapse nights.

## Concrete improvements (ranked by impact × effort)
1. **Ship `MAX_MCP_CALLS=3` per-lane cap.** Hard-stop external MCP calls after 3; fall back to cached/REST helpers. Effort: M. Impact: directly kills the 06-02-style collapse (would lift 02/03/06/08 from ~3/6 toward 5/6). File(s): `scripts/nightly/lib/headless.sh` + per-lane preamble note.
2. **Front-load each MCP lane with a single cached intel snapshot.** Run one Sentry+Supabase+PostHog pull at phase-0 into a shared JSON; lanes read the file, not the MCP. Effort: M. Impact: removes serial-call summation entirely. File(s): `scripts/nightly/tools/intel-snapshot.sh` (new).
3. **Add a `polish:try` priority-queue line to lane 05's preamble.** Feed the aggregated `polish:try:<slug>` counts so lane 05 works the top-clicked modes first. Effort: S. Impact: aligns party-polish work with founder one-click signal. File(s): lane 05 prompt.
4. **Preflight stash-or-warn for unpushed founder commits.** Stash dirty HEAD instead of aborting the whole run. Effort: M. Impact: recovers ~1 lost night/window. File(s): `scripts/nightly/lib/headless.sh` preflight.
5. **Auto-prune dead experiment flags OR auto-open PostHog flags.** A lane-03 step that lists wired-but-0-exposure flags and either removes them or files the flag-creation task. Effort: S. Impact: stops wasted instrumentation; unblocks A/B data. File(s): lane 03 prompt + `scripts/nightly/tools/dead-flag-scan.sh` (new).

## New helper scripts to ship
- **`scripts/nightly/tools/feedback-aggregate.sh`** — wraps the python aggregation lane 7 ran by hand tonight (counts `night/mode/idea/reddit/polish` callbacks across last-7 ndjson). Fires every night for lane 7 + feeds lane 04/05 priority. Effort: S.
- **`scripts/nightly/tools/intel-snapshot.sh`** — see improvement #2; one cached Sentry/Supabase/PostHog pull shared by all MCP lanes. Effort: M.

## New product/feature ideas surfaced by this week's data
- **Fix-and-ship `/en/multiplayer` CLS (0.057→0.29, AutoHideHeader null).** Source: lane 02, 06-05. Cheapest perf win available; fix already authored. Multiplayer is the highest-INP/CLS route — directly hurts the competitive flow.
- **Polish queue from `polish:try` votes:** sealed-bid (×3) and word-tower (×2) lead. Source: Telegram callbacks. Founder is actively asking for these — make them the admin party-game polish backlog.
- **Index the next hot read path** after `idx_web_vitals_player_id`'s win (3207ms→index). Source: lane 02, 06-05. Profile/leaderboard reads are candidates.
- **Build the 3 `idea:build`-voted concepts** (hashes 22d362d4, 6fd3c339, a462044e vs 1 pass). Source: Telegram. User wants ideas built — map hashes to `docs/nightly/ideas/*` and queue for lane 05.

## Loop changes shipped THIS WEEK
- **06-04:** `LEXI_LANE_FILE_CAP` 8→999 in `headless.sh` (founder directive) — unblocked full-locale ships in one night.
- No other infra commits landed; `MAX_MCP_CALLS` cap proposed 11+ nights running, still un-shipped (carried as improvement #1).
