# Loop Self-Review — 2026-07-30

Window: 2026-07-24..07-30 (7 nights). Sources: `docs/nightly/reports/2026-07-2{4..30}.md`, `~/logs/lexi-nightly/run-2026072{4..9}*.log` + `run-20260730-010001.log`, `docs/nightly/feedback/*.ndjson`.

## Run health (last 7 nights)
Denominator = nights the lane was actually SCHEDULED (the scheduler runs 8/12 per night on rotation).

| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 4/6 | ~15m | 6.5 | 2 fails, both rc75 (usage window), not lane logic. 07-26 restored 14 salvaged files. |
| 02 perf | 5/6 | ~12m | 2.4 | Best night 07-30: Mgmt-API SQL fallback applied a 3-night-stale migration without MCP. |
| 03 engagement | 5/5 | ~13m | 2.4 | Most reliable lane, 2nd window running. Shipped a `deactivate` verb + used it same night. |
| 04 competitor | 2/2 | ~14m | 2.5 | 10 ideas surfaced. 1 of 2 polish pitches targeted a PUBLIC mode → wasted a founder vote. |
| 05 landing | 5/5 | ~16m | 3.0 | 0 reverts, 5 windows running. GSAP count-up on gems (07-29) + cards (07-30). |
| 06 seo | 2/2 | ~10m | 2.5 | ES `aprender inglés` meta 07-28. Rotation-limited, not failure-limited. |
| 07 self-learn | **1/6** | ~15m | 2 | **Scheduled out 07-26..07-29 as "zero brief signal." learnings.md went 6 days stale.** |
| 08 adsense | 2/2 | ~11m | 1.5 | 07-27 correctly no-op'd (nothing cleared the bar); 07-28 leaderboard 220→330w. |
| 09 monetization | 4/4 | n/a | 2.5 | Always bundled into 03/12 output; never emits its own report block. |
| 10 dictionary | 1/2 | ~14m | 4 | Lowest-frequency lane in the rotation. |
| 11 mode-qa | 4/6 | ~14m | 3.5 | sealed-bid 45→79%, 0 reverts. 1 rc124 idle-kill. Visual QA blocked 5 nights. |
| 12 telemetry | 2/2 | ~13m | 3.5 | **Ran TWICE on 07-30**, wrote two contradicting blocks (74/39 DEAD vs 37/33 DEAD). |

**Night-level:** 07-24 ✅ · 07-25 ❌ **0 ships** (3× rc75 → circuit-breaker → 5 deferred) · 07-26 ✅ 8/8 · 07-27 ✅ 8/8 · 07-28 ✅ 8/8 · 07-29 ⚠️ 6/8 · 07-30 ✅ (in progress).
**Gate:** 0 false-drops (was 2/6 last window). But 4 of 5 shipping nights went out via a FALLBACK tier — `baseline-red` (07-26, 07-28), `typecheck-tier` (07-27, 07-29). The gate has not passed cleanly in 5 nights.
**Push:** rejected→rebase→retry on 07-28 and 07-29; the retry loop handled both in <3.5m. Working as designed.

## What's working well
- **The gate's fallback tiers are the fix we asked for last week.** Last window's #1 friction ("drop-all on unattributable red" — 25 clean files dropped over 2 nights) produced **zero** false-drops this window. `baseline-red` proves the red pre-exists on clean master; `typecheck-tier` ships at reduced strength when `tsc --noEmit` + `test:changed` pass standalone. Both are load-bearing now. Do not regress them.
- **Stacked-incremental mode QA is the most reliable value engine in the loop** — sealed-bid 45→60→70→77→79% across 5 nights, 0 reverts, a TDD test per fix. Two modes in a row (shiritori 42→90 last window).
- **Lanes ship inside their 15m budget.** Every completed lane landed in 7–20m. The Mandatory-Minimum-Artifact floor held: no lane produced nothing while running.
- **The autonomy matrix is calibrated.** 07-30 lane 01 correctly deferred a coin-deficit report (HARD LINE) and a zero-stack-frame PostHog error, while lane 02 autonomously applied a live RLS migration. Nobody asked permission for a reversible fix; nobody touched the economy.
- **Lanes now ship their own tooling.** Lane 03 added `posthog-experiment.sh deactivate` and used it the same night to kill a flag that had been *logged* for 5 nights. That converts a recurring observation into a one-time fix.

## Friction points
- **Pre-lane latency is now the loop's dominant cost.** 07-29: `nightly-loop start 01:01` → `baseline sha 05:24` is **4h23m with not one log line**, then brief 22m, then the MCP probe 3h07m → lane 1 at **10:33**, push at **15:48**. 07-25: start 01:13 → lane 1 at 06:45 → 3 lanes hit rc75 → circuit-breaker → **0 ships**. The lanes are healthy; they're just being started after the usable window is gone.
- **The supabase MCP probe hangs ~1h51m per attempt.** `scripts/nightly/lib/mcp-probe.sh:63` runs `gtimeout "$to" "$cmd" | head -1` inside `resp=$(…)`. `gtimeout` bounds `npx` only; the command substitution waits for **every** writer of the pipe to close, so an orphaned grandchild holding stdout blocks it past the 90s budget. 3 attempts × ~1h51m = 3h07m on 07-29 — and the probe failed anyway, after lane 02 proved on 07-30 that the Management-API fallback works without MCP at all.
- **4 test files are red on clean master**, so every night pays to *prove* the baseline is red rather than just passing: `daily/__tests__/DailyChallengeGame.trackGameStart.test.tsx`, `tutorial/ModeCoach.test.tsx`, `api/stats/record-game/__tests__/route.test.ts`, `daily/__tests__/WordWheelResults.crossPromoTranslation.test.tsx`.
- **Visual QA has been impossible for 5+ nights.** An agent-browser cookie-consent overlay renders outside the snapshot a11y tree; `find text`, `click @ref`, and coordinate `mouse down+up` all failed (07-30). It is the sole remaining blocker on sealed-bid promotion AND the reason lane 02 has re-derived the same CLS hypotheses for three weeks.
- **The scheduler starves lane 07.** "Zero brief signal" is structurally true for self-learn — it reads reports, not signals — so it lost 4 of 5 nights and every lane read a 6-day-stale preamble, including a stale admin-beta mode list that made lane 05 waste two target-selection passes on public modes.
- **No lane-run idempotence.** Lane 12 executed twice on 07-30 and its second pass rediscovered its own uncommitted fix and re-diagnosed it from scratch.

## Concrete improvements (ranked by impact × effort)
1. **Bound the MCP probe pipeline, not just `npx`** — in `scripts/nightly/lib/mcp-probe.sh:63`, wrap the whole pipeline: `resp=$("$to_bin" -k 5 "$to" bash -c '<printf | env cmd | head -1>')`. Also drop `NIGHTLY_PROBE_RETRIES` 3→1 and demote the probe to informational (lane 02 proved the Management-API fallback works). Effort: **S**. Impact: reclaims up to **3h07m** of usage window per night; directly reduces rc75/rc124 lane deaths. File: `scripts/nightly/lib/mcp-probe.sh`, `scripts/nightly/run.sh:384`.
2. **Log and cap the 01:00→baseline gap** — 4h23m elapses with no log line, so we can't tell a quota wait from a hang. Emit a heartbeat every 5m in that phase and hard-cap it; if the window isn't usable, either start lanes anyway or abort with an alert (silent 4h wait = Class 4). Effort: **S**. Impact: turns the loop's largest unexplained cost into a measurable one; prevents another 07-25-style 0-ship night. File: `scripts/nightly/run.sh` (pre-baseline phase).
3. **Pre-seed the cookie-consent dismissal in the browser profile** — set the consent cookie/localStorage key before first navigation instead of trying to click a portal-rendered dialog. Effort: **S/M**. Impact: unblocks sealed-bid 79%→promotion AND gives lane 02 the Layout-Shifts capture it has needed for 3 weeks (target: MP CLS 0.949 → <0.1). File: new `scripts/nightly/tools/browser-consent-seed.sh` + a line in the lane 02 / lane 11 prompts.
4. **Pin lane 07 to a fixed weekly slot, exempt from signal ranking** — self-learn has no brief signal by construction, so signal-ranking will always starve it. Effort: **S**. Impact: every lane reads a ≤7-day-old preamble instead of a 6-day-stale one with a wrong mode list. File: `scripts/nightly/run.sh` lane-scheduler block.
5. **Fix the 4 baseline-red test files in one dedicated lane-night** — assign to lane 01 or a one-off. Effort: **S/M**. Impact: the gate passes cleanly again, so 4-of-5 nights stop shipping "at reduced strength" and the fallback tiers go back to being a safety net rather than the normal path.
6. **Lane-run idempotence guard** — before dispatch, skip any lane whose `### Lane <n>` header already exists in tonight's report. Effort: **S**. Impact: kills duplicate runs (lane 12, 07-30) and the contradicting report blocks they produce. File: `scripts/nightly/run.sh` lane dispatch.
7. **Impact checks must assert a plausible denominator** — require the exposure/denominator event to be non-zero before emitting a verdict; report `no-exposure` instead of `neutral`. 07-30 produced two `neutral` verdicts that were really "the feature was never seen" (`issue-probe`: the upstream `game_feedback` card is throttled to 0/7d; `brain-drill`: the brief's metric name doesn't exist). Effort: **S**. Impact: stops the ledger accumulating false `neutral` rows that suppress real follow-up. File: lane 01/03 prompts + `docs/nightly/impact-ledger.ndjson` writer.

## New helper scripts to ship
- **`scripts/nightly/tools/browser-consent-seed.sh`** — seeds the cookie-consent cookie/localStorage key into the agent-browser profile so screenshots aren't blocked by an undismissable portal dialog. Fires the same manual dance ≥5 nights running (lane 11 visual QA, lane 02 CLS capture). Called from the lane 02 and lane 11 prompts before first navigation. Effort: **S/M**.
- **`scripts/nightly/tools/supabase-sql.sh`** — thin wrapper over the Management API raw-SQL endpoint that lane 02 hand-rolled on 07-30 to apply a stale migration without MCP. Codifies the fallback so no future lane has to discover it, and makes MCP-down a non-event. Called from lane 01 + lane 02. Effort: **S**.
> Not proposing a third — nothing else repeated ≥3 nights that isn't already scripted.

## New product/feature ideas surfaced by this week's data
- **Surface Brain Drill on the daily hub.** `drill_completed = 0 events over 8 days` (07-30 lane 01) — the founder-voted word-replay feature shipped 07-26 into a mode with *no traffic*. The feature isn't the problem; discovery is. Cheapest test: one Brain Drill card in the `/daily` quest list.
- **Ship the `RoomListView` lobby skeleton — it is the single largest CWV win available.** `/en/multiplayer` CLS 0.949 with root cause confirmed as the socket `connecting→lobby` full-viewport DOM swap (07-28); `NativeLanguageBanner` and `CookieConsent` are both ruled out. 4–6h, founder priority #1 (SPEED), and MP is the highest-traffic non-daily route.
- **Crossword clue difficulty tiers (★1/★2/★3).** Source: Arduu SAT-word thread, r/wordgames 07-27 — the OP names the "playable vs. vocab worksheet" tension explicitly and difficulty tiering as their solution. One `difficulty` field in the clue JSON + a badge render, no gameplay change. Doubles as an **education** hook (founder priority #3): teachers can assign a tier.
- **Promote sealed-bid.** 79% readiness, 0 code blockers, 5 consecutive audit nights, Elo badge + countdown timer + full RTL pass all shipped. It is waiting on one screenshot, not on engineering.

## Loop changes shipped THIS WEEK
- `scripts/nightly/lib/posthog-experiment.sh` — added an idempotent `deactivate <key>` verb (lane 03, 07-30) and used it the same night to kill `exp-mp-room-join-loading-v1`, a zombie flag that had been *logged and not actioned* for 5 nights. `bash -n` clean. This is the pattern to copy: when a lane keeps reporting the same finding, give it the write verb.
- Gate fallback tiers (`baseline-red`, `typecheck-tier`) — carried 4 of 5 shipping nights and closed out last window's #1 friction item at 0 false-drops.
