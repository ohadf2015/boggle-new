# Loop Self-Review — 2026-06-02

## Run health (last 7 nights)
| Lane | Completion | Avg duration (success) | Avg files | Notes |
|------|------------|------------------------|-----------|-------|
| 01 triage | 3/7 (43%) | ~5m | 2-3 | MCP-heavy; succeeds only when scoped to 1 fix |
| 02 perf | 3/7 (43%) | ~12m | 2-8 | Improved from 1/7; multiplayer CWV still deferred |
| 03 engagement | 4/7 (57%) | ~15m | 2-27 | Wide variance; 27-file run was 06-01 experiment |
| 04 competitor | 7/7 (100%) | ~15m | 3-12 | Zero timeouts; WebSearch compensates for Reddit block |
| 05 landing | 2/7 (29%) | ~10m | 7-12 | Worst lane; only ships with founder directive |
| 06 seo | 4/7 (57%) | ~10m | 3-10 | Reliable when scoped; GSC still blocked |
| 07 self-learn | 7/7 (100%) | ~7m | 2-3 | Zero dependencies = zero failures |
| 08 adsense | 6/7 (86%) | ~13m | 1-10 | Big improvement from 2/6 last window |

**Gate outcomes:** 3/6 nights passed gate → pushed (50%). Gate is the binding constraint, not individual lanes.

## What's working well
- **Phase 0 intel brief** is the single biggest improvement this window. 06-01 evening had 9/9 lanes complete in <10min — zero timeouts — directly attributable to pre-ranked signals eliminating live MCP.
- **Lightweight lanes (04, 07, 08) are the reliability backbone.** 100%, 100%, 86% — they always ship. The loop's value floor comes from these.
- **KEEP_TIMEOUT_PARTIALS + docs-only salvage** means even failed runs produce value. 6 nights stable.
- **Founder `polish:try` feedback loop** is accelerating: 6 taps this week across 4 different modes (sealed-bid, blast, word-vault, word-forge). Shows broadening engagement with admin-gated modes.

## Friction points
- **Gate failure on clean-timed lanes** (06-01 evening). All lanes finished, gate rejected 3 rounds. Root cause: lanes don't verify their own output. Each lane trusts the gate to catch problems, but the gate drops ALL authored code on failure. Lanes need to self-verify.
- **Lane 05 is structurally broken at 29%.** The "build a landing page" scope exceeds the 12-min budget 5/7 times. Only ships with pre-built code or founder directive. Needs prompt redesign.
- **Search collector** runs every night, returns 0 signals for 8+ nights. Dead weight in Phase 0.
- **Reddit API blocked 7+ nights.** Lane 04 compensates but native drafts are gone. OAuth setup or retirement overdue.
- **GSC scope missing 6+ nights.** Lane 06 can't pull live search data. `webmasters.readonly` ADC fix is trivial but hasn't happened.
- **05-31 preflight abort** on dirty HEAD wasted 2 retries. No stash-or-branch handler.

## Concrete improvements (ranked by impact x effort)
1. **Per-lane self-verification** — add `npx tsc --noEmit <changed-files> && npx eslint <changed-files>` to the lane exit protocol in `headless.sh`. Effort: S. Impact: prevents 06-01-style gate failure where all lanes complete but authored code breaks. File: `scripts/nightly/headless.sh`.
2. **Remove search collector** — delete the search section from Phase 0 intel gathering. 0 signals for 8 nights. Effort: S. Impact: saves ~30s Phase 0 budget + removes stale "search=0" noise. File: Phase 0 prompt / `scripts/nightly/lib/`.
3. **Redesign lane 05 prompt** — cap scope to "edit existing page metadata/CTA" or "wire pre-built code." Ban "build new page from scratch." Effort: S (prompt edit). Impact: ship rate 29% → target 60%+. File: lane 05 prompt.
4. **Add GSC webmasters.readonly scope** — add scope to Application Default Credentials. Effort: S. Impact: unblocks live SEO data for lane 06 (6 nights blocked). File: GCP IAM config.
5. **Reddit OAuth or retire** — either configure OAuth (spec at `docs/nightly/reddit-oauth-setup.md`) or remove Reddit fetch from lane 04. 7 nights blocked = decision needed. Effort: M. Impact: unblocks native Reddit drafts or saves wasted fetch cycles.
6. **Dirty-HEAD handler** — stash WIP or create temp branch instead of aborting preflight on unpushed commits. Effort: M. Impact: prevents 05-31-style full-night waste (3 retries, 0 lanes run). File: `scripts/nightly/headless.sh` preflight section.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/lane-self-verify.sh`** — given a list of authored files, runs tsc --noEmit + eslint on only those files. Returns exit 0 (clean) or exit 1 (dirty) with specific errors. Called at lane exit before artifact update. Removes per-lane reimplementation of verification. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Word Detective daily** — founder tapped `idea:build` on 06-01. Show a dictionary definition with blanked target word, 5 guesses. Lane 04 concept (06-01): S effort, reuses daily-mode infra. Source: lane 04 report + Telegram `idea:build:6fd3c339`.
- **Word Heist MP** — lane 04 top idea 06-02. After scoring, 3s steal window for opponents to extend the word and claim points. Validated by Grabble/Gamewright board games. Zero browser equivalents. Source: lane 04 report 06-02.
- **Tower Seed Daily** — shared daily board for Word Tower. Same "did you beat floor 12?" hook as Word Wheel. Word Tower is admin-only, zero prior pitches. Source: lane 04 report 06-02.
- **Polish broadening across admin modes** — founder `polish:try` expanded to word-vault + word-forge this week (was only sealed-bid before). Signal: these modes are getting playtested. Source: Telegram feedback aggregation.

## Loop changes shipped THIS WEEK
- **06-01 `a4eb5361a`:** lane 09 wired, re-gate convergence fix (bounded while), per-lane minute-budget + scope-discipline in headless.sh contract.
- **06-01 `7b2471aef`:** baseline-poison salvage (re-gate lint-skipped), reddit snapshot path fix.
- **05-31 `8176f9baa`:** repaired 5 red test files that forced nightly docs-only salvage.
