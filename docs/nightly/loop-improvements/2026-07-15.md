# Loop Self-Review — 2026-07-15

## Run health (last 7 nights, 07-09..07-15)
| Lane | Code ships | Avg duration | Avg files/ship | Notes |
|------|-----------|--------------|----------------|-------|
| 01 triage | 6/7 | ~2-3m | 1-2 fix + 1 restore | Pixi null-guards in rAF ticks; migrations written-but-unapplied |
| 02 perf | 6/7 | ~1-2m | 0-1 | MP CLS + memo passes; no backend (MCP dry) |
| 03 engagement | 5/7 | ~2-3m | 2-4 | flag wiring + PostHog-delta revert (exp-mp-room-join) |
| 04 competitor | 0/7 | ~8-10m | research-only | ideas + Reddit drafts, never code |
| 05 landing | 5/7 | ~2m | 1-4 | mode polish (Gem Hunt, Clue Scramble, Shiritori); 1 research-only |
| 06 seo | 2/7 | ~2m | 1 | rotation; ES education + breadcrumb JSON-LD |
| 07 self-learn | 3/7 | ~3-5m | 1-3 | learnings + loop-improvements rewrites |
| 08 adsense | 2/7 | ~2m | 0-1 | rotation; schema/word-count audits |
| 09 monetization | 7/7 | ~1m | 1-2 | SupporterInterestCard, daily-first funnel |
| 10 dict | 2/7 | ~3m | 2-5 | rotation; word-bank index + Hebrew race fix |
| 11 mode-qa | 6/7 | ~2m | 1-3 | crossword 55→88%; word-tower Released; rc124 07-11 |
| 12 telemetry | 5/7 | ~2-3m | 1-2 | results_viewed 3 modes; game_abandoned SPA gap |

## What's working well
- **Gate false-negatives finally attacked at root** — `c3973c715` caps vitest fork concurrency, the actual cause of 3 salvage nights. Prior weeks only restored dropped code the next morning (wasting a full lane/night); this week we fixed the classifier. If it holds, 4/7-salvage → mostly-full-pass.
- **0 pushed-code reverts, 40+ nights** — salvage drops PRE-push so master never regresses; restores (`6fb8daf47`, `2270e1de8`) round-trip clean.
- **Mode-readiness treadmill repeatable + fast** — crossword +33pts in 4 nights via systematic audit (generation errors → null traps → ClueBar toggle → timer cleanup). ~1 night from release.
- **Backbone lanes 01/02/09/11 at 6-7/7** — Pixi null-guards, memo passes, monetization funnel, mode-QA ship nearly every night.

## Friction points
- **Supabase MCP dead 20 straight nights** — 4 migrations written and stale-backlogged; triage burns effort authoring migrations it cannot apply. Human-blocked on a never-expire PAT. This is the single biggest structural drag.
- **Rotation lanes (04/06/08/10) look like failures in a 7-night window** — 04 shipped 0 code (research-only by design), 06/08/10 fire ~2/7 (scheduler rotation). The success-rate table misleads unless "off-rotation ≠ failure" is stated. Consider tagging scheduled-off nights in the run log so the metric is honest.
- **Lane 04 never ships code** — 7/7 research-only. Its ideas DO convert (both polish votes → lane-05 slices), but as a code lane it's 0. Either reframe it as a pure idea-feeder (no code expectation) or give it a small buildable-slice mandate.
- **game_abandoned instrumented but crater undiagnosed** — 53-56% completion, 262 abandons. We keep WIRING telemetry; nobody has yet queried WHERE the drop happens.

## Concrete improvements (ranked by impact × effort)
1. **Verify the vitest-fork fix converts salvage→full** — lane 07 next 3 nights must read the gate outcome line and log full-pass vs salvage explicitly. Effort: S. Impact: confirms the #2 infra fix actually worked (or surfaces the next bottleneck, rc124 per-suite). File: lane-07 prompt (add "STEP 1b: classify each of last 3 nights' gate as full|reduced|salvage from the report outcome line").
2. **Diagnose the completion crater, stop wiring** — lane 12/03 prompt should pivot: one night querying PostHog `game_abandoned` grouped by mode + last-screen, NOT more results_viewed wiring. Effort: M. Impact: turns 262 abandons/period into a targeted fix. File: lane-12 + lane-03 prompts.
3. **Add a `next_gate_expectation` line to run.sh salvage path** — when the gate drops code, log WHICH check failed (lint|type|test|build) + rc, so lane 07 doesn't have to reverse-engineer it. Effort: S. Impact: removes the nightly "was it code or false-negative?" guessing. File: `scripts/nightly/run.sh`.
4. **Tag rotation-off nights in the report** — emit `lane NN: off-rotation` instead of silence, so the 7-night success-rate isn't read as failure. Effort: S. Impact: honest metrics, less lane-07 misattribution. File: `scripts/nightly/run.sh`.
5. **Lane 05 scope guard** — prompt already says "all 5 locales first"; add a hard "if the slice needs >1 new component, defer to a smaller slice" line, since 07-12 went research-only on over-scope. Effort: S. Impact: fewer research-only landing nights. File: lane-05 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/gate-outcome.sh`** — greps the night's report + run log for the gate outcome (full|reduced|salvage) + which check failed + rc, prints one line. Removes lane-07's nightly manual reverse-engineering (fired 3+ nights this window). Effort: S. Deferred this run (out of time budget); next lane-07 night should author it and reference from the lane-07 prompt STEP 1.

## New product/feature ideas surfaced by this week's data
- **Crossword public launch push** — lane 11 has crossword at 88% (from 55% on 07-12). Source: reports 07-12→07-15. One more QA night unblocks a brand-new released mode + its landing/SEO surface (education angle: "crossword vocabulary practice").
- **Attack MP room-join UX properly** — `exp-mp-room-join-loading-v1` REGRESSED (16 rageclicks vs 1) because it gated UI behind socket latency. Source: 07-12 revert. The underlying pain (users rage-clicking during room join) is REAL — solve it with optimistic UI, not a disabled button.
- **Homepage LCP poster frame** — 393KB animated winner.webp with no poster, TTFB-dominant ~1500ms. Source: lane 02 open watch. A static first-frame poster is a free LCP win on the highest-traffic page.

## Loop changes shipped THIS WEEK
- **`c3973c715` (07-14)** — vitest fork-oversubscription cap: the first infra fix that targets the gate false-negative ROOT rather than restoring dropped code after the fact. Drained 2 migrations into the same commit.
- **`2270e1de8` (07-14)** — restored 07-14 lane code (21 files) the gate false-dropped; **`6fb8daf47`** restored 07-12+13 drops. Salvage→restore round-trips proven clean.
- **`20d410bc9`** — contention-aware timeout + closed 2 lane defer gaps (failed lanes now re-run at backoff instead of silently skipping).
