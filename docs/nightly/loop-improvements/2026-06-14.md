# Loop Self-Review — 2026-06-14

## Run health (last 6 nights, 06-09..06-14)
| Lane | Success rate | Avg duration | Avg files | Notes |
|------|--------------|--------------|-----------|-------|
| 01 triage | 5/6 | short | 1–2 | 06-09 skipped; 6 REVOKEs + 4 index-drops, 0 reverts |
| 02 perf | 6/6 | med | 1–3 | revalidate doctrine + cone-shard OOM fix; INP watches noisy |
| 03 engagement | 6/6 | short | 2–4 | 3 experiments wired, ALL dark — flag-creation is the leak |
| 04 competitor | 6/6 | short | docs | prompt-only, 0 timeouts; ideas + reply drafts |
| 05 landing | 6/6 | long | 2–9 | 06-12 partial (4 langs); word-vault hub + polish all shipped |
| 06 seo | 6/6 | med | 4 | seo-daily; ES scrabble surge + HE daily momentum |
| 07 self-learn | 6/6 | short | 3 | prompt-only, never times out |
| 08 adsense | 5/6 | med | 1–28 | 27-blog ISR batch 06-11; verification meta 06-13 |
| 09 monetization | 4/4 (10-13) | med | 3–5 | ad-UX/upsell; offerwalls dark (no A/B infra) |
| 10 dictionary | 4/4 (10-13) | long | 4–7 | dual-judge pipeline; heavy, OOM-risk on gate |

End-to-end run times: 06-09 4h08m, 06-10 8h48m, 06-12 7h43m, 06-13 8h51m — **gate dominates wall-clock**, not lanes. No exit-124 in lane execution this window; all timeouts were in the GATE harness.

## What's working well
- **Prompt-only lanes (04/06/07) = 18/18 ship, 0 timeouts.** Eliminating MCP round-trips is the single strongest reliability lever — confirmed 3rd consecutive window.
- **Mandatory-Artifact floor never zero** — even on red/salvage nights (06-09/11/12/13) every lane left a gate-clean docs artifact.
- **Security + index-drop autonomy** (lane 01) — 10 reversible DB changes shipped this window, 0 reverts. Lowest-risk highest-trust lane.
- **revalidate>force-dynamic doctrine** — repeated LCP wins (`/es/mp` 4903→1706ms, 27 blog pages) with zero regressions.
- **Cone-size test sharding** (`ef650a29e`) materially reduced exit-144 OOM on the pre-push hook.

## Friction points
- **Gate is the bottleneck, not lanes.** Runs take 4–9h almost entirely in the gate (full lint+type+test+build), and vitest heap OOM wedged it on 06-11. Lanes finish in minutes; the gate's full-suite run is where nights go red.
- **Push-fail stranding (06-11, 06-13).** Final commit landed locally but never reached origin (daemon/remote race). Code "shipped" per the report but absent from remote — silent and recurring.
- **3 dark experiments ≥5 nights.** Lane 03 keeps WIRING but no human creates the PostHog flag, so 94 quickPlay rage-clicks + 83% invite drop + 43% practice drop stay un-tested. The loop's biggest measured-but-idle value leak.
- **Lane 05 over-scope → partials.** 06-12 shipped only 4 locales. TDD + 5 locales + animation + page-wire rarely all fit one budget.
- **Supabase MCP flaky (06-12, 06-13 unavailable).** Lanes 01/02 degrade to PostHog-only; some triage skipped.

## Concrete improvements (ranked by impact × effort)
1. **Auto-append FLAG-NEEDED block to a persistent `docs/nightly/flag-queue.md`** — lane 03 already emits the digest line; have it ALSO upsert into one standing file the human checks weekly, so the 3 dark experiments stop scrolling past in nightly digests. Effort: S. Impact: unblocks 3 experiments targeting 94 rage-clicks + 83%/43% funnel drops. File: `docs/nightly/flag-queue.md` + lane-03 prompt.
2. **Gate: scope the authoritative test run to changed-cone files, not full suite** — the gate re-runs everything (4–9h, OOM-prone) when lanes only touched a handful of files. Reuse the existing cone-size sharder (`ef650a29e`) in the gate, not just pre-push. Effort: M. Impact: cuts gate wall-clock + kills the 06-11-class OOM wedge. File: nightly gate script + `scripts/nightly/lib/`.
3. **Push-verify step: grep origin for committed content, retry rebase-then-push** — after the end-of-run commit, confirm the change is on origin (not just local SHA) and rebase-retry on failure. Effort: S. Impact: closes the 06-11/06-13 silent-strand class. File: nightly runner push step.
4. **Lane 05/09 prompt: enforce "write all 5 locale files FIRST, then component"** — make locale-first an explicit ordered checklist so the ~8-file working-set cap never drops ES/JA again. Effort: S. Impact: ends partial-locale ships (06-12). File: lane-05 + lane-09 prompts.
5. **Lane 02 prompt: add explicit `/he` CLS+LCP diagnosis task** — new 06-14 watch (CLS 0.386, LCP 4764ms) at n=8; prompt lane 02 to re-measure at n≥50 and, if confirmed, inspect RTL font-swap. Effort: S. Impact: closes a POOR-rated route once sample clears floor. File: lane-02 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/flag-queue-upsert.sh`** — takes `<key> <variants> <hypothesis>`, idempotently upserts a row into `docs/nightly/flag-queue.md` (dedup by key). Called from lane 03 each night. Removes the manual "did anyone create the flag yet?" check. Effort: S. (Deferred to a code-capable lane — lane 07 is doc-only tonight and out of budget.)
- **`scripts/nightly/tools/push-verify.sh`** — after commit, `git fetch` + grep origin/master for a sentinel line from the diff; rebase-retry once on miss. Called from runner. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Quick-play "finding a match" status overlay → SHIP IT (un-dark `exp-mp-quickplay-wait-v1`).** 94 rage-clicks/7d on `/es/multiplayer?quickPlay=true` is the single highest-signal surface in the data; the overlay is code-complete and just needs a flag.
- **Invite-arrival status card (un-dark `exp-invite-arrival-clarity-v1`).** 83% invite drop (18→3/7d) — arriving invitees don't understand the state. Card is built. Source: 06-10/06-11 funnel rows.
- **Spanish "scrabble" landing capture.** "scrabble online" +44% impr (4669→6749), "scrabble online español" +1283% WoW — a dedicated ES `/scrabble-online-español`-style landing (or strengthened existing) could convert pos 5.6→top-3. Source: 06-14 GSC rows. (Revenue-positive: ES traffic = ad inventory + school-lead funnel.)
- **Practice retry-CTA (un-dark `exp-practice-wheel-cta-v1`).** 43%/78.6% practice abandon; retry CTA wired 06-14, needs flag. Source: 06-12/06-14 practice funnel.

## Loop changes shipped THIS WEEK
- **`ef650a29e` / `03d6abaed`** — pre-push tests sharded by measured cone size, killing exit-144 OOM on translation/LanguageContext mega-hubs (proposed in prior loop-improvements).
- **Mandatory-Artifact floor + locale-first guidance** continued to hold; no new loop-infra commit landed beyond the sharding fix.
