# Loop Self-Review — 2026-06-01

## Run health (last 7 nights, 05-26..06-01; 05-31 aborted)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 4/7 | ~timeout-prone | ~3 | Ships when MCP+scope OK; 124 on 05-28/29/30 |
| 02 perf | 1/7 | hits 124 most nights | ~4 | Worst lane; dual-MCP + multiplayer = over-scope |
| 03 engagement | 4/7 | mixed | ~10 (27 on 06-01) | Prompt-driven; timed out 05-29..05-31 then recovered |
| 04 competitor | 6/7 | ~fast | ~3 | Most reliable code lane; WebSearch fallback works |
| 05 landing | 3/7 | slow when scratch | ~11 | Ships only with pre-built/scoped code |
| 06 seo | 5/7 | medium | ~5 | Reliable when MCP cooperates; 124 on 05-27/05-30 |
| 07 self-learn | 7/7 | ~10m | 2 | Prompt-only; never times out |
| 08 adsense | 2/6 | timeout-prone | ~1.5 | Copy quality good when it ships |

> **06-01 is split:** 02:00 run timed out 5/6 code lanes; 19:05 evening rerun shipped lanes 02/03/05/06. Recovery driven by Phase-0 scoping + hardened MCP timeouts. Prior learnings mislabeled it a uniform "worst night."

## What's working well
- **Phase 0 intel brief is the single biggest lever proven this week** — the evening 06-01 recovery (4 previously-timing-out lanes shipped) maps directly to front-loaded ranked signals replacing blind MCP fan-out.
- **Lightweight, MCP-light lanes (07/04/06) carry the loop** — 7/7, 6/7, 5/7. The reliability gradient is almost perfectly explained by MCP-dependency + scope size, not by skill or prompt quality.
- **Docs-only salvage + Mandatory-Minimum-Artifact** keep no-ship nights from being total losses — every lane leaves a gate-clean artifact in `docs/`.
- **Founder-directive fast path** turned a Telegram tap (sealed-bid) into shipped+tested code inside 24h, twice.

## Friction points
- **Cumulative MCP call latency + over-scope blow the 12-min budget** (lanes 01/02/03/08). Note the sharpest finding: 4 of the 06-01 timed-out lanes made ZERO MCP calls — so scope, not MCP availability, was the killer there. The loop keeps blaming "MCP hangs" when the real failure is lanes attempting too much.
- **Lane 02 is structurally mis-scoped** — it's asked to fix multiplayer LCP (a large SSR-skeleton change) on a 12-min budget. It can't, so it times out 6/7 nights. The work is real; the lane sizing is wrong.
- **Lane 05 "new page from scratch" never fits the budget** — 3/7, all wins were pre-built or tightly scoped.
- **Search collector dead 7+ nights** — pure budget waste in Phase 0.
- **Reddit JSON API blocked 6+ nights** — lane 04 degrades to WebSearch; native drafts at 0.
- **Dirty-HEAD preflight abort (05-31)** wasted 2 retry cycles.

## Concrete improvements (ranked by impact × effort)
1. **Hard scope-cap lane 02 to one metric per night** — rewrite the lane-02 prompt: "Pick the SINGLE highest-p75 page from the brief, make ONE LCP/CLS fix (opacity-removal, image priority, font-display), ship it. Do NOT attempt multiplayer SSR skeleton — that is a human-queue item." Effort: S. Impact: lifts lane 02 from 14% toward the 04/06 tier; chips at the 8448ms multiplayer LCP over time. File: lane-02 prompt in `scripts/nightly/headless.sh`.
2. **Add MAX_MCP_CALLS=3 budget guard to lanes 01/02/03/08** — after 3 live MCP calls, force REST-helper or brief-only mode. Effort: M. Impact: caps the cumulative-latency timeout that hit 21+ lane-runs. File: `scripts/nightly/headless.sh` per-lane preamble + a small `scripts/nightly/tools/mcp-budget-guard.sh`.
3. **Remove the dead search collector from Phase 0** — 0 signals 7+ nights. Effort: S. Impact: reclaims preflight wall-clock for healthy collectors. File: Phase-0 collector list in `scripts/nightly/headless.sh` / `scripts/nightly/lib/`.
4. **Dirty-HEAD preflight handler** — on unpushed founder commits, auto-stash or branch instead of aborting. Effort: M. Impact: prevents 05-31-style 2-retry waste. File: preflight in `scripts/nightly/headless.sh`.
5. **Lane 05 prompt: forbid from-scratch pages unless a founder directive supplies code** — default lane-05 to title/meta/CTA/hero-copy scoped edits. Effort: S. Impact: lifts 43% ship rate; stops budget-burn on impossible scope. File: lane-05 prompt.
6. **Split `leaderboard/PageClient.tsx` (600+ lines)** — over the 500-line cap, blocks lane 03 wiring. Queue as a human/lane-01 refactor. Effort: M. Impact: unblocks engagement-CTA wiring. File: `fe-next/app/[locale]/leaderboard/PageClient.tsx`.

## New helper scripts to ship
- **`scripts/nightly/tools/mcp-budget-guard.sh`** — exports a per-lane MCP call counter; lanes source it and bail to REST/brief mode after N calls. Removes the recurring "lane hangs on call 4+" pattern that fired across 01/02/03/08 for 4+ nights. Called from each MCP-dependent lane preamble. Effort: M. (Proposed — not shipped this lane to stay within budget; spec only.)

## New product/feature ideas surfaced by this week's data
- **Multiplayer SSR skeleton loader** — source: lane 02, `/en/multiplayer` p75 LCP 8448ms (+167%), INP 584ms. Render a static board skeleton server-side so LCP fires before socket connect instead of after. Directly attacks the loop's #1 escalating perf regression.
- **Fading Grid Sprint (Blast modifier)** — source: Telegram `idea:build`+`polish:try:blast:f35b4c60`. Founder tapped it twice. Tiles decay on the countdown; validated against Wordrace tile-decay. Highest-signal unbuilt idea.
- **Sealed-bid share virality** — source: `polish:try:sealed-bid` x2. Mechanic already shipped admin-gated; the repeated taps say "iterate on this." Add the poker-reveal share card to the public funnel test.
- **Engagement: day-2 retry is 21% vs 30% target** — source: lane 03 funnel. The `exp-leaderboard-play-cta-v1` / `exp-daily-cta-on-results-v1` experiments target exactly this; ensure they reach stat-sig before retiring the 72d-old `show-signup-after-first-win` flag.

## Loop changes shipped THIS WEEK
- `a4eb5361a` fix(nightly): wire lane 09, converge re-gate salvage, scope-discipline lane budget
- `7b2471aef` chore(nightly): fix gate baseline-poison salvage + reddit snapshot path
- `c9e3eeabe` chore(nightly): autonomous improvement loop 2026-06-01
- (Phase-0 brief + hardened MCP timeouts — visible in 06-01 evening recovery, the main proof point this week.)
