# Loop Self-Review — 2026-06-01

## Run health (last 7 nights)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 2/6 (33%) | ~20m | 2 | Ships only when MCP healthy; 4 consecutive timeouts |
| 02 perf | 1/6 (17%) | ~18m | 2 | Dual MCP dep (supabase+posthog) kills budget |
| 03 engagement | 3/6 (50%) | ~30m | 6 | Shipped first 3 nights, then 3 consecutive timeouts |
| 04 competitor | 6/6 (100%) | ~15m | 3 | Zero MCP dep; WebSearch-only; bulletproof |
| 05 landing | 1/6 (17%) | ~27m | 6 | Only ships with founder directive (pre-built code) |
| 06 seo | 4/6 (67%) | ~22m | 4 | Shipped 4 straight then 2 consecutive timeouts |
| 07 self-learn | 5/5 (100%) | ~7m | 3 | Prompt-only, no MCP; fastest lane |
| 08 adsense | 4/5 (80%) | ~8m | 2 | Lightweight content edits; reliable |

Fleet: 25/45 shipped (56%), 20 timed out (44%). Trend: **DECLINING** (06-01 = 1/6 code lanes shipped).

## What's working well
- Lightweight lanes (04/07/08) maintain 80-100% ship rate by avoiding MCP entirely
- KEEP_TIMEOUT_PARTIALS prevents total-loss nights — partial files preserved and gate-validated across 4 nights with zero poisoning
- Mandatory-Minimum-Artifact ensures every lane leaves a paper trail even on timeout
- Autonomy matrix shipped 7+ autonomous fixes (search_path, REVOKE, pathTrace null, sync_coins qualify, RLS initplan, WASM build, security-invoker) with zero reverts
- Phase 0 intel brief provides structured signals; recovered sentry+supabase after 05-30 outage
- Lane 04 is the most productive lane overall: 6/6 ship rate, 5 ideas per night, zero infrastructure dependency

## Friction points
- **MCP cumulative timeout is systemic and worsening**: 20/45 runs (44%, up from 41%) time out. Individual 60s caps work but lanes making 5+ sequential MCP calls exceed their budget. Lanes 01/02/03 are structurally MCP-dependent with no fallback path.
- **Lane 02 architectural mismatch**: needs both Supabase execute_sql AND PostHog queries — any single hang kills the run. 17% ship rate proves the current design is unviable. The `/en/multiplayer` LCP regression (+167%) is going UNADDRESSED because the lane that would fix it keeps timing out.
- **Lane 05 scope mismatch**: prompt asks for "new landing page from scratch" which exceeds any lane's time budget. 17% rate; only ships with pre-built founder code. The prompt itself guarantees failure.
- **Lane 03 degrading without prompt change**: 3 consecutive ships then 3 consecutive timeouts. Same prompt both periods. Root cause is MCP environment, not prompt quality.
- **Lane 06 new regression**: was 4/4, now 0/2. Coincides with MCP instability on 05-30/06-01. Previously the most reliable code-shipping lane after 04.
- **05-31 preflight abort**: HEAD had unpushed founder commits → 2 retry attempts wasted. No graceful dirty-HEAD handling.
- **Reddit permanently blocked**: 6 consecutive nights. Spec exists (reddit-oauth-setup.md) but unimplemented. Lane 04's best content source is offline.
- **Search collector dead weight**: 0 signals for 7+ nights. Wastes Phase 0 budget on a broken collector every single run.
- **Critical perf regression unaddressed**: `/en/multiplayer` LCP 8448ms, CLS 0.510, INP 584ms — all worsening. Lane 02 (perf) can't ship. Lane 01 (triage) can't ship. No lane currently able to address the product's worst regression.

## Concrete improvements (ranked by impact x effort)
1. **Remove search collector from Phase 0** — delete the dead collector (0 signals for 7+ nights). Effort: S. Impact: saves ~30s Phase 0 budget, cleaner briefs, removes noise. File(s): Phase 0 collector script.
2. **MAX_MCP_CALLS=3 budget cap per lane** — after 3 MCP calls, switch to REST fallbacks or abort gracefully. Prevents cumulative timeout death spiral. Effort: S. Impact: timeout rate 44%→<30%. File(s): lane runner wrapper.
3. **Lane 02 → REST-only mode** — replace Supabase execute_sql with direct REST queries, PostHog via existing REST helper. Eliminate MCP dependency entirely. Effort: M. Impact: 17%→60%+ ship rate; unblocks the multiplayer LCP regression. File(s): lane-02 prompt + `scripts/nightly/tools/supabase-rest.sh`.
4. **Lane 05 scope to "improve existing page"** — stop asking for new-page-from-scratch. Rewrite prompt to iterate on existing pages (CTA placement, copy, above-fold layout). Effort: S. Impact: 17%→50%+. File(s): lane-05 prompt.
5. **Dirty-HEAD preflight handler** — when HEAD has unpushed non-docs commits, stash or create a temp branch instead of aborting. Effort: M. Impact: prevents 05-31-style lost nights. File(s): `scripts/nightly/headless.sh` preflight section.
6. **Emergency perf lane** — given lane 02 can't ship, create a one-off perf-fix task in lane 01 or 03 prompt specifically for the `/en/multiplayer` regression. Effort: S. Impact: addresses the worst active regression. File(s): lane-01 or lane-03 prompt (temporary directive).

## New helper scripts to ship (you may create these directly)
- **perf-regression-alert.sh** — reads `perf-baseline.json`, diffs against prior night's snapshot, surfaces >50% regressions as HIGH-priority signals for Phase 0 brief. Removes manual baseline comparison. Effort: S. Called from: Phase 0 collector.
- **mcp-call-counter.sh** — wrapper that counts MCP tool invocations per lane, logs to `~/logs/lexi-nightly/mcp-calls-<date>.log`, enables data-driven budget tuning. Effort: S. Called from: lane runner.

## New product/feature ideas surfaced by this week's data
- **Fading Grid Sprint** (lane 04, 05-29; founder `idea:build` + `polish:try`) — tiles disappear on countdown; score words before board goes dark. Blast V2 modifier. Small effort. Founder-validated interest. Strongest signal.
- **Hidden Word Race** (lane 04, 06-01) — all players race to guess same hidden word simultaneously. New MP engagement mechanic. Medium effort. Unique differentiator vs competitors.
- **Shiritori Start-Letter Beacon** (lane 04, 06-01) — 2s center-screen pulse showing required starting letter. Removes the #1 user friction in shiritori. Small effort. High UX impact.
- **Survival Rounds** (lane 04, 05-26; founder `idea:build`) — last-word-standing elimination MP mode. Medium effort. 6 nights waiting for implementation.

## Loop changes shipped THIS WEEK
- **KEEP_TIMEOUT_PARTIALS promoted to stable** — 4th night with zero gate-poisoning. Partial files now standard recovery path.
- **Phase 0 intel brief operational** — 4th night. Sentry+supabase recovered after 05-30 outage.
- **No new scripts shipped this week.** Prior proposals (MAX_MCP_CALLS cap, REST-only lane 02, search collector removal) remain unimplemented for 2+ nights. This is the top friction: meta-improvements are proposed but never executed because no lane owns implementation.
