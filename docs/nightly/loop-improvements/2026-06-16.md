# Loop Self-Review — 2026-06-16

## Run health (last 7 nights, 06-10..06-16)
| Lane | Ship rate | Avg duration* | Avg files | Notes |
|------|-----------|---------------|-----------|-------|
| 01 triage | 7/7 | ~17m | 2–4 | RLS REVOKE + index-drop autonomy; 0 reverts |
| 02 perf | 7/7 | ~43m | 2–4 | slow (Explore agent /he LCP root-cause); revalidate doctrine |
| 03 engagement | 6/7 | ~37m | 4–9 | high files, but 4 experiments DARK (no flags) |
| 04 competitor | 7/7 | ~10m | 3 | fastest, prompt-only, 0 timeouts |
| 05 landing | 6/7 | ~40m | 1–9 | polish + word-vault; 1 partial (ja/es cap 06-12) |
| 06 seo | 7/7 | ~23m | 3–7 | seo-daily skill, 0 timeouts |
| 07 self-learn | 7/7 | ~20m | 2 | prompt-only, never wedges |
| 08 adsense | 6/6 | n/a | 1–28 | 27-blog-ISR night blew gate cap |
| 09 monetization | 6/6 | n/a | 1–5 | thin output most nights |
| 10 dictionary | 6/6 | slowest | 4–9 | progressive slowdown → gate-timeout contributor |
*durations from run-20260616 lane boundaries; budgets are 600–900s idle-watchdog, finalize@+24m.

## What's working well
- **Mandatory-Minimum-Artifact floor** — 7/7 nights non-zero even through 4 docs-only salvages. The single best resilience lever.
- **Low/zero-MCP lanes (04/06/07) never time out** — 21/21 ships. MCP round-trips are the dominant timeout cause.
- **Autonomous DB doctrine (lane 01)** — REVOKE + index-drop with advisor+grep gate: 15 migrations this window, 0 reverts.
- **ISR revalidate doctrine (lane 02/08)** — /es/multiplayer LCP −28% cumulative, measurable, zero regressions.
- **Single end-of-run commit** — one rollback target; 30+ nights stable.

## Friction points
- **Gate runs the FULL vitest suite** → 4/7 nights wedged on pool-timeouts (281–358/night) + chronic-red suites (PracticeWheelSandbox, wordHandler.blast*, blastModeManager.thaw). rc=124 → code dropped, docs-only salvage. This is the #1 value leak: real code authored, gate-killed.
- **Stranded-push auto-recovery is broken** — `refs/nightly-pending/2026-06-13` has failed cherry-pick+push for 4 consecutive nights. Blind retry can't resolve a conflict against a moved origin. Affects: every night's preflight wastes a step + 06-13's real work never reached remote.
- **4 dark experiments, 0 flags, ≥7 nights** — lane 03 keeps wiring + instrumenting but the PostHog-flag handoff is human-blocked and never escalated forcefully. 83%/94-rage/43%/42% funnel leaks remain unmeasured.
- **Lane 02 slow (~43m)** — spends budget on Explore root-cause agents for /he. Useful but eats the window.
- **Supabase MCP down 4 of last 5 nights** — lane 01/02 migrations written-but-not-applied; silent debt accrues.
- **Blog-ISR batch + lane code same night** exceeds gate cap → code dropped (08).

## Concrete improvements (ranked by impact × effort)
1. **Scope gate tests to the changed cone, not full repo** — derive the test set from `git diff --name-only` since last push and run only those + their importers (the pre-push shard logic already exists per learnings; reuse it in the GATE). Effort: M. Impact: kills the 4/7 docs-only-salvage mode → recovers ~4 nights/wk of real code ships. File(s): nightly gate script (`scripts/nightly/*gate*`).
2. **Fix stranded-push recovery: detect-conflict → park, don't blind-retry** — if cherry-pick of `refs/nightly-pending/*` conflicts, stop retrying, write a one-line Telegram "STRANDED REF needs manual rebase: <ref>" and tag the ref so it's not re-attempted nightly. Effort: S. Impact: stops 4-night (and growing) silent loss; surfaces to human once. File(s): preflight in nightly runner.
3. **Lane 03 prompt: make "FLAG NEEDED" a Telegram digest line, not just a report block** — the report block is invisible to the human at decision time. Push the 4 flag specs into the manager-summary Telegram message every night until flags exist. Effort: S. Impact: unblocks 4 experiments measuring 4 top funnel leaks. File(s): lane-03 prompt + summary digest assembler.
4. **Split blog-ISR batches from feature code (lane 08 prompt)** — cap lane 08 at ≤8 blog files/night OR run blog-ISR on its own night with no other lane code. Effort: S. Impact: stops gate-cap code drops. File(s): lane-08 prompt.
5. **Lane 02 prompt: time-box root-cause Explore to one agent, then ship the smallest fix** — at ~43m lane 02 is the second-slowest; cap the diagnosis fan-out. Effort: S. Impact: −15m/night, fewer near-deadline edits. File(s): lane-02 prompt.
6. **Queue Supabase migrations to a `pending-migrations/` apply-list when MCP is down** — single file the human applies in one batch; stops per-night silent debt. Effort: S. Impact: closes the 4/5-night MCP gap. File(s): lane-01/02 prompts + `docs/nightly/pending-migrations.md`.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/changed-cone-tests.sh`** — `git diff --name-only <lastpush>..HEAD` → map to vitest files + direct importers → emit the scoped test glob the gate runs. Removes the full-suite wedge (fires every night). Effort: M. *(Deferred this run — needs gate-script integration + bash -n verify; queued #1 above. Did not create tonight to stay inside the 15m budget and avoid a half-written script poisoning the gate.)*

## New product/feature ideas surfaced by this week's data
- **Invite arrival status-card (ship the variant, not just the flag)** — invite_redirect_fired→invite_consumed is an 83% drop (18→3 users/7d), the largest measured funnel leak. Even un-A/B'd, a clear "Joining <host>'s game…" arrival card likely recovers signups. Source: lane 03 funnel, 06-10..06-16.
- **Spanish scrabble landing push** — `scrabble en español` +1283% WoW and `scrabble online` 7.6k impr at pos 5.5 but 0.3% CTR. A dedicated ES /scrabble-online landing with strong title/meta could capture a structural brand-adjacent gap. Source: lane 06 SEO, 06-16.
- **Quit-confirm on mid-game abandon** — game_started→game_completed drops 42% (185 silent quits/7d). A lightweight "Leave game? You'll lose your streak" confirm (already wired as exp-game-abandon-confirm-v1) addresses the largest in-game leak. Source: lane 03, 06-15.
- **Practice retry-CTA on /practice/wheelRush** — 43% abandon (47 start→27 complete); a one-tap "Try again" at game-over (wired as exp-practice-wheel-cta-v1) is a cheap plug. Source: lane 03, 06-14.

## Loop changes shipped THIS WEEK
- `e4f5a8abf` fix(nightly): glyph-agnostic MCP connected check + await supabase — fixed the false-negative MCP preflight WARN from prior loop-improvements.
- `d26deeb2a` chore(nightly): move loop schedule to 01:00 local — schedule change from prior week's review.
- `97adb9228` / `aeba9f680` autonomous improvement loop commits (06-14/06-15) — loop machinery upkeep.
- Pre-push test sharding by cone size (`ef650a29e`, `03d6abaed`, prior window) — landed and held; now the template for gate-test scoping (#1 above).
