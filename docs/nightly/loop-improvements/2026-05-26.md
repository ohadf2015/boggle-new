# Loop Self-Review — 2026-05-26

## Run health (last 6 nights: 05-21 to 05-26)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 3/6 (50%) | ~6m shipped, 20m+ timeout | 2 | MCP_TOOL_TIMEOUT fix showing first signal (05-26 shipped) |
| 02 perf | 2/6 (33%) | ~8m shipped, 39m timeout | 2 | Supabase execute_sql suspected hang vector |
| 03 engagement | 2/6 (33%) | ~7m shipped, 20m+ timeout | 4 | Both successes = high-value (funnel + experiment) |
| 04 competitor | 6/6 (100%) | ~5m | 2 | No MCP dependency, reddit-fetch.sh reliable |
| 05 landing | 0/6 (0%) | full budget burn | 0 | Zero output in 6 nights. Worst lane by far |
| 06 seo | 6/6 (100%) | ~6m | 4 | seo-daily skill, no heavy MCP |
| 07 self-learn | 4/6 (67%) | ~8m | 2 | Two timeouts when MCP servers hung at init |
| 08 adsense | 3/5 (60%) | ~4m | 1 | Lightweight but caught in 05-25 mass-timeout |

## What's working well
- Lightweight lanes (04, 06) at 100% prove the prompt+script approach works when MCP isn't involved
- MCP_TOOL_TIMEOUT (shipped 05-25) produced first measurable improvement: lane 01 shipped 05-26 after 2 consecutive timeouts
- Docs-only salvage ensures the run is never a total loss (5/7 runs shipped at least docs)
- Per-lane scoped revert = zero collateral damage in 6 nights of concurrent founder work
- Anti-repetition idea ledger immediately improved lane 04 output quality (no repeated pitches)
- Stream-json observability means next timeout produces ACTIONABLE data, not silence

## Friction points
- **Lane 05 burns ~$2.50/night for zero output** (opus/1500s, 6 consecutive failures). Pure waste.
- **20 MCP servers spawn per lane** but most lanes use 2-3. Startup cost + hang surface area is proportional to server count.
- **Stream-timeline data exists but hasn't been READ yet** — proposed 05-25 "grep for last tool before exit-124" hasn't happened. We're one grep away from a targeted fix.
- **Telegram feedback loop is decorative** — 7 taps in 7 nights, 5 are smoke-tests. The loop exists but doesn't generate signal.
- **Lane 03 PostHog REST pre-fetch** proposed 3 nights running, never shipped. Would remove the #1 hang vector for engagement.
- **Sentry MCP returns HTTP 500 intermittently** — lane 01 worked around it (search_events), but this isn't codified.

## Concrete improvements (ranked by impact x effort)
1. **Kill lane 05 or downgrade to sonnet/600s** — 0/6 ships = no information loss from downgrade. Save ~$2.50/night. If sonnet ships 1/3, that's infinite improvement over 0/6. Effort: S. Impact: cost savings + potentially first landing variant in 2 weeks. File: `scripts/nightly/run.sh` (lane 05 model/timeout config).
2. **Scope nightly MCP config to 4 servers** — create `scripts/nightly/mcp-nightly.json` with only sentry+supabase+posthog+gsc. Pass via `--mcp-config`. Fewer servers = faster init + smaller hang surface. Effort: S. Impact: -5s init, fewer hang vectors. File: `scripts/nightly/mcp-nightly.json` + `run.sh`.
3. **Ship lane 03 PostHog REST pre-fetch** — fetch 7d funnel JSON via `lib/posthog-rest.sh` BEFORE Claude dispatch. Inject as context file. Removes PostHog MCP as hang vector for engagement lane. Effort: S. Impact: lane 03 could go from 33%→66%+. File: `scripts/nightly/tools/prefetch-funnel.sh`.
4. **Grep stream-timeline for hung tools** — one-time diagnostic: `grep -B1 'exit_code.*124' stream-*.ndjson | grep '>'`. Names the exact MCP tool that hangs. Then add that tool to a skip-list or raise its specific timeout. Effort: S. Impact: converts inference→proof for the remaining ~30% timeout rate.
5. **Codify Sentry search_events fallback** — lane 01 manually worked around `search_issues` HTTP 500 on 05-26. Script a `sentry-fallback.sh` that tries search_issues, on 500 falls back to search_events. Effort: S. Impact: lane 01 resilience. File: `scripts/nightly/tools/sentry-fallback.sh`.

## New helper scripts to ship
- **prefetch-funnel.sh** — calls PostHog REST API for 7d funnel (pageview→game_started→game_completed→signup), writes JSON to `/tmp/nightly-funnel.json`. Lane 03 prompt injects this file instead of calling PostHog MCP. Effort: S. Called from: `run.sh` before lane 03 dispatch.
- **mcp-nightly.json** — minimal MCP server config (sentry, supabase, posthog, gsc-only). Reduces spawn from 20→4 servers. Called from: `run.sh` via `--mcp-config`. Effort: S.

## New product/feature ideas surfaced by this week's data
- **Daily Chain for word-alchemy** — Wordle-style daily-lock mechanic (one puzzle/UTC day + streak counter). Evidence: lane 04 research (r/wordgames "daily routine" thread, 44 comments); zero server cost (puzzles already inline). Most-cited return-hook pattern.
- **Replay CTA experiment (wired, awaiting activation)** — `exp-results-replay-cta-v1` instrumented 05-26. `game_completed→game_started` replay funnel was 100% dark until now. Evidence: lane 03 funnel analysis shows game_completed→signup at 10% 7d — replay may be a better intermediate conversion.
- **Survival Rounds MP mode** — elimination every 30s, last standing wins. Evidence: lane 04 found Spell Royale (r/wordgames this week) validating elimination-wave in word games. Zero new socket infra (reuses existing room/round system).
- **Shareable emoji result card** — r/wordgames demand confirmed 4 nights (05-22 to 05-25). Effort: S. Still un-built despite repeated surfacing. Founder passed on hash `41b1c6b6` — may need reframing as "mode-specific" rather than generic.

## Loop changes shipped THIS WEEK (05-21 to 05-26)
- `2a08d07844` — MCP_TOOL_TIMEOUT/MCP_TIMEOUT per-call watchdog (first positive signal 05-26)
- `6cc98a3d7` — stream-json tool timeline for hung-call attribution
- `3154c643a` — anti-repetition idea ledger (lane 04 dedup)
- `d6fd55f1e` — Telegram card loop for unpublished mode ideas
- `4b59a9a35` — file caps removed (zero cap-reverts since)
- `311af3d19` — Reddit curl + UA helper
- `3da3f4407` — MCP outage graceful degrade
- `c79e116f5` — isolated worktree gate
