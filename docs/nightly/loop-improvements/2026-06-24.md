# Loop Self-Review — 2026-06-24

## Run health (last 6 nights, 2026-06-19..24)
| Lane | Success rate | Avg files | Notes |
|------|--------------|-----------|-------|
| 01 triage | 4/6 | ~1-2 | reverted 06-22 (exit 75 usage-cascade); idempotent migrations + REVOKE otherwise |
| 02 perf | 6/6 | ~2-3 | workhorse, 0 reverts; policy consolidation + FK indexes + LCP tracking |
| 03 engagement | 6/6 | ~3-5 | wired 4 dark exps live, 0 reverts |
| 04 competitor | 6/6 | docs-only | ~4-5 ideas + reply drafts nightly, never times out |
| 05 landing/polish | 6/6 | ~4-6 | alchemy/crossword/sealed-bid TDD polish, 0 reverts |
| 06 seo | 6/6 | ~2-4 | ES/SV/HE keyword + native review, 0 reverts |
| 07 self-learn | 6/6 | 2 | prompt-only, ships every run |
| 08 adsense | 6/6 | ~2-3 | blog ISR + JSON-LD + E-E-A-T, 0 gate fails |
| 09 monetization | 4/6 | ~2 | partial nights |
| 10 dictionary | 4/6 | ~1 | partial nights |
| 11 mode-qa | 1/6 | ~2 | NEW 06-24; word-tower 28% first audit |
| 12 telemetry | 1/6 | docs-only | NEW 06-24; registry audit research-only |

## What's working well
- **6 lanes at 6/6, 0 reverts** (02/03/04/05/06/08) — the core loop is healthy and broad. Prompt-only + low-MCP lanes (04/07) never time out.
- **Infra self-heal landed and proved out** — `d91b5fa76` (off-master recover) + `8c705ba2c` (MCP boot retry) contained the 06-22 usage-cascade to 2 lanes instead of all. Self-heal beats silent abort.
- **Mandatory-Minimum-Artifact floor held 6/6** — every degraded/reverted night still shipped its docs artifact. Floor never zero.
- **Idempotent-migration doctrine** (lane 01/02) — `IF NOT EXISTS` + zero-callsite REVOKE ships autonomously, reverts cleanly. Two stuck migrations finally landed (06-23 is_catchup, 06-24 REVOKE).

## Friction points
- **Baseline-red tests poison the gate (06-23)** — DrillWordLimits, PracticeWheelSandbox, WordTowerCrane pre-fail on CLEAN master. A lane's good code was dropped + backed up to `salvaged-code-*` for unrelated red. This is the #1 silent quality leak: lanes lose shipped work to noise. Affects all code lanes.
- **No lane-start stagger vs shared usage window (06-22)** — lane 01 hit usage-limit backoff (120s > deadline) → exit 75. Shared 1M-token Sonnet window has no concurrency cap or stagger. Affects whichever lane runs when the quota drains.
- **MCP token drift (06-21/22)** — Supabase + Sentry MCP failed connect 3/3; lane 01 degraded to REST/Sentry-fallback. Recovered 06-23 but no automated refresh. Sentry MCP write-scope 403 still blocks manual issue resolves.
- **New lanes 11/12 ran 1/6** — launched 06-24 only; can't judge reliability yet. Watch ramp.

## Concrete improvements (ranked by impact × effort)
1. **Scope gate tests to changed files OR quarantine known-red suites** — the gate should not drop a lane's clean code for pre-existing red. Add a `KNOWN_RED_SUITES` skip-list (DrillWordLimits, PracticeWheelSandbox, WordTowerCrane) to the gate runner, or run `vitest related <changed>`. Effort: M. Impact: stops silent loss of shipped code (recovered 06-23 the hard way). File(s): nightly gate/run.sh test step.
2. **Triage the 3 baseline-red suites (lane 01 task)** — root-cause + fix or delete DrillWordLimits/PracticeWheelSandbox/WordTowerCrane so master is green. Effort: S-M. Impact: removes the gate-poison source entirely. File(s): the 3 `__tests__` files.
3. **Lane-start stagger + concurrency cap** — stagger lane launches by N seconds and cap parallel Sonnet lanes so one usage-limit backoff can't exit-75 the deadline. Effort: M. Impact: kills the 06-22 cascade class. File(s): scripts/nightly/run.sh scheduler.
4. **Escalate `/es/multiplayer` LCP +53% to a real perf lane task** — 3.1→5.9s is a structural floor (4× dynamic imports, socket-gated), not a single-reading blip. Give lane 02 an explicit Chrome-profiler subtask next run. Effort: M. Impact: top perf regression, ties to founder SPEED priority. File(s): app/[locale]/multiplayer route + lane 02 prompt.
5. **Auto-refresh Supabase MCP token preflight** — swap dead `SUPABASE_ACCESS_TOKEN` from the never-expire PAT automatically when probe fails, instead of WARN+degrade. Effort: S. Impact: removes lane-01 degradation nights. File(s): preflight probe in run.sh.

## New helper scripts to ship
- **scripts/nightly/tools/gate-test-scope.sh** — wraps the gate test step: runs only suites touching changed files + skips `KNOWN_RED_SUITES`, logs what it skipped (no silent truncation). Removes the 06-23 silent-code-loss class. Called from run.sh gate. Effort: M. (Defer build to a code lane — lane 07 is prompt-only this run; flagging it here as the actionable backlog item.)

## New product/feature ideas surfaced by this week's data
- **Fix brain-drill funnel telemetry → then A/B a drill onboarding** — lane 12 found brain-drill has `game_completed` but NO `game_started`, so its funnel is unmeasurable. Source: lane 12 registry audit 06-24. Cheap fix unlocks a measurable retention surface.
- **MP cold-start LCP is a conversion tax** — `/es/multiplayer` 5.9s LCP (lane 02 06-24) means Spanish MP players wait 6s before play. A lightweight pre-socket skeleton + deferred dynamic imports could lift MP entry conversion. Source: lane 02 LCP row.
- **Lean into the "scrabble/alfapet online" cluster** — ES "scrabble online" 10k+ impr at pos 5.2 with 0.34% CTR vs 6% potential (lane 06 06-23). A dedicated ES Scrabble-alternative landing + meta rewrite is a high-impression, low-CTR gap worth a focused lane-05/06 page.

## Loop changes shipped THIS WEEK
- `9f034ee0b` mode-readiness QA lane (11) + handoff ledger + readiness cards — shipped, ran first 06-24.
- `6471d489d` lane 12 telemetry coverage-health + audit — shipped, ran first 06-24.
- `49735d165` standing founder-priorities block + education/perf/autonomy prompt focus — shipped, now in all-lane preamble.
- `e9c75581b` cutoff-guard + stall alert (prior-week proposal) — shipped; helped contain 06-22 cascade.
