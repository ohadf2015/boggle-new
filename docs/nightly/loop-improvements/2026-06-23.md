# Loop Self-Review — 2026-06-23

## Run health (last 6 nightly reports: 06-18..06-23)
| Lane | Success | Avg dur | Avg files | Notes |
|------|---------|---------|-----------|-------|
| 01 triage | 5/6 | 20–30m | 2–3 | REVOKE + index drops reliable; 06-22 gate revert (flake, not code); MCP-blocked some nights |
| 02 perf | 6/6 | 15–25m | 3–6 | most reliable shipper, 0 reverts; /he + /es/mp LCP still open |
| 03 engagement | 5/6 | 20–30m | 1–9 | 3 flags wired; deactivations blocked on >500L refactor |
| 04 competitor | 6/6 | 10–15m | 3 | docs-only, never times out; jq parse errors on some RSS feeds |
| 05 landing | 4/6 | 10–20m | 3–8 | mode polish 0 reverts; 2 self-skips |
| 06 seo | 6/6 | 10–20m | 3–20 | ES/SV/HE wins; 06-20 timeout, 06-22 revert |
| 07 self-learn | 6/6 | 20–30m | 3–4 | prompt-only; 06-20 timeout in cascade |
| 08 adsense | 6/6 | 15–25m | 2–5 | 0 gate failures, E-E-A-T cleared all 6 |
| 09 monetization | 5/6 | 10–15m | 1–3 | PostHog flag deactivations; 06-20 idle-killed |
| 10 dictionary | 5/6 | 10–15m | 5–7 | 06-20 idle-killed |

## What's working well
- **Two rock-solid code shippers now** — 02 (perf) + 08 (adsense), both 6/6, 0 reverts. Reliability is no longer a single-lane story.
- **Infra self-heal pattern delivering** — `d91b5fa76` (off-master preflight) + `8c705ba2c` (MCP boot-probe retry) closed two silent-abort modes; `c920a05de` landed a long-stuck migration. Self-heal-then-alert beats silent-abort.
- **Mandatory-Minimum-Artifact floor** — even 06-20's 5-lane cascade and 06-22's gate revert left durable artifacts. No zero nights.
- **Build removed from gate verify** — typecheck+build-only re-gate salvaged code on both 06-18 and 06-22 when the full suite wedged.

## Friction points
- **Shared-usage cascade (06-20)** — 5 lanes (03/06/07/09/10) idle-killed (exit 124) when the shared Claude quota drained mid-run. No lane-start stagger or concurrency cap exists. Single biggest reliability hole.
- **Full-suite gate still flakes/wedges** — failed 06-18 AND 06-22 (not "then clean"). Each failure costs a revert + build-only re-gate. The full suite is unreliable as an authoritative gate.
- **Supabase MCP token drift** — WARN'd preflight 06-21/06-22 despite the never-expire PAT; recovered 06-23. Still costs lanes 01/06 their MCP path on bad nights → REST fallback only.
- **Sentry MCP 403 (write scope)** — blocks autonomous Sentry resolution (1NB 15d open, 1CW, 1KQ). Lane 01 can read but not act.
- **>500-line PageClient files block flag wiring** — `leaderboard/PageClient.tsx` (519L) has now deactivated 2 experiments. Refactor debt directly costs experiment throughput.

## Concrete improvements (ranked by impact × effort)
1. **Lane-start stagger + concurrency cap** — sequence lane launches with a 60–90s offset and cap concurrent lanes (e.g. 3) so a draining shared quota degrades gracefully instead of idle-killing 5 lanes. Effort: M. Impact: eliminates the 06-20-class cascade (would have saved 5 lanes). File(s): `scripts/nightly/run.sh` / `headless.sh`.
2. **Preflight credential probe + Telegram alert** — before lanes start, fire one cheap Supabase REST call (`/v1/projects` or `select 1`); on 401/timeout, alert + auto-swap to REST-fallback mode for all lanes instead of each lane discovering it mid-run. Effort: S. Impact: turns 2 WARN nights of lost MCP into a clean fallback. File(s): `scripts/nightly/preflight.sh`.
3. **Split the gate: typecheck+build authoritative, full-suite advisory** — full suite failed 06-18 + 06-22 from flakes, not lane code. Make typecheck+build the ship gate and run the full suite as an advisory signal (alert on fail, don't revert). Effort: M. Impact: stops flake-driven reverts of clean code (2 nights this window). File(s): gate logic in `headless.sh`.
4. **Lane 03 standing chore: chip away one >500L PageClient/night** — give lane 03 a STEP-0 to extract one sub-component from `leaderboard/PageClient.tsx` (519→<500) so blocked experiments (`exp-leaderboard-play-cta-v1`, `exp-results-replay-cta-v1`) can wire. Effort: S/night. Impact: unblocks 2 dead experiments → real conversion data. File(s): lane 03 prompt + the PageClient files.
5. **RSS fetch error-tolerant wrapper (lane 04)** — jq parse errors recur on some feeds (dailygames). Wrap each feed parse in a try/skip so one bad feed never aborts the sweep. Effort: S. Impact: lane 04 idea volume stability. File(s): `scripts/nightly/tools/` reddit/RSS helper.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/mcp-health-probe.sh`** — one idempotent function: probe Supabase REST (`/v1/projects`) + return 0/1, emit a single-line verdict, optionally `tg_alert` on failure. Called from preflight (improvement #2) and reusable by lanes 01/02/06 to decide MCP-vs-REST up front instead of mid-run discovery. Removes the repeated "try MCP, fail, fall back" dance ≥3 lanes do nightly. Effort: S. (Defer build to tomorrow — under finalize cutoff tonight.)

## New product/feature ideas surfaced by this week's data
- **Lazy-mount LobbyDailyEmber async fetch** — both `/he` LCP (3.3–6.2s oscillation) and `/es/mp` LCP +24% (3.1→3.9s) regressions trace to the async fetch added 06-21 (commit `ecda82ce8`). Defer/lazy-load the ember fetch below LCP fold → likely recovers both. Source: lane 02 perf rows 06-22/06-23.
- **Double down on mode-polish variable-reward toasts** — `polish:try` 10/10 / `polish:pass` 0 in last 7d; every shipped polish (alchemy streak PB toast, sealed-bid reveal stagger) got a "try" vote. The reward-celebration slice is the highest-ROI lane-05 pattern. Source: Telegram feedback aggregate.
- **Surface the Tower daily-letters → reward-ad refill (founder 06-02 directive)** — both a retention loop AND the standing revenue priority (rewarded-ad fill). Still only scoped. Lane 09 owns the flagged ad-value; lane 05 owns the refill UX. Source: founder directive + revenue standing priority.

## Loop changes shipped THIS WEEK
- `d91b5fa76` — off-master preflight self-heals onto master when lossless, else alerts (no more silent wrong-branch no-op).
- `8c705ba2c` — retry Supabase MCP boot probe on transient transport fail.
- `c920a05de` — applied the stuck RLS-initplan + FK migration; indexed `analytics_events`.
- `241476876` — isolated 1Hz MP lobby countdown to stop whole-view re-render (INP regression fix).
- `e9c75581b` (prior week, in effect) — cutoff-guard + stall alert; partial mitigation for the 06-20 cascade (full stagger still needed).
