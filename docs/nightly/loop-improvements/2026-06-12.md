# Loop Self-Review — 2026-06-12

## Run health (last 6 nights: 06-07..06-12)
| Lane | Code ship | MCP load | Notes |
|------|-----------|----------|-------|
| 01 triage | 5/6 | medium | DEFINER REVOKE pattern reliable; 06-09 docs-salvage (gate timeout); `rg` EACCES 06-07 |
| 02 perf | 4/6 | heavy | revalidate>force-dynamic doctrine; 2 timeouts (advisor + index analysis) |
| 03 engagement | 5/6 | medium | ships overlays cleanly but **3 experiments dark — flags never created** |
| 04 competitor | 6/6 | low | most reliable; prompt-only; 5 ideas + 3 reply drafts/night |
| 05 landing | 4/6 | medium | over-scope kills ES/ja locale (06-10, 06-12 cap-hit); Combo Meter unwired 06-09 |
| 06 seo | 6/6 | low | title-truncation CTR + ES +156% impr; prompt-only |
| 07 self-learn | 6/6 | none | prompt-only; never times out |
| 08 adsense | 5/6 | low | 27 blog pages→ISR 06-11; E-E-A-T schema propagation pending |
| 09 monetization | 3/6 | heavy | over-scope + offerwalls dark; 2 timeouts |
| 10 dictionary | 4/6 | heavy | vitest abort-trap wedges gate (06-09 OOM) |

Aggregate: ~38% lane-nights shipped code, ~23% timed out, gate-clean 2/6 nights.

## What's working well
- **Prompt-only lanes (04/06/07) = 18/18 ship.** The strongest reliability lever is eliminating MCP round-trips. Every never-timeout lane is low/zero-MCP.
- **`revalidate=N` doctrine** spread cleanly across word-wheel/hunt → /multiplayer → all 27 blog pages with zero regressions and a 65% LCP win. A validated pattern propagating itself across lanes is exactly what the loop should produce.
- **Mandatory-Minimum-Artifact floor** held every salvage night non-zero (06-07/09/11).
- **Author-then-ship-in-one-budget** finally closed AutoHideHeader CLS (06-10) after 3 nights of carry — proof the "don't carry written-but-unshipped fixes" rule works when enforced.
- **Founder `polish:try` → 1-night build** turnaround stayed tight (8 try votes → 6 components shipped).

## Friction points
- **Experiments ship dark (lanes 03, highest waste).** 3 fully-wired+tested+translated experiments have zero PostHog flags. Real engineering, zero measurable payoff. The loop has no handoff for the human flag-creation step. Growing 1/night.
- **Over-scope timeout cluster (02, 05, 09).** All three attempt TDD + 5 locales + animation/schema + page-wire in one 15-min budget. Predictable partial/timeout. ES/ja locale repeatedly dropped at file cap.
- **Gate clean-rate 2/6.** Vitest abort-traps on dictionary nights wedge the shared gate even at 8GB heap; gate unreliability — not lane capability — is the top system blocker.
- **Push-fail stranding (06-07).** Runner pushes without rebase; daemon advances master mid-run → non-fast-forward → commit stranded.
- **Stale search-intelligence brief recomputed 6/6 nights** — redundant discovery cost, no persistent cache.

## Concrete improvements (ranked by impact × effort)
1. **FLAG NEEDED digest block (lane 03 prompt).** When lane 03 wires/registers an experiment, it MUST append one line to the manager digest: `FLAG NEEDED: <key> | variants: <a,b> | hypothesis: <…> | targets: <funnel metric>`. Effort: S. Impact: unblocks 3 dark experiments worth real funnel lift (invite 83% drop, practice 43% drop). File: `scripts/nightly/prompts/03-engagement.md`.
2. **Locale-first hard ordering (lanes 05 + 09 prompts).** Write all 5 translation files BEFORE the component and page-wire. Effort: S. Impact: eliminates the ES/ja-incomplete ship class (2 nights this window). Files: `prompts/05-landing.md`, `prompts/09-*.md`.
3. **"Pick exactly ONE polish" scope cap (lane 05 prompt).** Explicit: one component OR one mode, not both; defer page-wire to next night if cap looms. Effort: S. Impact: attacks the 33% timeout rate on lane 05. File: `prompts/05-landing.md`.
4. **Per-lane MCP-call soft cap (lanes 02/09 prompts).** Add "≤4 MCP round-trips; if a 5th is needed, ship what you have + note the gap." Effort: S. Impact: 02/09 timed out 2/6 each — directly targets the 23% timeout rate. Files: `prompts/02-perf.md`, `prompts/09-*.md`.
5. **Rebase-before-push in runner.** `git pull --rebase` before the end-of-run push. Effort: M. Impact: recovers ~1/6 push-fail nights (06-07 stranded). File: `scripts/nightly/run.sh` (push step).
6. **Quarantine dictionary vitest into its own `--project`.** So lane-10 abort-traps can't wedge the shared gate. Effort: M. Impact: lifts gate clean-rate from 2/6. File: `fe-next/vitest.config.ts`.

## New helper scripts to ship
- **`scripts/nightly/tools/feedback_tally.sh`** — aggregates `polish:try|pass`, `idea:build|pass`, `night:good|meh` from `docs/nightly/feedback/*.ndjson` over a date window into a one-line summary. Lane 7 hand-rolls this grep every night (fired 3+ nights). Effort: S. Deferred tonight to avoid a half-written script near the cutoff; ship as first action next lane-7 run.

## New product/feature ideas surfaced by this week's data
- **Invite arrival spinner card** — source: `invite_landed` 18 → `invite_consumed` 3 (83% drop, 29 rage-clicks). Code-complete; just needs the flag. Highest-ROI conversion fix on the board.
- **Practice wheel-drag hint overlay** — source: `/es/practice/wheelRush` 47 started → 27 completed (43% drop, 5+ nights). Now instrumented (`practice_abandoned` 06-12); flag pending.
- **Hebrew daily internal-link cluster** — source: `המילה היומית` +3467% WoW, pos 8.7, 180 impr. Link shipped 06-12; cheapest SEO conversion left was capturing this ranking page's traffic.
- **Clue Budget Daily** — source: lane 04 06-12 (clue-economy mechanic, 100pts→buy hints). 100% idea:build rate says ship it admin-gated.

## Loop changes shipped THIS WEEK
- `00d96ad3f` — n≥50 sample-floor gate in `prompts/02-perf.md` (killed `/es/multiplayer` INP false-regression class). Proposed prior week, landed.
- `b266ca10e` — vitest heap → 8GB (reduced but did not eliminate OOM-wedge).
- AutoHideHeader CLS spacer-div (06-10) — closed a 3-night carry-forward proposed in 06-09 loop-improvements.
- `5cb7f28a0` — usage-limit-aware runner (sleep-until-reset + recompute deadline epochs); holding.
