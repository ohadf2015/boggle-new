# Loop Self-Review — 2026-06-18

## Run health (last 7 nights, 2026-06-12..06-18)
| Lane | Success | Avg duration | Avg files | Notes |
|------|---------|--------------|-----------|-------|
| 01 triage | 7/7 | ~29m | 3 | 10 zero-callsite REVOKE migrations, 0 reverts |
| 02 perf | 7/7 | ~34m | 3 | revalidate>force-dynamic; /he LCP root-caused 06-16 |
| 03 engagement | 7/7 | ~41m | 5 | 3/4 experiments live; 1 still dark |
| 04 competitor | 7/7 | ~27m | 3 | docs-only by design; 28 ideas, never times out |
| 05 landing | 7/7 | ~48m | 6 | slowest lane; 6 polish features, 0 reverts |
| 06 seo | 7/7 | ~32m | 4–20 | ES scrabble surge ridden; +800Δ click reframe |
| 07 self-learn | 7/7 | ~36m | 2 | learnings + loop-review nightly |
| 08 adsense | 7/7 | ~44m | mixed | 27 blog ISR; E-E-A-T complete, re-submit open |
| 09 monetization | 6/7 | ~38m | mixed | flagged ad-UX + edu upsell |
| 10 dictionary | 6/7 | ~15m | mixed | slowest-to-converge; contributes to gate timeout pre-fix |

## What's working well
- **Self-healing plumbing landed this window** — the loop fixed FOUR of its own top infra leaks via lane/nightly commits: gate cone-scope (`06be04691`), Supabase MCP transport-probe (`7f9c145f8`/`20b569a4f`), stranded-ref auto-dispose (`3a25418fb`), reddit RSS fallback (`c5b0c4c10`). The carry-forward backlog is shrinking, not just rotating.
- **PostHog dark backlog narrowed 4→1** — 3 experiments now wired+flagged (`exp-mp-quickplay-wait-v1` 206583, `exp-invite-arrival-clarity-v1` 206584, `exp-game-abandon-confirm-v1` wired `bc4d93d1f`). Only `exp-practice-wheel-cta-v1` remains.
- **Mandatory-Artifact floor = 100%** — every lane non-zero even on salvage nights.
- **Polish lane has perfect quality record** — 6 TDD features shipped, 0 regressions, 0 hot-fixes; `polish:try` feedback 11/0 confirms appetite.
- **Low-MCP lanes (04/06/07) never time out** — prompt-only is the reliability gold standard.

## Friction points
- **Gate verification gap** — cone-scope fix landed but UNVERIFIED. The run-log evidence (6/7 wedge) predates the fix, so we cannot yet confirm it works. Tonight is the first real test; need a clear PASS/FAIL signal in the run log, not buried in salvage logic.
- **Lane 05 is the slowest (~48m) and most over-scope-prone** — TDD + 5 locales + animation + page-wire repeatedly overruns into "partial" (ja/es dropped 06-12, tempo page-wire slipped to 06-18). The budget model doesn't match the work shape.
- **Experiment define→wire→flag still spans multiple nights** for `exp-practice-wheel-cta-v1` (4 nights dark). The "FLAG NEEDED" digest works for flag creation but doesn't force the wiring into the same lane.
- **Supabase migration apply still manual** — 3 migrations written but un-applied; MCP-headless fix helps connection, but nobody confirms the apply succeeded.
- **Lane 10 (dictionary) slowest-to-converge** — its progressive slowdown was a gate-timeout contributor pre-fix; watch whether cone-scope removed the dependency.

## Concrete improvements (ranked by impact × effort)
1. **Emit an explicit gate-scope verdict line** — make the gate log `GATE_SCOPE=changed-cone files=N rc=0` (or `WEDGE`) as a single grep-able line. Effort: S. Impact: lets lane 7 confirm `06be04691` works in ONE grep instead of inferring from salvage behavior; turns the #1 watch from "verifying" to closeable. File: `scripts/nightly/run.sh` (gate phase).
2. **Lane 05 budget split: locales-first checkpoint** — after writing the 5 locale strings + the pure module, emit a "polish-core landed" checkpoint; page-wiring becomes an explicit follow-on that can defer cleanly instead of leaving a half-wired feature. Effort: S. Impact: kills the recurring "partial — wiring deferred" (3× this window). File: lane-05 prompt.
3. **Force experiment wiring into the defining lane** — lane 03 prompt: an experiment may only be *registered* if its variant component is wired in the same run; otherwise don't register it. Effort: S. Impact: prevents `exp-practice-wheel-cta`-style 4-night dark drift; the 43% practice drop would be measurable. File: lane-03 prompt.
4. **Auto-verify migration apply** — when MCP is up, after applying a migration, run a confirming `SELECT` (policy exists / index gone) and log the verdict. Effort: M. Impact: closes the "written but un-applied, nobody knows" gap on the 3 pending perf migrations. File: `scripts/nightly/tools/migration-apply-verify.sh` (new).
5. **Promote `/es/mp` CLS +100% to a lane-02 STEP-0 must-investigate** — it's exceeded the 20% threshold for 4 nights with no root cause; the n≥50 floor is satisfied, so the only thing missing is a forced profiling pass. Effort: S (prompt), M (the fix). Impact: a real CWV regression on a money page (multiplayer). File: lane-02 prompt.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/migration-apply-verify.sh`** — given a migration file + a confirming SQL probe, apply via MCP then run the probe and emit `APPLIED|FAILED|MCP-DOWN`. Removes the recurring "3 migrations pending, status unknown" manual check (fired 3+ nights). Called from lanes 01/02. Effort: M.
- **`scripts/nightly/tools/gate-verdict.sh`** — wrap the integration-gate invocation to emit one grep-able verdict line (`GATE rc=N scope=cone files=N salvage=yes/no`). Lane 7 reads it directly instead of parsing salvage behavior. Effort: S.
(Skipping a 3rd — no other pattern fired ≥3 nights without an existing helper.)

## New product/feature ideas surfaced by this week's data
- **Multiplayer "quick rematch" CTA on results** — `exp-mp-quickplay-wait-v1` exists because `/en/multiplayer` shows 94 rage-clicks/7d on the seeking state; a one-tap rematch that reuses the lobby would convert that frustration into a session loop. Source: lane 03 rage-click metric.
- **Practice retry-loop reward** — practice funnel drops 43% (47→27 started→completed). A small variable-reward on practice completion (mirroring the polish features players love, `polish:try` 11/0) could lift completion. Source: lane 03 funnel + feedback.
- **Spanish "Scrabble en español" dedicated landing** — `scrabble en español` +1283% WoW, ES MP page just reframed off anti-intent for +800Δ est clicks; a purpose-built ES scrabble page (not just the MP page) could capture the structural brand-gap CTR. Source: lane 06 GSC delta.

## Loop changes shipped THIS WEEK
- `06be04691` scope integration gate to changed-cone (the prior week's #1 loop-improvement ask) — LANDED, verifying.
- `3a25418fb` land lane code reliably + auto-dispose stranded refs — clears the 06-13 strand that failed 4 nights.
- `7f9c145f8` / `20b569a4f` Supabase MCP reliable headless (connect-wait + transport probe).
- `c5b0c4c10` reddit search → RSS fallback on the oauth host.
- `74f8a405e` snapshot only untracked WIP (cut per-lane snapshot tax ~16min→0.2s).
