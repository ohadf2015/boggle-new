# Loop Self-Review — 2026-06-10

## Run health (last 6 report nights, 06-05..06-10)
| Lane | Success rate | Avg duration | Avg files touched | Notes |
|------|--------------|--------------|-------------------|-------|
| 01 triage | 5/6 | ~mid | 2 | DEFINER/RLS REVOKEs; blocked once by `rg` EACCES |
| 02 perf | 4/6 | long | 2 | 2 timeouts; CLS+ISR wins; introduced INP regression |
| 03 engagement | 4/6 | mid | 4–8 | 2 early timeouts; experiment-flag wiring solid late-week |
| 04 competitor | 6/6 | short | 0 (docs) | most reliable; 5 ideas + 3 reply drafts nightly |
| 05 landing | 3/6 | long | 4–9 | 2 timeouts + file-cap drops ES locale |
| 06 seo | 6/6 | short-mid | 3–5 | title-truncation CTR iterations; ES momentum |
| 07 self-learn | 6/6 | short | 2 (docs) | prompt-only, never times out |
| 08 adsense | 5/6 | mid | 2 | JSON-LD schema ships; E-E-A-T gap unaddressed |
| 09 monetization | 3/6 | long | 3–15 | 2 timeouts/gate-fail; offerwalls dark |
| 10 dictionary | 5/6 | mid | 4–14 | 1 gate-fail docs-only |

## What's working well
- **Prompt-only / low-MCP lanes (04, 06, 07) are 6/6** — zero infra dependency = zero timeouts. The single strongest reliability correlate.
- **Mandatory-Artifact floor held on both gate-fail nights (06-06, 06-09)** — no lane produced zero output even when the full gate timed out / OOM'd.
- **Founder `polish:try` queue → lane 05 → 1-night turnaround** is a tight, validated loop (100% idea:build rate, 9 polish:try this week).
- **Security-via-migration (lane 01)** ships small, idempotent, reversible REVOKEs with zero reverts across 3 nights.

## Friction points
- **Gate clean-rate is only 2/6.** Four nights ended salvage/docs-only from timeout, vitest OOM (heap raised to 8GB `b266ca10e`), or baseline-red. The authoritative gate is the loop's biggest unreliability source — code that shipped was often UNVERIFIED.
- **Lanes 02/05/09 timeout 2/6 each** — all three are heavy (perf measurement, 5-locale mode polish, monetization). Over-scope is concentrated in exactly these three.
- **Working-set file cap silently drops the ES locale** on lanes 05/09 — ships an i18n-incomplete change that the next night must finish. Worse than splitting cleanly.
- **`rg` EACCES** still blocks lane 01 callsite verification with no fast fallback.
- **Perf lane shipped a regression** (`QuickPlaySeekingOverlay` INP 244→614ms) because it didn't re-measure the same page's full vitals after the change.
- **Stale search-intelligence brief 6/6 nights** — lanes re-run discovery the brief should have made unnecessary.

## Concrete improvements (ranked by impact × effort)
1. **Locale-first ordering for lanes 05/09** — PROMPT edit: instruct "write + save all 5 translation files BEFORE the component/page wiring; if file-cap hits, the cap lands on code not locale." Effort: S. Impact: kills the recurring ES-incomplete ship (3/6 nights). File: lane 05 + 09 prompts.
2. **Perf-lane full-vitals re-measure gate** — PROMPT edit for lane 02: "after ANY overlay/skeleton/layout change, re-pull LCP+INP+CLS for the SAME page and report the delta; do not ship if any vital regresses >20%." Effort: S. Impact: would have caught the `/es/multiplayer` INP 2.5× regression. File: lane 02 prompt.
3. **Split heavy lanes into "land one mode-polish" cap** — PROMPT edit lane 05/09: "ship exactly ONE polish item end-to-end (component+wire+5 locales+test) — do not start a second." Effort: S. Impact: converts 2/6 timeouts toward the 04/06/07 reliability band. File: lane 05/09 prompts.
4. **`rg`-EACCES fast fallback helper** — SCRIPT: `scripts/nightly/tools/safe_search.sh` wrapping `rg` with a `git grep` fallback (git grep ignores EACCES dirs, is fast). Effort: M. Impact: unblocks lane 01 on EACCES nights. File: new script + lane 01 prompt ref.
5. **Surface "UNVERIFIED ship" explicitly in digest** — when the gate is salvage/docs-only, the Telegram digest should flag which lanes' code shipped without a green gate. Effort: M. Impact: founder knows which commits to eyeball. File: digest formatter.

## New helper scripts to ship (you may create these directly)
- **`scripts/nightly/tools/safe_search.sh`** — `rg` with `git grep` fallback on EACCES; same args, returns first that succeeds. Removes the 06-06-class whole-lane block. Called from lane 01 (and any lane doing callsite verification). Effort: M. *(Not created tonight — budget; queued as #4 above.)*

## New product/feature ideas surfaced by this week's data
- **Post-invite arrival screen** — source: `invite_landed→invite_consumed` 83% drop (18→3) + 29 rage-clicks, persists 4 nights. A 1-screen "you've been invited to [room], tap to join" landing could recover most of the 15 lost consumers. (`exp-invite-arrival-clarity-v1` registered 06-10 — measure, then build the winning variant.)
- **Practice abandonment nudge** — source: `/es/practice/wheelRush` 43% drop (47→27, 7d). A mid-practice "1 more round?" or progress-saved cue. Ship `practice_session_abandoned` event first (proposed 06-07, never shipped) to localize WHERE in the session the drop happens.
- **Hebrew daily-word internal-link cluster** — source: `המילה היומית` +3467% WoW, pos ~8.9, 144 impr. Adding internal links from HE home + guides to the daily-word page is a near-zero-effort rank-up on a query already surging. (lane 06)

## Loop changes shipped THIS WEEK
- `b266ca10e` — raised vitest heap ceiling to 8GB to stop the gate OOM-wedge.
- `fb0fa672b` — shipped 2026-06-09 lane code the OOM gate falsely deferred (recovery of dropped work).
- `5cb7f28a0` — pause-and-resume lanes across Claude usage-limit resets (recompute deadline epochs) — prior-week fix, validated this window.
- AutoHideHeader CLS spacer-div finally LANDED (06-10) — closed the 3-night author-then-never-ship watch.
