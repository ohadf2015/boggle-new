status: shipped
files_touched: none (Supabase migration applied via MCP: fix_search_path_update_subscriptions_updated_at — ALTER FUNCTION public.update_subscriptions_updated_at SET search_path='')
next_steps: |
  - Sentry JAVASCRIPT-NEXTJS-1WM/1WJ "Connection is closed" — deferred, stale one-off Redis-restart (07-22, 0 users). If it recurs, add targeted process.on('unhandledRejection') filter for ioredis CONNECTION_CLOSED during socket.io redis-adapter broadcasts, or check for a fixed @socket.io/redis-adapter version.
  - PostHog "Failed to fetch" (posthog-issue:019fca04) — deferred, root cause is public/widget.js (self-hosted feedback-devtools bundle) background fetch failing on network blips. Not fixable in this repo (prebuilt vendor bundle) — fix belongs in the widget's source SDK.
  - Supabase authenticated_security_definer_function_executable on upsert_push_token — closed, false lead (already correctly scoped, advisor re-flagging intentional grant).
  - See docs/nightly/triage-queue.md 2026-08-08 section for full detail on all 4 queued/closed items.
