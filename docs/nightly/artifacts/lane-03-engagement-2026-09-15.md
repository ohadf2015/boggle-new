status: shipped
attempted: Impact-check on exp-mp-quickplay-wait-v1/eager-disable ship; flag hygiene sweep; instrument the top rage-click target (student hub Play zone) instead of a blind new experiment, since the rageclick element had ZERO click instrumentation.
files_touched:
- fe-next/components/student/StudentHubPlayZone.tsx (2 new posthog.capture events: solo_practice, quick_duel cards)
- fe-next/components/student/PlayWithClassButton.tsx (1 new posthog.capture event: play_with_class card, hasGame path only)
findings:
- IMPACT CHECK already appended by an earlier pass tonight: `03-engagement-2026-09-12-mp-quickplay-seeking-eager-gap` verdict=improved (docs/nightly/impact-ledger.ndjson). Independently re-verified: $rageclick on /multiplayer = 4 in last 3d (down from baseline 8); growth:mp_quickplay_rapid_click = 0 events; growth:mp_quickplay_seeking = 1. Consistent with improved.
- Flag hygiene: diffed every `exp-*` key in fe-next/lib/experiments.ts against live PostHog flags — ALL already live, zero unwired gaps this run (re-check next run before trusting as permanent — did not do a full per-call-site grep on every key, time-boxed). Checked oldest live experiment `exp-results-replay-cta-v1` (since 2026-06-02) for a decided winner: only 255-273 users/arm, under the 1000/arm significance floor — not decided, no removal.
- ROOT CAUSE HYPOTHESIS for the brief's top item (rageclicks on /en/student, "Play" el_text, 5/3d): `PlayWithClassButton` renders as a non-interactive `m.div` (no onClick, no button role) whenever no teacher game is active — see the "status line, not an action" comment at PlayWithClassButton.tsx:51-56. A student tapping that card while idle gets NO response by design, which is exactly PostHog's rageclick definition (repeated clicks with no effect). This is a strong, low-effort candidate but needs the new event data to confirm before changing UX (e.g. disabled affordance / tooltip explaining "waiting for teacher").
- Chose NOT to ship a blind new experiment (GOAL 2) against this target — shipping a variant before confirming which of the 3 Play-zone cards is being rage-clicked, and why, would be guessing. Instrumented all 3 cards instead so tomorrow's run (or this one, once events land) can confirm the hypothesis with real click volume, then design the fix/experiment with a real target.
next_steps: |
  1. After ~24-48h of `student_play_zone_card_clicked` data: query event volume by `card` broken out by whether a teacher game is active, and cross-reference against $rageclick timestamps on /en/student. Confirms/refutes the play_with_class-idle-state hypothesis.
  2. If confirmed: ship a typed experiment (e.g. `exp-student-idle-play-card-v1`) — variant shows a disabled-but-explained affordance ("Ask your teacher to start!") instead of a silently inert div. That is GOAL 2, deferred one night for real data instead of a guess.
  3. exp-results-replay-cta-v1 (oldest live experiment, still undecided at n=255-273/arm after 3+ months) — flag for human review: either the SP results surface has too little traffic for this experiment to ever resolve, or it should be retired as inconclusive.
