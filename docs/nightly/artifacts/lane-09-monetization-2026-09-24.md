status: research-only
attempted: education lead-capture CTA on education landing pages; found funnel already comprehensive, no safe gap to ship.
files_touched: none
findings:
- for-schools lead-capture page (app/[locale]/education/for-schools/) already has: hero CTA, lead form, 6-locale content.ts, pricing schema, JSON-LD offers. Nothing to add.
- 16/29 education pages route through components/education/EducationLandingTemplate.tsx, which unconditionally renders TeacherAccessCTA + DistrictUpsellStrip (links to teacher/district href, i.e. for-schools) at the bottom. Funnel already wired for every marketing landing using the template.
- The 13 pages NOT using the template were individually checked: all are noindex tool/share routes by design (class-gap, chatgpt-reteach, unplugged-reteach, classroom-addon, miss-gap-*, team-tiles-unplugged, unplugged-grade-passback, duels, classroom-game, access) — Slack/parent-chat unfurl cards, ChatGPT Action handoffs, Google Classroom iframe attachments, teacher-projector routes. Adding a school-upsell CTA to a Classroom iframe or ChatGPT Action page would be wrong UX (not a real gap) — confirmed via source comments ("noindex — this is a share card, not an SEO landing" etc.) before deciding not to touch them.
- Ad-UX telemetry (rewarded_ad_offered/watched/declined, interstitial/rewarded lifecycle, ad_closed/ad_outcome quality study) is already fully wired in hooks/useRewardedAd.ts. The brief's "0/24h rewarded_ad_watched" signal is a traffic/volume signal, not a missing-instrumentation bug.
- IAP-interest probes (iap_viewed) already wired on 10+ surfaces: RemoveAdsProbe (settings), SupporterInterestCard (profile), teacher ProGate/upgrade/dashboard-banner/usage-prompt, education UnlockReportUpgradeCta. No unwired probe left to add.
next_steps: no near-term code gap found in this lane's usual playbook (lead-gen CTA, ad-UX telemetry, IAP probes) — the obvious wins from prior nights are already shipped. Next session should either (a) look at actual funnel CONVERSION on the existing for-schools lead form (PostHog funnel: hero CTA click -> lead form submit) rather than adding more entry points, or (b) revisit the ad-UX angle only once real rewarded_ad_watched volume returns (currently near-zero, brief flagged reach=0/low-confidence) so a change would be unmeasurable tonight anyway.
