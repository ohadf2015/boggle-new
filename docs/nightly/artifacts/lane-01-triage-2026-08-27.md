status: blocked
files_touched: none (fix drafted then reverted — ran out of time budget before the paired test fix could land)
next_steps: |
  ROOT CAUSE FOUND for brief item "[BOT_BLAST] Bot submission failed: object is not iterable"
  (sentry:JAVASCRIPT-NEXTJS-1Z4): this is a Class-3 asymmetric-path bug, not a one-off null-check.

  - Human word submission (fe-next/backend/handlers/wordValidationHandler.ts:138) cascades a
    blast word via `safeCascadeBlastWord` — clones the board, mutates the CLONE, and only commits
    (`blastState.playerBoards[username] = result.board`) on success. A mid-cascade throw leaves
    the authoritative board byte-for-byte intact.
  - BOTH bot paths call the RAW `cascadeBlastWord` directly instead:
      - fe-next/backend/services/gameLifecycle/botBlast.ts:163
      - fe-next/backend/services/gameLifecycle/botGame.ts:306
    `cascadeBlastWord` mutates `board` IN PLACE across several steps (board.grid via
    applyVortexLetterSwaps, then board.tileStates via computeGravityResult). Whatever throws
    "object is not iterable" partway through (likely inside processTilesForWord/blastGravity
    given a malformed vortex/gravity input — exact line unconfirmed, no Sentry stack trace
    available this run, MCP `sentry`/`supabase` servers never connected before the 15-min budget
    ran out) leaves the bot's board HALF-MUTATED, not just un-cascaded. The outer try/catch
    (botBlast.ts:228, botGame.ts:333) only logs — it doesn't roll back, because there's nothing
    to roll back to (no clone was ever made).

  FIX (drafted, reverted for lack of time to land the paired test change before finalize cutoff):
    In both botBlast.ts and botGame.ts, replace the `cascadeBlastWord` import/call with
    `safeCascadeBlastWord` (already exported from blastModeManager.ts, already proven on the
    human path) and commit the result the same way wordValidationHandler.ts does:
      const cascadeResult = safeCascadeBlastWord(board, wordPath, word, state.wave ?? 1, language);
      if (cascadeResult.ok) {
        if (!state.playerBoards) state.playerBoards = {};
        state.playerBoards[bot.username] = cascadeResult.board;
      } else {
        logger.error(...); // board kept intact, matches human-path log message
      }
      // use cascadeResult.board / cascadeResult.clearedCount downstream instead of board/result

  BLOCKER hit: fe-next/backend/services/gameLifecycle/__tests__/botBlast.test.ts mocks
  `cascadeBlastWord` directly (line 63, 130) with no `safeCascadeBlastWord` entry — the source
  fix alone breaks that mock (undefined call) and the finalize-cutoff hook blocked touching a
  NEW file (the test) after the source edit already landed, so I reverted the source edit rather
  than ship a red test.

  TOMORROW: add `safeCascadeBlastWord: vi.fn((board) => { board.totalMoves = (board.totalMoves
  ?? 0) + 1; return { board, clearedCount: 1, totalMoves: board.totalMoves, ok: true }; })` to
  the `mocks` object AND the `blastModeManager` mock in botBlast.test.ts FIRST, then land both
  source call-site fixes (botBlast.ts + botGame.ts — botGame.ts has no dedicated test mocking
  cascadeBlastWord, so it's lower-risk to touch). This is a same-shape fix in one sitting,
  ~15 min of work now that the diagnosis is done — do not re-diagnose from scratch.

  ALSO CONFIRMED (no action needed): brief's "Security Definer View: public_profiles" +
  IMPACT CHECK item — public_profiles is DEFINER-BY-DESIGN, migration
  20260815100000_friends_read_public_profiles.sql:34-38 explicitly says "Do NOT remediate it by
  setting security_invoker = on — that silently re-breaks every friends surface". Do not touch
  this again; it is not a real finding.

  NOT YET TOUCHED (queue for next run): "Game hasn't started yet" (sentry:...1Y8), "No estás en
  este juego" (sentry:...1VX), React #418 hydration (posthog). sentry/supabase MCP servers never
  became available this run (empty toolset the whole session) — worth checking if they're
  actually misconfigured rather than just slow to connect.
