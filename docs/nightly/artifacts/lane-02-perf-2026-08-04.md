status: shipped
files_touched: fe-next/app/[locale]/practice/[mode]/PageClient.tsx, docs/nightly/perf-baseline.json, docs/nightly/impact-ledger.ndjson, docs/nightly/reports/2026-08-04.md
next_steps: Run a real Lighthouse/CrUX trace on /es/practice/classic?play=1 -- the bundle-split shipped tonight is a safe correctness win but chunk-size alone (140KB raw per 08-02) doesn't fully explain the 9748ms LCP figure; likely network/image/font on the ES locale. Also unexamined tonight: /es/multiplayer?room=... (6712ms) and the unindexed vocabulary_lessons FK (still only 2 rows, no index warranted yet -- re-check row count as classroom data grows).
