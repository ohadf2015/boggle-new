status: shipped
files_touched:
  - fe-next/supabase/migrations/20260617030000_perf_drop_unused_index.sql (new — drops unused idx_offerwall_postbacks_user)
  - docs/nightly/perf-baseline.json (updated with 7d PostHog data + 3 pending advisor items)
next_steps: |
  BACKEND (human action needed — Supabase MCP unavailable 2 nights running):
  - Apply 20260615030000_perf_advisor_fixes.sql: ALTER POLICY tar_insert_authenticated (select auth.uid()) + CREATE INDEX idx_word_pacts_player2_id
  - Apply 20260617030000_perf_drop_unused_index.sql: DROP INDEX idx_offerwall_postbacks_user
  FRONTEND:
  - /es/multiplayer CLS regression: 0.144→0.288 (+100%, n=91 both runs) — investigate layout shifts on multiplayer page
  - /he LCP flat at 4764ms (POOR, n=82) — above-fold render path investigation needed
  - /en LCP 4440ms (POOR, first n>=50 reading) — establish second baseline night before acting
