status: research-only
files_touched: none (docs/nightly/triage-queue.md)
next_steps: |
  URGENT: Apply migration 20260607100000_word_wheel_catchup.sql to prod.
  SQL is in fe-next/supabase/migrations/20260607100000_word_wheel_catchup.sql
  — ADD COLUMN IF NOT EXISTS, safe+idempotent. Every word wheel submit is 500ing.
  Run in Supabase SQL editor:
    ALTER TABLE public.daily_word_wheel_attempts ADD COLUMN IF NOT EXISTS is_catchup boolean NOT NULL DEFAULT false;
    CREATE INDEX IF NOT EXISTS idx_word_wheel_attempts_catchup ON public.daily_word_wheel_attempts (player_id, is_catchup) WHERE is_catchup = false;
