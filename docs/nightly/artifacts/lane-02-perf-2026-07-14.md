status: research-only
attempted: Add web_vitals index + fix multiplayer CLS/INP
files_touched: docs/nightly/perf-baseline.json
next_steps: |
  - Supabase MCP absent (19th night) — 4 migrations still unapplied; human must mint never-expire PAT
  - /en/multiplayer CLS 0.574 (unchanged): root cause = isActive toggle removes AutoHideHeader from
    document flow AFTER socket roundtrip (>500ms after click = counts as CLS). Fix: optimistically
    set isActive on click, revert on socket error — PageClient.tsx ~line 300 join handler.
  - /es/multiplayer CLS CONFIRMED IMPROVED 0.369→0.296 (-20%) from 07-12 spacer fix. Good.
  - Below-floor INP watch: homepage / at 648ms INP (n=31) — when n reaches 50, investigate
    mode-card click handler (likely heavy synchronous state update on game mode selection).
  - Homepage LCP: /es 4128ms (n=31), /en 3715ms (n=28) — below floor but alarming.
findings: |
  - Word-wheel (7781ms LCP in brief): route already well-optimized — Pixi lazy, no above-fold images,
    dynamic imports correct. LCP source is likely network/TTFB on cold render, not JS.
  - ConnectionBanner is fixed-position — does NOT cause CLS (confirmed).
  - Bundle: no regression vs 07-13 (30,832 KB total, top chunks unchanged).
  - /es/multiplayer INP 478ms (+15.5% vs 414ms baseline) — below 20% regression threshold, DEFERRED.
