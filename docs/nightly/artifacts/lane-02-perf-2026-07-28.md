status: research-only
attempted: Fix Auth RLS initplan on public.subscriptions + investigate top LCP regressions (classic?play=1 4500ms, word-wheel 3908ms)
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/reports/2026-07-28.md
  - docs/nightly/impact-ledger.ndjson

findings:
  backend: Supabase MCP unavailable (2nd night). Migration 20260727020000_fix_subscriptions_rls_initplan.sql still pending apply.
  frontend_lcp: game-init bound (Pixi+dict). Below n=50 floor. LCP improved on measured routes — /en/multiplayer 2425→1726ms (-29%), /es/multiplayer 3348→2605ms (-22%).
  cls_root_cause: /en/multiplayer CLS 0.949 = socket connecting→lobby DOM swap (full-viewport replace). NativeLanguageBanner ruled out. Fix = lobby skeleton pre-socket (~4-6h refactor).

next_steps:
  - HUMAN: apply migration 20260727020000_fix_subscriptions_rls_initplan.sql (mint never-expire Supabase PAT)
  - Multiplayer CLS: render lobby skeleton before socket connects (assign lane 11 or refactor lane)
  - Game LCP: faster client init (Pixi tree-shaking, dict lazy-load) — multi-night
