status: shipped
attempted: run coverage audit (DEAD/CRATERED), handle IMPACT CHECK for room_joined_via_link, wire/fix top telemetry gap
files_touched: scripts/nightly/lib/posthog-coverage.sh (extractor false-positive fix), docs/nightly/impact-ledger.ndjson (2 entries), docs/nightly/reports/2026-09-01.md
next_steps: IMPACT CHECK verdict=neutral (0 exposure, not a regression). Classifier fixed — 5 phantom DEAD events (won/daily/desktop/banked/solo_results) were doc-comment literals, not real events; the standing "classifier false-positives" open item in nightly-learnings.md can likely be closed/downgraded next time it's reviewed. Real DEAD list is now 12 events (was reported as 17). Did not get to wire a fix for any of them tonight (time spent fixing the classifier itself, which was corrupting every lane-12 run's DEAD list). Next run: pick highest-value from the clean 12-item DEAD list — room_joined_via_code is real but has no manual-code-entry UI to wire it to (traced: only caller of trackGuestJoin fires room_joined_via_link); referral_link_clicked call site is correct, looks like genuinely low-traffic not broken. Per-mode completion holes query not run tonight (time-boxed).
