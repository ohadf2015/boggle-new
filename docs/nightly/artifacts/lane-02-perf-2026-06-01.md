---
status: shipped
attempted: Fix top Supabase advisor warnings (permissive policies, unused indexes) + frontend INP improvement for leaderboard avatars; document multiplayer LCP regression
files_touched:
  - fe-next/app/[locale]/leaderboard/PageClient.tsx (disableEffects on table Avatar rows)
  - docs/nightly/perf-baseline.json (updated with 2026-06-01 snapshot)
  - docs/nightly/perf-watch.md (3 new entries: multiplayer LCP regression, leaderboard prefetch, word-wheel watch)
  - docs/nightly/reports/2026-06-01.md (lane 02 appended)
  - supabase migration: merge_tar_select_policies_drop_unused_indexes (applied live)
next_steps: |
  HIGH PRIORITY — /en/multiplayer LCP regressed 3165ms→8448ms (n=48, high confidence):
    Implement skeleton SSR for multiplayer page — server-render lobby/waiting UI as LCP target,
    load Pixi+Socket.IO behind it. Same pattern needed for /he/daily (4088ms LCP).
  MEDIUM — /en/leaderboard LCP (7132ms historical): server-side prefetch of public leaderboard
    data in page.tsx ISR. Pass as initialData to useLeaderboard. ~30 lines.
  WATCH — /he/daily/word-wheel LCP 1399ms→2568ms (n=10, possibly noise — confirm 3 nights).
  BACKEND — auth_db_connections_absolute: switch Auth from fixed 10 connections to % allocation
    (Supabase dashboard setting, no code change).
---
