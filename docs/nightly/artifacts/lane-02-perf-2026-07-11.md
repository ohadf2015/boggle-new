---
status: research-only
attempted: Impact-check fetchLanding parallel (2026-07-08), surface CLS regression on multiplayer, update perf-baseline.json
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson
  - docs/nightly/perf-watch.md
  - docs/nightly/reports/2026-07-11.md
next_steps: |
  HUMAN: mint never-expire Supabase PAT (16th consecutive MCP-absent night; 4 migrations unappliable).
  CLS fix for /en/multiplayer + /he: reserve header height for conditional banners
  (ClassroomModeBanner/EducationHeader/SpectatorBanner) — they shift content on mount.
  multiplayer PageClient.tsx is 671 lines, needs focused investigation (not autonomous fix).
  INP on /es/multiplayer (420ms) also failing.
---

## Summary

Impact check: fetchLanding parallel (2026-07-08) = improved. /en LCP 2308ms vs 6904ms baseline (66% down).

New finding — 7-day window n>=50:
  /es/multiplayer: LCP 2694ms POOR, INP 420ms POOR, CLS 0.681 CRITICAL (n=54)
  /en/multiplayer: LCP 2459ms borderline, INP 264ms needs-improvement, CLS 0.575 CRITICAL (n=87)
  /he:             LCP 2328ms OK, INP 160ms OK, CLS 1.043 CRITICAL (n=59)

Not fixed: 671-line PageClient blast radius too high. Documented in perf-watch.md.
Backend: Supabase MCP absent (16th night). 4 migrations still pending apply.
Bundle: stable (+10KB from lane-01 WordTowerScene fix, within noise).
