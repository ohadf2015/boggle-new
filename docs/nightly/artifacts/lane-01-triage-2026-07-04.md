status: research-only
attempted: Triage top Sentry errors + Supabase security advisors
files_touched: docs/nightly/triage-queue.md, docs/nightly/reports/2026-07-04.md, docs/nightly/impact-ledger.ndjson
next_steps: |
  - Manually resolve in Sentry UI: 1CW (guards in place), 1KQ (dead code), 1M7 (old/transient), 1MC (low priority)
  - 1MC username fix: find UsernameSchema, strip parens/invalid chars client-side before MP join emit
  - teacher_access_requests RLS: add WITH CHECK (auth.uid() IS NOT NULL) if anon submissions not needed
  - upsert_push_token: no action (intentional SECURITY DEFINER, already has SET search_path='')
  - Sentry MCP token is read-only (403 on update_issue) — report to founder for token upgrade
