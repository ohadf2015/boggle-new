status: research-only
attempted: Query Supabase pg_stat_statements + advisors, pull PostHog + own web_vitals table for 24h Web Vitals, identify regressions, update perf-baseline.json
files_touched:
  - docs/nightly/perf-baseline.json (updated — 9th snapshot, 8 routes now tracked vs 1 prior)
  - docs/nightly/perf-watch.md (appended 3 new items)
  - docs/nightly/reports/2026-06-03.md (lane 02 section appended)
next_steps: |
  PRIORITY 1: /en homepage LCP 7107ms (n=12) — use Chrome DevTools LCP attribution to identify element.
    LandingHero mascot has priority; suspect leaderboard sidebar avatars or large text.
  PRIORITY 2: /en/free-multiplayer-word-game CLS 1.0 — GSAP ScrollTrigger pin-spacer creates layout shift.
    Fix: add anticipatePin:1 to ScrollTrigger config in Showcase3DClient.tsx useGSAP block, OR
    pre-set section min-height matching h-[100svh] before JS runs.
    Needs visual verification + playwright test before shipping.
  WATCH: /en/multiplayer LCP 3010ms→3616ms (+20%) — confirm over 2 more nights before fixing.
  BACKEND: No new actionable items. WAL parser steady growth — escalate to Supabase support ticket if
    crosses 4M total_ms next snapshot.
