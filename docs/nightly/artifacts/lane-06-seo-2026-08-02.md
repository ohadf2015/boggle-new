status: research-only
attempted: seo-daily skill (GSC+Bing 28d pull), Bing AI-perf scrape, education-intent scan
files_touched: none — ran out of time budget before a safe, verifiable edit could land
next_steps: |
  Report at docs/seo-daily/2026-08-02.md (fresh, real GSC+Bing data — first pull attempt
  silently used a stale /tmp/seo-data.json from 08-01, caught + re-pulled with explicit RC check).
  Top targets for tomorrow:
  1. CTR: /es/juego-de-palabras-multijugador dominates ALL top-10 CTR opps (scrabble
     online español cluster, 12192 impr, pos 4.9, CTR 0.24% vs 6% expected). Title/meta
     ALREADY contain the query verbatim — do NOT do a cosmetic title rewrite (skill rule).
     Root cause is likely missing rich-result eligibility (schema/rating) or a SERP
     competitor squeeze — investigate SERP directly before touching copy.
  2. Rank-up: /en/play-boggle-online-free ("boggle online" pos 13, impr 332) — page
     itself already has FAQ+internal links section; checked all 7 pages that reference
     "Play Boggle Online Free" and confirmed their hrefs correctly point to
     /${locale}/play-boggle-online-free (initial rg output misread as a dead /l link —
     false alarm, verified + ruled out, don't re-chase).
  3. Rank-up: /he/daily (מילת היום pos 9.8 impr 263, המילה היומית pos 8.6 impr 214) —
     not yet inspected, do this first tomorrow (smaller page, native-Hebrew review
     needed per STEP 4b).
  4. Bing parity: 238 queries ranking on Google, missing on Bing — top 10 listed in
     report, all /es/ scrabble + /he/ + /sv/ pages. IndexNow submission not yet run.
  5. No education-intent query surfaced in GSC top opps or Bing AI grounding_queries
     this window (checked both) — no education landing/CTR action taken tonight,
     not a gap worth forcing.
  Native review (STEP 4b): N/A — no locale strings edited this run.
  New landing: none — no >50impr query found with a missing page.
