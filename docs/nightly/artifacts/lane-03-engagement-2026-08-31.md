status: shipped
attempted: flag hygiene sweep (exp-* wired check + ensure live), new typed experiment targeting multiplayer rageclick funnel drop, 2-3 instrumentation events for education/multiplayer gap, impact ledger append
files_touched: fe-next/lib/experiments.ts, fe-next/components/education/TeacherGate.tsx, fe-next/components/education/__tests__/TeacherGate.test.tsx, fe-next/app/[locale]/education/access/PageClient.tsx, fe-next/utils/growthTracking.ts, docs/nightly/reports/2026-08-31.md, docs/nightly/impact-ledger.ndjson
next_steps: exp-singleplayer-word-goal-v1 is defined but has zero non-test call sites (unwired) — wire into classic/survival SP UI before it can serve, or drop it. No flag was decided (winner, p<0.05, ≥1000/arm) this run so nothing was retired — re-check exp-mp-quickplay-wait-v1 and exp-mp-quickplay-eager-disable-v1 (both already cover the top /multiplayer rageclick signal, score 0.75) for maturity next run. vitest CLI is broken in this env (rolldown/node:util styleText mismatch, unrelated to this change) — verified via eslint + tsc --noEmit -p . (exit 0) instead of the test runner; nightly gate should re-run tests authoritatively.
