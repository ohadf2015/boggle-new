status: partial
attempted: flag hygiene sweep (ensure wired exp- flags live) + one new experiment targeting rage-click hotspots from brief + 2-3 instrumentation events
files_touched: docs/nightly/impact-ledger.ndjson (impact-check verdict appended), docs/nightly/triage-queue.md (rage-click investigation writeup)
next_steps: |
  - IMPACT CHECK closed: avatar-builder press-feedback fix (2026-08-09) verdict=improved (0 rageclicks on /profile 7d, baseline 2).
  - Flag hygiene: all exp-* experiments in lib/experiments.ts confirmed wired (call sites found for exp-game-abandon-confirm-v1, exp-mp-round-feedback-top-v1, exp-mp-quickplay-eager-disable-v1, exp-mp-round-issue-probe-v1 — prior memory calling these "unwired" is stale). One truly orphaned PostHog flag found: exp-practice-wheel-cta-v1 (active=true, zero code references anywhere) — safe to leave (0% exposure impact since unwired) but a human could delete it in PostHog.
  - Did NOT ship a new experiment or new instrumentation events this run: spent the budget tracing the #1 brief signal (21 $rageclick/7d on bare /en/multiplayer, by far the largest surface) instead of guessing blind. Traced both CTA paths (ArenaCTAStrip, RoomListView join cards) — both already have proper whileTap/disabled/dim press feedback, so the previously-validated "missing active:scale" fix pattern does NOT apply here. Root cause still unknown: $el_text/$elements are null on all 21 events (PostHog autocapture isn't resolving the clicked element) — likely CrazyGamesFriendsStrip or the live-match-status bar, neither inspected. Full writeup + concrete next query in docs/nightly/triage-queue.md under 2026-08-16.
  - Tomorrow: either add data-ph-capture-attribute labeling to the unresolved elements, or pull $session_id for a few of the 21 events and watch a session recording, THEN scope the actual experiment/fix. Do not re-run the same broad rage-click query — the element-identity gap is the blocker, not more counting.
