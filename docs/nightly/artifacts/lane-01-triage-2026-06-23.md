status: shipped
files_touched:
  - supabase migration: word_wheel_catchup_column (ADD COLUMN is_catchup + index on daily_word_wheel_attempts)
  - supabase migration: update_difficulty_auth_uid_guard (auth.uid() guard on SECURITY DEFINER function)
  - docs/nightly/triage-queue.md (4 deferred items appended)
  - docs/nightly/reports/2026-06-23.md (lane 1 block appended)
next_steps:
  - Monitor word-wheel 500s — migration applied; verify error stops appearing in Sentry/PostHog
  - Review update_difficulty_auth_uid_guard by eod — confirm no service-role admin path passes foreign p_user_id
  - Bulk SECURITY DEFINER audit (68 functions) — needs human triage, each function requires individual callsite grep
  - null.clear PostHog remnants — monitor; close if no new occurrences in 7d
