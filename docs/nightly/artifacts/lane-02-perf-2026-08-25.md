status: shipped
files_touched: docs/nightly/impact-ledger.ndjson, docs/nightly/perf-baseline.json, docs/nightly/reports/2026-08-25.md
next_steps: mandatory impact-checks done for word-tower/singleplayer LCP fixes (both improved directionally, n below 50-floor — re-check again once n grows). Brief's top LCP routes (multiplayer variants, ES landing) code-reviewed clean, already carry prior nights' fixes — no code shipped this run. Backend advisor item still dashboard-only, non-actionable. Tomorrow: retry supabase MCP for pg_stat_statements/advisor sweep (didn't connect in-window tonight); revisit multiplayer LCP with real field data once concurrent lanes' dictionary/multiplayer edits land (may shift the bottleneck).
