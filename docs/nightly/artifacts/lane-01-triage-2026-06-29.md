status: partial
files_touched:
  - fe-next/components/wordTower/towerSprites.ts (impactRing rAF null-clear guard)
next_steps:
  - Supabase MCP unavailable tonight — apply search_path fix for get_friend_threads + REVOKE on upsert_push_token via migration (queue below)
  - Username validation Sentry error (JAVASCRIPT-NEXTJS-1MC) root cause not found via rg — check backend/ handlers or supabase RPC validation
  - churn-signals 502 already handled in useChurnSignals.ts at debug level (not a code regression)
