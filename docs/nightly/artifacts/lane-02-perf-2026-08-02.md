status: research-only
attempted: IMPACT CHECK for RLS-subscriptions fix + top LCP regression (word-wheel) + frontend/backend quick wins per brief
files_touched: docs/nightly/perf-baseline.json, docs/nightly/impact-ledger.ndjson, docs/nightly/reports/2026-08-02.md
next_steps: |
  - IMPACT CHECK CLOSED: 07-30 RLS-initplan fix on subscriptions verified IMPROVED (auth_rls_initplan lints 1->0). Verdict in impact-ledger.ndjson.
  - vocabulary_lessons unindexed FK: SKIP permanently unless table grows past a few hundred rows (currently 2 rows, no measurable index benefit).
  - practice/[mode] eager-sandbox-import: CLOSED, verified NOT worth splitting (140KB raw chunk, well under 200KB-gzip threshold). Do not re-flag unless chunk grows.
  - word-wheel LCP: brief shows 7086ms vs 3936ms prior baseline (near-2x). Prior nights already confirmed the LCP element is canvas/DOM-drawn (not an Image, no priority-prop fix applies). Before attempting any fix: first verify this is a REAL regression and not noise by checking the n-count (sample-floor rule) via posthog-query.sh, and diff commits touching WordWheelChallenge.tsx in the last 24h.
  - No code shipped tonight: both backend and frontend candidates were investigated and correctly failed the hard-rule bar (unmeasurable index benefit; bundle below split threshold) rather than being force-shipped.
  - LATE FINDING (background Explore agent returned after this lane's finalize cutoff, not actioned): fe-next/components/daily/WordWheelChallenge.tsx ('use client', ~755 lines) imports framer-motion + lucide-react synchronously at the top (lines ~6-7) AND blocks the ready-screen render on an async fetch to /api/daily-challenge/word-wheel/check-played/ (5s timeout, lines ~255-298) before generateWordWheelPuzzle() runs (line ~307). This is a MORE concrete LCP culprit than the "canvas-drawn, unfixable" verdict from 07-31 -- that verdict may have been about the wrong bottleneck (framework paint vs blocking data-fetch-before-render). Candidate fix: render the ready screen optimistically without waiting on check-played, and/or dynamic-import the phase-specific renderers (ReadyPhase/PlayingPhase) so framer-motion/lucide-react load after first paint. NEEDS: read the full file first (it's 755 lines, use Explore or chunked Read), confirm the fetch is actually render-blocking (not fire-and-forget), then TDD the fix. Untested hypothesis -- verify before shipping.
