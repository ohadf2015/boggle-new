status: shipped
attempted: STEP 0 - improve one existing admin-beta mode (self-select target), one axis, existing files only
files_touched: fe-next/app/[locale]/sealed-bid/page.tsx
next_steps: Sealed Bid is now heavily polished (speed bonus, lucky-streak 2x, hot-streak badge, per-round breakdown, and now legendary/error haptics all present) — next rotation pick a different admin-gated mode (word-craft gems/cards or crossword) to avoid diminishing returns on this one. wheel-rush and connections are now PUBLIC (memory/rotation list is stale, verified via BattleModeCard.tsx + lib/dailyModes.ts on 2026-09-26) - drop them from the admin-beta target list.
