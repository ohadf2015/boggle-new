status: partial
files_touched:
  - fe-next/next.config.mjs (serverExternalPackages fix — ~6MB bundle reduction expected)
  - docs/nightly/perf-baseline.json (updated with 7d vitals + regression flags)

shipped:
  - serverExternalPackages: ['@arvidbt/swedish-words', 'an-array-of-english-words', 'an-array-of-spanish-words'] added to next.config.mjs. Prevents Turbopack from bundling these server-only word dicts into client static/chunks. Expected win: 6452KB chunk eliminated from client bundle on next build.

vitals_snapshot_7d:
  - /es/multiplayer: LCP 7291ms (+23% regression vs 5932ms baseline, n=55), CLS 0.265 (was 0.14), INP 368ms
  - /en/multiplayer: NEW above floor — LCP 4380ms, INP 504ms (severe), CLS 0.392 (severe), n=55
  - /he: LCP 2702ms (improving, trend 3297 to 2702ms over 4 nights), CLS 0.088 (now OK)
  - 24h window: sub-floor (0 routes n>=50) — no regressions flaggable

deferred:
  - Supabase MCP absent 2nd consecutive night. No management API PAT in nightly env.
  - DB advisor items carried: (1) FK index word_clubs.owner_id, (2) drop unused idx_word_wheel_attempts_catchup, (3) auth conn pool absolute→percentage (dashboard-only)

next_steps:
  - Add SUPABASE_ACCESS_TOKEN (never-expire PAT) to ~/.config/lexi-nightly/env to unblock DB migrations
  - Verify 6MB chunk gone after next prod build (check if 7fd32332*.js shrinks)
  - /en/multiplayer INP 504ms + CLS 0.392 = structural SSR issue — apply same ssr:true treatment as a83548724 (fixed /multiplayer blank FCP 06-26)
  - /es/multiplayer CLS 0.265 regression needs investigation — likely same ssr:false root
