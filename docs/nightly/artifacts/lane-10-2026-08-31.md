# Lane 10 — Dictionary improvement — 2026-08-31

status: shipped
attempted: run dictionary-improvement workflow for ja/sv/es (limit 25 each), then charset-clean candidate files.

## Result

Workflow (`dictionary-improvement`, run `wf_5ef5e3d7-544`) generated candidates for 5
langs — requested ja/sv/es plus en/he it self-selected — dual-judged (two personas,
must agree ≥0.75), appended survivors to `fe-next/backend/dictionary/candidates/*.txt`.

| lang | proposed | kept (judge-agreed) | appended |
|---|---|---|---|
| en | 40 | 40 | 33 |
| he | 40 | 40 | 7 |
| sv | 44 | 44 | 12 |
| ja | 40 | 40 | 8 |
| es | 40 | 40 | 30 |

Total appended: 90 candidate words across 5 files.

## Form-correctness cleanup

Ran the charset-verification pass (en `^[a-z]+$`, es `^[a-zñ]+$`, sv `^[a-zåäö]+$`,
he `^[א-ת]+$`, ja `^[ぁ-ゖー]+$`) against all 5 touched files (not just the 3 requested,
since the workflow also touched en/he). **0 violations dropped** — every judge-approved
word already matched its language charset.

## Hard limits respected
- Only touched `fe-next/backend/dictionary/candidates/*.txt` + this artifact.
- No backend code, migrations, translations, or validation-set files edited.
- These are CANDIDATES only — not yet in the live dictionary; they go through the
  existing verify→promote→heal pipeline (Wiktionary/Jisho/milog + offensive filter)
  before reaching gameplay.

files_touched: fe-next/backend/dictionary/candidates/en.txt, candidates/es.txt, candidates/he.txt, candidates/ja.txt, candidates/sv.txt
next_steps: none — candidates await the standard promote pipeline. Lane 10 has now run
(prior 2 windows had 0 runs, was flagged as retirement candidate — no longer applicable).
