status: shipped
attempted: Fix Supabase SECURITY DEFINER anon-callable RLS helpers + diagnose top Sentry errors
files_touched:
  - docs/nightly/reports/2026-06-08.md (appended Lane 1 section)
  - docs/nightly/triage-queue.md (appended 2026-06-08 entries)
  - Supabase migration applied: revoke_anon_secdef_rls_helpers (DB only, no file)
next_steps: |
  - Verify SUPABASE_JWT_SECRET is set in Railway env (fixes churn-signals 502 root)
  - Confirm curator/propose route still works after REVOKE (should be fine — uses authenticated session)
  - If profiles/relation error recurs on /api/coins, audit that route for schema-less queries
