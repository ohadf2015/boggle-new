status: research-only
attempted: fix top brief items — posthog Load-failed TypeError, sentry Connection-closed, supabase security_definer RPC revoke
files_touched: none (no code shipped)
next_steps: |
  All 7 brief candidates diagnosed via Sentry/PostHog/Supabase MCP but none were safe to
  ship blind this window — see docs/nightly/triage-queue.md "## 2026-07-31" block for full
  diagnostic detail per item (issue IDs, sampled event URLs, stack status) so the next lane
  doesn't need to re-query:
    1. Supabase upsert_push_token SECURITY DEFINER advisor — VERIFIED SAFE, closed, no action.
    2. Sentry JAVASCRIPT-NEXTJS-1PH (CapacitorGameConnect.then) — STALE, already fixed 07-05, no action.
    3. Sentry JAVASCRIPT-NEXTJS-1WM (Connection is closed, ioredis publish race) — stale (last
       seen 07-22, 0 users), low priority, still open in Sentry.
    4. PostHog TypeError "Failed to fetch" /en/multiplayer (019f34a3...) — sourcemaps missing
       for the 2 implicated chunks, single-user retry burst, root fetch call unidentified.
       NEXT: upload sourcemaps or grep multiplayer fetch call sites for an unguarded call.
    5. PostHog React #418 hydration mismatch x3 issues (/, /he, /en/multiplayer) — minified
       React errors carry no frame info from PostHog alone; needs local SSR-vs-CSR repro per
       page. NEXT: lane with more budget (02 or 11) should repro locally with React dev build.
    6. Player report: volume button (`components/InGameAudioButton.tsx:47-50`, fixed
       top-left in RTL) plausibly overlaps the score HUD on /he/daily/word-hunt — needs a
       live RTL screenshot before touching z-index/offset. NEXT: lane 05/11 with
       agent-browser, screenshot /he/daily/word-hunt, adjust offset if confirmed.
