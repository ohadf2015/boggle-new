status: shipped
attempted: fix top LCP/perf regressions from brief (imposketch.io slow LCP routes, board-k impact check), then bundle/frontend scan on fe-next
files_touched:
  - fe-next/app/[locale]/admin/PageClient.tsx (DailyActivityChart recharts import -> next/dynamic(ssr:false), conditionally rendered behind authToken)
  - docs/nightly/perf-baseline.json (today's snapshot + notes)
  - docs/nightly/impact-ledger.ndjson (admin-chart-dynamic-import entry appended; no new verdict for 08-31 board-k check, already closed 09-12, tonight's re-check fell below n>=50 floor)
  - docs/nightly/reports/2026-09-13.md (Lane 02 section appended)
next_steps: |
  - Brief's top items (imposketch.io, tryshopli.com) are EXTERNAL domains sharing our PostHog project -- not this codebase, skip in future briefs or filter by $host at brief-generation time.
  - /en/multiplayer LCP 2760ms/INP 504ms/CLS 0.265 (n=187, real signal, carried since 09-09) still needs Chrome DevTools INP/CLS attribution profiling before anyone touches PageClient.tsx or RoomListView.tsx (both large, actively-instrumented) -- not safe to guess-fix in a single lane budget.
  - Supabase advisor "auth_db_connections_absolute" (percentage vs absolute connection strategy) is a dashboard config toggle, not a migration -- still human queue.
  - eslint on changed file is clean; a full-repo `tsc --noEmit` was started as a background sanity check but not waited on (repo-wide tsc is the exact anti-pattern this lane is told to avoid) -- the isolated nightly gate will re-verify types/build authoritatively.
