status: research-only
attempted: Investigated top 5 brief items: CapacitorGameConnect (fix in nativePGS.ts looks complete but still fires — needs Sentry trace to confirm remaining path), word_pacts impact check (Supabase MCP not available), upsert_push_token SECURITY DEFINER (intentional — authenticated-only server-side call), RLS always-true teacher_access_requests (deferred — design call), null .clear() JAVASCRIPT-NEXTJS-1PV (couldn't localize without Sentry trace)
files_touched: none (Sentry + Supabase MCP both unavailable this run)
next_steps: (1) Re-run impact check for idx_word_pacts_player2_id via Supabase MCP exec_sql when connected; (2) Get Sentry trace for JAVASCRIPT-NEXTJS-1PH to confirm CapacitorGameConnect fix is complete — check if awardPlayGames.ts unlockAchievement().then() chain is the remaining callsite; (3) Get Sentry trace for JAVASCRIPT-NEXTJS-1PV null .clear() — reach=6 warrants localization
