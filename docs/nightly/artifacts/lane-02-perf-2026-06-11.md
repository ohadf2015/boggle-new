status: shipped
files_touched:
  - docs/nightly/perf-baseline.json
  - supabase migration: drop_unused_idx_user_reports_pending (applied live to prod)
next_steps: |
  - /he home CLS 0.386 undiagnosed — n=8, needs n>=50 before verdict. Watch.
  - /en INP 360ms + /es/multiplayer INP 352ms (7d, n>=50) — source undiagnosed, likely heavy client component on critical path. Needs route-level profiling.
  - word_pacts FK index (player2_id) — re-evaluate when table reaches 1000+ rows.
  - auth_db_connections_absolute advisor — switch to percentage-based allocation when scaling.
