status: partial
files_touched:
  - fe-next/app/[locale]/juego-de-palabras-multijugador/components/HeroAnimated.tsx
  - docs/nightly/perf-baseline.json
attempted: Fix top backend query (advisor/pg_stat_statements) + top frontend LCP regression (multiplayer room + wordwheel/wordhunt daily), update perf-baseline.json
shipped: ES multiplayer landing hero (juego-de-palabras-multijugador/components/HeroAnimated.tsx) had its priority LCP image wrapped in a framer-motion m.div with mount-time scale/rotate entrance tween -- delays visual settle behind hydration, same anti-pattern already fixed in the sibling RoomListView.tsx multiplayer hero (initial={false}). Applied identical fix. Lint-clean on changed file.
not_shipped: Backend brief items (unindexed FK vocabulary_lessons_classroom_id_fkey, unused idx_word_pacts_player2_id, auth_db_connections_absolute) -- supabase MCP never connected within lane window, no REST/mgmt-API fallback script in scripts/nightly/lib/ (only posthog-*.sh exist). Hard rule requires EXPLAIN ANALYZE before any index -- deferred, not actioned speculatively.
next_steps: (1) Backend -- next lane with supabase MCP/DB access should EXPLAIN ANALYZE the FK-join queries and unused-index candidates from tonight's brief before adding/dropping anything. (2) Frontend -- re-query PostHog LCP for /es/juego-de-palabras-multijugador in ~3 days to confirm the tween-removal delta (see impact-ledger entry). (3) Consider grepping other landing-page Hero*.tsx files for the same m.div-wraps-priority-image entrance-tween pattern -- this is the second instance found in 2 nights (RoomListView 07-30ish, HeroAnimated 08-01) so likely a repeated authoring pattern across other landing pages (education, home, etc).
