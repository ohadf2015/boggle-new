status: partial
files_touched:
  - fe-next/supabase/migrations/20260716020000_web_vitals_player_id_fk_index.sql (new)
  - docs/nightly/perf-baseline.json (updated)
  - docs/nightly/reports/2026-07-16.md (appended)
  - docs/nightly/impact-ledger.ndjson (appended)

findings:
  backend:
    - Supabase MCP absent (21st night) — wrote FK index migration for web_vitals.player_id
    - 6 migrations total pending apply — HUMAN: mint never-expire PAT
  frontend:
    - Homepage (/) above n≥50 floor for first time: LCP 4880ms, INP 992ms, CLS 0.865
      Hero image already has priority=true + preload — not the fix target
    - /en/multiplayer CLS REGRESSION: 0.576→0.976 (+70%, n=105, confirmed)
    - /es/multiplayer CLS REGRESSION: 0.421→0.716 (+70%, n=70, confirmed)
    - Root cause: async socket state (ConnectionBanner/SpectatorBanner null→visible shift)
      Structural fix needed in PageClient.tsx ~L599-623 — not viable in budget

next_steps: |
  HUMAN: mint never-expire Supabase PAT (6 migrations backlogged, 20+ nights).
  Frontend: reserve stable min-h for ConnectionBanner/SpectatorBanner slot in
    multiplayer PageClient.tsx so socket hydration doesn't cause CLS.
  Frontend: browser trace on homepage to identify CLS source (not the hero image).
