status: research-only
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/artifacts/lane-02-perf-2026-06-24.md
next_steps: |
  /es/multiplayer LCP at 5932ms (+53% from 3876ms baseline, n=62 both runs — confirmed regression).
  Root cause structural: all 4 views (HostView/PlayerView/MultiplayerFlow/ResultsPage) are
  ssr:false dynamic imports, socket-gated. PlayfulBackground is CSS-only. Mascot has priority=true.
  Needs Chrome profiler trace to identify LCP element and whether socket reconnect or re-render moves it.
  Backend: nothing new — FK index + unused index already fixed 06-23 migration.
  auth_db_connections_absolute remains human-queue (Supabase dashboard config change).
