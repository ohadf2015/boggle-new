status: shipped
attempted: audit current mode (wheel-rush) from docs/nightly/mode-readiness.md ledger, run code audit + safe fixes, update ledger + report
files_touched: docs/nightly/mode-readiness.md, docs/nightly/reports/2026-09-19.md (docs only, no app code changed)
next_steps: wheel-rush at 85%. 4th consecutive clean-delta night (no wheel-rush commits since 09-16). Audited closestRival.ts, wordDirection.ts, letterCoverage.ts (incl. Hebrew sofit-normalization ordering) — all clean. Visual QA remains structurally blocked for an agent (no standalone route) and stays owner:review-by-eod per the 2026-09-12 escalation — do not re-attempt live 2-tab MP capture without a dedicated ≥10min block at the START of a night. Next night: continue delta-audit technique or pick a still-unnamed dimension (e.g. very-long-word / max-score-overflow edge case, or a11y focus-order on the built-word chip row) since standard dimensions are exhausted.
