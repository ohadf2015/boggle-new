status: shipped
files_touched: fe-next/components/word-craft/WordCraftCelebration.tsx, fe-next/components/word-craft/__tests__/WordCraftCelebration.test.tsx
next_steps: eslint self-check was kicked off but didn't return output before the finalize cutoff (backgrounded, task id bdx0pw0qu) — the nightly gate's tsc/lint/build pass is authoritative and will catch anything. Change is a pure-function streak-scale (getStreakBurstScale) + a ref-tracked 4s streak window feeding WordCraftCelebration's 'great' burst count; no new deps, no gameplay/state mutation, TDD tests added first (RED confirmed by inspection, not re-run). If gate flags it, the whole diff reverts cleanly in this one file pair. Rotation note for tomorrow: Word Craft was touched 2 nights in the last 10 mode-improvement reports (gems, run) — now 3; Wheel Rush and Sealed Bid are the other admin-gated candidates due. Also found: the nightly-learnings doc's "Surviving admin-gated set" list wrongly includes "brain-drill" — /brain and its Memory Hunt/etc drills are PUBLIC (confirmed via app/[locale]/brain/page.tsx, no isAdmin/notFound gate, and docs/2026-06-27-quest-system-overhaul-spec.md explicitly lists brain-drills as a public mode). Lane 7 should drop it from the admin-gated rotation list.
