status: partial
files_touched: docs/nightly/impact-ledger.ndjson, docs/nightly/triage-queue.md, docs/nightly/reports/2026-09-26.md (docs only, no fe-next/ code)
next_steps: Impact check found `game_feedback` on mp_round has near-zero volume (0 events last 17d) — the real problem to fix isn't any one experiment, it's discoverability of the feedback prompt itself. Sweep confirmed 4 wired exp-mp-round-* flags are live in PostHog (idempotent ensure, all "exists"). exp-game-abandon-confirm-v1 is defined in lib/experiments.ts but resolveQuitConfirmDescription() has zero callers — wire it into the singleplayer quit-confirm dialog before its flag can go live (M-effort, logged in triage-queue.md). Did not reach STEP 3 (new experiment) or STEP 4 (new instrumentation) — ran out of time budget after the flag sweep + impact check; next run should pick those up, ideally targeting why mp_round feedback volume is so low before adding another mp_round experiment on top of a near-zero-traffic surface.
