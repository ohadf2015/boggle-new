status: research-only
attempted: Apply pending Supabase FK index migrations (web_vitals + word_pacts); diagnose CLS regression across all routes; update perf-baseline.json
files_touched:
  - docs/nightly/perf-baseline.json (updated with 7-day WebVitals for 4 routes)
next_steps: |
  CRITICAL — CLS across ALL routes catastrophic (0.673–0.979), newly observed on homepage too.
  1. DevTools visual QA on /en and /en/multiplayer — use Web Vitals Chrome extension CLS breakdown.
  2. Hypotheses:
     a. NativeLanguageBanner: null→rendered after hydration adds height, shifts content below.
        Fix: always render with height:0 overflow-hidden when no suggestion, OR position:fixed overlay.
     b. AutoHideHeader spacer drift: min-h values (60–124px) may not match actual Header height.
        Fix: measure header + sync spacer, or ResizeObserver to keep them locked.
  3. Supabase MCP absent (23rd consecutive night) — 6 migrations unapplied (carry 3–22).
     HUMAN: mint never-expire PAT, drain all 6 on recovered night.
  4. Homepage LCP 5127ms — next: static poster frame for winner.webp (393KB animated).
