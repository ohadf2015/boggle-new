status: shipped
attempted: fix top backend perf (unindexed FK on vocabulary_lessons) + top frontend LCP (word-tower daily es/en route) per brief
files_touched:
  - fe-next/components/wordTower/WordTowerGame.tsx (dynamic-import WordTowerLeaderboard modal, ssr:false — pulls it and the 464KB Avatar module out of the word-tower page's critical bundle)
  - docs/nightly/perf-baseline.json (today's snapshot)
  - docs/nightly/impact-ledger.ndjson (1 entry)
  - docs/nightly/reports/2026-08-15.md (lane section appended)
next_steps: |
  Backend still fully blocked — supabase MCP has now failed to connect 2 nights running (08-14, 08-15).
  Worth escalating like the preflight-abort item in learnings: if it's a systemic connect failure, not
  transient, someone should check the MCP server config outside the nightly loop.
  Frontend: only 1 of 2 brief LCP items (word-tower) got a code fix; "/" (10621ms) was checked for the
  two common patterns and was clean — its actual cause is unknown, needs deeper investigation (network
  waterfall / bundle trace) next run. /es/daily/word-hunt (8624ms) and /es/juego-de-palabras-multijugador
  (6269ms) not investigated at all this run.
  Re-check word-tower LCP via impact ledger entry in 3 days once traffic accumulates n>=50.
