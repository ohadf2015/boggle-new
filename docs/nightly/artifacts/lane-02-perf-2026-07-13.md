status: research-only
files_touched: docs/nightly/perf-baseline.json
next_steps: |
  1. VERIFY CLS fix (07-12 spacer-collapse): check tomorrow 24h window for /en/multiplayer CLS < 0.3
  2. VERIFY INP regression (/es/multiplayer 414ms): should recover after exp-mp-room-join-loading-v1 revert settles
  3. Homepage LCP 6319ms: winner.webp (393KB animated WebP) is the LCP element. No static poster exists.
     Create static winner.png and show animated only post-hydration. Also: SSR_LANDING_DATA_BUDGET_MS=1500ms
     cache-miss budget contributes to TTFB — consider reducing to 800ms with faster fallback.
  4. Supabase MCP: 18th consecutive night absent. Apply 4 pending migrations once token is minted.
  5. /es/multiplayer INP (414ms): if still failing after experiment revert settles, check onRoomClick handler
     in MultiplayerFlow — wrap non-urgent state updates in startTransition.
  6. Cleanup: joiningRoomCode still in RoomListView interface but not destructured (dead prop).

findings: |
  - Backend: Supabase MCP absent (18th night). 4 migrations pending. 3 advisor warnings carried.
  - Frontend 7d (n>=50): /en/multiplayer LCP 2459ms INP 284ms CLS 0.574 (n=76)
                         /es/multiplayer LCP 2380ms INP 414ms CLS 0.369 (n=66)
  - /es INP regression +30% (318->414ms) — exp-mp-room-join-loading-v1 (disabled buttons). Reverted 07-12.
  - CLS 0.574 on /en/multiplayer includes pre-fix data. 07-12 fix pending verification.
  - Homepage LCP: winner.webp 393KB animated WebP; play.webp exists at 191KB (different pose).
  - PageClient.tsx 671 lines (over cap); RoomListView dead joiningRoomCode interface prop.
