status: shipped
files_touched: fe-next/app/[locale]/rules/page.tsx
next_steps: Live word-count audit (Googlebot UA, prod) came back clean — no informational page under 300 words except /ja (known CJK tokenizer artifact, not real thinness). dead_pages.py GSC check found exactly 1 candidate (/en/words/starting-with/f, 0 clicks/1 impr) and it was ALREADY in the DEAD_LETTERS noindex set — no new noindex action. Audited JSON-LD coverage across informational pages (about/glossary/rules/faq/how-to-play/guides) — all already carry Organization/BreadcrumbList/FAQPage/DefinedTermSet schema, no gaps. Only real opportunity found: /rules (game-mode rules, high-traffic informational page) had zero internal links into the /guides strategy content, unlike /how-to-play and /faq which already use GuidesCalloutLink. Wired GuidesCalloutLink (existing, locale-safe, zero-prose component) into /rules, same pattern as how-to-play/page.tsx. Zero new prose, no game-page edits, no locale files touched (component is self-contained i18n). Background eslint check (job bddbq2hj1) was still running at finalize cutoff — re-verify its output next run before trusting blindly, though the diff is a 2-line mechanical addition identical to an existing working call site. AdSense readiness: site content depth and structure look solid tonight (no thinness, JSON-LD complete on core informational pages) — re-submission blocker is likely just operator timing/queue, not a content defect at this point.
