status: research-only
files_touched: docs/nightly/perf-baseline.json, docs/nightly/reports/2026-09-19.md
next_steps: nothing safely actionable tonight — both Supabase advisor items (unindexed FK on vocabulary_lessons 29 rows, unused index on word_pacts 1 row) are trivial-table false positives, re-confirmed and closed. No lexiclash PostHog route clears the 50-sample floor (max n=14) so no frontend regression verdict is possible. Brief's headline LCP item (imposketch.io) is cross-tenant noise on the shared PostHog project, not lexiclash — future briefs should filter by $host before scoring. The one real, recurring backend signal is realtime.list_changes WAL cost, climbing 42.84%→57.40% of DB time over 8 weeks per db_perf_top_query_audit — that needs a founder-scope decision on Realtime publication membership (see 50-supabase-perf.md), not a lane-02 code fix. Tomorrow: re-check PostHog sample counts (traffic may grow past 50) and re-run the FK/index row counts in case tables have grown.
