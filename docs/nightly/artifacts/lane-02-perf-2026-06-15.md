---
status: partial
files_touched:
  - fe-next/supabase/migrations/20260615030000_perf_advisor_fixes.sql
  - docs/nightly/perf-baseline.json
  - docs/nightly/perf-watch.md
  - docs/nightly/reports/2026-06-15.md (appended)
next_steps: |
  Supabase MCP unavailable tonight — migration 20260615030000_perf_advisor_fixes.sql
  written but NOT applied. Needs live apply via MCP or Supabase dashboard SQL editor:
    ALTER POLICY "tar_insert_authenticated" ON public.teacher_access_requests
      WITH CHECK ((select auth.uid()) IS NOT NULL);
    CREATE INDEX IF NOT EXISTS idx_word_pacts_player2_id ON public.word_pacts (player2_id);

  /he INP REGRESSION: 158ms->264ms (+67%, n=58 both nights, above n>=50 floor).
  Watch 2 more nights. If still >200ms on 06-17, profile with Chrome DevTools INP
  attribution on /he. Suspects: RTL interaction handler cost, Rubik font swap on
  first interaction, applyAccentVar running synchronously in click path.

  /he CLS still POOR (0.460, worsening from 0.386). Investigate with CLS timeline
  in DevTools with locale=he.
---
