status: research-only
attempted: fix top Sentry/PostHog errors from brief (r.clear TypeError, duration TypeError), skip stale restores per brief priority
files_touched: docs/nightly/triage-queue.md, docs/nightly/reports/2026-09-20.md
next_steps: |
  Both top brief items (Sentry JAVASCRIPT-NEXTJS-25R, PostHog 019e8e6b) traced to the
  same already-fixed bug (bec779286, 2026-09-19) -- no code change needed.
  Sentry issue could not be marked resolved via MCP (403, read-only token) -- needs a
  manual click in the Sentry UI (see triage-queue.md).
  Did NOT get to: the 1 security_definer_view supabase advisor hit (untriaged), or any
  of the 6 stale restore-queue tags -- next run should pick those up first since the
  sentry/posthog sweep is now clear.
