status: shipped
files_touched:
  - fe-next/supabase/migrations/20260623030000_perf_fk_index_and_drop_unused.sql
  - docs/nightly/perf-baseline.json
  - docs/nightly/reports/2026-06-23.md
backend_fixes:
  - idx_word_clubs_owner_id: CREATE INDEX IF NOT EXISTS (advisor: unindexed FK word_clubs_owner_id_fkey)
  - idx_word_wheel_attempts_catchup: DROP INDEX IF EXISTS (advisor: unused index, 0 scans)
frontend_findings:
  - /es/multiplayer p75 LCP REGRESSION 3127->3876ms (+24%, 7d n=75 both runs pass n>=50 gate). Hypothesis: LobbyDailyEmber (useDailyChallengeStatus, added 06-21). No code fix this run — needs profiler.
  - /he p75 LCP improved 3297->3112ms (-5.8%). INP 264->280ms (+6%, below 20% threshold).
  - 24h routes all sub-floor (n<50) — deferred per mandatory sample gate.
next_steps:
  - Profile /es/multiplayer in Chrome DevTools to confirm LobbyDailyEmber as LCP blocker
  - If confirmed: defer useDailyChallengeStatus fetch to non-blocking position (after paint)
  - auth_db_connections_absolute: human config change in Supabase dashboard (pool strategy absolute->percentage)
