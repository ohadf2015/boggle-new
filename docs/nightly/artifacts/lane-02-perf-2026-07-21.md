status: shipped
files_touched:
  - fe-next/components/landing/LandingView.tsx
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson
impact_checks:
  cls-verify-07-14: REGRESSED (0.674 vs 0.369 baseline — pre-existing, matches 07-20)
  lcp-animatepresence-07-18: IMPROVED (4836ms vs 5301ms baseline)
fix: LandingView ad-banner spacer always rendered in SSR (hidden sm:block) — ads load into pre-committed 104px slot, no post-hydration layout shift on desktop web
next_steps:
  - Check /es/multiplayer CLS regression — apply same always-render fix as 07-20 MP fix
  - Homepage CLS 0.921 has additional sources (NativeLanguageBanner, EventBanner) — check in 4d if spacer fix alone moves it
  - Supabase MCP absent (25th night) — 6 migrations pending, drain on next connected night
