status: research-only
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson (verdict appended)
  - docs/nightly/reports/2026-07-10.md (lane 02 section appended)

findings:
  - Supabase MCP absent (15th consecutive night) — 4 migrations pending apply, RLS initplan impact check DEFERRED
  - PostHog: 0 routes above n≥50 floor (5th consecutive night) — all LCP regressions in brief are sub-floor single samples, not actionable
  - Multiplayer hero (RoomListView.tsx:201) confirmed has priority prop — already optimized
  - MultiplayerFlow uses ssr:true for lobby FCP — already optimized (documented at lines 65-77)
  - VirtualGamesList.tsx:39:23 gate lint error CONFIRMED RESOLVED — eslint exits clean; remove from recurring-pitfalls watch list
  - Bundle stable: dictionary chunk 6449KB (lazy), all top-5 within 15KB of yesterday baseline

next_steps:
  - HUMAN: mint never-expire Supabase PAT (SUPABASE_ACCESS_TOKEN) — 15th night blocked; apply 4 pending migrations
  - Remove VirtualGamesList:39:23 from learnings "What to avoid" — no longer failing
  - When PostHog n>=50 data returns: recheck /en/multiplayer and /en/daily/word-wheel LCP with p75 stats
