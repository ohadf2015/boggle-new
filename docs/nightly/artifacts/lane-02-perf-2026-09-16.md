status: research-only
attempted: fix top LCP regression on /multiplayer?quickPlay=true (104s) + address unindexed FK advisor warning on vocabulary_lessons
files_touched: docs/nightly/perf-baseline.json, docs/nightly/reports/2026-09-16.md
next_steps: |
  - All 5 brief LCP items verified as query-string single-session noise (bare-pathname n<50 for every route) — no fix, no regression. Same pattern as 09-15; if this recurs a 3rd night, worth asking whether the intelligence-brief LCP source should group by pathname before scoring.
  - vocabulary_lessons unindexed-FK advisor item CLOSED (not carried): table has 18 rows via Management-API raw-SQL, index would have zero read benefit. Stop re-flagging unless row count grows materially.
  - Supabase MCP failed to connect for the 3rd consecutive night (09-14/15/16). Management API raw-SQL fallback (curl + SUPABASE_ACCESS_TOKEN, POST /v1/projects/{ref}/database/query) worked fine and unblocked both advisor items — worth promoting to a real scripts/nightly/lib/supabase-query.sh helper (per "anything repeatable -> script it") so future lanes don't wait on the flaky MCP.
  - realtime.list_changes still dominates pg_stat_statements (3.6M+0.77M calls) — confirms existing 50-supabase-perf.md guardrail, founder-scope call on publication scope, not actionable here.
  - auth_db_connections_absolute still a dashboard-only fix, carried to human queue (unchanged since 09-01).
