status: shipped
files_touched:
  - fe-next/app/[locale]/(home)/page.tsx  (added winner.webp preload link)
  - docs/nightly/perf-baseline.json       (updated to 29th snapshot)
  - docs/nightly/reports/2026-06-25.md    (lane 02 section appended)

what_was_done: >
  Backend advisor items confirmed stale: word_clubs FK index already exists
  in migration 20260322700000. idx_word_wheel_attempts_catchup queued for
  Supabase MCP verify (idx_scan=0 → safe to drop).
  Frontend: added explicit <link rel=preload fetchPriority=high> for
  /mascot/winner.webp to homepage server component (page.tsx). Fires before
  React client subtree emits <Image priority> preload, giving browser
  ~100-300ms head-start on the 402KB LCP image (/he route, n=67, 2927ms).
  Mirrors established pattern from /multiplayer/page.tsx (play.webp).

next_steps: >
  - Chrome profiler trace on /es/multiplayer: identify LCP element and
    whether socket-join re-renders cause 5932ms.
  - Verify idx_word_wheel_attempts_catchup idx_scan via Supabase MCP;
    drop if zero (reduces write overhead on daily_word_wheel_attempts).
  - Monitor /he LCP in next 7d PostHog window: expect winner.webp preload
    to show measurable improvement.
  - /he INP 352ms and /es/multiplayer INP 368ms both POOR — needs profiler.
