status: shipped
files_touched:
  - fe-next/components/landing/HeroStyleMascot.tsx (added fetchPriority="high" to animated hero mascot — matches server preload, LCP element per in-code comment)
  - docs/nightly/impact-ledger.ndjson (3 entries: 2 impact-check verdicts neutral/healthy, 1 shipped-fix entry)
  - docs/nightly/perf-baseline.json (today's snapshot + notes)
  - supabase: applied stale migration 20260727020000_fix_subscriptions_rls_initplan.sql live (was pending 3+ nights, Supabase MCP/Docker unavailable) via Management API raw-SQL fallback; recorded in supabase_migrations.schema_migrations
next_steps: |
  - Supabase MCP did not connect during this lane's toolset window (turn 1, and stayed absent) — used
    Management API REST fallback (api.supabase.com/v1/projects/<ref>/database/query with
    SUPABASE_ACCESS_TOKEN) successfully instead. Worth wiring this as a documented fallback path
    (scripts/nightly/lib/) for future nights when MCP is slow/down, since it worked cleanly here.
  - Did not re-query PostHog for fresh 24h LCP numbers per brief-first contract (brief already gave
    targets). Tomorrow's lane should verify HeroStyleMascot fetchPriority fix moved LCP on `/` and `/es`.
  - CLS crisis (0.9+ on multiplayer/homepage, 6+ nights open) still unaddressed — needs headless
    Layout-Shifts capture per nightly-learnings open watch, out of scope for tonight's time budget.
  - Practice-classic eager sandbox import (dynamic() candidate) still deferred, needs local bundle
    size verification before shipping.
