status: research-only
attempted: CLS impact check for /en/multiplayer + root-cause investigation + perf-baseline update
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson
next_steps: |
  CLS root cause on /en/multiplayer still unresolved (0.975→0.967 post-07-20 fix, barely moved).
  Hypotheses for next run (DevTools QA needed):
  1. AnimatePresence blocks serializing opacity:0 into SSR on multiplayer page (same as homepage 07-18)
  2. RoomListView skeleton→rooms swap: height mismatch when socket payload arrives
  NativeLanguageBanner (layout.tsx:677) contributes ~0.07 CLS on appearance — move to fixed
  positioning or reserve height in layout to remove from flow. But cannot explain 0.97 alone.
  Supabase MCP not probed this run — 6 migrations still pending (FK index carry_count=6).
  Homepage LCP 4716ms — within noise of 4836 baseline, no regression.
