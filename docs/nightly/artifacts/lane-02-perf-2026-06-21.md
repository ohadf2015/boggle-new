status: shipped
files_touched:
  - fe-next/components/lobby/LobbyDailyEmber.tsx  (INP fix: analytics→useEffect, popover hidden attr)
  - docs/nightly/perf-baseline.json               (updated with 7d readings)
  - docs/nightly/reports/2026-06-21.md            (created, lane 02 section)

findings:
  - /he LCP RESOLVED: 6273ms→3464ms (7d n=70) — off watch list
  - /he INP worsening: 264→336ms (3rd consecutive regression, n=70) — root cause unknown, deferred
  - /es/multiplayer INP 328ms n=59: first above n≥50 floor; LobbyDailyEmber (ecda82ce8) suspected cause

fix_applied: LobbyDailyEmber.tsx
  1. posthog.capture moved from setState updater → useEffect (fires post-paint)
  2. {open && <popover>} → hidden={!open} (no DOM mount on click)
  eslint: clean

next_steps:
  - /he INP (336ms worsening): profile homepage with DevTools INP breakdown; suspect ModeCoach bundle or AnimatePresence transitions
  - /es/multiplayer INP: verify improvement in next 7d reading (n should stay ≥50)
  - Supabase MCP token: still expired (3rd night), security migration 20260619030000 pending
  - Backend pg_stat_statements: not queried this run — check top slow queries next lane
