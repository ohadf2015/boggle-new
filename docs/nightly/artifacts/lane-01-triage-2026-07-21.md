status: partial
attempted: Fix CapacitorGameConnect.then() error, verify word_pacts index impact, address RLS/security advisors (upsert_push_token SECURITY DEFINER, teacher_access_requests always-true policy)
files_touched:
  - docs/nightly/impact-ledger.ndjson (2 entries appended: word_pacts verdict + GameConnect resolve)
  - docs/nightly/triage-queue.md (6 entries for 2026-07-21)
  - docs/nightly/reports/2026-07-21.md (Lane 1 section appended)
next_steps:
  - Human: resolve Sentry issue 132085085 (CapacitorGameConnect) in Sentry UI — MCP returned 403
  - Human: fix teacher_access_requests tar_insert_any policy (allow authenticated only; DROP + CREATE scoped to authenticated WITH CHECK auth.uid() = user_id)
  - Next lane: investigate PostHog TypeError Load failed (68 occ, 18 users) — check word-wheel WASM fetch for missing error boundary
  - Next lane: investigate React #418 hydration mismatch (9 users, ongoing) — reproduce in dev with non-minified build to find offending component
