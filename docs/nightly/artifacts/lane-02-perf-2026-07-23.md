---
status: research-only
attempted: Check MP CLS impact verdict, investigate CLS root cause, apply Supabase FK index migration, update perf baseline
files_touched:
  - docs/nightly/perf-baseline.json (updated 7-day web vitals snapshot)
  - docs/nightly/impact-ledger.ndjson (MP CLS verdict appended)
  - docs/nightly/reports/2026-07-23.md (lane 02 section appended)
next_steps: |
  1. CLS root cause needs visual QA — socket state transitions on /en/multiplayer (connecting→lobby→in-game DOM swap) are prime suspect. Use Chrome DevTools Layout Shifts panel or PerformanceObserver with sources to attribute the shifting element.
  2. Supabase MCP not available this run — apply 6 pending migrations (esp. web_vitals_player_id_fk_index carry=7d) on next MCP-up night.
  3. NativeLanguageBanner conditional mount is CLS-causing for browser-locale-mismatch users — consider min-height reservation or SSR skeleton.
---

## Impact Check
MP CLS (7-day p75): **0.961** vs baseline 0.979 — verdict: **NEUTRAL** (<2%, 07-20 fix didn't move it meaningfully)

## Web Vitals (7-day, n≥50)
| Route | LCP p75 | INP p75 | CLS p75 | n |
|---|---|---|---|---|
| /en/multiplayer | 1547ms | 210ms | 0.961 | 143 |
| / | 4870ms | 196ms | 0.903 | 69 |
| /es/multiplayer | 3785ms | 424ms | 0.565 | 138 |
| /en | 3142ms | 300ms | 0.972 | 64 |
| /es/juego-palabras-multijugador | 3050ms | 172ms | 0.263 | 65 |

## Root Cause Findings
- **EventBanner**: already ssr:false; animation is CSS transform (not CLS). Ruled out.
- **AutoHideHeader**: correctly renders spacer div matching header dims. Ruled out.
- **NativeLanguageBanner**: transform animation (not CLS); conditional mount is CLS-causing for locale-mismatch users.
- **Prime suspect**: socket state machine DOM swaps on /en/multiplayer — needs visual QA to confirm.
