---
status: shipped
files_touched:
  - fe-next/app/[locale]/multiplayer/page.tsx (force-dynamic → revalidate=3600)
  - docs/nightly/perf-baseline.json (updated snapshot)
  - Supabase migration applied via MCP: drop_unused_idx_blocked_entities_expires_at
next_steps:
  - Monitor /es/multiplayer LCP next night — should drop from ~4991ms toward ~1700ms with ISR cache warm
  - INP 352ms on /es/multiplayer still undiagnosed — needs profiling when 24h n>=50 floor is met
  - auth_db_connections_absolute advisor still open (switch to percentage-based; low risk, deferred)
  - word_pacts FK index: still 1 row, skip; re-evaluate at 1000+ rows
---

## What ran

### Backend
- **Dropped `idx_blocked_entities_expires_at`** (Supabase migration applied via MCP): idx_scan=0, idx_tup_read=0 since creation. blocked_entities queries filter by `block_type`+`value`, never `expires_at`. Removes write overhead on every INSERT/UPDATE with zero read cost.
- **WAL parser dominance** (83.5% DB CPU): infrastructure Realtime internal — no app-code lever. Audit verdict INVESTIGATE since 06-05, unchanged.
- **word_pacts FK index**: still 1 row — seq scan optimal, skipped.
- **pg_timezone_names** (343ms/call): PostgREST catalog query on schema-cache reload — infrastructure, cannot fix from app.
- **Schema introspection** (2498ms mean, 19 calls): PostgREST cache rebuild on each NOTIFY pgrst.reload — infrastructure.

### Frontend
- **`/multiplayer/page.tsx` `force-dynamic` → `revalidate=3600`**: No searchParams, cookies, auth.getUser, or headers() anywhere in the server component or generatePageMetadata. force-dynamic was unnecessary dead weight. ISR will cache rendered HTML for 1h — same fix that reduced daily route LCP from ~4903ms to ~1706ms (06-09).
- **PostHog 24h window**: 0 routes above n>=50 floor. 7-day: only `/es/multiplayer` (n=142, LCP=4991ms +15% vs baseline, INP=352ms unchanged).
- ESLint: clean on changed file.
