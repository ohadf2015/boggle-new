status: shipped
attempted: fix top LCP regressions (leaderboard 8130ms) + backend advisor items from brief
files_touched:
  - fe-next/app/[locale]/leaderboard/PageClientNoSsr.tsx (added loading fallback to ssr:false dynamic import)
  - docs/nightly/perf-baseline.json
  - docs/nightly/reports/2026-08-08.md
  - docs/nightly/impact-ledger.ndjson
next_steps: |
  Re-measure /en/leaderboard LCP with n>=50 in 3 days (impact ledger entry).
  Backend advisor items (unindexed FK on vocabulary_lessons, Auth DB connection %) still
  deferred — near-zero row counts / dashboard-only setting, no measurable win found yet.
  Consider full SSR for leaderboard (removing ssr:false) as a bigger follow-up — deferred
  because it risks the isSupabaseEnabled SSR/client flash (Class 1 dual-source-of-truth);
  needs more than a 10-min lane to do safely.
