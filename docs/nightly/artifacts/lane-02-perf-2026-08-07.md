status: research-only
attempted: fix unindexed FK on vocabulary_lessons_classroom_id_fkey (backend) + LCP fix on /multiplayer room page and /daily/word-wheel (frontend), per intel brief top items.
files_touched: docs/nightly/perf-baseline.json, docs/nightly/reports/2026-08-07.md, docs/nightly/impact-ledger.ndjson (docs-only, no fe-next/ code changed)
next_steps: All 4 brief-flagged frontend LCP routes were below the 50-sample floor tonight (n=21/11/5/1 over 24h) -- re-check with a wider window until n>=50 before treating as regressions. Backend unindexed-FK target (vocabulary_lessons) is a 2-row table 3 nights running -- either drop it from the brief's ranking or wait until the table has real volume. word-wheel canvas mount-size fix (8443b0d3f) landed just before this lane; re-measure its LCP route once fresh samples clear the floor to confirm improvement.
