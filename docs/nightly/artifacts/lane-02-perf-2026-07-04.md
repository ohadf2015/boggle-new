---
status: research-only
attempted: Fix top backend perf issues (unindexed FK, unused index) and frontend LCP wins (homepage/leaderboard)
files_touched: docs/nightly/perf-baseline.json
next_steps: |
  - HUMAN ACTION REQUIRED: Supabase MCP absent 9 nights — mint never-expire PAT into nightly env.
    2 migrations pending: 20260628 (RLS initplan+FK index, carry=8), 20260701 (drop unused index, carry=3)
  - Traffic dip: all routes sub-floor (n<50). Homepage LCP fix documented (useState gate on LandingView).
  - Opacity-0 Framer issue NOT reproducible — may already be resolved.
  - 6.4MB dictionary chunk confirmed lazy-loaded, not a LCP blocker.
---

## Backend
Supabase MCP absent (9th night). 2 migrations written but unapplied (carry_count 8 and 3).

## Frontend
PostHog: 0 routes n>=50 in 24h or 7d. /he peaked n=42 (was 60 last week — traffic dip).
All routes sub-floor: no regression verdicts possible.
- Opacity-0 Framer Motion issue NOT found in DailyChallengeLanding.tsx/DailyRedirect.tsx — may be resolved.
- LandingView: no <img> without priority, image loading not the homepage LCP bottleneck.
- Homepage LCP root cause documented: useState(mounted)+showFTUE gates LandingView render.
  Fix: render LandingView unconditionally; gate FTUE overlay separately. Prioritize when / reaches n>=50.

## Bundle
Total uncompressed: 41,048KB. 6.4MB dictionary chunk (7fd32332) confirmed LAZY — not critical path.
