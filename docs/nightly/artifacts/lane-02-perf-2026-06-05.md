status: partial
files_touched:
  - docs/nightly/perf-baseline.json (updated with today's snapshot)
  - supabase migration: idx_web_vitals_player_id (applied live, not a file)

backend_fix_shipped:
  - idx_web_vitals_player_id: btree index on public.web_vitals(player_id)
  - Evidence: Supabase advisor flagged + EXPLAIN ANALYZE confirmed 3207ms seq scan on 99111 rows
  - Migration applied successfully via apply_migration

frontend_diagnosis_only:
  - /en/multiplayer CLS regressed 0.057→0.29 (x5 worse, n=16)
  - Root cause: AutoHideHeader (fe-next/components/AutoHideHeader.tsx:34) returns null
    when isInGame flips post-hydration — causes ~60px layout shift
  - Fix NOT shipped (time budget exhausted before safe edit)

next_steps:
  - Fix AutoHideHeader CLS: replace `return null` at line 34 with a zero-height placeholder
    so content below does not shift. File: fe-next/components/AutoHideHeader.tsx
  - Investigate /he/daily/word-wheel LCP 7864ms (high priority, undiagnosed)
  - Investigate /he/daily LCP 5932ms
  - Confirm idx_web_vitals_player_id improved query time via EXPLAIN on next run
