---
status: partial
attempted: Fix FK index on word_club_members + drop unused idx_word_wheel_attempts_catchup + remove h1 animation delay causing LCP opacity-0 blind spot
files_touched:
  - fe-next/components/landing/LandingHero.tsx (h1 fadeInUp delay 0.15s → 0s)
  - docs/nightly/perf-baseline.json (updated with 2026-07-05 snapshot)
next_steps: |
  BACKEND (BLOCKED — Supabase MCP absent 10 nights):
  - Apply migration 20260628020000_rls_initplan_fix_and_word_clubs_fk_index.sql (carry=9)
  - Apply migration 20260701020000_drop_unused_index_word_wheel_catchup.sql (carry=4)
  - Human action required: mint never-expire SUPABASE_ACCESS_TOKEN PAT, inject into nightly env
  FRONTEND (next lane):
  - LazyMotion refactor: ModeCard.tsx imports { m } from framer-motion synchronously → main landing bundle.
    Wrap with LazyMotion + loadFeatures (domAnimation) in LandingChallengeCards or LandingView.
    Estimated ~30KB gzip reduction from main chunk. Multi-file, needs careful testing.
  - Double-hydration: LandingView renders both HomeHub (md:hidden) + desktop tree simultaneously.
    Both are in DOM and hydrated. Architectural fix: split into separate RSC-rendered subtrees.
  - Auth DB connections: switch Railway Auth server from absolute (10) to percentage-based pooler
    (Supabase dashboard: Auth Settings → Connection pooler strategy). Human-only change.
notes: |
  All PostHog routes sub-floor (n<50 in 24h). No regression verdicts possible.
  h1 fix: CSS animation-fill-mode:both with 0.15s delay = LCP element at opacity:0 for 150ms
  before browser counts it as visible. Removing delay fires LCP ~150ms earlier.
  ESLint clean (exit 0). Gate will verify.
---
