status: research-only
attempted: fix top frontend LCP regressions (tryshopli mood pages + MP room pages) and check supabase advisor items (unindexed FK, unused index)
files_touched: docs/nightly/perf-baseline.json, docs/nightly/perf-watch.md
next_steps: |
  - Brief's top 3 items (tryshopli.com/il/mood/*) are NOT in this repo -- external site, no code here to fix. Fix the brief generator to scope to lexiclash-owned domains only.
  - Brief's items 4-5 (/multiplayer?room=XXX) are structurally unmeasurable per-URL (unique room code per URL, n always ~1). 3rd consecutive night this pattern wastes lane time -- fix brief generator to group PostHog LCP by route template (strip query string) not exact URL. Aggregate /multiplayer route n=52/24h if real work is needed there.
  - Backend advisor items (unindexed FK vocabulary_lessons_classroom_id_fkey, unused index idx_word_pacts_player2_id, auth_db_connections_absolute) carried unchanged 3+ nights -- tables still tiny (2 rows / 1 row per last confirmed check), no measurable EXPLAIN ANALYZE benefit expected. auth_db_connections_absolute is a dashboard setting, not a migration -- human queue.
  - supabase MCP did not connect within this lane's time budget tonight (ToolSearch returned no supabase tools after 2 checks); no local REST fallback script exists in scripts/nightly/lib/ for supabase (only posthog-query.sh). Consider adding one, same pattern as posthog helper, so backend checks aren't blocked on MCP boot timing.
