---
status: research-only
attempted: Impact-check 07-06 index migration, verify all LCP optimizations, update perf baseline
files_touched:
  - docs/nightly/perf-baseline.json (updated 2026-07-09 snapshot)
  - docs/nightly/impact-ledger.ndjson (appended deferred verdict for 02-perf-2026-07-06-qpc-index-cleanup)
next_steps: |
  Supabase MCP absent (14th night) — HUMAN must mint never-expire PAT.
  Once MCP up: run impact-check query (indexname/idx_scan on quick_play_challenges + word_club_members).
  Apply 4 pending migrations (20260628/0701/0706/0707).
  PostHog 0 routes above n≥50 (4th consecutive night) — no regression verdicts until traffic grows.
  Auth connections absolute→percentage: Supabase dashboard (human).
notes: |
  All prior LCP fixes confirmed in-place — no new code changes needed:
  homepage winner.webp preload correct (unoptimized=raw URL),
  fetchLandingData parallelized, multiplayer arena-hub-hero preload wired,
  next/font self-hosted (no cross-origin font fetch),
  below-fold images default-lazy (no priority misuse),
  bundle within 3KB of prior baseline (no regression),
  web_vitals FK advisor warning = stale data (index already exists).
---
