status: shipped
attempted: Drop unused idx_user_reports_target (backend), diagnose /es/multiplayer INP regression + /en/daily LCP, update perf baseline
files_touched:
  - docs/nightly/perf-baseline.json (updated 2026-06-13 snapshot, first bundle baseline)
  - migration drop_unused_idx_user_reports_target applied via Supabase MCP (0 idx_scans, 0 rows — pure write overhead)
next_steps:
  - /es/multiplayer INP 452ms — MONITOR 2 more nights (both n>=50). If still elevated 06-15, profile lobby interaction handlers.
  - /en/daily LCP (brief stale, below n>=50 floor) — ISR fix viable once floor confirmed (searchParams only in generateMetadata, not page component).
  - word_pacts FK — defer until 1000+ rows.
  - Auth DB connections absolute→percentage advisory — deferred, low risk at current scale.
  - Bundle baseline set (40325KB uncompressed, top chunk 6085KB) — tomorrow can detect regressions.
