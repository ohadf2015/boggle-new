status: research-only
attempted: impact-check education-hero LCP verdict; then top brief backend (auth_db_connections/unindexed FK) + frontend (esl-word-games / word-wheel LCP) fixes.
files_touched: docs/nightly/impact-ledger.ndjson (verdict append), docs/nightly/perf-baseline.json (rewrite), docs/nightly/perf-watch.md (append), docs/nightly/reports/2026-08-26.md (append)
next_steps: supabase MCP didn't connect 2 nights running -- backend brief items (auth_db_connections toggle, vocabulary_lessons FK index) still unactionable, need MCP up to EXPLAIN ANALYZE before indexing. word-wheel daily LCP (12280ms) root-caused as CSR-gate (dynamic()+Suspense + client-only useLanguage() loading fallback) -- fix is SSR a static loading shell + consider ssr:true outer shell like the MultiplayerFlow precedent; WordWheelChallenge.tsx is 500+ lines of tested game logic so needs a dedicated lane/human pass, not a blind edit. education-hero eager-LCP fix confirmed IMPROVED (8460->2608ms).
