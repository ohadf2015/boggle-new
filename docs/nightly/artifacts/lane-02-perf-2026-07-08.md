status: shipped
files_touched:
  - fe-next/lib/landing/fetchLandingData.ts (parallelized season_rpc + independent queries)
  - docs/nightly/perf-baseline.json (updated)
  - docs/nightly/reports/2026-07-08.md (appended)
  - docs/nightly/impact-ledger.ndjson (appended)

change_summary: >
  fetchLandingDataUncached had a serial waterfall: season_rpc ran alone first,
  then game_results + daily_word_hunt_stats + fetchGameModeStats started.
  None of those three depend on the season ID. Fixed: all four now run in
  parallel via a single Promise.all. Leaderboard still waits for currentSeasonId.
  Estimated saving: 50-150ms per cold cache miss (TTL=30s). ESLint clean.

next_steps: >
  - Supabase MCP token still absent (13th night) — 4 migrations pending apply
    (20260628, 20260701, 20260706, 20260707). Human must mint never-expire PAT.
  - Auth DB connections absolute→percentage: human Supabase dashboard config.
  - PostHog still 0 routes n>=50 — can't measure LCP regression verdicts until
    traffic grows. Check fetchLandingData impact in 3 days.
