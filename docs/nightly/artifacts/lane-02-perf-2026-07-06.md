status: partial
attempted: Fix RLS initplan on quick_play_challenges, add FK index on word_club_members, investigate LCP regressions on /en/daily/word-wheel (4180ms) and /en/multiplayer (3802ms)
files_touched:
  - fe-next/supabase/migrations/20260706020000_quick_play_challenges_index_cleanup.sql
  - docs/nightly/perf-baseline.json
  - docs/nightly/reports/2026-07-06.md
next_steps: |
  CRITICAL: Human must mint never-expire Supabase PAT — MCP absent 11 consecutive nights.
  3 migrations written but unapplied (20260628, 20260701, 20260706).
  quick_play_challenges_accept RLS initplan fix needs pg_policies query first:
    SELECT polname, pg_get_expr(polqual, polrelid) FROM pg_policy
    WHERE polrelid = 'public.quick_play_challenges'::regclass;
  Then DROP+CREATE POLICY with (select auth.<fn>()) wrapper.
  Frontend: LazyMotion refactor for ModeCard (framer-motion in critical path) — high-priority open watch.
