status: partial
attempted: Fix SECURITY DEFINER upsert_push_token, chat Enter key sends instead of newline, special tiles in MP bots not working; queue coin-grant and feature requests
files_touched:
  - fe-next/components/friends/messaging/MessageComposer.tsx
next_steps: >
  Investigate MP special tiles (ice/explosion) with bots — not found by quick
  grep, needs server-side socket handler deep search. upsert_push_token
  SECURITY DEFINER advisory: authenticated callers are intentional; review-by-eod.
