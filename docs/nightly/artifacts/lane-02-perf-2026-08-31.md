status: shipped
attempted: fix top brief items — sv/singleplayer autoStart=bots LCP (18.1s), MP room LCP (8s), unindexed FK on vocabulary_lessons, unused index on word_pacts; update perf-baseline.json
files_touched: fe-next/lib/boardSelection.ts (pickRichestBoardClient default k 6->3, halves synchronous board-gen work blocking single-player grid paint, shared by 7 call sites), docs/nightly/perf-baseline.json, docs/nightly/reports/2026-08-31.md, docs/nightly/impact-ledger.ndjson
next_steps: |
  - Backend blocked 4th consecutive night: supabase MCP never connected (08-25/26/27/31). No REST/curl fallback script exists under scripts/nightly/lib — worth scripting one (per core principle: anything repeatable -> script it) so a future lane isn't fully blocked when MCP flakes. FK index (vocabulary_lessons_classroom_id_fkey) and unused-index drop (idx_word_pacts_player2_id) both still need EXPLAIN ANALYZE / idx_scan confirmation before any migration — untouched.
  - Frontend: verified every brief LCP item directly against PostHog (raw HogQL, not the brief's precomputed score) — ALL below n=50 floor (sv/singleplayer?autoStart=bots n=1, MP room=XXX n<10, en/quests n=2). This is the SAME per-query-param-URL noise pattern logged 3x already in perf-watch.md since 08-07. The nightly brief generator should group LCP by route template (strip query params) before scoring — recommend lane 7 or a scripts/nightly change to fix this at the source so it stops wasting lane budget on unmeasurable per-URL "regressions".
  - Only /en/multiplayer cleared the sample floor tonight (n=59, p75 LCP 1546ms, GOOD, no regression).
  - Code-inspected singleplayer PageClient.tsx (38 lines): SinglePlayerView is `next/dynamic` with `ssr:false` — same CSR-game-shell-blocks-LCP architecture already diagnosed for MP lobby and word-wheel daily (perf-watch.md, ~4-6h SSR-skeleton fix, human queue). Not a new bug, not safe to fix same-night.
  - Bundle: no fresh build run (forbidden near cutoff); reused last night's .next (23:45 08-30) as a snapshot only, not a same-day delta signal.
