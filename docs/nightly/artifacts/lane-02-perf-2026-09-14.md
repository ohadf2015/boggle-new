status: shipped
files_touched: fe-next/components/daily/WordWheelChallenge.tsx, docs/nightly/perf-baseline.json, docs/nightly/impact-ledger.ndjson, fe-next/docs/nightly/reports/2026-09-14.md
next_steps: eslint on WordWheelChallenge.tsx was launched but didn't return within lane budget (task b130clesa) -- gate will catch it if wrong, but re-check that output first thing if this lane's diff looks suspicious. /en/multiplayer CWV sample dropped 187->7 in 24h (traffic dip, not a fix) -- still needs DevTools INP/CLS profiling per perf-watch.md, unowned. Brief's other LCP routes (/he/profile, /he) all n<=2 -- too low-traffic to ever clear the 50-sample floor on a 24h window; consider widening lane's frontend query to 7d for these specific routes next time.
