status: shipped
attempted: fix top LCP regressions (lexiclash MP route) + supabase advisor items (unindexed FK, unused index) per brief
files_touched: docs/nightly/perf-baseline.json, docs/nightly/reports/2026-09-01.md, docs/nightly/impact-ledger.ndjson (no fe-next code files — no frontend fix qualified under the sample-gate rule); DB migration applied directly via supabase MCP: idx_vocabulary_lessons_classroom_id
next_steps: frontend brief generator still needs to group PostHog LCP by route template (strip query params) — same noise flagged since 2026-08-07, keeps burning lane time re-diagnosing single-URL n=1 "regressions". auth_db_connections_absolute advisor item is a dashboard toggle for human queue. word_pacts unused index closed as not worth dropping (16kB).
