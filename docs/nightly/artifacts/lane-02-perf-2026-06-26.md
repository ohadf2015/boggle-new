status: research-only
files_touched:
  - docs/nightly/perf-baseline.json

findings:
  - 24h PostHog: all routes sub-floor (n<50), no actionable regression
  - 7d vitals: /es/multiplayer LCP 5932ms (stable), /he LCP 2927ms (improving)
  - Supabase MCP did not connect this run — advisor items deferred
  - BUNDLE DISCOVERY: 6085KB chunk = @arvidbt/swedish-words bundled client-side.
    No direct import in source (grep clean). Turbopack picks it up via
    package.json dependencies. serverDicts.ts reads via fs (correct) but
    Turbopack static analysis still bundles it. Fix is clear and safe.

founder_directive_note: Word Tower UX/gameplay improvements = Lane 05/11, not Lane 02.

next_steps:
  - FIX (safe, high impact): add to next.config.ts serverExternalPackages:
    ['@arvidbt/swedish-words', 'an-array-of-english-words', 'an-array-of-spanish-words']
    All three are server-only API route deps. Expected: ~6MB+ removed from client bundle.
  - Identify 3284KB chunk (107js-lrs4mq0.js) contents
  - Verify + drop idx_word_wheel_attempts_catchup when Supabase MCP connects
  - Auth DB connections: human config in Supabase dashboard
