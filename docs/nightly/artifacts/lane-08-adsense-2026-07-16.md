status: shipped
files_touched:
  - fe-next/app/[locale]/education/page.tsx
next_steps: |
  - Blog cadence blocked on Higgsfield CLI sudo-install (hero image generation) — unblock to resume 1 blog/2 days cadence
  - /en/words/starting-with dead-letter set (f,i,o,w,c,d,y) already noindexed from prior runs; re-run dead_pages.py in ~7d to see if new letters drop to 0 clicks
  - No thin informational pages found tonight; if AdSense re-submission is considered, main gap is blog volume (0 new posts since ~07-06)
  - Consider adding BreadcrumbList JSON-LD to /en/guides (679w, lowest informational word count) on a future night
