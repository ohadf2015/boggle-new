status: shipped
attempted: run brief's 4 supabase impact-check queries (dropped indexes, FK indexes, web_vitals index, subscriptions RLS) + frontend LCP investigation on daily/word-wheel route
files_touched: docs/nightly/impact-ledger.ndjson, docs/nightly/perf-baseline.json, docs/nightly/reports/2026-07-31.md (docs only, no fe-next code changed)
next_steps: |
  Backend: all 4 impact checks verdict'd improved/neutral, 0 regressions -- nothing to fix tonight.
  Frontend READY-MADE FIX for tomorrow's lane (found via Explore agent, not shipped tonight -- ran out of safe verification time):
    components/daily/WordWheelChallenge.tsx:13-14 eagerly imports WordWheelGame + WordWheelResults,
    but WordWheelGame only renders during 'playing' phase and WordWheelResults only during 'completed'/'already-played'
    phase (usage at line 686 and 718). Convert both to next/dynamic() with a loading fallback -- same pattern already
    used for WordWheelChallenge itself in page.tsx. BEFORE shipping: check whether WordWheelGame renders a
    canvas/WebGL surface (WordWheelEffectsCanvas is imported nearby) -- if so it may need `ssr: false` like other
    game-board dynamic imports in this codebase. Also confirm chunk size >200KB gzip per the no-premature-code-splitting
    hard rule before committing.
  Also open (carried, not owned by this lane): MP CLS 0.92 socket-DOM-swap refactor (perf-watch.md, needs human 4-6h),
  98 unindexed_foreign_keys advisor INFO-lints (not actioned -- speculative-index hard rule, would need EXPLAIN ANALYZE
  per table against a real slow query first).
