status: shipped
attempted: live word-count audit prod + GSC dead-page check, pick <=2 highest-leverage informational-page fixes
files_touched: fe-next/app/[locale]/guides/PageClient.tsx
next_steps: word-count audit found zero thin informational pages tonight (all >500 real words; /ja 279 is the known CJK tokenizer artifact, not real thinness). dead_pages.py found zero noindex candidates. JSON-LD audit initially flagged glossary/about as missing structured data but both already ship full Organization/BreadcrumbList/AboutPage/DefinedTermSet schema inline (raw <script>, not the named wrapper components — false alarm from an identifier-based grep). Shipped: internal-linking gap — no informational page (about/blog/faq/guides) linked to /tools/word-solver. Added one contextual link to /guides "Further Reading" relatedReading list per locale (en/he/sv/ja/es), pointing at each locale's own /tools/word-solver path. Zero new prose, zero word-count added. eslint on changed file: clean (RC=0). Tomorrow: consider linking word-solver from /faq or /blog too; the pre-existing relatedReading hrefs for the OTHER 3 links (blog posts, best-online-word-games) are hardcoded to /en/... in every locale block — worth fixing to locale-correct paths in a future lane if those posts exist per-locale.
