status: shipped
attempted: restore stale drop 20260817-010002 (10 files), then Sentry/Supabase/PostHog top items
files_touched:
  - fe-next/components/education/EducationHeroBanner.tsx (loading=eager+fetchPriority=high for LCP, dropped dead Image import)
  - fe-next/components/multiplayer/JoinRoomModal.tsx (Loader2 spinner on isJoining)
  - docs/nightly/restore-queue.ndjson (partial-resolve 20260817-010002)
  - docs/nightly/reports/2026-08-25.md
  - docs/nightly/impact-ledger.ndjson
  - supabase migration (live, via MCP): fix_connections_daily_leaderboard_security_definer
next_steps: |
  Restore 20260817-010002 only partially recovered — growthTracking.ts/MultiplayerFlow.tsx
  (trackGuestJoin for room_joined_via_link) and GemHunt inventory-greyout (GemHuntWinScene +
  GemHuntPageClient) still sit only in the backup at
  ~/logs/lexi-nightly/salvaged-code-20260817-010002/, because the live files have diverged
  with newer uncommitted WIP (streak_ignition/next_step_shown events etc) that a blind
  rsync would have clobbered. Next run: hand-merge those 3 pieces on top of current state,
  or dispatch a dedicated lane. Did not touch Sentry/PostHog items from the brief
  (React #418, GSI_LOGGER FedCM, "Game hasn't started yet", "No estás en este juego",
  audio InvalidStateError) — ran out of budget after the Supabase fix + restore triage;
  pick those up first next time.
