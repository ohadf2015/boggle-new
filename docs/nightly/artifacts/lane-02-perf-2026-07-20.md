status: shipped
files_touched:
  - fe-next/app/[locale]/multiplayer/PageClient.tsx (CLS fix: remove `? null` that dropped AutoHideHeader spacer on reconnect)
next_steps:
  - Visual QA on /en/multiplayer: verify spacer doesn't create blank band above game UI
  - Check 7-day CLS delta in 3 days (baseline p75=0.979, expect improvement toward <0.25)
  - Supabase MCP still intermittent — 5 migrations pending apply (web_vitals FK index + 4 others)
  - Homepage CLS 0.903 unaddressed — secondary investigation needed (mascot/banner sources)
