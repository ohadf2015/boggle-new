status: shipped
files_touched: docs/nightly/mode-readiness.md, docs/nightly/reports/2026-09-12.md, docs/nightly/impact-ledger.ndjson
next_steps: wheel-rush stays Current at 80%. Visual-QA capture is now owner:review-by-eod (human-queue) after 5 deferred agent-nights promising a live 2-tab MP session that never landed — a future night should only re-attempt it if it can genuinely open the run with a dedicated ~10 min block, not squeeze it at the tail. Disconnect/refresh-mid-round live verification still open, can piggyback on the same human-run session. No code files touched tonight (verification-only), zero lint/build/test risk.
