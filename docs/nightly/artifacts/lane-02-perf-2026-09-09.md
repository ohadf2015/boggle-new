status: research-only
attempted: fix mp quickplay LCP (85s!) + daily/word-wheel LCP regressions; check FK index on vocabulary_lessons; update perf-baseline.json
files_touched: docs/nightly/perf-baseline.json, docs/nightly/perf-watch.md, docs/nightly/reports/2026-09-09.md, docs/nightly/impact-ledger.ndjson
next_steps: |
  Backend: closed. vocabulary_lessons FK index already shipped 09-01 (056_teacher_vocabulary_builder.sql:163-164),
  confirmed gone from fresh get_advisors(performance) output. auth_db_connections_absolute stays human queue
  (dashboard toggle, not code) -- unchanged since 09-01, stop re-flagging it.

  Frontend: the brief's 85515ms LCP on /en/multiplayer?quickPlay=true is sample noise (n=14, below the
  n>=50 floor) -- do NOT chase it. But querying the parent route WITHOUT the quickPlay filter found
  /en/multiplayer at n=187, the first night it clears the floor, failing all 3 CWV thresholds:
  LCP 2760ms, INP 504ms, CLS 0.265. Did not code a fix -- root cause needs real browser profiling to
  verify (CLS: RoomListView.tsx:309-332 skeleton is a fixed 3-card height, real room count/height varies;
  INP: PageClient.tsx is 811 lines / ~20 hooks mounted synchronously at lobby load) and agent-browser's
  standing cookie-consent-overlay blocker (memory, 20+ nights) means CLS can't be captured/verified
  headlessly right now. Logged full detail + suspected causes to docs/nightly/perf-watch.md.
  Tomorrow: if n stays >=50 for 2 more nights and thresholds still fail, that's the trigger to actually
  profile (Chrome DevTools INP/CLS attribution) before touching either file -- both already carry
  perf-work history (CLS improved 0.92->0.265 per memory since a prior fix), so a guess-fix risks
  regressing that. Also worth escalating the agent-browser cookie-consent blocker itself since it's now
  blocking this lane's ability to verify any MP fix, not just lane 11's visual QA.
