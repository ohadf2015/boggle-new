status: shipped
attempted: STEP 0 — improve one existing admin-beta game mode (self-select rotation), smallest coherent slice.
files_touched: fe-next/components/drills/MemoryHuntCompletePhase.tsx, fe-next/translations/{en,he,sv,ja,es,ru}.js
next_steps: Mode = Memory Hunt (brain-drill, admin-beta). Axis = variable-reward/feel. Added a "flawless clear" state (wordsFound===totalWords && no lives lost) that was previously indistinguishable from an 85%+ platinum run: a "celebration" Pixi burst (gated on useShouldReduceMotion) + a gold "flawless" badge chip, new i18n key `brain.drills.flawless` in all 6 locale files (5 required + ru, which is live per memory). eslint clean on the changed component. Rotation avoided: word-craft (3x recent), sealed-bid (2x recent), connections (graduated, not admin set). Next lane: verify wheel-rush and memory-hunt's actual admin-gate mechanism — grep found NO isAdmin/notFound gate on their page.tsx or GameModeSelector entries, contradicting the nightly-learnings "admin-gated set" list; may already be hub-hidden-only (soft gate) rather than route-gated, worth a dedicated audit.
