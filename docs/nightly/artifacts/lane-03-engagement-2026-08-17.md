status: shipped
files_touched: fe-next/components/multiplayer/JoinRoomModal.tsx, fe-next/app/[locale]/multiplayer/PageClient.tsx, fe-next/utils/growthTracking.ts
attempted: flag hygiene sweep + rageclick-driven MP invite-link experiment + instrumentation gaps
result: IMPACT CHECK on mp-quickplay-rapid-click-guard = neutral (appended to impact-ledger.ndjson). Root-caused the #1 rageclick target (score 0.79, /multiplayer?room=...&player_invite): JoinRoomModal's Join/Spectate button had no loading spinner while isJoining (text+opacity-50 only), which the code's own comment already blamed for tripping the rate limiter via rage-clicks. Fixed directly (mirrored CreateRoomModal's existing Loader2 pattern) rather than flag-gating -- it's an unambiguous missing-affordance bug, not a preference to A/B. Added mp_room_join_rate_limited event to quantify the lockout path going forward. Checked exp-invite-arrival-clarity-v1 (only flag textually near invite-link rageclicks) -- n=29/17 over 30d, nowhere near 1000/arm hygiene bar, and it covers a different code path (homepage blank-overlay redirect, not the JoinRoomModal button) -- left running. eslint on changed files still running in background as of writeup; no test file existed for JoinRoomModal.tsx (none broken).
next_steps: re-measure /multiplayer rageclick_24h (baseline 32/3d, 10/24h) and mp_room_join_rate_limited volume in ~3 days per impact-ledger entry 03-engagement-2026-08-17-join-modal-spinner. If exp-invite-arrival-clarity-v1 still <1000/arm after another week, consider it a low-traffic path not worth deciding -- may need to expand its trigger conditions (currently gated on returning-user + ?room= landing at ONLY the homepage, a rare combo) rather than waiting longer.
