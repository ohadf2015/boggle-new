status: research-only
files_touched: docs/nightly/perf-baseline.json
next_steps: |
  1. /he LCP 6273ms (CONFIRMED 2nd reading, n=82) — root cause unresolved.
     Suspects: HeroStyleMascot src changes post-hydration (usePlayerStyle context
     defeats priority preload); ModeCoach JS bundle on homepage. Next: DevTools
     LCP element identification on /he, check if usePlayerStyle can be seeded
     from a cookie for SSR hydration match.
  2. /he INP 264ms (+43%, NEW regression) — root cause: he.js 694KB (largest,
     +23% vs en.js 561KB). Fix: audit + remove dead translation keys from he.js
     to bring it under 600KB. Queue for lane 06 (SEO/i18n owner) or standalone task.
  3. Supabase MCP still unauthorized (2nd night) — security migration
     20260619030000 (REVOKE on update_difficulty_after_game + tighten
     tar_insert_any) still pending apply. Human action: refresh SUPABASE_ACCESS_TOKEN.
  4. Brief's daily-route LCP targets (/daily/word-wheel, /sv/daily) all below
     n<50 floor — INELIGIBLE by mandatory sample gate. No action warranted.
