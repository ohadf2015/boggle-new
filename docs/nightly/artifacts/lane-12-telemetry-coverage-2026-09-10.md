status: shipped
attempted: run posthog coverage audit (DEAD/CRATERED + per-mode completion), triage, fix one high-value gap
files_touched: scripts/nightly/prompts/12-telemetry-coverage.md (query template fix, corrected twice this run: (1) added properties.$host filter — this PostHog project is shared by ~12 unrelated apps; imposketch.io's own game_started events fabricated a phantom mode=none per-mode completion hole; (2) widened to $host='www.lexiclash.live' OR $host IS NULL after advisor review caught that server-side emits like mp_player_dropped/coins_awarded/drill_completed have NULL $host and a host-only filter would have silently DEAD-flagged them)
next_steps: TOP PRIORITY — the DEAD=69 count in tonight's report is likely overstated. event LIKE 'growth:%' carries large parallel volume for registry names (growth:game_started 1205/7d, growth:game_completed 459/7d, growth:signup_prompt_shown 205/7d, etc) that nightly_coverage_classify in scripts/nightly/lib/posthog-coverage.sh never counts (it groups by bare event name only). This is the 6-week-open "classifier false-positives" item from nightly-learnings, confirmed still live tonight. Fix nightly_extract_growth_events/nightly_coverage_classify to UNION bare + growth:-prefixed volume per event before classifying DEAD — do this before trusting any DEAD/CRATERED count from this lane again. SECOND: singleplayer mode has the lowest completion rate of any mode (609 started/186 completed = 30.5% in 14d) but advisor flagged a likely cause — SinglePlayerGame.tsx:70 and useSinglePlayerEffects.ts:92 both call trackGameStart('singleplayer', ...) with no dedupe (unlike trackGameEnd's endEmittedModes guard), so the denominator may be ~2x inflated; 186/305 = 61%, in line with other modes. Check for doubled starts before assuming a missing end-call. mp_quickplay_seeking still CRATERED 2 nights running (47->10 w/w) but game_started also fell ~56% w/w same window — general traffic dip, not a regression; don't re-flag unless it diverges from overall traffic trend.
