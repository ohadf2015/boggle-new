status: shipped
files_touched:
  - fe-next/components/daily/WordWheelPixiRing.tsx (null-guard fix on double-destroy path)
  - supabase migration: revoke_anon_grant_offerwall_coins (REVOKE EXECUTE on public.grant_offerwall_coins FROM anon, public)
next_steps:
  - Verify offerwall ayet postback still works after REVOKE (route uses service role, not anon — should be fine)
  - Mark Sentry JAVASCRIPT-NEXTJS-1KQ (churn-signals 502) as resolved — already fixed in getBearerUser.ts
  - Backend: audit upsert_player_word SECURITY DEFINER — does it need to bypass RLS for community word writes? If not, switch to SECURITY INVOKER.
  - Backend: trace "relation profiles does not exist" at POST /api/coins — search_path issue in a DB function, may be self-healed.
