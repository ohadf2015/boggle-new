status: shipped
files_touched:
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson (6 entries appended)
  - docs/nightly/reports/2026-07-26.md (lane 02 section appended)
  - supabase DB (6 migrations applied directly via MCP execute_sql)

backend_wins:
  - RLS InitPlan fix: quest_achievement_feed (auth.role per-query not per-row)
  - RLS InitPlan fix: quick_play_challenges_accept/insert + quick_play_results_insert (auth.uid per-query)
  - Dropped unused indexes: idx_word_wheel_attempts_catchup, quick_play_challenges_challenger_idx
  - Added FK indexes: word_clubs(owner_id), word_club_members(user_id), word_pacts(player2_id)
  - Added web_vitals(player_id) index CONCURRENTLY

frontend_wins: none — CLS root cause still needs DevTools Layout Shifts attribution

impact_check: 02-perf-2026-07-21-landing-ad-banner-spacer → neutral (CLS 0.921→0.934, n=50)

next_steps: |
  - CLS root cause on / (0.934) and /en/multiplayer (0.962) still needs DevTools Layout Shifts
    panel attribution — no headless path available. Candidate: GlobalBottomNav ResizeObserver
    async AdMob SizeChanged event. Needs Playwriter session.
  - auth DB connection strategy advisor warning: switch to percentage-based in Supabase dashboard
    (human config change — cannot be done via execute_sql).
  - Zombie PostHog flag exp-mp-room-join-loading-v1 (0 call sites, active) — deactivate in PostHog UI (human).
