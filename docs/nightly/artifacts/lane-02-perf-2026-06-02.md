---
status: research-only
files_touched:
  - docs/nightly/perf-baseline.json
next_steps: |
  CLS 0.512 on /en/multiplayer needs visual verification before fixing.
  Two root cause candidates:
  1. PageClient SSR skeleton height < hydrated lobby height — GamePageSeoContent rendered
     BELOW MultiplayerPageClient in page.tsx shifts down on hydration.
     Fix = min-height on wrapper OR reorder SEO block above fold.
  2. ConnectionBanner/SpectatorBanner/ClassroomModeBanner appear after socket connect
     and push content.
  INP 528ms: heavy socket event handlers on main thread — investigate with Chrome DevTools.
  WAL parser: 82% DB CPU, needs Supabase Realtime publication audit from Dashboard.
  word_review_state FK index: still 0 rows, re-check when table has data.
---

## Summary

**Backend:** No new regressions. WAL parser stable at 82% DB CPU (infrastructure).
word_review_state FK index skipped (0 rows). Auth connections = Dashboard config only.

**Frontend:** /en/multiplayer (n=16): LCP 3010ms (-377ms trend), INP 528ms (-56ms),
CLS 0.512 (bad, root cause unclear — needs visual verification before fix).

**No code changes shipped.** Baseline updated: docs/nightly/perf-baseline.json
