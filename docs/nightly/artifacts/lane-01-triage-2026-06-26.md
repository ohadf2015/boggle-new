status: shipped
files_touched:
  - supabase migration: fix_get_random_words_search_path (ALTER FUNCTION SET search_path = '')
  - supabase migration: fix_get_random_words_search_path_v2 (corrected to SET search_path = 'public')
  - docs/nightly/triage-queue.md (appended 2026-06-26 section)
  - docs/nightly/reports/2026-06-26.md (appended Lane 1 section)
next_steps:
  - Verify daily challenge generation still works (get_random_words_from_bank search_path pinned to 'public')
  - upsert_push_token SECURITY DEFINER: refactor to accept p_user_id + service_role call + REVOKE authenticated (06-25 queue entry)
  - Sentry null.clear / coin-sync: pull stack traces directly from Sentry UI (project-slug search not returning these)
  - Word Tower improvements (founder directive): Lane 05/11 scope — not this lane
