status: shipped
files_touched: fe-next/components/education/EducationHeroBanner.tsx, docs/nightly/perf-baseline.json, docs/nightly/impact-ledger.ndjson, docs/nightly/reports/2026-08-17.md
next_steps: word-craft (n=10, 5712ms) and /he (n=11, 4780ms) not code-audited this run -- time budget; all 4 brief LCP candidates were below the 50-sample floor so treat those ms figures as noisy, re-pull with a 7d window before trusting them. Backend STEP1 (pg_stat_statements/advisor via supabase MCP execute_sql) skipped again this run -- MCP wasn't exercised, still owed. esl-word-games hero fix needs a 7-day re-check (n=2 tonight, too low to confirm impact yet) -- query_hint is in the impact-ledger entry.
