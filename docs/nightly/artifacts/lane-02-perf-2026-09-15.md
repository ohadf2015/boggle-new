status: shipped
attempted: fix top LCP regressions (en homepage, student/profile) + backend advisor items (unindexed FK, auth conn pct) if EXPLAIN confirms
files_touched: app/[locale]/student/profile/PageClient.tsx (priority on above-fold avatar Image); docs/nightly/perf-baseline.json, docs/nightly/perf-watch.md, docs/nightly/reports/2026-09-15.md (docs)
next_steps: >
  Every brief LCP item failed the mandatory 50-sample floor when re-queried live (n=1-37, not >=50) --
  no regression verdict issued, correctly held the gate rather than acting on noise (/en's real p75 is
  1676ms, not the brief's 10652ms). Shipped one safe general SPEED win instead: student/profile's
  above-fold avatar <Image> was missing `priority`, added, eslint-clean. Rejected a subagent's other
  claim (sync framer-motion `m` import) after checking app/LazyMotionRoot.tsx -- that's the app's
  intentional root-level LazyMotion bundle, not per-file bloat; did not touch it.
  Backend: supabase MCP never connected this lane's window -- vocabulary_lessons unindexed-FK and
  auth_db_connections_absolute advisor items carried unverified, no speculative index added (hard rule).
  New finding logged to perf-watch.md: /en/student/profile's real LCP bottleneck is almost certainly its
  5+ call fetch waterfall before paint (auth, achievements, duels, classroom), not the image -- recommend
  parallelizing those fetches as a lower-risk first cut before any SSR redesign. Tomorrow: retry supabase
  MCP for the FK index confirmation, and consider a dedicated pass on student/profile's fetch waterfall.
