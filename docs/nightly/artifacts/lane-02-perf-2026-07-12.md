# Lane 02 — Performance — 2026-07-12

status: shipped
files_touched:
  - fe-next/app/[locale]/multiplayer/PageClient.tsx (1-char prop removal)
  - docs/nightly/perf-baseline.json
  - docs/nightly/impact-ledger.ndjson
  - docs/nightly/reports/2026-07-12.md

## What shipped

**CLS fix — /en/multiplayer (CLS 0.574 → expected ~0.29)**

Removed `collapseSpacerWhenHidden={showResults}` from `AutoHideHeader` in `PageClient.tsx:653`.

Root cause: AutoHideHeader's own JSDoc explicitly warns "Leave OFF (default) on pages like /multiplayer where collapsing the spacer regressed CLS to 0.29." A prior commit passed `collapseSpacerWhenHidden={showResults}` to eliminate an empty band on the results screen — trading CLS 0.29 for CLS 0.574. Reverting to default restores the spacer height during results, eliminating the 60-124px layout shift.

Evidence:
- /en/multiplayer: p75 CLS = 0.574, n=80 (7-day, above 50-sample floor) — CRITICAL
- /es/multiplayer: p75 CLS = 0.254, n=59 — CRITICAL
- AutoHideHeader JSDoc documents this exact failure mode at line 13-20

**Impact check (prior ship)**

`02-perf-2026-07-05-lcp-h1-delay`: measured 1921ms vs 6522ms baseline. Verdict: improved (70% reduction). n=28 sub-floor — direction clear and consistent.

## What was NOT done

- Backend: Supabase MCP absent (17th consecutive night) — 4 migrations pending apply
- SpectatorBanner CLS (y:-100 entrance animation, no reserved height) — secondary CLS contributor
- ConnectionStatusIndicator 1500ms delayed banner — secondary CLS contributor
- INP on /multiplayer (268ms/318ms) — separate investigation needed

## next_steps

- Check CLS in 3 days: `SELECT quantile(0.75)(toFloat(properties.$web_vitals_CLS_value)) AS p75_cls, count() AS n FROM events WHERE event='$web_vitals' AND properties.$pathname='/en/multiplayer' AND timestamp > now() - INTERVAL 7 DAY`
- If CLS still >0.1 after fix: investigate SpectatorBanner entrance animation (no reserved height) and ConnectionStatusIndicator 1500ms delayed banner
- Supabase MCP: human must mint never-expire PAT (17 nights blocked, 4 migrations unapplied)
