status: shipped
files_touched:
  - supabase/migrations/20260619040000_perf_policy_consolidation.sql (new — applied LIVE via REST)
  - docs/nightly/perf-baseline.json (updated)
  - docs/nightly/artifacts/lane-02-perf-2026-06-19.md (this file)

backend_applied_live:
  - policy_consolidation: teacher_access_allowlist — 6 policies → 3 (taa_admin_insert, taa_select, taa_update). Eliminated 5 duplicate/redundant policies including 3 inline-EXISTS admin duplicates and merged admin+own-email SELECT/UPDATE pairs.
  - policy_consolidation: teacher_access_requests — 2 SELECT policies (tar_admin_select + tar_select_own) → 1 (tar_select: is_admin_user() OR auth.uid()=user_id)
  - index_drop: idx_word_pacts_player2_id (word_pacts, 0 scans)
  - index_drop: idx_tar_status (teacher_access_requests, 0 scans)
  - index_drop: idx_taa_consumed (teacher_access_allowlist, 0 scans)
  Result: 20 advisor WARNs (multiple_permissive_policies) → 0; 8 unused index INFOs → 5

frontend_findings:
  - /he LCP: 6273ms 7d p75 (n=85) vs 4764ms baseline (+31.6%) — WATCH, not flagged as code regression yet (mascot has priority, no code root cause found)
  - /es/multiplayer INP: 400ms vs 442ms baseline (-9.5%) — improving, cellNodeMap fix likely contributing
  - 24h window: 0 routes above n≥50 floor

supabase_mcp: UNAUTHORIZED (token config) — used REST API (api.supabase.com/v1/projects/{ref}/database/query) throughout

next_steps:
  - /he LCP at 6273ms — needs 2nd baseline reading to confirm regression; if persists, investigate font loading (Hebrew Rubik/Fredoka glyph set), above-fold content below Suspense boundary
  - Remaining 5 unused indexes (curator_proposals x3, word_review_state, word_wheel_attempts_catchup) — safe to drop next run
  - Supabase MCP token config fix — investigate SUPABASE_ACCESS_TOKEN scoping for npx-spawned MCP
  - /es/multiplayer CLS (0.207) still POOR — investigate late-rendered ad placeholders or images without aspect-ratio
