---
status: partial
files_touched:
  - fe-next/supabase/migrations/20260608030000_fix_rls_auth_uid_initplan_curator_proposals.sql (applied to prod + committed)
  - docs/nightly/perf-baseline.json
  - docs/nightly/reports/2026-06-08.md
next_steps: |
  Frontend LCP on /daily/word-wheel (~6168ms) and /brain (~5444ms): force-dynamic disables SSR
  cache, game loaded via dynamic import. Needs browser trace to confirm LCP element. Possible fix:
  ISR revalidate:3600 if no per-request secrets in SSR. AutoHideHeader CLS 0.29 on /en/multiplayer
  still unshipped — 3rd night carrying, ship next run. word_scores multiple permissive SELECT
  policies: consolidate carefully to avoid breaking curator access.
---
