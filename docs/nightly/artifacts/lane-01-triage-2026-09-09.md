status: research-only
files_touched: none (docs/nightly/triage-queue.md, docs/nightly/reports/2026-09-09.md only; one DB migration applied then reverted same run)
next_steps: |
  - Top brief restore (20260904-020001) was moot/stale-base — closed, not restored. Lane 7 should teach the restore-scorer to check `git log` on target files since the drop date.
  - Sentry 233 already fixed by c8e142e8b (same-day). Sentry 234 is a stale single-user empty-payload event — watch for recurrence.
  - Player-reported Word Tower "ice" not-in-dictionary bug: traced the full pipeline, found no bug on paper (dict build + canon casing both consistent, "ice" confirmed present in an-array-of-english-words). Needs LIVE repro next run — try a stale 24h IndexedDB dictionary cache.
  - Supabase function_search_path_mutable (7 fns): attempted autonomously per the matrix, broke get_random_words_from_bank (unqualified table ref), reverted immediately. Do NOT retry as a batch — fix one function body at a time (schema-qualify refs) with a live verification call before setting search_path. Flag this in nightly-learnings: the matrix's "ship autonomously" bullet for this advisor class is unsafe as written for this repo.
