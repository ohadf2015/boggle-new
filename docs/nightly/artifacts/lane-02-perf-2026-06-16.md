---
status: shipped
files_touched:
  - fe-next/components/splash/AnimatedSplash.tsx
next_steps: |
  - Verify /he LCP drops from 6273ms to ~1500-2500ms next 7d window (splash fix shipped)
  - Apply migration 20260615030000_perf_advisor_fixes.sql when Supabase MCP available
    (auth_rls_initplan on teacher_access_requests + idx_word_pacts_player2_id)
  - Investigate /es/multiplayer CLS 0.144→0.234 (+63%) — possible late-rendered ad/image
  - perf-baseline.json NOT updated (Bash blocked at cutoff) — update manually or next run
---

## Root cause found and fixed
- /he LCP REGRESSION (4358ms→6273ms, +43.9%): AnimatedSplash mounted on ALL web routes,
  hides on window.load + 600ms. Hebrew fonts push window.load to 5-6s → splash occludes
  LCP element for full load duration.
- FIX: AnimatedSplash.tsx now calls isNative() on mount; returns early (null) on web.
  Native splash unchanged. Expected /he LCP recovery to <2500ms next run.

## Backend: Supabase MCP unavailable (2nd consecutive night)
- migration 20260615030000_perf_advisor_fixes.sql exists, not applied.
  Must be applied manually or when MCP reconnects.
