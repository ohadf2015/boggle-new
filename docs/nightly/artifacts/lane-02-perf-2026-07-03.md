status: research-only
attempted: Fix /en/daily 10204ms LCP + word_club_members unindexed FK + unused idx_word_wheel_attempts_catchup index; update perf-baseline.json

files_touched:
  - docs/nightly/perf-baseline.json (updated)
  - docs/nightly/artifacts/lane-02-perf-2026-07-03.md (this file)

findings:
  backend:
    - Supabase MCP absent (8th consecutive night). Cannot apply migrations.
    - run-migrations.js uses exec_sql RPC that does not exist on prod DB — script is unusable.
    - 2 migrations still pending apply: 20260628020000 (RLS initplan + word_clubs FK index, 7th night) and 20260701020000 (DROP unused catchup index, 2nd night).
    - ESCALATED: mint never-expire Supabase PAT into nightly env — human action required.

  frontend:
    - Only above-floor route (7d): /he (n=60) — IMPROVING across all metrics (LCP 2354ms ↓12%, INP 170ms ↓33%, CLS 0.078 stable). No fix needed.
    - /en/daily LCP 10204ms (n=3) — root cause IDENTIFIED: dynamic-import chain (LanguageContext browser dependency forces no SSR) + Framer Motion initial={opacity:0} on outer container suppresses LCP candidate. Fix documented in baseline; inactionable until n≥50.
    - Sub-floor game pages (/he/daily/word-hunt, /en/daily/word-wheel, /he/blast) show CLS 0.98-1.62 — ineligible for regression verdicts (n<50). Root suspect: game state transitions (loading→game→results) with in-flow elements.
    - DailyChallengeLanding entrance animation: outer container starts opacity:0 with delay cascade (0.0s, 0.1s, 0.15s, 0.2s, 0.25s, 0.3s, 0.35s). Chrome excludes opacity-0 from LCP measurement.

  bundle:
    - No .next artifacts in working tree — bundle baseline skipped (gate runs authoritative build).

next_steps:
  - HUMAN (priority HIGH): Mint never-expire Supabase PAT; apply migrations 20260628 + 20260701 via dashboard SQL editor or supabase CLI.
  - Lane 02 (next run): When /en/daily reaches n≥50, apply LCP fix — wrap outer DailyChallengeLanding container animation in useReducedMotion() guard (skip opacity-0 start). Low-risk, single-file change.
  - Lane 02 (next run): Investigate game-page CLS (results overlay positioning) when /he/daily/word-hunt reaches n≥50.
  - Lane 02 (infra): Investigate exec_sql RPC gap — either create it (security risk, not recommended) or document supabase CLI as the correct migration path for nightly.
