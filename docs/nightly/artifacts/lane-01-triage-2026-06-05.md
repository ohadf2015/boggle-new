status: partial
attempted: Fix TypeError null.x (Sentry 13Y), REVOKE upsert_push_token anon execute, harden web_vitals RLS, root-cause churn-signals 502
files_touched:
  - fe-next/app/globals.css (added LTR @utility for shadow-hard-pink/cyan/purple/lime — party hub colored card shadows were RTL-only, invisible in LTR)
next_steps:
  - churn-signals 502: replace getUserFromRequest() anon-client+auth.getUser() with local SUPABASE_JWT_SECRET JWT verify (auth-adjacent, deferred)
  - blast TypeError null.x (JAVASCRIPT-NEXTJS-13Y): needs source maps to trace minified Pixi rAF crash; single user, no recurrence
  - upsert_push_token SECURITY DEFINER: confirm function body uses auth.uid() not p_user_id; if yes, add REVOKE EXECUTE FROM anon
  - web_vitals RLS always-true: confirm anon inserts are by design; if yes, add migration comment
