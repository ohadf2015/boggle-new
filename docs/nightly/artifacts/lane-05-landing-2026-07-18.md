---
status: shipped
attempted: improve word-craft cards mode (admin-gated) — randomize hardcoded seed + add back/exit buttons
files_touched:
  - fe-next/app/[locale]/word-craft/RunPageClient.tsx
next_steps: none — shipped complete. Next run could add variable-reward escalation to the cards run (score multiplier per round) or improve CardPickScreen visuals.
---
