status: research-only
attempted: Fix top Sentry errors — is_catchup missing column, coin transaction failures, null clear() TypeError, profiles relation missing
blocker: SUPABASE_ACCESS_TOKEN expired — REST 401, CLI link rejected, MCP Unauthorized

root_causes_diagnosed:
  - is_catchup column missing from daily_word_wheel_attempts (migration 20260607100000 written, never applied)
  - coin transaction 500s: suspected sync_coins RPC unqualified profiles reference + empty search_path
  - null clear() TypeError: Sentry stack trace needed (MCP not connected)
  - churn-signals 502: external infra

next_steps:
  - Refresh SUPABASE_ACCESS_TOKEN (human action, morning)
  - Apply: ALTER TABLE public.daily_word_wheel_attempts ADD COLUMN IF NOT EXISTS is_catchup boolean NOT NULL DEFAULT false;
  - Check sync_coins search_path: SELECT proconfig FROM pg_proc WHERE proname = 'sync_coins'
  - Re-run triage with Sentry MCP for null clear() stack trace
