status: research-only
files_touched: docs/nightly/triage-queue.md, docs/nightly/reports/2026-06-25.md
next_steps: |
  - Railway: set SUPABASE_JWT_SECRET (Supabase Dashboard → Settings → API → JWT Secret) — eliminates 1KQ 502s immediately, zero code needed
  - Read Sentry stack trace for 1JR ("profiles" relation) to find actual callsite
  - Read Sentry stack trace for 1JP/1JQ (coin failures) to see which RPC path throws
  - upsert_push_token SECURITY DEFINER fix: add p_user_id param to function body + switch route to service_role client + REVOKE authenticated; design decision needed first
