status: shipped
attempted: STEP 0 self-select admin-beta mode improvement (rotate away from last-improved mode), smallest coherent slice
files_touched: fe-next/components/word-craft/WordCraftGameOverScene.tsx, fe-next/translations/{en,he,sv,ja,es}.js
next_steps: word-craft "close game" tension badge shipped (feel axis). eslint run on the changed component was still pending when the lane hit finalize cutoff — background job id bf9xzui58, re-check its output next run before trusting this slice blindly, though the edit is small/typed and diff is low-risk. Next rotation candidates (not touched in 3+ nights): wheel-rush (learnings say ESCALATE the visual-QA/HE-JA-review blocker instead of re-auditing code), brain-drill.
