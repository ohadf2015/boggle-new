status: shipped
attempted: restore dropped work 20260831-010002 (19 files), fix gate error, ship
files_touched: fe-next/components/education/TeacherGate.tsx (moved trackExposure() render-time call into useEffect — likely gate/lint-safety fix), plus 18 restored-as-is files from backup (education access PageClient, TeacherGate.test.tsx, PuzzleCard shake intensity, experiments.ts new flag, growthTracking.ts event, boardSelection.ts, dictionary candidates x5, translations x5, fe-next/docs/nightly housekeeping x2) — all eslint-clean on touched .ts/.tsx (exit 0)
next_steps: gate will run full lint/tsc/test/build authoritatively. If it reddens again, the render-time trackExposure() call was the leading suspect and is now fixed; check TeacherGate.test.tsx assertions next (uses screen.getByText('common.loading') — confirm that key resolves in test env). Also resolved restore-queue tag 20260831-010002. Did not reach the other 2 stale restores (20260817-010002, 20260827-010001) or the Sentry/Supabase/PostHog brief items (EmptyRanges ReferenceError, FedCM GSI_LOGGER, security_definer_view, "Game hasn't started yet") — queued below for tomorrow.
