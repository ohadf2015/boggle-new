status: research-only
attempted: fix top LCP regressions on imposketch.io /r/FOX4 /r/WOLF0 routes + check bundle/backend per brief
files_touched: docs/nightly/perf-baseline.json, docs/nightly/reports/2026-09-26.md
next_steps: brief's phase-0 LCP aggregation needs a $host filter for lexiclash.live before ranking — it's currently dominated by imposketch.io (a sibling app on the same PostHog project, 435 events/24h vs our 44). Fix at the source (phase-0 query) so future nights don't re-chase this phantom. Backend: no real regression found (23-row table, already-indexed hot query) — nothing pending.
