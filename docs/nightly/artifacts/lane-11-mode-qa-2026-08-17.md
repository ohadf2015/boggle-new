status: research-only
attempted: wheel-rush (night 7, was 72%). Declined the 08-16 live 2-tab MP visual-QA plan tonight — budget too tight to build+run an unscripted 2-client agent-browser session plus survive round-timer waits (~2-3min) and still self-verify. Instead resolved the carried MINOR ledger item ("resetGameForNewRound never nulls wheelRushState"): read gameStateManager.ts:346-367, confirmed `game.wheelRushState = null;` already at line 361 (fixed 08-13, re-verified 08-14, ledger stale-re-added it 08-16). Closed as phantom, no code change.
files_touched: none (docs/nightly/mode-readiness.md, docs/nightly/reports/2026-08-17.md — ledger/report updates only)
next_steps: wheel-rush readiness now 74% (was 72%). Escalating per learnings' own rule — this is the 7th audited night, still blocked solely on live visual-QA capture (no standalone route/flag-override; only reachable via a live weighted MP match). Next session working this mode should go in with the SOLE goal of building a scripted 2-client MP session (join/host → finish round1 → results-screen GameModeSelector picks wheel-rush → round2 → screenshot mid-round + HE/RTL) with a dedicated budget large enough for the unavoidable ~2-3min round-timer wait — do not open it as one task among several again. Do not re-list the wheelRushState reset as open; it is fixed and verified twice now.
