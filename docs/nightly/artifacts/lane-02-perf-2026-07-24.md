status: research-only
attempted: Fix web_vitals unindexed FK + unused word_pacts index via Supabase MCP; investigate homepage CLS/LCP root cause
files_touched: docs/nightly/perf-baseline.json
next_steps: |
  1. BACKEND (blocked on MCP): Apply two pending migrations when MCP comes up:
     - 20260716020000_web_vitals_player_id_fk_index.sql (carry 8 days)
     - 20260715020000_word_pacts_player2_id_fk_index.sql (carry 9 days)
  2. CLS ROOT CAUSE (needs visual QA):
     - Homepage / CLS 0.903: prime suspect is CookieConsent nextDynamic with no
       loading:()=>null (null->banner shift on first visit). Add loading:()=>null to fix.
     - Multiplayer /en/multiplayer CLS 0.962: open with ES-locale browser to trigger
       NativeLanguageBanner conditional mount — likely the remaining source after AutoHideHeader fix.
  3. Frontend LCP: Homepage already well-optimized (preload + priority on winner.webp,
     all Framer animations use whileInView). No quick wins without CLS element attribution.
