status: shipped
files_touched:
  - fe-next/server/redisAdapter.ts (attachAdapter: wrap pubClient.publish to catch "Connection is closed." unhandled rejections)
next_steps:
  - Verify on next Redis restart that JAVASCRIPT-NEXTJS-1WM burst no longer fires (check_after_days: 3)
  - React #418 hydration mismatch on homepage (/he, /) needs dev-mode Android Chrome repro — queued
  - Ron coin grant request in feedback — human-queue coin economy change
  - Supabase advisor authenticated_security_definer for upsert_push_token is expected (authenticated users legitimately call it) — no action needed
