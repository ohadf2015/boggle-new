{
  "date": "2026-08-14",
  "note": "supabase MCP never connected this run (toolset stayed empty after repeated ToolSearch checks over ~15min) -- no REST fallback script exists under scripts/nightly/lib/ (checked, absent) -- backend STEP 1 skipped entirely rather than apply an unverified migration (hard rule: index only after EXPLAIN ANALYZE confirms). Carried forward from 08-13: vocabulary_lessons FK confirmed 4 consecutive nights as non-actionable (2-row table, zero index benefit) -- still true, not re-verified tonight, needs no more nightly re-checks. New brief item idx_word_pacts_player2_id (unused index) NOT dropped -- no usage-stat confirmation possible without MCP, deferred. Frontend: found and fixed a real Class-5-pattern LCP delay on /friends -- FriendsPageClient wrapped its ENTIRE above-the-fold content (auth gate text + FriendsList) in a framer-motion m.div with initial={opacity:0,y:20}/animate/duration:0.3, which is exactly the documented anti-pattern (entrance opacity tween on a large/fullscreen layer delays LCP paint -- Chrome excludes opacity:0 elements from LCP candidacy until they cross a visibility threshold). Removed the tween (static div, no animation) -- lowest-risk fix, matches the repo's own recurring-pitfalls Class 5 guidance verbatim. Did not have time to also build + measure before/after LCP (no live prod deploy in this session) -- logged as a metric to re-check via impact ledger in 3 days once it ships and PostHog accumulates n>=50 on /es/friends and /en/friends. Also scanned /en/multiplayer PageClient.tsx (670 lines) for the same pattern -- none found (no opacity-entrance tweens, no missing dynamic() splits) -- did not ship a second frontend fix, did not force one under time pressure.",
  "backend": {
    "mcp_status": "never connected this run -- STEP 1 (execute_sql, EXPLAIN ANALYZE, apply_migration) entirely skipped, not a false-negative, genuinely unavailable",
    "unindexed_foreign_keys_still_open": "public.vocabulary_lessons_classroom_id_fkey -- carried from 08-13 (4 consecutive nights confirmed non-actionable, 2-row table). Not re-verified tonight (no MCP).",
    "unused_index_new_item": "public.idx_word_pacts_player2_id on word_pacts -- brief-flagged tonight, NOT dropped (no usage-stat confirmation without MCP), deferred to next run"
  },
  "frontend": {
    "by_route": [
      { "route": "/en/multiplayer", "p75_lcp_ms": 1672, "n_24h": 55, "vs_baseline": "healthy 08-13 snapshot, not re-measured tonight (no fresh PostHog query per brief-first contract)" }
    ],
    "shipped_fix": {
      "route": "/friends (es/en/all locales)",
      "file": "fe-next/app/[locale]/friends/PageClient.tsx",
      "change": "removed framer-motion m.div entrance opacity/y tween wrapping entire above-the-fold content -- replaced with static div, dropped now-unused `m` import",
      "brief_evidence": "es/friends LCP 5302ms, es/friends?ref=... LCP 3908ms",
      "measured_before_after": "not measured -- no deploy in this session, re-check via impact ledger in 3 days"
    }
  },
  "bundle": {
    "note": "not re-measured tonight -- no fresh build run (time-budget discipline, brief-first contract did not call for a bundle re-scan given no chunk-affecting change shipped: import removal only, no new deps)",
    "carried_from_08-13": {
      "total_chunks_kb": 31482,
      "total_chunk_files": 964
    }
  }
}
