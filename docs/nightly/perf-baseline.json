{
  "date": "2026-08-15",
  "note": "supabase MCP never connected this run either (repeated ToolSearch checks over the run, tools stayed absent) -- same as 08-14. Backend STEP 1 skipped again, not re-verified. vocabulary_lessons FK: per learnings doc this is confirmed non-actionable 4+ consecutive nights (2-row table) -- not re-checked tonight per the doc's own 'no more nightly re-checks needed' note. idx_word_pacts_player2_id (unused index) still not droppable without a usage-stat query -- deferred again. auth_db_connections_absolute advisor item is a Supabase dashboard toggle (percentage-based connection pool), not a migration/code fix -- out of scope for this lane, belongs in human queue. Frontend: shipped a real bundle-splitting fix on the worst-LCP route from tonight's brief (es/word-tower?daily=1, 18176ms). WordTowerGame.tsx statically imported WordTowerLeaderboard (a click-triggered modal that pulls in the Avatar module -- flagged in a prior perf audit as a 464KB untree-shakeable chunk) into the page's critical-path bundle even though it never renders on first paint. Converted to next/dynamic(ssr:false). Verified: eslint clean on the changed file. Did not run a full build/bundle-size re-measure (time-budget discipline -- single import-graph change, no new deps, no chunk-size claim being made). Checked '/' (2nd-worst LCP, 10621ms) for the same two known patterns (missing next/image priority, Class-5 opacity-entrance tween) -- neither present in app/[locale]/PageClient.tsx (no images, no motion.* usage) -- did not force a second fix under time pressure.",
  "backend": {
    "mcp_status": "never connected this run -- STEP 1 (execute_sql, EXPLAIN ANALYZE, apply_migration) entirely skipped, consistent with 08-14",
    "unindexed_foreign_keys_still_open": "public.vocabulary_lessons_classroom_id_fkey -- carried, non-actionable per learnings doc (2-row table, 4+ nights confirmed). No further nightly re-checks needed per that doc.",
    "unused_index_still_open": "public.idx_word_pacts_player2_id on word_pacts -- brief-flagged again tonight, still not droppable without MCP usage-stat confirmation",
    "auth_db_connections_advisor": "dashboard config toggle (percentage vs absolute connection allocation), not a code/migration fix -- deferred to human queue"
  },
  "frontend": {
    "by_route_from_brief": [
      { "route": "/es/word-tower?daily=1", "lcp_ms": 18176, "action": "fixed tonight -- see shipped_fix" },
      { "route": "/", "lcp_ms": 10621, "action": "checked for known patterns (missing image priority, Class-5 opacity tween) -- neither found, no fix shipped" },
      { "route": "/es/daily/word-hunt", "lcp_ms": 8624, "action": "not investigated this run -- time budget" },
      { "route": "/es/juego-de-palabras-multijugador", "lcp_ms": 6269, "action": "not investigated this run -- time budget" },
      { "route": "/en/word-tower?daily=1", "lcp_ms": 5536, "action": "same root cause as the es variant -- covered by tonight's fix" }
    ],
    "shipped_fix": {
      "route": "/[locale]/word-tower (daily=1)",
      "file": "fe-next/components/wordTower/WordTowerGame.tsx",
      "change": "converted static import of WordTowerLeaderboard (click-triggered modal, pulls in the Avatar SVG module) to next/dynamic(..., { ssr: false }) -- removes it from the page's critical-path bundle",
      "verified": "eslint clean on changed file; no full build run (time budget)",
      "measured_before_after": "not measured -- no deploy in this session, re-check via impact ledger in 3 days once PostHog accumulates n>=50 on the word-tower daily routes"
    }
  },
  "bundle": {
    "note": "not re-measured tonight -- single import-graph split, no new deps, no full build run (time-budget discipline)",
    "carried_from_08-13": {
      "total_chunks_kb": 31482,
      "total_chunk_files": 964
    }
  }
}
