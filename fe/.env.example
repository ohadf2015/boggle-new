# Public URL for QR codes (used when deployed)
# If not set, will default to window.location.origin
# Example: https://boggle.yourdomain.com
REACT_APP_PUBLIC_URL=

# WebSocket URL (optional)
# If not set, will be automatically determined based on current location
# For separate backend deployment, set this to your backend WebSocket URL
# Examples:
#   ws://your-backend-domain.com (for http)
#   wss://your-backend-domain.com (for https)
REACT_APP_WS_URL=
