'use client';

import { PageLoader } from '@/components/ui/PageLoader';

/**
 * Loading component for page transitions
 * Uses PageLoader with mascot and letter tiles for consistent brand experience
 */
export default function Loading() {
  return <PageLoader />;
}
