/**
 * @deprecated Profile picture upload has been removed.
 * All avatars now use the custom SVG avatar system.
 * This test file is kept as a placeholder to avoid import errors.
 */
describe('useProfilePictureUpload (deprecated)', () => {
  it('is deprecated — profile pictures removed in favor of custom avatars', () => {
    expect(true).toBe(true);
  });
});
