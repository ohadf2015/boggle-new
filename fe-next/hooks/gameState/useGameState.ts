/**
 * @deprecated This hook is replaced by the Zustand store (./store.ts).
 * Only kept for type re-exports used by host hooks (Player, ComboState, etc.).
 * Types are re-exported from ./types.ts; prefer importing from there directly.
 */

export type { Player, TournamentData, TournamentStanding, UseGameStateReturn } from './types';
