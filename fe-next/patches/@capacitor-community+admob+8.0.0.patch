diff --git a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
index c629139..e037e0f 100644
--- a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
+++ b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
@@ -33,6 +33,16 @@ public class BannerExecutor extends Executor {
     private RelativeLayout mAdViewLayout;
     private AdView mAdView;
     private ViewGroup mViewGroup;
+    // True once the CURRENT AdView has successfully loaded at least one ad.
+    // AdMob auto-refresh (~30-60s, configured server-side) re-invokes this
+    // AdView's AdListener on every refresh; a refresh no-fill fires
+    // onAdFailedToLoad. Upstream tears the AdView down on ANY failure, which
+    // destroys a perfectly-good displayed banner on the first refresh no-fill
+    // (the "banner disappears after a while" bug). Google's own contract keeps
+    // the last good ad on a refresh failure — this flag lets us honor that:
+    // destroy ONLY when no ad was ever shown (initial no-fill). Reset to false
+    // only in createNewAdView (a fresh AdView + fresh listener).
+    private boolean bannerHasLoaded = false;
 
     public BannerExecutor(
         Supplier<Context> contextSupplier,
@@ -44,7 +54,20 @@ public class BannerExecutor extends Executor {
     }
 
     public void initialize() {
-        mViewGroup = (ViewGroup) ((ViewGroup) activitySupplier.get().findViewById(android.R.id.content)).getChildAt(0);
+        ensureViewGroup();
+    }
+
+    private void ensureViewGroup() {
+        if (mViewGroup != null) return;
+        try {
+            Activity activity = activitySupplier.get();
+            if (activity == null) return;
+            ViewGroup content = (ViewGroup) activity.findViewById(android.R.id.content);
+            if (content == null) return;
+            ViewGroup child = content.getChildCount() > 0 ? (ViewGroup) content.getChildAt(0) : null;
+            mViewGroup = child != null ? child : content;
+        } catch (Exception ignored) {
+        }
     }
 
     public void showBanner(final PluginCall call) {
@@ -84,10 +107,29 @@ public class BannerExecutor extends Executor {
             mAdViewLayout = new RelativeLayout(contextSupplier.get());
             mAdViewLayout.setHorizontalGravity(Gravity.CENTER_HORIZONTAL);
             mAdViewLayout.setVerticalGravity(Gravity.BOTTOM);
+            mAdViewLayout.setClipChildren(true);
+            mAdViewLayout.setClipToPadding(true);
+
+            // Banner-height container (NOT WRAP_CONTENT). The ad's hardware surface
+            // composites as its own GPU layer; if the container is left tall (a
+            // momentarily-oversized creative makes WRAP_CONTENT measure ~480dp) that
+            // tall layer punctures the WebView across the middle of the screen and the
+            // splash/app-icon shows through — the "logo/navy container hiding the page"
+            // bug. Pin the container to the ad's declared height (capped 120dp) so it's
+            // a thin strip anchored above the tabs; the page fills everything above it.
+            float __dens = contextSupplier.get().getResources().getDisplayMetrics().density;
+            int __bannerH;
+            {
+                com.google.android.gms.ads.AdSize __sz = mAdView.getAdSize();
+                int __cap = (int) (120 * __dens);
+                __bannerH = (__sz != null && __sz.getHeight() > 0)
+                    ? Math.min((int) (__sz.getHeight() * __dens), __cap)
+                    : __cap;
+            }
 
             final CoordinatorLayout.LayoutParams mAdViewLayoutParams = new CoordinatorLayout.LayoutParams(
-                CoordinatorLayout.LayoutParams.WRAP_CONTENT,
-                CoordinatorLayout.LayoutParams.WRAP_CONTENT
+                CoordinatorLayout.LayoutParams.MATCH_PARENT,
+                __bannerH
             );
 
             // TODO: Make an enum like the AdSizeEnum?
@@ -107,13 +149,23 @@ public class BannerExecutor extends Executor {
             if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.VANILLA_ICE_CREAM) {
                 View rootView = activitySupplier.get().getWindow().getDecorView();
                 rootView.setOnApplyWindowInsetsListener((v, insets) -> {
-                    int bottomInset = insets.getSystemWindowInsetBottom();
-                    int topInset = insets.getSystemWindowInsetTop();
+                    // Use TYPE-SPECIFIC insets, NOT the deprecated getSystemWindowInset*()
+                    // which lumps in the IME (keyboard) inset. On Android 15 the IME inset
+                    // is reported as a ~972px stale "hint" even with no keyboard visible, so
+                    // a BOTTOM banner's bottom margin (max(inset, userMargin)) jumps to ~972px
+                    // and the banner is shoved ~a full keyboard-height UP into mid-screen,
+                    // where its native surface occludes the WebView content below it
+                    // (the "blank section / stray logo below the ad"). navigationBars() = 48px.
+                    int bottomInset = insets.getInsets(android.view.WindowInsets.Type.navigationBars()).bottom;
+                    int topInset = insets.getInsets(android.view.WindowInsets.Type.statusBars()).top;
+                    // Honor caller-supplied margin via max(): the caller already accounts for
+                    // the safe area via --bottom-nav-height, so summing would double-count.
+                    int userMarginPx = (int) (adOptions.margin * v.getResources().getDisplayMetrics().density);
 
                     if ("TOP_CENTER".equals(adOptions.position)) {
-                        mAdViewLayoutParams.setMargins(0, topInset, 0, 0);
+                        mAdViewLayoutParams.setMargins(0, Math.max(topInset, userMarginPx), 0, 0);
                     } else {
-                        mAdViewLayoutParams.setMargins(0, 0, 0, bottomInset);
+                        mAdViewLayoutParams.setMargins(0, 0, 0, Math.max(bottomInset, userMarginPx));
                     }
 
                     mAdViewLayout.setLayoutParams(mAdViewLayoutParams);
@@ -205,7 +257,7 @@ public class BannerExecutor extends Executor {
                     .get()
                     .runOnUiThread(() -> {
                         if (mAdView != null) {
-                            mViewGroup.removeView(mAdViewLayout);
+                            if (mViewGroup != null) mViewGroup.removeView(mAdViewLayout);
                             mAdViewLayout.removeView(mAdView);
                             mAdView.destroy();
                             mAdView = null;
@@ -226,6 +278,10 @@ public class BannerExecutor extends Executor {
         activitySupplier
             .get()
             .runOnUiThread(() -> {
+                if (mAdView == null) {
+                    Log.d(logTag, "Banner: updateExistingAdView skipped, mAdView is null");
+                    return;
+                }
                 final AdRequest adRequest = RequestHelper.createRequest(adOptions);
                 mAdView.loadAd(adRequest);
             });
@@ -245,12 +301,36 @@ public class BannerExecutor extends Executor {
                 AdViewIdHelper.assignIdToAdView(mAdView, adOptions, adRequest, logTag, contextSupplier.get());
                 // Add the AdView to the view hierarchy.
                 mAdViewLayout.addView(mAdView);
+                // Hard-constrain the banner to its declared ad-size height. An oversized
+                // ad creative (e.g. a 320x480 test creative on a registered test device,
+                // or a misbehaving adaptive size under Android 15/16 edge-to-edge) would
+                // otherwise grow the AdView into a tall, mostly-transparent native surface
+                // that composites OVER and occludes the WebView content beneath it — the
+                // long-reported "blank section / stray logo below the ad". Real banner ads
+                // are already this size, so this is a no-op for them; only a pathological
+                // creative is clamped (and clipped) so it can never cover the page.
+                try {
+                    float dens = contextSupplier.get().getResources().getDisplayMetrics().density;
+                    int maxHpx = (int) (120 * dens); // generous banner ceiling (real banners ~50-90dp)
+                    com.google.android.gms.ads.AdSize sz = mAdView.getAdSize();
+                    int hPx = (sz != null && sz.getHeight() > 0) ? Math.min((int) (sz.getHeight() * dens), maxHpx) : maxHpx;
+                    int wPx = (sz != null && sz.getWidth() > 0)
+                        ? (int) (sz.getWidth() * dens)
+                        : android.widget.RelativeLayout.LayoutParams.WRAP_CONTENT;
+                    mAdView.setLayoutParams(new android.widget.RelativeLayout.LayoutParams(wPx, hPx));
+                    mAdView.setClipChildren(true);
+                    mAdViewLayout.setClipChildren(true);
+                    mAdViewLayout.setClipToPadding(true);
+                } catch (Exception ignored) {}
+                // Fresh AdView + fresh listener: no ad displayed yet.
+                bannerHasLoaded = false;
                 // Start loading the ad.
                 mAdView.loadAd(adRequest);
                 mAdView.setAdListener(
                     new AdListener() {
                         @Override
                         public void onAdLoaded() {
+                            bannerHasLoaded = true;
                             final BannerAdSizeInfo sizeInfo = new BannerAdSizeInfo(mAdView);
 
                             notifyListeners(BannerAdPluginEvents.SizeChanged.getWebEventName(), sizeInfo);
@@ -260,8 +340,19 @@ public class BannerExecutor extends Executor {
 
                         @Override
                         public void onAdFailedToLoad(@NonNull LoadAdError adError) {
+                            // Refresh no-fill on an already-displayed banner: keep the
+                            // last good ad on screen (Google's default contract) and emit
+                            // NOTHING. Emitting FailedToLoad would trip the JS resetVar()
+                            // listener and collapse --admob-banner-height to 0, reflowing
+                            // page content OVER the still-visible ad. Only the INITIAL
+                            // load failure (nothing shown yet) tears down + collapses.
+                            if (bannerHasLoaded) {
+                                super.onAdFailedToLoad(adError);
+                                return;
+                            }
+
                             if (mAdView != null) {
-                                mViewGroup.removeView(mAdViewLayout);
+                                if (mViewGroup != null) mViewGroup.removeView(mAdViewLayout);
                                 mAdViewLayout.removeView(mAdView);
                                 mAdView.destroy();
                                 mAdView = null;
@@ -297,7 +388,12 @@ public class BannerExecutor extends Executor {
                 );
 
                 // Add AdViewLayout top of the WebView
-                mViewGroup.addView(mAdViewLayout);
+                ensureViewGroup();
+                if (mViewGroup != null) {
+                    mViewGroup.addView(mAdViewLayout);
+                } else {
+                    Log.e(logTag, "Banner: mViewGroup null after ensureViewGroup, skip addView");
+                }
             });
     }
 }
diff --git a/node_modules/@capacitor-community/admob/ios/Sources/AdMobPlugin/Banner/BannerExecutor.swift b/node_modules/@capacitor-community/admob/ios/Sources/AdMobPlugin/Banner/BannerExecutor.swift
index 70081af..08efed3 100644
--- a/node_modules/@capacitor-community/admob/ios/Sources/AdMobPlugin/Banner/BannerExecutor.swift
+++ b/node_modules/@capacitor-community/admob/ios/Sources/AdMobPlugin/Banner/BannerExecutor.swift
@@ -7,6 +7,14 @@ class BannerExecutor: NSObject, BannerViewDelegate {
     var bannerView: BannerView!
     var adPosition: String = ""
     var Margin: Int = 0
+    // True once the CURRENT bannerView has successfully loaded at least one ad.
+    // AdMob auto-refresh re-invokes the delegate on every refresh; a refresh
+    // no-fill fires didFailToReceiveAdWithError. Upstream removes the banner on
+    // ANY failure, tearing down a perfectly-good displayed banner on the first
+    // refresh no-fill (the "banner disappears after a while" bug). Keep the last
+    // good ad on a refresh failure (Google's contract); remove ONLY on the
+    // initial no-fill (nothing shown yet).
+    var bannerHasLoaded = false
 
     func showBanner(_ call: CAPPluginCall, _ request: Request, _ adUnitID: String) {
         if let rootViewController = plugin?.getRootVC() {
@@ -41,6 +49,8 @@ class BannerExecutor: NSObject, BannerViewDelegate {
             }
 
             self.bannerView = BannerView(adSize: bannerSize)
+            // Fresh bannerView: no ad displayed yet.
+            self.bannerHasLoaded = false
             self.addBannerViewToView(self.bannerView, adPosition, adMargin)
             self.bannerView.translatesAutoresizingMaskIntoConstraints = false
             self.bannerView.adUnitID = adUnitID
@@ -119,6 +129,7 @@ class BannerExecutor: NSObject, BannerViewDelegate {
     /// Tells the delegate an ad request loaded an ad.
     func bannerViewDidReceiveAd(_ bannerView: BannerView) {
         NSLog("bannerViewDidReceiveAd")
+        self.bannerHasLoaded = true
         if let rootViewController = plugin?.getRootVC() {
             rootViewController.view.addSubview(bannerView)
             rootViewController.view.addConstraints(
@@ -150,6 +161,15 @@ class BannerExecutor: NSObject, BannerViewDelegate {
     func bannerView(_ bannerView: BannerView,
                     didFailToReceiveAdWithError error: Error) {
         NSLog("bannerView:didFailToReceiveAdWithError: \(error.localizedDescription)")
+        // Refresh no-fill on an already-displayed banner: keep the last good ad
+        // on screen (Google's default contract) and emit NOTHING. Emitting
+        // FailedToLoad would trip the JS resetVar() listener and collapse
+        // --admob-banner-height to 0, reflowing page content OVER the still-
+        // visible ad. Only the INITIAL load failure (nothing shown yet) removes
+        // the banner + collapses the reserved strip.
+        if self.bannerHasLoaded {
+            return
+        }
         self.removeBannerViewToView()
         self.plugin?.notifyListeners(BannerAdPluginEvents.SizeChanged.rawValue, data: [
             "width": 0,
