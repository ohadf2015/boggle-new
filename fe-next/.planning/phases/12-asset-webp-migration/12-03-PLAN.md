---
phase: 12-asset-webp-migration
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "WorldBackground component loads parallax images without 404 errors"
    - "All three worlds display correctly themed backgrounds"
    - "Parallax effect works (layers move at different depths)"
  artifacts:
    - path: "public/images/adventure/backgrounds/"
      provides: "World background images"
      contains: ["meadows.webp", "springs.webp", "caverns.webp"]
    - path: "public/images/adventure/parallax/"
      provides: "Parallax layer images"
  key_links:
    - from: "components/adventure/themed/WorldBackground.tsx"
      to: "public/images/adventure/parallax/"
      via: "backgroundImage: url(${layer.source})"
    - from: "lib/adventure/themes/world1.ts"
      to: "public/images/adventure/backgrounds/meadows.webp"
      via: "illustrationPath"
---

<objective>
Verify assets load correctly and clean up after human approval.

Purpose: Confirm 404 errors are resolved and world theming visual flow is restored
Output: Verified integration, optional backup cleanup
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-asset-webp-migration/12-01-SUMMARY.md
@.planning/phases/12-asset-webp-migration/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify static file serving</name>
  <files>public/images/adventure/backgrounds/, public/images/adventure/parallax/</files>
  <action>
    Verify files are accessible (without starting dev server):

    1. Check file existence and format:
    ```bash
    ls -la public/images/adventure/backgrounds/
    ls -la public/images/adventure/parallax/
    file public/images/adventure/backgrounds/*.webp
    file public/images/adventure/parallax/*.webp
    ```

    2. If dev server is running on localhost:3000, test HTTP access:
    ```bash
    curl -I http://localhost:3000/images/adventure/backgrounds/meadows.webp 2>/dev/null | head -5
    curl -I http://localhost:3000/images/adventure/parallax/meadows-hills.webp 2>/dev/null | head -5
    ```
    Expected: HTTP 200 OK with Content-Type: image/webp

    Note: If server not running, file existence verification is sufficient.
  </action>
  <verify>
    All files exist and are valid WebP format.
    HTTP requests return 200 (if server running).
  </verify>
  <done>Static file serving verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Asset restoration from backup:
    - 3 background images (meadows, springs, caverns)
    - 8 parallax layer images
    - All files in correct production locations
    - Git staging and .gitignore configured
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to adventure mode: http://localhost:3000/adventure
    3. Open browser dev tools (Network tab)
    4. Play a level in each world:
       - World 1 (Alphabet Meadows): Check for meadow background and parallax
       - World 2 (Synonym Springs): Check for springs background and parallax
       - World 3 (Root Caverns): Check for caverns background and parallax

    For each world, verify:
    - [ ] No 404 errors in Network tab for /images/adventure/
    - [ ] Background image visible behind game board
    - [ ] Parallax layers visible and moving slightly on scroll/tilt

    5. Check git status:
    ```bash
    git status
    ```
    - New files should be staged (11 WebP files)
    - Backup folders should NOT appear as untracked
  </how-to-verify>
  <resume-signal>Type "approved" if all worlds display correctly, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Optional cleanup (after approval)</name>
  <files>public/images/adventure-png-backup/</files>
  <action>
    After human verification passes, backup folders can optionally be removed.

    **Recommended**: Keep backups for now (they're gitignored, won't affect repo).

    **If user requests cleanup**:
    ```bash
    # Verify backups are gitignored first
    git check-ignore public/images/adventure-png-backup/

    # Remove backup folder (optional)
    # rm -rf public/images/adventure-png-backup/
    ```

    Note: This task is informational. Do NOT delete backups unless explicitly requested.
    The PNG originals may be useful for future re-processing.
  </action>
  <verify>
    If cleanup performed: backup folder no longer exists.
    If kept: backup folder exists but is gitignored.
  </verify>
  <done>Cleanup decision documented</done>
</task>

</tasks>

<verification>
1. All 11 WebP files load without 404 errors
2. Human verified visual rendering in all 3 worlds
3. Git status shows staged files, no untracked backup folders
4. Phase success criteria met
</verification>

<success_criteria>
- [ ] WorldBackground component loads parallax images without 404 errors
- [ ] Human verified all 3 worlds display correct themed backgrounds
- [ ] Parallax effect visible (layers at different depths)
- [ ] Git staging confirmed (11 new files)
- [ ] Backup folders gitignored (not in untracked)
</success_criteria>

<output>
After completion, create `.planning/phases/12-asset-webp-migration/12-03-SUMMARY.md`
</output>
