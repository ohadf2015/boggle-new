---
phase: 12-asset-webp-migration
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "New WebP files are staged in git"
    - "Backup folders are gitignored"
    - "No .png references remain in adventure code"
  artifacts:
    - path: ".gitignore"
      provides: "Backup folder exclusion"
      contains: ["adventure-png-backup", "collectibles/*-png-backup", "logos-png-backup"]
  key_links:
    - from: "git status"
      to: "staged files"
      via: "git add"
---

<objective>
Stage new WebP files in git and configure .gitignore for backup folders.

Purpose: Ensure new assets are tracked and backup folders won't accidentally be committed
Output: Git staging configured, .gitignore updated
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-asset-webp-migration/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage new WebP files with git</name>
  <files>public/images/adventure/backgrounds/, public/images/adventure/parallax/</files>
  <action>
    Stage all new WebP files for commit:
    ```bash
    git add public/images/adventure/backgrounds/
    git add public/images/adventure/parallax/
    ```

    Note: This plan may run in parallel with 12-01. If directories don't exist yet,
    wait for 12-01 to complete or run `git add` after files are copied.
  </action>
  <verify>
    ```bash
    git status --short public/images/adventure/backgrounds/ public/images/adventure/parallax/
    ```
    Shows files as staged (A = added).
  </verify>
  <done>11 WebP files staged for commit</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore for backup folders</name>
  <files>.gitignore</files>
  <action>
    Add backup folder patterns to .gitignore to prevent accidental commits:

    Check if patterns already exist:
    ```bash
    grep -E "(adventure-png-backup|collectibles.*-png-backup|logos-png-backup)" .gitignore
    ```

    If NOT already present, add to .gitignore (in "# Generated/backup assets" section or at end):
    ```
    # Asset backup folders (keep locally, don't commit)
    public/images/adventure-png-backup/
    public/collectibles/*-png-backup/
    public/logos-png-backup/
    ```

    Note: Use specific paths, not broad wildcards like `*backup*`.
  </action>
  <verify>
    ```bash
    grep "adventure-png-backup" .gitignore
    grep "collectibles.*-png-backup" .gitignore
    grep "logos-png-backup" .gitignore
    ```
    All three patterns present.
  </verify>
  <done>.gitignore updated with backup folder exclusions</done>
</task>

<task type="auto">
  <name>Task 3: Verify no .png references in adventure code</name>
  <files>lib/adventure/themes/*.ts, components/adventure/**/*.tsx</files>
  <action>
    Search for any remaining .png references that should be .webp:
    ```bash
    grep -r "\.png" lib/adventure/themes/ components/adventure/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test."
    ```

    Expected result: No matches, or only matches that are NOT related to backgrounds/parallax.

    If any background/parallax references still point to .png, they need to be updated.
    However, research confirmed code already references .webp paths, so this should pass.
  </action>
  <verify>
    ```bash
    grep -r "/images/adventure/backgrounds.*\.png" lib/adventure/themes/ components/adventure/ || echo "No .png background references found"
    grep -r "/images/adventure/parallax.*\.png" lib/adventure/themes/ components/adventure/ || echo "No .png parallax references found"
    ```
    Both commands should output "No .png references found".
  </verify>
  <done>Verified no stale .png references in adventure code</done>
</task>

</tasks>

<verification>
1. `git status` shows 11 new files staged in backgrounds/ and parallax/
2. `.gitignore` contains backup folder patterns
3. No .png references for backgrounds/parallax in theme configs or components
</verification>

<success_criteria>
- [ ] All 11 WebP files staged (`git status` shows them)
- [ ] Backup folders in .gitignore (3 patterns added)
- [ ] No stale .png references found in adventure code
</success_criteria>

<output>
After completion, create `.planning/phases/12-asset-webp-migration/12-02-SUMMARY.md`
</output>
