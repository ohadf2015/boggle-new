---
phase: 19-achievement-system
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - app/[locale]/student/page.tsx
  - app/[locale]/student/profile/page.tsx
  - components/education/PracticeSessionProvider.tsx
  - components/education/index.ts
autonomous: true

must_haves:
  truths:
    - "Leaderboard visible on student dashboard"
    - "Achievement unlocks trigger during practice sessions"
    - "Profile page shows achievement grid with progress"
    - "XP earning integrates with achievement progress tracking"
  artifacts:
    - path: "app/[locale]/student/page.tsx"
      provides: "Student dashboard with leaderboard"
      contains: "ClassroomLeaderboard"
    - path: "app/[locale]/student/profile/page.tsx"
      provides: "Student profile with achievements"
      contains: "EducationBadgeGrid"
  key_links:
    - from: "components/education/PracticeSessionProvider.tsx"
      to: "hooks/useAchievementUnlock.ts"
      via: "integrates achievement checking"
      pattern: "useAchievementUnlock"
---

<objective>
Integrate achievement system into student dashboard and practice flows.

Purpose: Wire all Phase 19 components together - leaderboard on dashboard, achievement unlocks during practice, badge grid on profile page.

Output: Updated student pages with achievement integration, PracticeSessionProvider with unlock detection.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-achievement-system/19-CONTEXT.md
@.planning/phases/19-achievement-system/19-01-SUMMARY.md
@.planning/phases/19-achievement-system/19-02-SUMMARY.md
@.planning/phases/19-achievement-system/19-03-SUMMARY.md
@.planning/phases/19-achievement-system/19-04-SUMMARY.md
@app/[locale]/student/page.tsx (existing dashboard)
@components/education/PracticeSessionProvider.tsx (XP integration point)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Student Dashboard with Leaderboard</name>
  <files>app/[locale]/student/page.tsx</files>
  <action>
Update student dashboard to include ClassroomLeaderboard:

**Changes:**
1. Import ClassroomLeaderboard from components/education
2. Get current classroom ID from student context/session
3. Add leaderboard section to dashboard layout

**Layout Integration:**
```
Student Dashboard
├── Welcome + Streak Display (existing)
├── Classroom Leaderboard (NEW)
│   └── Shows top 3 + current rank
├── Assigned Lessons (existing)
└── Continue Practice CTA (existing)
```

**Positioning:**
- Desktop: Sidebar or right column
- Mobile: Below welcome, above lessons

**Conditional Rendering:**
- Only show leaderboard if student is in a classroom
- Show "Join a classroom to see leaderboard" if no classroom

**No new tests** - page-level integration, tested via E2E or manual verification.
  </action>
  <verify>
Run: `npm run build` - no errors
Manual: Visit /en/student - leaderboard appears if in classroom
  </verify>
  <done>Dashboard shows classroom leaderboard for enrolled students</done>
</task>

<task type="auto">
  <name>Task 2: Practice Session Achievement Integration</name>
  <files>
    components/education/PracticeSessionProvider.tsx
    components/education/index.ts
  </files>
  <action>
Update PracticeSessionProvider to check for achievement unlocks:

**Changes to PracticeSessionProvider:**
1. Import useAchievementUnlock hook
2. After XP is awarded, call checkForUnlocks with updated progress
3. Render AchievementUnlockModal when currentUnlock is not null
4. Pass acknowledgeUnlock to modal onClose

**Flow:**
```
Student completes practice activity
  → awardPracticeXp calculates XP
  → XP is persisted to database
  → checkForUnlocks compares progress to thresholds
  → If unlock detected, add to queue
  → Modal displays for each unlock in queue
  → acknowledgeUnlock advances to next or clears
```

**Achievement Check Triggers:**
- After each flashcard session completion
- After each board practice completion
- After lesson is marked complete

**Progress Data to Track:**
- total_xp (from existing XP system)
- total_words_mastered (from student_lesson_progress)
- current_level (from XP system)
- current_streak (from XP system)
- total_practice_sessions (from XP system)
- bosses_defeated (new counter needed)
- combos_achieved (from practice session)

**Update barrel export:**
Add AchievementUnlockModal to components/education/index.ts exports.

**Tests (8+ tests):**
- Achievement check runs after XP award
- Modal renders when unlock detected
- Modal closes and advances queue
- Multiple unlocks show sequentially
- No re-trigger for already unlocked achievements
  </action>
  <verify>
Run: `npm run test:frontend -- PracticeSessionProvider`
Existing tests pass + 8 new tests pass
  </verify>
  <done>Practice sessions trigger achievement unlock checks and celebrations</done>
</task>

<task type="auto">
  <name>Task 3: Student Profile Page with Achievements</name>
  <files>app/[locale]/student/profile/page.tsx</files>
  <action>
Create or update student profile page to show achievement grid:

**If page doesn't exist, create it:**
```
app/[locale]/student/profile/page.tsx
```

**Page Layout:**
```
Student Profile
├── Profile Header
│   ├── Avatar + Display Name
│   ├── Level Badge + XP Progress
│   └── Streak Display
├── Achievement Grid (NEW)
│   ├── Overall Completion
│   ├── Pinned Badges
│   └── Category Sections
└── Statistics Section
    ├── Words Mastered
    ├── Lessons Completed
    └── Practice Sessions
```

**Data Fetching:**
1. Fetch student achievements from database
2. Pass to EducationBadgeGrid component
3. Handle pin updates (update student_achievements.is_pinned)

**Styling:**
- Neo-brutalist consistent with student dashboard
- Full-width on mobile, max-width container on desktop

**Navigation:**
- Add "View Profile" link to student dashboard header
- Add "Profile" tab to student navigation if using tabs

**No new tests** - page-level, tested via E2E or manual verification.
  </action>
  <verify>
Run: `npm run build` - no errors
Manual: Visit /en/student/profile - achievement grid appears with categories
  </verify>
  <done>Profile page displays full achievement grid with pinning</done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. All tests pass: `npm run test`
3. Student dashboard shows leaderboard (manual)
4. Practice completion triggers achievement check (manual)
5. Achievement modal appears on unlock (manual)
6. Profile page shows achievement grid (manual)
7. Pinning works and persists (manual)
</verification>

<success_criteria>
- Leaderboard visible on student dashboard
- Achievement unlocks trigger during practice
- Profile shows full achievement grid
- Pinning badges persists correctly
- Build and all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/19-achievement-system/19-05-SUMMARY.md`
</output>
