---
phase: 19-achievement-system
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - components/education/EducationBadgeGrid.tsx
  - components/education/EducationBadgeGrid.test.tsx
  - components/education/AchievementProgressCard.tsx
  - components/education/AchievementProgressCard.test.tsx
  - translations/en.js
  - translations/he.js
  - translations/sv.js
  - translations/ja.js
autonomous: true

must_haves:
  truths:
    - "Student sees all achievements in profile with completion progress"
    - "Student can pin up to 3 featured badges"
    - "Progress bars show 'Word Master Silver: 142/250 words'"
    - "Total completion shown: '60% Complete - 12/20 badges'"
    - "Locked badges show hints: '??? - Play 5 boss battles to unlock'"
  artifacts:
    - path: "components/education/EducationBadgeGrid.tsx"
      provides: "Achievement grid display for profile"
      min_lines: 150
    - path: "components/education/AchievementProgressCard.tsx"
      provides: "Individual achievement card with progress"
      min_lines: 80
  key_links:
    - from: "components/education/EducationBadgeGrid.tsx"
      to: "components/education/AchievementProgressCard.tsx"
      via: "renders cards"
      pattern: "AchievementProgressCard"
---

<objective>
Create profile badge display components for viewing earned achievements.

Purpose: GAMIFY-07 requirement - students view achievement badges in profile with completion progress, pinned badges, and hints for locked achievements.

Output: EducationBadgeGrid, AchievementProgressCard components, translations in 4 languages.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-achievement-system/19-CONTEXT.md
@.planning/phases/19-achievement-system/19-01-SUMMARY.md (achievement definitions)
@components/profile/ProfileAchievements.tsx (pattern reference)
@components/AchievementBadge.tsx (existing badge component)
</context>

<tasks>

<task type="auto">
  <name>Task 1: AchievementProgressCard Component</name>
  <files>
    components/education/AchievementProgressCard.tsx
    components/education/AchievementProgressCard.test.tsx
  </files>
  <action>
Create AchievementProgressCard component:

**Props:**
```typescript
interface AchievementProgressCardProps {
  achievement: {
    key: string;
    category: string;
    icon: string;
    isSecret: boolean;
    currentTier: 'bronze' | 'silver' | 'gold' | 'platinum' | null;
    progressValue: number;
    nextThreshold: number;
    isMaxTier: boolean;
  };
  isPinned: boolean;
  onTogglePin: (key: string) => void;
  canPin: boolean; // false if 3 already pinned and not this one
}
```

**UI Design (Neo-Brutalist):**

**Earned Badge (has currentTier):**
- Badge icon in tier-colored circle (bronze/silver/gold/platinum)
- Name + current tier label
- Progress bar to next tier: "142/250 words"
- Percent text: "57% to Silver"
- If max tier: "Max Tier!" badge in neo-yellow
- Pin button (star icon, filled if pinned)

**Locked Badge (currentTier = null, not secret):**
- Grayed out icon
- Name visible but muted
- Hint text: "Complete 1 lesson to unlock"
- Lock icon overlay
- No pin button

**Secret Badge (isSecret = true, locked):**
- "???" icon
- "Secret Achievement"
- Cryptic hint: "Hidden until discovered..."
- Mystery styling with question marks

**Styling:**
- Card: bg-neo-navy/50 border-2 border-neo-black rounded-neo
- Earned: full color, shadow-hard
- Locked: opacity-50, no shadow
- Progress bar: bg-neo-navy-light, fill with tier color
- Pin button: neo-yellow when pinned, neo-white/30 when unpinned

**Tests (12+ tests):**
- Renders earned badge with correct tier color
- Shows progress bar with current/next values
- Shows "Max Tier!" when at platinum
- Renders locked badge with hint
- Renders secret badge as "???"
- Pin button toggles on click
- Pin button disabled when canPin=false
- Uses correct translations
- Handles RTL layout
- Accessible: proper ARIA labels
  </action>
  <verify>
Run: `npm run test:frontend -- AchievementProgressCard`
All 12+ tests pass
  </verify>
  <done>Card shows achievement with tier, progress, and pin functionality</done>
</task>

<task type="auto">
  <name>Task 2: EducationBadgeGrid Component</name>
  <files>
    components/education/EducationBadgeGrid.tsx
    components/education/EducationBadgeGrid.test.tsx
    translations/en.js
    translations/he.js
    translations/sv.js
    translations/ja.js
  </files>
  <action>
Create EducationBadgeGrid component:

**Props:**
```typescript
interface EducationBadgeGridProps {
  studentId: string;
  achievements: StudentAchievement[]; // From database
  className?: string;
}
```

**UI Layout:**

1. **Header Section:**
   - "Achievements" title with trophy emoji
   - Completion summary: "60% Complete - 12/20 badges"
   - Progress bar for overall completion

2. **Pinned Badges Section** (if any pinned):
   - "Featured Badges" subheader with star icon
   - Horizontal row of 1-3 pinned badges (larger size)
   - Max 3 pins enforced

3. **Category Sections** (collapsible):
   - "Progress Milestones" (5 badges)
   - "Skill-Based Feats" (4 badges)
   - "Consistency Habits" (5 badges)
   - "Exploration" (4 badges)
   - Each section shows count: "3/5 earned"
   - Badges in 2-3 column grid

4. **Footer:**
   - Secret badges hint: "2 secret badges remain hidden..."

**Pin Logic:**
- Max 3 pinned badges at once
- Toggle pin: if < 3 pinned, allow pin; if already pinned, unpin
- Persist pins to database (student_achievements.is_pinned)

**Sorting:**
- Within each category: earned first (by tier desc), then locked
- Secret badges at end of their category

**Translations (add to education.achievements section):**
- title: "Achievements"
- completion: "{percent}% Complete - {earned}/{total} badges"
- featured: "Featured Badges"
- progress: "Progress Milestones"
- skill: "Skill-Based Feats"
- consistency: "Consistency Habits"
- exploration: "Exploration"
- earned: "{count}/{total} earned"
- secretRemaining: "{count} secret badges remain hidden..."
- maxPins: "Unpin another badge first"

For each achievement key, add translations:
- education.achievements.{key}.name: Achievement name
- education.achievements.{key}.description: Achievement description
- education.achievements.{key}.hint: Unlock hint for locked badges

**Tests (16+ tests):**
- Shows overall completion percentage
- Shows pinned badges section when badges pinned
- Groups badges by category
- Shows earned count per category
- Sorts earned badges before locked within category
- Enforces max 3 pins
- Shows secret badge hint count
- Handles empty state (no achievements)
- Handles all unlocked state
- Uses correct translations
- Handles RTL layout
- Category sections collapsible
  </action>
  <verify>
Run: `npm run test:frontend -- EducationBadgeGrid`
All 16+ tests pass
Run: `npm run lint` - no errors
  </verify>
  <done>Grid displays all achievements with categories, progress, and pinning</done>
</task>

</tasks>

<verification>
1. Card tests pass: `npm run test:frontend -- AchievementProgressCard`
2. Grid tests pass: `npm run test:frontend -- EducationBadgeGrid`
3. Pinning logic enforces max 3
4. Progress bars show correct values
5. Lint passes: `npm run lint`
6. Build passes: `npm run build`
7. Translation keys for all 18 achievements + UI text in 4 languages
</verification>

<success_criteria>
- Profile shows all achievements with tier progress
- Pin up to 3 featured badges
- Progress bars with "X/Y words" format
- Overall completion percentage displayed
- Locked badges show hints, secrets show "???"
- All tests passing, build passing
</success_criteria>

<output>
After completion, create `.planning/phases/19-achievement-system/19-04-SUMMARY.md`
</output>
