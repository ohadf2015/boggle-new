---
phase: 07-video-cutscenes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remotion/Root.tsx
  - remotion/compositions/LevelIntro/index.tsx
  - remotion/compositions/LevelIntro/types.ts
  - remotion/shared/components/TextOverlay.tsx
  - remotion/shared/utils/interpolations.ts
autonomous: true

must_haves:
  truths:
    - "Level intro composition renders 8-second video with world flyby animation"
    - "Ken Burns zoom effect creates dynamic camera movement"
    - "Composition accepts worldId and locale props for parameterized rendering"
    - "Hebrew text renders right-to-left when locale is 'he'"
  artifacts:
    - path: "remotion/compositions/LevelIntro/index.tsx"
      provides: "Level intro composition with Ken Burns zoom"
      exports: ["LevelIntro", "LevelIntroSchema"]
    - path: "remotion/compositions/LevelIntro/types.ts"
      provides: "Zod schema for composition props"
      exports: ["LevelIntroSchema", "LevelIntroProps"]
    - path: "remotion/shared/components/TextOverlay.tsx"
      provides: "RTL-aware text overlay component"
      exports: ["TextOverlay"]
    - path: "remotion/shared/utils/interpolations.ts"
      provides: "Reusable easing and interpolation helpers"
      exports: ["fadeIn", "fadeOut", "kenBurnsZoom"]
  key_links:
    - from: "remotion/Root.tsx"
      to: "remotion/compositions/LevelIntro/index.tsx"
      via: "Composition registration"
      pattern: "<Composition.*id=\"LevelIntro\""
    - from: "remotion/compositions/LevelIntro/index.tsx"
      to: "public/images/adventure/backgrounds/*.webp"
      via: "staticFile() asset loading"
      pattern: "staticFile.*backgrounds"
---

<objective>
Create Remotion LevelIntro composition for world flyby cutscenes (5-10s).

Purpose: Establish mood and atmosphere when entering a world with quick visual sweep - "establishing shots" not storytelling.

Output: LevelIntro composition registered in Root.tsx, renderable via CLI with worldId and locale props.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-video-cutscenes/07-CONTEXT.md
@.planning/phases/07-video-cutscenes/07-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@remotion/Root.tsx
@remotion/compositions/TestComposition.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared utilities and TextOverlay component</name>
  <files>
    remotion/shared/utils/interpolations.ts
    remotion/shared/components/TextOverlay.tsx
  </files>
  <action>
Create reusable utilities for Remotion compositions:

**interpolations.ts:**
- `fadeIn(frame, startFrame, duration)` - interpolate opacity 0->1
- `fadeOut(frame, startFrame, duration)` - interpolate opacity 1->0
- `kenBurnsZoom(frame, startFrame, duration, startScale, endScale)` - zoom effect
- All use `extrapolateRight: 'clamp'` and `extrapolateLeft: 'clamp'`

**TextOverlay.tsx:**
- RTL-aware text component for video overlays
- Props: `text: string`, `locale: 'en' | 'he' | 'sv' | 'ja'`, `style?: CSSProperties`
- Set `dir="rtl"` and `textAlign: 'right'` when locale is 'he'
- Use Fredoka font for non-Hebrew, Rubik for Hebrew (per design system)
- Apply neo-brutalist text shadow: `'4px 4px 0px black'`
- Use `unicodeBidi: 'embed'` for proper bidirectional text
  </action>
  <verify>
```bash
# TypeScript compiles without errors
npx tsc --noEmit remotion/shared/utils/interpolations.ts remotion/shared/components/TextOverlay.tsx
```
  </verify>
  <done>
Shared utilities export fadeIn, fadeOut, kenBurnsZoom functions.
TextOverlay component renders with RTL support and neo-brutalist styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LevelIntro composition</name>
  <files>
    remotion/compositions/LevelIntro/types.ts
    remotion/compositions/LevelIntro/index.tsx
  </files>
  <action>
Create the LevelIntro composition for world flyby cutscenes:

**types.ts requirements:**
- Zod schema with worldId enum ('meadows', 'springs', 'caverns') and locale enum
- Export inferred TypeScript type from schema

**index.tsx requirements:**
- Use Remotion's `AbsoluteFill`, `useCurrentFrame`, `staticFile` APIs
- Import and use shared utilities (fadeIn, fadeOut, kenBurnsZoom, TextOverlay)

**Duration and timing:**
- Total: 240 frames (8 seconds at 30fps) - within 5-10s guideline
- Ken Burns effect: zoom from 1.15x to 1.0x over first 180 frames (6s)
- Fade in: 0-15 frames (0.5s)
- Fade out: 210-240 frames (1s)
- World name text overlay: last 90 frames only, bottom center

**Visual requirements:**
- Background image: Load from `public/images/adventure/backgrounds/${worldId}.webp` using staticFile
- Neo-navy fallback background: #1a1a2e
- Ken Burns via transform: scale() on background div
- backgroundSize: 'cover', backgroundPosition: 'center'

**World names (for text overlay):**
- Provide translation lookup object for all 4 locales
- meadows: "Alphabet Meadows" / Hebrew / Swedish / Japanese equivalents
- springs: "Synonym Springs" equivalents
- caverns: "Root Caverns" equivalents

**Audio:** No Audio component yet (audio sourcing is open question from research)
  </action>
  <verify>
```bash
# Preview composition in Remotion Studio
npm run remotion:preview
# Navigate to LevelIntro composition, verify it loads
```
  </verify>
  <done>
LevelIntro composition displays world background with Ken Burns zoom, fade in/out, and world name text overlay.
Accepts worldId and locale props for parameterized rendering.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register composition in Root.tsx and test render</name>
  <files>
    remotion/Root.tsx
  </files>
  <action>
Update Root.tsx to register LevelIntro composition:

1. Import LevelIntro and LevelIntroSchema from './compositions/LevelIntro'
2. Add new Composition entry with:
   - id: "LevelIntro"
   - durationInFrames: 240
   - fps: 30
   - width: 1920, height: 1080
   - schema: LevelIntroSchema
   - defaultProps: { worldId: 'meadows', locale: 'en' }

3. Test render with CLI for meadows world, English locale

4. Verify output file exists and plays correctly
  </action>
  <verify>
```bash
# Render test video
npx remotion render remotion/index.ts LevelIntro out/level-intro-test.mp4 --props='{"worldId":"meadows","locale":"en"}' --codec=h264

# Verify file exists and has reasonable size (should be <500KB for 8s)
ls -la out/level-intro-test.mp4
```
  </verify>
  <done>
LevelIntro composition registered in Root.tsx.
Test render produces valid MP4 file with Ken Burns zoom animation.
  </done>
</task>

</tasks>

<verification>
1. `npm run remotion:preview` opens studio without errors
2. LevelIntro composition visible in studio sidebar
3. Preview shows world background with zoom animation
4. Hebrew locale shows RTL text alignment
5. Test render produces playable MP4 file
</verification>

<success_criteria>
- LevelIntro composition renders 8-second video
- Ken Burns zoom effect visible (1.15x -> 1.0x)
- World name appears with fade-in during last 3 seconds
- Hebrew text renders right-to-left
- Render command produces MP4 < 500KB
</success_criteria>

<output>
After completion, create `.planning/phases/07-video-cutscenes/07-01-SUMMARY.md`
</output>
