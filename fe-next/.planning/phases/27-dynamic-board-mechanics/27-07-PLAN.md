---
phase: 27-dynamic-board-mechanics
plan: 07
type: execute
wave: 5
depends_on: ["27-06"]
files_modified:
  - components/adventure/AdventureTile.css
  - components/adventure/__tests__/AdventureTile.specialStyles.test.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User sees locked tiles with distinct visual appearance (grey overlay, padlock indicator)"
    - "User sees multiplier tiles with distinct visual appearance (pulsing glow, 2x badge)"
    - "Locked tile styling matches neo-brutalist design system (hard shadows, bold colors)"
    - "Multiplier tile styling matches neo-brutalist design system (lime/gold accents)"
    - "Reduced motion users see static visual feedback without animation"
  artifacts:
    - path: "components/adventure/AdventureTile.css"
      provides: "CSS styling for locked and multiplier tiles"
      contains: "tile-locked-enhanced"
    - path: "components/adventure/AdventureTile.css"
      provides: "CSS styling for locked and multiplier tiles"
      contains: "tile-multiplier-enhanced"
    - path: "components/adventure/__tests__/AdventureTile.specialStyles.test.tsx"
      provides: "Visual styling tests for locked and multiplier tiles"
      min_lines: 50
  key_links:
    - from: "components/adventure/AdventureTile.tsx"
      to: "components/adventure/AdventureTile.css"
      via: "CSS class application"
      pattern: "tile-locked-enhanced|tile-multiplier-enhanced"
---

<objective>
Add missing CSS styling for locked and multiplier tile types.

Purpose: Close gap from Phase 27 verification — locked and multiplier tiles have logic but NO visual styling. Users cannot distinguish them from standard tiles, violating the "distinct visuals" requirement.

Output: Complete CSS styling for tile-locked-enhanced and tile-multiplier-enhanced classes following existing tile styling patterns (ice, gold, bomb, etc.).
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-dynamic-board-mechanics/27-VERIFICATION.md

# Existing styling patterns to follow
@components/adventure/AdventureTile.css - Existing tile styling (ice-enhanced, gold-enhanced patterns)
@components/adventure/AdventureTile.tsx - Component using these CSS classes (lines 157-158)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write visual styling tests for locked and multiplier tiles (TDD RED)</name>
  <files>components/adventure/__tests__/AdventureTile.specialStyles.test.tsx</files>
  <action>
    Create tests verifying CSS classes exist and have expected styling patterns.

    Test approach: Since we can't test actual CSS rendering in Jest, verify:
    1. CSS file contains the expected class definitions
    2. Class naming follows project conventions
    3. Keyframe animations are defined for both tile types
    4. Reduced motion media query includes new tile types

    Test cases for LOCKED tiles:
    - tile-locked-enhanced class exists in CSS
    - Keyframe animation for locked glow/pulse defined
    - Reduced motion override for tile-locked-enhanced defined

    Test cases for MULTIPLIER tiles:
    - tile-multiplier-enhanced class exists in CSS
    - Keyframe animation for multiplier glow defined
    - Reduced motion override for tile-multiplier-enhanced defined

    Test pattern (read CSS file and check for patterns):
    ```typescript
    import { readFileSync } from 'fs';
    import { join } from 'path';

    describe('AdventureTile Special Styles', () => {
      const cssContent = readFileSync(
        join(__dirname, '../AdventureTile.css'),
        'utf-8'
      );

      describe('Locked Tile Styling', () => {
        it('should define tile-locked-enhanced class', () => {
          expect(cssContent).toMatch(/\.tile-locked-enhanced\s*\{/);
        });

        it('should define locked glow keyframe animation', () => {
          expect(cssContent).toMatch(/@keyframes locked-/);
        });

        it('should include tile-locked-enhanced in reduced motion rules', () => {
          expect(cssContent).toMatch(
            /@media \(prefers-reduced-motion: reduce\)[\s\S]*tile-locked-enhanced/
          );
        });
      });

      describe('Multiplier Tile Styling', () => {
        it('should define tile-multiplier-enhanced class', () => {
          expect(cssContent).toMatch(/\.tile-multiplier-enhanced\s*\{/);
        });

        it('should define multiplier glow keyframe animation', () => {
          expect(cssContent).toMatch(/@keyframes multiplier-/);
        });

        it('should include tile-multiplier-enhanced in reduced motion rules', () => {
          expect(cssContent).toMatch(
            /@media \(prefers-reduced-motion: reduce\)[\s\S]*tile-multiplier-enhanced/
          );
        });
      });
    });
    ```

    Run tests to verify they FAIL (RED phase):
    ```bash
    npm run test:frontend -- AdventureTile.specialStyles
    ```
  </action>
  <verify>
    - Tests exist: `ls components/adventure/__tests__/AdventureTile.specialStyles.test.tsx`
    - Tests FAIL (RED): `npm run test:frontend -- AdventureTile.specialStyles` returns non-zero
  </verify>
  <done>Test file created with 6+ failing tests for locked and multiplier CSS</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styling for locked and multiplier tiles (TDD GREEN)</name>
  <files>components/adventure/AdventureTile.css</files>
  <action>
    Add CSS styling following existing patterns (ice-enhanced, gold-enhanced, bomb-enhanced).

    **LOCKED TILE STYLING** (grey/steel theme - restricted/blocked feel):
    ```css
    /* ==============================================
       LOCKED TILE EFFECTS
       ============================================== */

    /* Locked pulse - subtle warning that tile is restricted */
    @keyframes locked-pulse {
      0%, 100% {
        box-shadow:
          0 0 8px rgba(128, 128, 128, 0.4),
          0 0 16px rgba(64, 64, 64, 0.2);
        filter: brightness(0.9) saturate(0.7);
      }
      50% {
        box-shadow:
          0 0 12px rgba(128, 128, 128, 0.6),
          0 0 24px rgba(64, 64, 64, 0.3);
        filter: brightness(0.85) saturate(0.6);
      }
    }

    /* Locked chain overlay - diagonal striped pattern */
    @keyframes locked-chains {
      0% {
        background-position: 0 0;
      }
      100% {
        background-position: 10px 10px;
      }
    }

    .tile-locked-enhanced {
      position: relative;
      overflow: hidden;
      animation: locked-pulse 2.5s ease-in-out infinite;
      /* Desaturated overlay to show locked state */
      filter: saturate(0.6) brightness(0.9);
    }

    /* Diagonal chain pattern overlay */
    .tile-locked-enhanced::before {
      content: '';
      position: absolute;
      inset: 0;
      background: repeating-linear-gradient(
        45deg,
        rgba(64, 64, 64, 0.15) 0px,
        rgba(64, 64, 64, 0.15) 2px,
        transparent 2px,
        transparent 8px
      );
      animation: locked-chains 2s linear infinite;
      pointer-events: none;
      z-index: 3;
    }

    /* Steel bar overlay */
    .tile-locked-enhanced::after {
      content: '';
      position: absolute;
      inset: 2px;
      border-radius: inherit;
      background: linear-gradient(
        180deg,
        rgba(128, 128, 128, 0.3) 0%,
        transparent 30%,
        transparent 70%,
        rgba(64, 64, 64, 0.2) 100%
      );
      pointer-events: none;
      z-index: 4;
    }
    ```

    **MULTIPLIER TILE STYLING** (lime/gold theme - bonus/reward feel):
    ```css
    /* ==============================================
       MULTIPLIER TILE EFFECTS
       ============================================== */

    /* Multiplier power pulse - energetic glow */
    @keyframes multiplier-pulse {
      0%, 100% {
        box-shadow:
          0 0 10px rgba(212, 255, 0, 0.5),
          0 0 20px rgba(255, 215, 0, 0.3);
        filter: brightness(1.1);
      }
      50% {
        box-shadow:
          0 0 20px rgba(212, 255, 0, 0.8),
          0 0 40px rgba(255, 215, 0, 0.5),
          0 0 60px rgba(212, 255, 0, 0.2);
        filter: brightness(1.25);
      }
    }

    /* Radiant energy lines */
    @keyframes multiplier-radiate {
      0% {
        transform: rotate(0deg);
        opacity: 0.4;
      }
      100% {
        transform: rotate(360deg);
        opacity: 0.4;
      }
    }

    /* Sparkle burst */
    @keyframes multiplier-sparkle {
      0%, 100% {
        opacity: 0;
        transform: scale(0.5);
      }
      50% {
        opacity: 1;
        transform: scale(1);
      }
    }

    .tile-multiplier-enhanced {
      position: relative;
      overflow: visible;
      animation: multiplier-pulse 1.8s ease-in-out infinite;
      /* Slight golden tint */
      filter: brightness(1.1) saturate(1.2);
    }

    /* Radiant energy overlay */
    .tile-multiplier-enhanced::before {
      content: '';
      position: absolute;
      inset: -4px;
      border: 2px dashed rgba(212, 255, 0, 0.5);
      border-radius: 50%;
      animation: multiplier-radiate 4s linear infinite;
      pointer-events: none;
      z-index: 2;
    }

    /* Inner glow highlight */
    .tile-multiplier-enhanced::after {
      content: '';
      position: absolute;
      inset: 2px;
      border-radius: inherit;
      background: radial-gradient(
        circle at 30% 30%,
        rgba(255, 255, 255, 0.4) 0%,
        rgba(212, 255, 0, 0.2) 30%,
        transparent 60%
      );
      pointer-events: none;
      z-index: 3;
    }
    ```

    **Add to REDUCED MOTION section** (around line 793):
    Update the existing @media (prefers-reduced-motion: reduce) block to include:
    - .tile-locked-enhanced
    - .tile-multiplier-enhanced
    - .tile-locked-enhanced::before
    - .tile-multiplier-enhanced::before

    And add static fallbacks:
    ```css
    .tile-locked-enhanced {
      filter: saturate(0.6) brightness(0.9);
      box-shadow: 0 0 8px rgba(128, 128, 128, 0.4);
    }

    .tile-multiplier-enhanced {
      filter: brightness(1.1) saturate(1.2);
      box-shadow: 0 0 15px rgba(212, 255, 0, 0.5);
    }
    ```

    Insert CSS in appropriate locations following file organization.
  </action>
  <verify>
    - Tests PASS (GREEN): `npm run test:frontend -- AdventureTile.specialStyles`
    - TypeScript compiles: `npx tsc --noEmit`
    - Lint passes: `npm run lint`
  </verify>
  <done>CSS styling added for tile-locked-enhanced and tile-multiplier-enhanced, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify complete Phase 27 gap closure</name>
  <files>N/A - verification only</files>
  <action>
    Run complete verification to confirm gap is closed:

    1. Run new styling tests:
       ```bash
       npm run test:frontend -- AdventureTile.specialStyles
       ```

    2. Run ALL Phase 27 tests to check for regressions:
       ```bash
       npm run test:frontend -- useCascadeLoop useSpecialTileActivation AdventureGrid.framerLayout ExplosionEffect AdventureGame.cascade AdventureGame.specialTiles AdventureTile.specialStyles
       ```

    3. Verify build succeeds:
       ```bash
       npm run build
       ```

    4. Confirm CSS classes are properly linked:
       - AdventureTile.tsx line 157: `locked: 'tile-locked-enhanced'`
       - AdventureTile.tsx line 158: `multiplier: 'tile-multiplier-enhanced'`
       - AdventureTile.css: Both classes now defined

    5. Visual spot-check (optional):
       - Start dev server: `npm run dev`
       - Navigate to adventure mode
       - Confirm locked/multiplier tiles (if present in level) have distinct appearance
  </action>
  <verify>
    - All Phase 27 tests pass
    - Build succeeds
    - CSS properly integrated
  </verify>
  <done>Phase 27 gap closed: locked and multiplier tiles now have distinct visual styling</done>
</task>

</tasks>

<verification>
```bash
# Run new styling tests
npm run test:frontend -- AdventureTile.specialStyles

# Run all Phase 27 tests
npm run test:frontend -- useCascadeLoop useSpecialTileActivation AdventureGrid.framerLayout ExplosionEffect AdventureGame.cascade AdventureGame.specialTiles AdventureTile.specialStyles

# Verify TypeScript
npx tsc --noEmit

# Verify lint
npm run lint

# Verify build
npm run build
```
</verification>

<success_criteria>
- [ ] tile-locked-enhanced CSS class defined with animations
- [ ] tile-multiplier-enhanced CSS class defined with animations
- [ ] Reduced motion support for both tile types
- [ ] All new tests pass
- [ ] All existing Phase 27 tests pass (no regressions)
- [ ] TypeScript compiles without errors
- [ ] Lint passes
- [ ] Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/27-dynamic-board-mechanics/27-07-SUMMARY.md`
</output>
