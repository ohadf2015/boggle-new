---
phase: 26-meta-progression-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/utils/adventureXpUtils.ts
  - shared/utils/__tests__/adventureXpUtils.test.ts
autonomous: true

must_haves:
  truths:
    - "Adventure XP is calculated using exponential curve formula"
    - "Level is correctly derived from total XP"
    - "XP progress percentage is accurate within level"
    - "Level up detection works when XP crosses threshold"
  artifacts:
    - path: "shared/utils/adventureXpUtils.ts"
      provides: "XP calculation, level derivation, progress percentage"
      exports: ["getXpForLevel", "getLevelFromXp", "getXpProgress", "checkLevelUp", "calculateAdventureXp"]
      min_lines: 80
    - path: "shared/utils/__tests__/adventureXpUtils.test.ts"
      provides: "TDD tests for adventure XP utilities"
      min_lines: 150
  key_links:
    - from: "shared/utils/adventureXpUtils.ts"
      to: "backend/modules/xpManager.ts"
      via: "Similar formula pattern, adventure-specific"
      pattern: "getXpForLevel|getLevelFromXp"
---

<objective>
Implement adventure-specific XP calculation utilities using TDD.

Purpose: Adventure mode needs its own XP progression system separate from education XP. This creates the core business logic that UI components will consume.

Output: Tested utilities for XP calculation, level derivation, and level-up detection.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-meta-progression-foundation/26-RESEARCH.md
@backend/modules/xpManager.ts
@shared/utils/xpUtils.ts
</context>

<feature>
  <name>Adventure XP Utilities</name>
  <files>shared/utils/adventureXpUtils.ts, shared/utils/__tests__/adventureXpUtils.test.ts</files>
  <behavior>
    Adventure XP uses an exponential curve (RuneScape-inspired formula).

    Expected behaviors:
    - getXpForLevel(1) -> 0 (level 1 requires 0 XP)
    - getXpForLevel(2) -> ~100 (early levels are fast)
    - getXpForLevel(10) -> ~2000 (mid levels moderate)
    - getXpForLevel(30) -> ~20000 (late levels slower)
    - getLevelFromXp(0) -> 1
    - getLevelFromXp(150) -> 2
    - getXpProgress(totalXp) -> { currentLevel, progressPercent, xpInCurrentLevel, xpNeededForNextLevel, isMaxLevel }
    - checkLevelUp(oldLevel, newLevel) -> { leveledUp: boolean, newLevel?: number }
    - calculateAdventureXp(difficulty, combo, bonuses) -> number

    Formula: XP per level = floor(i + 300 * 2^(i/7)) / 4
    Max level: 50 (soft cap with linear progression after)
  </behavior>
  <implementation>
    1. Create test file with failing tests covering all edge cases
    2. Implement getXpForLevel with exponential formula
    3. Implement getLevelFromXp using iterative search
    4. Implement getXpProgress for UI consumption
    5. Implement checkLevelUp for level-up detection
    6. Implement calculateAdventureXp for game integration
    7. Export all functions and types
  </implementation>
</feature>

<verification>
```bash
# Run adventure XP tests
npm run test:backend -- adventureXpUtils.test.ts --coverage

# Verify exports
grep -E "export (function|const|type)" shared/utils/adventureXpUtils.ts
```
</verification>

<success_criteria>
- All tests pass (expect 15+ test cases)
- Coverage >= 90% for adventureXpUtils.ts
- Exponential curve produces sensible values (level 10 requires ~10x more XP than level 2)
- No dependencies on external state (pure functions)
</success_criteria>

<output>
After completion, create `.planning/phases/26-meta-progression-foundation/26-01-SUMMARY.md`
</output>
