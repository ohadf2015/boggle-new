---
phase: 26-meta-progression-foundation
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - hooks/useScreenShake.ts
  - hooks/__tests__/useScreenShake.test.ts
autonomous: true

must_haves:
  truths:
    - "Screen shake triggers with configurable intensity (2-8px)"
    - "Screen shake respects prefers-reduced-motion preference"
    - "Screen shake uses CSS transform (not layout properties)"
    - "Shake duration is configurable (100-300ms)"
  artifacts:
    - path: "hooks/useScreenShake.ts"
      provides: "Screen shake hook with Web Animations API"
      exports: ["useScreenShake"]
      min_lines: 60
    - path: "hooks/__tests__/useScreenShake.test.ts"
      provides: "TDD tests for screen shake hook"
      min_lines: 80
  key_links:
    - from: "hooks/useScreenShake.ts"
      to: "hooks/useDevicePerformance.ts"
      via: "Reduced motion detection"
      pattern: "prefersReducedMotion"
---

<objective>
Implement screen shake hook using TDD for game juice feedback.

Purpose: Screen shake provides visceral feedback on impactful events (combos, level ups). Must use transform (GPU-accelerated) and respect accessibility preferences.

Output: Tested hook that shakes any element with configurable intensity and duration.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-meta-progression-foundation/26-RESEARCH.md
@hooks/useDevicePerformance.ts
</context>

<feature>
  <name>Screen Shake Hook</name>
  <files>hooks/useScreenShake.ts, hooks/__tests__/useScreenShake.test.ts</files>
  <behavior>
    useScreenShake() returns:
    - shakeRef: RefObject<HTMLDivElement> - attach to container element
    - shake(intensity?, duration?) - trigger shake animation

    Parameters:
    - intensity: number (2-8px, default 4px)
    - duration: number (100-300ms, default 200ms)

    Behavior:
    - Uses Web Animations API (element.animate())
    - Only animates transform property (GPU-accelerated)
    - Returns early if prefers-reduced-motion is enabled
    - Provides flash feedback as reduced-motion alternative
    - Keyframes: translate pattern creates random-feeling shake
  </behavior>
  <implementation>
    1. Create test file with failing tests covering:
       - Basic shake animation triggers
       - Reduced motion preference skips animation
       - Intensity scales shake magnitude
       - Duration controls animation length
       - Flash feedback for reduced motion users
    2. Implement useScreenShake hook
    3. Use useDevicePerformance for reduced motion detection
    4. Use Web Animations API for performant animations
  </implementation>
</feature>

<verification>
```bash
# Run screen shake tests
npm run test:frontend -- useScreenShake.test.ts --coverage

# Verify no layout properties animated
grep -E "(width|height|left|top|margin)" hooks/useScreenShake.ts && echo "ERROR: Found layout properties" || echo "OK: No layout properties"
```
</verification>

<success_criteria>
- All tests pass (expect 10+ test cases)
- Hook uses only transform property for animations
- Reduced motion preference is respected
- No layout thrashing (only transform/opacity)
</success_criteria>

<output>
After completion, create `.planning/phases/26-meta-progression-foundation/26-03-SUMMARY.md`
</output>
