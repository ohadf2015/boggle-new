---
phase: 11-teacher-vocabulary-builder
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - components/teacher/TeacherDashboard.tsx
  - components/teacher/ClassroomManager.tsx
  - components/teacher/LessonBuilder.tsx
  - components/teacher/StudentProgressView.tsx
  - components/teacher/ClassProgressChart.tsx
  - app/[locale]/teacher/page.tsx
  - app/[locale]/teacher/layout.tsx
autonomous: false

must_haves:
  truths:
    - "Teacher dashboard shows list of classrooms with member counts"
    - "Classroom manager allows create/edit/delete classrooms with join codes"
    - "Lesson builder displays word list with canIntegrate indicators"
    - "Progress chart shows words learned and accuracy over time"
  artifacts:
    - path: "components/teacher/TeacherDashboard.tsx"
      provides: "Main teacher dashboard view"
      min_lines: 100
    - path: "components/teacher/ClassProgressChart.tsx"
      provides: "Recharts visualization of class progress"
      contains: "import { LineChart"
    - path: "app/[locale]/teacher/page.tsx"
      provides: "Teacher dashboard route"
      exports: ["default"]
  key_links:
    - from: "components/teacher/TeacherDashboard.tsx"
      to: "hooks/useClassroom.ts"
      via: "import useClassrooms"
      pattern: "useClassrooms"
    - from: "components/teacher/ClassProgressChart.tsx"
      to: "recharts"
      via: "import LineChart"
      pattern: "LineChart.*XAxis.*YAxis"
---

<objective>
Build the Teacher Dashboard UI with classroom management, lesson builder, and progress visualization.

Purpose: Provide teachers with a comprehensive interface to manage classrooms, create vocabulary lessons, and track student progress.
Output: Full teacher dashboard with Recharts visualizations and neo-brutalist styling.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-teacher-vocabulary-builder/11-RESEARCH.md
@.planning/phases/11-teacher-vocabulary-builder/11-03-SUMMARY.md
@components/admin/InvalidWordsManager.tsx
@fe-next/CLAUDE.md (design system)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teacher dashboard route and layout</name>
  <files>app/[locale]/teacher/page.tsx, app/[locale]/teacher/layout.tsx</files>
  <action>
Create Next.js App Router pages for teacher dashboard:

**layout.tsx:**
- Use AuthGuard to require authentication
- Check user_role === 'teacher' or 'admin', redirect if not
- Provide TeacherDashboardContext if needed
- Neo-brutalist dark background (neo-navy gradient)

**page.tsx:**
- Default export TeacherDashboard component
- Handle loading/error states
- Use translations via t() for all text
- Mobile-responsive layout

Add translation keys to translations/en.json, he.json, sv.json, ja.json:
```json
"teacher": {
  "dashboard": {
    "title": "Teacher Dashboard",
    "classrooms": "Classrooms",
    "lessons": "Vocabulary Lessons",
    "students": "Students",
    "progress": "Progress"
  },
  "classroom": {
    "create": "Create Classroom",
    "name": "Classroom Name",
    "joinCode": "Join Code",
    "copyCode": "Copy Code",
    "members": "{{count}} students"
  },
  "lesson": {
    "create": "Create Lesson",
    "words": "{{count}} words",
    "canIntegrate": "Can embed in games",
    "cannotIntegrate": "Track only"
  }
}
```
  </action>
  <verify>Page renders at /en/teacher without errors</verify>
  <done>Teacher dashboard route with auth guard and translation keys.</done>
</task>

<task type="auto">
  <name>Task 2: Create classroom and lesson components</name>
  <files>components/teacher/TeacherDashboard.tsx, components/teacher/ClassroomManager.tsx, components/teacher/LessonBuilder.tsx</files>
  <action>
Create teacher dashboard components following neo-brutalist design system:

**TeacherDashboard.tsx:**
- Tab navigation: Classrooms | Lessons | Students | Progress
- Use Radix Tabs component
- Show classroom list or lesson list based on active tab
- Empty states with call-to-action buttons
- shadow-hard styling, border-neo borders

**ClassroomManager.tsx:**
- List classrooms in card grid (3 cols lg, 1 col mobile)
- Each card shows: name, join code (with copy button), member count, language
- Create classroom modal with Radix Dialog
- Edit/delete classroom with confirmation
- Join code displayed prominently (large font, copy on click)

**LessonBuilder.tsx:**
- Lesson name and description inputs
- Word list with:
  - Checkmark icon (green) for canIntegrate=true
  - Warning icon (yellow) for canIntegrate=false
- Add word input (validates via useWordIntegration)
- Drag-to-reorder words (optional, can use simple up/down buttons)
- Assign to classroom dropdown
- Save button with loading state

All components must:
- Use t() for all text
- Support RTL (Hebrew)
- Follow neo-brutalist design (shadow-hard, border-neo, dark theme)
- Be responsive (container queries where appropriate)
  </action>
  <verify>Components render without TypeScript errors: `npx tsc --noEmit components/teacher/*.tsx`</verify>
  <done>Dashboard, classroom manager, and lesson builder components with neo-brutalist styling.</done>
</task>

<task type="auto">
  <name>Task 3: Create progress visualization components</name>
  <files>components/teacher/StudentProgressView.tsx, components/teacher/ClassProgressChart.tsx</files>
  <action>
Create progress tracking components with Recharts:

**StudentProgressView.tsx:**
- Table of students with: name, words attempted, words mastered, accuracy %, last active
- Click student row to expand and show per-word breakdown
- Pagination (20 students per page)
- Sort by column headers
- Export to CSV button (optional)

**ClassProgressChart.tsx:**
- Recharts LineChart showing class aggregate progress over time
- X-axis: dates
- Y-axis (left): words learned (cumulative)
- Y-axis (right): accuracy %
- Tooltip with detailed info
- Legend for multiple metrics
- ResponsiveContainer for mobile support
- Neo-brutalist styling (dark background, high-contrast colors: neo-cyan, neo-pink)

```tsx
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';

// Use neo-brutalist colors
const CHART_COLORS = {
  wordsLearned: '#00FFFF', // neo-cyan
  accuracy: '#FF1493', // neo-pink
  grid: '#333333',
  text: '#FFFFFF'
};
```

Add empty state when no data: "No progress data yet. Assign lessons to start tracking."
  </action>
  <verify>Recharts imports work: `grep -r "LineChart" components/teacher/ClassProgressChart.tsx`</verify>
  <done>Student progress table and class progress chart with Recharts visualization.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Teacher Dashboard UI with classroom management, lesson builder, and progress charts</what-built>
  <how-to-verify>
1. Navigate to http://localhost:3000/en/teacher (must be logged in as teacher)
2. Verify Classrooms tab:
   - Click "Create Classroom"
   - Enter name, verify join code is generated
   - Copy join code works
3. Verify Lessons tab:
   - Click "Create Lesson"
   - Add words, verify checkmark/warning icons show correctly
   - Save lesson
4. Verify Progress tab:
   - View progress chart (may be empty initially)
   - Check chart is responsive on mobile
5. Test Hebrew RTL: Switch to /he/teacher
6. Verify neo-brutalist styling (dark theme, hard shadows)
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] /[locale]/teacher route accessible to teachers only
- [ ] Classroom creation shows generated join code
- [ ] Lesson builder shows word integration status
- [ ] Progress chart renders with Recharts
- [ ] All text uses t() translations
- [ ] RTL works for Hebrew
- [ ] Neo-brutalist design applied consistently
</verification>

<success_criteria>
Teachers can access a dashboard to manage classrooms, create vocabulary lessons with word integration indicators, and view student progress over time with Recharts visualizations. UI follows neo-brutalist design and supports all 4 languages.
</success_criteria>

<output>
After completion, create `.planning/phases/11-teacher-vocabulary-builder/11-05-SUMMARY.md`
</output>
