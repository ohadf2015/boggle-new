---
phase: 24-crazygames-portal-integration
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - hooks/useCrazyGamesLifecycle.ts
  - components/game/InGameScreen.tsx
  - components/views/ResultsPage.tsx
  - components/multiplayer/MultiplayerFlow.tsx
  - components/singleplayer/game/hooks/useSinglePlayerCore.ts
autonomous: true

must_haves:
  truths:
    - "gameplayStart() is called when actual gameplay begins (timer starts)"
    - "gameplayStop() is called when gameplay ends (round over, pause menu, ads)"
    - "happytime() is called on major achievements (high score, boss defeat)"
    - "Initial download size measurement reflects actual gameplay period only"
  artifacts:
    - path: "hooks/useCrazyGamesLifecycle.ts"
      provides: "Centralized gameplay lifecycle management"
      exports: ["useCrazyGamesLifecycle", "CrazyGamesLifecycleProvider"]
  key_links:
    - from: "components/game/InGameScreen.tsx"
      to: "hooks/useCrazyGamesLifecycle.ts"
      via: "useGameplayTracking"
      pattern: "startGameplay|stopGameplay"
    - from: "components/views/ResultsPage.tsx"
      to: "hooks/useCrazyGamesLifecycle.ts"
      via: "stopGameplay on mount"
      pattern: "stopGameplay"
---

<objective>
Integrate CrazyGames SDK gameplay lifecycle events throughout the application to accurately track when players are actively playing.

Purpose: CrazyGames uses gameplayStart/Stop to measure initial download size (assets loaded before first gameplayStart). Accurate lifecycle tracking also enables proper ad timing - ads should only show during gameplayStop periods.

Output: Lifecycle hook and integration points that correctly report gameplay state to CrazyGames SDK.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-crazygames-portal-integration/24-RESEARCH.md

# Existing lifecycle hook
@hooks/useCrazyGamesLifecycle.ts

# Game screens to integrate
@components/game/InGameScreen.tsx
@components/views/ResultsPage.tsx
@components/multiplayer/MultiplayerFlow.tsx
@components/singleplayer/game/hooks/useSinglePlayerCore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance useCrazyGamesLifecycle hook</name>
  <files>hooks/useCrazyGamesLifecycle.ts</files>
  <action>
Enhance the existing useCrazyGamesLifecycle hook with additional tracking features:

1. Review existing implementation and extend if needed:
   - Verify it already tracks gameplay state
   - Add isPlaying state to prevent double start/stop calls
   - Add happytime trigger function

2. Add gameplay state tracking:
```typescript
interface CrazyGamesLifecycleState {
  isPlaying: boolean;
  lastStartTime: number | null;
  totalPlayTime: number;
}
```

3. Ensure idempotent operations:
   - startGameplay() is no-op if already playing
   - stopGameplay() is no-op if already stopped
   - Log warnings in dev mode for debugging

4. Add happytime() trigger:
   - Export function to call SDK happytime on achievements
   - Use for: high scores, boss defeats, completing adventure levels
   - Throttle to max once per 30 seconds (CrazyGames recommendation)

5. Add automatic cleanup:
   - stopGameplay on tab visibility change (hidden)
   - Resume on visibility change (visible)
   - Stop on component unmount

Keep the hook lightweight - just lifecycle events, no game logic.
  </action>
  <verify>
npm run test -- hooks/__tests__/useCrazyGamesLifecycle.test.ts
npm run lint -- hooks/useCrazyGamesLifecycle.ts
npm run build
  </verify>
  <done>
useCrazyGamesLifecycle hook extended with isPlaying tracking, happytime throttling, visibility change handling
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate lifecycle events in game screens</name>
  <files>
    components/game/InGameScreen.tsx
    components/views/ResultsPage.tsx
    components/multiplayer/MultiplayerFlow.tsx
    components/singleplayer/game/hooks/useSinglePlayerCore.ts
  </files>
  <action>
Wire up gameplay lifecycle events to the appropriate game screens:

1. InGameScreen.tsx (multiplayer in-game):
   - Call startGameplay() when round timer starts
   - Call stopGameplay() when round ends
   - Ensure cleanup on unmount

2. ResultsPage.tsx:
   - Call stopGameplay() on mount (results = not playing)
   - Call happytime() if player achieved high score or won

3. MultiplayerFlow.tsx:
   - stopGameplay() when in lobby (waiting for players)
   - startGameplay() delegated to InGameScreen
   - stopGameplay() on game end

4. useSinglePlayerCore.ts (singleplayer/adventure):
   - startGameplay() when timer starts
   - stopGameplay() when round/level ends
   - happytime() on adventure level completion or high score

Integration pattern:
```typescript
const { startGameplay, stopGameplay, happytime } = useCrazyGamesLifecycle();

useEffect(() => {
  if (gameState === 'playing') {
    startGameplay();
  } else {
    stopGameplay();
  }
}, [gameState, startGameplay, stopGameplay]);
```

Do NOT add lifecycle calls to menu screens, settings, etc. - only actual gameplay.
  </action>
  <verify>
npm run lint -- components/game/InGameScreen.tsx components/views/ResultsPage.tsx components/multiplayer/MultiplayerFlow.tsx components/singleplayer/game/hooks/useSinglePlayerCore.ts
npm run build
  </verify>
  <done>
Gameplay lifecycle events integrated in InGameScreen, ResultsPage, MultiplayerFlow, and useSinglePlayerCore
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. `npm run lint` passes for all modified files
3. Test lifecycle events:
   a. Run `npm run dev`
   b. Open browser DevTools console
   c. Start a game - should see lifecycle log (if dev logging enabled)
   d. End game - should see stop event
   e. Verify no double-start or double-stop calls
4. Test with CrazyGames SDK (when NEXT_PUBLIC_CRAZYGAMES_ENABLED=true):
   a. Lifecycle events should call SDK methods
   b. Verify in CrazyGames QA tool if available
</verification>

<success_criteria>
1. gameplayStart() called exactly once when gameplay begins
2. gameplayStop() called exactly once when gameplay ends
3. No duplicate or missing lifecycle calls
4. happytime() called on major achievements (throttled)
5. Visibility change handling works (pause game = stop gameplay)
</success_criteria>

<output>
After completion, create `.planning/phases/24-crazygames-portal-integration/24-03-SUMMARY.md`
</output>
