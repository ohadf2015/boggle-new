---
phase: 06-ai-asset-generation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - raw/adventure/lexi-idle-1.png
  - raw/adventure/lexi-idle-2.png
  - raw/adventure/lexi-celebrate-1.png
  - raw/adventure/lexi-celebrate-2.png
  - raw/adventure/lexi-sad-1.png
  - raw/adventure/lexi-sad-2.png
  - raw/adventure/lexi-hint-1.png
  - raw/adventure/lexi-hint-2.png
autonomous: false

must_haves:
  truths:
    - "Lexi appears consistent across all 8 sprites (same character design)"
    - "Each emotional state is clearly distinguishable"
    - "Sprites work as 2-frame animation pairs"
    - "Character matches existing Lexi mascot aesthetic"
  artifacts:
    - path: "raw/adventure/lexi-idle-1.png"
      provides: "Lexi idle animation frame 1"
    - path: "raw/adventure/lexi-idle-2.png"
      provides: "Lexi idle animation frame 2"
    - path: "raw/adventure/lexi-celebrate-1.png"
      provides: "Lexi celebration animation frame 1"
    - path: "raw/adventure/lexi-celebrate-2.png"
      provides: "Lexi celebration animation frame 2"
    - path: "raw/adventure/lexi-sad-1.png"
      provides: "Lexi sad animation frame 1"
    - path: "raw/adventure/lexi-sad-2.png"
      provides: "Lexi sad animation frame 2"
    - path: "raw/adventure/lexi-hint-1.png"
      provides: "Lexi hint animation frame 1"
    - path: "raw/adventure/lexi-hint-2.png"
      provides: "Lexi hint animation frame 2"
  key_links:
    - from: "raw/adventure/lexi-*.png"
      to: "scripts/adventure-assets.json"
      via: "manifest input paths with removeBg: true"
      pattern: "lexi.*\\.png"
    - from: "public/images/adventure/sprites/lexi-*.webp"
      to: "components/adventure/LexiReaction.tsx"
      via: "sprite path references"
      pattern: "lexi-idle|lexi-celebrate|lexi-sad|lexi-hint"
---

<objective>
Generate Lexi mascot sprite sheets for adventure mode with 4 emotional states: idle, celebrate, sad, and hint.

Purpose: Provide animated Lexi reactions that match the existing mascot aesthetic. These sprites power the LexiReaction component from Phase 5, giving players visual feedback during gameplay.

Output: 8 raw PNG images (4 states x 2 frames each) ready for background removal and optimization
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-asset-generation/06-01-SUMMARY.md
@.planning/phases/06-ai-asset-generation/06-RESEARCH.md
@docs/asset-generation-prompts.md

# Existing Lexi reference (character consistency)
@public/mascot/main-nobg.gif
@components/adventure/LexiReaction.tsx
@components/ui/InteractiveMascot.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze existing Lexi mascot for consistency</name>
  <files>N/A (analysis only)</files>
  <action>
Review existing Lexi mascot assets in public/mascot/ to establish character reference:

1. Examine main-nobg.gif, celebration-nobg.gif, study-nobg.gif, oops-nobg.gif
2. Note key character traits:
   - Owl character (species)
   - Color palette (body, eyes, accessories)
   - Style (cartoon, proportions, line weight)
   - Expression range (existing states to match)

Document observations for prompt construction. The goal is to generate NEW sprites that feel like the same character, suitable for adventure mode.

Key characteristics to preserve:
- Owl body shape and proportions
- Eye style and expression range
- Overall cartoon aesthetic
- Recognizable as "Lexi" to existing players
  </action>
  <verify>
```bash
ls -la public/mascot/*-nobg.gif
```
Existing mascot files examined and character traits identified.
  </verify>
  <done>Lexi character reference analyzed for consistency</done>
</task>

<task type="auto">
  <name>Task 2: Generate idle state sprites</name>
  <files>
    raw/adventure/lexi-idle-1.png
    raw/adventure/lexi-idle-2.png
  </files>
  <action>
Generate 2 frames for Lexi idle state using the `mcp__mcp-image__generate_image` MCP tool.

**Idle State Description:**
Lexi in a relaxed, neutral pose. Subtle breathing animation difference between frames.

**Frame 1 (lexi-idle-1.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, friendly expression, relaxed neutral pose, eyes open, slight smile, standing upright, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-idle-1"

**Frame 2 (lexi-idle-2.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, friendly expression, relaxed neutral pose, gentle head tilt, soft expression, relaxed stance, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-idle-2"

After generation, move files to raw/adventure/ directory:
```bash
mkdir -p raw/adventure
mv lexi-idle-1.png lexi-idle-2.png raw/adventure/
```
  </action>
  <verify>
```bash
ls -la raw/adventure/lexi-idle-*.png
```
Both idle frames exist as PNG files.
  </verify>
  <done>Lexi idle animation frames generated</done>
</task>

<task type="auto">
  <name>Task 3: Generate celebrate state sprites</name>
  <files>
    raw/adventure/lexi-celebrate-1.png
    raw/adventure/lexi-celebrate-2.png
  </files>
  <action>
Generate 2 frames for Lexi celebration state using the `mcp__mcp-image__generate_image` MCP tool.

**Celebrate State Description:**
Lexi showing joy and excitement. High energy poses.

**Frame 1 (lexi-celebrate-1.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, joyful excited expression, celebrating pose, wings spread wide up high, beaming smile, triumphant, wings raised in victory, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-celebrate-1"

**Frame 2 (lexi-celebrate-2.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, joyful excited expression, celebrating pose, slight jump in air, wings still raised, ecstatic expression, wings raised in victory, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-celebrate-2"

After generation, move files to raw/adventure/ directory:
```bash
mv lexi-celebrate-1.png lexi-celebrate-2.png raw/adventure/
```

This state is used when player achieves milestones (long words, combos, level complete).
  </action>
  <verify>
```bash
ls -la raw/adventure/lexi-celebrate-*.png
```
Both celebrate frames exist as PNG files.
  </verify>
  <done>Lexi celebration animation frames generated</done>
</task>

<task type="auto">
  <name>Task 4: Generate sad state sprites</name>
  <files>
    raw/adventure/lexi-sad-1.png
    raw/adventure/lexi-sad-2.png
  </files>
  <action>
Generate 2 frames for Lexi sad/disappointed state using the `mcp__mcp-image__generate_image` MCP tool.

**Sad State Description:**
Lexi showing mild disappointment or concern (NOT crying or distressed, just thoughtful/sad).

**Frame 1 (lexi-sad-1.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, slightly sad concerned expression, drooped posture, looking slightly down, gentle frown, shoulders drooped, wings down at sides, clean simple design, bold outlines, muted but still colorful, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-sad-1"

**Frame 2 (lexi-sad-2.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, slightly sad concerned expression, drooped posture, looking to side, contemplative, subtle head tilt down, wings down at sides, clean simple design, bold outlines, muted but still colorful, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-sad-2"

After generation, move files to raw/adventure/ directory:
```bash
mv lexi-sad-1.png lexi-sad-2.png raw/adventure/
```

This state is used when player is struggling or makes mistakes. Should be sympathetic, not punishing.
  </action>
  <verify>
```bash
ls -la raw/adventure/lexi-sad-*.png
```
Both sad frames exist as PNG files.
  </verify>
  <done>Lexi sad animation frames generated</done>
</task>

<task type="auto">
  <name>Task 5: Generate hint state sprites</name>
  <files>
    raw/adventure/lexi-hint-1.png
    raw/adventure/lexi-hint-2.png
  </files>
  <action>
Generate 2 frames for Lexi hint/helpful state using the `mcp__mcp-image__generate_image` MCP tool.

**Hint State Description:**
Lexi being helpful and encouraging, like giving advice.

**Frame 1 (lexi-hint-1.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, helpful encouraging expression, friendly gesture, one wing pointing forward like giving a tip, knowing smile, helpful posture, one wing raised pointing or waving, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-hint-1"

**Frame 2 (lexi-hint-2.png):**
Call `mcp__mcp-image__generate_image` with:
- prompt: "stylized cartoon owl mascot, helpful encouraging expression, friendly gesture, both wings in welcoming gesture, warm encouraging expression, one wing raised pointing or waving, clean simple design, bold outlines, vibrant colors, game character sprite, white/transparent background, full body, centered composition"
- aspectRatio: "1:1"
- fileName: "lexi-hint-2"

After generation, move files to raw/adventure/ directory:
```bash
mv lexi-hint-1.png lexi-hint-2.png raw/adventure/
```

This state is used when giving hints or encouraging stuck players.
  </action>
  <verify>
```bash
ls -la raw/adventure/lexi-hint-*.png
```
Both hint frames exist as PNG files.
  </verify>
  <done>Lexi hint animation frames generated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>8 Lexi mascot sprites across 4 emotional states</what-built>
  <how-to-verify>
1. Open all 8 sprites in raw/adventure/lexi-*.png
2. Verify CHARACTER CONSISTENCY:
   - All 8 sprites look like the same owl character
   - Body proportions are consistent
   - Color palette is consistent
   - Style is consistent (same cartoon aesthetic)
3. Verify EMOTIONAL CLARITY:
   - Idle: neutral, relaxed
   - Celebrate: joyful, excited
   - Sad: concerned, sympathetic (not distressed)
   - Hint: helpful, encouraging
4. Verify ANIMATION PAIRING:
   - Frame 1 and 2 of each state work as animation pair
   - Difference is subtle enough for smooth loop
5. Compare to existing mascots in public/mascot/
   - New sprites should feel like same character family
  </how-to-verify>
  <resume-signal>Type "approved" if Lexi sprites look consistent and expressive, or describe which states need regeneration</resume-signal>
</task>

</tasks>

<verification>
1. All 8 sprite images exist:
```bash
ls raw/adventure/lexi-*.png | wc -l  # Should be 8
```
2. Character is consistent across all sprites
3. Each emotional state is clearly distinguishable
4. Animation pairs work together
5. User approved visual quality and consistency
</verification>

<success_criteria>
- 8 Lexi sprites generated (4 states x 2 frames)
- Character consistency maintained across all sprites
- Emotional states clearly distinguishable
- Sprites suitable for 2-frame animation loops
- Style matches existing Lexi mascot aesthetic
- User approved quality
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-asset-generation/06-03-SUMMARY.md`
</output>
