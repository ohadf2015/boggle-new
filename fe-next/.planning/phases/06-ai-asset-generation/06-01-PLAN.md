---
phase: 06-ai-asset-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/adventure/backgrounds/.gitkeep
  - public/images/adventure/parallax/.gitkeep
  - public/images/adventure/sprites/.gitkeep
  - scripts/adventure-assets.json
  - docs/asset-generation-prompts.md
autonomous: true

must_haves:
  truths:
    - "Directory structure exists for all adventure asset types"
    - "Asset manifest defines all required assets with processing options"
    - "Prompt templates documented for reproducible generation"
  artifacts:
    - path: "public/images/adventure/backgrounds"
      provides: "Directory for world background images"
    - path: "public/images/adventure/parallax"
      provides: "Directory for parallax layer images"
    - path: "public/images/adventure/sprites"
      provides: "Directory for Lexi sprite images"
    - path: "scripts/adventure-assets.json"
      provides: "Asset pipeline manifest"
      contains: "baseOutputDir"
    - path: "docs/asset-generation-prompts.md"
      provides: "AI prompt templates for consistent regeneration"
      min_lines: 50
  key_links:
    - from: "scripts/adventure-assets.json"
      to: "scripts/asset-pipeline.ts"
      via: "manifest format compatibility"
      pattern: "baseOutputDir.*assets"
---

<objective>
Set up asset infrastructure for Phase 6: directory structure, asset manifest for pipeline processing, and documented prompt templates for AI image generation.

Purpose: Establish the foundation for organized asset generation and processing. The manifest enables batch processing via existing asset-pipeline.ts, and prompt templates ensure reproducible, consistent AI generations.

Output: Directory structure, pipeline manifest, prompt documentation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-asset-generation/06-RESEARCH.md
@.planning/phases/06-ai-asset-generation/06-CONTEXT.md

@scripts/asset-pipeline.ts
@scripts/remove-background.py
@scripts/optimize-image.ts
@lib/adventure/themes/world1.ts
@lib/adventure/themes/world2.ts
@lib/adventure/themes/world3.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create asset directory structure</name>
  <files>
    public/images/adventure/backgrounds/.gitkeep
    public/images/adventure/parallax/.gitkeep
    public/images/adventure/sprites/.gitkeep
  </files>
  <action>
Create the directory structure defined in 06-RESEARCH.md:

```
public/images/adventure/
├── backgrounds/           # Full-scene backgrounds (1024x1024)
│   └── .gitkeep
├── parallax/              # Parallax layer assets (1024x512)
│   └── .gitkeep
└── sprites/               # Lexi mascot sprites
    └── .gitkeep
```

Note: Tiles directory NOT needed - ThemedTile.tsx uses CSS overlays (sparkle, frost, flames) which are already implemented. No tile images required.

Use mkdir -p and touch for .gitkeep files.
  </action>
  <verify>
```bash
ls -la public/images/adventure/backgrounds/
ls -la public/images/adventure/parallax/
ls -la public/images/adventure/sprites/
```
All three directories exist with .gitkeep files.
  </verify>
  <done>Directory structure created matching research specification</done>
</task>

<task type="auto">
  <name>Task 2: Create asset pipeline manifest</name>
  <files>scripts/adventure-assets.json</files>
  <action>
Create a manifest file for batch processing all adventure assets via asset-pipeline.ts.

The manifest should define all assets from 06-RESEARCH.md:

**World Backgrounds (3 total):**
- meadows.webp (World 1)
- springs.webp (World 2)
- caverns.webp (World 3)

**Parallax Layers (8 total):**
- meadows-hills.webp, meadows-grass.webp (World 1: 2 layers)
- springs-waterfall.webp, springs-mist.webp, springs-rocks.webp (World 2: 3 layers)
- caverns-crystals-far.webp, caverns-stalactites.webp, caverns-crystals-near.webp (World 3: 3 layers)

**Lexi Sprites (8 total - 2 frames per state):**
- lexi-idle-1.webp, lexi-idle-2.webp
- lexi-celebrate-1.webp, lexi-celebrate-2.webp
- lexi-sad-1.webp, lexi-sad-2.webp
- lexi-hint-1.webp, lexi-hint-2.webp

Manifest format (compatible with asset-pipeline.ts):
```json
{
  "baseInputDir": "raw/adventure",
  "baseOutputDir": "public/images/adventure",
  "defaultRemoveBg": false,
  "defaultTargetKb": 200,
  "assets": [
    { "input": "meadows-bg.png", "output": "backgrounds/meadows", "removeBg": false },
    ...
  ]
}
```

Set removeBg: false for backgrounds (full scenes).
Set removeBg: true for Lexi sprites (character extraction).
Set targetKb: 150 for parallax layers (smaller budget per layer).
  </action>
  <verify>
```bash
cat scripts/adventure-assets.json | head -50
node -e "const m = require('./scripts/adventure-assets.json'); console.log('Assets:', m.assets.length)"
```
Manifest is valid JSON with 19 assets (3 backgrounds + 8 parallax + 8 sprites).
  </verify>
  <done>Asset manifest created with all 19 adventure assets defined</done>
</task>

<task type="auto">
  <name>Task 3: Document AI generation prompts</name>
  <files>docs/asset-generation-prompts.md</files>
  <action>
Create comprehensive prompt documentation for reproducible AI image generation.

Document prompts for each asset category based on 06-CONTEXT.md decisions:
- Art style: Stylized cartoon (Cuphead/Hollow Knight energy), bold outlines, saturated colors
- Use natural language (no hex codes or technical terms in prompts)
- Document exact Midjourney-style prompt structure for consistency

**World Background Prompts:**
Include prompts for each world with thematic elements:
- Meadows: sunny, flowers, grass, butterflies, pastoral
- Springs: waterfalls, streams, mist, cool blues, serene
- Caverns: crystals, underground, purple glow, mysterious

**Parallax Layer Prompts:**
Note that layers must maintain visual cohesion (same lighting, color temp).
Include depth guidance (distant = desaturated, foreground = vibrant).

**Lexi Sprite Prompts:**
- Reference existing mascot (public/mascot/main-nobg.gif) for character consistency
- Document 4 states: idle (relaxed), celebrate (arms up, joyful), sad (drooped, concerned), hint (helpful, pointing)
- Note: Use --cref equivalent in MCP tool if available for character consistency

**Post-Processing Notes:**
- All images run through rembg for sprites
- All images run through Sharp WebP optimization
- Target sizes: backgrounds <200KB, parallax <150KB, sprites <100KB

Include example prompts that worked well and anti-patterns to avoid.
  </action>
  <verify>
```bash
wc -l docs/asset-generation-prompts.md
grep -c "World 1\|World 2\|World 3\|Lexi" docs/asset-generation-prompts.md
```
File has 50+ lines and covers all 4 asset categories.
  </verify>
  <done>Prompt templates documented for all asset types with style guidelines and anti-patterns</done>
</task>

</tasks>

<verification>
1. Directory structure exists:
   - `ls public/images/adventure/` shows backgrounds/, parallax/, sprites/
2. Manifest is valid:
   - `node -e "require('./scripts/adventure-assets.json')"` runs without error
3. Prompts documented:
   - `docs/asset-generation-prompts.md` exists with 50+ lines
</verification>

<success_criteria>
- Asset directories created (backgrounds, parallax, sprites)
- Pipeline manifest defines all 19 assets with correct processing options
- Prompt documentation enables reproducible AI generation
- No tile assets needed (CSS overlays already implemented)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-asset-generation/06-01-SUMMARY.md`
</output>
