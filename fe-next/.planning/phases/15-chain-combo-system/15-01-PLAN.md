---
phase: 15-chain-combo-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - hooks/__tests__/useAdventureGame.chainCombo.test.ts
  - hooks/useAdventureGame.ts
autonomous: true

must_haves:
  truths:
    - "Chain tile applies 1.5x combo multiplier when used during active combo"
    - "Chain tile marks adjacent tiles as chained for visual feedback"
    - "Chain tile works correctly with other special tiles (gold 3x stacks with chain 1.5x)"
  artifacts:
    - path: "hooks/__tests__/useAdventureGame.chainCombo.test.ts"
      provides: "Chain tile combo logic tests"
      min_lines: 150
    - path: "hooks/useAdventureGame.ts"
      provides: "Enhanced chain tile activation logic"
      contains: "CHAIN_COMBO_MULTIPLIER"
  key_links:
    - from: "hooks/useAdventureGame.ts"
      to: "types/adventure.ts"
      via: "TileState.isChained property"
      pattern: "isChained.*=.*true"
---

<objective>
Activate and test chain tile combo logic in useAdventureGame hook.

Purpose: Chain tiles are ALREADY STUBBED (lines 354-447) but need full TDD test coverage and verification. This plan activates the existing chain tile infrastructure with comprehensive tests proving the 1.5x multiplier works correctly during combos.

Output: Tested chain tile activation with 80%+ coverage, verified integration with gold/ice/bomb tiles.
</objective>

<execution_context>
@/Users/ohadfisher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohadfisher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chain-combo-system/15-RESEARCH.md

# Existing implementation reference
@hooks/useAdventureGame.ts (lines 354-447 - chain tile stub)
@hooks/__tests__/useAdventureGame.specialTiles.test.ts (test patterns)
@types/adventure.ts (TileType, TileState definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive chain tile tests (RED phase)</name>
  <files>hooks/__tests__/useAdventureGame.chainCombo.test.ts</files>
  <action>
Create new test file following existing test patterns from useAdventureGame.specialTiles.test.ts.

Test cases to include (all should FAIL initially):

1. **Chain tile 1.5x multiplier tests:**
   - Should apply 1.5x multiplier when chain tile used during active combo (comboCount > 0)
   - Should NOT apply multiplier when chain tile used without active combo (comboCount = 0)
   - Should apply enhanced bonus: finalScore * (1 + comboCount * 0.1 * 1.5)

2. **Chain tile adjacent linking tests:**
   - Should mark all 8 adjacent tiles as isChained = true
   - Should NOT mark tiles beyond adjacency range
   - Should handle edge cases (chain tile at grid corners/edges)

3. **Chain tile activation effect tests:**
   - Should set activationEffect = 'link' on chain tiles
   - Should set activationTimestamp for animation timing

4. **Chain + other special tile interaction tests:**
   - Chain + Gold: Both multipliers should stack (gold 3x * chain bonus)
   - Chain + Ice: Chain adjacent linking should unfreeze ice tiles
   - Chain + Bomb: Chain bonus applies before bomb row clear

Use createMockLevelConfig and createMockGrid fixtures from specialTiles.test.ts pattern.
Include beforeEach/afterEach with jest.useFakeTimers() for combo timing tests.
  </action>
  <verify>npm run test hooks/__tests__/useAdventureGame.chainCombo.test.ts -- --passWithNoTests (should create file, tests will fail)</verify>
  <done>Test file exists with 10+ test cases covering chain tile logic, all tests initially fail (RED phase complete)</done>
</task>

<task type="auto">
  <name>Task 2: Verify and enhance chain tile implementation (GREEN phase)</name>
  <files>hooks/useAdventureGame.ts</files>
  <action>
Review existing chain tile code at lines 354-447 and ensure tests pass:

1. **Verify CHAIN_COMBO_MULTIPLIER constant** exists (should be 1.5)

2. **Verify combo bonus calculation** (lines 358-362):
   ```typescript
   if (chainPositions.length > 0 && state.gameState.comboCount > 0) {
     const comboBonus = state.gameState.comboCount * 0.1 * CHAIN_COMBO_MULTIPLIER;
     finalScore = Math.floor(finalScore * (1 + comboBonus));
   }
   ```

3. **Verify activation effect** (lines 363-370):
   - Sets activationEffect = 'link'
   - Sets activationTimestamp

4. **Verify adjacent tile chaining** (lines 432-446):
   - 8-directional neighbor marking
   - Bounds checking for grid edges

5. **Fix any issues** to make tests pass. Common issues:
   - comboCount check might be off-by-one
   - Missing structural sharing optimization for chained tiles
   - Edge case handling for corner chain tiles

Run tests after each fix to verify GREEN phase progress.
  </action>
  <verify>npm run test hooks/__tests__/useAdventureGame.chainCombo.test.ts (all tests pass)</verify>
  <done>All chain tile tests pass, existing functionality verified and enhanced</done>
</task>

<task type="auto">
  <name>Task 3: Refactor and verify integration (REFACTOR phase)</name>
  <files>hooks/useAdventureGame.ts, hooks/__tests__/useAdventureGame.chainCombo.test.ts</files>
  <action>
1. **Run full special tiles test suite** to ensure no regressions:
   ```bash
   npm run test hooks/__tests__/useAdventureGame.specialTiles.test.ts
   npm run test hooks/__tests__/useAdventureGame.activationEffects.test.ts
   ```

2. **Review structural sharing optimization** (lines 282-307):
   - Ensure chain tile updates use the same rowsToClone pattern
   - Add chain tile neighbors to rowsToClone set if modifying adjacent tiles

3. **Clean up any code duplication** between chain tile logic and other special tiles

4. **Add JSDoc comments** if missing on chain-related functions

5. **Verify build passes:**
   ```bash
   npm run build
   ```
  </action>
  <verify>npm run test:backend && npm run build (all tests pass, build succeeds)</verify>
  <done>Chain tile logic refactored, no regressions in existing special tile tests, build passes</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Chain tile tests: `npm run test hooks/__tests__/useAdventureGame.chainCombo.test.ts` - ALL PASS
2. Existing tile tests: `npm run test hooks/__tests__/useAdventureGame.specialTiles.test.ts` - NO REGRESSIONS
3. Build: `npm run build` - SUCCESS
4. Lint: `npm run lint hooks/useAdventureGame.ts` - NO ERRORS
</verification>

<success_criteria>
- 10+ chain tile tests covering multiplier, adjacent linking, and special tile interactions
- All chain tile tests pass (100%)
- No regressions in existing special tile tests
- Build and lint pass
- Chain tile applies 1.5x combo multiplier verified by tests
- Adjacent tile marking (isChained = true) verified by tests
</success_criteria>

<output>
After completion, create `.planning/phases/15-chain-combo-system/15-01-SUMMARY.md`
</output>
