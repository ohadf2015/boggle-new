
### Lane 2 — Engagement A/B + flag hygiene
- Flags retired: none — no decided winner (p<0.05, n≥1000/arm) surfaced from the brief; per playbook, determining significance needs a per-flag PostHog experiment-results query, out of scope for this lane's time budget.
- New experiment: none shipped this run. Investigated wiring `exp-game-abandon-confirm-v1` (found genuinely unwired — pure `resolveQuitConfirmDescription()` helper has zero non-test callers) but the quit-confirm dialog render site wasn't located inside the time budget (prop-drills through `SinglePlayerGame.tsx` → `SinglePlayerShell.tsx`, not yet traced to the actual `ConfirmationDialog` JSX). Logged as a ready-to-pick-up task in `docs/nightly/triage-queue.md` with the exact trace so the next lane can finish it in one pass instead of re-discovering it.
- PostHog flags ensured live: skipped — no new experiment shipped this run, so nothing new to ensure. `exp-game-abandon-confirm-v1` flag already exists (id 209541, active, rollout 100) and needs no `ensure` once wired.
- New events instrumented: `word_craft_submit_blocked_dict_loading`, `word_craft_submit_blocked_blank_unassigned`, `word_craft_dict_retry_clicked` — targets the brief's top signal (rage clicks on `/en/word-craft`, score 0.667). Traced the submit path first and confirmed the UI already disables the board correctly during dict-load/unassigned-blank; these events test whether a race (keyboard/enter bypassing the disabled state) is the actual rage-click cause before any UI fix is attempted.
- Largest funnel drop targeted: not re-queried (brief's rage-click slice was fresh, not stale) — targeted the brief's #1 ranked signal directly per brief-first contract instead of re-running the homepage→game-start→game-complete→return-day-2 funnel.
