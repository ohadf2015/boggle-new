#### Mode readiness verdict
- Mode: wheel-rush (Wheel Rush)
- Readiness: 77% (was 76%)
- URL: https://www.lexiclash.live/en/multiplayer (no standalone route — reached via live MP match, weighted-random mode rotation)
- Visual QA: not captured this run — assessment is code-audit only (structural blocker: no route/flag-override for this mode, needs a live 2-tab MP session with dedicated budget)
- Blockers remaining: visual QA (structural, no route), disconnect/refresh-mid-round live verification, NEW MAJOR (logged not fixed) — fallback stats path silently zeroes First-Finds/Bandit award data
- Fixed this run: none — 1 area verified clean (desktop/TV adapter), 1 new MAJOR bug found and logged (too risky to rush-fix under time budget given 3 prior incidents on this exact field)
- Verdict: needs more nights — plateaued on visual QA blocker, but still finding+documenting real bugs in unaudited areas each night

### Lane — Mode QA (wheel-rush)
- **Desktop/TV adapter (`components/multiplayer/desktop/WheelRushDesktopAdapter.tsx`) — verified clean.** Fog meter clamped, all strings via `t()` (spot-checked 4 keys in en+he), composed via shared `MultiplayerDesktopShell` slot system matching the released Blast desktop adapter pattern, no perf red flags.
- **MAJOR (not fixed) — `lib/results/wheelRushStatsFallback.ts:19-36`.** `deriveWheelRushStatsFromScores` hardcodes `wordsStolen: 0`/`wordsStolenFromMe: 0` for every player when the server's real `wheelRushSummary.playerStats` payload is lost (server scoring threw, or a late join missed the cache). This resolved/fallback record is what `WheelRushDomination.tsx`/`WheelRushResultsScene.tsx` actually render (`ResultsPage.tsx:495` `effectiveWheelRushStats`), so on this failure path every player sees a confident "Steal Rate: 0% / First Finds: 0" and the Bandit/Trailblazer award silently never fires — even though a real completed match's true first-finder sum across players is almost never 0. Root cause: the fallback only has `ScoreLike[]` (username/score/allWords) to work with, no `firstFinders` data, so it can't reconstruct the real count. Correct fix needs either threading `firstFinders` to the client or an `isFallback` flag to conditionally hide the steal-derived UI — deferred to a dedicated night: this exact field has caused 3 prior incorrect edits this month (08-08 misdiagnosis wave, 08-26 regression+revert), so a rushed multi-file threading change under the finalize-cutoff budget is a worse bet than the bug itself. Logged in ledger, owner: review-by-eod.
