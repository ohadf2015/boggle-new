# Nightly Run — 2026-06-21

### Lane 02 — Performance
- Backend fixes: none (Supabase MCP deferred; time budget allocated to confirmed frontend INP regression)
- Frontend fixes:
  - `components/lobby/LobbyDailyEmber.tsx` — 2 INP improvements for `/es/multiplayer` (n=59, 328ms, first above floor):
    1. Analytics (`posthog.capture`) moved from `setState` updater → `useEffect` (fires post-paint, off interaction→paint critical path)
    2. Popover changed from `{open && <div>}` conditional render → `hidden={!open}` attribute (eliminates DOM mount/unmount on click, avoids React reconciliation in the interaction frame)
- Top findings:
  - `/he` LCP **RESOLVED** — 6273ms→3464ms (7d, n=70). Off watch list.
  - `/he` INP **worsening** — 184→264→336ms (3 readings, all n≥70). Root cause unknown; NOT addressed this run (LobbyDailyEmber is MP-only). Candidates: ModeCoach bundle, AnimatePresence overhead on homepage.
  - `/es/multiplayer` INP **328ms, n=59** — first above n≥50 floor. Fix applied above; verify next 7d reading.
- Baseline file updated: docs/nightly/perf-baseline.json
- Deferred to human (perf-watch.md): 0 new items (supabase security migration 20260619030000 already tracked)
