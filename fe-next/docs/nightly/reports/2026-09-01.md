
#### Mode readiness verdict
- Mode: wheel-rush (Wheel Rush)
- Readiness: 79% (was 77%)
- URL: https://www.lexiclash.live/en/wheel-rush?wheel-rush=1 (structural note: this mode has no standalone route — the URL 404s, it only renders inside a live MP match; carried from prior nights)
- Visual QA: not captured this run — assessment is code-audit only
- Blockers remaining: live 2-tab MP visual QA (structural, needs dedicated ~2-3min-real-time budget) · disconnect/refresh-mid-round live verification (code path verified clean by read, never exercised live) · orphaned `wheelRush.results.stealRate`/`.steals` i18n keys (6 locales, cosmetic, hand to clean-translations)
- Fixed this run: deleted orphaned `components/results/WheelRushDomination.tsx` + its test — component was superseded by `WheelRushResultsScene.tsx` on 2026-08-26 and had zero production importers, but still carried a latent version of last night's "confident 0% shown to every player" bug
- Verdict: needs 2-3 more nights (structural visual-QA blocker + live-verification items remain; code audit surface is narrowing — most remaining code paths this mode touches have now been read at least once)

### Lane — Mode QA (wheel-rush)
- **Corrected an overstated finding from 2026-08-31.** Last night's report logged a "NEW MAJOR" bug: `lib/results/wheelRushStatsFallback.ts` hardcodes `wordsStolen: 0`/`wordsStolenFromMe: 0` in its fallback path, and the finding claimed this makes "every player" see a confident "Steal Rate: 0% / First Finds: 0" via both `WheelRushDomination.tsx` and `WheelRushResultsScene.tsx`.
- **Root cause of the overstatement:** `WheelRushDomination.tsx` has had **zero production importers since 2026-08-26**, when `WheelRushResultsScene.tsx` replaced it as the sole live wheel-rush results renderer (confirmed via a whole-repo grep excluding the component's own file — only its own test file references it; `WheelRushResultsScene.tsx`'s own top-of-file comment documents the replacement). The 08-31 audit named the right field but the wrong (dead) consumer.
- **Re-verified the actually-live component (`WheelRushResultsScene.tsx`):** `recap.totalSteals`/`recap.stealRate` are computed but never rendered (only `recap.totalLocks` reaches the stats-row UI, per 08-26's own collapse). The one live consumer of `wordsStolen` is the Bandit/Trailblazer award, already guarded on `> 0` — on a fallback-fired match it just doesn't show an award. That's a genuinely MINOR degraded-but-honest state (no wrong number is ever shown to a real player today), not the MAJOR "false info" bug as logged.
- **Fixed:** deleted `components/results/WheelRushDomination.tsx` + `components/results/__tests__/WheelRushDomination.test.tsx` (`git rm`). Reversible, zero blast radius (confirmed no other file references it, including dynamic imports) — removes dead code that carried a latent copy of this exact bug, so it can't resurface if the component is ever accidentally re-wired.
- **Not touched:** the deeper fix some future night may still want — the fallback path genuinely can't reconstruct a real first-finder count from `ScoreLike[]` data (no per-word finder info), so `wordsStolen: 0` there is a placeholder either way. Since the one live surface degrades gracefully already, this is now a low-priority "nice to have precision" item, not a blocker.
- Visual QA: not captured this run (budget went to verifying the 08-31 claim + shipping the dead-code fix). Code-audit only.

### Lane 02 — Performance
- Backend fixes: added `idx_vocabulary_lessons_classroom_id` btree index (advisor-flagged unindexed FK `vocabulary_lessons_classroom_id_fkey`; confirmed via `pg_indexes` no covering index existed before applying). Checked `idx_word_pacts_player2_id` ("unused index" advisor item) — confirmed `idx_scan=0` but index is 16kB on a tiny table, not worth dropping, closed as low-value. `auth_db_connections_absolute` advisor item is a dashboard config toggle, not a migration — deferred to human queue.
- Frontend fixes: none shipped. Re-ran the brief's PostHog LCP items through the mandatory n>=50 sample gate grouped by route (stripping query params, filtered `$host LIKE lexiclash`) — every LexiClash route failed the floor tonight, including `/en/multiplayer` itself (n=45, just under, down from 08-31's n=59). The brief's top-scored items (tryshopli.com, stoquant.com, imposketch.io) are OTHER apps sharing this PostHog project, not LexiClash — correctly excluded. The `lexiclash.live/es/multiplayer?room=QF96CJ` brief item is a single invite-room URL — same per-query-param noise pattern flagged since 2026-08-07 (brief generator still needs to group by route template, not raw URL). Verified the MP lobby hero (`app/[locale]/multiplayer/page.tsx:66-81`) already carries a prior lane's `ReactDOM.preload`+srcset LCP fix — no further action needed there.
- Top regressions: none confirmed — all candidate routes below the 50-sample floor; no false "regression" logged per the anti-noise rule.
- Baseline file updated: docs/nightly/perf-baseline.json
- Deferred to human (perf-watch.md): 1 (`auth_db_connections_absolute` dashboard toggle)

#### Mode improvement shipped
- Mode: Lightning Round (admin/experimental brain drill, `/brain/drills/lightning-round`, noindex)
- URL: https://lexiclash.live/en/brain/drills/lightning-round/  (mirrors on /he, /sv, /ja, /es)
- Local URL: http://localhost:3001/en/brain/drills/lightning-round/
- Axis: feel
- What changed: countdown timer chip now pulses (motion-safe, honors prefers-reduced-motion) in the final 5s instead of only changing color — urgency is felt, not just read, especially on small phone screens.
- Files touched: fe-next/components/drills/LightningRound.tsx
- Next step for user: open URL or refresh home as admin, play 1 round to <5s left, reply 👍/👎 to the bot
- Note: rotation check showed last 3 mode-improvement nights hit Word Craft / Sealed Bid / Connections — Lightning Round's own combo/streak variable-reward layer was already shipped previously (comment in file), so this pass deliberately used a different axis (feel) rather than re-touching reward. Other brain-drill siblings (rare-gems, combo-master, pattern-switcher, memory-hunt) still due for a pass.

### Lane 2 — Engagement A/B + flag hygiene
- Flags retired: none — brief provided no decided-winner (p<0.05, n>=1000/arm) data this run; skipping retirement to avoid guessing.
- New experiment: none shipped — top rageclick target (`?room=` invite arrival, EYM2W3/mobile-lobby) is ALREADY covered by wired `exp-invite-arrival-clarity-v1` (control=blank overlay vs status-card+spinner) with `invite_redirect_fired`/`invite_consumed`/`invite_room_dead` already instrumented in `utils/growthTracking.ts`. Investigated a second candidate gap (`useSinglePlayerConfig.ts:171-179` — silent `router.replace` from `/singleplayer?autoStart=bots` to `/multiplayer?quickPlay=true` for returning players, same blank-redirect shape, ties to the `es/singleplayer?autoStart=bots` rageclick item) but ran out of safe time budget to implement+test it under strict TDD; leaving as next-run pickup rather than shipping a rushed/untested redirect-path change.
- PostHog flags ensured live: swept all 21 wired `exp-*` keys in `lib/experiments.ts` (grep-verified real non-test call sites first) via `posthog-experiment.sh ensure`. 20/21 already `exists`; 1 newly `created`: `exp-teacher-gate-redirect-clarity-v1` (id 263537) — was defined+wired but had no live PostHog flag, so it was serving control-only until now. `exp-singleplayer-word-goal-v1` remains genuinely UNWIRED (0 non-test call sites) — not flagged live, per hard precondition (don't create a flag for code that doesn't read it).
- New events instrumented: none this run (see above — deferred, not fabricated).
- Largest funnel drop targeted: `?room=` invite arrival rageclick (score 0.799, reach 5) — confirmed already mitigated by existing wired experiment + instrumentation; recommend an IMPACT CHECK on `invite_redirect_fired`→`invite_consumed` next run instead of new code.
