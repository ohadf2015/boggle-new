
### Lane 12 — Telemetry coverage health
- Registry events: 108 total (per docs/telemetry-audit-2026-06-23.md) · DEAD/never-wired backlog still large; raw-name classifier has known false-positives (fragments like `won`/`daily`/`desktop` are enum values, not events) — cross-checked manually against `growth:`-prefixed live volume instead.
- Newly-dead since last run: none confirmed. `notification_delivered` (15/7d, was 0) and `push_prompt_granted` (5/7d, was 1) have recovered off the dead list — do not re-flag.
- Impact check: `referral_link_clicked` (shipped 08-16) — verdict **neutral**, not regressed. 0 root `?ref=CODE` pageviews in 14d — genuine zero-exposure (nobody has clicked a real referral link yet), not a wiring break. Confirmed call site intact (`ReferralCodeClaimer` mounted globally in `DeferredLayoutWidgets`). Distinct from the unrelated `/friends?ref=<username>` invite-a-friend param, which does get traffic.
- Fixed this run: `results_autoplay_cancelled` (never-wired, §1b backlog) — `fe-next/components/singleplayer/SinglePlayerResults.tsx:392` (onCancel of `AutoPlayCountdown`). Sibling backlog item `results_cta_clicked` (wired-but-silent) traced: the quick-replay CTA only renders after this same cancel action AND behind the `exp-results-replay-cta-v1` flag — so its 0 volume is explained (rare compound gate), not a regression. Test added: `SinglePlayerResults.replayCTA.test.tsx` (results_autoplay_cancelled instrumentation).
- Also found (not fixed, handed to backlog): `room_joined_via_link`'s `trackGuestJoin()` helper exists in `growthTracking.ts:1007` but has zero callers — real guest-join-via-invite path is `MultiplayerFlow.tsx` `handleInvitationAutoJoin` (~line 238), which never calls it. Next lane-12 target.
- Backlog remaining (§1b): ~20+ items — next: `room_joined_via_link` (wired helper, needs its one missing call site).
