
## 2026-09-09 — Lane 03 engagement
- **es/singleplayer?autoStart=bots rage-click (score 0.878, reach 9, 24h)** — hypothesis: the returning-player gate in `useSinglePlayerConfig.ts` (~L202-210) renders the interactive `playing` board on initial state (grid=null) for one paint before `router.replace()` lands on `/multiplayer?quickPlay=true`. A returning bots player hits a frozen/empty board they can click before being yanked to a different mode — classic Class-3 asymmetric-path silent transition. Instrumented `sp_bots_mp_redirect_fired` (locale, entry) this run so tomorrow's PostHog query can confirm the redirect rate correlates with the rageclick reach before a UI fix (e.g. defer 'playing' phase until the redirect decision resolves) is attempted. NOT fixed tonight — needs the correlation data + browser verification, which didn't fit this lane's time budget.

## 2026-09-16 — Lane 03 engagement
- **/en/word-craft rage-click (score 0.667, reach 2, 24h)** — traced the board's interactive gate: submit IS correctly disabled while dict loads (`disabled={!canInteract || !dict}` in WordCraftGameScreen.tsx) and while a joker tile is unassigned, so the classic "clickable-looking but dead" pattern isn't present at the DOM level. Two silent failure paths existed though: submitMove() silently no-ops (SET_ERROR only, no telemetry) when blocked on dict-still-loading OR an unassigned blank — either could still explain repeated taps via a fast keyboard/enter path racing the disabled state. Instrumented `word_craft_submit_blocked_dict_loading`, `word_craft_submit_blocked_blank_unassigned`, `word_craft_dict_retry_clicked` this run so tomorrow's PostHog query can confirm/rule out the hypothesis before any UI change. NOT fixed tonight — reach=2 is too thin to justify a blind fix; needs the correlation data.
- **`exp-game-abandon-confirm-v1` confirmed UNWIRED (verified this run)** — `resolveQuitConfirmDescription()` in `lib/experiments/quitConfirmDescription.ts` is pure, tested, and correctly implements the stats-shown variant, but has ZERO non-test callers (`rg -n "resolveQuitConfirmDescription"` only hits its own test file). The quit-confirm dialog flow is `useSinglePlayerCore.ts:229` (useNavigationGuard message) → `showQuitConfirm` state → prop-drilled through `SinglePlayerGame.tsx` commonProps → `SinglePlayerShell.tsx` (`components/singleplayer/game/SinglePlayerShell.tsx`) which is where the actual dialog markup must live (not yet located this run — ran out of time budget mid-trace). Also missing: `singlePlayer.quitConfirmMessageWithStats` translation key does not exist in any locale yet. Recommended owner: next lane — (1) find the ConfirmationDialog render inside SinglePlayerShell, (2) wire `useExperiment('exp-game-abandon-confirm-v1')` + `resolveQuitConfirmDescription`, (3) add the 5-locale translation key, (4) the PostHog flag itself already exists (id unconfirmed, key present in `flags` list) so no `ensure` needed once wired.
