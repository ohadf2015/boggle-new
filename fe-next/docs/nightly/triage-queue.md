
## 2026-09-09 — Lane 03 engagement
- **es/singleplayer?autoStart=bots rage-click (score 0.878, reach 9, 24h)** — hypothesis: the returning-player gate in `useSinglePlayerConfig.ts` (~L202-210) renders the interactive `playing` board on initial state (grid=null) for one paint before `router.replace()` lands on `/multiplayer?quickPlay=true`. A returning bots player hits a frozen/empty board they can click before being yanked to a different mode — classic Class-3 asymmetric-path silent transition. Instrumented `sp_bots_mp_redirect_fired` (locale, entry) this run so tomorrow's PostHog query can confirm the redirect rate correlates with the rageclick reach before a UI fix (e.g. defer 'playing' phase until the redirect decision resolves) is attempted. NOT fixed tonight — needs the correlation data + browser verification, which didn't fit this lane's time budget.
