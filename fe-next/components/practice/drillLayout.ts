/** Active-drill root: fill a sized parent, never the page. */
export const DRILL_ROOT_CLASS = 'h-full min-h-0 flex flex-col overflow-hidden bg-neo-navy';

/** Scrollable inner list. Chrome around it stays put. */
export const DRILL_SCROLL_CLASS = 'flex-1 min-h-0 overflow-y-auto';
