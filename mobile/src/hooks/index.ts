// Export all custom hooks
export { useGameState } from './useGameState';
export { usePlayer } from './usePlayer';

// Re-export types
export type { Player, GameSettings, GameState, GameResult } from './useGameState';
export type { PlayerProfile, Achievement } from './usePlayer';
